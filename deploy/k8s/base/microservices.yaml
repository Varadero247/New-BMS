# Health & Safety Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-health-safety
  namespace: ims
  labels:
    app: api-health-safety
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-health-safety
  template:
    metadata:
      labels:
        app: api-health-safety
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4001'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-health-safety
          image: ims/api-health-safety:latest
          ports:
            - containerPort: 4001
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4001'
            - name: OTEL_SERVICE_NAME
              value: 'api-health-safety'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-health-safety
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4001
      targetPort: 4001
  selector:
    app: api-health-safety
---
# Environment Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-environment
  namespace: ims
  labels:
    app: api-environment
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-environment
  template:
    metadata:
      labels:
        app: api-environment
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4002'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-environment
          image: ims/api-environment:latest
          ports:
            - containerPort: 4002
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4002'
            - name: OTEL_SERVICE_NAME
              value: 'api-environment'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-environment
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4002
      targetPort: 4002
  selector:
    app: api-environment
---
# Quality Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-quality
  namespace: ims
  labels:
    app: api-quality
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-quality
  template:
    metadata:
      labels:
        app: api-quality
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4003'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-quality
          image: ims/api-quality:latest
          ports:
            - containerPort: 4003
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4003'
            - name: OTEL_SERVICE_NAME
              value: 'api-quality'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-quality
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4003
      targetPort: 4003
  selector:
    app: api-quality
---
# AI Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-ai
  namespace: ims
  labels:
    app: api-ai
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-ai
  template:
    metadata:
      labels:
        app: api-ai
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4004'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-ai
          image: ims/api-ai:latest
          ports:
            - containerPort: 4004
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4004'
            - name: OTEL_SERVICE_NAME
              value: 'api-ai'
          resources:
            requests:
              memory: '256Mi'
              cpu: '100m'
            limits:
              memory: '512Mi'
              cpu: '500m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-ai
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4004
      targetPort: 4004
  selector:
    app: api-ai
---
# Inventory Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-inventory
  namespace: ims
  labels:
    app: api-inventory
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-inventory
  template:
    metadata:
      labels:
        app: api-inventory
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4005'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-inventory
          image: ims/api-inventory:latest
          ports:
            - containerPort: 4005
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4005'
            - name: OTEL_SERVICE_NAME
              value: 'api-inventory'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4005
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-inventory
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4005
      targetPort: 4005
  selector:
    app: api-inventory
---
# HR Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-hr
  namespace: ims
  labels:
    app: api-hr
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-hr
  template:
    metadata:
      labels:
        app: api-hr
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4006'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-hr
          image: ims/api-hr:latest
          ports:
            - containerPort: 4006
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4006'
            - name: OTEL_SERVICE_NAME
              value: 'api-hr'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4006
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4006
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-hr
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4006
      targetPort: 4006
  selector:
    app: api-hr
---
# Payroll Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-payroll
  namespace: ims
  labels:
    app: api-payroll
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-payroll
  template:
    metadata:
      labels:
        app: api-payroll
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4007'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-payroll
          image: ims/api-payroll:latest
          ports:
            - containerPort: 4007
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4007'
            - name: OTEL_SERVICE_NAME
              value: 'api-payroll'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4007
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4007
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-payroll
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4007
      targetPort: 4007
  selector:
    app: api-payroll
---
# Workflows Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-workflows
  namespace: ims
  labels:
    app: api-workflows
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-workflows
  template:
    metadata:
      labels:
        app: api-workflows
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4008'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-workflows
          image: ims/api-workflows:latest
          ports:
            - containerPort: 4008
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4008'
            - name: OTEL_SERVICE_NAME
              value: 'api-workflows'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4008
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4008
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-workflows
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4008
      targetPort: 4008
  selector:
    app: api-workflows
---
# Project Management Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-project-management
  namespace: ims
  labels:
    app: api-project-management
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-project-management
  template:
    metadata:
      labels:
        app: api-project-management
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4009'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-project-management
          image: ims/api-project-management:latest
          ports:
            - containerPort: 4009
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4009'
            - name: OTEL_SERVICE_NAME
              value: 'api-project-management'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4009
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4009
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-project-management
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4009
      targetPort: 4009
  selector:
    app: api-project-management
---
# Automotive Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-automotive
  namespace: ims
  labels:
    app: api-automotive
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-automotive
  template:
    metadata:
      labels:
        app: api-automotive
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4010'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-automotive
          image: ims/api-automotive:latest
          ports:
            - containerPort: 4010
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4010'
            - name: OTEL_SERVICE_NAME
              value: 'api-automotive'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-automotive
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4010
      targetPort: 4010
  selector:
    app: api-automotive
---
# Medical Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-medical
  namespace: ims
  labels:
    app: api-medical
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-medical
  template:
    metadata:
      labels:
        app: api-medical
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4011'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-medical
          image: ims/api-medical:latest
          ports:
            - containerPort: 4011
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4011'
            - name: OTEL_SERVICE_NAME
              value: 'api-medical'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4011
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4011
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-medical
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4011
      targetPort: 4011
  selector:
    app: api-medical
---
# Aerospace Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-aerospace
  namespace: ims
  labels:
    app: api-aerospace
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-aerospace
  template:
    metadata:
      labels:
        app: api-aerospace
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4012'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-aerospace
          image: ims/api-aerospace:latest
          ports:
            - containerPort: 4012
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4012'
            - name: OTEL_SERVICE_NAME
              value: 'api-aerospace'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-aerospace
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4012
      targetPort: 4012
  selector:
    app: api-aerospace
---
# Finance Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-finance
  namespace: ims
  labels:
    app: api-finance
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-finance
  template:
    metadata:
      labels:
        app: api-finance
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4013'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-finance
          image: ims/api-finance:latest
          ports:
            - containerPort: 4013
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4013'
            - name: OTEL_SERVICE_NAME
              value: 'api-finance'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4013
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4013
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-finance
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4013
      targetPort: 4013
  selector:
    app: api-finance
---
# CRM Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-crm
  namespace: ims
  labels:
    app: api-crm
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-crm
  template:
    metadata:
      labels:
        app: api-crm
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4014'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-crm
          image: ims/api-crm:latest
          ports:
            - containerPort: 4014
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4014'
            - name: OTEL_SERVICE_NAME
              value: 'api-crm'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4014
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4014
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-crm
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4014
      targetPort: 4014
  selector:
    app: api-crm
---
# InfoSec Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-infosec
  namespace: ims
  labels:
    app: api-infosec
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-infosec
  template:
    metadata:
      labels:
        app: api-infosec
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4015'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-infosec
          image: ims/api-infosec:latest
          ports:
            - containerPort: 4015
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4015'
            - name: OTEL_SERVICE_NAME
              value: 'api-infosec'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4015
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4015
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-infosec
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4015
      targetPort: 4015
  selector:
    app: api-infosec
---
# ESG Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-esg
  namespace: ims
  labels:
    app: api-esg
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-esg
  template:
    metadata:
      labels:
        app: api-esg
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4016'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-esg
          image: ims/api-esg:latest
          ports:
            - containerPort: 4016
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4016'
            - name: OTEL_SERVICE_NAME
              value: 'api-esg'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4016
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4016
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-esg
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4016
      targetPort: 4016
  selector:
    app: api-esg
---
# CMMS Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-cmms
  namespace: ims
  labels:
    app: api-cmms
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-cmms
  template:
    metadata:
      labels:
        app: api-cmms
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4017'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-cmms
          image: ims/api-cmms:latest
          ports:
            - containerPort: 4017
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4017'
            - name: OTEL_SERVICE_NAME
              value: 'api-cmms'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4017
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4017
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-cmms
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4017
      targetPort: 4017
  selector:
    app: api-cmms
---
# Portal Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-portal
  namespace: ims
  labels:
    app: api-portal
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-portal
  template:
    metadata:
      labels:
        app: api-portal
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4018'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-portal
          image: ims/api-portal:latest
          ports:
            - containerPort: 4018
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4018'
            - name: OTEL_SERVICE_NAME
              value: 'api-portal'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4018
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4018
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-portal
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4018
      targetPort: 4018
  selector:
    app: api-portal
---
# Food Safety Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-food-safety
  namespace: ims
  labels:
    app: api-food-safety
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-food-safety
  template:
    metadata:
      labels:
        app: api-food-safety
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4019'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-food-safety
          image: ims/api-food-safety:latest
          ports:
            - containerPort: 4019
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4019'
            - name: OTEL_SERVICE_NAME
              value: 'api-food-safety'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4019
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4019
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-food-safety
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4019
      targetPort: 4019
  selector:
    app: api-food-safety
---
# Energy Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-energy
  namespace: ims
  labels:
    app: api-energy
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-energy
  template:
    metadata:
      labels:
        app: api-energy
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4020'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-energy
          image: ims/api-energy:latest
          ports:
            - containerPort: 4020
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4020'
            - name: OTEL_SERVICE_NAME
              value: 'api-energy'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4020
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4020
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-energy
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4020
      targetPort: 4020
  selector:
    app: api-energy
---
# Analytics Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-analytics
  namespace: ims
  labels:
    app: api-analytics
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-analytics
  template:
    metadata:
      labels:
        app: api-analytics
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4021'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-analytics
          image: ims/api-analytics:latest
          ports:
            - containerPort: 4021
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4021'
            - name: OTEL_SERVICE_NAME
              value: 'api-analytics'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4021
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4021
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-analytics
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4021
      targetPort: 4021
  selector:
    app: api-analytics
---
# Field Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-field-service
  namespace: ims
  labels:
    app: api-field-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-field-service
  template:
    metadata:
      labels:
        app: api-field-service
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4022'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-field-service
          image: ims/api-field-service:latest
          ports:
            - containerPort: 4022
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4022'
            - name: OTEL_SERVICE_NAME
              value: 'api-field-service'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4022
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4022
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-field-service
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4022
      targetPort: 4022
  selector:
    app: api-field-service
---
# ISO 42001 AI Management Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-iso42001
  namespace: ims
  labels:
    app: api-iso42001
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-iso42001
  template:
    metadata:
      labels:
        app: api-iso42001
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4023'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-iso42001
          image: ims/api-iso42001:latest
          ports:
            - containerPort: 4023
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4023'
            - name: OTEL_SERVICE_NAME
              value: 'api-iso42001'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4023
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4023
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-iso42001
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4023
      targetPort: 4023
  selector:
    app: api-iso42001
---
# ISO 37001 Anti-Bribery Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-iso37001
  namespace: ims
  labels:
    app: api-iso37001
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-iso37001
  template:
    metadata:
      labels:
        app: api-iso37001
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4024'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-iso37001
          image: ims/api-iso37001:latest
          ports:
            - containerPort: 4024
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4024'
            - name: OTEL_SERVICE_NAME
              value: 'api-iso37001'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4024
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4024
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-iso37001
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4024
      targetPort: 4024
  selector:
    app: api-iso37001
---
# Marketing Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-marketing
  namespace: ims
  labels:
    app: api-marketing
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-marketing
  template:
    metadata:
      labels:
        app: api-marketing
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4025'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-marketing
          image: ims/api-marketing:latest
          ports:
            - containerPort: 4025
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4025'
            - name: OTEL_SERVICE_NAME
              value: 'api-marketing'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4025
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4025
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-marketing
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4025
      targetPort: 4025
  selector:
    app: api-marketing
---
# Partners Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-partners
  namespace: ims
  labels:
    app: api-partners
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-partners
  template:
    metadata:
      labels:
        app: api-partners
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4026'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-partners
          image: ims/api-partners:latest
          ports:
            - containerPort: 4026
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4026'
            - name: OTEL_SERVICE_NAME
              value: 'api-partners'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4026
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4026
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-partners
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4026
      targetPort: 4026
  selector:
    app: api-partners
---
# Risk Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-risk
  namespace: ims
  labels:
    app: api-risk
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-risk
  template:
    metadata:
      labels:
        app: api-risk
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4027'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-risk
          image: ims/api-risk:latest
          ports:
            - containerPort: 4027
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4027'
            - name: OTEL_SERVICE_NAME
              value: 'api-risk'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4027
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4027
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-risk
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4027
      targetPort: 4027
  selector:
    app: api-risk
---
# Training Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-training
  namespace: ims
  labels:
    app: api-training
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-training
  template:
    metadata:
      labels:
        app: api-training
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4028'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-training
          image: ims/api-training:latest
          ports:
            - containerPort: 4028
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4028'
            - name: OTEL_SERVICE_NAME
              value: 'api-training'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4028
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4028
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-training
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4028
      targetPort: 4028
  selector:
    app: api-training
---
# Suppliers Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-suppliers
  namespace: ims
  labels:
    app: api-suppliers
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-suppliers
  template:
    metadata:
      labels:
        app: api-suppliers
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4029'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-suppliers
          image: ims/api-suppliers:latest
          ports:
            - containerPort: 4029
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4029'
            - name: OTEL_SERVICE_NAME
              value: 'api-suppliers'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4029
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4029
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-suppliers
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4029
      targetPort: 4029
  selector:
    app: api-suppliers
---
# Assets Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-assets
  namespace: ims
  labels:
    app: api-assets
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-assets
  template:
    metadata:
      labels:
        app: api-assets
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4030'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-assets
          image: ims/api-assets:latest
          ports:
            - containerPort: 4030
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4030'
            - name: OTEL_SERVICE_NAME
              value: 'api-assets'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4030
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4030
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-assets
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4030
      targetPort: 4030
  selector:
    app: api-assets
---
# Documents Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-documents
  namespace: ims
  labels:
    app: api-documents
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-documents
  template:
    metadata:
      labels:
        app: api-documents
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4031'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-documents
          image: ims/api-documents:latest
          ports:
            - containerPort: 4031
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4031'
            - name: OTEL_SERVICE_NAME
              value: 'api-documents'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4031
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4031
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-documents
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4031
      targetPort: 4031
  selector:
    app: api-documents
---
# Complaints Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-complaints
  namespace: ims
  labels:
    app: api-complaints
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-complaints
  template:
    metadata:
      labels:
        app: api-complaints
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4032'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-complaints
          image: ims/api-complaints:latest
          ports:
            - containerPort: 4032
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4032'
            - name: OTEL_SERVICE_NAME
              value: 'api-complaints'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4032
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4032
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-complaints
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4032
      targetPort: 4032
  selector:
    app: api-complaints
---
# Contracts Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-contracts
  namespace: ims
  labels:
    app: api-contracts
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-contracts
  template:
    metadata:
      labels:
        app: api-contracts
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4033'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-contracts
          image: ims/api-contracts:latest
          ports:
            - containerPort: 4033
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4033'
            - name: OTEL_SERVICE_NAME
              value: 'api-contracts'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4033
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4033
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-contracts
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4033
      targetPort: 4033
  selector:
    app: api-contracts
---
# Permit to Work Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-ptw
  namespace: ims
  labels:
    app: api-ptw
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-ptw
  template:
    metadata:
      labels:
        app: api-ptw
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4034'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-ptw
          image: ims/api-ptw:latest
          ports:
            - containerPort: 4034
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4034'
            - name: OTEL_SERVICE_NAME
              value: 'api-ptw'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4034
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4034
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-ptw
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4034
      targetPort: 4034
  selector:
    app: api-ptw
---
# Regulatory Monitor Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-reg-monitor
  namespace: ims
  labels:
    app: api-reg-monitor
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-reg-monitor
  template:
    metadata:
      labels:
        app: api-reg-monitor
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4035'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-reg-monitor
          image: ims/api-reg-monitor:latest
          ports:
            - containerPort: 4035
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4035'
            - name: OTEL_SERVICE_NAME
              value: 'api-reg-monitor'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4035
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4035
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-reg-monitor
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4035
      targetPort: 4035
  selector:
    app: api-reg-monitor
---
# Incidents Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-incidents
  namespace: ims
  labels:
    app: api-incidents
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-incidents
  template:
    metadata:
      labels:
        app: api-incidents
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4036'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-incidents
          image: ims/api-incidents:latest
          ports:
            - containerPort: 4036
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4036'
            - name: OTEL_SERVICE_NAME
              value: 'api-incidents'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4036
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4036
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-incidents
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4036
      targetPort: 4036
  selector:
    app: api-incidents
---
# Audits Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-audits
  namespace: ims
  labels:
    app: api-audits
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-audits
  template:
    metadata:
      labels:
        app: api-audits
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4037'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-audits
          image: ims/api-audits:latest
          ports:
            - containerPort: 4037
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4037'
            - name: OTEL_SERVICE_NAME
              value: 'api-audits'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4037
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4037
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-audits
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4037
      targetPort: 4037
  selector:
    app: api-audits
---
# Management Review Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-mgmt-review
  namespace: ims
  labels:
    app: api-mgmt-review
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-mgmt-review
  template:
    metadata:
      labels:
        app: api-mgmt-review
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4038'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-mgmt-review
          image: ims/api-mgmt-review:latest
          ports:
            - containerPort: 4038
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4038'
            - name: OTEL_SERVICE_NAME
              value: 'api-mgmt-review'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4038
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4038
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-mgmt-review
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4038
      targetPort: 4038
  selector:
    app: api-mgmt-review
---
# Setup Wizard Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-setup-wizard
  namespace: ims
  labels:
    app: api-setup-wizard
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-setup-wizard
  template:
    metadata:
      labels:
        app: api-setup-wizard
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4039'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-setup-wizard
          image: ims/api-setup-wizard:latest
          ports:
            - containerPort: 4039
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4039'
            - name: OTEL_SERVICE_NAME
              value: 'api-setup-wizard'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4039
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4039
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-setup-wizard
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4039
      targetPort: 4039
  selector:
    app: api-setup-wizard
---
# Chemicals Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-chemicals
  namespace: ims
  labels:
    app: api-chemicals
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-chemicals
  template:
    metadata:
      labels:
        app: api-chemicals
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4040'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-chemicals
          image: ims/api-chemicals:latest
          ports:
            - containerPort: 4040
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4040'
            - name: OTEL_SERVICE_NAME
              value: 'api-chemicals'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4040
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4040
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-chemicals
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4040
      targetPort: 4040
  selector:
    app: api-chemicals
---
# Emergency Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-emergency
  namespace: ims
  labels:
    app: api-emergency
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-emergency
  template:
    metadata:
      labels:
        app: api-emergency
        tier: backend
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '4041'
        prometheus.io/path: '/metrics'
    spec:
      containers:
        - name: api-emergency
          image: ims/api-emergency:latest
          ports:
            - containerPort: 4041
          envFrom:
            - configMapRef:
                name: ims-config
            - secretRef:
                name: ims-secrets
          env:
            - name: PORT
              value: '4041'
            - name: OTEL_SERVICE_NAME
              value: 'api-emergency'
          resources:
            requests:
              memory: '128Mi'
              cpu: '50m'
            limits:
              memory: '256Mi'
              cpu: '200m'
          livenessProbe:
            httpGet:
              path: /health
              port: 4041
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4041
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-emergency
  namespace: ims
spec:
  type: ClusterIP
  ports:
    - port: 4041
      targetPort: 4041
  selector:
    app: api-emergency
