version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: ims-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-ims}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ims-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: >
      sh -c '
        if [ -n "$REDIS_PASSWORD" ]; then
          redis-server --requirepass "$REDIS_PASSWORD"
        else
          redis-server
        fi
      '
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  # API Services
  api-gateway:
    build:
      context: .
      dockerfile: ./apps/api-gateway/Dockerfile
    container_name: ims-api-gateway
    restart: unless-stopped
    ports:
      - '4000:4000'
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      REDIS_URL: redis://:${REDIS_PASSWORD:-}@redis:6379
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-}
      SERVICE_HEALTH_SAFETY_URL: http://api-health-safety:4001
      SERVICE_ENVIRONMENT_URL: http://api-environment:4002
      SERVICE_QUALITY_URL: http://api-quality:4003
      SERVICE_AI_ANALYSIS_URL: http://api-ai-analysis:4004
      SERVICE_AI_URL: http://api-ai-analysis:4004
      SERVICE_INVENTORY_URL: http://api-inventory:4005
      SERVICE_HR_URL: http://host.docker.internal:4006
      SERVICE_PAYROLL_URL: http://host.docker.internal:4007
      SERVICE_WORKFLOWS_URL: http://host.docker.internal:4008
      SERVICE_PROJECT_MANAGEMENT_URL: http://host.docker.internal:4009
      SERVICE_AUTOMOTIVE_URL: http://host.docker.internal:4010
      SERVICE_MEDICAL_URL: http://host.docker.internal:4011
      SERVICE_AEROSPACE_URL: http://host.docker.internal:4012
      SERVICE_FINANCE_URL: http://host.docker.internal:4013
      SERVICE_CRM_URL: http://host.docker.internal:4014
      SERVICE_INFOSEC_URL: http://host.docker.internal:4015
      SERVICE_ESG_URL: http://host.docker.internal:4016
      SERVICE_CMMS_URL: http://host.docker.internal:4017
      SERVICE_PORTAL_URL: http://host.docker.internal:4018
      SERVICE_FOOD_SAFETY_URL: http://host.docker.internal:4019
      SERVICE_ENERGY_URL: http://host.docker.internal:4020
      SERVICE_ANALYTICS_URL: http://host.docker.internal:4021
      SERVICE_FIELD_SERVICE_URL: http://host.docker.internal:4022
      SERVICE_ISO42001_URL: http://host.docker.internal:4023
      SERVICE_ISO37001_URL: http://host.docker.internal:4024
      CSRF_ENABLED: "false"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      api-health-safety:
        condition: service_healthy
      api-environment:
        condition: service_healthy
      api-quality:
        condition: service_healthy
      api-ai-analysis:
        condition: service_healthy
      api-inventory:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-health-safety:
    build:
      context: .
      dockerfile: ./apps/api-health-safety/Dockerfile
    container_name: ims-api-health-safety
    restart: unless-stopped
    expose:
      - '4001'
    environment:
      NODE_ENV: production
      PORT: 4001
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      HEALTH_SAFETY_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-environment:
    build:
      context: .
      dockerfile: ./apps/api-environment/Dockerfile
    container_name: ims-api-environment
    restart: unless-stopped
    expose:
      - '4002'
    environment:
      NODE_ENV: production
      PORT: 4002
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      ENVIRONMENT_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-quality:
    build:
      context: .
      dockerfile: ./apps/api-quality/Dockerfile
    container_name: ims-api-quality
    restart: unless-stopped
    expose:
      - '4003'
    environment:
      NODE_ENV: production
      PORT: 4003
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      QUALITY_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-ai-analysis:
    build:
      context: .
      dockerfile: ./apps/api-ai-analysis/Dockerfile
    container_name: ims-api-ai-analysis
    restart: unless-stopped
    expose:
      - '4004'
    environment:
      NODE_ENV: production
      PORT: 4004
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      AI_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-inventory:
    build:
      context: .
      dockerfile: ./apps/api-inventory/Dockerfile
    container_name: ims-api-inventory
    restart: unless-stopped
    expose:
      - '4005'
    environment:
      NODE_ENV: production
      PORT: 4005
      INVENTORY_PORT: 4005
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      INVENTORY_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4005/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-hr:
    build:
      context: .
      dockerfile: ./apps/api-hr/Dockerfile
    container_name: ims-api-hr
    restart: unless-stopped
    expose:
      - '4006'
    environment:
      NODE_ENV: production
      PORT: 4006
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      HR_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4006/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-payroll:
    build:
      context: .
      dockerfile: ./apps/api-payroll/Dockerfile
    container_name: ims-api-payroll
    restart: unless-stopped
    expose:
      - '4007'
    environment:
      NODE_ENV: production
      PORT: 4007
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      PAYROLL_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4007/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-workflows:
    build:
      context: .
      dockerfile: ./apps/api-workflows/Dockerfile
    container_name: ims-api-workflows
    restart: unless-stopped
    expose:
      - '4008'
    environment:
      NODE_ENV: production
      PORT: 4008
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      WORKFLOWS_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4008/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-project-management:
    build:
      context: .
      dockerfile: ./apps/api-project-management/Dockerfile
    container_name: ims-api-project-management
    restart: unless-stopped
    expose:
      - '4009'
    environment:
      NODE_ENV: production
      PORT: 4009
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      PROJECT_MANAGEMENT_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4009/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Web Applications
  web-dashboard:
    build:
      context: .
      dockerfile: ./apps/web-dashboard/Dockerfile
    container_name: ims-web-dashboard
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-health-safety:
    build:
      context: .
      dockerfile: ./apps/web-health-safety/Dockerfile
    container_name: ims-web-health-safety
    restart: unless-stopped
    ports:
      - '3001:3001'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-environment:
    build:
      context: .
      dockerfile: ./apps/web-environment/Dockerfile
    container_name: ims-web-environment
    restart: unless-stopped
    ports:
      - '3002:3002'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3002"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-quality:
    build:
      context: .
      dockerfile: ./apps/web-quality/Dockerfile
    container_name: ims-web-quality
    restart: unless-stopped
    ports:
      - '3003:3003'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3003"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-settings:
    build:
      context: .
      dockerfile: ./apps/web-settings/Dockerfile
    container_name: ims-web-settings
    restart: unless-stopped
    ports:
      - '3004:3004'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3004"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-hr:
    build:
      context: .
      dockerfile: ./apps/web-hr/Dockerfile
    container_name: ims-web-hr
    restart: unless-stopped
    ports:
      - '3006:3006'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3006"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-payroll:
    build:
      context: .
      dockerfile: ./apps/web-payroll/Dockerfile
    container_name: ims-web-payroll
    restart: unless-stopped
    ports:
      - '3007:3007'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3007"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-inventory:
    build:
      context: .
      dockerfile: ./apps/web-inventory/Dockerfile
    container_name: ims-web-inventory
    restart: unless-stopped
    ports:
      - '3005:3005'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3005"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-workflows:
    build:
      context: .
      dockerfile: ./apps/web-workflows/Dockerfile
    container_name: ims-web-workflows
    restart: unless-stopped
    ports:
      - '3008:3008'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3008"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-project-management:
    build:
      context: .
      dockerfile: ./apps/web-project-management/Dockerfile
    container_name: ims-web-project-management
    restart: unless-stopped
    ports:
      - '3009:3009'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3009"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  # ── Phase 1-10 API Services ──────────────────────────────────────

  api-automotive:
    build:
      context: .
      dockerfile: ./apps/api-automotive/Dockerfile
    container_name: ims-api-automotive
    restart: unless-stopped
    expose:
      - '4010'
    environment:
      NODE_ENV: production
      PORT: 4010
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      AUTOMOTIVE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4010/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-medical:
    build:
      context: .
      dockerfile: ./apps/api-medical/Dockerfile
    container_name: ims-api-medical
    restart: unless-stopped
    expose:
      - '4011'
    environment:
      NODE_ENV: production
      PORT: 4011
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      MEDICAL_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4011/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-aerospace:
    build:
      context: .
      dockerfile: ./apps/api-aerospace/Dockerfile
    container_name: ims-api-aerospace
    restart: unless-stopped
    expose:
      - '4012'
    environment:
      NODE_ENV: production
      PORT: 4012
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      AEROSPACE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4012/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-finance:
    build:
      context: .
      dockerfile: ./apps/api-finance/Dockerfile
    container_name: ims-api-finance
    restart: unless-stopped
    expose:
      - '4013'
    environment:
      NODE_ENV: production
      PORT: 4013
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      FINANCE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4013/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-crm:
    build:
      context: .
      dockerfile: ./apps/api-crm/Dockerfile
    container_name: ims-api-crm
    restart: unless-stopped
    expose:
      - '4014'
    environment:
      NODE_ENV: production
      PORT: 4014
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      CRM_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4014/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-infosec:
    build:
      context: .
      dockerfile: ./apps/api-infosec/Dockerfile
    container_name: ims-api-infosec
    restart: unless-stopped
    expose:
      - '4015'
    environment:
      NODE_ENV: production
      PORT: 4015
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      INFOSEC_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4015/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-esg:
    build:
      context: .
      dockerfile: ./apps/api-esg/Dockerfile
    container_name: ims-api-esg
    restart: unless-stopped
    expose:
      - '4016'
    environment:
      NODE_ENV: production
      PORT: 4016
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ESG_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4016/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-cmms:
    build:
      context: .
      dockerfile: ./apps/api-cmms/Dockerfile
    container_name: ims-api-cmms
    restart: unless-stopped
    expose:
      - '4017'
    environment:
      NODE_ENV: production
      PORT: 4017
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      CMMS_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4017/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-portal:
    build:
      context: .
      dockerfile: ./apps/api-portal/Dockerfile
    container_name: ims-api-portal
    restart: unless-stopped
    expose:
      - '4018'
    environment:
      NODE_ENV: production
      PORT: 4018
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      PORTAL_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4018/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-food-safety:
    build:
      context: .
      dockerfile: ./apps/api-food-safety/Dockerfile
    container_name: ims-api-food-safety
    restart: unless-stopped
    expose:
      - '4019'
    environment:
      NODE_ENV: production
      PORT: 4019
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      FOOD_SAFETY_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4019/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-energy:
    build:
      context: .
      dockerfile: ./apps/api-energy/Dockerfile
    container_name: ims-api-energy
    restart: unless-stopped
    expose:
      - '4020'
    environment:
      NODE_ENV: production
      PORT: 4020
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ENERGY_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4020/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-analytics:
    build:
      context: .
      dockerfile: ./apps/api-analytics/Dockerfile
    container_name: ims-api-analytics
    restart: unless-stopped
    expose:
      - '4021'
    environment:
      NODE_ENV: production
      PORT: 4021
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ANALYTICS_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4021/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-field-service:
    build:
      context: .
      dockerfile: ./apps/api-field-service/Dockerfile
    container_name: ims-api-field-service
    restart: unless-stopped
    expose:
      - '4022'
    environment:
      NODE_ENV: production
      PORT: 4022
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      FIELD_SERVICE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4022/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-iso42001:
    build:
      context: .
      dockerfile: ./apps/api-iso42001/Dockerfile
    container_name: ims-api-iso42001
    restart: unless-stopped
    expose:
      - '4023'
    environment:
      NODE_ENV: production
      PORT: 4023
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ISO42001_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4023/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  api-iso37001:
    build:
      context: .
      dockerfile: ./apps/api-iso37001/Dockerfile
    container_name: ims-api-iso37001
    restart: unless-stopped
    expose:
      - '4024'
    environment:
      NODE_ENV: production
      PORT: 4024
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ISO37001_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      CORE_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4024/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  web-automotive:
    build:
      context: .
      dockerfile: ./apps/web-automotive/Dockerfile
    container_name: ims-web-automotive
    restart: unless-stopped
    ports:
      - '3010:3010'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3010"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-medical:
    build:
      context: .
      dockerfile: ./apps/web-medical/Dockerfile
    container_name: ims-web-medical
    restart: unless-stopped
    ports:
      - '3011:3011'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3011"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-aerospace:
    build:
      context: .
      dockerfile: ./apps/web-aerospace/Dockerfile
    container_name: ims-web-aerospace
    restart: unless-stopped
    ports:
      - '3012:3012'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3012"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-finance:
    build:
      context: .
      dockerfile: ./apps/web-finance/Dockerfile
    container_name: ims-web-finance
    restart: unless-stopped
    ports:
      - '3013:3013'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3013"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-crm:
    build:
      context: .
      dockerfile: ./apps/web-crm/Dockerfile
    container_name: ims-web-crm
    restart: unless-stopped
    ports:
      - '3014:3014'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3014"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-infosec:
    build:
      context: .
      dockerfile: ./apps/web-infosec/Dockerfile
    container_name: ims-web-infosec
    restart: unless-stopped
    ports:
      - '3015:3015'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3015"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-esg:
    build:
      context: .
      dockerfile: ./apps/web-esg/Dockerfile
    container_name: ims-web-esg
    restart: unless-stopped
    ports:
      - '3016:3016'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3016"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-cmms:
    build:
      context: .
      dockerfile: ./apps/web-cmms/Dockerfile
    container_name: ims-web-cmms
    restart: unless-stopped
    ports:
      - '3017:3017'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3017"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-customer-portal:
    build:
      context: .
      dockerfile: ./apps/web-customer-portal/Dockerfile
    container_name: ims-web-customer-portal
    restart: unless-stopped
    ports:
      - '3018:3018'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3018"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-supplier-portal:
    build:
      context: .
      dockerfile: ./apps/web-supplier-portal/Dockerfile
    container_name: ims-web-supplier-portal
    restart: unless-stopped
    ports:
      - '3019:3019'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3019"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-food-safety:
    build:
      context: .
      dockerfile: ./apps/web-food-safety/Dockerfile
    container_name: ims-web-food-safety
    restart: unless-stopped
    ports:
      - '3020:3020'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3020"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-energy:
    build:
      context: .
      dockerfile: ./apps/web-energy/Dockerfile
    container_name: ims-web-energy
    restart: unless-stopped
    ports:
      - '3021:3021'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3021"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-analytics:
    build:
      context: .
      dockerfile: ./apps/web-analytics/Dockerfile
    container_name: ims-web-analytics
    restart: unless-stopped
    ports:
      - '3022:3022'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3022"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-field-service:
    build:
      context: .
      dockerfile: ./apps/web-field-service/Dockerfile
    container_name: ims-web-field-service
    restart: unless-stopped
    ports:
      - '3023:3023'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3023"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-iso42001:
    build:
      context: .
      dockerfile: ./apps/web-iso42001/Dockerfile
    container_name: ims-web-iso42001
    restart: unless-stopped
    ports:
      - '3024:3024'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3024"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  web-iso37001:
    build:
      context: .
      dockerfile: ./apps/web-iso37001/Dockerfile
    container_name: ims-web-iso37001
    restart: unless-stopped
    ports:
      - '3025:3025'
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
    depends_on:
      - api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3025"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  api-marketing:
    build:
      context: .
      dockerfile: ./apps/api-marketing/Dockerfile
    container_name: ims-api-marketing
    restart: unless-stopped
    expose:
      - '4025'
    environment:
      NODE_ENV: production
      PORT: 4025
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      MARKETING_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      HUBSPOT_API_KEY: ${HUBSPOT_API_KEY:-}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4025/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

  api-partners:
    build:
      context: .
      dockerfile: ./apps/api-partners/Dockerfile
    container_name: ims-api-partners
    restart: unless-stopped
    expose:
      - '4026'
    environment:
      NODE_ENV: production
      PORT: 4026
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      MARKETING_DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ims}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4026/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s

volumes:
  postgres_data:
  redis_data:
