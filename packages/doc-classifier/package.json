{
  "name": "@ims/doc-classifier",
  "version": "1.0.0",
  "description": "Document classification and categorisation utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "jest" },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": { "^.+\\.tsx?$": ["ts-jest", { "isolatedModules": true, "diagnostics": false }] }
  },
  "devDependencies": { "ts-jest": "^29.0.0", "jest": "^29.0.0", "@types/jest": "^29.0.0", "typescript": "^5.0.0" }
}
