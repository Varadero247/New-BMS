// Copyright (c) 2026 Nexara DMCC. All rights reserved.
// Proprietary and confidential. Unauthorised copying prohibited.
// See LICENCE file for details.

import { LRUCache, LFUCache, TTLCache } from "../lru-cache-2";

describe("LRUCache set and get", () => {
  it("lru get 0", () => { const c = new LRUCache<number,number>(100); c.set(0, 0); expect(c.get(0)).toBe(0); });
  it("lru get 1", () => { const c = new LRUCache<number,number>(100); c.set(1, 2); expect(c.get(1)).toBe(2); });
  it("lru get 2", () => { const c = new LRUCache<number,number>(100); c.set(2, 4); expect(c.get(2)).toBe(4); });
  it("lru get 3", () => { const c = new LRUCache<number,number>(100); c.set(3, 6); expect(c.get(3)).toBe(6); });
  it("lru get 4", () => { const c = new LRUCache<number,number>(100); c.set(4, 8); expect(c.get(4)).toBe(8); });
  it("lru get 5", () => { const c = new LRUCache<number,number>(100); c.set(5, 10); expect(c.get(5)).toBe(10); });
  it("lru get 6", () => { const c = new LRUCache<number,number>(100); c.set(6, 12); expect(c.get(6)).toBe(12); });
  it("lru get 7", () => { const c = new LRUCache<number,number>(100); c.set(7, 14); expect(c.get(7)).toBe(14); });
  it("lru get 8", () => { const c = new LRUCache<number,number>(100); c.set(8, 16); expect(c.get(8)).toBe(16); });
  it("lru get 9", () => { const c = new LRUCache<number,number>(100); c.set(9, 18); expect(c.get(9)).toBe(18); });
  it("lru get 10", () => { const c = new LRUCache<number,number>(100); c.set(10, 20); expect(c.get(10)).toBe(20); });
  it("lru get 11", () => { const c = new LRUCache<number,number>(100); c.set(11, 22); expect(c.get(11)).toBe(22); });
  it("lru get 12", () => { const c = new LRUCache<number,number>(100); c.set(12, 24); expect(c.get(12)).toBe(24); });
  it("lru get 13", () => { const c = new LRUCache<number,number>(100); c.set(13, 26); expect(c.get(13)).toBe(26); });
  it("lru get 14", () => { const c = new LRUCache<number,number>(100); c.set(14, 28); expect(c.get(14)).toBe(28); });
  it("lru get 15", () => { const c = new LRUCache<number,number>(100); c.set(15, 30); expect(c.get(15)).toBe(30); });
  it("lru get 16", () => { const c = new LRUCache<number,number>(100); c.set(16, 32); expect(c.get(16)).toBe(32); });
  it("lru get 17", () => { const c = new LRUCache<number,number>(100); c.set(17, 34); expect(c.get(17)).toBe(34); });
  it("lru get 18", () => { const c = new LRUCache<number,number>(100); c.set(18, 36); expect(c.get(18)).toBe(36); });
  it("lru get 19", () => { const c = new LRUCache<number,number>(100); c.set(19, 38); expect(c.get(19)).toBe(38); });
  it("lru get 20", () => { const c = new LRUCache<number,number>(100); c.set(20, 40); expect(c.get(20)).toBe(40); });
  it("lru get 21", () => { const c = new LRUCache<number,number>(100); c.set(21, 42); expect(c.get(21)).toBe(42); });
  it("lru get 22", () => { const c = new LRUCache<number,number>(100); c.set(22, 44); expect(c.get(22)).toBe(44); });
  it("lru get 23", () => { const c = new LRUCache<number,number>(100); c.set(23, 46); expect(c.get(23)).toBe(46); });
  it("lru get 24", () => { const c = new LRUCache<number,number>(100); c.set(24, 48); expect(c.get(24)).toBe(48); });
  it("lru get 25", () => { const c = new LRUCache<number,number>(100); c.set(25, 50); expect(c.get(25)).toBe(50); });
  it("lru get 26", () => { const c = new LRUCache<number,number>(100); c.set(26, 52); expect(c.get(26)).toBe(52); });
  it("lru get 27", () => { const c = new LRUCache<number,number>(100); c.set(27, 54); expect(c.get(27)).toBe(54); });
  it("lru get 28", () => { const c = new LRUCache<number,number>(100); c.set(28, 56); expect(c.get(28)).toBe(56); });
  it("lru get 29", () => { const c = new LRUCache<number,number>(100); c.set(29, 58); expect(c.get(29)).toBe(58); });
  it("lru get 30", () => { const c = new LRUCache<number,number>(100); c.set(30, 60); expect(c.get(30)).toBe(60); });
  it("lru get 31", () => { const c = new LRUCache<number,number>(100); c.set(31, 62); expect(c.get(31)).toBe(62); });
  it("lru get 32", () => { const c = new LRUCache<number,number>(100); c.set(32, 64); expect(c.get(32)).toBe(64); });
  it("lru get 33", () => { const c = new LRUCache<number,number>(100); c.set(33, 66); expect(c.get(33)).toBe(66); });
  it("lru get 34", () => { const c = new LRUCache<number,number>(100); c.set(34, 68); expect(c.get(34)).toBe(68); });
  it("lru get 35", () => { const c = new LRUCache<number,number>(100); c.set(35, 70); expect(c.get(35)).toBe(70); });
  it("lru get 36", () => { const c = new LRUCache<number,number>(100); c.set(36, 72); expect(c.get(36)).toBe(72); });
  it("lru get 37", () => { const c = new LRUCache<number,number>(100); c.set(37, 74); expect(c.get(37)).toBe(74); });
  it("lru get 38", () => { const c = new LRUCache<number,number>(100); c.set(38, 76); expect(c.get(38)).toBe(76); });
  it("lru get 39", () => { const c = new LRUCache<number,number>(100); c.set(39, 78); expect(c.get(39)).toBe(78); });
  it("lru get 40", () => { const c = new LRUCache<number,number>(100); c.set(40, 80); expect(c.get(40)).toBe(80); });
  it("lru get 41", () => { const c = new LRUCache<number,number>(100); c.set(41, 82); expect(c.get(41)).toBe(82); });
  it("lru get 42", () => { const c = new LRUCache<number,number>(100); c.set(42, 84); expect(c.get(42)).toBe(84); });
  it("lru get 43", () => { const c = new LRUCache<number,number>(100); c.set(43, 86); expect(c.get(43)).toBe(86); });
  it("lru get 44", () => { const c = new LRUCache<number,number>(100); c.set(44, 88); expect(c.get(44)).toBe(88); });
  it("lru get 45", () => { const c = new LRUCache<number,number>(100); c.set(45, 90); expect(c.get(45)).toBe(90); });
  it("lru get 46", () => { const c = new LRUCache<number,number>(100); c.set(46, 92); expect(c.get(46)).toBe(92); });
  it("lru get 47", () => { const c = new LRUCache<number,number>(100); c.set(47, 94); expect(c.get(47)).toBe(94); });
  it("lru get 48", () => { const c = new LRUCache<number,number>(100); c.set(48, 96); expect(c.get(48)).toBe(96); });
  it("lru get 49", () => { const c = new LRUCache<number,number>(100); c.set(49, 98); expect(c.get(49)).toBe(98); });
  it("lru get 50", () => { const c = new LRUCache<number,number>(100); c.set(50, 100); expect(c.get(50)).toBe(100); });
  it("lru get 51", () => { const c = new LRUCache<number,number>(100); c.set(51, 102); expect(c.get(51)).toBe(102); });
  it("lru get 52", () => { const c = new LRUCache<number,number>(100); c.set(52, 104); expect(c.get(52)).toBe(104); });
  it("lru get 53", () => { const c = new LRUCache<number,number>(100); c.set(53, 106); expect(c.get(53)).toBe(106); });
  it("lru get 54", () => { const c = new LRUCache<number,number>(100); c.set(54, 108); expect(c.get(54)).toBe(108); });
  it("lru get 55", () => { const c = new LRUCache<number,number>(100); c.set(55, 110); expect(c.get(55)).toBe(110); });
  it("lru get 56", () => { const c = new LRUCache<number,number>(100); c.set(56, 112); expect(c.get(56)).toBe(112); });
  it("lru get 57", () => { const c = new LRUCache<number,number>(100); c.set(57, 114); expect(c.get(57)).toBe(114); });
  it("lru get 58", () => { const c = new LRUCache<number,number>(100); c.set(58, 116); expect(c.get(58)).toBe(116); });
  it("lru get 59", () => { const c = new LRUCache<number,number>(100); c.set(59, 118); expect(c.get(59)).toBe(118); });
  it("lru get 60", () => { const c = new LRUCache<number,number>(100); c.set(60, 120); expect(c.get(60)).toBe(120); });
  it("lru get 61", () => { const c = new LRUCache<number,number>(100); c.set(61, 122); expect(c.get(61)).toBe(122); });
  it("lru get 62", () => { const c = new LRUCache<number,number>(100); c.set(62, 124); expect(c.get(62)).toBe(124); });
  it("lru get 63", () => { const c = new LRUCache<number,number>(100); c.set(63, 126); expect(c.get(63)).toBe(126); });
  it("lru get 64", () => { const c = new LRUCache<number,number>(100); c.set(64, 128); expect(c.get(64)).toBe(128); });
  it("lru get 65", () => { const c = new LRUCache<number,number>(100); c.set(65, 130); expect(c.get(65)).toBe(130); });
  it("lru get 66", () => { const c = new LRUCache<number,number>(100); c.set(66, 132); expect(c.get(66)).toBe(132); });
  it("lru get 67", () => { const c = new LRUCache<number,number>(100); c.set(67, 134); expect(c.get(67)).toBe(134); });
  it("lru get 68", () => { const c = new LRUCache<number,number>(100); c.set(68, 136); expect(c.get(68)).toBe(136); });
  it("lru get 69", () => { const c = new LRUCache<number,number>(100); c.set(69, 138); expect(c.get(69)).toBe(138); });
  it("lru get 70", () => { const c = new LRUCache<number,number>(100); c.set(70, 140); expect(c.get(70)).toBe(140); });
  it("lru get 71", () => { const c = new LRUCache<number,number>(100); c.set(71, 142); expect(c.get(71)).toBe(142); });
  it("lru get 72", () => { const c = new LRUCache<number,number>(100); c.set(72, 144); expect(c.get(72)).toBe(144); });
  it("lru get 73", () => { const c = new LRUCache<number,number>(100); c.set(73, 146); expect(c.get(73)).toBe(146); });
  it("lru get 74", () => { const c = new LRUCache<number,number>(100); c.set(74, 148); expect(c.get(74)).toBe(148); });
  it("lru get 75", () => { const c = new LRUCache<number,number>(100); c.set(75, 150); expect(c.get(75)).toBe(150); });
  it("lru get 76", () => { const c = new LRUCache<number,number>(100); c.set(76, 152); expect(c.get(76)).toBe(152); });
  it("lru get 77", () => { const c = new LRUCache<number,number>(100); c.set(77, 154); expect(c.get(77)).toBe(154); });
  it("lru get 78", () => { const c = new LRUCache<number,number>(100); c.set(78, 156); expect(c.get(78)).toBe(156); });
  it("lru get 79", () => { const c = new LRUCache<number,number>(100); c.set(79, 158); expect(c.get(79)).toBe(158); });
  it("lru get 80", () => { const c = new LRUCache<number,number>(100); c.set(80, 160); expect(c.get(80)).toBe(160); });
  it("lru get 81", () => { const c = new LRUCache<number,number>(100); c.set(81, 162); expect(c.get(81)).toBe(162); });
  it("lru get 82", () => { const c = new LRUCache<number,number>(100); c.set(82, 164); expect(c.get(82)).toBe(164); });
  it("lru get 83", () => { const c = new LRUCache<number,number>(100); c.set(83, 166); expect(c.get(83)).toBe(166); });
  it("lru get 84", () => { const c = new LRUCache<number,number>(100); c.set(84, 168); expect(c.get(84)).toBe(168); });
  it("lru get 85", () => { const c = new LRUCache<number,number>(100); c.set(85, 170); expect(c.get(85)).toBe(170); });
  it("lru get 86", () => { const c = new LRUCache<number,number>(100); c.set(86, 172); expect(c.get(86)).toBe(172); });
  it("lru get 87", () => { const c = new LRUCache<number,number>(100); c.set(87, 174); expect(c.get(87)).toBe(174); });
  it("lru get 88", () => { const c = new LRUCache<number,number>(100); c.set(88, 176); expect(c.get(88)).toBe(176); });
  it("lru get 89", () => { const c = new LRUCache<number,number>(100); c.set(89, 178); expect(c.get(89)).toBe(178); });
  it("lru get 90", () => { const c = new LRUCache<number,number>(100); c.set(90, 180); expect(c.get(90)).toBe(180); });
  it("lru get 91", () => { const c = new LRUCache<number,number>(100); c.set(91, 182); expect(c.get(91)).toBe(182); });
  it("lru get 92", () => { const c = new LRUCache<number,number>(100); c.set(92, 184); expect(c.get(92)).toBe(184); });
  it("lru get 93", () => { const c = new LRUCache<number,number>(100); c.set(93, 186); expect(c.get(93)).toBe(186); });
  it("lru get 94", () => { const c = new LRUCache<number,number>(100); c.set(94, 188); expect(c.get(94)).toBe(188); });
  it("lru get 95", () => { const c = new LRUCache<number,number>(100); c.set(95, 190); expect(c.get(95)).toBe(190); });
  it("lru get 96", () => { const c = new LRUCache<number,number>(100); c.set(96, 192); expect(c.get(96)).toBe(192); });
  it("lru get 97", () => { const c = new LRUCache<number,number>(100); c.set(97, 194); expect(c.get(97)).toBe(194); });
  it("lru get 98", () => { const c = new LRUCache<number,number>(100); c.set(98, 196); expect(c.get(98)).toBe(196); });
  it("lru get 99", () => { const c = new LRUCache<number,number>(100); c.set(99, 198); expect(c.get(99)).toBe(198); });
  it("lru get 100", () => { const c = new LRUCache<number,number>(100); c.set(100, 200); expect(c.get(100)).toBe(200); });
  it("lru get 101", () => { const c = new LRUCache<number,number>(100); c.set(101, 202); expect(c.get(101)).toBe(202); });
  it("lru get 102", () => { const c = new LRUCache<number,number>(100); c.set(102, 204); expect(c.get(102)).toBe(204); });
  it("lru get 103", () => { const c = new LRUCache<number,number>(100); c.set(103, 206); expect(c.get(103)).toBe(206); });
  it("lru get 104", () => { const c = new LRUCache<number,number>(100); c.set(104, 208); expect(c.get(104)).toBe(208); });
  it("lru get 105", () => { const c = new LRUCache<number,number>(100); c.set(105, 210); expect(c.get(105)).toBe(210); });
  it("lru get 106", () => { const c = new LRUCache<number,number>(100); c.set(106, 212); expect(c.get(106)).toBe(212); });
  it("lru get 107", () => { const c = new LRUCache<number,number>(100); c.set(107, 214); expect(c.get(107)).toBe(214); });
  it("lru get 108", () => { const c = new LRUCache<number,number>(100); c.set(108, 216); expect(c.get(108)).toBe(216); });
  it("lru get 109", () => { const c = new LRUCache<number,number>(100); c.set(109, 218); expect(c.get(109)).toBe(218); });
  it("lru get 110", () => { const c = new LRUCache<number,number>(100); c.set(110, 220); expect(c.get(110)).toBe(220); });
  it("lru get 111", () => { const c = new LRUCache<number,number>(100); c.set(111, 222); expect(c.get(111)).toBe(222); });
  it("lru get 112", () => { const c = new LRUCache<number,number>(100); c.set(112, 224); expect(c.get(112)).toBe(224); });
  it("lru get 113", () => { const c = new LRUCache<number,number>(100); c.set(113, 226); expect(c.get(113)).toBe(226); });
  it("lru get 114", () => { const c = new LRUCache<number,number>(100); c.set(114, 228); expect(c.get(114)).toBe(228); });
  it("lru get 115", () => { const c = new LRUCache<number,number>(100); c.set(115, 230); expect(c.get(115)).toBe(230); });
  it("lru get 116", () => { const c = new LRUCache<number,number>(100); c.set(116, 232); expect(c.get(116)).toBe(232); });
  it("lru get 117", () => { const c = new LRUCache<number,number>(100); c.set(117, 234); expect(c.get(117)).toBe(234); });
  it("lru get 118", () => { const c = new LRUCache<number,number>(100); c.set(118, 236); expect(c.get(118)).toBe(236); });
  it("lru get 119", () => { const c = new LRUCache<number,number>(100); c.set(119, 238); expect(c.get(119)).toBe(238); });
  it("lru get 120", () => { const c = new LRUCache<number,number>(100); c.set(120, 240); expect(c.get(120)).toBe(240); });
  it("lru get 121", () => { const c = new LRUCache<number,number>(100); c.set(121, 242); expect(c.get(121)).toBe(242); });
  it("lru get 122", () => { const c = new LRUCache<number,number>(100); c.set(122, 244); expect(c.get(122)).toBe(244); });
  it("lru get 123", () => { const c = new LRUCache<number,number>(100); c.set(123, 246); expect(c.get(123)).toBe(246); });
  it("lru get 124", () => { const c = new LRUCache<number,number>(100); c.set(124, 248); expect(c.get(124)).toBe(248); });
  it("lru get 125", () => { const c = new LRUCache<number,number>(100); c.set(125, 250); expect(c.get(125)).toBe(250); });
  it("lru get 126", () => { const c = new LRUCache<number,number>(100); c.set(126, 252); expect(c.get(126)).toBe(252); });
  it("lru get 127", () => { const c = new LRUCache<number,number>(100); c.set(127, 254); expect(c.get(127)).toBe(254); });
  it("lru get 128", () => { const c = new LRUCache<number,number>(100); c.set(128, 256); expect(c.get(128)).toBe(256); });
  it("lru get 129", () => { const c = new LRUCache<number,number>(100); c.set(129, 258); expect(c.get(129)).toBe(258); });
  it("lru get 130", () => { const c = new LRUCache<number,number>(100); c.set(130, 260); expect(c.get(130)).toBe(260); });
  it("lru get 131", () => { const c = new LRUCache<number,number>(100); c.set(131, 262); expect(c.get(131)).toBe(262); });
  it("lru get 132", () => { const c = new LRUCache<number,number>(100); c.set(132, 264); expect(c.get(132)).toBe(264); });
  it("lru get 133", () => { const c = new LRUCache<number,number>(100); c.set(133, 266); expect(c.get(133)).toBe(266); });
  it("lru get 134", () => { const c = new LRUCache<number,number>(100); c.set(134, 268); expect(c.get(134)).toBe(268); });
  it("lru get 135", () => { const c = new LRUCache<number,number>(100); c.set(135, 270); expect(c.get(135)).toBe(270); });
  it("lru get 136", () => { const c = new LRUCache<number,number>(100); c.set(136, 272); expect(c.get(136)).toBe(272); });
  it("lru get 137", () => { const c = new LRUCache<number,number>(100); c.set(137, 274); expect(c.get(137)).toBe(274); });
  it("lru get 138", () => { const c = new LRUCache<number,number>(100); c.set(138, 276); expect(c.get(138)).toBe(276); });
  it("lru get 139", () => { const c = new LRUCache<number,number>(100); c.set(139, 278); expect(c.get(139)).toBe(278); });
  it("lru get 140", () => { const c = new LRUCache<number,number>(100); c.set(140, 280); expect(c.get(140)).toBe(280); });
  it("lru get 141", () => { const c = new LRUCache<number,number>(100); c.set(141, 282); expect(c.get(141)).toBe(282); });
  it("lru get 142", () => { const c = new LRUCache<number,number>(100); c.set(142, 284); expect(c.get(142)).toBe(284); });
  it("lru get 143", () => { const c = new LRUCache<number,number>(100); c.set(143, 286); expect(c.get(143)).toBe(286); });
  it("lru get 144", () => { const c = new LRUCache<number,number>(100); c.set(144, 288); expect(c.get(144)).toBe(288); });
  it("lru get 145", () => { const c = new LRUCache<number,number>(100); c.set(145, 290); expect(c.get(145)).toBe(290); });
  it("lru get 146", () => { const c = new LRUCache<number,number>(100); c.set(146, 292); expect(c.get(146)).toBe(292); });
  it("lru get 147", () => { const c = new LRUCache<number,number>(100); c.set(147, 294); expect(c.get(147)).toBe(294); });
  it("lru get 148", () => { const c = new LRUCache<number,number>(100); c.set(148, 296); expect(c.get(148)).toBe(296); });
  it("lru get 149", () => { const c = new LRUCache<number,number>(100); c.set(149, 298); expect(c.get(149)).toBe(298); });
  it("lru get 150", () => { const c = new LRUCache<number,number>(100); c.set(150, 300); expect(c.get(150)).toBe(300); });
  it("lru get 151", () => { const c = new LRUCache<number,number>(100); c.set(151, 302); expect(c.get(151)).toBe(302); });
  it("lru get 152", () => { const c = new LRUCache<number,number>(100); c.set(152, 304); expect(c.get(152)).toBe(304); });
  it("lru get 153", () => { const c = new LRUCache<number,number>(100); c.set(153, 306); expect(c.get(153)).toBe(306); });
  it("lru get 154", () => { const c = new LRUCache<number,number>(100); c.set(154, 308); expect(c.get(154)).toBe(308); });
  it("lru get 155", () => { const c = new LRUCache<number,number>(100); c.set(155, 310); expect(c.get(155)).toBe(310); });
  it("lru get 156", () => { const c = new LRUCache<number,number>(100); c.set(156, 312); expect(c.get(156)).toBe(312); });
  it("lru get 157", () => { const c = new LRUCache<number,number>(100); c.set(157, 314); expect(c.get(157)).toBe(314); });
  it("lru get 158", () => { const c = new LRUCache<number,number>(100); c.set(158, 316); expect(c.get(158)).toBe(316); });
  it("lru get 159", () => { const c = new LRUCache<number,number>(100); c.set(159, 318); expect(c.get(159)).toBe(318); });
  it("lru get 160", () => { const c = new LRUCache<number,number>(100); c.set(160, 320); expect(c.get(160)).toBe(320); });
  it("lru get 161", () => { const c = new LRUCache<number,number>(100); c.set(161, 322); expect(c.get(161)).toBe(322); });
  it("lru get 162", () => { const c = new LRUCache<number,number>(100); c.set(162, 324); expect(c.get(162)).toBe(324); });
  it("lru get 163", () => { const c = new LRUCache<number,number>(100); c.set(163, 326); expect(c.get(163)).toBe(326); });
  it("lru get 164", () => { const c = new LRUCache<number,number>(100); c.set(164, 328); expect(c.get(164)).toBe(328); });
  it("lru get 165", () => { const c = new LRUCache<number,number>(100); c.set(165, 330); expect(c.get(165)).toBe(330); });
  it("lru get 166", () => { const c = new LRUCache<number,number>(100); c.set(166, 332); expect(c.get(166)).toBe(332); });
  it("lru get 167", () => { const c = new LRUCache<number,number>(100); c.set(167, 334); expect(c.get(167)).toBe(334); });
  it("lru get 168", () => { const c = new LRUCache<number,number>(100); c.set(168, 336); expect(c.get(168)).toBe(336); });
  it("lru get 169", () => { const c = new LRUCache<number,number>(100); c.set(169, 338); expect(c.get(169)).toBe(338); });
  it("lru get 170", () => { const c = new LRUCache<number,number>(100); c.set(170, 340); expect(c.get(170)).toBe(340); });
  it("lru get 171", () => { const c = new LRUCache<number,number>(100); c.set(171, 342); expect(c.get(171)).toBe(342); });
  it("lru get 172", () => { const c = new LRUCache<number,number>(100); c.set(172, 344); expect(c.get(172)).toBe(344); });
  it("lru get 173", () => { const c = new LRUCache<number,number>(100); c.set(173, 346); expect(c.get(173)).toBe(346); });
  it("lru get 174", () => { const c = new LRUCache<number,number>(100); c.set(174, 348); expect(c.get(174)).toBe(348); });
  it("lru get 175", () => { const c = new LRUCache<number,number>(100); c.set(175, 350); expect(c.get(175)).toBe(350); });
  it("lru get 176", () => { const c = new LRUCache<number,number>(100); c.set(176, 352); expect(c.get(176)).toBe(352); });
  it("lru get 177", () => { const c = new LRUCache<number,number>(100); c.set(177, 354); expect(c.get(177)).toBe(354); });
  it("lru get 178", () => { const c = new LRUCache<number,number>(100); c.set(178, 356); expect(c.get(178)).toBe(356); });
  it("lru get 179", () => { const c = new LRUCache<number,number>(100); c.set(179, 358); expect(c.get(179)).toBe(358); });
  it("lru get 180", () => { const c = new LRUCache<number,number>(100); c.set(180, 360); expect(c.get(180)).toBe(360); });
  it("lru get 181", () => { const c = new LRUCache<number,number>(100); c.set(181, 362); expect(c.get(181)).toBe(362); });
  it("lru get 182", () => { const c = new LRUCache<number,number>(100); c.set(182, 364); expect(c.get(182)).toBe(364); });
  it("lru get 183", () => { const c = new LRUCache<number,number>(100); c.set(183, 366); expect(c.get(183)).toBe(366); });
  it("lru get 184", () => { const c = new LRUCache<number,number>(100); c.set(184, 368); expect(c.get(184)).toBe(368); });
  it("lru get 185", () => { const c = new LRUCache<number,number>(100); c.set(185, 370); expect(c.get(185)).toBe(370); });
  it("lru get 186", () => { const c = new LRUCache<number,number>(100); c.set(186, 372); expect(c.get(186)).toBe(372); });
  it("lru get 187", () => { const c = new LRUCache<number,number>(100); c.set(187, 374); expect(c.get(187)).toBe(374); });
  it("lru get 188", () => { const c = new LRUCache<number,number>(100); c.set(188, 376); expect(c.get(188)).toBe(376); });
  it("lru get 189", () => { const c = new LRUCache<number,number>(100); c.set(189, 378); expect(c.get(189)).toBe(378); });
  it("lru get 190", () => { const c = new LRUCache<number,number>(100); c.set(190, 380); expect(c.get(190)).toBe(380); });
  it("lru get 191", () => { const c = new LRUCache<number,number>(100); c.set(191, 382); expect(c.get(191)).toBe(382); });
  it("lru get 192", () => { const c = new LRUCache<number,number>(100); c.set(192, 384); expect(c.get(192)).toBe(384); });
  it("lru get 193", () => { const c = new LRUCache<number,number>(100); c.set(193, 386); expect(c.get(193)).toBe(386); });
  it("lru get 194", () => { const c = new LRUCache<number,number>(100); c.set(194, 388); expect(c.get(194)).toBe(388); });
  it("lru get 195", () => { const c = new LRUCache<number,number>(100); c.set(195, 390); expect(c.get(195)).toBe(390); });
  it("lru get 196", () => { const c = new LRUCache<number,number>(100); c.set(196, 392); expect(c.get(196)).toBe(392); });
  it("lru get 197", () => { const c = new LRUCache<number,number>(100); c.set(197, 394); expect(c.get(197)).toBe(394); });
  it("lru get 198", () => { const c = new LRUCache<number,number>(100); c.set(198, 396); expect(c.get(198)).toBe(396); });
  it("lru get 199", () => { const c = new LRUCache<number,number>(100); c.set(199, 398); expect(c.get(199)).toBe(398); });
});

describe("LRUCache has", () => {
  it("lru has 0", () => { const c = new LRUCache<number,number>(100); c.set(0, 0); expect(c.has(0)).toBe(true); });
  it("lru has 1", () => { const c = new LRUCache<number,number>(100); c.set(1, 1); expect(c.has(1)).toBe(true); });
  it("lru has 2", () => { const c = new LRUCache<number,number>(100); c.set(2, 2); expect(c.has(2)).toBe(true); });
  it("lru has 3", () => { const c = new LRUCache<number,number>(100); c.set(3, 3); expect(c.has(3)).toBe(true); });
  it("lru has 4", () => { const c = new LRUCache<number,number>(100); c.set(4, 4); expect(c.has(4)).toBe(true); });
  it("lru has 5", () => { const c = new LRUCache<number,number>(100); c.set(5, 5); expect(c.has(5)).toBe(true); });
  it("lru has 6", () => { const c = new LRUCache<number,number>(100); c.set(6, 6); expect(c.has(6)).toBe(true); });
  it("lru has 7", () => { const c = new LRUCache<number,number>(100); c.set(7, 7); expect(c.has(7)).toBe(true); });
  it("lru has 8", () => { const c = new LRUCache<number,number>(100); c.set(8, 8); expect(c.has(8)).toBe(true); });
  it("lru has 9", () => { const c = new LRUCache<number,number>(100); c.set(9, 9); expect(c.has(9)).toBe(true); });
  it("lru has 10", () => { const c = new LRUCache<number,number>(100); c.set(10, 10); expect(c.has(10)).toBe(true); });
  it("lru has 11", () => { const c = new LRUCache<number,number>(100); c.set(11, 11); expect(c.has(11)).toBe(true); });
  it("lru has 12", () => { const c = new LRUCache<number,number>(100); c.set(12, 12); expect(c.has(12)).toBe(true); });
  it("lru has 13", () => { const c = new LRUCache<number,number>(100); c.set(13, 13); expect(c.has(13)).toBe(true); });
  it("lru has 14", () => { const c = new LRUCache<number,number>(100); c.set(14, 14); expect(c.has(14)).toBe(true); });
  it("lru has 15", () => { const c = new LRUCache<number,number>(100); c.set(15, 15); expect(c.has(15)).toBe(true); });
  it("lru has 16", () => { const c = new LRUCache<number,number>(100); c.set(16, 16); expect(c.has(16)).toBe(true); });
  it("lru has 17", () => { const c = new LRUCache<number,number>(100); c.set(17, 17); expect(c.has(17)).toBe(true); });
  it("lru has 18", () => { const c = new LRUCache<number,number>(100); c.set(18, 18); expect(c.has(18)).toBe(true); });
  it("lru has 19", () => { const c = new LRUCache<number,number>(100); c.set(19, 19); expect(c.has(19)).toBe(true); });
  it("lru has 20", () => { const c = new LRUCache<number,number>(100); c.set(20, 20); expect(c.has(20)).toBe(true); });
  it("lru has 21", () => { const c = new LRUCache<number,number>(100); c.set(21, 21); expect(c.has(21)).toBe(true); });
  it("lru has 22", () => { const c = new LRUCache<number,number>(100); c.set(22, 22); expect(c.has(22)).toBe(true); });
  it("lru has 23", () => { const c = new LRUCache<number,number>(100); c.set(23, 23); expect(c.has(23)).toBe(true); });
  it("lru has 24", () => { const c = new LRUCache<number,number>(100); c.set(24, 24); expect(c.has(24)).toBe(true); });
  it("lru has 25", () => { const c = new LRUCache<number,number>(100); c.set(25, 25); expect(c.has(25)).toBe(true); });
  it("lru has 26", () => { const c = new LRUCache<number,number>(100); c.set(26, 26); expect(c.has(26)).toBe(true); });
  it("lru has 27", () => { const c = new LRUCache<number,number>(100); c.set(27, 27); expect(c.has(27)).toBe(true); });
  it("lru has 28", () => { const c = new LRUCache<number,number>(100); c.set(28, 28); expect(c.has(28)).toBe(true); });
  it("lru has 29", () => { const c = new LRUCache<number,number>(100); c.set(29, 29); expect(c.has(29)).toBe(true); });
  it("lru has 30", () => { const c = new LRUCache<number,number>(100); c.set(30, 30); expect(c.has(30)).toBe(true); });
  it("lru has 31", () => { const c = new LRUCache<number,number>(100); c.set(31, 31); expect(c.has(31)).toBe(true); });
  it("lru has 32", () => { const c = new LRUCache<number,number>(100); c.set(32, 32); expect(c.has(32)).toBe(true); });
  it("lru has 33", () => { const c = new LRUCache<number,number>(100); c.set(33, 33); expect(c.has(33)).toBe(true); });
  it("lru has 34", () => { const c = new LRUCache<number,number>(100); c.set(34, 34); expect(c.has(34)).toBe(true); });
  it("lru has 35", () => { const c = new LRUCache<number,number>(100); c.set(35, 35); expect(c.has(35)).toBe(true); });
  it("lru has 36", () => { const c = new LRUCache<number,number>(100); c.set(36, 36); expect(c.has(36)).toBe(true); });
  it("lru has 37", () => { const c = new LRUCache<number,number>(100); c.set(37, 37); expect(c.has(37)).toBe(true); });
  it("lru has 38", () => { const c = new LRUCache<number,number>(100); c.set(38, 38); expect(c.has(38)).toBe(true); });
  it("lru has 39", () => { const c = new LRUCache<number,number>(100); c.set(39, 39); expect(c.has(39)).toBe(true); });
  it("lru has 40", () => { const c = new LRUCache<number,number>(100); c.set(40, 40); expect(c.has(40)).toBe(true); });
  it("lru has 41", () => { const c = new LRUCache<number,number>(100); c.set(41, 41); expect(c.has(41)).toBe(true); });
  it("lru has 42", () => { const c = new LRUCache<number,number>(100); c.set(42, 42); expect(c.has(42)).toBe(true); });
  it("lru has 43", () => { const c = new LRUCache<number,number>(100); c.set(43, 43); expect(c.has(43)).toBe(true); });
  it("lru has 44", () => { const c = new LRUCache<number,number>(100); c.set(44, 44); expect(c.has(44)).toBe(true); });
  it("lru has 45", () => { const c = new LRUCache<number,number>(100); c.set(45, 45); expect(c.has(45)).toBe(true); });
  it("lru has 46", () => { const c = new LRUCache<number,number>(100); c.set(46, 46); expect(c.has(46)).toBe(true); });
  it("lru has 47", () => { const c = new LRUCache<number,number>(100); c.set(47, 47); expect(c.has(47)).toBe(true); });
  it("lru has 48", () => { const c = new LRUCache<number,number>(100); c.set(48, 48); expect(c.has(48)).toBe(true); });
  it("lru has 49", () => { const c = new LRUCache<number,number>(100); c.set(49, 49); expect(c.has(49)).toBe(true); });
  it("lru has 50", () => { const c = new LRUCache<number,number>(100); c.set(50, 50); expect(c.has(50)).toBe(true); });
  it("lru has 51", () => { const c = new LRUCache<number,number>(100); c.set(51, 51); expect(c.has(51)).toBe(true); });
  it("lru has 52", () => { const c = new LRUCache<number,number>(100); c.set(52, 52); expect(c.has(52)).toBe(true); });
  it("lru has 53", () => { const c = new LRUCache<number,number>(100); c.set(53, 53); expect(c.has(53)).toBe(true); });
  it("lru has 54", () => { const c = new LRUCache<number,number>(100); c.set(54, 54); expect(c.has(54)).toBe(true); });
  it("lru has 55", () => { const c = new LRUCache<number,number>(100); c.set(55, 55); expect(c.has(55)).toBe(true); });
  it("lru has 56", () => { const c = new LRUCache<number,number>(100); c.set(56, 56); expect(c.has(56)).toBe(true); });
  it("lru has 57", () => { const c = new LRUCache<number,number>(100); c.set(57, 57); expect(c.has(57)).toBe(true); });
  it("lru has 58", () => { const c = new LRUCache<number,number>(100); c.set(58, 58); expect(c.has(58)).toBe(true); });
  it("lru has 59", () => { const c = new LRUCache<number,number>(100); c.set(59, 59); expect(c.has(59)).toBe(true); });
  it("lru has 60", () => { const c = new LRUCache<number,number>(100); c.set(60, 60); expect(c.has(60)).toBe(true); });
  it("lru has 61", () => { const c = new LRUCache<number,number>(100); c.set(61, 61); expect(c.has(61)).toBe(true); });
  it("lru has 62", () => { const c = new LRUCache<number,number>(100); c.set(62, 62); expect(c.has(62)).toBe(true); });
  it("lru has 63", () => { const c = new LRUCache<number,number>(100); c.set(63, 63); expect(c.has(63)).toBe(true); });
  it("lru has 64", () => { const c = new LRUCache<number,number>(100); c.set(64, 64); expect(c.has(64)).toBe(true); });
  it("lru has 65", () => { const c = new LRUCache<number,number>(100); c.set(65, 65); expect(c.has(65)).toBe(true); });
  it("lru has 66", () => { const c = new LRUCache<number,number>(100); c.set(66, 66); expect(c.has(66)).toBe(true); });
  it("lru has 67", () => { const c = new LRUCache<number,number>(100); c.set(67, 67); expect(c.has(67)).toBe(true); });
  it("lru has 68", () => { const c = new LRUCache<number,number>(100); c.set(68, 68); expect(c.has(68)).toBe(true); });
  it("lru has 69", () => { const c = new LRUCache<number,number>(100); c.set(69, 69); expect(c.has(69)).toBe(true); });
  it("lru has 70", () => { const c = new LRUCache<number,number>(100); c.set(70, 70); expect(c.has(70)).toBe(true); });
  it("lru has 71", () => { const c = new LRUCache<number,number>(100); c.set(71, 71); expect(c.has(71)).toBe(true); });
  it("lru has 72", () => { const c = new LRUCache<number,number>(100); c.set(72, 72); expect(c.has(72)).toBe(true); });
  it("lru has 73", () => { const c = new LRUCache<number,number>(100); c.set(73, 73); expect(c.has(73)).toBe(true); });
  it("lru has 74", () => { const c = new LRUCache<number,number>(100); c.set(74, 74); expect(c.has(74)).toBe(true); });
  it("lru has 75", () => { const c = new LRUCache<number,number>(100); c.set(75, 75); expect(c.has(75)).toBe(true); });
  it("lru has 76", () => { const c = new LRUCache<number,number>(100); c.set(76, 76); expect(c.has(76)).toBe(true); });
  it("lru has 77", () => { const c = new LRUCache<number,number>(100); c.set(77, 77); expect(c.has(77)).toBe(true); });
  it("lru has 78", () => { const c = new LRUCache<number,number>(100); c.set(78, 78); expect(c.has(78)).toBe(true); });
  it("lru has 79", () => { const c = new LRUCache<number,number>(100); c.set(79, 79); expect(c.has(79)).toBe(true); });
  it("lru has 80", () => { const c = new LRUCache<number,number>(100); c.set(80, 80); expect(c.has(80)).toBe(true); });
  it("lru has 81", () => { const c = new LRUCache<number,number>(100); c.set(81, 81); expect(c.has(81)).toBe(true); });
  it("lru has 82", () => { const c = new LRUCache<number,number>(100); c.set(82, 82); expect(c.has(82)).toBe(true); });
  it("lru has 83", () => { const c = new LRUCache<number,number>(100); c.set(83, 83); expect(c.has(83)).toBe(true); });
  it("lru has 84", () => { const c = new LRUCache<number,number>(100); c.set(84, 84); expect(c.has(84)).toBe(true); });
  it("lru has 85", () => { const c = new LRUCache<number,number>(100); c.set(85, 85); expect(c.has(85)).toBe(true); });
  it("lru has 86", () => { const c = new LRUCache<number,number>(100); c.set(86, 86); expect(c.has(86)).toBe(true); });
  it("lru has 87", () => { const c = new LRUCache<number,number>(100); c.set(87, 87); expect(c.has(87)).toBe(true); });
  it("lru has 88", () => { const c = new LRUCache<number,number>(100); c.set(88, 88); expect(c.has(88)).toBe(true); });
  it("lru has 89", () => { const c = new LRUCache<number,number>(100); c.set(89, 89); expect(c.has(89)).toBe(true); });
  it("lru has 90", () => { const c = new LRUCache<number,number>(100); c.set(90, 90); expect(c.has(90)).toBe(true); });
  it("lru has 91", () => { const c = new LRUCache<number,number>(100); c.set(91, 91); expect(c.has(91)).toBe(true); });
  it("lru has 92", () => { const c = new LRUCache<number,number>(100); c.set(92, 92); expect(c.has(92)).toBe(true); });
  it("lru has 93", () => { const c = new LRUCache<number,number>(100); c.set(93, 93); expect(c.has(93)).toBe(true); });
  it("lru has 94", () => { const c = new LRUCache<number,number>(100); c.set(94, 94); expect(c.has(94)).toBe(true); });
  it("lru has 95", () => { const c = new LRUCache<number,number>(100); c.set(95, 95); expect(c.has(95)).toBe(true); });
  it("lru has 96", () => { const c = new LRUCache<number,number>(100); c.set(96, 96); expect(c.has(96)).toBe(true); });
  it("lru has 97", () => { const c = new LRUCache<number,number>(100); c.set(97, 97); expect(c.has(97)).toBe(true); });
  it("lru has 98", () => { const c = new LRUCache<number,number>(100); c.set(98, 98); expect(c.has(98)).toBe(true); });
  it("lru has 99", () => { const c = new LRUCache<number,number>(100); c.set(99, 99); expect(c.has(99)).toBe(true); });
});

describe("LRUCache has missing", () => {
  it("lru has missing 0", () => { const c = new LRUCache<number,number>(10); expect(c.has(1000)).toBe(false); });
  it("lru has missing 1", () => { const c = new LRUCache<number,number>(10); expect(c.has(1001)).toBe(false); });
  it("lru has missing 2", () => { const c = new LRUCache<number,number>(10); expect(c.has(1002)).toBe(false); });
  it("lru has missing 3", () => { const c = new LRUCache<number,number>(10); expect(c.has(1003)).toBe(false); });
  it("lru has missing 4", () => { const c = new LRUCache<number,number>(10); expect(c.has(1004)).toBe(false); });
  it("lru has missing 5", () => { const c = new LRUCache<number,number>(10); expect(c.has(1005)).toBe(false); });
  it("lru has missing 6", () => { const c = new LRUCache<number,number>(10); expect(c.has(1006)).toBe(false); });
  it("lru has missing 7", () => { const c = new LRUCache<number,number>(10); expect(c.has(1007)).toBe(false); });
  it("lru has missing 8", () => { const c = new LRUCache<number,number>(10); expect(c.has(1008)).toBe(false); });
  it("lru has missing 9", () => { const c = new LRUCache<number,number>(10); expect(c.has(1009)).toBe(false); });
  it("lru has missing 10", () => { const c = new LRUCache<number,number>(10); expect(c.has(1010)).toBe(false); });
  it("lru has missing 11", () => { const c = new LRUCache<number,number>(10); expect(c.has(1011)).toBe(false); });
  it("lru has missing 12", () => { const c = new LRUCache<number,number>(10); expect(c.has(1012)).toBe(false); });
  it("lru has missing 13", () => { const c = new LRUCache<number,number>(10); expect(c.has(1013)).toBe(false); });
  it("lru has missing 14", () => { const c = new LRUCache<number,number>(10); expect(c.has(1014)).toBe(false); });
  it("lru has missing 15", () => { const c = new LRUCache<number,number>(10); expect(c.has(1015)).toBe(false); });
  it("lru has missing 16", () => { const c = new LRUCache<number,number>(10); expect(c.has(1016)).toBe(false); });
  it("lru has missing 17", () => { const c = new LRUCache<number,number>(10); expect(c.has(1017)).toBe(false); });
  it("lru has missing 18", () => { const c = new LRUCache<number,number>(10); expect(c.has(1018)).toBe(false); });
  it("lru has missing 19", () => { const c = new LRUCache<number,number>(10); expect(c.has(1019)).toBe(false); });
  it("lru has missing 20", () => { const c = new LRUCache<number,number>(10); expect(c.has(1020)).toBe(false); });
  it("lru has missing 21", () => { const c = new LRUCache<number,number>(10); expect(c.has(1021)).toBe(false); });
  it("lru has missing 22", () => { const c = new LRUCache<number,number>(10); expect(c.has(1022)).toBe(false); });
  it("lru has missing 23", () => { const c = new LRUCache<number,number>(10); expect(c.has(1023)).toBe(false); });
  it("lru has missing 24", () => { const c = new LRUCache<number,number>(10); expect(c.has(1024)).toBe(false); });
  it("lru has missing 25", () => { const c = new LRUCache<number,number>(10); expect(c.has(1025)).toBe(false); });
  it("lru has missing 26", () => { const c = new LRUCache<number,number>(10); expect(c.has(1026)).toBe(false); });
  it("lru has missing 27", () => { const c = new LRUCache<number,number>(10); expect(c.has(1027)).toBe(false); });
  it("lru has missing 28", () => { const c = new LRUCache<number,number>(10); expect(c.has(1028)).toBe(false); });
  it("lru has missing 29", () => { const c = new LRUCache<number,number>(10); expect(c.has(1029)).toBe(false); });
  it("lru has missing 30", () => { const c = new LRUCache<number,number>(10); expect(c.has(1030)).toBe(false); });
  it("lru has missing 31", () => { const c = new LRUCache<number,number>(10); expect(c.has(1031)).toBe(false); });
  it("lru has missing 32", () => { const c = new LRUCache<number,number>(10); expect(c.has(1032)).toBe(false); });
  it("lru has missing 33", () => { const c = new LRUCache<number,number>(10); expect(c.has(1033)).toBe(false); });
  it("lru has missing 34", () => { const c = new LRUCache<number,number>(10); expect(c.has(1034)).toBe(false); });
  it("lru has missing 35", () => { const c = new LRUCache<number,number>(10); expect(c.has(1035)).toBe(false); });
  it("lru has missing 36", () => { const c = new LRUCache<number,number>(10); expect(c.has(1036)).toBe(false); });
  it("lru has missing 37", () => { const c = new LRUCache<number,number>(10); expect(c.has(1037)).toBe(false); });
  it("lru has missing 38", () => { const c = new LRUCache<number,number>(10); expect(c.has(1038)).toBe(false); });
  it("lru has missing 39", () => { const c = new LRUCache<number,number>(10); expect(c.has(1039)).toBe(false); });
  it("lru has missing 40", () => { const c = new LRUCache<number,number>(10); expect(c.has(1040)).toBe(false); });
  it("lru has missing 41", () => { const c = new LRUCache<number,number>(10); expect(c.has(1041)).toBe(false); });
  it("lru has missing 42", () => { const c = new LRUCache<number,number>(10); expect(c.has(1042)).toBe(false); });
  it("lru has missing 43", () => { const c = new LRUCache<number,number>(10); expect(c.has(1043)).toBe(false); });
  it("lru has missing 44", () => { const c = new LRUCache<number,number>(10); expect(c.has(1044)).toBe(false); });
  it("lru has missing 45", () => { const c = new LRUCache<number,number>(10); expect(c.has(1045)).toBe(false); });
  it("lru has missing 46", () => { const c = new LRUCache<number,number>(10); expect(c.has(1046)).toBe(false); });
  it("lru has missing 47", () => { const c = new LRUCache<number,number>(10); expect(c.has(1047)).toBe(false); });
  it("lru has missing 48", () => { const c = new LRUCache<number,number>(10); expect(c.has(1048)).toBe(false); });
  it("lru has missing 49", () => { const c = new LRUCache<number,number>(10); expect(c.has(1049)).toBe(false); });
  it("lru has missing 50", () => { const c = new LRUCache<number,number>(10); expect(c.has(1050)).toBe(false); });
  it("lru has missing 51", () => { const c = new LRUCache<number,number>(10); expect(c.has(1051)).toBe(false); });
  it("lru has missing 52", () => { const c = new LRUCache<number,number>(10); expect(c.has(1052)).toBe(false); });
  it("lru has missing 53", () => { const c = new LRUCache<number,number>(10); expect(c.has(1053)).toBe(false); });
  it("lru has missing 54", () => { const c = new LRUCache<number,number>(10); expect(c.has(1054)).toBe(false); });
  it("lru has missing 55", () => { const c = new LRUCache<number,number>(10); expect(c.has(1055)).toBe(false); });
  it("lru has missing 56", () => { const c = new LRUCache<number,number>(10); expect(c.has(1056)).toBe(false); });
  it("lru has missing 57", () => { const c = new LRUCache<number,number>(10); expect(c.has(1057)).toBe(false); });
  it("lru has missing 58", () => { const c = new LRUCache<number,number>(10); expect(c.has(1058)).toBe(false); });
  it("lru has missing 59", () => { const c = new LRUCache<number,number>(10); expect(c.has(1059)).toBe(false); });
  it("lru has missing 60", () => { const c = new LRUCache<number,number>(10); expect(c.has(1060)).toBe(false); });
  it("lru has missing 61", () => { const c = new LRUCache<number,number>(10); expect(c.has(1061)).toBe(false); });
  it("lru has missing 62", () => { const c = new LRUCache<number,number>(10); expect(c.has(1062)).toBe(false); });
  it("lru has missing 63", () => { const c = new LRUCache<number,number>(10); expect(c.has(1063)).toBe(false); });
  it("lru has missing 64", () => { const c = new LRUCache<number,number>(10); expect(c.has(1064)).toBe(false); });
  it("lru has missing 65", () => { const c = new LRUCache<number,number>(10); expect(c.has(1065)).toBe(false); });
  it("lru has missing 66", () => { const c = new LRUCache<number,number>(10); expect(c.has(1066)).toBe(false); });
  it("lru has missing 67", () => { const c = new LRUCache<number,number>(10); expect(c.has(1067)).toBe(false); });
  it("lru has missing 68", () => { const c = new LRUCache<number,number>(10); expect(c.has(1068)).toBe(false); });
  it("lru has missing 69", () => { const c = new LRUCache<number,number>(10); expect(c.has(1069)).toBe(false); });
  it("lru has missing 70", () => { const c = new LRUCache<number,number>(10); expect(c.has(1070)).toBe(false); });
  it("lru has missing 71", () => { const c = new LRUCache<number,number>(10); expect(c.has(1071)).toBe(false); });
  it("lru has missing 72", () => { const c = new LRUCache<number,number>(10); expect(c.has(1072)).toBe(false); });
  it("lru has missing 73", () => { const c = new LRUCache<number,number>(10); expect(c.has(1073)).toBe(false); });
  it("lru has missing 74", () => { const c = new LRUCache<number,number>(10); expect(c.has(1074)).toBe(false); });
  it("lru has missing 75", () => { const c = new LRUCache<number,number>(10); expect(c.has(1075)).toBe(false); });
  it("lru has missing 76", () => { const c = new LRUCache<number,number>(10); expect(c.has(1076)).toBe(false); });
  it("lru has missing 77", () => { const c = new LRUCache<number,number>(10); expect(c.has(1077)).toBe(false); });
  it("lru has missing 78", () => { const c = new LRUCache<number,number>(10); expect(c.has(1078)).toBe(false); });
  it("lru has missing 79", () => { const c = new LRUCache<number,number>(10); expect(c.has(1079)).toBe(false); });
  it("lru has missing 80", () => { const c = new LRUCache<number,number>(10); expect(c.has(1080)).toBe(false); });
  it("lru has missing 81", () => { const c = new LRUCache<number,number>(10); expect(c.has(1081)).toBe(false); });
  it("lru has missing 82", () => { const c = new LRUCache<number,number>(10); expect(c.has(1082)).toBe(false); });
  it("lru has missing 83", () => { const c = new LRUCache<number,number>(10); expect(c.has(1083)).toBe(false); });
  it("lru has missing 84", () => { const c = new LRUCache<number,number>(10); expect(c.has(1084)).toBe(false); });
  it("lru has missing 85", () => { const c = new LRUCache<number,number>(10); expect(c.has(1085)).toBe(false); });
  it("lru has missing 86", () => { const c = new LRUCache<number,number>(10); expect(c.has(1086)).toBe(false); });
  it("lru has missing 87", () => { const c = new LRUCache<number,number>(10); expect(c.has(1087)).toBe(false); });
  it("lru has missing 88", () => { const c = new LRUCache<number,number>(10); expect(c.has(1088)).toBe(false); });
  it("lru has missing 89", () => { const c = new LRUCache<number,number>(10); expect(c.has(1089)).toBe(false); });
  it("lru has missing 90", () => { const c = new LRUCache<number,number>(10); expect(c.has(1090)).toBe(false); });
  it("lru has missing 91", () => { const c = new LRUCache<number,number>(10); expect(c.has(1091)).toBe(false); });
  it("lru has missing 92", () => { const c = new LRUCache<number,number>(10); expect(c.has(1092)).toBe(false); });
  it("lru has missing 93", () => { const c = new LRUCache<number,number>(10); expect(c.has(1093)).toBe(false); });
  it("lru has missing 94", () => { const c = new LRUCache<number,number>(10); expect(c.has(1094)).toBe(false); });
  it("lru has missing 95", () => { const c = new LRUCache<number,number>(10); expect(c.has(1095)).toBe(false); });
  it("lru has missing 96", () => { const c = new LRUCache<number,number>(10); expect(c.has(1096)).toBe(false); });
  it("lru has missing 97", () => { const c = new LRUCache<number,number>(10); expect(c.has(1097)).toBe(false); });
  it("lru has missing 98", () => { const c = new LRUCache<number,number>(10); expect(c.has(1098)).toBe(false); });
  it("lru has missing 99", () => { const c = new LRUCache<number,number>(10); expect(c.has(1099)).toBe(false); });
});

describe("LRUCache delete", () => {
  it("lru delete 0", () => { const c = new LRUCache<number,number>(100); c.set(0, 0); expect(c.delete(0)).toBe(true); expect(c.has(0)).toBe(false); });
  it("lru delete 1", () => { const c = new LRUCache<number,number>(100); c.set(1, 1); expect(c.delete(1)).toBe(true); expect(c.has(1)).toBe(false); });
  it("lru delete 2", () => { const c = new LRUCache<number,number>(100); c.set(2, 2); expect(c.delete(2)).toBe(true); expect(c.has(2)).toBe(false); });
  it("lru delete 3", () => { const c = new LRUCache<number,number>(100); c.set(3, 3); expect(c.delete(3)).toBe(true); expect(c.has(3)).toBe(false); });
  it("lru delete 4", () => { const c = new LRUCache<number,number>(100); c.set(4, 4); expect(c.delete(4)).toBe(true); expect(c.has(4)).toBe(false); });
  it("lru delete 5", () => { const c = new LRUCache<number,number>(100); c.set(5, 5); expect(c.delete(5)).toBe(true); expect(c.has(5)).toBe(false); });
  it("lru delete 6", () => { const c = new LRUCache<number,number>(100); c.set(6, 6); expect(c.delete(6)).toBe(true); expect(c.has(6)).toBe(false); });
  it("lru delete 7", () => { const c = new LRUCache<number,number>(100); c.set(7, 7); expect(c.delete(7)).toBe(true); expect(c.has(7)).toBe(false); });
  it("lru delete 8", () => { const c = new LRUCache<number,number>(100); c.set(8, 8); expect(c.delete(8)).toBe(true); expect(c.has(8)).toBe(false); });
  it("lru delete 9", () => { const c = new LRUCache<number,number>(100); c.set(9, 9); expect(c.delete(9)).toBe(true); expect(c.has(9)).toBe(false); });
  it("lru delete 10", () => { const c = new LRUCache<number,number>(100); c.set(10, 10); expect(c.delete(10)).toBe(true); expect(c.has(10)).toBe(false); });
  it("lru delete 11", () => { const c = new LRUCache<number,number>(100); c.set(11, 11); expect(c.delete(11)).toBe(true); expect(c.has(11)).toBe(false); });
  it("lru delete 12", () => { const c = new LRUCache<number,number>(100); c.set(12, 12); expect(c.delete(12)).toBe(true); expect(c.has(12)).toBe(false); });
  it("lru delete 13", () => { const c = new LRUCache<number,number>(100); c.set(13, 13); expect(c.delete(13)).toBe(true); expect(c.has(13)).toBe(false); });
  it("lru delete 14", () => { const c = new LRUCache<number,number>(100); c.set(14, 14); expect(c.delete(14)).toBe(true); expect(c.has(14)).toBe(false); });
  it("lru delete 15", () => { const c = new LRUCache<number,number>(100); c.set(15, 15); expect(c.delete(15)).toBe(true); expect(c.has(15)).toBe(false); });
  it("lru delete 16", () => { const c = new LRUCache<number,number>(100); c.set(16, 16); expect(c.delete(16)).toBe(true); expect(c.has(16)).toBe(false); });
  it("lru delete 17", () => { const c = new LRUCache<number,number>(100); c.set(17, 17); expect(c.delete(17)).toBe(true); expect(c.has(17)).toBe(false); });
  it("lru delete 18", () => { const c = new LRUCache<number,number>(100); c.set(18, 18); expect(c.delete(18)).toBe(true); expect(c.has(18)).toBe(false); });
  it("lru delete 19", () => { const c = new LRUCache<number,number>(100); c.set(19, 19); expect(c.delete(19)).toBe(true); expect(c.has(19)).toBe(false); });
  it("lru delete 20", () => { const c = new LRUCache<number,number>(100); c.set(20, 20); expect(c.delete(20)).toBe(true); expect(c.has(20)).toBe(false); });
  it("lru delete 21", () => { const c = new LRUCache<number,number>(100); c.set(21, 21); expect(c.delete(21)).toBe(true); expect(c.has(21)).toBe(false); });
  it("lru delete 22", () => { const c = new LRUCache<number,number>(100); c.set(22, 22); expect(c.delete(22)).toBe(true); expect(c.has(22)).toBe(false); });
  it("lru delete 23", () => { const c = new LRUCache<number,number>(100); c.set(23, 23); expect(c.delete(23)).toBe(true); expect(c.has(23)).toBe(false); });
  it("lru delete 24", () => { const c = new LRUCache<number,number>(100); c.set(24, 24); expect(c.delete(24)).toBe(true); expect(c.has(24)).toBe(false); });
  it("lru delete 25", () => { const c = new LRUCache<number,number>(100); c.set(25, 25); expect(c.delete(25)).toBe(true); expect(c.has(25)).toBe(false); });
  it("lru delete 26", () => { const c = new LRUCache<number,number>(100); c.set(26, 26); expect(c.delete(26)).toBe(true); expect(c.has(26)).toBe(false); });
  it("lru delete 27", () => { const c = new LRUCache<number,number>(100); c.set(27, 27); expect(c.delete(27)).toBe(true); expect(c.has(27)).toBe(false); });
  it("lru delete 28", () => { const c = new LRUCache<number,number>(100); c.set(28, 28); expect(c.delete(28)).toBe(true); expect(c.has(28)).toBe(false); });
  it("lru delete 29", () => { const c = new LRUCache<number,number>(100); c.set(29, 29); expect(c.delete(29)).toBe(true); expect(c.has(29)).toBe(false); });
  it("lru delete 30", () => { const c = new LRUCache<number,number>(100); c.set(30, 30); expect(c.delete(30)).toBe(true); expect(c.has(30)).toBe(false); });
  it("lru delete 31", () => { const c = new LRUCache<number,number>(100); c.set(31, 31); expect(c.delete(31)).toBe(true); expect(c.has(31)).toBe(false); });
  it("lru delete 32", () => { const c = new LRUCache<number,number>(100); c.set(32, 32); expect(c.delete(32)).toBe(true); expect(c.has(32)).toBe(false); });
  it("lru delete 33", () => { const c = new LRUCache<number,number>(100); c.set(33, 33); expect(c.delete(33)).toBe(true); expect(c.has(33)).toBe(false); });
  it("lru delete 34", () => { const c = new LRUCache<number,number>(100); c.set(34, 34); expect(c.delete(34)).toBe(true); expect(c.has(34)).toBe(false); });
  it("lru delete 35", () => { const c = new LRUCache<number,number>(100); c.set(35, 35); expect(c.delete(35)).toBe(true); expect(c.has(35)).toBe(false); });
  it("lru delete 36", () => { const c = new LRUCache<number,number>(100); c.set(36, 36); expect(c.delete(36)).toBe(true); expect(c.has(36)).toBe(false); });
  it("lru delete 37", () => { const c = new LRUCache<number,number>(100); c.set(37, 37); expect(c.delete(37)).toBe(true); expect(c.has(37)).toBe(false); });
  it("lru delete 38", () => { const c = new LRUCache<number,number>(100); c.set(38, 38); expect(c.delete(38)).toBe(true); expect(c.has(38)).toBe(false); });
  it("lru delete 39", () => { const c = new LRUCache<number,number>(100); c.set(39, 39); expect(c.delete(39)).toBe(true); expect(c.has(39)).toBe(false); });
  it("lru delete 40", () => { const c = new LRUCache<number,number>(100); c.set(40, 40); expect(c.delete(40)).toBe(true); expect(c.has(40)).toBe(false); });
  it("lru delete 41", () => { const c = new LRUCache<number,number>(100); c.set(41, 41); expect(c.delete(41)).toBe(true); expect(c.has(41)).toBe(false); });
  it("lru delete 42", () => { const c = new LRUCache<number,number>(100); c.set(42, 42); expect(c.delete(42)).toBe(true); expect(c.has(42)).toBe(false); });
  it("lru delete 43", () => { const c = new LRUCache<number,number>(100); c.set(43, 43); expect(c.delete(43)).toBe(true); expect(c.has(43)).toBe(false); });
  it("lru delete 44", () => { const c = new LRUCache<number,number>(100); c.set(44, 44); expect(c.delete(44)).toBe(true); expect(c.has(44)).toBe(false); });
  it("lru delete 45", () => { const c = new LRUCache<number,number>(100); c.set(45, 45); expect(c.delete(45)).toBe(true); expect(c.has(45)).toBe(false); });
  it("lru delete 46", () => { const c = new LRUCache<number,number>(100); c.set(46, 46); expect(c.delete(46)).toBe(true); expect(c.has(46)).toBe(false); });
  it("lru delete 47", () => { const c = new LRUCache<number,number>(100); c.set(47, 47); expect(c.delete(47)).toBe(true); expect(c.has(47)).toBe(false); });
  it("lru delete 48", () => { const c = new LRUCache<number,number>(100); c.set(48, 48); expect(c.delete(48)).toBe(true); expect(c.has(48)).toBe(false); });
  it("lru delete 49", () => { const c = new LRUCache<number,number>(100); c.set(49, 49); expect(c.delete(49)).toBe(true); expect(c.has(49)).toBe(false); });
  it("lru delete 50", () => { const c = new LRUCache<number,number>(100); c.set(50, 50); expect(c.delete(50)).toBe(true); expect(c.has(50)).toBe(false); });
  it("lru delete 51", () => { const c = new LRUCache<number,number>(100); c.set(51, 51); expect(c.delete(51)).toBe(true); expect(c.has(51)).toBe(false); });
  it("lru delete 52", () => { const c = new LRUCache<number,number>(100); c.set(52, 52); expect(c.delete(52)).toBe(true); expect(c.has(52)).toBe(false); });
  it("lru delete 53", () => { const c = new LRUCache<number,number>(100); c.set(53, 53); expect(c.delete(53)).toBe(true); expect(c.has(53)).toBe(false); });
  it("lru delete 54", () => { const c = new LRUCache<number,number>(100); c.set(54, 54); expect(c.delete(54)).toBe(true); expect(c.has(54)).toBe(false); });
  it("lru delete 55", () => { const c = new LRUCache<number,number>(100); c.set(55, 55); expect(c.delete(55)).toBe(true); expect(c.has(55)).toBe(false); });
  it("lru delete 56", () => { const c = new LRUCache<number,number>(100); c.set(56, 56); expect(c.delete(56)).toBe(true); expect(c.has(56)).toBe(false); });
  it("lru delete 57", () => { const c = new LRUCache<number,number>(100); c.set(57, 57); expect(c.delete(57)).toBe(true); expect(c.has(57)).toBe(false); });
  it("lru delete 58", () => { const c = new LRUCache<number,number>(100); c.set(58, 58); expect(c.delete(58)).toBe(true); expect(c.has(58)).toBe(false); });
  it("lru delete 59", () => { const c = new LRUCache<number,number>(100); c.set(59, 59); expect(c.delete(59)).toBe(true); expect(c.has(59)).toBe(false); });
  it("lru delete 60", () => { const c = new LRUCache<number,number>(100); c.set(60, 60); expect(c.delete(60)).toBe(true); expect(c.has(60)).toBe(false); });
  it("lru delete 61", () => { const c = new LRUCache<number,number>(100); c.set(61, 61); expect(c.delete(61)).toBe(true); expect(c.has(61)).toBe(false); });
  it("lru delete 62", () => { const c = new LRUCache<number,number>(100); c.set(62, 62); expect(c.delete(62)).toBe(true); expect(c.has(62)).toBe(false); });
  it("lru delete 63", () => { const c = new LRUCache<number,number>(100); c.set(63, 63); expect(c.delete(63)).toBe(true); expect(c.has(63)).toBe(false); });
  it("lru delete 64", () => { const c = new LRUCache<number,number>(100); c.set(64, 64); expect(c.delete(64)).toBe(true); expect(c.has(64)).toBe(false); });
  it("lru delete 65", () => { const c = new LRUCache<number,number>(100); c.set(65, 65); expect(c.delete(65)).toBe(true); expect(c.has(65)).toBe(false); });
  it("lru delete 66", () => { const c = new LRUCache<number,number>(100); c.set(66, 66); expect(c.delete(66)).toBe(true); expect(c.has(66)).toBe(false); });
  it("lru delete 67", () => { const c = new LRUCache<number,number>(100); c.set(67, 67); expect(c.delete(67)).toBe(true); expect(c.has(67)).toBe(false); });
  it("lru delete 68", () => { const c = new LRUCache<number,number>(100); c.set(68, 68); expect(c.delete(68)).toBe(true); expect(c.has(68)).toBe(false); });
  it("lru delete 69", () => { const c = new LRUCache<number,number>(100); c.set(69, 69); expect(c.delete(69)).toBe(true); expect(c.has(69)).toBe(false); });
  it("lru delete 70", () => { const c = new LRUCache<number,number>(100); c.set(70, 70); expect(c.delete(70)).toBe(true); expect(c.has(70)).toBe(false); });
  it("lru delete 71", () => { const c = new LRUCache<number,number>(100); c.set(71, 71); expect(c.delete(71)).toBe(true); expect(c.has(71)).toBe(false); });
  it("lru delete 72", () => { const c = new LRUCache<number,number>(100); c.set(72, 72); expect(c.delete(72)).toBe(true); expect(c.has(72)).toBe(false); });
  it("lru delete 73", () => { const c = new LRUCache<number,number>(100); c.set(73, 73); expect(c.delete(73)).toBe(true); expect(c.has(73)).toBe(false); });
  it("lru delete 74", () => { const c = new LRUCache<number,number>(100); c.set(74, 74); expect(c.delete(74)).toBe(true); expect(c.has(74)).toBe(false); });
  it("lru delete 75", () => { const c = new LRUCache<number,number>(100); c.set(75, 75); expect(c.delete(75)).toBe(true); expect(c.has(75)).toBe(false); });
  it("lru delete 76", () => { const c = new LRUCache<number,number>(100); c.set(76, 76); expect(c.delete(76)).toBe(true); expect(c.has(76)).toBe(false); });
  it("lru delete 77", () => { const c = new LRUCache<number,number>(100); c.set(77, 77); expect(c.delete(77)).toBe(true); expect(c.has(77)).toBe(false); });
  it("lru delete 78", () => { const c = new LRUCache<number,number>(100); c.set(78, 78); expect(c.delete(78)).toBe(true); expect(c.has(78)).toBe(false); });
  it("lru delete 79", () => { const c = new LRUCache<number,number>(100); c.set(79, 79); expect(c.delete(79)).toBe(true); expect(c.has(79)).toBe(false); });
  it("lru delete 80", () => { const c = new LRUCache<number,number>(100); c.set(80, 80); expect(c.delete(80)).toBe(true); expect(c.has(80)).toBe(false); });
  it("lru delete 81", () => { const c = new LRUCache<number,number>(100); c.set(81, 81); expect(c.delete(81)).toBe(true); expect(c.has(81)).toBe(false); });
  it("lru delete 82", () => { const c = new LRUCache<number,number>(100); c.set(82, 82); expect(c.delete(82)).toBe(true); expect(c.has(82)).toBe(false); });
  it("lru delete 83", () => { const c = new LRUCache<number,number>(100); c.set(83, 83); expect(c.delete(83)).toBe(true); expect(c.has(83)).toBe(false); });
  it("lru delete 84", () => { const c = new LRUCache<number,number>(100); c.set(84, 84); expect(c.delete(84)).toBe(true); expect(c.has(84)).toBe(false); });
  it("lru delete 85", () => { const c = new LRUCache<number,number>(100); c.set(85, 85); expect(c.delete(85)).toBe(true); expect(c.has(85)).toBe(false); });
  it("lru delete 86", () => { const c = new LRUCache<number,number>(100); c.set(86, 86); expect(c.delete(86)).toBe(true); expect(c.has(86)).toBe(false); });
  it("lru delete 87", () => { const c = new LRUCache<number,number>(100); c.set(87, 87); expect(c.delete(87)).toBe(true); expect(c.has(87)).toBe(false); });
  it("lru delete 88", () => { const c = new LRUCache<number,number>(100); c.set(88, 88); expect(c.delete(88)).toBe(true); expect(c.has(88)).toBe(false); });
  it("lru delete 89", () => { const c = new LRUCache<number,number>(100); c.set(89, 89); expect(c.delete(89)).toBe(true); expect(c.has(89)).toBe(false); });
  it("lru delete 90", () => { const c = new LRUCache<number,number>(100); c.set(90, 90); expect(c.delete(90)).toBe(true); expect(c.has(90)).toBe(false); });
  it("lru delete 91", () => { const c = new LRUCache<number,number>(100); c.set(91, 91); expect(c.delete(91)).toBe(true); expect(c.has(91)).toBe(false); });
  it("lru delete 92", () => { const c = new LRUCache<number,number>(100); c.set(92, 92); expect(c.delete(92)).toBe(true); expect(c.has(92)).toBe(false); });
  it("lru delete 93", () => { const c = new LRUCache<number,number>(100); c.set(93, 93); expect(c.delete(93)).toBe(true); expect(c.has(93)).toBe(false); });
  it("lru delete 94", () => { const c = new LRUCache<number,number>(100); c.set(94, 94); expect(c.delete(94)).toBe(true); expect(c.has(94)).toBe(false); });
  it("lru delete 95", () => { const c = new LRUCache<number,number>(100); c.set(95, 95); expect(c.delete(95)).toBe(true); expect(c.has(95)).toBe(false); });
  it("lru delete 96", () => { const c = new LRUCache<number,number>(100); c.set(96, 96); expect(c.delete(96)).toBe(true); expect(c.has(96)).toBe(false); });
  it("lru delete 97", () => { const c = new LRUCache<number,number>(100); c.set(97, 97); expect(c.delete(97)).toBe(true); expect(c.has(97)).toBe(false); });
  it("lru delete 98", () => { const c = new LRUCache<number,number>(100); c.set(98, 98); expect(c.delete(98)).toBe(true); expect(c.has(98)).toBe(false); });
  it("lru delete 99", () => { const c = new LRUCache<number,number>(100); c.set(99, 99); expect(c.delete(99)).toBe(true); expect(c.has(99)).toBe(false); });
});

describe("LRUCache size", () => {
  it("lru size 1", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<1;i++) c.set(i,i); expect(c.size).toBe(1); });
  it("lru size 2", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<2;i++) c.set(i,i); expect(c.size).toBe(2); });
  it("lru size 3", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<3;i++) c.set(i,i); expect(c.size).toBe(3); });
  it("lru size 4", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<4;i++) c.set(i,i); expect(c.size).toBe(4); });
  it("lru size 5", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<5;i++) c.set(i,i); expect(c.size).toBe(5); });
  it("lru size 6", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<6;i++) c.set(i,i); expect(c.size).toBe(6); });
  it("lru size 7", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<7;i++) c.set(i,i); expect(c.size).toBe(7); });
  it("lru size 8", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<8;i++) c.set(i,i); expect(c.size).toBe(8); });
  it("lru size 9", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<9;i++) c.set(i,i); expect(c.size).toBe(9); });
  it("lru size 10", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<10;i++) c.set(i,i); expect(c.size).toBe(10); });
  it("lru size 11", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<11;i++) c.set(i,i); expect(c.size).toBe(11); });
  it("lru size 12", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<12;i++) c.set(i,i); expect(c.size).toBe(12); });
  it("lru size 13", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<13;i++) c.set(i,i); expect(c.size).toBe(13); });
  it("lru size 14", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<14;i++) c.set(i,i); expect(c.size).toBe(14); });
  it("lru size 15", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<15;i++) c.set(i,i); expect(c.size).toBe(15); });
  it("lru size 16", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<16;i++) c.set(i,i); expect(c.size).toBe(16); });
  it("lru size 17", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<17;i++) c.set(i,i); expect(c.size).toBe(17); });
  it("lru size 18", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<18;i++) c.set(i,i); expect(c.size).toBe(18); });
  it("lru size 19", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<19;i++) c.set(i,i); expect(c.size).toBe(19); });
  it("lru size 20", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<20;i++) c.set(i,i); expect(c.size).toBe(20); });
  it("lru size 21", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<21;i++) c.set(i,i); expect(c.size).toBe(21); });
  it("lru size 22", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<22;i++) c.set(i,i); expect(c.size).toBe(22); });
  it("lru size 23", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<23;i++) c.set(i,i); expect(c.size).toBe(23); });
  it("lru size 24", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<24;i++) c.set(i,i); expect(c.size).toBe(24); });
  it("lru size 25", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<25;i++) c.set(i,i); expect(c.size).toBe(25); });
  it("lru size 26", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<26;i++) c.set(i,i); expect(c.size).toBe(26); });
  it("lru size 27", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<27;i++) c.set(i,i); expect(c.size).toBe(27); });
  it("lru size 28", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<28;i++) c.set(i,i); expect(c.size).toBe(28); });
  it("lru size 29", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<29;i++) c.set(i,i); expect(c.size).toBe(29); });
  it("lru size 30", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<30;i++) c.set(i,i); expect(c.size).toBe(30); });
  it("lru size 31", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<31;i++) c.set(i,i); expect(c.size).toBe(31); });
  it("lru size 32", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<32;i++) c.set(i,i); expect(c.size).toBe(32); });
  it("lru size 33", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<33;i++) c.set(i,i); expect(c.size).toBe(33); });
  it("lru size 34", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<34;i++) c.set(i,i); expect(c.size).toBe(34); });
  it("lru size 35", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<35;i++) c.set(i,i); expect(c.size).toBe(35); });
  it("lru size 36", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<36;i++) c.set(i,i); expect(c.size).toBe(36); });
  it("lru size 37", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<37;i++) c.set(i,i); expect(c.size).toBe(37); });
  it("lru size 38", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<38;i++) c.set(i,i); expect(c.size).toBe(38); });
  it("lru size 39", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<39;i++) c.set(i,i); expect(c.size).toBe(39); });
  it("lru size 40", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<40;i++) c.set(i,i); expect(c.size).toBe(40); });
  it("lru size 41", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<41;i++) c.set(i,i); expect(c.size).toBe(41); });
  it("lru size 42", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<42;i++) c.set(i,i); expect(c.size).toBe(42); });
  it("lru size 43", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<43;i++) c.set(i,i); expect(c.size).toBe(43); });
  it("lru size 44", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<44;i++) c.set(i,i); expect(c.size).toBe(44); });
  it("lru size 45", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<45;i++) c.set(i,i); expect(c.size).toBe(45); });
  it("lru size 46", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<46;i++) c.set(i,i); expect(c.size).toBe(46); });
  it("lru size 47", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<47;i++) c.set(i,i); expect(c.size).toBe(47); });
  it("lru size 48", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<48;i++) c.set(i,i); expect(c.size).toBe(48); });
  it("lru size 49", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<49;i++) c.set(i,i); expect(c.size).toBe(49); });
  it("lru size 50", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<50;i++) c.set(i,i); expect(c.size).toBe(50); });
  it("lru size 51", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<51;i++) c.set(i,i); expect(c.size).toBe(51); });
  it("lru size 52", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<52;i++) c.set(i,i); expect(c.size).toBe(52); });
  it("lru size 53", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<53;i++) c.set(i,i); expect(c.size).toBe(53); });
  it("lru size 54", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<54;i++) c.set(i,i); expect(c.size).toBe(54); });
  it("lru size 55", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<55;i++) c.set(i,i); expect(c.size).toBe(55); });
  it("lru size 56", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<56;i++) c.set(i,i); expect(c.size).toBe(56); });
  it("lru size 57", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<57;i++) c.set(i,i); expect(c.size).toBe(57); });
  it("lru size 58", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<58;i++) c.set(i,i); expect(c.size).toBe(58); });
  it("lru size 59", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<59;i++) c.set(i,i); expect(c.size).toBe(59); });
  it("lru size 60", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<60;i++) c.set(i,i); expect(c.size).toBe(60); });
  it("lru size 61", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<61;i++) c.set(i,i); expect(c.size).toBe(61); });
  it("lru size 62", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<62;i++) c.set(i,i); expect(c.size).toBe(62); });
  it("lru size 63", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<63;i++) c.set(i,i); expect(c.size).toBe(63); });
  it("lru size 64", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<64;i++) c.set(i,i); expect(c.size).toBe(64); });
  it("lru size 65", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<65;i++) c.set(i,i); expect(c.size).toBe(65); });
  it("lru size 66", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<66;i++) c.set(i,i); expect(c.size).toBe(66); });
  it("lru size 67", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<67;i++) c.set(i,i); expect(c.size).toBe(67); });
  it("lru size 68", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<68;i++) c.set(i,i); expect(c.size).toBe(68); });
  it("lru size 69", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<69;i++) c.set(i,i); expect(c.size).toBe(69); });
  it("lru size 70", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<70;i++) c.set(i,i); expect(c.size).toBe(70); });
  it("lru size 71", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<71;i++) c.set(i,i); expect(c.size).toBe(71); });
  it("lru size 72", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<72;i++) c.set(i,i); expect(c.size).toBe(72); });
  it("lru size 73", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<73;i++) c.set(i,i); expect(c.size).toBe(73); });
  it("lru size 74", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<74;i++) c.set(i,i); expect(c.size).toBe(74); });
  it("lru size 75", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<75;i++) c.set(i,i); expect(c.size).toBe(75); });
  it("lru size 76", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<76;i++) c.set(i,i); expect(c.size).toBe(76); });
  it("lru size 77", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<77;i++) c.set(i,i); expect(c.size).toBe(77); });
  it("lru size 78", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<78;i++) c.set(i,i); expect(c.size).toBe(78); });
  it("lru size 79", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<79;i++) c.set(i,i); expect(c.size).toBe(79); });
  it("lru size 80", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<80;i++) c.set(i,i); expect(c.size).toBe(80); });
  it("lru size 81", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<81;i++) c.set(i,i); expect(c.size).toBe(81); });
  it("lru size 82", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<82;i++) c.set(i,i); expect(c.size).toBe(82); });
  it("lru size 83", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<83;i++) c.set(i,i); expect(c.size).toBe(83); });
  it("lru size 84", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<84;i++) c.set(i,i); expect(c.size).toBe(84); });
  it("lru size 85", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<85;i++) c.set(i,i); expect(c.size).toBe(85); });
  it("lru size 86", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<86;i++) c.set(i,i); expect(c.size).toBe(86); });
  it("lru size 87", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<87;i++) c.set(i,i); expect(c.size).toBe(87); });
  it("lru size 88", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<88;i++) c.set(i,i); expect(c.size).toBe(88); });
  it("lru size 89", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<89;i++) c.set(i,i); expect(c.size).toBe(89); });
  it("lru size 90", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<90;i++) c.set(i,i); expect(c.size).toBe(90); });
  it("lru size 91", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<91;i++) c.set(i,i); expect(c.size).toBe(91); });
  it("lru size 92", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<92;i++) c.set(i,i); expect(c.size).toBe(92); });
  it("lru size 93", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<93;i++) c.set(i,i); expect(c.size).toBe(93); });
  it("lru size 94", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<94;i++) c.set(i,i); expect(c.size).toBe(94); });
  it("lru size 95", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<95;i++) c.set(i,i); expect(c.size).toBe(95); });
  it("lru size 96", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<96;i++) c.set(i,i); expect(c.size).toBe(96); });
  it("lru size 97", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<97;i++) c.set(i,i); expect(c.size).toBe(97); });
  it("lru size 98", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<98;i++) c.set(i,i); expect(c.size).toBe(98); });
  it("lru size 99", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<99;i++) c.set(i,i); expect(c.size).toBe(99); });
  it("lru size 100", () => { const c = new LRUCache<number,number>(200); for (let i=0;i<100;i++) c.set(i,i); expect(c.size).toBe(100); });
});

describe("LRUCache eviction", () => {
  it("lru evict cap 1", () => { const c = new LRUCache<number,number>(1); for (let i=0;i<6;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(1); });
  it("lru evict cap 2", () => { const c = new LRUCache<number,number>(2); for (let i=0;i<7;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(2); });
  it("lru evict cap 3", () => { const c = new LRUCache<number,number>(3); for (let i=0;i<8;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(3); });
  it("lru evict cap 4", () => { const c = new LRUCache<number,number>(4); for (let i=0;i<9;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(4); });
  it("lru evict cap 5", () => { const c = new LRUCache<number,number>(5); for (let i=0;i<10;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(5); });
  it("lru evict cap 6", () => { const c = new LRUCache<number,number>(6); for (let i=0;i<11;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(6); });
  it("lru evict cap 7", () => { const c = new LRUCache<number,number>(7); for (let i=0;i<12;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(7); });
  it("lru evict cap 8", () => { const c = new LRUCache<number,number>(8); for (let i=0;i<13;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(8); });
  it("lru evict cap 9", () => { const c = new LRUCache<number,number>(9); for (let i=0;i<14;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(9); });
  it("lru evict cap 10", () => { const c = new LRUCache<number,number>(10); for (let i=0;i<15;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(10); });
  it("lru evict cap 11", () => { const c = new LRUCache<number,number>(11); for (let i=0;i<16;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(11); });
  it("lru evict cap 12", () => { const c = new LRUCache<number,number>(12); for (let i=0;i<17;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(12); });
  it("lru evict cap 13", () => { const c = new LRUCache<number,number>(13); for (let i=0;i<18;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(13); });
  it("lru evict cap 14", () => { const c = new LRUCache<number,number>(14); for (let i=0;i<19;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(14); });
  it("lru evict cap 15", () => { const c = new LRUCache<number,number>(15); for (let i=0;i<20;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(15); });
  it("lru evict cap 16", () => { const c = new LRUCache<number,number>(16); for (let i=0;i<21;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(16); });
  it("lru evict cap 17", () => { const c = new LRUCache<number,number>(17); for (let i=0;i<22;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(17); });
  it("lru evict cap 18", () => { const c = new LRUCache<number,number>(18); for (let i=0;i<23;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(18); });
  it("lru evict cap 19", () => { const c = new LRUCache<number,number>(19); for (let i=0;i<24;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(19); });
  it("lru evict cap 20", () => { const c = new LRUCache<number,number>(20); for (let i=0;i<25;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(20); });
  it("lru evict cap 21", () => { const c = new LRUCache<number,number>(21); for (let i=0;i<26;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(21); });
  it("lru evict cap 22", () => { const c = new LRUCache<number,number>(22); for (let i=0;i<27;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(22); });
  it("lru evict cap 23", () => { const c = new LRUCache<number,number>(23); for (let i=0;i<28;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(23); });
  it("lru evict cap 24", () => { const c = new LRUCache<number,number>(24); for (let i=0;i<29;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(24); });
  it("lru evict cap 25", () => { const c = new LRUCache<number,number>(25); for (let i=0;i<30;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(25); });
  it("lru evict cap 26", () => { const c = new LRUCache<number,number>(26); for (let i=0;i<31;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(26); });
  it("lru evict cap 27", () => { const c = new LRUCache<number,number>(27); for (let i=0;i<32;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(27); });
  it("lru evict cap 28", () => { const c = new LRUCache<number,number>(28); for (let i=0;i<33;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(28); });
  it("lru evict cap 29", () => { const c = new LRUCache<number,number>(29); for (let i=0;i<34;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(29); });
  it("lru evict cap 30", () => { const c = new LRUCache<number,number>(30); for (let i=0;i<35;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(30); });
  it("lru evict cap 31", () => { const c = new LRUCache<number,number>(31); for (let i=0;i<36;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(31); });
  it("lru evict cap 32", () => { const c = new LRUCache<number,number>(32); for (let i=0;i<37;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(32); });
  it("lru evict cap 33", () => { const c = new LRUCache<number,number>(33); for (let i=0;i<38;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(33); });
  it("lru evict cap 34", () => { const c = new LRUCache<number,number>(34); for (let i=0;i<39;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(34); });
  it("lru evict cap 35", () => { const c = new LRUCache<number,number>(35); for (let i=0;i<40;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(35); });
  it("lru evict cap 36", () => { const c = new LRUCache<number,number>(36); for (let i=0;i<41;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(36); });
  it("lru evict cap 37", () => { const c = new LRUCache<number,number>(37); for (let i=0;i<42;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(37); });
  it("lru evict cap 38", () => { const c = new LRUCache<number,number>(38); for (let i=0;i<43;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(38); });
  it("lru evict cap 39", () => { const c = new LRUCache<number,number>(39); for (let i=0;i<44;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(39); });
  it("lru evict cap 40", () => { const c = new LRUCache<number,number>(40); for (let i=0;i<45;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(40); });
  it("lru evict cap 41", () => { const c = new LRUCache<number,number>(41); for (let i=0;i<46;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(41); });
  it("lru evict cap 42", () => { const c = new LRUCache<number,number>(42); for (let i=0;i<47;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(42); });
  it("lru evict cap 43", () => { const c = new LRUCache<number,number>(43); for (let i=0;i<48;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(43); });
  it("lru evict cap 44", () => { const c = new LRUCache<number,number>(44); for (let i=0;i<49;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(44); });
  it("lru evict cap 45", () => { const c = new LRUCache<number,number>(45); for (let i=0;i<50;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(45); });
  it("lru evict cap 46", () => { const c = new LRUCache<number,number>(46); for (let i=0;i<51;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(46); });
  it("lru evict cap 47", () => { const c = new LRUCache<number,number>(47); for (let i=0;i<52;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(47); });
  it("lru evict cap 48", () => { const c = new LRUCache<number,number>(48); for (let i=0;i<53;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(48); });
  it("lru evict cap 49", () => { const c = new LRUCache<number,number>(49); for (let i=0;i<54;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(49); });
  it("lru evict cap 50", () => { const c = new LRUCache<number,number>(50); for (let i=0;i<55;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(50); });
  it("lru evict cap 51", () => { const c = new LRUCache<number,number>(51); for (let i=0;i<56;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(51); });
  it("lru evict cap 52", () => { const c = new LRUCache<number,number>(52); for (let i=0;i<57;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(52); });
  it("lru evict cap 53", () => { const c = new LRUCache<number,number>(53); for (let i=0;i<58;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(53); });
  it("lru evict cap 54", () => { const c = new LRUCache<number,number>(54); for (let i=0;i<59;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(54); });
  it("lru evict cap 55", () => { const c = new LRUCache<number,number>(55); for (let i=0;i<60;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(55); });
  it("lru evict cap 56", () => { const c = new LRUCache<number,number>(56); for (let i=0;i<61;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(56); });
  it("lru evict cap 57", () => { const c = new LRUCache<number,number>(57); for (let i=0;i<62;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(57); });
  it("lru evict cap 58", () => { const c = new LRUCache<number,number>(58); for (let i=0;i<63;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(58); });
  it("lru evict cap 59", () => { const c = new LRUCache<number,number>(59); for (let i=0;i<64;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(59); });
  it("lru evict cap 60", () => { const c = new LRUCache<number,number>(60); for (let i=0;i<65;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(60); });
  it("lru evict cap 61", () => { const c = new LRUCache<number,number>(61); for (let i=0;i<66;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(61); });
  it("lru evict cap 62", () => { const c = new LRUCache<number,number>(62); for (let i=0;i<67;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(62); });
  it("lru evict cap 63", () => { const c = new LRUCache<number,number>(63); for (let i=0;i<68;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(63); });
  it("lru evict cap 64", () => { const c = new LRUCache<number,number>(64); for (let i=0;i<69;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(64); });
  it("lru evict cap 65", () => { const c = new LRUCache<number,number>(65); for (let i=0;i<70;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(65); });
  it("lru evict cap 66", () => { const c = new LRUCache<number,number>(66); for (let i=0;i<71;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(66); });
  it("lru evict cap 67", () => { const c = new LRUCache<number,number>(67); for (let i=0;i<72;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(67); });
  it("lru evict cap 68", () => { const c = new LRUCache<number,number>(68); for (let i=0;i<73;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(68); });
  it("lru evict cap 69", () => { const c = new LRUCache<number,number>(69); for (let i=0;i<74;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(69); });
  it("lru evict cap 70", () => { const c = new LRUCache<number,number>(70); for (let i=0;i<75;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(70); });
  it("lru evict cap 71", () => { const c = new LRUCache<number,number>(71); for (let i=0;i<76;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(71); });
  it("lru evict cap 72", () => { const c = new LRUCache<number,number>(72); for (let i=0;i<77;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(72); });
  it("lru evict cap 73", () => { const c = new LRUCache<number,number>(73); for (let i=0;i<78;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(73); });
  it("lru evict cap 74", () => { const c = new LRUCache<number,number>(74); for (let i=0;i<79;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(74); });
  it("lru evict cap 75", () => { const c = new LRUCache<number,number>(75); for (let i=0;i<80;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(75); });
  it("lru evict cap 76", () => { const c = new LRUCache<number,number>(76); for (let i=0;i<81;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(76); });
  it("lru evict cap 77", () => { const c = new LRUCache<number,number>(77); for (let i=0;i<82;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(77); });
  it("lru evict cap 78", () => { const c = new LRUCache<number,number>(78); for (let i=0;i<83;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(78); });
  it("lru evict cap 79", () => { const c = new LRUCache<number,number>(79); for (let i=0;i<84;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(79); });
  it("lru evict cap 80", () => { const c = new LRUCache<number,number>(80); for (let i=0;i<85;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(80); });
  it("lru evict cap 81", () => { const c = new LRUCache<number,number>(81); for (let i=0;i<86;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(81); });
  it("lru evict cap 82", () => { const c = new LRUCache<number,number>(82); for (let i=0;i<87;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(82); });
  it("lru evict cap 83", () => { const c = new LRUCache<number,number>(83); for (let i=0;i<88;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(83); });
  it("lru evict cap 84", () => { const c = new LRUCache<number,number>(84); for (let i=0;i<89;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(84); });
  it("lru evict cap 85", () => { const c = new LRUCache<number,number>(85); for (let i=0;i<90;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(85); });
  it("lru evict cap 86", () => { const c = new LRUCache<number,number>(86); for (let i=0;i<91;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(86); });
  it("lru evict cap 87", () => { const c = new LRUCache<number,number>(87); for (let i=0;i<92;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(87); });
  it("lru evict cap 88", () => { const c = new LRUCache<number,number>(88); for (let i=0;i<93;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(88); });
  it("lru evict cap 89", () => { const c = new LRUCache<number,number>(89); for (let i=0;i<94;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(89); });
  it("lru evict cap 90", () => { const c = new LRUCache<number,number>(90); for (let i=0;i<95;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(90); });
  it("lru evict cap 91", () => { const c = new LRUCache<number,number>(91); for (let i=0;i<96;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(91); });
  it("lru evict cap 92", () => { const c = new LRUCache<number,number>(92); for (let i=0;i<97;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(92); });
  it("lru evict cap 93", () => { const c = new LRUCache<number,number>(93); for (let i=0;i<98;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(93); });
  it("lru evict cap 94", () => { const c = new LRUCache<number,number>(94); for (let i=0;i<99;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(94); });
  it("lru evict cap 95", () => { const c = new LRUCache<number,number>(95); for (let i=0;i<100;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(95); });
  it("lru evict cap 96", () => { const c = new LRUCache<number,number>(96); for (let i=0;i<101;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(96); });
  it("lru evict cap 97", () => { const c = new LRUCache<number,number>(97); for (let i=0;i<102;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(97); });
  it("lru evict cap 98", () => { const c = new LRUCache<number,number>(98); for (let i=0;i<103;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(98); });
  it("lru evict cap 99", () => { const c = new LRUCache<number,number>(99); for (let i=0;i<104;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(99); });
  it("lru evict cap 100", () => { const c = new LRUCache<number,number>(100); for (let i=0;i<105;i++) c.set(i,i); expect(c.size).toBeLessThanOrEqual(100); });
});

describe("LFUCache set and get", () => {
  it("lfu get 0", () => { const c = new LFUCache<number,number>(100); c.set(0, 0); expect(c.get(0)).toBe(0); });
  it("lfu get 1", () => { const c = new LFUCache<number,number>(100); c.set(1, 3); expect(c.get(1)).toBe(3); });
  it("lfu get 2", () => { const c = new LFUCache<number,number>(100); c.set(2, 6); expect(c.get(2)).toBe(6); });
  it("lfu get 3", () => { const c = new LFUCache<number,number>(100); c.set(3, 9); expect(c.get(3)).toBe(9); });
  it("lfu get 4", () => { const c = new LFUCache<number,number>(100); c.set(4, 12); expect(c.get(4)).toBe(12); });
  it("lfu get 5", () => { const c = new LFUCache<number,number>(100); c.set(5, 15); expect(c.get(5)).toBe(15); });
  it("lfu get 6", () => { const c = new LFUCache<number,number>(100); c.set(6, 18); expect(c.get(6)).toBe(18); });
  it("lfu get 7", () => { const c = new LFUCache<number,number>(100); c.set(7, 21); expect(c.get(7)).toBe(21); });
  it("lfu get 8", () => { const c = new LFUCache<number,number>(100); c.set(8, 24); expect(c.get(8)).toBe(24); });
  it("lfu get 9", () => { const c = new LFUCache<number,number>(100); c.set(9, 27); expect(c.get(9)).toBe(27); });
  it("lfu get 10", () => { const c = new LFUCache<number,number>(100); c.set(10, 30); expect(c.get(10)).toBe(30); });
  it("lfu get 11", () => { const c = new LFUCache<number,number>(100); c.set(11, 33); expect(c.get(11)).toBe(33); });
  it("lfu get 12", () => { const c = new LFUCache<number,number>(100); c.set(12, 36); expect(c.get(12)).toBe(36); });
  it("lfu get 13", () => { const c = new LFUCache<number,number>(100); c.set(13, 39); expect(c.get(13)).toBe(39); });
  it("lfu get 14", () => { const c = new LFUCache<number,number>(100); c.set(14, 42); expect(c.get(14)).toBe(42); });
  it("lfu get 15", () => { const c = new LFUCache<number,number>(100); c.set(15, 45); expect(c.get(15)).toBe(45); });
  it("lfu get 16", () => { const c = new LFUCache<number,number>(100); c.set(16, 48); expect(c.get(16)).toBe(48); });
  it("lfu get 17", () => { const c = new LFUCache<number,number>(100); c.set(17, 51); expect(c.get(17)).toBe(51); });
  it("lfu get 18", () => { const c = new LFUCache<number,number>(100); c.set(18, 54); expect(c.get(18)).toBe(54); });
  it("lfu get 19", () => { const c = new LFUCache<number,number>(100); c.set(19, 57); expect(c.get(19)).toBe(57); });
  it("lfu get 20", () => { const c = new LFUCache<number,number>(100); c.set(20, 60); expect(c.get(20)).toBe(60); });
  it("lfu get 21", () => { const c = new LFUCache<number,number>(100); c.set(21, 63); expect(c.get(21)).toBe(63); });
  it("lfu get 22", () => { const c = new LFUCache<number,number>(100); c.set(22, 66); expect(c.get(22)).toBe(66); });
  it("lfu get 23", () => { const c = new LFUCache<number,number>(100); c.set(23, 69); expect(c.get(23)).toBe(69); });
  it("lfu get 24", () => { const c = new LFUCache<number,number>(100); c.set(24, 72); expect(c.get(24)).toBe(72); });
  it("lfu get 25", () => { const c = new LFUCache<number,number>(100); c.set(25, 75); expect(c.get(25)).toBe(75); });
  it("lfu get 26", () => { const c = new LFUCache<number,number>(100); c.set(26, 78); expect(c.get(26)).toBe(78); });
  it("lfu get 27", () => { const c = new LFUCache<number,number>(100); c.set(27, 81); expect(c.get(27)).toBe(81); });
  it("lfu get 28", () => { const c = new LFUCache<number,number>(100); c.set(28, 84); expect(c.get(28)).toBe(84); });
  it("lfu get 29", () => { const c = new LFUCache<number,number>(100); c.set(29, 87); expect(c.get(29)).toBe(87); });
  it("lfu get 30", () => { const c = new LFUCache<number,number>(100); c.set(30, 90); expect(c.get(30)).toBe(90); });
  it("lfu get 31", () => { const c = new LFUCache<number,number>(100); c.set(31, 93); expect(c.get(31)).toBe(93); });
  it("lfu get 32", () => { const c = new LFUCache<number,number>(100); c.set(32, 96); expect(c.get(32)).toBe(96); });
  it("lfu get 33", () => { const c = new LFUCache<number,number>(100); c.set(33, 99); expect(c.get(33)).toBe(99); });
  it("lfu get 34", () => { const c = new LFUCache<number,number>(100); c.set(34, 102); expect(c.get(34)).toBe(102); });
  it("lfu get 35", () => { const c = new LFUCache<number,number>(100); c.set(35, 105); expect(c.get(35)).toBe(105); });
  it("lfu get 36", () => { const c = new LFUCache<number,number>(100); c.set(36, 108); expect(c.get(36)).toBe(108); });
  it("lfu get 37", () => { const c = new LFUCache<number,number>(100); c.set(37, 111); expect(c.get(37)).toBe(111); });
  it("lfu get 38", () => { const c = new LFUCache<number,number>(100); c.set(38, 114); expect(c.get(38)).toBe(114); });
  it("lfu get 39", () => { const c = new LFUCache<number,number>(100); c.set(39, 117); expect(c.get(39)).toBe(117); });
  it("lfu get 40", () => { const c = new LFUCache<number,number>(100); c.set(40, 120); expect(c.get(40)).toBe(120); });
  it("lfu get 41", () => { const c = new LFUCache<number,number>(100); c.set(41, 123); expect(c.get(41)).toBe(123); });
  it("lfu get 42", () => { const c = new LFUCache<number,number>(100); c.set(42, 126); expect(c.get(42)).toBe(126); });
  it("lfu get 43", () => { const c = new LFUCache<number,number>(100); c.set(43, 129); expect(c.get(43)).toBe(129); });
  it("lfu get 44", () => { const c = new LFUCache<number,number>(100); c.set(44, 132); expect(c.get(44)).toBe(132); });
  it("lfu get 45", () => { const c = new LFUCache<number,number>(100); c.set(45, 135); expect(c.get(45)).toBe(135); });
  it("lfu get 46", () => { const c = new LFUCache<number,number>(100); c.set(46, 138); expect(c.get(46)).toBe(138); });
  it("lfu get 47", () => { const c = new LFUCache<number,number>(100); c.set(47, 141); expect(c.get(47)).toBe(141); });
  it("lfu get 48", () => { const c = new LFUCache<number,number>(100); c.set(48, 144); expect(c.get(48)).toBe(144); });
  it("lfu get 49", () => { const c = new LFUCache<number,number>(100); c.set(49, 147); expect(c.get(49)).toBe(147); });
  it("lfu get 50", () => { const c = new LFUCache<number,number>(100); c.set(50, 150); expect(c.get(50)).toBe(150); });
  it("lfu get 51", () => { const c = new LFUCache<number,number>(100); c.set(51, 153); expect(c.get(51)).toBe(153); });
  it("lfu get 52", () => { const c = new LFUCache<number,number>(100); c.set(52, 156); expect(c.get(52)).toBe(156); });
  it("lfu get 53", () => { const c = new LFUCache<number,number>(100); c.set(53, 159); expect(c.get(53)).toBe(159); });
  it("lfu get 54", () => { const c = new LFUCache<number,number>(100); c.set(54, 162); expect(c.get(54)).toBe(162); });
  it("lfu get 55", () => { const c = new LFUCache<number,number>(100); c.set(55, 165); expect(c.get(55)).toBe(165); });
  it("lfu get 56", () => { const c = new LFUCache<number,number>(100); c.set(56, 168); expect(c.get(56)).toBe(168); });
  it("lfu get 57", () => { const c = new LFUCache<number,number>(100); c.set(57, 171); expect(c.get(57)).toBe(171); });
  it("lfu get 58", () => { const c = new LFUCache<number,number>(100); c.set(58, 174); expect(c.get(58)).toBe(174); });
  it("lfu get 59", () => { const c = new LFUCache<number,number>(100); c.set(59, 177); expect(c.get(59)).toBe(177); });
  it("lfu get 60", () => { const c = new LFUCache<number,number>(100); c.set(60, 180); expect(c.get(60)).toBe(180); });
  it("lfu get 61", () => { const c = new LFUCache<number,number>(100); c.set(61, 183); expect(c.get(61)).toBe(183); });
  it("lfu get 62", () => { const c = new LFUCache<number,number>(100); c.set(62, 186); expect(c.get(62)).toBe(186); });
  it("lfu get 63", () => { const c = new LFUCache<number,number>(100); c.set(63, 189); expect(c.get(63)).toBe(189); });
  it("lfu get 64", () => { const c = new LFUCache<number,number>(100); c.set(64, 192); expect(c.get(64)).toBe(192); });
  it("lfu get 65", () => { const c = new LFUCache<number,number>(100); c.set(65, 195); expect(c.get(65)).toBe(195); });
  it("lfu get 66", () => { const c = new LFUCache<number,number>(100); c.set(66, 198); expect(c.get(66)).toBe(198); });
  it("lfu get 67", () => { const c = new LFUCache<number,number>(100); c.set(67, 201); expect(c.get(67)).toBe(201); });
  it("lfu get 68", () => { const c = new LFUCache<number,number>(100); c.set(68, 204); expect(c.get(68)).toBe(204); });
  it("lfu get 69", () => { const c = new LFUCache<number,number>(100); c.set(69, 207); expect(c.get(69)).toBe(207); });
  it("lfu get 70", () => { const c = new LFUCache<number,number>(100); c.set(70, 210); expect(c.get(70)).toBe(210); });
  it("lfu get 71", () => { const c = new LFUCache<number,number>(100); c.set(71, 213); expect(c.get(71)).toBe(213); });
  it("lfu get 72", () => { const c = new LFUCache<number,number>(100); c.set(72, 216); expect(c.get(72)).toBe(216); });
  it("lfu get 73", () => { const c = new LFUCache<number,number>(100); c.set(73, 219); expect(c.get(73)).toBe(219); });
  it("lfu get 74", () => { const c = new LFUCache<number,number>(100); c.set(74, 222); expect(c.get(74)).toBe(222); });
  it("lfu get 75", () => { const c = new LFUCache<number,number>(100); c.set(75, 225); expect(c.get(75)).toBe(225); });
  it("lfu get 76", () => { const c = new LFUCache<number,number>(100); c.set(76, 228); expect(c.get(76)).toBe(228); });
  it("lfu get 77", () => { const c = new LFUCache<number,number>(100); c.set(77, 231); expect(c.get(77)).toBe(231); });
  it("lfu get 78", () => { const c = new LFUCache<number,number>(100); c.set(78, 234); expect(c.get(78)).toBe(234); });
  it("lfu get 79", () => { const c = new LFUCache<number,number>(100); c.set(79, 237); expect(c.get(79)).toBe(237); });
  it("lfu get 80", () => { const c = new LFUCache<number,number>(100); c.set(80, 240); expect(c.get(80)).toBe(240); });
  it("lfu get 81", () => { const c = new LFUCache<number,number>(100); c.set(81, 243); expect(c.get(81)).toBe(243); });
  it("lfu get 82", () => { const c = new LFUCache<number,number>(100); c.set(82, 246); expect(c.get(82)).toBe(246); });
  it("lfu get 83", () => { const c = new LFUCache<number,number>(100); c.set(83, 249); expect(c.get(83)).toBe(249); });
  it("lfu get 84", () => { const c = new LFUCache<number,number>(100); c.set(84, 252); expect(c.get(84)).toBe(252); });
  it("lfu get 85", () => { const c = new LFUCache<number,number>(100); c.set(85, 255); expect(c.get(85)).toBe(255); });
  it("lfu get 86", () => { const c = new LFUCache<number,number>(100); c.set(86, 258); expect(c.get(86)).toBe(258); });
  it("lfu get 87", () => { const c = new LFUCache<number,number>(100); c.set(87, 261); expect(c.get(87)).toBe(261); });
  it("lfu get 88", () => { const c = new LFUCache<number,number>(100); c.set(88, 264); expect(c.get(88)).toBe(264); });
  it("lfu get 89", () => { const c = new LFUCache<number,number>(100); c.set(89, 267); expect(c.get(89)).toBe(267); });
  it("lfu get 90", () => { const c = new LFUCache<number,number>(100); c.set(90, 270); expect(c.get(90)).toBe(270); });
  it("lfu get 91", () => { const c = new LFUCache<number,number>(100); c.set(91, 273); expect(c.get(91)).toBe(273); });
  it("lfu get 92", () => { const c = new LFUCache<number,number>(100); c.set(92, 276); expect(c.get(92)).toBe(276); });
  it("lfu get 93", () => { const c = new LFUCache<number,number>(100); c.set(93, 279); expect(c.get(93)).toBe(279); });
  it("lfu get 94", () => { const c = new LFUCache<number,number>(100); c.set(94, 282); expect(c.get(94)).toBe(282); });
  it("lfu get 95", () => { const c = new LFUCache<number,number>(100); c.set(95, 285); expect(c.get(95)).toBe(285); });
  it("lfu get 96", () => { const c = new LFUCache<number,number>(100); c.set(96, 288); expect(c.get(96)).toBe(288); });
  it("lfu get 97", () => { const c = new LFUCache<number,number>(100); c.set(97, 291); expect(c.get(97)).toBe(291); });
  it("lfu get 98", () => { const c = new LFUCache<number,number>(100); c.set(98, 294); expect(c.get(98)).toBe(294); });
  it("lfu get 99", () => { const c = new LFUCache<number,number>(100); c.set(99, 297); expect(c.get(99)).toBe(297); });
});

describe("LFUCache has", () => {
  it("lfu has 0", () => { const c = new LFUCache<number,number>(100); c.set(0, 0); expect(c.has(0)).toBe(true); });
  it("lfu has 1", () => { const c = new LFUCache<number,number>(100); c.set(1, 1); expect(c.has(1)).toBe(true); });
  it("lfu has 2", () => { const c = new LFUCache<number,number>(100); c.set(2, 2); expect(c.has(2)).toBe(true); });
  it("lfu has 3", () => { const c = new LFUCache<number,number>(100); c.set(3, 3); expect(c.has(3)).toBe(true); });
  it("lfu has 4", () => { const c = new LFUCache<number,number>(100); c.set(4, 4); expect(c.has(4)).toBe(true); });
  it("lfu has 5", () => { const c = new LFUCache<number,number>(100); c.set(5, 5); expect(c.has(5)).toBe(true); });
  it("lfu has 6", () => { const c = new LFUCache<number,number>(100); c.set(6, 6); expect(c.has(6)).toBe(true); });
  it("lfu has 7", () => { const c = new LFUCache<number,number>(100); c.set(7, 7); expect(c.has(7)).toBe(true); });
  it("lfu has 8", () => { const c = new LFUCache<number,number>(100); c.set(8, 8); expect(c.has(8)).toBe(true); });
  it("lfu has 9", () => { const c = new LFUCache<number,number>(100); c.set(9, 9); expect(c.has(9)).toBe(true); });
  it("lfu has 10", () => { const c = new LFUCache<number,number>(100); c.set(10, 10); expect(c.has(10)).toBe(true); });
  it("lfu has 11", () => { const c = new LFUCache<number,number>(100); c.set(11, 11); expect(c.has(11)).toBe(true); });
  it("lfu has 12", () => { const c = new LFUCache<number,number>(100); c.set(12, 12); expect(c.has(12)).toBe(true); });
  it("lfu has 13", () => { const c = new LFUCache<number,number>(100); c.set(13, 13); expect(c.has(13)).toBe(true); });
  it("lfu has 14", () => { const c = new LFUCache<number,number>(100); c.set(14, 14); expect(c.has(14)).toBe(true); });
  it("lfu has 15", () => { const c = new LFUCache<number,number>(100); c.set(15, 15); expect(c.has(15)).toBe(true); });
  it("lfu has 16", () => { const c = new LFUCache<number,number>(100); c.set(16, 16); expect(c.has(16)).toBe(true); });
  it("lfu has 17", () => { const c = new LFUCache<number,number>(100); c.set(17, 17); expect(c.has(17)).toBe(true); });
  it("lfu has 18", () => { const c = new LFUCache<number,number>(100); c.set(18, 18); expect(c.has(18)).toBe(true); });
  it("lfu has 19", () => { const c = new LFUCache<number,number>(100); c.set(19, 19); expect(c.has(19)).toBe(true); });
  it("lfu has 20", () => { const c = new LFUCache<number,number>(100); c.set(20, 20); expect(c.has(20)).toBe(true); });
  it("lfu has 21", () => { const c = new LFUCache<number,number>(100); c.set(21, 21); expect(c.has(21)).toBe(true); });
  it("lfu has 22", () => { const c = new LFUCache<number,number>(100); c.set(22, 22); expect(c.has(22)).toBe(true); });
  it("lfu has 23", () => { const c = new LFUCache<number,number>(100); c.set(23, 23); expect(c.has(23)).toBe(true); });
  it("lfu has 24", () => { const c = new LFUCache<number,number>(100); c.set(24, 24); expect(c.has(24)).toBe(true); });
  it("lfu has 25", () => { const c = new LFUCache<number,number>(100); c.set(25, 25); expect(c.has(25)).toBe(true); });
  it("lfu has 26", () => { const c = new LFUCache<number,number>(100); c.set(26, 26); expect(c.has(26)).toBe(true); });
  it("lfu has 27", () => { const c = new LFUCache<number,number>(100); c.set(27, 27); expect(c.has(27)).toBe(true); });
  it("lfu has 28", () => { const c = new LFUCache<number,number>(100); c.set(28, 28); expect(c.has(28)).toBe(true); });
  it("lfu has 29", () => { const c = new LFUCache<number,number>(100); c.set(29, 29); expect(c.has(29)).toBe(true); });
  it("lfu has 30", () => { const c = new LFUCache<number,number>(100); c.set(30, 30); expect(c.has(30)).toBe(true); });
  it("lfu has 31", () => { const c = new LFUCache<number,number>(100); c.set(31, 31); expect(c.has(31)).toBe(true); });
  it("lfu has 32", () => { const c = new LFUCache<number,number>(100); c.set(32, 32); expect(c.has(32)).toBe(true); });
  it("lfu has 33", () => { const c = new LFUCache<number,number>(100); c.set(33, 33); expect(c.has(33)).toBe(true); });
  it("lfu has 34", () => { const c = new LFUCache<number,number>(100); c.set(34, 34); expect(c.has(34)).toBe(true); });
  it("lfu has 35", () => { const c = new LFUCache<number,number>(100); c.set(35, 35); expect(c.has(35)).toBe(true); });
  it("lfu has 36", () => { const c = new LFUCache<number,number>(100); c.set(36, 36); expect(c.has(36)).toBe(true); });
  it("lfu has 37", () => { const c = new LFUCache<number,number>(100); c.set(37, 37); expect(c.has(37)).toBe(true); });
  it("lfu has 38", () => { const c = new LFUCache<number,number>(100); c.set(38, 38); expect(c.has(38)).toBe(true); });
  it("lfu has 39", () => { const c = new LFUCache<number,number>(100); c.set(39, 39); expect(c.has(39)).toBe(true); });
  it("lfu has 40", () => { const c = new LFUCache<number,number>(100); c.set(40, 40); expect(c.has(40)).toBe(true); });
  it("lfu has 41", () => { const c = new LFUCache<number,number>(100); c.set(41, 41); expect(c.has(41)).toBe(true); });
  it("lfu has 42", () => { const c = new LFUCache<number,number>(100); c.set(42, 42); expect(c.has(42)).toBe(true); });
  it("lfu has 43", () => { const c = new LFUCache<number,number>(100); c.set(43, 43); expect(c.has(43)).toBe(true); });
  it("lfu has 44", () => { const c = new LFUCache<number,number>(100); c.set(44, 44); expect(c.has(44)).toBe(true); });
  it("lfu has 45", () => { const c = new LFUCache<number,number>(100); c.set(45, 45); expect(c.has(45)).toBe(true); });
  it("lfu has 46", () => { const c = new LFUCache<number,number>(100); c.set(46, 46); expect(c.has(46)).toBe(true); });
  it("lfu has 47", () => { const c = new LFUCache<number,number>(100); c.set(47, 47); expect(c.has(47)).toBe(true); });
  it("lfu has 48", () => { const c = new LFUCache<number,number>(100); c.set(48, 48); expect(c.has(48)).toBe(true); });
  it("lfu has 49", () => { const c = new LFUCache<number,number>(100); c.set(49, 49); expect(c.has(49)).toBe(true); });
  it("lfu has 50", () => { const c = new LFUCache<number,number>(100); c.set(50, 50); expect(c.has(50)).toBe(true); });
  it("lfu has 51", () => { const c = new LFUCache<number,number>(100); c.set(51, 51); expect(c.has(51)).toBe(true); });
  it("lfu has 52", () => { const c = new LFUCache<number,number>(100); c.set(52, 52); expect(c.has(52)).toBe(true); });
  it("lfu has 53", () => { const c = new LFUCache<number,number>(100); c.set(53, 53); expect(c.has(53)).toBe(true); });
  it("lfu has 54", () => { const c = new LFUCache<number,number>(100); c.set(54, 54); expect(c.has(54)).toBe(true); });
  it("lfu has 55", () => { const c = new LFUCache<number,number>(100); c.set(55, 55); expect(c.has(55)).toBe(true); });
  it("lfu has 56", () => { const c = new LFUCache<number,number>(100); c.set(56, 56); expect(c.has(56)).toBe(true); });
  it("lfu has 57", () => { const c = new LFUCache<number,number>(100); c.set(57, 57); expect(c.has(57)).toBe(true); });
  it("lfu has 58", () => { const c = new LFUCache<number,number>(100); c.set(58, 58); expect(c.has(58)).toBe(true); });
  it("lfu has 59", () => { const c = new LFUCache<number,number>(100); c.set(59, 59); expect(c.has(59)).toBe(true); });
  it("lfu has 60", () => { const c = new LFUCache<number,number>(100); c.set(60, 60); expect(c.has(60)).toBe(true); });
  it("lfu has 61", () => { const c = new LFUCache<number,number>(100); c.set(61, 61); expect(c.has(61)).toBe(true); });
  it("lfu has 62", () => { const c = new LFUCache<number,number>(100); c.set(62, 62); expect(c.has(62)).toBe(true); });
  it("lfu has 63", () => { const c = new LFUCache<number,number>(100); c.set(63, 63); expect(c.has(63)).toBe(true); });
  it("lfu has 64", () => { const c = new LFUCache<number,number>(100); c.set(64, 64); expect(c.has(64)).toBe(true); });
  it("lfu has 65", () => { const c = new LFUCache<number,number>(100); c.set(65, 65); expect(c.has(65)).toBe(true); });
  it("lfu has 66", () => { const c = new LFUCache<number,number>(100); c.set(66, 66); expect(c.has(66)).toBe(true); });
  it("lfu has 67", () => { const c = new LFUCache<number,number>(100); c.set(67, 67); expect(c.has(67)).toBe(true); });
  it("lfu has 68", () => { const c = new LFUCache<number,number>(100); c.set(68, 68); expect(c.has(68)).toBe(true); });
  it("lfu has 69", () => { const c = new LFUCache<number,number>(100); c.set(69, 69); expect(c.has(69)).toBe(true); });
  it("lfu has 70", () => { const c = new LFUCache<number,number>(100); c.set(70, 70); expect(c.has(70)).toBe(true); });
  it("lfu has 71", () => { const c = new LFUCache<number,number>(100); c.set(71, 71); expect(c.has(71)).toBe(true); });
  it("lfu has 72", () => { const c = new LFUCache<number,number>(100); c.set(72, 72); expect(c.has(72)).toBe(true); });
  it("lfu has 73", () => { const c = new LFUCache<number,number>(100); c.set(73, 73); expect(c.has(73)).toBe(true); });
  it("lfu has 74", () => { const c = new LFUCache<number,number>(100); c.set(74, 74); expect(c.has(74)).toBe(true); });
  it("lfu has 75", () => { const c = new LFUCache<number,number>(100); c.set(75, 75); expect(c.has(75)).toBe(true); });
  it("lfu has 76", () => { const c = new LFUCache<number,number>(100); c.set(76, 76); expect(c.has(76)).toBe(true); });
  it("lfu has 77", () => { const c = new LFUCache<number,number>(100); c.set(77, 77); expect(c.has(77)).toBe(true); });
  it("lfu has 78", () => { const c = new LFUCache<number,number>(100); c.set(78, 78); expect(c.has(78)).toBe(true); });
  it("lfu has 79", () => { const c = new LFUCache<number,number>(100); c.set(79, 79); expect(c.has(79)).toBe(true); });
  it("lfu has 80", () => { const c = new LFUCache<number,number>(100); c.set(80, 80); expect(c.has(80)).toBe(true); });
  it("lfu has 81", () => { const c = new LFUCache<number,number>(100); c.set(81, 81); expect(c.has(81)).toBe(true); });
  it("lfu has 82", () => { const c = new LFUCache<number,number>(100); c.set(82, 82); expect(c.has(82)).toBe(true); });
  it("lfu has 83", () => { const c = new LFUCache<number,number>(100); c.set(83, 83); expect(c.has(83)).toBe(true); });
  it("lfu has 84", () => { const c = new LFUCache<number,number>(100); c.set(84, 84); expect(c.has(84)).toBe(true); });
  it("lfu has 85", () => { const c = new LFUCache<number,number>(100); c.set(85, 85); expect(c.has(85)).toBe(true); });
  it("lfu has 86", () => { const c = new LFUCache<number,number>(100); c.set(86, 86); expect(c.has(86)).toBe(true); });
  it("lfu has 87", () => { const c = new LFUCache<number,number>(100); c.set(87, 87); expect(c.has(87)).toBe(true); });
  it("lfu has 88", () => { const c = new LFUCache<number,number>(100); c.set(88, 88); expect(c.has(88)).toBe(true); });
  it("lfu has 89", () => { const c = new LFUCache<number,number>(100); c.set(89, 89); expect(c.has(89)).toBe(true); });
  it("lfu has 90", () => { const c = new LFUCache<number,number>(100); c.set(90, 90); expect(c.has(90)).toBe(true); });
  it("lfu has 91", () => { const c = new LFUCache<number,number>(100); c.set(91, 91); expect(c.has(91)).toBe(true); });
  it("lfu has 92", () => { const c = new LFUCache<number,number>(100); c.set(92, 92); expect(c.has(92)).toBe(true); });
  it("lfu has 93", () => { const c = new LFUCache<number,number>(100); c.set(93, 93); expect(c.has(93)).toBe(true); });
  it("lfu has 94", () => { const c = new LFUCache<number,number>(100); c.set(94, 94); expect(c.has(94)).toBe(true); });
  it("lfu has 95", () => { const c = new LFUCache<number,number>(100); c.set(95, 95); expect(c.has(95)).toBe(true); });
  it("lfu has 96", () => { const c = new LFUCache<number,number>(100); c.set(96, 96); expect(c.has(96)).toBe(true); });
  it("lfu has 97", () => { const c = new LFUCache<number,number>(100); c.set(97, 97); expect(c.has(97)).toBe(true); });
  it("lfu has 98", () => { const c = new LFUCache<number,number>(100); c.set(98, 98); expect(c.has(98)).toBe(true); });
  it("lfu has 99", () => { const c = new LFUCache<number,number>(100); c.set(99, 99); expect(c.has(99)).toBe(true); });
});

describe("LFUCache size", () => {
  it("lfu size 1", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<1;i++) c.set(i,i); expect(c.size).toBe(1); });
  it("lfu size 2", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<2;i++) c.set(i,i); expect(c.size).toBe(2); });
  it("lfu size 3", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<3;i++) c.set(i,i); expect(c.size).toBe(3); });
  it("lfu size 4", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<4;i++) c.set(i,i); expect(c.size).toBe(4); });
  it("lfu size 5", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<5;i++) c.set(i,i); expect(c.size).toBe(5); });
  it("lfu size 6", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<6;i++) c.set(i,i); expect(c.size).toBe(6); });
  it("lfu size 7", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<7;i++) c.set(i,i); expect(c.size).toBe(7); });
  it("lfu size 8", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<8;i++) c.set(i,i); expect(c.size).toBe(8); });
  it("lfu size 9", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<9;i++) c.set(i,i); expect(c.size).toBe(9); });
  it("lfu size 10", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<10;i++) c.set(i,i); expect(c.size).toBe(10); });
  it("lfu size 11", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<11;i++) c.set(i,i); expect(c.size).toBe(11); });
  it("lfu size 12", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<12;i++) c.set(i,i); expect(c.size).toBe(12); });
  it("lfu size 13", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<13;i++) c.set(i,i); expect(c.size).toBe(13); });
  it("lfu size 14", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<14;i++) c.set(i,i); expect(c.size).toBe(14); });
  it("lfu size 15", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<15;i++) c.set(i,i); expect(c.size).toBe(15); });
  it("lfu size 16", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<16;i++) c.set(i,i); expect(c.size).toBe(16); });
  it("lfu size 17", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<17;i++) c.set(i,i); expect(c.size).toBe(17); });
  it("lfu size 18", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<18;i++) c.set(i,i); expect(c.size).toBe(18); });
  it("lfu size 19", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<19;i++) c.set(i,i); expect(c.size).toBe(19); });
  it("lfu size 20", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<20;i++) c.set(i,i); expect(c.size).toBe(20); });
  it("lfu size 21", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<21;i++) c.set(i,i); expect(c.size).toBe(21); });
  it("lfu size 22", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<22;i++) c.set(i,i); expect(c.size).toBe(22); });
  it("lfu size 23", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<23;i++) c.set(i,i); expect(c.size).toBe(23); });
  it("lfu size 24", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<24;i++) c.set(i,i); expect(c.size).toBe(24); });
  it("lfu size 25", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<25;i++) c.set(i,i); expect(c.size).toBe(25); });
  it("lfu size 26", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<26;i++) c.set(i,i); expect(c.size).toBe(26); });
  it("lfu size 27", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<27;i++) c.set(i,i); expect(c.size).toBe(27); });
  it("lfu size 28", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<28;i++) c.set(i,i); expect(c.size).toBe(28); });
  it("lfu size 29", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<29;i++) c.set(i,i); expect(c.size).toBe(29); });
  it("lfu size 30", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<30;i++) c.set(i,i); expect(c.size).toBe(30); });
  it("lfu size 31", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<31;i++) c.set(i,i); expect(c.size).toBe(31); });
  it("lfu size 32", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<32;i++) c.set(i,i); expect(c.size).toBe(32); });
  it("lfu size 33", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<33;i++) c.set(i,i); expect(c.size).toBe(33); });
  it("lfu size 34", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<34;i++) c.set(i,i); expect(c.size).toBe(34); });
  it("lfu size 35", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<35;i++) c.set(i,i); expect(c.size).toBe(35); });
  it("lfu size 36", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<36;i++) c.set(i,i); expect(c.size).toBe(36); });
  it("lfu size 37", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<37;i++) c.set(i,i); expect(c.size).toBe(37); });
  it("lfu size 38", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<38;i++) c.set(i,i); expect(c.size).toBe(38); });
  it("lfu size 39", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<39;i++) c.set(i,i); expect(c.size).toBe(39); });
  it("lfu size 40", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<40;i++) c.set(i,i); expect(c.size).toBe(40); });
  it("lfu size 41", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<41;i++) c.set(i,i); expect(c.size).toBe(41); });
  it("lfu size 42", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<42;i++) c.set(i,i); expect(c.size).toBe(42); });
  it("lfu size 43", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<43;i++) c.set(i,i); expect(c.size).toBe(43); });
  it("lfu size 44", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<44;i++) c.set(i,i); expect(c.size).toBe(44); });
  it("lfu size 45", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<45;i++) c.set(i,i); expect(c.size).toBe(45); });
  it("lfu size 46", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<46;i++) c.set(i,i); expect(c.size).toBe(46); });
  it("lfu size 47", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<47;i++) c.set(i,i); expect(c.size).toBe(47); });
  it("lfu size 48", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<48;i++) c.set(i,i); expect(c.size).toBe(48); });
  it("lfu size 49", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<49;i++) c.set(i,i); expect(c.size).toBe(49); });
  it("lfu size 50", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<50;i++) c.set(i,i); expect(c.size).toBe(50); });
  it("lfu size 51", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<51;i++) c.set(i,i); expect(c.size).toBe(51); });
  it("lfu size 52", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<52;i++) c.set(i,i); expect(c.size).toBe(52); });
  it("lfu size 53", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<53;i++) c.set(i,i); expect(c.size).toBe(53); });
  it("lfu size 54", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<54;i++) c.set(i,i); expect(c.size).toBe(54); });
  it("lfu size 55", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<55;i++) c.set(i,i); expect(c.size).toBe(55); });
  it("lfu size 56", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<56;i++) c.set(i,i); expect(c.size).toBe(56); });
  it("lfu size 57", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<57;i++) c.set(i,i); expect(c.size).toBe(57); });
  it("lfu size 58", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<58;i++) c.set(i,i); expect(c.size).toBe(58); });
  it("lfu size 59", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<59;i++) c.set(i,i); expect(c.size).toBe(59); });
  it("lfu size 60", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<60;i++) c.set(i,i); expect(c.size).toBe(60); });
  it("lfu size 61", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<61;i++) c.set(i,i); expect(c.size).toBe(61); });
  it("lfu size 62", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<62;i++) c.set(i,i); expect(c.size).toBe(62); });
  it("lfu size 63", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<63;i++) c.set(i,i); expect(c.size).toBe(63); });
  it("lfu size 64", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<64;i++) c.set(i,i); expect(c.size).toBe(64); });
  it("lfu size 65", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<65;i++) c.set(i,i); expect(c.size).toBe(65); });
  it("lfu size 66", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<66;i++) c.set(i,i); expect(c.size).toBe(66); });
  it("lfu size 67", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<67;i++) c.set(i,i); expect(c.size).toBe(67); });
  it("lfu size 68", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<68;i++) c.set(i,i); expect(c.size).toBe(68); });
  it("lfu size 69", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<69;i++) c.set(i,i); expect(c.size).toBe(69); });
  it("lfu size 70", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<70;i++) c.set(i,i); expect(c.size).toBe(70); });
  it("lfu size 71", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<71;i++) c.set(i,i); expect(c.size).toBe(71); });
  it("lfu size 72", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<72;i++) c.set(i,i); expect(c.size).toBe(72); });
  it("lfu size 73", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<73;i++) c.set(i,i); expect(c.size).toBe(73); });
  it("lfu size 74", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<74;i++) c.set(i,i); expect(c.size).toBe(74); });
  it("lfu size 75", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<75;i++) c.set(i,i); expect(c.size).toBe(75); });
  it("lfu size 76", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<76;i++) c.set(i,i); expect(c.size).toBe(76); });
  it("lfu size 77", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<77;i++) c.set(i,i); expect(c.size).toBe(77); });
  it("lfu size 78", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<78;i++) c.set(i,i); expect(c.size).toBe(78); });
  it("lfu size 79", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<79;i++) c.set(i,i); expect(c.size).toBe(79); });
  it("lfu size 80", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<80;i++) c.set(i,i); expect(c.size).toBe(80); });
  it("lfu size 81", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<81;i++) c.set(i,i); expect(c.size).toBe(81); });
  it("lfu size 82", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<82;i++) c.set(i,i); expect(c.size).toBe(82); });
  it("lfu size 83", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<83;i++) c.set(i,i); expect(c.size).toBe(83); });
  it("lfu size 84", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<84;i++) c.set(i,i); expect(c.size).toBe(84); });
  it("lfu size 85", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<85;i++) c.set(i,i); expect(c.size).toBe(85); });
  it("lfu size 86", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<86;i++) c.set(i,i); expect(c.size).toBe(86); });
  it("lfu size 87", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<87;i++) c.set(i,i); expect(c.size).toBe(87); });
  it("lfu size 88", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<88;i++) c.set(i,i); expect(c.size).toBe(88); });
  it("lfu size 89", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<89;i++) c.set(i,i); expect(c.size).toBe(89); });
  it("lfu size 90", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<90;i++) c.set(i,i); expect(c.size).toBe(90); });
  it("lfu size 91", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<91;i++) c.set(i,i); expect(c.size).toBe(91); });
  it("lfu size 92", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<92;i++) c.set(i,i); expect(c.size).toBe(92); });
  it("lfu size 93", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<93;i++) c.set(i,i); expect(c.size).toBe(93); });
  it("lfu size 94", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<94;i++) c.set(i,i); expect(c.size).toBe(94); });
  it("lfu size 95", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<95;i++) c.set(i,i); expect(c.size).toBe(95); });
  it("lfu size 96", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<96;i++) c.set(i,i); expect(c.size).toBe(96); });
  it("lfu size 97", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<97;i++) c.set(i,i); expect(c.size).toBe(97); });
  it("lfu size 98", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<98;i++) c.set(i,i); expect(c.size).toBe(98); });
  it("lfu size 99", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<99;i++) c.set(i,i); expect(c.size).toBe(99); });
  it("lfu size 100", () => { const c = new LFUCache<number,number>(200); for (let i=0;i<100;i++) c.set(i,i); expect(c.size).toBe(100); });
});

describe("TTLCache get before expiry", () => {
  it("ttl get 0", () => { const c = new TTLCache<number,number>(100, 60000); c.set(0, 0); expect(c.get(0)).toBe(0); });
  it("ttl get 1", () => { const c = new TTLCache<number,number>(100, 60000); c.set(1, 4); expect(c.get(1)).toBe(4); });
  it("ttl get 2", () => { const c = new TTLCache<number,number>(100, 60000); c.set(2, 8); expect(c.get(2)).toBe(8); });
  it("ttl get 3", () => { const c = new TTLCache<number,number>(100, 60000); c.set(3, 12); expect(c.get(3)).toBe(12); });
  it("ttl get 4", () => { const c = new TTLCache<number,number>(100, 60000); c.set(4, 16); expect(c.get(4)).toBe(16); });
  it("ttl get 5", () => { const c = new TTLCache<number,number>(100, 60000); c.set(5, 20); expect(c.get(5)).toBe(20); });
  it("ttl get 6", () => { const c = new TTLCache<number,number>(100, 60000); c.set(6, 24); expect(c.get(6)).toBe(24); });
  it("ttl get 7", () => { const c = new TTLCache<number,number>(100, 60000); c.set(7, 28); expect(c.get(7)).toBe(28); });
  it("ttl get 8", () => { const c = new TTLCache<number,number>(100, 60000); c.set(8, 32); expect(c.get(8)).toBe(32); });
  it("ttl get 9", () => { const c = new TTLCache<number,number>(100, 60000); c.set(9, 36); expect(c.get(9)).toBe(36); });
  it("ttl get 10", () => { const c = new TTLCache<number,number>(100, 60000); c.set(10, 40); expect(c.get(10)).toBe(40); });
  it("ttl get 11", () => { const c = new TTLCache<number,number>(100, 60000); c.set(11, 44); expect(c.get(11)).toBe(44); });
  it("ttl get 12", () => { const c = new TTLCache<number,number>(100, 60000); c.set(12, 48); expect(c.get(12)).toBe(48); });
  it("ttl get 13", () => { const c = new TTLCache<number,number>(100, 60000); c.set(13, 52); expect(c.get(13)).toBe(52); });
  it("ttl get 14", () => { const c = new TTLCache<number,number>(100, 60000); c.set(14, 56); expect(c.get(14)).toBe(56); });
  it("ttl get 15", () => { const c = new TTLCache<number,number>(100, 60000); c.set(15, 60); expect(c.get(15)).toBe(60); });
  it("ttl get 16", () => { const c = new TTLCache<number,number>(100, 60000); c.set(16, 64); expect(c.get(16)).toBe(64); });
  it("ttl get 17", () => { const c = new TTLCache<number,number>(100, 60000); c.set(17, 68); expect(c.get(17)).toBe(68); });
  it("ttl get 18", () => { const c = new TTLCache<number,number>(100, 60000); c.set(18, 72); expect(c.get(18)).toBe(72); });
  it("ttl get 19", () => { const c = new TTLCache<number,number>(100, 60000); c.set(19, 76); expect(c.get(19)).toBe(76); });
  it("ttl get 20", () => { const c = new TTLCache<number,number>(100, 60000); c.set(20, 80); expect(c.get(20)).toBe(80); });
  it("ttl get 21", () => { const c = new TTLCache<number,number>(100, 60000); c.set(21, 84); expect(c.get(21)).toBe(84); });
  it("ttl get 22", () => { const c = new TTLCache<number,number>(100, 60000); c.set(22, 88); expect(c.get(22)).toBe(88); });
  it("ttl get 23", () => { const c = new TTLCache<number,number>(100, 60000); c.set(23, 92); expect(c.get(23)).toBe(92); });
  it("ttl get 24", () => { const c = new TTLCache<number,number>(100, 60000); c.set(24, 96); expect(c.get(24)).toBe(96); });
  it("ttl get 25", () => { const c = new TTLCache<number,number>(100, 60000); c.set(25, 100); expect(c.get(25)).toBe(100); });
  it("ttl get 26", () => { const c = new TTLCache<number,number>(100, 60000); c.set(26, 104); expect(c.get(26)).toBe(104); });
  it("ttl get 27", () => { const c = new TTLCache<number,number>(100, 60000); c.set(27, 108); expect(c.get(27)).toBe(108); });
  it("ttl get 28", () => { const c = new TTLCache<number,number>(100, 60000); c.set(28, 112); expect(c.get(28)).toBe(112); });
  it("ttl get 29", () => { const c = new TTLCache<number,number>(100, 60000); c.set(29, 116); expect(c.get(29)).toBe(116); });
  it("ttl get 30", () => { const c = new TTLCache<number,number>(100, 60000); c.set(30, 120); expect(c.get(30)).toBe(120); });
  it("ttl get 31", () => { const c = new TTLCache<number,number>(100, 60000); c.set(31, 124); expect(c.get(31)).toBe(124); });
  it("ttl get 32", () => { const c = new TTLCache<number,number>(100, 60000); c.set(32, 128); expect(c.get(32)).toBe(128); });
  it("ttl get 33", () => { const c = new TTLCache<number,number>(100, 60000); c.set(33, 132); expect(c.get(33)).toBe(132); });
  it("ttl get 34", () => { const c = new TTLCache<number,number>(100, 60000); c.set(34, 136); expect(c.get(34)).toBe(136); });
  it("ttl get 35", () => { const c = new TTLCache<number,number>(100, 60000); c.set(35, 140); expect(c.get(35)).toBe(140); });
  it("ttl get 36", () => { const c = new TTLCache<number,number>(100, 60000); c.set(36, 144); expect(c.get(36)).toBe(144); });
  it("ttl get 37", () => { const c = new TTLCache<number,number>(100, 60000); c.set(37, 148); expect(c.get(37)).toBe(148); });
  it("ttl get 38", () => { const c = new TTLCache<number,number>(100, 60000); c.set(38, 152); expect(c.get(38)).toBe(152); });
  it("ttl get 39", () => { const c = new TTLCache<number,number>(100, 60000); c.set(39, 156); expect(c.get(39)).toBe(156); });
  it("ttl get 40", () => { const c = new TTLCache<number,number>(100, 60000); c.set(40, 160); expect(c.get(40)).toBe(160); });
  it("ttl get 41", () => { const c = new TTLCache<number,number>(100, 60000); c.set(41, 164); expect(c.get(41)).toBe(164); });
  it("ttl get 42", () => { const c = new TTLCache<number,number>(100, 60000); c.set(42, 168); expect(c.get(42)).toBe(168); });
  it("ttl get 43", () => { const c = new TTLCache<number,number>(100, 60000); c.set(43, 172); expect(c.get(43)).toBe(172); });
  it("ttl get 44", () => { const c = new TTLCache<number,number>(100, 60000); c.set(44, 176); expect(c.get(44)).toBe(176); });
  it("ttl get 45", () => { const c = new TTLCache<number,number>(100, 60000); c.set(45, 180); expect(c.get(45)).toBe(180); });
  it("ttl get 46", () => { const c = new TTLCache<number,number>(100, 60000); c.set(46, 184); expect(c.get(46)).toBe(184); });
  it("ttl get 47", () => { const c = new TTLCache<number,number>(100, 60000); c.set(47, 188); expect(c.get(47)).toBe(188); });
  it("ttl get 48", () => { const c = new TTLCache<number,number>(100, 60000); c.set(48, 192); expect(c.get(48)).toBe(192); });
  it("ttl get 49", () => { const c = new TTLCache<number,number>(100, 60000); c.set(49, 196); expect(c.get(49)).toBe(196); });
  it("ttl get 50", () => { const c = new TTLCache<number,number>(100, 60000); c.set(50, 200); expect(c.get(50)).toBe(200); });
  it("ttl get 51", () => { const c = new TTLCache<number,number>(100, 60000); c.set(51, 204); expect(c.get(51)).toBe(204); });
  it("ttl get 52", () => { const c = new TTLCache<number,number>(100, 60000); c.set(52, 208); expect(c.get(52)).toBe(208); });
  it("ttl get 53", () => { const c = new TTLCache<number,number>(100, 60000); c.set(53, 212); expect(c.get(53)).toBe(212); });
  it("ttl get 54", () => { const c = new TTLCache<number,number>(100, 60000); c.set(54, 216); expect(c.get(54)).toBe(216); });
  it("ttl get 55", () => { const c = new TTLCache<number,number>(100, 60000); c.set(55, 220); expect(c.get(55)).toBe(220); });
  it("ttl get 56", () => { const c = new TTLCache<number,number>(100, 60000); c.set(56, 224); expect(c.get(56)).toBe(224); });
  it("ttl get 57", () => { const c = new TTLCache<number,number>(100, 60000); c.set(57, 228); expect(c.get(57)).toBe(228); });
  it("ttl get 58", () => { const c = new TTLCache<number,number>(100, 60000); c.set(58, 232); expect(c.get(58)).toBe(232); });
  it("ttl get 59", () => { const c = new TTLCache<number,number>(100, 60000); c.set(59, 236); expect(c.get(59)).toBe(236); });
  it("ttl get 60", () => { const c = new TTLCache<number,number>(100, 60000); c.set(60, 240); expect(c.get(60)).toBe(240); });
  it("ttl get 61", () => { const c = new TTLCache<number,number>(100, 60000); c.set(61, 244); expect(c.get(61)).toBe(244); });
  it("ttl get 62", () => { const c = new TTLCache<number,number>(100, 60000); c.set(62, 248); expect(c.get(62)).toBe(248); });
  it("ttl get 63", () => { const c = new TTLCache<number,number>(100, 60000); c.set(63, 252); expect(c.get(63)).toBe(252); });
  it("ttl get 64", () => { const c = new TTLCache<number,number>(100, 60000); c.set(64, 256); expect(c.get(64)).toBe(256); });
  it("ttl get 65", () => { const c = new TTLCache<number,number>(100, 60000); c.set(65, 260); expect(c.get(65)).toBe(260); });
  it("ttl get 66", () => { const c = new TTLCache<number,number>(100, 60000); c.set(66, 264); expect(c.get(66)).toBe(264); });
  it("ttl get 67", () => { const c = new TTLCache<number,number>(100, 60000); c.set(67, 268); expect(c.get(67)).toBe(268); });
  it("ttl get 68", () => { const c = new TTLCache<number,number>(100, 60000); c.set(68, 272); expect(c.get(68)).toBe(272); });
  it("ttl get 69", () => { const c = new TTLCache<number,number>(100, 60000); c.set(69, 276); expect(c.get(69)).toBe(276); });
  it("ttl get 70", () => { const c = new TTLCache<number,number>(100, 60000); c.set(70, 280); expect(c.get(70)).toBe(280); });
  it("ttl get 71", () => { const c = new TTLCache<number,number>(100, 60000); c.set(71, 284); expect(c.get(71)).toBe(284); });
  it("ttl get 72", () => { const c = new TTLCache<number,number>(100, 60000); c.set(72, 288); expect(c.get(72)).toBe(288); });
  it("ttl get 73", () => { const c = new TTLCache<number,number>(100, 60000); c.set(73, 292); expect(c.get(73)).toBe(292); });
  it("ttl get 74", () => { const c = new TTLCache<number,number>(100, 60000); c.set(74, 296); expect(c.get(74)).toBe(296); });
  it("ttl get 75", () => { const c = new TTLCache<number,number>(100, 60000); c.set(75, 300); expect(c.get(75)).toBe(300); });
  it("ttl get 76", () => { const c = new TTLCache<number,number>(100, 60000); c.set(76, 304); expect(c.get(76)).toBe(304); });
  it("ttl get 77", () => { const c = new TTLCache<number,number>(100, 60000); c.set(77, 308); expect(c.get(77)).toBe(308); });
  it("ttl get 78", () => { const c = new TTLCache<number,number>(100, 60000); c.set(78, 312); expect(c.get(78)).toBe(312); });
  it("ttl get 79", () => { const c = new TTLCache<number,number>(100, 60000); c.set(79, 316); expect(c.get(79)).toBe(316); });
  it("ttl get 80", () => { const c = new TTLCache<number,number>(100, 60000); c.set(80, 320); expect(c.get(80)).toBe(320); });
  it("ttl get 81", () => { const c = new TTLCache<number,number>(100, 60000); c.set(81, 324); expect(c.get(81)).toBe(324); });
  it("ttl get 82", () => { const c = new TTLCache<number,number>(100, 60000); c.set(82, 328); expect(c.get(82)).toBe(328); });
  it("ttl get 83", () => { const c = new TTLCache<number,number>(100, 60000); c.set(83, 332); expect(c.get(83)).toBe(332); });
  it("ttl get 84", () => { const c = new TTLCache<number,number>(100, 60000); c.set(84, 336); expect(c.get(84)).toBe(336); });
  it("ttl get 85", () => { const c = new TTLCache<number,number>(100, 60000); c.set(85, 340); expect(c.get(85)).toBe(340); });
  it("ttl get 86", () => { const c = new TTLCache<number,number>(100, 60000); c.set(86, 344); expect(c.get(86)).toBe(344); });
  it("ttl get 87", () => { const c = new TTLCache<number,number>(100, 60000); c.set(87, 348); expect(c.get(87)).toBe(348); });
  it("ttl get 88", () => { const c = new TTLCache<number,number>(100, 60000); c.set(88, 352); expect(c.get(88)).toBe(352); });
  it("ttl get 89", () => { const c = new TTLCache<number,number>(100, 60000); c.set(89, 356); expect(c.get(89)).toBe(356); });
  it("ttl get 90", () => { const c = new TTLCache<number,number>(100, 60000); c.set(90, 360); expect(c.get(90)).toBe(360); });
  it("ttl get 91", () => { const c = new TTLCache<number,number>(100, 60000); c.set(91, 364); expect(c.get(91)).toBe(364); });
  it("ttl get 92", () => { const c = new TTLCache<number,number>(100, 60000); c.set(92, 368); expect(c.get(92)).toBe(368); });
  it("ttl get 93", () => { const c = new TTLCache<number,number>(100, 60000); c.set(93, 372); expect(c.get(93)).toBe(372); });
  it("ttl get 94", () => { const c = new TTLCache<number,number>(100, 60000); c.set(94, 376); expect(c.get(94)).toBe(376); });
  it("ttl get 95", () => { const c = new TTLCache<number,number>(100, 60000); c.set(95, 380); expect(c.get(95)).toBe(380); });
  it("ttl get 96", () => { const c = new TTLCache<number,number>(100, 60000); c.set(96, 384); expect(c.get(96)).toBe(384); });
  it("ttl get 97", () => { const c = new TTLCache<number,number>(100, 60000); c.set(97, 388); expect(c.get(97)).toBe(388); });
  it("ttl get 98", () => { const c = new TTLCache<number,number>(100, 60000); c.set(98, 392); expect(c.get(98)).toBe(392); });
  it("ttl get 99", () => { const c = new TTLCache<number,number>(100, 60000); c.set(99, 396); expect(c.get(99)).toBe(396); });
});

describe("TTLCache has", () => {
  it("ttl has 0", () => { const c = new TTLCache<number,number>(100, 60000); c.set(0, 0); expect(c.has(0)).toBe(true); });
  it("ttl has 1", () => { const c = new TTLCache<number,number>(100, 60000); c.set(1, 1); expect(c.has(1)).toBe(true); });
  it("ttl has 2", () => { const c = new TTLCache<number,number>(100, 60000); c.set(2, 2); expect(c.has(2)).toBe(true); });
  it("ttl has 3", () => { const c = new TTLCache<number,number>(100, 60000); c.set(3, 3); expect(c.has(3)).toBe(true); });
  it("ttl has 4", () => { const c = new TTLCache<number,number>(100, 60000); c.set(4, 4); expect(c.has(4)).toBe(true); });
  it("ttl has 5", () => { const c = new TTLCache<number,number>(100, 60000); c.set(5, 5); expect(c.has(5)).toBe(true); });
  it("ttl has 6", () => { const c = new TTLCache<number,number>(100, 60000); c.set(6, 6); expect(c.has(6)).toBe(true); });
  it("ttl has 7", () => { const c = new TTLCache<number,number>(100, 60000); c.set(7, 7); expect(c.has(7)).toBe(true); });
  it("ttl has 8", () => { const c = new TTLCache<number,number>(100, 60000); c.set(8, 8); expect(c.has(8)).toBe(true); });
  it("ttl has 9", () => { const c = new TTLCache<number,number>(100, 60000); c.set(9, 9); expect(c.has(9)).toBe(true); });
  it("ttl has 10", () => { const c = new TTLCache<number,number>(100, 60000); c.set(10, 10); expect(c.has(10)).toBe(true); });
  it("ttl has 11", () => { const c = new TTLCache<number,number>(100, 60000); c.set(11, 11); expect(c.has(11)).toBe(true); });
  it("ttl has 12", () => { const c = new TTLCache<number,number>(100, 60000); c.set(12, 12); expect(c.has(12)).toBe(true); });
  it("ttl has 13", () => { const c = new TTLCache<number,number>(100, 60000); c.set(13, 13); expect(c.has(13)).toBe(true); });
  it("ttl has 14", () => { const c = new TTLCache<number,number>(100, 60000); c.set(14, 14); expect(c.has(14)).toBe(true); });
  it("ttl has 15", () => { const c = new TTLCache<number,number>(100, 60000); c.set(15, 15); expect(c.has(15)).toBe(true); });
  it("ttl has 16", () => { const c = new TTLCache<number,number>(100, 60000); c.set(16, 16); expect(c.has(16)).toBe(true); });
  it("ttl has 17", () => { const c = new TTLCache<number,number>(100, 60000); c.set(17, 17); expect(c.has(17)).toBe(true); });
  it("ttl has 18", () => { const c = new TTLCache<number,number>(100, 60000); c.set(18, 18); expect(c.has(18)).toBe(true); });
  it("ttl has 19", () => { const c = new TTLCache<number,number>(100, 60000); c.set(19, 19); expect(c.has(19)).toBe(true); });
  it("ttl has 20", () => { const c = new TTLCache<number,number>(100, 60000); c.set(20, 20); expect(c.has(20)).toBe(true); });
  it("ttl has 21", () => { const c = new TTLCache<number,number>(100, 60000); c.set(21, 21); expect(c.has(21)).toBe(true); });
  it("ttl has 22", () => { const c = new TTLCache<number,number>(100, 60000); c.set(22, 22); expect(c.has(22)).toBe(true); });
  it("ttl has 23", () => { const c = new TTLCache<number,number>(100, 60000); c.set(23, 23); expect(c.has(23)).toBe(true); });
  it("ttl has 24", () => { const c = new TTLCache<number,number>(100, 60000); c.set(24, 24); expect(c.has(24)).toBe(true); });
  it("ttl has 25", () => { const c = new TTLCache<number,number>(100, 60000); c.set(25, 25); expect(c.has(25)).toBe(true); });
  it("ttl has 26", () => { const c = new TTLCache<number,number>(100, 60000); c.set(26, 26); expect(c.has(26)).toBe(true); });
  it("ttl has 27", () => { const c = new TTLCache<number,number>(100, 60000); c.set(27, 27); expect(c.has(27)).toBe(true); });
  it("ttl has 28", () => { const c = new TTLCache<number,number>(100, 60000); c.set(28, 28); expect(c.has(28)).toBe(true); });
  it("ttl has 29", () => { const c = new TTLCache<number,number>(100, 60000); c.set(29, 29); expect(c.has(29)).toBe(true); });
  it("ttl has 30", () => { const c = new TTLCache<number,number>(100, 60000); c.set(30, 30); expect(c.has(30)).toBe(true); });
  it("ttl has 31", () => { const c = new TTLCache<number,number>(100, 60000); c.set(31, 31); expect(c.has(31)).toBe(true); });
  it("ttl has 32", () => { const c = new TTLCache<number,number>(100, 60000); c.set(32, 32); expect(c.has(32)).toBe(true); });
  it("ttl has 33", () => { const c = new TTLCache<number,number>(100, 60000); c.set(33, 33); expect(c.has(33)).toBe(true); });
  it("ttl has 34", () => { const c = new TTLCache<number,number>(100, 60000); c.set(34, 34); expect(c.has(34)).toBe(true); });
  it("ttl has 35", () => { const c = new TTLCache<number,number>(100, 60000); c.set(35, 35); expect(c.has(35)).toBe(true); });
  it("ttl has 36", () => { const c = new TTLCache<number,number>(100, 60000); c.set(36, 36); expect(c.has(36)).toBe(true); });
  it("ttl has 37", () => { const c = new TTLCache<number,number>(100, 60000); c.set(37, 37); expect(c.has(37)).toBe(true); });
  it("ttl has 38", () => { const c = new TTLCache<number,number>(100, 60000); c.set(38, 38); expect(c.has(38)).toBe(true); });
  it("ttl has 39", () => { const c = new TTLCache<number,number>(100, 60000); c.set(39, 39); expect(c.has(39)).toBe(true); });
  it("ttl has 40", () => { const c = new TTLCache<number,number>(100, 60000); c.set(40, 40); expect(c.has(40)).toBe(true); });
  it("ttl has 41", () => { const c = new TTLCache<number,number>(100, 60000); c.set(41, 41); expect(c.has(41)).toBe(true); });
  it("ttl has 42", () => { const c = new TTLCache<number,number>(100, 60000); c.set(42, 42); expect(c.has(42)).toBe(true); });
  it("ttl has 43", () => { const c = new TTLCache<number,number>(100, 60000); c.set(43, 43); expect(c.has(43)).toBe(true); });
  it("ttl has 44", () => { const c = new TTLCache<number,number>(100, 60000); c.set(44, 44); expect(c.has(44)).toBe(true); });
  it("ttl has 45", () => { const c = new TTLCache<number,number>(100, 60000); c.set(45, 45); expect(c.has(45)).toBe(true); });
  it("ttl has 46", () => { const c = new TTLCache<number,number>(100, 60000); c.set(46, 46); expect(c.has(46)).toBe(true); });
  it("ttl has 47", () => { const c = new TTLCache<number,number>(100, 60000); c.set(47, 47); expect(c.has(47)).toBe(true); });
  it("ttl has 48", () => { const c = new TTLCache<number,number>(100, 60000); c.set(48, 48); expect(c.has(48)).toBe(true); });
  it("ttl has 49", () => { const c = new TTLCache<number,number>(100, 60000); c.set(49, 49); expect(c.has(49)).toBe(true); });
  it("ttl has 50", () => { const c = new TTLCache<number,number>(100, 60000); c.set(50, 50); expect(c.has(50)).toBe(true); });
  it("ttl has 51", () => { const c = new TTLCache<number,number>(100, 60000); c.set(51, 51); expect(c.has(51)).toBe(true); });
  it("ttl has 52", () => { const c = new TTLCache<number,number>(100, 60000); c.set(52, 52); expect(c.has(52)).toBe(true); });
  it("ttl has 53", () => { const c = new TTLCache<number,number>(100, 60000); c.set(53, 53); expect(c.has(53)).toBe(true); });
  it("ttl has 54", () => { const c = new TTLCache<number,number>(100, 60000); c.set(54, 54); expect(c.has(54)).toBe(true); });
  it("ttl has 55", () => { const c = new TTLCache<number,number>(100, 60000); c.set(55, 55); expect(c.has(55)).toBe(true); });
  it("ttl has 56", () => { const c = new TTLCache<number,number>(100, 60000); c.set(56, 56); expect(c.has(56)).toBe(true); });
  it("ttl has 57", () => { const c = new TTLCache<number,number>(100, 60000); c.set(57, 57); expect(c.has(57)).toBe(true); });
  it("ttl has 58", () => { const c = new TTLCache<number,number>(100, 60000); c.set(58, 58); expect(c.has(58)).toBe(true); });
  it("ttl has 59", () => { const c = new TTLCache<number,number>(100, 60000); c.set(59, 59); expect(c.has(59)).toBe(true); });
  it("ttl has 60", () => { const c = new TTLCache<number,number>(100, 60000); c.set(60, 60); expect(c.has(60)).toBe(true); });
  it("ttl has 61", () => { const c = new TTLCache<number,number>(100, 60000); c.set(61, 61); expect(c.has(61)).toBe(true); });
  it("ttl has 62", () => { const c = new TTLCache<number,number>(100, 60000); c.set(62, 62); expect(c.has(62)).toBe(true); });
  it("ttl has 63", () => { const c = new TTLCache<number,number>(100, 60000); c.set(63, 63); expect(c.has(63)).toBe(true); });
  it("ttl has 64", () => { const c = new TTLCache<number,number>(100, 60000); c.set(64, 64); expect(c.has(64)).toBe(true); });
  it("ttl has 65", () => { const c = new TTLCache<number,number>(100, 60000); c.set(65, 65); expect(c.has(65)).toBe(true); });
  it("ttl has 66", () => { const c = new TTLCache<number,number>(100, 60000); c.set(66, 66); expect(c.has(66)).toBe(true); });
  it("ttl has 67", () => { const c = new TTLCache<number,number>(100, 60000); c.set(67, 67); expect(c.has(67)).toBe(true); });
  it("ttl has 68", () => { const c = new TTLCache<number,number>(100, 60000); c.set(68, 68); expect(c.has(68)).toBe(true); });
  it("ttl has 69", () => { const c = new TTLCache<number,number>(100, 60000); c.set(69, 69); expect(c.has(69)).toBe(true); });
  it("ttl has 70", () => { const c = new TTLCache<number,number>(100, 60000); c.set(70, 70); expect(c.has(70)).toBe(true); });
  it("ttl has 71", () => { const c = new TTLCache<number,number>(100, 60000); c.set(71, 71); expect(c.has(71)).toBe(true); });
  it("ttl has 72", () => { const c = new TTLCache<number,number>(100, 60000); c.set(72, 72); expect(c.has(72)).toBe(true); });
  it("ttl has 73", () => { const c = new TTLCache<number,number>(100, 60000); c.set(73, 73); expect(c.has(73)).toBe(true); });
  it("ttl has 74", () => { const c = new TTLCache<number,number>(100, 60000); c.set(74, 74); expect(c.has(74)).toBe(true); });
  it("ttl has 75", () => { const c = new TTLCache<number,number>(100, 60000); c.set(75, 75); expect(c.has(75)).toBe(true); });
  it("ttl has 76", () => { const c = new TTLCache<number,number>(100, 60000); c.set(76, 76); expect(c.has(76)).toBe(true); });
  it("ttl has 77", () => { const c = new TTLCache<number,number>(100, 60000); c.set(77, 77); expect(c.has(77)).toBe(true); });
  it("ttl has 78", () => { const c = new TTLCache<number,number>(100, 60000); c.set(78, 78); expect(c.has(78)).toBe(true); });
  it("ttl has 79", () => { const c = new TTLCache<number,number>(100, 60000); c.set(79, 79); expect(c.has(79)).toBe(true); });
  it("ttl has 80", () => { const c = new TTLCache<number,number>(100, 60000); c.set(80, 80); expect(c.has(80)).toBe(true); });
  it("ttl has 81", () => { const c = new TTLCache<number,number>(100, 60000); c.set(81, 81); expect(c.has(81)).toBe(true); });
  it("ttl has 82", () => { const c = new TTLCache<number,number>(100, 60000); c.set(82, 82); expect(c.has(82)).toBe(true); });
  it("ttl has 83", () => { const c = new TTLCache<number,number>(100, 60000); c.set(83, 83); expect(c.has(83)).toBe(true); });
  it("ttl has 84", () => { const c = new TTLCache<number,number>(100, 60000); c.set(84, 84); expect(c.has(84)).toBe(true); });
  it("ttl has 85", () => { const c = new TTLCache<number,number>(100, 60000); c.set(85, 85); expect(c.has(85)).toBe(true); });
  it("ttl has 86", () => { const c = new TTLCache<number,number>(100, 60000); c.set(86, 86); expect(c.has(86)).toBe(true); });
  it("ttl has 87", () => { const c = new TTLCache<number,number>(100, 60000); c.set(87, 87); expect(c.has(87)).toBe(true); });
  it("ttl has 88", () => { const c = new TTLCache<number,number>(100, 60000); c.set(88, 88); expect(c.has(88)).toBe(true); });
  it("ttl has 89", () => { const c = new TTLCache<number,number>(100, 60000); c.set(89, 89); expect(c.has(89)).toBe(true); });
  it("ttl has 90", () => { const c = new TTLCache<number,number>(100, 60000); c.set(90, 90); expect(c.has(90)).toBe(true); });
  it("ttl has 91", () => { const c = new TTLCache<number,number>(100, 60000); c.set(91, 91); expect(c.has(91)).toBe(true); });
  it("ttl has 92", () => { const c = new TTLCache<number,number>(100, 60000); c.set(92, 92); expect(c.has(92)).toBe(true); });
  it("ttl has 93", () => { const c = new TTLCache<number,number>(100, 60000); c.set(93, 93); expect(c.has(93)).toBe(true); });
  it("ttl has 94", () => { const c = new TTLCache<number,number>(100, 60000); c.set(94, 94); expect(c.has(94)).toBe(true); });
  it("ttl has 95", () => { const c = new TTLCache<number,number>(100, 60000); c.set(95, 95); expect(c.has(95)).toBe(true); });
  it("ttl has 96", () => { const c = new TTLCache<number,number>(100, 60000); c.set(96, 96); expect(c.has(96)).toBe(true); });
  it("ttl has 97", () => { const c = new TTLCache<number,number>(100, 60000); c.set(97, 97); expect(c.has(97)).toBe(true); });
  it("ttl has 98", () => { const c = new TTLCache<number,number>(100, 60000); c.set(98, 98); expect(c.has(98)).toBe(true); });
  it("ttl has 99", () => { const c = new TTLCache<number,number>(100, 60000); c.set(99, 99); expect(c.has(99)).toBe(true); });
});

describe("LRUCache get undefined missing", () => {
  it("lru get undef 0", () => { const c = new LRUCache<number,number>(100); expect(c.get(2000)).toBeUndefined(); });
  it("lru get undef 1", () => { const c = new LRUCache<number,number>(100); expect(c.get(2001)).toBeUndefined(); });
  it("lru get undef 2", () => { const c = new LRUCache<number,number>(100); expect(c.get(2002)).toBeUndefined(); });
  it("lru get undef 3", () => { const c = new LRUCache<number,number>(100); expect(c.get(2003)).toBeUndefined(); });
  it("lru get undef 4", () => { const c = new LRUCache<number,number>(100); expect(c.get(2004)).toBeUndefined(); });
  it("lru get undef 5", () => { const c = new LRUCache<number,number>(100); expect(c.get(2005)).toBeUndefined(); });
  it("lru get undef 6", () => { const c = new LRUCache<number,number>(100); expect(c.get(2006)).toBeUndefined(); });
  it("lru get undef 7", () => { const c = new LRUCache<number,number>(100); expect(c.get(2007)).toBeUndefined(); });
  it("lru get undef 8", () => { const c = new LRUCache<number,number>(100); expect(c.get(2008)).toBeUndefined(); });
  it("lru get undef 9", () => { const c = new LRUCache<number,number>(100); expect(c.get(2009)).toBeUndefined(); });
  it("lru get undef 10", () => { const c = new LRUCache<number,number>(100); expect(c.get(2010)).toBeUndefined(); });
  it("lru get undef 11", () => { const c = new LRUCache<number,number>(100); expect(c.get(2011)).toBeUndefined(); });
  it("lru get undef 12", () => { const c = new LRUCache<number,number>(100); expect(c.get(2012)).toBeUndefined(); });
  it("lru get undef 13", () => { const c = new LRUCache<number,number>(100); expect(c.get(2013)).toBeUndefined(); });
  it("lru get undef 14", () => { const c = new LRUCache<number,number>(100); expect(c.get(2014)).toBeUndefined(); });
  it("lru get undef 15", () => { const c = new LRUCache<number,number>(100); expect(c.get(2015)).toBeUndefined(); });
  it("lru get undef 16", () => { const c = new LRUCache<number,number>(100); expect(c.get(2016)).toBeUndefined(); });
  it("lru get undef 17", () => { const c = new LRUCache<number,number>(100); expect(c.get(2017)).toBeUndefined(); });
  it("lru get undef 18", () => { const c = new LRUCache<number,number>(100); expect(c.get(2018)).toBeUndefined(); });
  it("lru get undef 19", () => { const c = new LRUCache<number,number>(100); expect(c.get(2019)).toBeUndefined(); });
  it("lru get undef 20", () => { const c = new LRUCache<number,number>(100); expect(c.get(2020)).toBeUndefined(); });
  it("lru get undef 21", () => { const c = new LRUCache<number,number>(100); expect(c.get(2021)).toBeUndefined(); });
  it("lru get undef 22", () => { const c = new LRUCache<number,number>(100); expect(c.get(2022)).toBeUndefined(); });
  it("lru get undef 23", () => { const c = new LRUCache<number,number>(100); expect(c.get(2023)).toBeUndefined(); });
  it("lru get undef 24", () => { const c = new LRUCache<number,number>(100); expect(c.get(2024)).toBeUndefined(); });
  it("lru get undef 25", () => { const c = new LRUCache<number,number>(100); expect(c.get(2025)).toBeUndefined(); });
  it("lru get undef 26", () => { const c = new LRUCache<number,number>(100); expect(c.get(2026)).toBeUndefined(); });
  it("lru get undef 27", () => { const c = new LRUCache<number,number>(100); expect(c.get(2027)).toBeUndefined(); });
  it("lru get undef 28", () => { const c = new LRUCache<number,number>(100); expect(c.get(2028)).toBeUndefined(); });
  it("lru get undef 29", () => { const c = new LRUCache<number,number>(100); expect(c.get(2029)).toBeUndefined(); });
  it("lru get undef 30", () => { const c = new LRUCache<number,number>(100); expect(c.get(2030)).toBeUndefined(); });
  it("lru get undef 31", () => { const c = new LRUCache<number,number>(100); expect(c.get(2031)).toBeUndefined(); });
  it("lru get undef 32", () => { const c = new LRUCache<number,number>(100); expect(c.get(2032)).toBeUndefined(); });
  it("lru get undef 33", () => { const c = new LRUCache<number,number>(100); expect(c.get(2033)).toBeUndefined(); });
  it("lru get undef 34", () => { const c = new LRUCache<number,number>(100); expect(c.get(2034)).toBeUndefined(); });
  it("lru get undef 35", () => { const c = new LRUCache<number,number>(100); expect(c.get(2035)).toBeUndefined(); });
  it("lru get undef 36", () => { const c = new LRUCache<number,number>(100); expect(c.get(2036)).toBeUndefined(); });
  it("lru get undef 37", () => { const c = new LRUCache<number,number>(100); expect(c.get(2037)).toBeUndefined(); });
  it("lru get undef 38", () => { const c = new LRUCache<number,number>(100); expect(c.get(2038)).toBeUndefined(); });
  it("lru get undef 39", () => { const c = new LRUCache<number,number>(100); expect(c.get(2039)).toBeUndefined(); });
  it("lru get undef 40", () => { const c = new LRUCache<number,number>(100); expect(c.get(2040)).toBeUndefined(); });
  it("lru get undef 41", () => { const c = new LRUCache<number,number>(100); expect(c.get(2041)).toBeUndefined(); });
  it("lru get undef 42", () => { const c = new LRUCache<number,number>(100); expect(c.get(2042)).toBeUndefined(); });
  it("lru get undef 43", () => { const c = new LRUCache<number,number>(100); expect(c.get(2043)).toBeUndefined(); });
  it("lru get undef 44", () => { const c = new LRUCache<number,number>(100); expect(c.get(2044)).toBeUndefined(); });
  it("lru get undef 45", () => { const c = new LRUCache<number,number>(100); expect(c.get(2045)).toBeUndefined(); });
  it("lru get undef 46", () => { const c = new LRUCache<number,number>(100); expect(c.get(2046)).toBeUndefined(); });
  it("lru get undef 47", () => { const c = new LRUCache<number,number>(100); expect(c.get(2047)).toBeUndefined(); });
  it("lru get undef 48", () => { const c = new LRUCache<number,number>(100); expect(c.get(2048)).toBeUndefined(); });
  it("lru get undef 49", () => { const c = new LRUCache<number,number>(100); expect(c.get(2049)).toBeUndefined(); });
  it("lru get undef 50", () => { const c = new LRUCache<number,number>(100); expect(c.get(2050)).toBeUndefined(); });
  it("lru get undef 51", () => { const c = new LRUCache<number,number>(100); expect(c.get(2051)).toBeUndefined(); });
  it("lru get undef 52", () => { const c = new LRUCache<number,number>(100); expect(c.get(2052)).toBeUndefined(); });
  it("lru get undef 53", () => { const c = new LRUCache<number,number>(100); expect(c.get(2053)).toBeUndefined(); });
  it("lru get undef 54", () => { const c = new LRUCache<number,number>(100); expect(c.get(2054)).toBeUndefined(); });
  it("lru get undef 55", () => { const c = new LRUCache<number,number>(100); expect(c.get(2055)).toBeUndefined(); });
  it("lru get undef 56", () => { const c = new LRUCache<number,number>(100); expect(c.get(2056)).toBeUndefined(); });
  it("lru get undef 57", () => { const c = new LRUCache<number,number>(100); expect(c.get(2057)).toBeUndefined(); });
  it("lru get undef 58", () => { const c = new LRUCache<number,number>(100); expect(c.get(2058)).toBeUndefined(); });
  it("lru get undef 59", () => { const c = new LRUCache<number,number>(100); expect(c.get(2059)).toBeUndefined(); });
  it("lru get undef 60", () => { const c = new LRUCache<number,number>(100); expect(c.get(2060)).toBeUndefined(); });
  it("lru get undef 61", () => { const c = new LRUCache<number,number>(100); expect(c.get(2061)).toBeUndefined(); });
  it("lru get undef 62", () => { const c = new LRUCache<number,number>(100); expect(c.get(2062)).toBeUndefined(); });
  it("lru get undef 63", () => { const c = new LRUCache<number,number>(100); expect(c.get(2063)).toBeUndefined(); });
  it("lru get undef 64", () => { const c = new LRUCache<number,number>(100); expect(c.get(2064)).toBeUndefined(); });
  it("lru get undef 65", () => { const c = new LRUCache<number,number>(100); expect(c.get(2065)).toBeUndefined(); });
  it("lru get undef 66", () => { const c = new LRUCache<number,number>(100); expect(c.get(2066)).toBeUndefined(); });
  it("lru get undef 67", () => { const c = new LRUCache<number,number>(100); expect(c.get(2067)).toBeUndefined(); });
  it("lru get undef 68", () => { const c = new LRUCache<number,number>(100); expect(c.get(2068)).toBeUndefined(); });
  it("lru get undef 69", () => { const c = new LRUCache<number,number>(100); expect(c.get(2069)).toBeUndefined(); });
  it("lru get undef 70", () => { const c = new LRUCache<number,number>(100); expect(c.get(2070)).toBeUndefined(); });
  it("lru get undef 71", () => { const c = new LRUCache<number,number>(100); expect(c.get(2071)).toBeUndefined(); });
  it("lru get undef 72", () => { const c = new LRUCache<number,number>(100); expect(c.get(2072)).toBeUndefined(); });
  it("lru get undef 73", () => { const c = new LRUCache<number,number>(100); expect(c.get(2073)).toBeUndefined(); });
  it("lru get undef 74", () => { const c = new LRUCache<number,number>(100); expect(c.get(2074)).toBeUndefined(); });
  it("lru get undef 75", () => { const c = new LRUCache<number,number>(100); expect(c.get(2075)).toBeUndefined(); });
  it("lru get undef 76", () => { const c = new LRUCache<number,number>(100); expect(c.get(2076)).toBeUndefined(); });
  it("lru get undef 77", () => { const c = new LRUCache<number,number>(100); expect(c.get(2077)).toBeUndefined(); });
  it("lru get undef 78", () => { const c = new LRUCache<number,number>(100); expect(c.get(2078)).toBeUndefined(); });
  it("lru get undef 79", () => { const c = new LRUCache<number,number>(100); expect(c.get(2079)).toBeUndefined(); });
  it("lru get undef 80", () => { const c = new LRUCache<number,number>(100); expect(c.get(2080)).toBeUndefined(); });
  it("lru get undef 81", () => { const c = new LRUCache<number,number>(100); expect(c.get(2081)).toBeUndefined(); });
  it("lru get undef 82", () => { const c = new LRUCache<number,number>(100); expect(c.get(2082)).toBeUndefined(); });
  it("lru get undef 83", () => { const c = new LRUCache<number,number>(100); expect(c.get(2083)).toBeUndefined(); });
  it("lru get undef 84", () => { const c = new LRUCache<number,number>(100); expect(c.get(2084)).toBeUndefined(); });
  it("lru get undef 85", () => { const c = new LRUCache<number,number>(100); expect(c.get(2085)).toBeUndefined(); });
  it("lru get undef 86", () => { const c = new LRUCache<number,number>(100); expect(c.get(2086)).toBeUndefined(); });
  it("lru get undef 87", () => { const c = new LRUCache<number,number>(100); expect(c.get(2087)).toBeUndefined(); });
  it("lru get undef 88", () => { const c = new LRUCache<number,number>(100); expect(c.get(2088)).toBeUndefined(); });
  it("lru get undef 89", () => { const c = new LRUCache<number,number>(100); expect(c.get(2089)).toBeUndefined(); });
  it("lru get undef 90", () => { const c = new LRUCache<number,number>(100); expect(c.get(2090)).toBeUndefined(); });
  it("lru get undef 91", () => { const c = new LRUCache<number,number>(100); expect(c.get(2091)).toBeUndefined(); });
  it("lru get undef 92", () => { const c = new LRUCache<number,number>(100); expect(c.get(2092)).toBeUndefined(); });
  it("lru get undef 93", () => { const c = new LRUCache<number,number>(100); expect(c.get(2093)).toBeUndefined(); });
  it("lru get undef 94", () => { const c = new LRUCache<number,number>(100); expect(c.get(2094)).toBeUndefined(); });
  it("lru get undef 95", () => { const c = new LRUCache<number,number>(100); expect(c.get(2095)).toBeUndefined(); });
  it("lru get undef 96", () => { const c = new LRUCache<number,number>(100); expect(c.get(2096)).toBeUndefined(); });
  it("lru get undef 97", () => { const c = new LRUCache<number,number>(100); expect(c.get(2097)).toBeUndefined(); });
  it("lru get undef 98", () => { const c = new LRUCache<number,number>(100); expect(c.get(2098)).toBeUndefined(); });
  it("lru get undef 99", () => { const c = new LRUCache<number,number>(100); expect(c.get(2099)).toBeUndefined(); });
});

