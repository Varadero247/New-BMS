{
  "name": "@ims/bulk-export",
  "version": "1.0.0",
  "description": "Streaming XLSX/PDF/CSV bulk export engine",
  "main": "src/index.ts",
  "scripts": { "test": "jest" },
  "jest": {
    "displayName": "bulk-export",
    "testEnvironment": "node",
    "testMatch": ["<rootDir>/__tests__/**/*.test.ts"],
    "transform": { "^.+\\.tsx?$": ["ts-jest", { "isolatedModules": true, "diagnostics": false }] }
  },
  "devDependencies": { "ts-jest": "*", "jest": "*", "@types/jest": "*", "typescript": "*" }
}
