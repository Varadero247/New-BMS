// Copyright (c) 2026 Nexara DMCC. All rights reserved.
// Proprietary and confidential. Unauthorised copying prohibited.
// See LICENCE file for details.

import { CartesianTree, Treap } from "../cartesian-tree";

describe("CartesianTree fromArray size", () => {
  it("ct1", () => { const arr = Array.from({length:1},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(1); });
  it("ct2", () => { const arr = Array.from({length:2},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(2); });
  it("ct3", () => { const arr = Array.from({length:3},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(3); });
  it("ct4", () => { const arr = Array.from({length:4},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(4); });
  it("ct5", () => { const arr = Array.from({length:5},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(5); });
  it("ct6", () => { const arr = Array.from({length:6},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(6); });
  it("ct7", () => { const arr = Array.from({length:7},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(7); });
  it("ct8", () => { const arr = Array.from({length:8},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(8); });
  it("ct9", () => { const arr = Array.from({length:9},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(9); });
  it("ct10", () => { const arr = Array.from({length:10},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(10); });
  it("ct11", () => { const arr = Array.from({length:11},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(11); });
  it("ct12", () => { const arr = Array.from({length:12},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(12); });
  it("ct13", () => { const arr = Array.from({length:13},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(13); });
  it("ct14", () => { const arr = Array.from({length:14},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(14); });
  it("ct15", () => { const arr = Array.from({length:15},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(15); });
  it("ct16", () => { const arr = Array.from({length:16},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(16); });
  it("ct17", () => { const arr = Array.from({length:17},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(17); });
  it("ct18", () => { const arr = Array.from({length:18},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(18); });
  it("ct19", () => { const arr = Array.from({length:19},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(19); });
  it("ct20", () => { const arr = Array.from({length:20},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(20); });
  it("ct21", () => { const arr = Array.from({length:21},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(21); });
  it("ct22", () => { const arr = Array.from({length:22},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(22); });
  it("ct23", () => { const arr = Array.from({length:23},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(23); });
  it("ct24", () => { const arr = Array.from({length:24},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(24); });
  it("ct25", () => { const arr = Array.from({length:25},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(25); });
  it("ct26", () => { const arr = Array.from({length:26},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(26); });
  it("ct27", () => { const arr = Array.from({length:27},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(27); });
  it("ct28", () => { const arr = Array.from({length:28},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(28); });
  it("ct29", () => { const arr = Array.from({length:29},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(29); });
  it("ct30", () => { const arr = Array.from({length:30},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(30); });
  it("ct31", () => { const arr = Array.from({length:31},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(31); });
  it("ct32", () => { const arr = Array.from({length:32},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(32); });
  it("ct33", () => { const arr = Array.from({length:33},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(33); });
  it("ct34", () => { const arr = Array.from({length:34},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(34); });
  it("ct35", () => { const arr = Array.from({length:35},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(35); });
  it("ct36", () => { const arr = Array.from({length:36},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(36); });
  it("ct37", () => { const arr = Array.from({length:37},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(37); });
  it("ct38", () => { const arr = Array.from({length:38},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(38); });
  it("ct39", () => { const arr = Array.from({length:39},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(39); });
  it("ct40", () => { const arr = Array.from({length:40},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(40); });
  it("ct41", () => { const arr = Array.from({length:41},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(41); });
  it("ct42", () => { const arr = Array.from({length:42},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(42); });
  it("ct43", () => { const arr = Array.from({length:43},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(43); });
  it("ct44", () => { const arr = Array.from({length:44},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(44); });
  it("ct45", () => { const arr = Array.from({length:45},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(45); });
  it("ct46", () => { const arr = Array.from({length:46},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(46); });
  it("ct47", () => { const arr = Array.from({length:47},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(47); });
  it("ct48", () => { const arr = Array.from({length:48},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(48); });
  it("ct49", () => { const arr = Array.from({length:49},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(49); });
  it("ct50", () => { const arr = Array.from({length:50},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(50); });
  it("ct51", () => { const arr = Array.from({length:51},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(51); });
  it("ct52", () => { const arr = Array.from({length:52},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(52); });
  it("ct53", () => { const arr = Array.from({length:53},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(53); });
  it("ct54", () => { const arr = Array.from({length:54},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(54); });
  it("ct55", () => { const arr = Array.from({length:55},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(55); });
  it("ct56", () => { const arr = Array.from({length:56},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(56); });
  it("ct57", () => { const arr = Array.from({length:57},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(57); });
  it("ct58", () => { const arr = Array.from({length:58},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(58); });
  it("ct59", () => { const arr = Array.from({length:59},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(59); });
  it("ct60", () => { const arr = Array.from({length:60},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(60); });
  it("ct61", () => { const arr = Array.from({length:61},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(61); });
  it("ct62", () => { const arr = Array.from({length:62},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(62); });
  it("ct63", () => { const arr = Array.from({length:63},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(63); });
  it("ct64", () => { const arr = Array.from({length:64},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(64); });
  it("ct65", () => { const arr = Array.from({length:65},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(65); });
  it("ct66", () => { const arr = Array.from({length:66},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(66); });
  it("ct67", () => { const arr = Array.from({length:67},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(67); });
  it("ct68", () => { const arr = Array.from({length:68},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(68); });
  it("ct69", () => { const arr = Array.from({length:69},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(69); });
  it("ct70", () => { const arr = Array.from({length:70},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(70); });
  it("ct71", () => { const arr = Array.from({length:71},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(71); });
  it("ct72", () => { const arr = Array.from({length:72},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(72); });
  it("ct73", () => { const arr = Array.from({length:73},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(73); });
  it("ct74", () => { const arr = Array.from({length:74},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(74); });
  it("ct75", () => { const arr = Array.from({length:75},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(75); });
  it("ct76", () => { const arr = Array.from({length:76},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(76); });
  it("ct77", () => { const arr = Array.from({length:77},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(77); });
  it("ct78", () => { const arr = Array.from({length:78},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(78); });
  it("ct79", () => { const arr = Array.from({length:79},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(79); });
  it("ct80", () => { const arr = Array.from({length:80},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(80); });
  it("ct81", () => { const arr = Array.from({length:81},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(81); });
  it("ct82", () => { const arr = Array.from({length:82},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(82); });
  it("ct83", () => { const arr = Array.from({length:83},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(83); });
  it("ct84", () => { const arr = Array.from({length:84},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(84); });
  it("ct85", () => { const arr = Array.from({length:85},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(85); });
  it("ct86", () => { const arr = Array.from({length:86},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(86); });
  it("ct87", () => { const arr = Array.from({length:87},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(87); });
  it("ct88", () => { const arr = Array.from({length:88},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(88); });
  it("ct89", () => { const arr = Array.from({length:89},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(89); });
  it("ct90", () => { const arr = Array.from({length:90},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(90); });
  it("ct91", () => { const arr = Array.from({length:91},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(91); });
  it("ct92", () => { const arr = Array.from({length:92},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(92); });
  it("ct93", () => { const arr = Array.from({length:93},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(93); });
  it("ct94", () => { const arr = Array.from({length:94},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(94); });
  it("ct95", () => { const arr = Array.from({length:95},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(95); });
  it("ct96", () => { const arr = Array.from({length:96},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(96); });
  it("ct97", () => { const arr = Array.from({length:97},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(97); });
  it("ct98", () => { const arr = Array.from({length:98},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(98); });
  it("ct99", () => { const arr = Array.from({length:99},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(99); });
  it("ct100", () => { const arr = Array.from({length:100},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(100); });
  it("ct101", () => { const arr = Array.from({length:101},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(101); });
  it("ct102", () => { const arr = Array.from({length:102},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(102); });
  it("ct103", () => { const arr = Array.from({length:103},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(103); });
  it("ct104", () => { const arr = Array.from({length:104},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(104); });
  it("ct105", () => { const arr = Array.from({length:105},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(105); });
  it("ct106", () => { const arr = Array.from({length:106},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(106); });
  it("ct107", () => { const arr = Array.from({length:107},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(107); });
  it("ct108", () => { const arr = Array.from({length:108},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(108); });
  it("ct109", () => { const arr = Array.from({length:109},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(109); });
  it("ct110", () => { const arr = Array.from({length:110},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(110); });
  it("ct111", () => { const arr = Array.from({length:111},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(111); });
  it("ct112", () => { const arr = Array.from({length:112},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(112); });
  it("ct113", () => { const arr = Array.from({length:113},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(113); });
  it("ct114", () => { const arr = Array.from({length:114},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(114); });
  it("ct115", () => { const arr = Array.from({length:115},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(115); });
  it("ct116", () => { const arr = Array.from({length:116},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(116); });
  it("ct117", () => { const arr = Array.from({length:117},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(117); });
  it("ct118", () => { const arr = Array.from({length:118},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(118); });
  it("ct119", () => { const arr = Array.from({length:119},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(119); });
  it("ct120", () => { const arr = Array.from({length:120},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(120); });
  it("ct121", () => { const arr = Array.from({length:121},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(121); });
  it("ct122", () => { const arr = Array.from({length:122},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(122); });
  it("ct123", () => { const arr = Array.from({length:123},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(123); });
  it("ct124", () => { const arr = Array.from({length:124},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(124); });
  it("ct125", () => { const arr = Array.from({length:125},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(125); });
  it("ct126", () => { const arr = Array.from({length:126},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(126); });
  it("ct127", () => { const arr = Array.from({length:127},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(127); });
  it("ct128", () => { const arr = Array.from({length:128},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(128); });
  it("ct129", () => { const arr = Array.from({length:129},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(129); });
  it("ct130", () => { const arr = Array.from({length:130},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(130); });
  it("ct131", () => { const arr = Array.from({length:131},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(131); });
  it("ct132", () => { const arr = Array.from({length:132},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(132); });
  it("ct133", () => { const arr = Array.from({length:133},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(133); });
  it("ct134", () => { const arr = Array.from({length:134},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(134); });
  it("ct135", () => { const arr = Array.from({length:135},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(135); });
  it("ct136", () => { const arr = Array.from({length:136},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(136); });
  it("ct137", () => { const arr = Array.from({length:137},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(137); });
  it("ct138", () => { const arr = Array.from({length:138},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(138); });
  it("ct139", () => { const arr = Array.from({length:139},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(139); });
  it("ct140", () => { const arr = Array.from({length:140},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(140); });
  it("ct141", () => { const arr = Array.from({length:141},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(141); });
  it("ct142", () => { const arr = Array.from({length:142},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(142); });
  it("ct143", () => { const arr = Array.from({length:143},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(143); });
  it("ct144", () => { const arr = Array.from({length:144},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(144); });
  it("ct145", () => { const arr = Array.from({length:145},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(145); });
  it("ct146", () => { const arr = Array.from({length:146},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(146); });
  it("ct147", () => { const arr = Array.from({length:147},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(147); });
  it("ct148", () => { const arr = Array.from({length:148},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(148); });
  it("ct149", () => { const arr = Array.from({length:149},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(149); });
  it("ct150", () => { const arr = Array.from({length:150},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(150); });
  it("ct151", () => { const arr = Array.from({length:151},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(151); });
  it("ct152", () => { const arr = Array.from({length:152},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(152); });
  it("ct153", () => { const arr = Array.from({length:153},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(153); });
  it("ct154", () => { const arr = Array.from({length:154},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(154); });
  it("ct155", () => { const arr = Array.from({length:155},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(155); });
  it("ct156", () => { const arr = Array.from({length:156},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(156); });
  it("ct157", () => { const arr = Array.from({length:157},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(157); });
  it("ct158", () => { const arr = Array.from({length:158},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(158); });
  it("ct159", () => { const arr = Array.from({length:159},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(159); });
  it("ct160", () => { const arr = Array.from({length:160},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(160); });
  it("ct161", () => { const arr = Array.from({length:161},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(161); });
  it("ct162", () => { const arr = Array.from({length:162},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(162); });
  it("ct163", () => { const arr = Array.from({length:163},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(163); });
  it("ct164", () => { const arr = Array.from({length:164},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(164); });
  it("ct165", () => { const arr = Array.from({length:165},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(165); });
  it("ct166", () => { const arr = Array.from({length:166},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(166); });
  it("ct167", () => { const arr = Array.from({length:167},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(167); });
  it("ct168", () => { const arr = Array.from({length:168},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(168); });
  it("ct169", () => { const arr = Array.from({length:169},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(169); });
  it("ct170", () => { const arr = Array.from({length:170},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(170); });
  it("ct171", () => { const arr = Array.from({length:171},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(171); });
  it("ct172", () => { const arr = Array.from({length:172},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(172); });
  it("ct173", () => { const arr = Array.from({length:173},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(173); });
  it("ct174", () => { const arr = Array.from({length:174},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(174); });
  it("ct175", () => { const arr = Array.from({length:175},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(175); });
  it("ct176", () => { const arr = Array.from({length:176},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(176); });
  it("ct177", () => { const arr = Array.from({length:177},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(177); });
  it("ct178", () => { const arr = Array.from({length:178},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(178); });
  it("ct179", () => { const arr = Array.from({length:179},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(179); });
  it("ct180", () => { const arr = Array.from({length:180},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(180); });
  it("ct181", () => { const arr = Array.from({length:181},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(181); });
  it("ct182", () => { const arr = Array.from({length:182},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(182); });
  it("ct183", () => { const arr = Array.from({length:183},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(183); });
  it("ct184", () => { const arr = Array.from({length:184},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(184); });
  it("ct185", () => { const arr = Array.from({length:185},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(185); });
  it("ct186", () => { const arr = Array.from({length:186},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(186); });
  it("ct187", () => { const arr = Array.from({length:187},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(187); });
  it("ct188", () => { const arr = Array.from({length:188},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(188); });
  it("ct189", () => { const arr = Array.from({length:189},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(189); });
  it("ct190", () => { const arr = Array.from({length:190},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(190); });
  it("ct191", () => { const arr = Array.from({length:191},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(191); });
  it("ct192", () => { const arr = Array.from({length:192},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(192); });
  it("ct193", () => { const arr = Array.from({length:193},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(193); });
  it("ct194", () => { const arr = Array.from({length:194},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(194); });
  it("ct195", () => { const arr = Array.from({length:195},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(195); });
  it("ct196", () => { const arr = Array.from({length:196},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(196); });
  it("ct197", () => { const arr = Array.from({length:197},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(197); });
  it("ct198", () => { const arr = Array.from({length:198},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(198); });
  it("ct199", () => { const arr = Array.from({length:199},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(199); });
  it("ct200", () => { const arr = Array.from({length:200},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.size).toBe(200); });
});

describe("CartesianTree min", () => {
  it("cmin1", () => { const arr = Array.from({length:1},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin2", () => { const arr = Array.from({length:2},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin3", () => { const arr = Array.from({length:3},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin4", () => { const arr = Array.from({length:4},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin5", () => { const arr = Array.from({length:5},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin6", () => { const arr = Array.from({length:6},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin7", () => { const arr = Array.from({length:7},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin8", () => { const arr = Array.from({length:8},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin9", () => { const arr = Array.from({length:9},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin10", () => { const arr = Array.from({length:10},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin11", () => { const arr = Array.from({length:11},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin12", () => { const arr = Array.from({length:12},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin13", () => { const arr = Array.from({length:13},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin14", () => { const arr = Array.from({length:14},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin15", () => { const arr = Array.from({length:15},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin16", () => { const arr = Array.from({length:16},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin17", () => { const arr = Array.from({length:17},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin18", () => { const arr = Array.from({length:18},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin19", () => { const arr = Array.from({length:19},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin20", () => { const arr = Array.from({length:20},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin21", () => { const arr = Array.from({length:21},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin22", () => { const arr = Array.from({length:22},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin23", () => { const arr = Array.from({length:23},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin24", () => { const arr = Array.from({length:24},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin25", () => { const arr = Array.from({length:25},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin26", () => { const arr = Array.from({length:26},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin27", () => { const arr = Array.from({length:27},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin28", () => { const arr = Array.from({length:28},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin29", () => { const arr = Array.from({length:29},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin30", () => { const arr = Array.from({length:30},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin31", () => { const arr = Array.from({length:31},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin32", () => { const arr = Array.from({length:32},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin33", () => { const arr = Array.from({length:33},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin34", () => { const arr = Array.from({length:34},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin35", () => { const arr = Array.from({length:35},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin36", () => { const arr = Array.from({length:36},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin37", () => { const arr = Array.from({length:37},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin38", () => { const arr = Array.from({length:38},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin39", () => { const arr = Array.from({length:39},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin40", () => { const arr = Array.from({length:40},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin41", () => { const arr = Array.from({length:41},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin42", () => { const arr = Array.from({length:42},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin43", () => { const arr = Array.from({length:43},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin44", () => { const arr = Array.from({length:44},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin45", () => { const arr = Array.from({length:45},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin46", () => { const arr = Array.from({length:46},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin47", () => { const arr = Array.from({length:47},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin48", () => { const arr = Array.from({length:48},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin49", () => { const arr = Array.from({length:49},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin50", () => { const arr = Array.from({length:50},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin51", () => { const arr = Array.from({length:51},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin52", () => { const arr = Array.from({length:52},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin53", () => { const arr = Array.from({length:53},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin54", () => { const arr = Array.from({length:54},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin55", () => { const arr = Array.from({length:55},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin56", () => { const arr = Array.from({length:56},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin57", () => { const arr = Array.from({length:57},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin58", () => { const arr = Array.from({length:58},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin59", () => { const arr = Array.from({length:59},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin60", () => { const arr = Array.from({length:60},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin61", () => { const arr = Array.from({length:61},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin62", () => { const arr = Array.from({length:62},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin63", () => { const arr = Array.from({length:63},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin64", () => { const arr = Array.from({length:64},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin65", () => { const arr = Array.from({length:65},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin66", () => { const arr = Array.from({length:66},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin67", () => { const arr = Array.from({length:67},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin68", () => { const arr = Array.from({length:68},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin69", () => { const arr = Array.from({length:69},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin70", () => { const arr = Array.from({length:70},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin71", () => { const arr = Array.from({length:71},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin72", () => { const arr = Array.from({length:72},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin73", () => { const arr = Array.from({length:73},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin74", () => { const arr = Array.from({length:74},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin75", () => { const arr = Array.from({length:75},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin76", () => { const arr = Array.from({length:76},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin77", () => { const arr = Array.from({length:77},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin78", () => { const arr = Array.from({length:78},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin79", () => { const arr = Array.from({length:79},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin80", () => { const arr = Array.from({length:80},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin81", () => { const arr = Array.from({length:81},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin82", () => { const arr = Array.from({length:82},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin83", () => { const arr = Array.from({length:83},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin84", () => { const arr = Array.from({length:84},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin85", () => { const arr = Array.from({length:85},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin86", () => { const arr = Array.from({length:86},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin87", () => { const arr = Array.from({length:87},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin88", () => { const arr = Array.from({length:88},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin89", () => { const arr = Array.from({length:89},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin90", () => { const arr = Array.from({length:90},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin91", () => { const arr = Array.from({length:91},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin92", () => { const arr = Array.from({length:92},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin93", () => { const arr = Array.from({length:93},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin94", () => { const arr = Array.from({length:94},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin95", () => { const arr = Array.from({length:95},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin96", () => { const arr = Array.from({length:96},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin97", () => { const arr = Array.from({length:97},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin98", () => { const arr = Array.from({length:98},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin99", () => { const arr = Array.from({length:99},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin100", () => { const arr = Array.from({length:100},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin101", () => { const arr = Array.from({length:101},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin102", () => { const arr = Array.from({length:102},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin103", () => { const arr = Array.from({length:103},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin104", () => { const arr = Array.from({length:104},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin105", () => { const arr = Array.from({length:105},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin106", () => { const arr = Array.from({length:106},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin107", () => { const arr = Array.from({length:107},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin108", () => { const arr = Array.from({length:108},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin109", () => { const arr = Array.from({length:109},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin110", () => { const arr = Array.from({length:110},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin111", () => { const arr = Array.from({length:111},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin112", () => { const arr = Array.from({length:112},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin113", () => { const arr = Array.from({length:113},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin114", () => { const arr = Array.from({length:114},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin115", () => { const arr = Array.from({length:115},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin116", () => { const arr = Array.from({length:116},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin117", () => { const arr = Array.from({length:117},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin118", () => { const arr = Array.from({length:118},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin119", () => { const arr = Array.from({length:119},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin120", () => { const arr = Array.from({length:120},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin121", () => { const arr = Array.from({length:121},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin122", () => { const arr = Array.from({length:122},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin123", () => { const arr = Array.from({length:123},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin124", () => { const arr = Array.from({length:124},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin125", () => { const arr = Array.from({length:125},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin126", () => { const arr = Array.from({length:126},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin127", () => { const arr = Array.from({length:127},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin128", () => { const arr = Array.from({length:128},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin129", () => { const arr = Array.from({length:129},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin130", () => { const arr = Array.from({length:130},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin131", () => { const arr = Array.from({length:131},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin132", () => { const arr = Array.from({length:132},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin133", () => { const arr = Array.from({length:133},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin134", () => { const arr = Array.from({length:134},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin135", () => { const arr = Array.from({length:135},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin136", () => { const arr = Array.from({length:136},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin137", () => { const arr = Array.from({length:137},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin138", () => { const arr = Array.from({length:138},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin139", () => { const arr = Array.from({length:139},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin140", () => { const arr = Array.from({length:140},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin141", () => { const arr = Array.from({length:141},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin142", () => { const arr = Array.from({length:142},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin143", () => { const arr = Array.from({length:143},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin144", () => { const arr = Array.from({length:144},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin145", () => { const arr = Array.from({length:145},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin146", () => { const arr = Array.from({length:146},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin147", () => { const arr = Array.from({length:147},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin148", () => { const arr = Array.from({length:148},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin149", () => { const arr = Array.from({length:149},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin150", () => { const arr = Array.from({length:150},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin151", () => { const arr = Array.from({length:151},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin152", () => { const arr = Array.from({length:152},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin153", () => { const arr = Array.from({length:153},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin154", () => { const arr = Array.from({length:154},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin155", () => { const arr = Array.from({length:155},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin156", () => { const arr = Array.from({length:156},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin157", () => { const arr = Array.from({length:157},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin158", () => { const arr = Array.from({length:158},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin159", () => { const arr = Array.from({length:159},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin160", () => { const arr = Array.from({length:160},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin161", () => { const arr = Array.from({length:161},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin162", () => { const arr = Array.from({length:162},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin163", () => { const arr = Array.from({length:163},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin164", () => { const arr = Array.from({length:164},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin165", () => { const arr = Array.from({length:165},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin166", () => { const arr = Array.from({length:166},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin167", () => { const arr = Array.from({length:167},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin168", () => { const arr = Array.from({length:168},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin169", () => { const arr = Array.from({length:169},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin170", () => { const arr = Array.from({length:170},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin171", () => { const arr = Array.from({length:171},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin172", () => { const arr = Array.from({length:172},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin173", () => { const arr = Array.from({length:173},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin174", () => { const arr = Array.from({length:174},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin175", () => { const arr = Array.from({length:175},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin176", () => { const arr = Array.from({length:176},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin177", () => { const arr = Array.from({length:177},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin178", () => { const arr = Array.from({length:178},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin179", () => { const arr = Array.from({length:179},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin180", () => { const arr = Array.from({length:180},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin181", () => { const arr = Array.from({length:181},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin182", () => { const arr = Array.from({length:182},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin183", () => { const arr = Array.from({length:183},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin184", () => { const arr = Array.from({length:184},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin185", () => { const arr = Array.from({length:185},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin186", () => { const arr = Array.from({length:186},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin187", () => { const arr = Array.from({length:187},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin188", () => { const arr = Array.from({length:188},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin189", () => { const arr = Array.from({length:189},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin190", () => { const arr = Array.from({length:190},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin191", () => { const arr = Array.from({length:191},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin192", () => { const arr = Array.from({length:192},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin193", () => { const arr = Array.from({length:193},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin194", () => { const arr = Array.from({length:194},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin195", () => { const arr = Array.from({length:195},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin196", () => { const arr = Array.from({length:196},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin197", () => { const arr = Array.from({length:197},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin198", () => { const arr = Array.from({length:198},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin199", () => { const arr = Array.from({length:199},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
  it("cmin200", () => { const arr = Array.from({length:200},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.min()).toBe(1); });
});

describe("CartesianTree max", () => {
  it("cmax1", () => { const arr = Array.from({length:1},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(1); });
  it("cmax2", () => { const arr = Array.from({length:2},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(2); });
  it("cmax3", () => { const arr = Array.from({length:3},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(3); });
  it("cmax4", () => { const arr = Array.from({length:4},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(4); });
  it("cmax5", () => { const arr = Array.from({length:5},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(5); });
  it("cmax6", () => { const arr = Array.from({length:6},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(6); });
  it("cmax7", () => { const arr = Array.from({length:7},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(7); });
  it("cmax8", () => { const arr = Array.from({length:8},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(8); });
  it("cmax9", () => { const arr = Array.from({length:9},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(9); });
  it("cmax10", () => { const arr = Array.from({length:10},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(10); });
  it("cmax11", () => { const arr = Array.from({length:11},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(11); });
  it("cmax12", () => { const arr = Array.from({length:12},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(12); });
  it("cmax13", () => { const arr = Array.from({length:13},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(13); });
  it("cmax14", () => { const arr = Array.from({length:14},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(14); });
  it("cmax15", () => { const arr = Array.from({length:15},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(15); });
  it("cmax16", () => { const arr = Array.from({length:16},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(16); });
  it("cmax17", () => { const arr = Array.from({length:17},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(17); });
  it("cmax18", () => { const arr = Array.from({length:18},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(18); });
  it("cmax19", () => { const arr = Array.from({length:19},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(19); });
  it("cmax20", () => { const arr = Array.from({length:20},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(20); });
  it("cmax21", () => { const arr = Array.from({length:21},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(21); });
  it("cmax22", () => { const arr = Array.from({length:22},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(22); });
  it("cmax23", () => { const arr = Array.from({length:23},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(23); });
  it("cmax24", () => { const arr = Array.from({length:24},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(24); });
  it("cmax25", () => { const arr = Array.from({length:25},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(25); });
  it("cmax26", () => { const arr = Array.from({length:26},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(26); });
  it("cmax27", () => { const arr = Array.from({length:27},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(27); });
  it("cmax28", () => { const arr = Array.from({length:28},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(28); });
  it("cmax29", () => { const arr = Array.from({length:29},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(29); });
  it("cmax30", () => { const arr = Array.from({length:30},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(30); });
  it("cmax31", () => { const arr = Array.from({length:31},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(31); });
  it("cmax32", () => { const arr = Array.from({length:32},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(32); });
  it("cmax33", () => { const arr = Array.from({length:33},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(33); });
  it("cmax34", () => { const arr = Array.from({length:34},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(34); });
  it("cmax35", () => { const arr = Array.from({length:35},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(35); });
  it("cmax36", () => { const arr = Array.from({length:36},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(36); });
  it("cmax37", () => { const arr = Array.from({length:37},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(37); });
  it("cmax38", () => { const arr = Array.from({length:38},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(38); });
  it("cmax39", () => { const arr = Array.from({length:39},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(39); });
  it("cmax40", () => { const arr = Array.from({length:40},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(40); });
  it("cmax41", () => { const arr = Array.from({length:41},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(41); });
  it("cmax42", () => { const arr = Array.from({length:42},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(42); });
  it("cmax43", () => { const arr = Array.from({length:43},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(43); });
  it("cmax44", () => { const arr = Array.from({length:44},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(44); });
  it("cmax45", () => { const arr = Array.from({length:45},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(45); });
  it("cmax46", () => { const arr = Array.from({length:46},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(46); });
  it("cmax47", () => { const arr = Array.from({length:47},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(47); });
  it("cmax48", () => { const arr = Array.from({length:48},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(48); });
  it("cmax49", () => { const arr = Array.from({length:49},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(49); });
  it("cmax50", () => { const arr = Array.from({length:50},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(50); });
  it("cmax51", () => { const arr = Array.from({length:51},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(51); });
  it("cmax52", () => { const arr = Array.from({length:52},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(52); });
  it("cmax53", () => { const arr = Array.from({length:53},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(53); });
  it("cmax54", () => { const arr = Array.from({length:54},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(54); });
  it("cmax55", () => { const arr = Array.from({length:55},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(55); });
  it("cmax56", () => { const arr = Array.from({length:56},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(56); });
  it("cmax57", () => { const arr = Array.from({length:57},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(57); });
  it("cmax58", () => { const arr = Array.from({length:58},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(58); });
  it("cmax59", () => { const arr = Array.from({length:59},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(59); });
  it("cmax60", () => { const arr = Array.from({length:60},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(60); });
  it("cmax61", () => { const arr = Array.from({length:61},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(61); });
  it("cmax62", () => { const arr = Array.from({length:62},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(62); });
  it("cmax63", () => { const arr = Array.from({length:63},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(63); });
  it("cmax64", () => { const arr = Array.from({length:64},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(64); });
  it("cmax65", () => { const arr = Array.from({length:65},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(65); });
  it("cmax66", () => { const arr = Array.from({length:66},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(66); });
  it("cmax67", () => { const arr = Array.from({length:67},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(67); });
  it("cmax68", () => { const arr = Array.from({length:68},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(68); });
  it("cmax69", () => { const arr = Array.from({length:69},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(69); });
  it("cmax70", () => { const arr = Array.from({length:70},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(70); });
  it("cmax71", () => { const arr = Array.from({length:71},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(71); });
  it("cmax72", () => { const arr = Array.from({length:72},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(72); });
  it("cmax73", () => { const arr = Array.from({length:73},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(73); });
  it("cmax74", () => { const arr = Array.from({length:74},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(74); });
  it("cmax75", () => { const arr = Array.from({length:75},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(75); });
  it("cmax76", () => { const arr = Array.from({length:76},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(76); });
  it("cmax77", () => { const arr = Array.from({length:77},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(77); });
  it("cmax78", () => { const arr = Array.from({length:78},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(78); });
  it("cmax79", () => { const arr = Array.from({length:79},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(79); });
  it("cmax80", () => { const arr = Array.from({length:80},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(80); });
  it("cmax81", () => { const arr = Array.from({length:81},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(81); });
  it("cmax82", () => { const arr = Array.from({length:82},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(82); });
  it("cmax83", () => { const arr = Array.from({length:83},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(83); });
  it("cmax84", () => { const arr = Array.from({length:84},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(84); });
  it("cmax85", () => { const arr = Array.from({length:85},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(85); });
  it("cmax86", () => { const arr = Array.from({length:86},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(86); });
  it("cmax87", () => { const arr = Array.from({length:87},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(87); });
  it("cmax88", () => { const arr = Array.from({length:88},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(88); });
  it("cmax89", () => { const arr = Array.from({length:89},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(89); });
  it("cmax90", () => { const arr = Array.from({length:90},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(90); });
  it("cmax91", () => { const arr = Array.from({length:91},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(91); });
  it("cmax92", () => { const arr = Array.from({length:92},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(92); });
  it("cmax93", () => { const arr = Array.from({length:93},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(93); });
  it("cmax94", () => { const arr = Array.from({length:94},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(94); });
  it("cmax95", () => { const arr = Array.from({length:95},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(95); });
  it("cmax96", () => { const arr = Array.from({length:96},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(96); });
  it("cmax97", () => { const arr = Array.from({length:97},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(97); });
  it("cmax98", () => { const arr = Array.from({length:98},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(98); });
  it("cmax99", () => { const arr = Array.from({length:99},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(99); });
  it("cmax100", () => { const arr = Array.from({length:100},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(100); });
  it("cmax101", () => { const arr = Array.from({length:101},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(101); });
  it("cmax102", () => { const arr = Array.from({length:102},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(102); });
  it("cmax103", () => { const arr = Array.from({length:103},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(103); });
  it("cmax104", () => { const arr = Array.from({length:104},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(104); });
  it("cmax105", () => { const arr = Array.from({length:105},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(105); });
  it("cmax106", () => { const arr = Array.from({length:106},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(106); });
  it("cmax107", () => { const arr = Array.from({length:107},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(107); });
  it("cmax108", () => { const arr = Array.from({length:108},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(108); });
  it("cmax109", () => { const arr = Array.from({length:109},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(109); });
  it("cmax110", () => { const arr = Array.from({length:110},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(110); });
  it("cmax111", () => { const arr = Array.from({length:111},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(111); });
  it("cmax112", () => { const arr = Array.from({length:112},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(112); });
  it("cmax113", () => { const arr = Array.from({length:113},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(113); });
  it("cmax114", () => { const arr = Array.from({length:114},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(114); });
  it("cmax115", () => { const arr = Array.from({length:115},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(115); });
  it("cmax116", () => { const arr = Array.from({length:116},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(116); });
  it("cmax117", () => { const arr = Array.from({length:117},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(117); });
  it("cmax118", () => { const arr = Array.from({length:118},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(118); });
  it("cmax119", () => { const arr = Array.from({length:119},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(119); });
  it("cmax120", () => { const arr = Array.from({length:120},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(120); });
  it("cmax121", () => { const arr = Array.from({length:121},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(121); });
  it("cmax122", () => { const arr = Array.from({length:122},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(122); });
  it("cmax123", () => { const arr = Array.from({length:123},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(123); });
  it("cmax124", () => { const arr = Array.from({length:124},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(124); });
  it("cmax125", () => { const arr = Array.from({length:125},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(125); });
  it("cmax126", () => { const arr = Array.from({length:126},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(126); });
  it("cmax127", () => { const arr = Array.from({length:127},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(127); });
  it("cmax128", () => { const arr = Array.from({length:128},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(128); });
  it("cmax129", () => { const arr = Array.from({length:129},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(129); });
  it("cmax130", () => { const arr = Array.from({length:130},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(130); });
  it("cmax131", () => { const arr = Array.from({length:131},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(131); });
  it("cmax132", () => { const arr = Array.from({length:132},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(132); });
  it("cmax133", () => { const arr = Array.from({length:133},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(133); });
  it("cmax134", () => { const arr = Array.from({length:134},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(134); });
  it("cmax135", () => { const arr = Array.from({length:135},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(135); });
  it("cmax136", () => { const arr = Array.from({length:136},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(136); });
  it("cmax137", () => { const arr = Array.from({length:137},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(137); });
  it("cmax138", () => { const arr = Array.from({length:138},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(138); });
  it("cmax139", () => { const arr = Array.from({length:139},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(139); });
  it("cmax140", () => { const arr = Array.from({length:140},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(140); });
  it("cmax141", () => { const arr = Array.from({length:141},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(141); });
  it("cmax142", () => { const arr = Array.from({length:142},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(142); });
  it("cmax143", () => { const arr = Array.from({length:143},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(143); });
  it("cmax144", () => { const arr = Array.from({length:144},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(144); });
  it("cmax145", () => { const arr = Array.from({length:145},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(145); });
  it("cmax146", () => { const arr = Array.from({length:146},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(146); });
  it("cmax147", () => { const arr = Array.from({length:147},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(147); });
  it("cmax148", () => { const arr = Array.from({length:148},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(148); });
  it("cmax149", () => { const arr = Array.from({length:149},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(149); });
  it("cmax150", () => { const arr = Array.from({length:150},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(150); });
  it("cmax151", () => { const arr = Array.from({length:151},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(151); });
  it("cmax152", () => { const arr = Array.from({length:152},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(152); });
  it("cmax153", () => { const arr = Array.from({length:153},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(153); });
  it("cmax154", () => { const arr = Array.from({length:154},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(154); });
  it("cmax155", () => { const arr = Array.from({length:155},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(155); });
  it("cmax156", () => { const arr = Array.from({length:156},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(156); });
  it("cmax157", () => { const arr = Array.from({length:157},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(157); });
  it("cmax158", () => { const arr = Array.from({length:158},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(158); });
  it("cmax159", () => { const arr = Array.from({length:159},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(159); });
  it("cmax160", () => { const arr = Array.from({length:160},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(160); });
  it("cmax161", () => { const arr = Array.from({length:161},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(161); });
  it("cmax162", () => { const arr = Array.from({length:162},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(162); });
  it("cmax163", () => { const arr = Array.from({length:163},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(163); });
  it("cmax164", () => { const arr = Array.from({length:164},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(164); });
  it("cmax165", () => { const arr = Array.from({length:165},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(165); });
  it("cmax166", () => { const arr = Array.from({length:166},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(166); });
  it("cmax167", () => { const arr = Array.from({length:167},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(167); });
  it("cmax168", () => { const arr = Array.from({length:168},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(168); });
  it("cmax169", () => { const arr = Array.from({length:169},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(169); });
  it("cmax170", () => { const arr = Array.from({length:170},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(170); });
  it("cmax171", () => { const arr = Array.from({length:171},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(171); });
  it("cmax172", () => { const arr = Array.from({length:172},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(172); });
  it("cmax173", () => { const arr = Array.from({length:173},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(173); });
  it("cmax174", () => { const arr = Array.from({length:174},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(174); });
  it("cmax175", () => { const arr = Array.from({length:175},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(175); });
  it("cmax176", () => { const arr = Array.from({length:176},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(176); });
  it("cmax177", () => { const arr = Array.from({length:177},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(177); });
  it("cmax178", () => { const arr = Array.from({length:178},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(178); });
  it("cmax179", () => { const arr = Array.from({length:179},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(179); });
  it("cmax180", () => { const arr = Array.from({length:180},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(180); });
  it("cmax181", () => { const arr = Array.from({length:181},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(181); });
  it("cmax182", () => { const arr = Array.from({length:182},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(182); });
  it("cmax183", () => { const arr = Array.from({length:183},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(183); });
  it("cmax184", () => { const arr = Array.from({length:184},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(184); });
  it("cmax185", () => { const arr = Array.from({length:185},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(185); });
  it("cmax186", () => { const arr = Array.from({length:186},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(186); });
  it("cmax187", () => { const arr = Array.from({length:187},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(187); });
  it("cmax188", () => { const arr = Array.from({length:188},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(188); });
  it("cmax189", () => { const arr = Array.from({length:189},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(189); });
  it("cmax190", () => { const arr = Array.from({length:190},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(190); });
  it("cmax191", () => { const arr = Array.from({length:191},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(191); });
  it("cmax192", () => { const arr = Array.from({length:192},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(192); });
  it("cmax193", () => { const arr = Array.from({length:193},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(193); });
  it("cmax194", () => { const arr = Array.from({length:194},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(194); });
  it("cmax195", () => { const arr = Array.from({length:195},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(195); });
  it("cmax196", () => { const arr = Array.from({length:196},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(196); });
  it("cmax197", () => { const arr = Array.from({length:197},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(197); });
  it("cmax198", () => { const arr = Array.from({length:198},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(198); });
  it("cmax199", () => { const arr = Array.from({length:199},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(199); });
  it("cmax200", () => { const arr = Array.from({length:200},(_, i)=>i+1); const t = CartesianTree.fromArray(arr); expect(t.max()).toBe(200); });
});

describe("CartesianTree toArray", () => {
  it("carr1", () => { const arr=[]; for(var x=0;x<1;x++) arr.push(1-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(1); });
  it("carr2", () => { const arr=[]; for(var x=0;x<2;x++) arr.push(2-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(2); });
  it("carr3", () => { const arr=[]; for(var x=0;x<3;x++) arr.push(3-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(3); });
  it("carr4", () => { const arr=[]; for(var x=0;x<4;x++) arr.push(4-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(4); });
  it("carr5", () => { const arr=[]; for(var x=0;x<5;x++) arr.push(5-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(5); });
  it("carr6", () => { const arr=[]; for(var x=0;x<6;x++) arr.push(6-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(6); });
  it("carr7", () => { const arr=[]; for(var x=0;x<7;x++) arr.push(7-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(7); });
  it("carr8", () => { const arr=[]; for(var x=0;x<8;x++) arr.push(8-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(8); });
  it("carr9", () => { const arr=[]; for(var x=0;x<9;x++) arr.push(9-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(9); });
  it("carr10", () => { const arr=[]; for(var x=0;x<10;x++) arr.push(10-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(10); });
  it("carr11", () => { const arr=[]; for(var x=0;x<11;x++) arr.push(11-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(11); });
  it("carr12", () => { const arr=[]; for(var x=0;x<12;x++) arr.push(12-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(12); });
  it("carr13", () => { const arr=[]; for(var x=0;x<13;x++) arr.push(13-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(13); });
  it("carr14", () => { const arr=[]; for(var x=0;x<14;x++) arr.push(14-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(14); });
  it("carr15", () => { const arr=[]; for(var x=0;x<15;x++) arr.push(15-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(15); });
  it("carr16", () => { const arr=[]; for(var x=0;x<16;x++) arr.push(16-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(16); });
  it("carr17", () => { const arr=[]; for(var x=0;x<17;x++) arr.push(17-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(17); });
  it("carr18", () => { const arr=[]; for(var x=0;x<18;x++) arr.push(18-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(18); });
  it("carr19", () => { const arr=[]; for(var x=0;x<19;x++) arr.push(19-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(19); });
  it("carr20", () => { const arr=[]; for(var x=0;x<20;x++) arr.push(20-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(20); });
  it("carr21", () => { const arr=[]; for(var x=0;x<21;x++) arr.push(21-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(21); });
  it("carr22", () => { const arr=[]; for(var x=0;x<22;x++) arr.push(22-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(22); });
  it("carr23", () => { const arr=[]; for(var x=0;x<23;x++) arr.push(23-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(23); });
  it("carr24", () => { const arr=[]; for(var x=0;x<24;x++) arr.push(24-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(24); });
  it("carr25", () => { const arr=[]; for(var x=0;x<25;x++) arr.push(25-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(25); });
  it("carr26", () => { const arr=[]; for(var x=0;x<26;x++) arr.push(26-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(26); });
  it("carr27", () => { const arr=[]; for(var x=0;x<27;x++) arr.push(27-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(27); });
  it("carr28", () => { const arr=[]; for(var x=0;x<28;x++) arr.push(28-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(28); });
  it("carr29", () => { const arr=[]; for(var x=0;x<29;x++) arr.push(29-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(29); });
  it("carr30", () => { const arr=[]; for(var x=0;x<30;x++) arr.push(30-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(30); });
  it("carr31", () => { const arr=[]; for(var x=0;x<31;x++) arr.push(31-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(31); });
  it("carr32", () => { const arr=[]; for(var x=0;x<32;x++) arr.push(32-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(32); });
  it("carr33", () => { const arr=[]; for(var x=0;x<33;x++) arr.push(33-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(33); });
  it("carr34", () => { const arr=[]; for(var x=0;x<34;x++) arr.push(34-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(34); });
  it("carr35", () => { const arr=[]; for(var x=0;x<35;x++) arr.push(35-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(35); });
  it("carr36", () => { const arr=[]; for(var x=0;x<36;x++) arr.push(36-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(36); });
  it("carr37", () => { const arr=[]; for(var x=0;x<37;x++) arr.push(37-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(37); });
  it("carr38", () => { const arr=[]; for(var x=0;x<38;x++) arr.push(38-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(38); });
  it("carr39", () => { const arr=[]; for(var x=0;x<39;x++) arr.push(39-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(39); });
  it("carr40", () => { const arr=[]; for(var x=0;x<40;x++) arr.push(40-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(40); });
  it("carr41", () => { const arr=[]; for(var x=0;x<41;x++) arr.push(41-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(41); });
  it("carr42", () => { const arr=[]; for(var x=0;x<42;x++) arr.push(42-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(42); });
  it("carr43", () => { const arr=[]; for(var x=0;x<43;x++) arr.push(43-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(43); });
  it("carr44", () => { const arr=[]; for(var x=0;x<44;x++) arr.push(44-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(44); });
  it("carr45", () => { const arr=[]; for(var x=0;x<45;x++) arr.push(45-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(45); });
  it("carr46", () => { const arr=[]; for(var x=0;x<46;x++) arr.push(46-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(46); });
  it("carr47", () => { const arr=[]; for(var x=0;x<47;x++) arr.push(47-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(47); });
  it("carr48", () => { const arr=[]; for(var x=0;x<48;x++) arr.push(48-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(48); });
  it("carr49", () => { const arr=[]; for(var x=0;x<49;x++) arr.push(49-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(49); });
  it("carr50", () => { const arr=[]; for(var x=0;x<50;x++) arr.push(50-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(50); });
  it("carr51", () => { const arr=[]; for(var x=0;x<51;x++) arr.push(51-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(51); });
  it("carr52", () => { const arr=[]; for(var x=0;x<52;x++) arr.push(52-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(52); });
  it("carr53", () => { const arr=[]; for(var x=0;x<53;x++) arr.push(53-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(53); });
  it("carr54", () => { const arr=[]; for(var x=0;x<54;x++) arr.push(54-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(54); });
  it("carr55", () => { const arr=[]; for(var x=0;x<55;x++) arr.push(55-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(55); });
  it("carr56", () => { const arr=[]; for(var x=0;x<56;x++) arr.push(56-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(56); });
  it("carr57", () => { const arr=[]; for(var x=0;x<57;x++) arr.push(57-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(57); });
  it("carr58", () => { const arr=[]; for(var x=0;x<58;x++) arr.push(58-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(58); });
  it("carr59", () => { const arr=[]; for(var x=0;x<59;x++) arr.push(59-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(59); });
  it("carr60", () => { const arr=[]; for(var x=0;x<60;x++) arr.push(60-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(60); });
  it("carr61", () => { const arr=[]; for(var x=0;x<61;x++) arr.push(61-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(61); });
  it("carr62", () => { const arr=[]; for(var x=0;x<62;x++) arr.push(62-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(62); });
  it("carr63", () => { const arr=[]; for(var x=0;x<63;x++) arr.push(63-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(63); });
  it("carr64", () => { const arr=[]; for(var x=0;x<64;x++) arr.push(64-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(64); });
  it("carr65", () => { const arr=[]; for(var x=0;x<65;x++) arr.push(65-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(65); });
  it("carr66", () => { const arr=[]; for(var x=0;x<66;x++) arr.push(66-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(66); });
  it("carr67", () => { const arr=[]; for(var x=0;x<67;x++) arr.push(67-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(67); });
  it("carr68", () => { const arr=[]; for(var x=0;x<68;x++) arr.push(68-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(68); });
  it("carr69", () => { const arr=[]; for(var x=0;x<69;x++) arr.push(69-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(69); });
  it("carr70", () => { const arr=[]; for(var x=0;x<70;x++) arr.push(70-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(70); });
  it("carr71", () => { const arr=[]; for(var x=0;x<71;x++) arr.push(71-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(71); });
  it("carr72", () => { const arr=[]; for(var x=0;x<72;x++) arr.push(72-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(72); });
  it("carr73", () => { const arr=[]; for(var x=0;x<73;x++) arr.push(73-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(73); });
  it("carr74", () => { const arr=[]; for(var x=0;x<74;x++) arr.push(74-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(74); });
  it("carr75", () => { const arr=[]; for(var x=0;x<75;x++) arr.push(75-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(75); });
  it("carr76", () => { const arr=[]; for(var x=0;x<76;x++) arr.push(76-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(76); });
  it("carr77", () => { const arr=[]; for(var x=0;x<77;x++) arr.push(77-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(77); });
  it("carr78", () => { const arr=[]; for(var x=0;x<78;x++) arr.push(78-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(78); });
  it("carr79", () => { const arr=[]; for(var x=0;x<79;x++) arr.push(79-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(79); });
  it("carr80", () => { const arr=[]; for(var x=0;x<80;x++) arr.push(80-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(80); });
  it("carr81", () => { const arr=[]; for(var x=0;x<81;x++) arr.push(81-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(81); });
  it("carr82", () => { const arr=[]; for(var x=0;x<82;x++) arr.push(82-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(82); });
  it("carr83", () => { const arr=[]; for(var x=0;x<83;x++) arr.push(83-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(83); });
  it("carr84", () => { const arr=[]; for(var x=0;x<84;x++) arr.push(84-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(84); });
  it("carr85", () => { const arr=[]; for(var x=0;x<85;x++) arr.push(85-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(85); });
  it("carr86", () => { const arr=[]; for(var x=0;x<86;x++) arr.push(86-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(86); });
  it("carr87", () => { const arr=[]; for(var x=0;x<87;x++) arr.push(87-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(87); });
  it("carr88", () => { const arr=[]; for(var x=0;x<88;x++) arr.push(88-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(88); });
  it("carr89", () => { const arr=[]; for(var x=0;x<89;x++) arr.push(89-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(89); });
  it("carr90", () => { const arr=[]; for(var x=0;x<90;x++) arr.push(90-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(90); });
  it("carr91", () => { const arr=[]; for(var x=0;x<91;x++) arr.push(91-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(91); });
  it("carr92", () => { const arr=[]; for(var x=0;x<92;x++) arr.push(92-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(92); });
  it("carr93", () => { const arr=[]; for(var x=0;x<93;x++) arr.push(93-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(93); });
  it("carr94", () => { const arr=[]; for(var x=0;x<94;x++) arr.push(94-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(94); });
  it("carr95", () => { const arr=[]; for(var x=0;x<95;x++) arr.push(95-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(95); });
  it("carr96", () => { const arr=[]; for(var x=0;x<96;x++) arr.push(96-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(96); });
  it("carr97", () => { const arr=[]; for(var x=0;x<97;x++) arr.push(97-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(97); });
  it("carr98", () => { const arr=[]; for(var x=0;x<98;x++) arr.push(98-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(98); });
  it("carr99", () => { const arr=[]; for(var x=0;x<99;x++) arr.push(99-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(99); });
  it("carr100", () => { const arr=[]; for(var x=0;x<100;x++) arr.push(100-x); const t = CartesianTree.fromArray(arr); expect(t.toArray()).toHaveLength(100); });
});

describe("CartesianTree rangeMin", () => {
  it("crmin2", () => { const arr=[]; for(var x=0;x<2;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,1)).toBe(1); });
  it("crmin3", () => { const arr=[]; for(var x=0;x<3;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,2)).toBe(1); });
  it("crmin4", () => { const arr=[]; for(var x=0;x<4;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,3)).toBe(1); });
  it("crmin5", () => { const arr=[]; for(var x=0;x<5;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,4)).toBe(1); });
  it("crmin6", () => { const arr=[]; for(var x=0;x<6;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,5)).toBe(1); });
  it("crmin7", () => { const arr=[]; for(var x=0;x<7;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,6)).toBe(1); });
  it("crmin8", () => { const arr=[]; for(var x=0;x<8;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,7)).toBe(1); });
  it("crmin9", () => { const arr=[]; for(var x=0;x<9;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,8)).toBe(1); });
  it("crmin10", () => { const arr=[]; for(var x=0;x<10;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,9)).toBe(1); });
  it("crmin11", () => { const arr=[]; for(var x=0;x<11;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,10)).toBe(1); });
  it("crmin12", () => { const arr=[]; for(var x=0;x<12;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,11)).toBe(1); });
  it("crmin13", () => { const arr=[]; for(var x=0;x<13;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,12)).toBe(1); });
  it("crmin14", () => { const arr=[]; for(var x=0;x<14;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,13)).toBe(1); });
  it("crmin15", () => { const arr=[]; for(var x=0;x<15;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,14)).toBe(1); });
  it("crmin16", () => { const arr=[]; for(var x=0;x<16;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,15)).toBe(1); });
  it("crmin17", () => { const arr=[]; for(var x=0;x<17;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,16)).toBe(1); });
  it("crmin18", () => { const arr=[]; for(var x=0;x<18;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,17)).toBe(1); });
  it("crmin19", () => { const arr=[]; for(var x=0;x<19;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,18)).toBe(1); });
  it("crmin20", () => { const arr=[]; for(var x=0;x<20;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,19)).toBe(1); });
  it("crmin21", () => { const arr=[]; for(var x=0;x<21;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,20)).toBe(1); });
  it("crmin22", () => { const arr=[]; for(var x=0;x<22;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,21)).toBe(1); });
  it("crmin23", () => { const arr=[]; for(var x=0;x<23;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,22)).toBe(1); });
  it("crmin24", () => { const arr=[]; for(var x=0;x<24;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,23)).toBe(1); });
  it("crmin25", () => { const arr=[]; for(var x=0;x<25;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,24)).toBe(1); });
  it("crmin26", () => { const arr=[]; for(var x=0;x<26;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,25)).toBe(1); });
  it("crmin27", () => { const arr=[]; for(var x=0;x<27;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,26)).toBe(1); });
  it("crmin28", () => { const arr=[]; for(var x=0;x<28;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,27)).toBe(1); });
  it("crmin29", () => { const arr=[]; for(var x=0;x<29;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,28)).toBe(1); });
  it("crmin30", () => { const arr=[]; for(var x=0;x<30;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,29)).toBe(1); });
  it("crmin31", () => { const arr=[]; for(var x=0;x<31;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,30)).toBe(1); });
  it("crmin32", () => { const arr=[]; for(var x=0;x<32;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,31)).toBe(1); });
  it("crmin33", () => { const arr=[]; for(var x=0;x<33;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,32)).toBe(1); });
  it("crmin34", () => { const arr=[]; for(var x=0;x<34;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,33)).toBe(1); });
  it("crmin35", () => { const arr=[]; for(var x=0;x<35;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,34)).toBe(1); });
  it("crmin36", () => { const arr=[]; for(var x=0;x<36;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,35)).toBe(1); });
  it("crmin37", () => { const arr=[]; for(var x=0;x<37;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,36)).toBe(1); });
  it("crmin38", () => { const arr=[]; for(var x=0;x<38;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,37)).toBe(1); });
  it("crmin39", () => { const arr=[]; for(var x=0;x<39;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,38)).toBe(1); });
  it("crmin40", () => { const arr=[]; for(var x=0;x<40;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,39)).toBe(1); });
  it("crmin41", () => { const arr=[]; for(var x=0;x<41;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,40)).toBe(1); });
  it("crmin42", () => { const arr=[]; for(var x=0;x<42;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,41)).toBe(1); });
  it("crmin43", () => { const arr=[]; for(var x=0;x<43;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,42)).toBe(1); });
  it("crmin44", () => { const arr=[]; for(var x=0;x<44;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,43)).toBe(1); });
  it("crmin45", () => { const arr=[]; for(var x=0;x<45;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,44)).toBe(1); });
  it("crmin46", () => { const arr=[]; for(var x=0;x<46;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,45)).toBe(1); });
  it("crmin47", () => { const arr=[]; for(var x=0;x<47;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,46)).toBe(1); });
  it("crmin48", () => { const arr=[]; for(var x=0;x<48;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,47)).toBe(1); });
  it("crmin49", () => { const arr=[]; for(var x=0;x<49;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,48)).toBe(1); });
  it("crmin50", () => { const arr=[]; for(var x=0;x<50;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,49)).toBe(1); });
  it("crmin51", () => { const arr=[]; for(var x=0;x<51;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,50)).toBe(1); });
  it("crmin52", () => { const arr=[]; for(var x=0;x<52;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,51)).toBe(1); });
  it("crmin53", () => { const arr=[]; for(var x=0;x<53;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,52)).toBe(1); });
  it("crmin54", () => { const arr=[]; for(var x=0;x<54;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,53)).toBe(1); });
  it("crmin55", () => { const arr=[]; for(var x=0;x<55;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,54)).toBe(1); });
  it("crmin56", () => { const arr=[]; for(var x=0;x<56;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,55)).toBe(1); });
  it("crmin57", () => { const arr=[]; for(var x=0;x<57;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,56)).toBe(1); });
  it("crmin58", () => { const arr=[]; for(var x=0;x<58;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,57)).toBe(1); });
  it("crmin59", () => { const arr=[]; for(var x=0;x<59;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,58)).toBe(1); });
  it("crmin60", () => { const arr=[]; for(var x=0;x<60;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,59)).toBe(1); });
  it("crmin61", () => { const arr=[]; for(var x=0;x<61;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,60)).toBe(1); });
  it("crmin62", () => { const arr=[]; for(var x=0;x<62;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,61)).toBe(1); });
  it("crmin63", () => { const arr=[]; for(var x=0;x<63;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,62)).toBe(1); });
  it("crmin64", () => { const arr=[]; for(var x=0;x<64;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,63)).toBe(1); });
  it("crmin65", () => { const arr=[]; for(var x=0;x<65;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,64)).toBe(1); });
  it("crmin66", () => { const arr=[]; for(var x=0;x<66;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,65)).toBe(1); });
  it("crmin67", () => { const arr=[]; for(var x=0;x<67;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,66)).toBe(1); });
  it("crmin68", () => { const arr=[]; for(var x=0;x<68;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,67)).toBe(1); });
  it("crmin69", () => { const arr=[]; for(var x=0;x<69;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,68)).toBe(1); });
  it("crmin70", () => { const arr=[]; for(var x=0;x<70;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,69)).toBe(1); });
  it("crmin71", () => { const arr=[]; for(var x=0;x<71;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,70)).toBe(1); });
  it("crmin72", () => { const arr=[]; for(var x=0;x<72;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,71)).toBe(1); });
  it("crmin73", () => { const arr=[]; for(var x=0;x<73;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,72)).toBe(1); });
  it("crmin74", () => { const arr=[]; for(var x=0;x<74;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,73)).toBe(1); });
  it("crmin75", () => { const arr=[]; for(var x=0;x<75;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,74)).toBe(1); });
  it("crmin76", () => { const arr=[]; for(var x=0;x<76;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,75)).toBe(1); });
  it("crmin77", () => { const arr=[]; for(var x=0;x<77;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,76)).toBe(1); });
  it("crmin78", () => { const arr=[]; for(var x=0;x<78;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,77)).toBe(1); });
  it("crmin79", () => { const arr=[]; for(var x=0;x<79;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,78)).toBe(1); });
  it("crmin80", () => { const arr=[]; for(var x=0;x<80;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,79)).toBe(1); });
  it("crmin81", () => { const arr=[]; for(var x=0;x<81;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,80)).toBe(1); });
  it("crmin82", () => { const arr=[]; for(var x=0;x<82;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,81)).toBe(1); });
  it("crmin83", () => { const arr=[]; for(var x=0;x<83;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,82)).toBe(1); });
  it("crmin84", () => { const arr=[]; for(var x=0;x<84;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,83)).toBe(1); });
  it("crmin85", () => { const arr=[]; for(var x=0;x<85;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,84)).toBe(1); });
  it("crmin86", () => { const arr=[]; for(var x=0;x<86;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,85)).toBe(1); });
  it("crmin87", () => { const arr=[]; for(var x=0;x<87;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,86)).toBe(1); });
  it("crmin88", () => { const arr=[]; for(var x=0;x<88;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,87)).toBe(1); });
  it("crmin89", () => { const arr=[]; for(var x=0;x<89;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,88)).toBe(1); });
  it("crmin90", () => { const arr=[]; for(var x=0;x<90;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,89)).toBe(1); });
  it("crmin91", () => { const arr=[]; for(var x=0;x<91;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,90)).toBe(1); });
  it("crmin92", () => { const arr=[]; for(var x=0;x<92;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,91)).toBe(1); });
  it("crmin93", () => { const arr=[]; for(var x=0;x<93;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,92)).toBe(1); });
  it("crmin94", () => { const arr=[]; for(var x=0;x<94;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,93)).toBe(1); });
  it("crmin95", () => { const arr=[]; for(var x=0;x<95;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,94)).toBe(1); });
  it("crmin96", () => { const arr=[]; for(var x=0;x<96;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,95)).toBe(1); });
  it("crmin97", () => { const arr=[]; for(var x=0;x<97;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,96)).toBe(1); });
  it("crmin98", () => { const arr=[]; for(var x=0;x<98;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,97)).toBe(1); });
  it("crmin99", () => { const arr=[]; for(var x=0;x<99;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,98)).toBe(1); });
  it("crmin100", () => { const arr=[]; for(var x=0;x<100;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,99)).toBe(1); });
  it("crmin101", () => { const arr=[]; for(var x=0;x<101;x++) arr.push(x+1); const t = CartesianTree.fromArray(arr); expect(t.rangeMin(0,100)).toBe(1); });
});

describe("Treap insert and has", () => {
  it("tr0", () => { const t = new Treap<number>(); t.insert(0, 1); expect(t.has(0)).toBe(true); });
  it("tr1", () => { const t = new Treap<number>(); t.insert(1, 2); expect(t.has(1)).toBe(true); });
  it("tr2", () => { const t = new Treap<number>(); t.insert(2, 3); expect(t.has(2)).toBe(true); });
  it("tr3", () => { const t = new Treap<number>(); t.insert(3, 4); expect(t.has(3)).toBe(true); });
  it("tr4", () => { const t = new Treap<number>(); t.insert(4, 5); expect(t.has(4)).toBe(true); });
  it("tr5", () => { const t = new Treap<number>(); t.insert(5, 6); expect(t.has(5)).toBe(true); });
  it("tr6", () => { const t = new Treap<number>(); t.insert(6, 7); expect(t.has(6)).toBe(true); });
  it("tr7", () => { const t = new Treap<number>(); t.insert(7, 8); expect(t.has(7)).toBe(true); });
  it("tr8", () => { const t = new Treap<number>(); t.insert(8, 9); expect(t.has(8)).toBe(true); });
  it("tr9", () => { const t = new Treap<number>(); t.insert(9, 10); expect(t.has(9)).toBe(true); });
  it("tr10", () => { const t = new Treap<number>(); t.insert(10, 11); expect(t.has(10)).toBe(true); });
  it("tr11", () => { const t = new Treap<number>(); t.insert(11, 12); expect(t.has(11)).toBe(true); });
  it("tr12", () => { const t = new Treap<number>(); t.insert(12, 13); expect(t.has(12)).toBe(true); });
  it("tr13", () => { const t = new Treap<number>(); t.insert(13, 14); expect(t.has(13)).toBe(true); });
  it("tr14", () => { const t = new Treap<number>(); t.insert(14, 15); expect(t.has(14)).toBe(true); });
  it("tr15", () => { const t = new Treap<number>(); t.insert(15, 16); expect(t.has(15)).toBe(true); });
  it("tr16", () => { const t = new Treap<number>(); t.insert(16, 17); expect(t.has(16)).toBe(true); });
  it("tr17", () => { const t = new Treap<number>(); t.insert(17, 18); expect(t.has(17)).toBe(true); });
  it("tr18", () => { const t = new Treap<number>(); t.insert(18, 19); expect(t.has(18)).toBe(true); });
  it("tr19", () => { const t = new Treap<number>(); t.insert(19, 20); expect(t.has(19)).toBe(true); });
  it("tr20", () => { const t = new Treap<number>(); t.insert(20, 21); expect(t.has(20)).toBe(true); });
  it("tr21", () => { const t = new Treap<number>(); t.insert(21, 22); expect(t.has(21)).toBe(true); });
  it("tr22", () => { const t = new Treap<number>(); t.insert(22, 23); expect(t.has(22)).toBe(true); });
  it("tr23", () => { const t = new Treap<number>(); t.insert(23, 24); expect(t.has(23)).toBe(true); });
  it("tr24", () => { const t = new Treap<number>(); t.insert(24, 25); expect(t.has(24)).toBe(true); });
  it("tr25", () => { const t = new Treap<number>(); t.insert(25, 26); expect(t.has(25)).toBe(true); });
  it("tr26", () => { const t = new Treap<number>(); t.insert(26, 27); expect(t.has(26)).toBe(true); });
  it("tr27", () => { const t = new Treap<number>(); t.insert(27, 28); expect(t.has(27)).toBe(true); });
  it("tr28", () => { const t = new Treap<number>(); t.insert(28, 29); expect(t.has(28)).toBe(true); });
  it("tr29", () => { const t = new Treap<number>(); t.insert(29, 30); expect(t.has(29)).toBe(true); });
  it("tr30", () => { const t = new Treap<number>(); t.insert(30, 31); expect(t.has(30)).toBe(true); });
  it("tr31", () => { const t = new Treap<number>(); t.insert(31, 32); expect(t.has(31)).toBe(true); });
  it("tr32", () => { const t = new Treap<number>(); t.insert(32, 33); expect(t.has(32)).toBe(true); });
  it("tr33", () => { const t = new Treap<number>(); t.insert(33, 34); expect(t.has(33)).toBe(true); });
  it("tr34", () => { const t = new Treap<number>(); t.insert(34, 35); expect(t.has(34)).toBe(true); });
  it("tr35", () => { const t = new Treap<number>(); t.insert(35, 36); expect(t.has(35)).toBe(true); });
  it("tr36", () => { const t = new Treap<number>(); t.insert(36, 37); expect(t.has(36)).toBe(true); });
  it("tr37", () => { const t = new Treap<number>(); t.insert(37, 38); expect(t.has(37)).toBe(true); });
  it("tr38", () => { const t = new Treap<number>(); t.insert(38, 39); expect(t.has(38)).toBe(true); });
  it("tr39", () => { const t = new Treap<number>(); t.insert(39, 40); expect(t.has(39)).toBe(true); });
  it("tr40", () => { const t = new Treap<number>(); t.insert(40, 41); expect(t.has(40)).toBe(true); });
  it("tr41", () => { const t = new Treap<number>(); t.insert(41, 42); expect(t.has(41)).toBe(true); });
  it("tr42", () => { const t = new Treap<number>(); t.insert(42, 43); expect(t.has(42)).toBe(true); });
  it("tr43", () => { const t = new Treap<number>(); t.insert(43, 44); expect(t.has(43)).toBe(true); });
  it("tr44", () => { const t = new Treap<number>(); t.insert(44, 45); expect(t.has(44)).toBe(true); });
  it("tr45", () => { const t = new Treap<number>(); t.insert(45, 46); expect(t.has(45)).toBe(true); });
  it("tr46", () => { const t = new Treap<number>(); t.insert(46, 47); expect(t.has(46)).toBe(true); });
  it("tr47", () => { const t = new Treap<number>(); t.insert(47, 48); expect(t.has(47)).toBe(true); });
  it("tr48", () => { const t = new Treap<number>(); t.insert(48, 49); expect(t.has(48)).toBe(true); });
  it("tr49", () => { const t = new Treap<number>(); t.insert(49, 50); expect(t.has(49)).toBe(true); });
  it("tr50", () => { const t = new Treap<number>(); t.insert(50, 51); expect(t.has(50)).toBe(true); });
  it("tr51", () => { const t = new Treap<number>(); t.insert(51, 52); expect(t.has(51)).toBe(true); });
  it("tr52", () => { const t = new Treap<number>(); t.insert(52, 53); expect(t.has(52)).toBe(true); });
  it("tr53", () => { const t = new Treap<number>(); t.insert(53, 54); expect(t.has(53)).toBe(true); });
  it("tr54", () => { const t = new Treap<number>(); t.insert(54, 55); expect(t.has(54)).toBe(true); });
  it("tr55", () => { const t = new Treap<number>(); t.insert(55, 56); expect(t.has(55)).toBe(true); });
  it("tr56", () => { const t = new Treap<number>(); t.insert(56, 57); expect(t.has(56)).toBe(true); });
  it("tr57", () => { const t = new Treap<number>(); t.insert(57, 58); expect(t.has(57)).toBe(true); });
  it("tr58", () => { const t = new Treap<number>(); t.insert(58, 59); expect(t.has(58)).toBe(true); });
  it("tr59", () => { const t = new Treap<number>(); t.insert(59, 60); expect(t.has(59)).toBe(true); });
  it("tr60", () => { const t = new Treap<number>(); t.insert(60, 61); expect(t.has(60)).toBe(true); });
  it("tr61", () => { const t = new Treap<number>(); t.insert(61, 62); expect(t.has(61)).toBe(true); });
  it("tr62", () => { const t = new Treap<number>(); t.insert(62, 63); expect(t.has(62)).toBe(true); });
  it("tr63", () => { const t = new Treap<number>(); t.insert(63, 64); expect(t.has(63)).toBe(true); });
  it("tr64", () => { const t = new Treap<number>(); t.insert(64, 65); expect(t.has(64)).toBe(true); });
  it("tr65", () => { const t = new Treap<number>(); t.insert(65, 66); expect(t.has(65)).toBe(true); });
  it("tr66", () => { const t = new Treap<number>(); t.insert(66, 67); expect(t.has(66)).toBe(true); });
  it("tr67", () => { const t = new Treap<number>(); t.insert(67, 68); expect(t.has(67)).toBe(true); });
  it("tr68", () => { const t = new Treap<number>(); t.insert(68, 69); expect(t.has(68)).toBe(true); });
  it("tr69", () => { const t = new Treap<number>(); t.insert(69, 70); expect(t.has(69)).toBe(true); });
  it("tr70", () => { const t = new Treap<number>(); t.insert(70, 71); expect(t.has(70)).toBe(true); });
  it("tr71", () => { const t = new Treap<number>(); t.insert(71, 72); expect(t.has(71)).toBe(true); });
  it("tr72", () => { const t = new Treap<number>(); t.insert(72, 73); expect(t.has(72)).toBe(true); });
  it("tr73", () => { const t = new Treap<number>(); t.insert(73, 74); expect(t.has(73)).toBe(true); });
  it("tr74", () => { const t = new Treap<number>(); t.insert(74, 75); expect(t.has(74)).toBe(true); });
  it("tr75", () => { const t = new Treap<number>(); t.insert(75, 76); expect(t.has(75)).toBe(true); });
  it("tr76", () => { const t = new Treap<number>(); t.insert(76, 77); expect(t.has(76)).toBe(true); });
  it("tr77", () => { const t = new Treap<number>(); t.insert(77, 78); expect(t.has(77)).toBe(true); });
  it("tr78", () => { const t = new Treap<number>(); t.insert(78, 79); expect(t.has(78)).toBe(true); });
  it("tr79", () => { const t = new Treap<number>(); t.insert(79, 80); expect(t.has(79)).toBe(true); });
  it("tr80", () => { const t = new Treap<number>(); t.insert(80, 81); expect(t.has(80)).toBe(true); });
  it("tr81", () => { const t = new Treap<number>(); t.insert(81, 82); expect(t.has(81)).toBe(true); });
  it("tr82", () => { const t = new Treap<number>(); t.insert(82, 83); expect(t.has(82)).toBe(true); });
  it("tr83", () => { const t = new Treap<number>(); t.insert(83, 84); expect(t.has(83)).toBe(true); });
  it("tr84", () => { const t = new Treap<number>(); t.insert(84, 85); expect(t.has(84)).toBe(true); });
  it("tr85", () => { const t = new Treap<number>(); t.insert(85, 86); expect(t.has(85)).toBe(true); });
  it("tr86", () => { const t = new Treap<number>(); t.insert(86, 87); expect(t.has(86)).toBe(true); });
  it("tr87", () => { const t = new Treap<number>(); t.insert(87, 88); expect(t.has(87)).toBe(true); });
  it("tr88", () => { const t = new Treap<number>(); t.insert(88, 89); expect(t.has(88)).toBe(true); });
  it("tr89", () => { const t = new Treap<number>(); t.insert(89, 90); expect(t.has(89)).toBe(true); });
  it("tr90", () => { const t = new Treap<number>(); t.insert(90, 91); expect(t.has(90)).toBe(true); });
  it("tr91", () => { const t = new Treap<number>(); t.insert(91, 92); expect(t.has(91)).toBe(true); });
  it("tr92", () => { const t = new Treap<number>(); t.insert(92, 93); expect(t.has(92)).toBe(true); });
  it("tr93", () => { const t = new Treap<number>(); t.insert(93, 94); expect(t.has(93)).toBe(true); });
  it("tr94", () => { const t = new Treap<number>(); t.insert(94, 95); expect(t.has(94)).toBe(true); });
  it("tr95", () => { const t = new Treap<number>(); t.insert(95, 96); expect(t.has(95)).toBe(true); });
  it("tr96", () => { const t = new Treap<number>(); t.insert(96, 97); expect(t.has(96)).toBe(true); });
  it("tr97", () => { const t = new Treap<number>(); t.insert(97, 98); expect(t.has(97)).toBe(true); });
  it("tr98", () => { const t = new Treap<number>(); t.insert(98, 99); expect(t.has(98)).toBe(true); });
  it("tr99", () => { const t = new Treap<number>(); t.insert(99, 100); expect(t.has(99)).toBe(true); });
  it("tr100", () => { const t = new Treap<number>(); t.insert(100, 101); expect(t.has(100)).toBe(true); });
  it("tr101", () => { const t = new Treap<number>(); t.insert(101, 102); expect(t.has(101)).toBe(true); });
  it("tr102", () => { const t = new Treap<number>(); t.insert(102, 103); expect(t.has(102)).toBe(true); });
  it("tr103", () => { const t = new Treap<number>(); t.insert(103, 104); expect(t.has(103)).toBe(true); });
  it("tr104", () => { const t = new Treap<number>(); t.insert(104, 105); expect(t.has(104)).toBe(true); });
  it("tr105", () => { const t = new Treap<number>(); t.insert(105, 106); expect(t.has(105)).toBe(true); });
  it("tr106", () => { const t = new Treap<number>(); t.insert(106, 107); expect(t.has(106)).toBe(true); });
  it("tr107", () => { const t = new Treap<number>(); t.insert(107, 108); expect(t.has(107)).toBe(true); });
  it("tr108", () => { const t = new Treap<number>(); t.insert(108, 109); expect(t.has(108)).toBe(true); });
  it("tr109", () => { const t = new Treap<number>(); t.insert(109, 110); expect(t.has(109)).toBe(true); });
  it("tr110", () => { const t = new Treap<number>(); t.insert(110, 111); expect(t.has(110)).toBe(true); });
  it("tr111", () => { const t = new Treap<number>(); t.insert(111, 112); expect(t.has(111)).toBe(true); });
  it("tr112", () => { const t = new Treap<number>(); t.insert(112, 113); expect(t.has(112)).toBe(true); });
  it("tr113", () => { const t = new Treap<number>(); t.insert(113, 114); expect(t.has(113)).toBe(true); });
  it("tr114", () => { const t = new Treap<number>(); t.insert(114, 115); expect(t.has(114)).toBe(true); });
  it("tr115", () => { const t = new Treap<number>(); t.insert(115, 116); expect(t.has(115)).toBe(true); });
  it("tr116", () => { const t = new Treap<number>(); t.insert(116, 117); expect(t.has(116)).toBe(true); });
  it("tr117", () => { const t = new Treap<number>(); t.insert(117, 118); expect(t.has(117)).toBe(true); });
  it("tr118", () => { const t = new Treap<number>(); t.insert(118, 119); expect(t.has(118)).toBe(true); });
  it("tr119", () => { const t = new Treap<number>(); t.insert(119, 120); expect(t.has(119)).toBe(true); });
  it("tr120", () => { const t = new Treap<number>(); t.insert(120, 121); expect(t.has(120)).toBe(true); });
  it("tr121", () => { const t = new Treap<number>(); t.insert(121, 122); expect(t.has(121)).toBe(true); });
  it("tr122", () => { const t = new Treap<number>(); t.insert(122, 123); expect(t.has(122)).toBe(true); });
  it("tr123", () => { const t = new Treap<number>(); t.insert(123, 124); expect(t.has(123)).toBe(true); });
  it("tr124", () => { const t = new Treap<number>(); t.insert(124, 125); expect(t.has(124)).toBe(true); });
  it("tr125", () => { const t = new Treap<number>(); t.insert(125, 126); expect(t.has(125)).toBe(true); });
  it("tr126", () => { const t = new Treap<number>(); t.insert(126, 127); expect(t.has(126)).toBe(true); });
  it("tr127", () => { const t = new Treap<number>(); t.insert(127, 128); expect(t.has(127)).toBe(true); });
  it("tr128", () => { const t = new Treap<number>(); t.insert(128, 129); expect(t.has(128)).toBe(true); });
  it("tr129", () => { const t = new Treap<number>(); t.insert(129, 130); expect(t.has(129)).toBe(true); });
  it("tr130", () => { const t = new Treap<number>(); t.insert(130, 131); expect(t.has(130)).toBe(true); });
  it("tr131", () => { const t = new Treap<number>(); t.insert(131, 132); expect(t.has(131)).toBe(true); });
  it("tr132", () => { const t = new Treap<number>(); t.insert(132, 133); expect(t.has(132)).toBe(true); });
  it("tr133", () => { const t = new Treap<number>(); t.insert(133, 134); expect(t.has(133)).toBe(true); });
  it("tr134", () => { const t = new Treap<number>(); t.insert(134, 135); expect(t.has(134)).toBe(true); });
  it("tr135", () => { const t = new Treap<number>(); t.insert(135, 136); expect(t.has(135)).toBe(true); });
  it("tr136", () => { const t = new Treap<number>(); t.insert(136, 137); expect(t.has(136)).toBe(true); });
  it("tr137", () => { const t = new Treap<number>(); t.insert(137, 138); expect(t.has(137)).toBe(true); });
  it("tr138", () => { const t = new Treap<number>(); t.insert(138, 139); expect(t.has(138)).toBe(true); });
  it("tr139", () => { const t = new Treap<number>(); t.insert(139, 140); expect(t.has(139)).toBe(true); });
  it("tr140", () => { const t = new Treap<number>(); t.insert(140, 141); expect(t.has(140)).toBe(true); });
  it("tr141", () => { const t = new Treap<number>(); t.insert(141, 142); expect(t.has(141)).toBe(true); });
  it("tr142", () => { const t = new Treap<number>(); t.insert(142, 143); expect(t.has(142)).toBe(true); });
  it("tr143", () => { const t = new Treap<number>(); t.insert(143, 144); expect(t.has(143)).toBe(true); });
  it("tr144", () => { const t = new Treap<number>(); t.insert(144, 145); expect(t.has(144)).toBe(true); });
  it("tr145", () => { const t = new Treap<number>(); t.insert(145, 146); expect(t.has(145)).toBe(true); });
  it("tr146", () => { const t = new Treap<number>(); t.insert(146, 147); expect(t.has(146)).toBe(true); });
  it("tr147", () => { const t = new Treap<number>(); t.insert(147, 148); expect(t.has(147)).toBe(true); });
  it("tr148", () => { const t = new Treap<number>(); t.insert(148, 149); expect(t.has(148)).toBe(true); });
  it("tr149", () => { const t = new Treap<number>(); t.insert(149, 150); expect(t.has(149)).toBe(true); });
  it("tr150", () => { const t = new Treap<number>(); t.insert(150, 151); expect(t.has(150)).toBe(true); });
  it("tr151", () => { const t = new Treap<number>(); t.insert(151, 152); expect(t.has(151)).toBe(true); });
  it("tr152", () => { const t = new Treap<number>(); t.insert(152, 153); expect(t.has(152)).toBe(true); });
  it("tr153", () => { const t = new Treap<number>(); t.insert(153, 154); expect(t.has(153)).toBe(true); });
  it("tr154", () => { const t = new Treap<number>(); t.insert(154, 155); expect(t.has(154)).toBe(true); });
  it("tr155", () => { const t = new Treap<number>(); t.insert(155, 156); expect(t.has(155)).toBe(true); });
  it("tr156", () => { const t = new Treap<number>(); t.insert(156, 157); expect(t.has(156)).toBe(true); });
  it("tr157", () => { const t = new Treap<number>(); t.insert(157, 158); expect(t.has(157)).toBe(true); });
  it("tr158", () => { const t = new Treap<number>(); t.insert(158, 159); expect(t.has(158)).toBe(true); });
  it("tr159", () => { const t = new Treap<number>(); t.insert(159, 160); expect(t.has(159)).toBe(true); });
  it("tr160", () => { const t = new Treap<number>(); t.insert(160, 161); expect(t.has(160)).toBe(true); });
  it("tr161", () => { const t = new Treap<number>(); t.insert(161, 162); expect(t.has(161)).toBe(true); });
  it("tr162", () => { const t = new Treap<number>(); t.insert(162, 163); expect(t.has(162)).toBe(true); });
  it("tr163", () => { const t = new Treap<number>(); t.insert(163, 164); expect(t.has(163)).toBe(true); });
  it("tr164", () => { const t = new Treap<number>(); t.insert(164, 165); expect(t.has(164)).toBe(true); });
  it("tr165", () => { const t = new Treap<number>(); t.insert(165, 166); expect(t.has(165)).toBe(true); });
  it("tr166", () => { const t = new Treap<number>(); t.insert(166, 167); expect(t.has(166)).toBe(true); });
  it("tr167", () => { const t = new Treap<number>(); t.insert(167, 168); expect(t.has(167)).toBe(true); });
  it("tr168", () => { const t = new Treap<number>(); t.insert(168, 169); expect(t.has(168)).toBe(true); });
  it("tr169", () => { const t = new Treap<number>(); t.insert(169, 170); expect(t.has(169)).toBe(true); });
  it("tr170", () => { const t = new Treap<number>(); t.insert(170, 171); expect(t.has(170)).toBe(true); });
  it("tr171", () => { const t = new Treap<number>(); t.insert(171, 172); expect(t.has(171)).toBe(true); });
  it("tr172", () => { const t = new Treap<number>(); t.insert(172, 173); expect(t.has(172)).toBe(true); });
  it("tr173", () => { const t = new Treap<number>(); t.insert(173, 174); expect(t.has(173)).toBe(true); });
  it("tr174", () => { const t = new Treap<number>(); t.insert(174, 175); expect(t.has(174)).toBe(true); });
  it("tr175", () => { const t = new Treap<number>(); t.insert(175, 176); expect(t.has(175)).toBe(true); });
  it("tr176", () => { const t = new Treap<number>(); t.insert(176, 177); expect(t.has(176)).toBe(true); });
  it("tr177", () => { const t = new Treap<number>(); t.insert(177, 178); expect(t.has(177)).toBe(true); });
  it("tr178", () => { const t = new Treap<number>(); t.insert(178, 179); expect(t.has(178)).toBe(true); });
  it("tr179", () => { const t = new Treap<number>(); t.insert(179, 180); expect(t.has(179)).toBe(true); });
  it("tr180", () => { const t = new Treap<number>(); t.insert(180, 181); expect(t.has(180)).toBe(true); });
  it("tr181", () => { const t = new Treap<number>(); t.insert(181, 182); expect(t.has(181)).toBe(true); });
  it("tr182", () => { const t = new Treap<number>(); t.insert(182, 183); expect(t.has(182)).toBe(true); });
  it("tr183", () => { const t = new Treap<number>(); t.insert(183, 184); expect(t.has(183)).toBe(true); });
  it("tr184", () => { const t = new Treap<number>(); t.insert(184, 185); expect(t.has(184)).toBe(true); });
  it("tr185", () => { const t = new Treap<number>(); t.insert(185, 186); expect(t.has(185)).toBe(true); });
  it("tr186", () => { const t = new Treap<number>(); t.insert(186, 187); expect(t.has(186)).toBe(true); });
  it("tr187", () => { const t = new Treap<number>(); t.insert(187, 188); expect(t.has(187)).toBe(true); });
  it("tr188", () => { const t = new Treap<number>(); t.insert(188, 189); expect(t.has(188)).toBe(true); });
  it("tr189", () => { const t = new Treap<number>(); t.insert(189, 190); expect(t.has(189)).toBe(true); });
  it("tr190", () => { const t = new Treap<number>(); t.insert(190, 191); expect(t.has(190)).toBe(true); });
  it("tr191", () => { const t = new Treap<number>(); t.insert(191, 192); expect(t.has(191)).toBe(true); });
  it("tr192", () => { const t = new Treap<number>(); t.insert(192, 193); expect(t.has(192)).toBe(true); });
  it("tr193", () => { const t = new Treap<number>(); t.insert(193, 194); expect(t.has(193)).toBe(true); });
  it("tr194", () => { const t = new Treap<number>(); t.insert(194, 195); expect(t.has(194)).toBe(true); });
  it("tr195", () => { const t = new Treap<number>(); t.insert(195, 196); expect(t.has(195)).toBe(true); });
  it("tr196", () => { const t = new Treap<number>(); t.insert(196, 197); expect(t.has(196)).toBe(true); });
  it("tr197", () => { const t = new Treap<number>(); t.insert(197, 198); expect(t.has(197)).toBe(true); });
  it("tr198", () => { const t = new Treap<number>(); t.insert(198, 199); expect(t.has(198)).toBe(true); });
  it("tr199", () => { const t = new Treap<number>(); t.insert(199, 200); expect(t.has(199)).toBe(true); });
});

describe("Treap size", () => {
  it("trsz1", () => { const t = new Treap<number>(); for(let i=0;i<1;i++) t.insert(i); expect(t.size).toBe(1); });
  it("trsz2", () => { const t = new Treap<number>(); for(let i=0;i<2;i++) t.insert(i); expect(t.size).toBe(2); });
  it("trsz3", () => { const t = new Treap<number>(); for(let i=0;i<3;i++) t.insert(i); expect(t.size).toBe(3); });
  it("trsz4", () => { const t = new Treap<number>(); for(let i=0;i<4;i++) t.insert(i); expect(t.size).toBe(4); });
  it("trsz5", () => { const t = new Treap<number>(); for(let i=0;i<5;i++) t.insert(i); expect(t.size).toBe(5); });
  it("trsz6", () => { const t = new Treap<number>(); for(let i=0;i<6;i++) t.insert(i); expect(t.size).toBe(6); });
  it("trsz7", () => { const t = new Treap<number>(); for(let i=0;i<7;i++) t.insert(i); expect(t.size).toBe(7); });
  it("trsz8", () => { const t = new Treap<number>(); for(let i=0;i<8;i++) t.insert(i); expect(t.size).toBe(8); });
  it("trsz9", () => { const t = new Treap<number>(); for(let i=0;i<9;i++) t.insert(i); expect(t.size).toBe(9); });
  it("trsz10", () => { const t = new Treap<number>(); for(let i=0;i<10;i++) t.insert(i); expect(t.size).toBe(10); });
  it("trsz11", () => { const t = new Treap<number>(); for(let i=0;i<11;i++) t.insert(i); expect(t.size).toBe(11); });
  it("trsz12", () => { const t = new Treap<number>(); for(let i=0;i<12;i++) t.insert(i); expect(t.size).toBe(12); });
  it("trsz13", () => { const t = new Treap<number>(); for(let i=0;i<13;i++) t.insert(i); expect(t.size).toBe(13); });
  it("trsz14", () => { const t = new Treap<number>(); for(let i=0;i<14;i++) t.insert(i); expect(t.size).toBe(14); });
  it("trsz15", () => { const t = new Treap<number>(); for(let i=0;i<15;i++) t.insert(i); expect(t.size).toBe(15); });
  it("trsz16", () => { const t = new Treap<number>(); for(let i=0;i<16;i++) t.insert(i); expect(t.size).toBe(16); });
  it("trsz17", () => { const t = new Treap<number>(); for(let i=0;i<17;i++) t.insert(i); expect(t.size).toBe(17); });
  it("trsz18", () => { const t = new Treap<number>(); for(let i=0;i<18;i++) t.insert(i); expect(t.size).toBe(18); });
  it("trsz19", () => { const t = new Treap<number>(); for(let i=0;i<19;i++) t.insert(i); expect(t.size).toBe(19); });
  it("trsz20", () => { const t = new Treap<number>(); for(let i=0;i<20;i++) t.insert(i); expect(t.size).toBe(20); });
  it("trsz21", () => { const t = new Treap<number>(); for(let i=0;i<21;i++) t.insert(i); expect(t.size).toBe(21); });
  it("trsz22", () => { const t = new Treap<number>(); for(let i=0;i<22;i++) t.insert(i); expect(t.size).toBe(22); });
  it("trsz23", () => { const t = new Treap<number>(); for(let i=0;i<23;i++) t.insert(i); expect(t.size).toBe(23); });
  it("trsz24", () => { const t = new Treap<number>(); for(let i=0;i<24;i++) t.insert(i); expect(t.size).toBe(24); });
  it("trsz25", () => { const t = new Treap<number>(); for(let i=0;i<25;i++) t.insert(i); expect(t.size).toBe(25); });
  it("trsz26", () => { const t = new Treap<number>(); for(let i=0;i<26;i++) t.insert(i); expect(t.size).toBe(26); });
  it("trsz27", () => { const t = new Treap<number>(); for(let i=0;i<27;i++) t.insert(i); expect(t.size).toBe(27); });
  it("trsz28", () => { const t = new Treap<number>(); for(let i=0;i<28;i++) t.insert(i); expect(t.size).toBe(28); });
  it("trsz29", () => { const t = new Treap<number>(); for(let i=0;i<29;i++) t.insert(i); expect(t.size).toBe(29); });
  it("trsz30", () => { const t = new Treap<number>(); for(let i=0;i<30;i++) t.insert(i); expect(t.size).toBe(30); });
  it("trsz31", () => { const t = new Treap<number>(); for(let i=0;i<31;i++) t.insert(i); expect(t.size).toBe(31); });
  it("trsz32", () => { const t = new Treap<number>(); for(let i=0;i<32;i++) t.insert(i); expect(t.size).toBe(32); });
  it("trsz33", () => { const t = new Treap<number>(); for(let i=0;i<33;i++) t.insert(i); expect(t.size).toBe(33); });
  it("trsz34", () => { const t = new Treap<number>(); for(let i=0;i<34;i++) t.insert(i); expect(t.size).toBe(34); });
  it("trsz35", () => { const t = new Treap<number>(); for(let i=0;i<35;i++) t.insert(i); expect(t.size).toBe(35); });
  it("trsz36", () => { const t = new Treap<number>(); for(let i=0;i<36;i++) t.insert(i); expect(t.size).toBe(36); });
  it("trsz37", () => { const t = new Treap<number>(); for(let i=0;i<37;i++) t.insert(i); expect(t.size).toBe(37); });
  it("trsz38", () => { const t = new Treap<number>(); for(let i=0;i<38;i++) t.insert(i); expect(t.size).toBe(38); });
  it("trsz39", () => { const t = new Treap<number>(); for(let i=0;i<39;i++) t.insert(i); expect(t.size).toBe(39); });
  it("trsz40", () => { const t = new Treap<number>(); for(let i=0;i<40;i++) t.insert(i); expect(t.size).toBe(40); });
  it("trsz41", () => { const t = new Treap<number>(); for(let i=0;i<41;i++) t.insert(i); expect(t.size).toBe(41); });
  it("trsz42", () => { const t = new Treap<number>(); for(let i=0;i<42;i++) t.insert(i); expect(t.size).toBe(42); });
  it("trsz43", () => { const t = new Treap<number>(); for(let i=0;i<43;i++) t.insert(i); expect(t.size).toBe(43); });
  it("trsz44", () => { const t = new Treap<number>(); for(let i=0;i<44;i++) t.insert(i); expect(t.size).toBe(44); });
  it("trsz45", () => { const t = new Treap<number>(); for(let i=0;i<45;i++) t.insert(i); expect(t.size).toBe(45); });
  it("trsz46", () => { const t = new Treap<number>(); for(let i=0;i<46;i++) t.insert(i); expect(t.size).toBe(46); });
  it("trsz47", () => { const t = new Treap<number>(); for(let i=0;i<47;i++) t.insert(i); expect(t.size).toBe(47); });
  it("trsz48", () => { const t = new Treap<number>(); for(let i=0;i<48;i++) t.insert(i); expect(t.size).toBe(48); });
  it("trsz49", () => { const t = new Treap<number>(); for(let i=0;i<49;i++) t.insert(i); expect(t.size).toBe(49); });
  it("trsz50", () => { const t = new Treap<number>(); for(let i=0;i<50;i++) t.insert(i); expect(t.size).toBe(50); });
  it("trsz51", () => { const t = new Treap<number>(); for(let i=0;i<51;i++) t.insert(i); expect(t.size).toBe(51); });
  it("trsz52", () => { const t = new Treap<number>(); for(let i=0;i<52;i++) t.insert(i); expect(t.size).toBe(52); });
  it("trsz53", () => { const t = new Treap<number>(); for(let i=0;i<53;i++) t.insert(i); expect(t.size).toBe(53); });
  it("trsz54", () => { const t = new Treap<number>(); for(let i=0;i<54;i++) t.insert(i); expect(t.size).toBe(54); });
  it("trsz55", () => { const t = new Treap<number>(); for(let i=0;i<55;i++) t.insert(i); expect(t.size).toBe(55); });
  it("trsz56", () => { const t = new Treap<number>(); for(let i=0;i<56;i++) t.insert(i); expect(t.size).toBe(56); });
  it("trsz57", () => { const t = new Treap<number>(); for(let i=0;i<57;i++) t.insert(i); expect(t.size).toBe(57); });
  it("trsz58", () => { const t = new Treap<number>(); for(let i=0;i<58;i++) t.insert(i); expect(t.size).toBe(58); });
  it("trsz59", () => { const t = new Treap<number>(); for(let i=0;i<59;i++) t.insert(i); expect(t.size).toBe(59); });
  it("trsz60", () => { const t = new Treap<number>(); for(let i=0;i<60;i++) t.insert(i); expect(t.size).toBe(60); });
  it("trsz61", () => { const t = new Treap<number>(); for(let i=0;i<61;i++) t.insert(i); expect(t.size).toBe(61); });
  it("trsz62", () => { const t = new Treap<number>(); for(let i=0;i<62;i++) t.insert(i); expect(t.size).toBe(62); });
  it("trsz63", () => { const t = new Treap<number>(); for(let i=0;i<63;i++) t.insert(i); expect(t.size).toBe(63); });
  it("trsz64", () => { const t = new Treap<number>(); for(let i=0;i<64;i++) t.insert(i); expect(t.size).toBe(64); });
  it("trsz65", () => { const t = new Treap<number>(); for(let i=0;i<65;i++) t.insert(i); expect(t.size).toBe(65); });
  it("trsz66", () => { const t = new Treap<number>(); for(let i=0;i<66;i++) t.insert(i); expect(t.size).toBe(66); });
  it("trsz67", () => { const t = new Treap<number>(); for(let i=0;i<67;i++) t.insert(i); expect(t.size).toBe(67); });
  it("trsz68", () => { const t = new Treap<number>(); for(let i=0;i<68;i++) t.insert(i); expect(t.size).toBe(68); });
  it("trsz69", () => { const t = new Treap<number>(); for(let i=0;i<69;i++) t.insert(i); expect(t.size).toBe(69); });
  it("trsz70", () => { const t = new Treap<number>(); for(let i=0;i<70;i++) t.insert(i); expect(t.size).toBe(70); });
  it("trsz71", () => { const t = new Treap<number>(); for(let i=0;i<71;i++) t.insert(i); expect(t.size).toBe(71); });
  it("trsz72", () => { const t = new Treap<number>(); for(let i=0;i<72;i++) t.insert(i); expect(t.size).toBe(72); });
  it("trsz73", () => { const t = new Treap<number>(); for(let i=0;i<73;i++) t.insert(i); expect(t.size).toBe(73); });
  it("trsz74", () => { const t = new Treap<number>(); for(let i=0;i<74;i++) t.insert(i); expect(t.size).toBe(74); });
  it("trsz75", () => { const t = new Treap<number>(); for(let i=0;i<75;i++) t.insert(i); expect(t.size).toBe(75); });
  it("trsz76", () => { const t = new Treap<number>(); for(let i=0;i<76;i++) t.insert(i); expect(t.size).toBe(76); });
  it("trsz77", () => { const t = new Treap<number>(); for(let i=0;i<77;i++) t.insert(i); expect(t.size).toBe(77); });
  it("trsz78", () => { const t = new Treap<number>(); for(let i=0;i<78;i++) t.insert(i); expect(t.size).toBe(78); });
  it("trsz79", () => { const t = new Treap<number>(); for(let i=0;i<79;i++) t.insert(i); expect(t.size).toBe(79); });
  it("trsz80", () => { const t = new Treap<number>(); for(let i=0;i<80;i++) t.insert(i); expect(t.size).toBe(80); });
  it("trsz81", () => { const t = new Treap<number>(); for(let i=0;i<81;i++) t.insert(i); expect(t.size).toBe(81); });
  it("trsz82", () => { const t = new Treap<number>(); for(let i=0;i<82;i++) t.insert(i); expect(t.size).toBe(82); });
  it("trsz83", () => { const t = new Treap<number>(); for(let i=0;i<83;i++) t.insert(i); expect(t.size).toBe(83); });
  it("trsz84", () => { const t = new Treap<number>(); for(let i=0;i<84;i++) t.insert(i); expect(t.size).toBe(84); });
  it("trsz85", () => { const t = new Treap<number>(); for(let i=0;i<85;i++) t.insert(i); expect(t.size).toBe(85); });
  it("trsz86", () => { const t = new Treap<number>(); for(let i=0;i<86;i++) t.insert(i); expect(t.size).toBe(86); });
  it("trsz87", () => { const t = new Treap<number>(); for(let i=0;i<87;i++) t.insert(i); expect(t.size).toBe(87); });
  it("trsz88", () => { const t = new Treap<number>(); for(let i=0;i<88;i++) t.insert(i); expect(t.size).toBe(88); });
  it("trsz89", () => { const t = new Treap<number>(); for(let i=0;i<89;i++) t.insert(i); expect(t.size).toBe(89); });
  it("trsz90", () => { const t = new Treap<number>(); for(let i=0;i<90;i++) t.insert(i); expect(t.size).toBe(90); });
  it("trsz91", () => { const t = new Treap<number>(); for(let i=0;i<91;i++) t.insert(i); expect(t.size).toBe(91); });
  it("trsz92", () => { const t = new Treap<number>(); for(let i=0;i<92;i++) t.insert(i); expect(t.size).toBe(92); });
  it("trsz93", () => { const t = new Treap<number>(); for(let i=0;i<93;i++) t.insert(i); expect(t.size).toBe(93); });
  it("trsz94", () => { const t = new Treap<number>(); for(let i=0;i<94;i++) t.insert(i); expect(t.size).toBe(94); });
  it("trsz95", () => { const t = new Treap<number>(); for(let i=0;i<95;i++) t.insert(i); expect(t.size).toBe(95); });
  it("trsz96", () => { const t = new Treap<number>(); for(let i=0;i<96;i++) t.insert(i); expect(t.size).toBe(96); });
  it("trsz97", () => { const t = new Treap<number>(); for(let i=0;i<97;i++) t.insert(i); expect(t.size).toBe(97); });
  it("trsz98", () => { const t = new Treap<number>(); for(let i=0;i<98;i++) t.insert(i); expect(t.size).toBe(98); });
  it("trsz99", () => { const t = new Treap<number>(); for(let i=0;i<99;i++) t.insert(i); expect(t.size).toBe(99); });
  it("trsz100", () => { const t = new Treap<number>(); for(let i=0;i<100;i++) t.insert(i); expect(t.size).toBe(100); });
});

describe("Treap delete", () => {
  it("trdel0", () => { const t = new Treap<number>(); t.insert(0); expect(t.delete(0)).toBe(true); expect(t.has(0)).toBe(false); });
  it("trdel1", () => { const t = new Treap<number>(); t.insert(1); expect(t.delete(1)).toBe(true); expect(t.has(1)).toBe(false); });
  it("trdel2", () => { const t = new Treap<number>(); t.insert(2); expect(t.delete(2)).toBe(true); expect(t.has(2)).toBe(false); });
  it("trdel3", () => { const t = new Treap<number>(); t.insert(3); expect(t.delete(3)).toBe(true); expect(t.has(3)).toBe(false); });
  it("trdel4", () => { const t = new Treap<number>(); t.insert(4); expect(t.delete(4)).toBe(true); expect(t.has(4)).toBe(false); });
  it("trdel5", () => { const t = new Treap<number>(); t.insert(5); expect(t.delete(5)).toBe(true); expect(t.has(5)).toBe(false); });
  it("trdel6", () => { const t = new Treap<number>(); t.insert(6); expect(t.delete(6)).toBe(true); expect(t.has(6)).toBe(false); });
  it("trdel7", () => { const t = new Treap<number>(); t.insert(7); expect(t.delete(7)).toBe(true); expect(t.has(7)).toBe(false); });
  it("trdel8", () => { const t = new Treap<number>(); t.insert(8); expect(t.delete(8)).toBe(true); expect(t.has(8)).toBe(false); });
  it("trdel9", () => { const t = new Treap<number>(); t.insert(9); expect(t.delete(9)).toBe(true); expect(t.has(9)).toBe(false); });
  it("trdel10", () => { const t = new Treap<number>(); t.insert(10); expect(t.delete(10)).toBe(true); expect(t.has(10)).toBe(false); });
  it("trdel11", () => { const t = new Treap<number>(); t.insert(11); expect(t.delete(11)).toBe(true); expect(t.has(11)).toBe(false); });
  it("trdel12", () => { const t = new Treap<number>(); t.insert(12); expect(t.delete(12)).toBe(true); expect(t.has(12)).toBe(false); });
  it("trdel13", () => { const t = new Treap<number>(); t.insert(13); expect(t.delete(13)).toBe(true); expect(t.has(13)).toBe(false); });
  it("trdel14", () => { const t = new Treap<number>(); t.insert(14); expect(t.delete(14)).toBe(true); expect(t.has(14)).toBe(false); });
  it("trdel15", () => { const t = new Treap<number>(); t.insert(15); expect(t.delete(15)).toBe(true); expect(t.has(15)).toBe(false); });
  it("trdel16", () => { const t = new Treap<number>(); t.insert(16); expect(t.delete(16)).toBe(true); expect(t.has(16)).toBe(false); });
  it("trdel17", () => { const t = new Treap<number>(); t.insert(17); expect(t.delete(17)).toBe(true); expect(t.has(17)).toBe(false); });
  it("trdel18", () => { const t = new Treap<number>(); t.insert(18); expect(t.delete(18)).toBe(true); expect(t.has(18)).toBe(false); });
  it("trdel19", () => { const t = new Treap<number>(); t.insert(19); expect(t.delete(19)).toBe(true); expect(t.has(19)).toBe(false); });
  it("trdel20", () => { const t = new Treap<number>(); t.insert(20); expect(t.delete(20)).toBe(true); expect(t.has(20)).toBe(false); });
  it("trdel21", () => { const t = new Treap<number>(); t.insert(21); expect(t.delete(21)).toBe(true); expect(t.has(21)).toBe(false); });
  it("trdel22", () => { const t = new Treap<number>(); t.insert(22); expect(t.delete(22)).toBe(true); expect(t.has(22)).toBe(false); });
  it("trdel23", () => { const t = new Treap<number>(); t.insert(23); expect(t.delete(23)).toBe(true); expect(t.has(23)).toBe(false); });
  it("trdel24", () => { const t = new Treap<number>(); t.insert(24); expect(t.delete(24)).toBe(true); expect(t.has(24)).toBe(false); });
  it("trdel25", () => { const t = new Treap<number>(); t.insert(25); expect(t.delete(25)).toBe(true); expect(t.has(25)).toBe(false); });
  it("trdel26", () => { const t = new Treap<number>(); t.insert(26); expect(t.delete(26)).toBe(true); expect(t.has(26)).toBe(false); });
  it("trdel27", () => { const t = new Treap<number>(); t.insert(27); expect(t.delete(27)).toBe(true); expect(t.has(27)).toBe(false); });
  it("trdel28", () => { const t = new Treap<number>(); t.insert(28); expect(t.delete(28)).toBe(true); expect(t.has(28)).toBe(false); });
  it("trdel29", () => { const t = new Treap<number>(); t.insert(29); expect(t.delete(29)).toBe(true); expect(t.has(29)).toBe(false); });
  it("trdel30", () => { const t = new Treap<number>(); t.insert(30); expect(t.delete(30)).toBe(true); expect(t.has(30)).toBe(false); });
  it("trdel31", () => { const t = new Treap<number>(); t.insert(31); expect(t.delete(31)).toBe(true); expect(t.has(31)).toBe(false); });
  it("trdel32", () => { const t = new Treap<number>(); t.insert(32); expect(t.delete(32)).toBe(true); expect(t.has(32)).toBe(false); });
  it("trdel33", () => { const t = new Treap<number>(); t.insert(33); expect(t.delete(33)).toBe(true); expect(t.has(33)).toBe(false); });
  it("trdel34", () => { const t = new Treap<number>(); t.insert(34); expect(t.delete(34)).toBe(true); expect(t.has(34)).toBe(false); });
  it("trdel35", () => { const t = new Treap<number>(); t.insert(35); expect(t.delete(35)).toBe(true); expect(t.has(35)).toBe(false); });
  it("trdel36", () => { const t = new Treap<number>(); t.insert(36); expect(t.delete(36)).toBe(true); expect(t.has(36)).toBe(false); });
  it("trdel37", () => { const t = new Treap<number>(); t.insert(37); expect(t.delete(37)).toBe(true); expect(t.has(37)).toBe(false); });
  it("trdel38", () => { const t = new Treap<number>(); t.insert(38); expect(t.delete(38)).toBe(true); expect(t.has(38)).toBe(false); });
  it("trdel39", () => { const t = new Treap<number>(); t.insert(39); expect(t.delete(39)).toBe(true); expect(t.has(39)).toBe(false); });
  it("trdel40", () => { const t = new Treap<number>(); t.insert(40); expect(t.delete(40)).toBe(true); expect(t.has(40)).toBe(false); });
  it("trdel41", () => { const t = new Treap<number>(); t.insert(41); expect(t.delete(41)).toBe(true); expect(t.has(41)).toBe(false); });
  it("trdel42", () => { const t = new Treap<number>(); t.insert(42); expect(t.delete(42)).toBe(true); expect(t.has(42)).toBe(false); });
  it("trdel43", () => { const t = new Treap<number>(); t.insert(43); expect(t.delete(43)).toBe(true); expect(t.has(43)).toBe(false); });
  it("trdel44", () => { const t = new Treap<number>(); t.insert(44); expect(t.delete(44)).toBe(true); expect(t.has(44)).toBe(false); });
  it("trdel45", () => { const t = new Treap<number>(); t.insert(45); expect(t.delete(45)).toBe(true); expect(t.has(45)).toBe(false); });
  it("trdel46", () => { const t = new Treap<number>(); t.insert(46); expect(t.delete(46)).toBe(true); expect(t.has(46)).toBe(false); });
  it("trdel47", () => { const t = new Treap<number>(); t.insert(47); expect(t.delete(47)).toBe(true); expect(t.has(47)).toBe(false); });
  it("trdel48", () => { const t = new Treap<number>(); t.insert(48); expect(t.delete(48)).toBe(true); expect(t.has(48)).toBe(false); });
  it("trdel49", () => { const t = new Treap<number>(); t.insert(49); expect(t.delete(49)).toBe(true); expect(t.has(49)).toBe(false); });
  it("trdel50", () => { const t = new Treap<number>(); t.insert(50); expect(t.delete(50)).toBe(true); expect(t.has(50)).toBe(false); });
  it("trdel51", () => { const t = new Treap<number>(); t.insert(51); expect(t.delete(51)).toBe(true); expect(t.has(51)).toBe(false); });
  it("trdel52", () => { const t = new Treap<number>(); t.insert(52); expect(t.delete(52)).toBe(true); expect(t.has(52)).toBe(false); });
  it("trdel53", () => { const t = new Treap<number>(); t.insert(53); expect(t.delete(53)).toBe(true); expect(t.has(53)).toBe(false); });
  it("trdel54", () => { const t = new Treap<number>(); t.insert(54); expect(t.delete(54)).toBe(true); expect(t.has(54)).toBe(false); });
  it("trdel55", () => { const t = new Treap<number>(); t.insert(55); expect(t.delete(55)).toBe(true); expect(t.has(55)).toBe(false); });
  it("trdel56", () => { const t = new Treap<number>(); t.insert(56); expect(t.delete(56)).toBe(true); expect(t.has(56)).toBe(false); });
  it("trdel57", () => { const t = new Treap<number>(); t.insert(57); expect(t.delete(57)).toBe(true); expect(t.has(57)).toBe(false); });
  it("trdel58", () => { const t = new Treap<number>(); t.insert(58); expect(t.delete(58)).toBe(true); expect(t.has(58)).toBe(false); });
  it("trdel59", () => { const t = new Treap<number>(); t.insert(59); expect(t.delete(59)).toBe(true); expect(t.has(59)).toBe(false); });
  it("trdel60", () => { const t = new Treap<number>(); t.insert(60); expect(t.delete(60)).toBe(true); expect(t.has(60)).toBe(false); });
  it("trdel61", () => { const t = new Treap<number>(); t.insert(61); expect(t.delete(61)).toBe(true); expect(t.has(61)).toBe(false); });
  it("trdel62", () => { const t = new Treap<number>(); t.insert(62); expect(t.delete(62)).toBe(true); expect(t.has(62)).toBe(false); });
  it("trdel63", () => { const t = new Treap<number>(); t.insert(63); expect(t.delete(63)).toBe(true); expect(t.has(63)).toBe(false); });
  it("trdel64", () => { const t = new Treap<number>(); t.insert(64); expect(t.delete(64)).toBe(true); expect(t.has(64)).toBe(false); });
  it("trdel65", () => { const t = new Treap<number>(); t.insert(65); expect(t.delete(65)).toBe(true); expect(t.has(65)).toBe(false); });
  it("trdel66", () => { const t = new Treap<number>(); t.insert(66); expect(t.delete(66)).toBe(true); expect(t.has(66)).toBe(false); });
  it("trdel67", () => { const t = new Treap<number>(); t.insert(67); expect(t.delete(67)).toBe(true); expect(t.has(67)).toBe(false); });
  it("trdel68", () => { const t = new Treap<number>(); t.insert(68); expect(t.delete(68)).toBe(true); expect(t.has(68)).toBe(false); });
  it("trdel69", () => { const t = new Treap<number>(); t.insert(69); expect(t.delete(69)).toBe(true); expect(t.has(69)).toBe(false); });
  it("trdel70", () => { const t = new Treap<number>(); t.insert(70); expect(t.delete(70)).toBe(true); expect(t.has(70)).toBe(false); });
  it("trdel71", () => { const t = new Treap<number>(); t.insert(71); expect(t.delete(71)).toBe(true); expect(t.has(71)).toBe(false); });
  it("trdel72", () => { const t = new Treap<number>(); t.insert(72); expect(t.delete(72)).toBe(true); expect(t.has(72)).toBe(false); });
  it("trdel73", () => { const t = new Treap<number>(); t.insert(73); expect(t.delete(73)).toBe(true); expect(t.has(73)).toBe(false); });
  it("trdel74", () => { const t = new Treap<number>(); t.insert(74); expect(t.delete(74)).toBe(true); expect(t.has(74)).toBe(false); });
  it("trdel75", () => { const t = new Treap<number>(); t.insert(75); expect(t.delete(75)).toBe(true); expect(t.has(75)).toBe(false); });
  it("trdel76", () => { const t = new Treap<number>(); t.insert(76); expect(t.delete(76)).toBe(true); expect(t.has(76)).toBe(false); });
  it("trdel77", () => { const t = new Treap<number>(); t.insert(77); expect(t.delete(77)).toBe(true); expect(t.has(77)).toBe(false); });
  it("trdel78", () => { const t = new Treap<number>(); t.insert(78); expect(t.delete(78)).toBe(true); expect(t.has(78)).toBe(false); });
  it("trdel79", () => { const t = new Treap<number>(); t.insert(79); expect(t.delete(79)).toBe(true); expect(t.has(79)).toBe(false); });
  it("trdel80", () => { const t = new Treap<number>(); t.insert(80); expect(t.delete(80)).toBe(true); expect(t.has(80)).toBe(false); });
  it("trdel81", () => { const t = new Treap<number>(); t.insert(81); expect(t.delete(81)).toBe(true); expect(t.has(81)).toBe(false); });
  it("trdel82", () => { const t = new Treap<number>(); t.insert(82); expect(t.delete(82)).toBe(true); expect(t.has(82)).toBe(false); });
  it("trdel83", () => { const t = new Treap<number>(); t.insert(83); expect(t.delete(83)).toBe(true); expect(t.has(83)).toBe(false); });
  it("trdel84", () => { const t = new Treap<number>(); t.insert(84); expect(t.delete(84)).toBe(true); expect(t.has(84)).toBe(false); });
  it("trdel85", () => { const t = new Treap<number>(); t.insert(85); expect(t.delete(85)).toBe(true); expect(t.has(85)).toBe(false); });
  it("trdel86", () => { const t = new Treap<number>(); t.insert(86); expect(t.delete(86)).toBe(true); expect(t.has(86)).toBe(false); });
  it("trdel87", () => { const t = new Treap<number>(); t.insert(87); expect(t.delete(87)).toBe(true); expect(t.has(87)).toBe(false); });
  it("trdel88", () => { const t = new Treap<number>(); t.insert(88); expect(t.delete(88)).toBe(true); expect(t.has(88)).toBe(false); });
  it("trdel89", () => { const t = new Treap<number>(); t.insert(89); expect(t.delete(89)).toBe(true); expect(t.has(89)).toBe(false); });
  it("trdel90", () => { const t = new Treap<number>(); t.insert(90); expect(t.delete(90)).toBe(true); expect(t.has(90)).toBe(false); });
  it("trdel91", () => { const t = new Treap<number>(); t.insert(91); expect(t.delete(91)).toBe(true); expect(t.has(91)).toBe(false); });
  it("trdel92", () => { const t = new Treap<number>(); t.insert(92); expect(t.delete(92)).toBe(true); expect(t.has(92)).toBe(false); });
  it("trdel93", () => { const t = new Treap<number>(); t.insert(93); expect(t.delete(93)).toBe(true); expect(t.has(93)).toBe(false); });
  it("trdel94", () => { const t = new Treap<number>(); t.insert(94); expect(t.delete(94)).toBe(true); expect(t.has(94)).toBe(false); });
  it("trdel95", () => { const t = new Treap<number>(); t.insert(95); expect(t.delete(95)).toBe(true); expect(t.has(95)).toBe(false); });
  it("trdel96", () => { const t = new Treap<number>(); t.insert(96); expect(t.delete(96)).toBe(true); expect(t.has(96)).toBe(false); });
  it("trdel97", () => { const t = new Treap<number>(); t.insert(97); expect(t.delete(97)).toBe(true); expect(t.has(97)).toBe(false); });
  it("trdel98", () => { const t = new Treap<number>(); t.insert(98); expect(t.delete(98)).toBe(true); expect(t.has(98)).toBe(false); });
  it("trdel99", () => { const t = new Treap<number>(); t.insert(99); expect(t.delete(99)).toBe(true); expect(t.has(99)).toBe(false); });
});
