// Copyright (c) 2026 Nexara DMCC. All rights reserved.
// Proprietary and confidential. Unauthorised copying prohibited.
// See LICENCE file for details.

import { fcfs, sjf, priorityScheduling, roundRobin } from "../scheduling-algorithms";

describe("fcfs single job", () => {
  it("fs1", () => { const r = fcfs([{id:0,burst:1,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(1); });
  it("fs2", () => { const r = fcfs([{id:0,burst:2,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(2); });
  it("fs3", () => { const r = fcfs([{id:0,burst:3,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(3); });
  it("fs4", () => { const r = fcfs([{id:0,burst:4,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(4); });
  it("fs5", () => { const r = fcfs([{id:0,burst:5,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(5); });
  it("fs6", () => { const r = fcfs([{id:0,burst:6,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(6); });
  it("fs7", () => { const r = fcfs([{id:0,burst:7,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(7); });
  it("fs8", () => { const r = fcfs([{id:0,burst:8,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(8); });
  it("fs9", () => { const r = fcfs([{id:0,burst:9,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(9); });
  it("fs10", () => { const r = fcfs([{id:0,burst:10,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(10); });
  it("fs11", () => { const r = fcfs([{id:0,burst:11,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(11); });
  it("fs12", () => { const r = fcfs([{id:0,burst:12,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(12); });
  it("fs13", () => { const r = fcfs([{id:0,burst:13,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(13); });
  it("fs14", () => { const r = fcfs([{id:0,burst:14,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(14); });
  it("fs15", () => { const r = fcfs([{id:0,burst:15,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(15); });
  it("fs16", () => { const r = fcfs([{id:0,burst:16,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(16); });
  it("fs17", () => { const r = fcfs([{id:0,burst:17,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(17); });
  it("fs18", () => { const r = fcfs([{id:0,burst:18,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(18); });
  it("fs19", () => { const r = fcfs([{id:0,burst:19,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(19); });
  it("fs20", () => { const r = fcfs([{id:0,burst:20,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(20); });
  it("fs21", () => { const r = fcfs([{id:0,burst:21,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(21); });
  it("fs22", () => { const r = fcfs([{id:0,burst:22,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(22); });
  it("fs23", () => { const r = fcfs([{id:0,burst:23,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(23); });
  it("fs24", () => { const r = fcfs([{id:0,burst:24,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(24); });
  it("fs25", () => { const r = fcfs([{id:0,burst:25,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(25); });
  it("fs26", () => { const r = fcfs([{id:0,burst:26,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(26); });
  it("fs27", () => { const r = fcfs([{id:0,burst:27,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(27); });
  it("fs28", () => { const r = fcfs([{id:0,burst:28,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(28); });
  it("fs29", () => { const r = fcfs([{id:0,burst:29,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(29); });
  it("fs30", () => { const r = fcfs([{id:0,burst:30,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(30); });
  it("fs31", () => { const r = fcfs([{id:0,burst:31,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(31); });
  it("fs32", () => { const r = fcfs([{id:0,burst:32,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(32); });
  it("fs33", () => { const r = fcfs([{id:0,burst:33,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(33); });
  it("fs34", () => { const r = fcfs([{id:0,burst:34,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(34); });
  it("fs35", () => { const r = fcfs([{id:0,burst:35,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(35); });
  it("fs36", () => { const r = fcfs([{id:0,burst:36,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(36); });
  it("fs37", () => { const r = fcfs([{id:0,burst:37,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(37); });
  it("fs38", () => { const r = fcfs([{id:0,burst:38,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(38); });
  it("fs39", () => { const r = fcfs([{id:0,burst:39,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(39); });
  it("fs40", () => { const r = fcfs([{id:0,burst:40,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(40); });
  it("fs41", () => { const r = fcfs([{id:0,burst:41,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(41); });
  it("fs42", () => { const r = fcfs([{id:0,burst:42,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(42); });
  it("fs43", () => { const r = fcfs([{id:0,burst:43,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(43); });
  it("fs44", () => { const r = fcfs([{id:0,burst:44,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(44); });
  it("fs45", () => { const r = fcfs([{id:0,burst:45,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(45); });
  it("fs46", () => { const r = fcfs([{id:0,burst:46,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(46); });
  it("fs47", () => { const r = fcfs([{id:0,burst:47,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(47); });
  it("fs48", () => { const r = fcfs([{id:0,burst:48,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(48); });
  it("fs49", () => { const r = fcfs([{id:0,burst:49,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(49); });
  it("fs50", () => { const r = fcfs([{id:0,burst:50,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(50); });
  it("fs51", () => { const r = fcfs([{id:0,burst:51,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(51); });
  it("fs52", () => { const r = fcfs([{id:0,burst:52,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(52); });
  it("fs53", () => { const r = fcfs([{id:0,burst:53,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(53); });
  it("fs54", () => { const r = fcfs([{id:0,burst:54,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(54); });
  it("fs55", () => { const r = fcfs([{id:0,burst:55,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(55); });
  it("fs56", () => { const r = fcfs([{id:0,burst:56,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(56); });
  it("fs57", () => { const r = fcfs([{id:0,burst:57,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(57); });
  it("fs58", () => { const r = fcfs([{id:0,burst:58,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(58); });
  it("fs59", () => { const r = fcfs([{id:0,burst:59,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(59); });
  it("fs60", () => { const r = fcfs([{id:0,burst:60,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(60); });
  it("fs61", () => { const r = fcfs([{id:0,burst:61,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(61); });
  it("fs62", () => { const r = fcfs([{id:0,burst:62,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(62); });
  it("fs63", () => { const r = fcfs([{id:0,burst:63,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(63); });
  it("fs64", () => { const r = fcfs([{id:0,burst:64,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(64); });
  it("fs65", () => { const r = fcfs([{id:0,burst:65,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(65); });
  it("fs66", () => { const r = fcfs([{id:0,burst:66,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(66); });
  it("fs67", () => { const r = fcfs([{id:0,burst:67,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(67); });
  it("fs68", () => { const r = fcfs([{id:0,burst:68,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(68); });
  it("fs69", () => { const r = fcfs([{id:0,burst:69,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(69); });
  it("fs70", () => { const r = fcfs([{id:0,burst:70,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(70); });
  it("fs71", () => { const r = fcfs([{id:0,burst:71,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(71); });
  it("fs72", () => { const r = fcfs([{id:0,burst:72,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(72); });
  it("fs73", () => { const r = fcfs([{id:0,burst:73,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(73); });
  it("fs74", () => { const r = fcfs([{id:0,burst:74,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(74); });
  it("fs75", () => { const r = fcfs([{id:0,burst:75,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(75); });
  it("fs76", () => { const r = fcfs([{id:0,burst:76,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(76); });
  it("fs77", () => { const r = fcfs([{id:0,burst:77,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(77); });
  it("fs78", () => { const r = fcfs([{id:0,burst:78,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(78); });
  it("fs79", () => { const r = fcfs([{id:0,burst:79,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(79); });
  it("fs80", () => { const r = fcfs([{id:0,burst:80,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(80); });
  it("fs81", () => { const r = fcfs([{id:0,burst:81,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(81); });
  it("fs82", () => { const r = fcfs([{id:0,burst:82,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(82); });
  it("fs83", () => { const r = fcfs([{id:0,burst:83,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(83); });
  it("fs84", () => { const r = fcfs([{id:0,burst:84,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(84); });
  it("fs85", () => { const r = fcfs([{id:0,burst:85,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(85); });
  it("fs86", () => { const r = fcfs([{id:0,burst:86,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(86); });
  it("fs87", () => { const r = fcfs([{id:0,burst:87,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(87); });
  it("fs88", () => { const r = fcfs([{id:0,burst:88,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(88); });
  it("fs89", () => { const r = fcfs([{id:0,burst:89,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(89); });
  it("fs90", () => { const r = fcfs([{id:0,burst:90,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(90); });
  it("fs91", () => { const r = fcfs([{id:0,burst:91,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(91); });
  it("fs92", () => { const r = fcfs([{id:0,burst:92,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(92); });
  it("fs93", () => { const r = fcfs([{id:0,burst:93,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(93); });
  it("fs94", () => { const r = fcfs([{id:0,burst:94,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(94); });
  it("fs95", () => { const r = fcfs([{id:0,burst:95,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(95); });
  it("fs96", () => { const r = fcfs([{id:0,burst:96,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(96); });
  it("fs97", () => { const r = fcfs([{id:0,burst:97,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(97); });
  it("fs98", () => { const r = fcfs([{id:0,burst:98,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(98); });
  it("fs99", () => { const r = fcfs([{id:0,burst:99,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(99); });
  it("fs100", () => { const r = fcfs([{id:0,burst:100,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(100); });
  it("fs101", () => { const r = fcfs([{id:0,burst:101,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(101); });
  it("fs102", () => { const r = fcfs([{id:0,burst:102,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(102); });
  it("fs103", () => { const r = fcfs([{id:0,burst:103,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(103); });
  it("fs104", () => { const r = fcfs([{id:0,burst:104,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(104); });
  it("fs105", () => { const r = fcfs([{id:0,burst:105,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(105); });
  it("fs106", () => { const r = fcfs([{id:0,burst:106,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(106); });
  it("fs107", () => { const r = fcfs([{id:0,burst:107,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(107); });
  it("fs108", () => { const r = fcfs([{id:0,burst:108,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(108); });
  it("fs109", () => { const r = fcfs([{id:0,burst:109,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(109); });
  it("fs110", () => { const r = fcfs([{id:0,burst:110,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(110); });
  it("fs111", () => { const r = fcfs([{id:0,burst:111,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(111); });
  it("fs112", () => { const r = fcfs([{id:0,burst:112,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(112); });
  it("fs113", () => { const r = fcfs([{id:0,burst:113,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(113); });
  it("fs114", () => { const r = fcfs([{id:0,burst:114,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(114); });
  it("fs115", () => { const r = fcfs([{id:0,burst:115,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(115); });
  it("fs116", () => { const r = fcfs([{id:0,burst:116,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(116); });
  it("fs117", () => { const r = fcfs([{id:0,burst:117,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(117); });
  it("fs118", () => { const r = fcfs([{id:0,burst:118,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(118); });
  it("fs119", () => { const r = fcfs([{id:0,burst:119,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(119); });
  it("fs120", () => { const r = fcfs([{id:0,burst:120,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(120); });
  it("fs121", () => { const r = fcfs([{id:0,burst:121,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(121); });
  it("fs122", () => { const r = fcfs([{id:0,burst:122,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(122); });
  it("fs123", () => { const r = fcfs([{id:0,burst:123,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(123); });
  it("fs124", () => { const r = fcfs([{id:0,burst:124,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(124); });
  it("fs125", () => { const r = fcfs([{id:0,burst:125,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(125); });
  it("fs126", () => { const r = fcfs([{id:0,burst:126,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(126); });
  it("fs127", () => { const r = fcfs([{id:0,burst:127,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(127); });
  it("fs128", () => { const r = fcfs([{id:0,burst:128,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(128); });
  it("fs129", () => { const r = fcfs([{id:0,burst:129,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(129); });
  it("fs130", () => { const r = fcfs([{id:0,burst:130,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(130); });
  it("fs131", () => { const r = fcfs([{id:0,burst:131,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(131); });
  it("fs132", () => { const r = fcfs([{id:0,burst:132,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(132); });
  it("fs133", () => { const r = fcfs([{id:0,burst:133,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(133); });
  it("fs134", () => { const r = fcfs([{id:0,burst:134,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(134); });
  it("fs135", () => { const r = fcfs([{id:0,burst:135,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(135); });
  it("fs136", () => { const r = fcfs([{id:0,burst:136,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(136); });
  it("fs137", () => { const r = fcfs([{id:0,burst:137,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(137); });
  it("fs138", () => { const r = fcfs([{id:0,burst:138,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(138); });
  it("fs139", () => { const r = fcfs([{id:0,burst:139,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(139); });
  it("fs140", () => { const r = fcfs([{id:0,burst:140,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(140); });
  it("fs141", () => { const r = fcfs([{id:0,burst:141,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(141); });
  it("fs142", () => { const r = fcfs([{id:0,burst:142,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(142); });
  it("fs143", () => { const r = fcfs([{id:0,burst:143,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(143); });
  it("fs144", () => { const r = fcfs([{id:0,burst:144,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(144); });
  it("fs145", () => { const r = fcfs([{id:0,burst:145,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(145); });
  it("fs146", () => { const r = fcfs([{id:0,burst:146,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(146); });
  it("fs147", () => { const r = fcfs([{id:0,burst:147,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(147); });
  it("fs148", () => { const r = fcfs([{id:0,burst:148,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(148); });
  it("fs149", () => { const r = fcfs([{id:0,burst:149,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(149); });
  it("fs150", () => { const r = fcfs([{id:0,burst:150,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(150); });
  it("fs151", () => { const r = fcfs([{id:0,burst:151,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(151); });
  it("fs152", () => { const r = fcfs([{id:0,burst:152,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(152); });
  it("fs153", () => { const r = fcfs([{id:0,burst:153,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(153); });
  it("fs154", () => { const r = fcfs([{id:0,burst:154,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(154); });
  it("fs155", () => { const r = fcfs([{id:0,burst:155,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(155); });
  it("fs156", () => { const r = fcfs([{id:0,burst:156,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(156); });
  it("fs157", () => { const r = fcfs([{id:0,burst:157,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(157); });
  it("fs158", () => { const r = fcfs([{id:0,burst:158,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(158); });
  it("fs159", () => { const r = fcfs([{id:0,burst:159,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(159); });
  it("fs160", () => { const r = fcfs([{id:0,burst:160,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(160); });
  it("fs161", () => { const r = fcfs([{id:0,burst:161,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(161); });
  it("fs162", () => { const r = fcfs([{id:0,burst:162,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(162); });
  it("fs163", () => { const r = fcfs([{id:0,burst:163,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(163); });
  it("fs164", () => { const r = fcfs([{id:0,burst:164,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(164); });
  it("fs165", () => { const r = fcfs([{id:0,burst:165,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(165); });
  it("fs166", () => { const r = fcfs([{id:0,burst:166,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(166); });
  it("fs167", () => { const r = fcfs([{id:0,burst:167,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(167); });
  it("fs168", () => { const r = fcfs([{id:0,burst:168,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(168); });
  it("fs169", () => { const r = fcfs([{id:0,burst:169,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(169); });
  it("fs170", () => { const r = fcfs([{id:0,burst:170,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(170); });
  it("fs171", () => { const r = fcfs([{id:0,burst:171,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(171); });
  it("fs172", () => { const r = fcfs([{id:0,burst:172,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(172); });
  it("fs173", () => { const r = fcfs([{id:0,burst:173,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(173); });
  it("fs174", () => { const r = fcfs([{id:0,burst:174,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(174); });
  it("fs175", () => { const r = fcfs([{id:0,burst:175,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(175); });
  it("fs176", () => { const r = fcfs([{id:0,burst:176,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(176); });
  it("fs177", () => { const r = fcfs([{id:0,burst:177,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(177); });
  it("fs178", () => { const r = fcfs([{id:0,burst:178,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(178); });
  it("fs179", () => { const r = fcfs([{id:0,burst:179,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(179); });
  it("fs180", () => { const r = fcfs([{id:0,burst:180,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(180); });
  it("fs181", () => { const r = fcfs([{id:0,burst:181,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(181); });
  it("fs182", () => { const r = fcfs([{id:0,burst:182,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(182); });
  it("fs183", () => { const r = fcfs([{id:0,burst:183,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(183); });
  it("fs184", () => { const r = fcfs([{id:0,burst:184,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(184); });
  it("fs185", () => { const r = fcfs([{id:0,burst:185,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(185); });
  it("fs186", () => { const r = fcfs([{id:0,burst:186,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(186); });
  it("fs187", () => { const r = fcfs([{id:0,burst:187,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(187); });
  it("fs188", () => { const r = fcfs([{id:0,burst:188,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(188); });
  it("fs189", () => { const r = fcfs([{id:0,burst:189,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(189); });
  it("fs190", () => { const r = fcfs([{id:0,burst:190,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(190); });
  it("fs191", () => { const r = fcfs([{id:0,burst:191,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(191); });
  it("fs192", () => { const r = fcfs([{id:0,burst:192,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(192); });
  it("fs193", () => { const r = fcfs([{id:0,burst:193,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(193); });
  it("fs194", () => { const r = fcfs([{id:0,burst:194,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(194); });
  it("fs195", () => { const r = fcfs([{id:0,burst:195,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(195); });
  it("fs196", () => { const r = fcfs([{id:0,burst:196,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(196); });
  it("fs197", () => { const r = fcfs([{id:0,burst:197,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(197); });
  it("fs198", () => { const r = fcfs([{id:0,burst:198,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(198); });
  it("fs199", () => { const r = fcfs([{id:0,burst:199,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(199); });
  it("fs200", () => { const r = fcfs([{id:0,burst:200,arrival:0}]); expect(r.avgWaiting).toBe(0); expect(r.avgTurnaround).toBe(200); });
});

describe("fcfs two jobs ordered", () => {
  it("f2_1", () => { const r = fcfs([{id:0,burst:1,arrival:0},{id:1,burst:1,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_2", () => { const r = fcfs([{id:0,burst:2,arrival:0},{id:1,burst:2,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_3", () => { const r = fcfs([{id:0,burst:3,arrival:0},{id:1,burst:3,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_4", () => { const r = fcfs([{id:0,burst:4,arrival:0},{id:1,burst:4,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_5", () => { const r = fcfs([{id:0,burst:5,arrival:0},{id:1,burst:5,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_6", () => { const r = fcfs([{id:0,burst:6,arrival:0},{id:1,burst:6,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_7", () => { const r = fcfs([{id:0,burst:7,arrival:0},{id:1,burst:7,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_8", () => { const r = fcfs([{id:0,burst:8,arrival:0},{id:1,burst:8,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_9", () => { const r = fcfs([{id:0,burst:9,arrival:0},{id:1,burst:9,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_10", () => { const r = fcfs([{id:0,burst:10,arrival:0},{id:1,burst:10,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_11", () => { const r = fcfs([{id:0,burst:11,arrival:0},{id:1,burst:11,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_12", () => { const r = fcfs([{id:0,burst:12,arrival:0},{id:1,burst:12,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_13", () => { const r = fcfs([{id:0,burst:13,arrival:0},{id:1,burst:13,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_14", () => { const r = fcfs([{id:0,burst:14,arrival:0},{id:1,burst:14,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_15", () => { const r = fcfs([{id:0,burst:15,arrival:0},{id:1,burst:15,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_16", () => { const r = fcfs([{id:0,burst:16,arrival:0},{id:1,burst:16,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_17", () => { const r = fcfs([{id:0,burst:17,arrival:0},{id:1,burst:17,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_18", () => { const r = fcfs([{id:0,burst:18,arrival:0},{id:1,burst:18,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_19", () => { const r = fcfs([{id:0,burst:19,arrival:0},{id:1,burst:19,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_20", () => { const r = fcfs([{id:0,burst:20,arrival:0},{id:1,burst:20,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_21", () => { const r = fcfs([{id:0,burst:21,arrival:0},{id:1,burst:21,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_22", () => { const r = fcfs([{id:0,burst:22,arrival:0},{id:1,burst:22,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_23", () => { const r = fcfs([{id:0,burst:23,arrival:0},{id:1,burst:23,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_24", () => { const r = fcfs([{id:0,burst:24,arrival:0},{id:1,burst:24,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_25", () => { const r = fcfs([{id:0,burst:25,arrival:0},{id:1,burst:25,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_26", () => { const r = fcfs([{id:0,burst:26,arrival:0},{id:1,burst:26,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_27", () => { const r = fcfs([{id:0,burst:27,arrival:0},{id:1,burst:27,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_28", () => { const r = fcfs([{id:0,burst:28,arrival:0},{id:1,burst:28,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_29", () => { const r = fcfs([{id:0,burst:29,arrival:0},{id:1,burst:29,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_30", () => { const r = fcfs([{id:0,burst:30,arrival:0},{id:1,burst:30,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_31", () => { const r = fcfs([{id:0,burst:31,arrival:0},{id:1,burst:31,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_32", () => { const r = fcfs([{id:0,burst:32,arrival:0},{id:1,burst:32,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_33", () => { const r = fcfs([{id:0,burst:33,arrival:0},{id:1,burst:33,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_34", () => { const r = fcfs([{id:0,burst:34,arrival:0},{id:1,burst:34,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_35", () => { const r = fcfs([{id:0,burst:35,arrival:0},{id:1,burst:35,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_36", () => { const r = fcfs([{id:0,burst:36,arrival:0},{id:1,burst:36,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_37", () => { const r = fcfs([{id:0,burst:37,arrival:0},{id:1,burst:37,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_38", () => { const r = fcfs([{id:0,burst:38,arrival:0},{id:1,burst:38,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_39", () => { const r = fcfs([{id:0,burst:39,arrival:0},{id:1,burst:39,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_40", () => { const r = fcfs([{id:0,burst:40,arrival:0},{id:1,burst:40,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_41", () => { const r = fcfs([{id:0,burst:41,arrival:0},{id:1,burst:41,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_42", () => { const r = fcfs([{id:0,burst:42,arrival:0},{id:1,burst:42,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_43", () => { const r = fcfs([{id:0,burst:43,arrival:0},{id:1,burst:43,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_44", () => { const r = fcfs([{id:0,burst:44,arrival:0},{id:1,burst:44,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_45", () => { const r = fcfs([{id:0,burst:45,arrival:0},{id:1,burst:45,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_46", () => { const r = fcfs([{id:0,burst:46,arrival:0},{id:1,burst:46,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_47", () => { const r = fcfs([{id:0,burst:47,arrival:0},{id:1,burst:47,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_48", () => { const r = fcfs([{id:0,burst:48,arrival:0},{id:1,burst:48,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_49", () => { const r = fcfs([{id:0,burst:49,arrival:0},{id:1,burst:49,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_50", () => { const r = fcfs([{id:0,burst:50,arrival:0},{id:1,burst:50,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_51", () => { const r = fcfs([{id:0,burst:51,arrival:0},{id:1,burst:51,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_52", () => { const r = fcfs([{id:0,burst:52,arrival:0},{id:1,burst:52,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_53", () => { const r = fcfs([{id:0,burst:53,arrival:0},{id:1,burst:53,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_54", () => { const r = fcfs([{id:0,burst:54,arrival:0},{id:1,burst:54,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_55", () => { const r = fcfs([{id:0,burst:55,arrival:0},{id:1,burst:55,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_56", () => { const r = fcfs([{id:0,burst:56,arrival:0},{id:1,burst:56,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_57", () => { const r = fcfs([{id:0,burst:57,arrival:0},{id:1,burst:57,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_58", () => { const r = fcfs([{id:0,burst:58,arrival:0},{id:1,burst:58,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_59", () => { const r = fcfs([{id:0,burst:59,arrival:0},{id:1,burst:59,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_60", () => { const r = fcfs([{id:0,burst:60,arrival:0},{id:1,burst:60,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_61", () => { const r = fcfs([{id:0,burst:61,arrival:0},{id:1,burst:61,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_62", () => { const r = fcfs([{id:0,burst:62,arrival:0},{id:1,burst:62,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_63", () => { const r = fcfs([{id:0,burst:63,arrival:0},{id:1,burst:63,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_64", () => { const r = fcfs([{id:0,burst:64,arrival:0},{id:1,burst:64,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_65", () => { const r = fcfs([{id:0,burst:65,arrival:0},{id:1,burst:65,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_66", () => { const r = fcfs([{id:0,burst:66,arrival:0},{id:1,burst:66,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_67", () => { const r = fcfs([{id:0,burst:67,arrival:0},{id:1,burst:67,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_68", () => { const r = fcfs([{id:0,burst:68,arrival:0},{id:1,burst:68,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_69", () => { const r = fcfs([{id:0,burst:69,arrival:0},{id:1,burst:69,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_70", () => { const r = fcfs([{id:0,burst:70,arrival:0},{id:1,burst:70,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_71", () => { const r = fcfs([{id:0,burst:71,arrival:0},{id:1,burst:71,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_72", () => { const r = fcfs([{id:0,burst:72,arrival:0},{id:1,burst:72,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_73", () => { const r = fcfs([{id:0,burst:73,arrival:0},{id:1,burst:73,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_74", () => { const r = fcfs([{id:0,burst:74,arrival:0},{id:1,burst:74,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_75", () => { const r = fcfs([{id:0,burst:75,arrival:0},{id:1,burst:75,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_76", () => { const r = fcfs([{id:0,burst:76,arrival:0},{id:1,burst:76,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_77", () => { const r = fcfs([{id:0,burst:77,arrival:0},{id:1,burst:77,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_78", () => { const r = fcfs([{id:0,burst:78,arrival:0},{id:1,burst:78,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_79", () => { const r = fcfs([{id:0,burst:79,arrival:0},{id:1,burst:79,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_80", () => { const r = fcfs([{id:0,burst:80,arrival:0},{id:1,burst:80,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_81", () => { const r = fcfs([{id:0,burst:81,arrival:0},{id:1,burst:81,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_82", () => { const r = fcfs([{id:0,burst:82,arrival:0},{id:1,burst:82,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_83", () => { const r = fcfs([{id:0,burst:83,arrival:0},{id:1,burst:83,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_84", () => { const r = fcfs([{id:0,burst:84,arrival:0},{id:1,burst:84,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_85", () => { const r = fcfs([{id:0,burst:85,arrival:0},{id:1,burst:85,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_86", () => { const r = fcfs([{id:0,burst:86,arrival:0},{id:1,burst:86,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_87", () => { const r = fcfs([{id:0,burst:87,arrival:0},{id:1,burst:87,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_88", () => { const r = fcfs([{id:0,burst:88,arrival:0},{id:1,burst:88,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_89", () => { const r = fcfs([{id:0,burst:89,arrival:0},{id:1,burst:89,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_90", () => { const r = fcfs([{id:0,burst:90,arrival:0},{id:1,burst:90,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_91", () => { const r = fcfs([{id:0,burst:91,arrival:0},{id:1,burst:91,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_92", () => { const r = fcfs([{id:0,burst:92,arrival:0},{id:1,burst:92,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_93", () => { const r = fcfs([{id:0,burst:93,arrival:0},{id:1,burst:93,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_94", () => { const r = fcfs([{id:0,burst:94,arrival:0},{id:1,burst:94,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_95", () => { const r = fcfs([{id:0,burst:95,arrival:0},{id:1,burst:95,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_96", () => { const r = fcfs([{id:0,burst:96,arrival:0},{id:1,burst:96,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_97", () => { const r = fcfs([{id:0,burst:97,arrival:0},{id:1,burst:97,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_98", () => { const r = fcfs([{id:0,burst:98,arrival:0},{id:1,burst:98,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_99", () => { const r = fcfs([{id:0,burst:99,arrival:0},{id:1,burst:99,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
  it("f2_100", () => { const r = fcfs([{id:0,burst:100,arrival:0},{id:1,burst:100,arrival:0}]); expect(r.avgWaiting).toBeGreaterThanOrEqual(0); });
});

describe("sjf single job", () => {
  it("ss1", () => { const r = sjf([{id:0,burst:1,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss2", () => { const r = sjf([{id:0,burst:2,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss3", () => { const r = sjf([{id:0,burst:3,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss4", () => { const r = sjf([{id:0,burst:4,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss5", () => { const r = sjf([{id:0,burst:5,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss6", () => { const r = sjf([{id:0,burst:6,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss7", () => { const r = sjf([{id:0,burst:7,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss8", () => { const r = sjf([{id:0,burst:8,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss9", () => { const r = sjf([{id:0,burst:9,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss10", () => { const r = sjf([{id:0,burst:10,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss11", () => { const r = sjf([{id:0,burst:11,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss12", () => { const r = sjf([{id:0,burst:12,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss13", () => { const r = sjf([{id:0,burst:13,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss14", () => { const r = sjf([{id:0,burst:14,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss15", () => { const r = sjf([{id:0,burst:15,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss16", () => { const r = sjf([{id:0,burst:16,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss17", () => { const r = sjf([{id:0,burst:17,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss18", () => { const r = sjf([{id:0,burst:18,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss19", () => { const r = sjf([{id:0,burst:19,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss20", () => { const r = sjf([{id:0,burst:20,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss21", () => { const r = sjf([{id:0,burst:21,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss22", () => { const r = sjf([{id:0,burst:22,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss23", () => { const r = sjf([{id:0,burst:23,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss24", () => { const r = sjf([{id:0,burst:24,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss25", () => { const r = sjf([{id:0,burst:25,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss26", () => { const r = sjf([{id:0,burst:26,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss27", () => { const r = sjf([{id:0,burst:27,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss28", () => { const r = sjf([{id:0,burst:28,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss29", () => { const r = sjf([{id:0,burst:29,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss30", () => { const r = sjf([{id:0,burst:30,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss31", () => { const r = sjf([{id:0,burst:31,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss32", () => { const r = sjf([{id:0,burst:32,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss33", () => { const r = sjf([{id:0,burst:33,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss34", () => { const r = sjf([{id:0,burst:34,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss35", () => { const r = sjf([{id:0,burst:35,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss36", () => { const r = sjf([{id:0,burst:36,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss37", () => { const r = sjf([{id:0,burst:37,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss38", () => { const r = sjf([{id:0,burst:38,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss39", () => { const r = sjf([{id:0,burst:39,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss40", () => { const r = sjf([{id:0,burst:40,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss41", () => { const r = sjf([{id:0,burst:41,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss42", () => { const r = sjf([{id:0,burst:42,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss43", () => { const r = sjf([{id:0,burst:43,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss44", () => { const r = sjf([{id:0,burst:44,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss45", () => { const r = sjf([{id:0,burst:45,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss46", () => { const r = sjf([{id:0,burst:46,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss47", () => { const r = sjf([{id:0,burst:47,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss48", () => { const r = sjf([{id:0,burst:48,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss49", () => { const r = sjf([{id:0,burst:49,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss50", () => { const r = sjf([{id:0,burst:50,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss51", () => { const r = sjf([{id:0,burst:51,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss52", () => { const r = sjf([{id:0,burst:52,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss53", () => { const r = sjf([{id:0,burst:53,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss54", () => { const r = sjf([{id:0,burst:54,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss55", () => { const r = sjf([{id:0,burst:55,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss56", () => { const r = sjf([{id:0,burst:56,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss57", () => { const r = sjf([{id:0,burst:57,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss58", () => { const r = sjf([{id:0,burst:58,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss59", () => { const r = sjf([{id:0,burst:59,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss60", () => { const r = sjf([{id:0,burst:60,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss61", () => { const r = sjf([{id:0,burst:61,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss62", () => { const r = sjf([{id:0,burst:62,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss63", () => { const r = sjf([{id:0,burst:63,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss64", () => { const r = sjf([{id:0,burst:64,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss65", () => { const r = sjf([{id:0,burst:65,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss66", () => { const r = sjf([{id:0,burst:66,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss67", () => { const r = sjf([{id:0,burst:67,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss68", () => { const r = sjf([{id:0,burst:68,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss69", () => { const r = sjf([{id:0,burst:69,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss70", () => { const r = sjf([{id:0,burst:70,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss71", () => { const r = sjf([{id:0,burst:71,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss72", () => { const r = sjf([{id:0,burst:72,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss73", () => { const r = sjf([{id:0,burst:73,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss74", () => { const r = sjf([{id:0,burst:74,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss75", () => { const r = sjf([{id:0,burst:75,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss76", () => { const r = sjf([{id:0,burst:76,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss77", () => { const r = sjf([{id:0,burst:77,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss78", () => { const r = sjf([{id:0,burst:78,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss79", () => { const r = sjf([{id:0,burst:79,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss80", () => { const r = sjf([{id:0,burst:80,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss81", () => { const r = sjf([{id:0,burst:81,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss82", () => { const r = sjf([{id:0,burst:82,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss83", () => { const r = sjf([{id:0,burst:83,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss84", () => { const r = sjf([{id:0,burst:84,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss85", () => { const r = sjf([{id:0,burst:85,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss86", () => { const r = sjf([{id:0,burst:86,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss87", () => { const r = sjf([{id:0,burst:87,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss88", () => { const r = sjf([{id:0,burst:88,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss89", () => { const r = sjf([{id:0,burst:89,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss90", () => { const r = sjf([{id:0,burst:90,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss91", () => { const r = sjf([{id:0,burst:91,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss92", () => { const r = sjf([{id:0,burst:92,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss93", () => { const r = sjf([{id:0,burst:93,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss94", () => { const r = sjf([{id:0,burst:94,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss95", () => { const r = sjf([{id:0,burst:95,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss96", () => { const r = sjf([{id:0,burst:96,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss97", () => { const r = sjf([{id:0,burst:97,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss98", () => { const r = sjf([{id:0,burst:98,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss99", () => { const r = sjf([{id:0,burst:99,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss100", () => { const r = sjf([{id:0,burst:100,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss101", () => { const r = sjf([{id:0,burst:101,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss102", () => { const r = sjf([{id:0,burst:102,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss103", () => { const r = sjf([{id:0,burst:103,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss104", () => { const r = sjf([{id:0,burst:104,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss105", () => { const r = sjf([{id:0,burst:105,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss106", () => { const r = sjf([{id:0,burst:106,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss107", () => { const r = sjf([{id:0,burst:107,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss108", () => { const r = sjf([{id:0,burst:108,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss109", () => { const r = sjf([{id:0,burst:109,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss110", () => { const r = sjf([{id:0,burst:110,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss111", () => { const r = sjf([{id:0,burst:111,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss112", () => { const r = sjf([{id:0,burst:112,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss113", () => { const r = sjf([{id:0,burst:113,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss114", () => { const r = sjf([{id:0,burst:114,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss115", () => { const r = sjf([{id:0,burst:115,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss116", () => { const r = sjf([{id:0,burst:116,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss117", () => { const r = sjf([{id:0,burst:117,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss118", () => { const r = sjf([{id:0,burst:118,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss119", () => { const r = sjf([{id:0,burst:119,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss120", () => { const r = sjf([{id:0,burst:120,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss121", () => { const r = sjf([{id:0,burst:121,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss122", () => { const r = sjf([{id:0,burst:122,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss123", () => { const r = sjf([{id:0,burst:123,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss124", () => { const r = sjf([{id:0,burst:124,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss125", () => { const r = sjf([{id:0,burst:125,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss126", () => { const r = sjf([{id:0,burst:126,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss127", () => { const r = sjf([{id:0,burst:127,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss128", () => { const r = sjf([{id:0,burst:128,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss129", () => { const r = sjf([{id:0,burst:129,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss130", () => { const r = sjf([{id:0,burst:130,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss131", () => { const r = sjf([{id:0,burst:131,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss132", () => { const r = sjf([{id:0,burst:132,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss133", () => { const r = sjf([{id:0,burst:133,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss134", () => { const r = sjf([{id:0,burst:134,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss135", () => { const r = sjf([{id:0,burst:135,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss136", () => { const r = sjf([{id:0,burst:136,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss137", () => { const r = sjf([{id:0,burst:137,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss138", () => { const r = sjf([{id:0,burst:138,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss139", () => { const r = sjf([{id:0,burst:139,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss140", () => { const r = sjf([{id:0,burst:140,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss141", () => { const r = sjf([{id:0,burst:141,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss142", () => { const r = sjf([{id:0,burst:142,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss143", () => { const r = sjf([{id:0,burst:143,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss144", () => { const r = sjf([{id:0,burst:144,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss145", () => { const r = sjf([{id:0,burst:145,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss146", () => { const r = sjf([{id:0,burst:146,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss147", () => { const r = sjf([{id:0,burst:147,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss148", () => { const r = sjf([{id:0,burst:148,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss149", () => { const r = sjf([{id:0,burst:149,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss150", () => { const r = sjf([{id:0,burst:150,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss151", () => { const r = sjf([{id:0,burst:151,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss152", () => { const r = sjf([{id:0,burst:152,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss153", () => { const r = sjf([{id:0,burst:153,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss154", () => { const r = sjf([{id:0,burst:154,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss155", () => { const r = sjf([{id:0,burst:155,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss156", () => { const r = sjf([{id:0,burst:156,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss157", () => { const r = sjf([{id:0,burst:157,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss158", () => { const r = sjf([{id:0,burst:158,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss159", () => { const r = sjf([{id:0,burst:159,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss160", () => { const r = sjf([{id:0,burst:160,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss161", () => { const r = sjf([{id:0,burst:161,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss162", () => { const r = sjf([{id:0,burst:162,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss163", () => { const r = sjf([{id:0,burst:163,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss164", () => { const r = sjf([{id:0,burst:164,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss165", () => { const r = sjf([{id:0,burst:165,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss166", () => { const r = sjf([{id:0,burst:166,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss167", () => { const r = sjf([{id:0,burst:167,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss168", () => { const r = sjf([{id:0,burst:168,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss169", () => { const r = sjf([{id:0,burst:169,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss170", () => { const r = sjf([{id:0,burst:170,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss171", () => { const r = sjf([{id:0,burst:171,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss172", () => { const r = sjf([{id:0,burst:172,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss173", () => { const r = sjf([{id:0,burst:173,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss174", () => { const r = sjf([{id:0,burst:174,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss175", () => { const r = sjf([{id:0,burst:175,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss176", () => { const r = sjf([{id:0,burst:176,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss177", () => { const r = sjf([{id:0,burst:177,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss178", () => { const r = sjf([{id:0,burst:178,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss179", () => { const r = sjf([{id:0,burst:179,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss180", () => { const r = sjf([{id:0,burst:180,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss181", () => { const r = sjf([{id:0,burst:181,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss182", () => { const r = sjf([{id:0,burst:182,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss183", () => { const r = sjf([{id:0,burst:183,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss184", () => { const r = sjf([{id:0,burst:184,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss185", () => { const r = sjf([{id:0,burst:185,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss186", () => { const r = sjf([{id:0,burst:186,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss187", () => { const r = sjf([{id:0,burst:187,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss188", () => { const r = sjf([{id:0,burst:188,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss189", () => { const r = sjf([{id:0,burst:189,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss190", () => { const r = sjf([{id:0,burst:190,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss191", () => { const r = sjf([{id:0,burst:191,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss192", () => { const r = sjf([{id:0,burst:192,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss193", () => { const r = sjf([{id:0,burst:193,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss194", () => { const r = sjf([{id:0,burst:194,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss195", () => { const r = sjf([{id:0,burst:195,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss196", () => { const r = sjf([{id:0,burst:196,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss197", () => { const r = sjf([{id:0,burst:197,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss198", () => { const r = sjf([{id:0,burst:198,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss199", () => { const r = sjf([{id:0,burst:199,arrival:0}]); expect(r.avgWaiting).toBe(0); });
  it("ss200", () => { const r = sjf([{id:0,burst:200,arrival:0}]); expect(r.avgWaiting).toBe(0); });
});

describe("priorityScheduling single job", () => {
  it("ps1", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:1}]); expect(r.avgWaiting).toBe(0); });
  it("ps2", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:2}]); expect(r.avgWaiting).toBe(0); });
  it("ps3", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:3}]); expect(r.avgWaiting).toBe(0); });
  it("ps4", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:4}]); expect(r.avgWaiting).toBe(0); });
  it("ps5", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:5}]); expect(r.avgWaiting).toBe(0); });
  it("ps6", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:6}]); expect(r.avgWaiting).toBe(0); });
  it("ps7", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:7}]); expect(r.avgWaiting).toBe(0); });
  it("ps8", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:8}]); expect(r.avgWaiting).toBe(0); });
  it("ps9", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:9}]); expect(r.avgWaiting).toBe(0); });
  it("ps10", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:10}]); expect(r.avgWaiting).toBe(0); });
  it("ps11", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:11}]); expect(r.avgWaiting).toBe(0); });
  it("ps12", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:12}]); expect(r.avgWaiting).toBe(0); });
  it("ps13", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:13}]); expect(r.avgWaiting).toBe(0); });
  it("ps14", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:14}]); expect(r.avgWaiting).toBe(0); });
  it("ps15", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:15}]); expect(r.avgWaiting).toBe(0); });
  it("ps16", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:16}]); expect(r.avgWaiting).toBe(0); });
  it("ps17", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:17}]); expect(r.avgWaiting).toBe(0); });
  it("ps18", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:18}]); expect(r.avgWaiting).toBe(0); });
  it("ps19", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:19}]); expect(r.avgWaiting).toBe(0); });
  it("ps20", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:20}]); expect(r.avgWaiting).toBe(0); });
  it("ps21", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:21}]); expect(r.avgWaiting).toBe(0); });
  it("ps22", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:22}]); expect(r.avgWaiting).toBe(0); });
  it("ps23", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:23}]); expect(r.avgWaiting).toBe(0); });
  it("ps24", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:24}]); expect(r.avgWaiting).toBe(0); });
  it("ps25", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:25}]); expect(r.avgWaiting).toBe(0); });
  it("ps26", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:26}]); expect(r.avgWaiting).toBe(0); });
  it("ps27", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:27}]); expect(r.avgWaiting).toBe(0); });
  it("ps28", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:28}]); expect(r.avgWaiting).toBe(0); });
  it("ps29", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:29}]); expect(r.avgWaiting).toBe(0); });
  it("ps30", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:30}]); expect(r.avgWaiting).toBe(0); });
  it("ps31", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:31}]); expect(r.avgWaiting).toBe(0); });
  it("ps32", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:32}]); expect(r.avgWaiting).toBe(0); });
  it("ps33", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:33}]); expect(r.avgWaiting).toBe(0); });
  it("ps34", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:34}]); expect(r.avgWaiting).toBe(0); });
  it("ps35", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:35}]); expect(r.avgWaiting).toBe(0); });
  it("ps36", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:36}]); expect(r.avgWaiting).toBe(0); });
  it("ps37", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:37}]); expect(r.avgWaiting).toBe(0); });
  it("ps38", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:38}]); expect(r.avgWaiting).toBe(0); });
  it("ps39", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:39}]); expect(r.avgWaiting).toBe(0); });
  it("ps40", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:40}]); expect(r.avgWaiting).toBe(0); });
  it("ps41", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:41}]); expect(r.avgWaiting).toBe(0); });
  it("ps42", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:42}]); expect(r.avgWaiting).toBe(0); });
  it("ps43", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:43}]); expect(r.avgWaiting).toBe(0); });
  it("ps44", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:44}]); expect(r.avgWaiting).toBe(0); });
  it("ps45", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:45}]); expect(r.avgWaiting).toBe(0); });
  it("ps46", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:46}]); expect(r.avgWaiting).toBe(0); });
  it("ps47", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:47}]); expect(r.avgWaiting).toBe(0); });
  it("ps48", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:48}]); expect(r.avgWaiting).toBe(0); });
  it("ps49", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:49}]); expect(r.avgWaiting).toBe(0); });
  it("ps50", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:50}]); expect(r.avgWaiting).toBe(0); });
  it("ps51", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:51}]); expect(r.avgWaiting).toBe(0); });
  it("ps52", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:52}]); expect(r.avgWaiting).toBe(0); });
  it("ps53", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:53}]); expect(r.avgWaiting).toBe(0); });
  it("ps54", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:54}]); expect(r.avgWaiting).toBe(0); });
  it("ps55", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:55}]); expect(r.avgWaiting).toBe(0); });
  it("ps56", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:56}]); expect(r.avgWaiting).toBe(0); });
  it("ps57", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:57}]); expect(r.avgWaiting).toBe(0); });
  it("ps58", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:58}]); expect(r.avgWaiting).toBe(0); });
  it("ps59", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:59}]); expect(r.avgWaiting).toBe(0); });
  it("ps60", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:60}]); expect(r.avgWaiting).toBe(0); });
  it("ps61", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:61}]); expect(r.avgWaiting).toBe(0); });
  it("ps62", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:62}]); expect(r.avgWaiting).toBe(0); });
  it("ps63", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:63}]); expect(r.avgWaiting).toBe(0); });
  it("ps64", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:64}]); expect(r.avgWaiting).toBe(0); });
  it("ps65", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:65}]); expect(r.avgWaiting).toBe(0); });
  it("ps66", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:66}]); expect(r.avgWaiting).toBe(0); });
  it("ps67", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:67}]); expect(r.avgWaiting).toBe(0); });
  it("ps68", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:68}]); expect(r.avgWaiting).toBe(0); });
  it("ps69", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:69}]); expect(r.avgWaiting).toBe(0); });
  it("ps70", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:70}]); expect(r.avgWaiting).toBe(0); });
  it("ps71", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:71}]); expect(r.avgWaiting).toBe(0); });
  it("ps72", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:72}]); expect(r.avgWaiting).toBe(0); });
  it("ps73", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:73}]); expect(r.avgWaiting).toBe(0); });
  it("ps74", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:74}]); expect(r.avgWaiting).toBe(0); });
  it("ps75", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:75}]); expect(r.avgWaiting).toBe(0); });
  it("ps76", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:76}]); expect(r.avgWaiting).toBe(0); });
  it("ps77", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:77}]); expect(r.avgWaiting).toBe(0); });
  it("ps78", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:78}]); expect(r.avgWaiting).toBe(0); });
  it("ps79", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:79}]); expect(r.avgWaiting).toBe(0); });
  it("ps80", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:80}]); expect(r.avgWaiting).toBe(0); });
  it("ps81", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:81}]); expect(r.avgWaiting).toBe(0); });
  it("ps82", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:82}]); expect(r.avgWaiting).toBe(0); });
  it("ps83", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:83}]); expect(r.avgWaiting).toBe(0); });
  it("ps84", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:84}]); expect(r.avgWaiting).toBe(0); });
  it("ps85", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:85}]); expect(r.avgWaiting).toBe(0); });
  it("ps86", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:86}]); expect(r.avgWaiting).toBe(0); });
  it("ps87", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:87}]); expect(r.avgWaiting).toBe(0); });
  it("ps88", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:88}]); expect(r.avgWaiting).toBe(0); });
  it("ps89", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:89}]); expect(r.avgWaiting).toBe(0); });
  it("ps90", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:90}]); expect(r.avgWaiting).toBe(0); });
  it("ps91", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:91}]); expect(r.avgWaiting).toBe(0); });
  it("ps92", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:92}]); expect(r.avgWaiting).toBe(0); });
  it("ps93", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:93}]); expect(r.avgWaiting).toBe(0); });
  it("ps94", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:94}]); expect(r.avgWaiting).toBe(0); });
  it("ps95", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:95}]); expect(r.avgWaiting).toBe(0); });
  it("ps96", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:96}]); expect(r.avgWaiting).toBe(0); });
  it("ps97", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:97}]); expect(r.avgWaiting).toBe(0); });
  it("ps98", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:98}]); expect(r.avgWaiting).toBe(0); });
  it("ps99", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:99}]); expect(r.avgWaiting).toBe(0); });
  it("ps100", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:100}]); expect(r.avgWaiting).toBe(0); });
  it("ps101", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:101}]); expect(r.avgWaiting).toBe(0); });
  it("ps102", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:102}]); expect(r.avgWaiting).toBe(0); });
  it("ps103", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:103}]); expect(r.avgWaiting).toBe(0); });
  it("ps104", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:104}]); expect(r.avgWaiting).toBe(0); });
  it("ps105", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:105}]); expect(r.avgWaiting).toBe(0); });
  it("ps106", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:106}]); expect(r.avgWaiting).toBe(0); });
  it("ps107", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:107}]); expect(r.avgWaiting).toBe(0); });
  it("ps108", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:108}]); expect(r.avgWaiting).toBe(0); });
  it("ps109", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:109}]); expect(r.avgWaiting).toBe(0); });
  it("ps110", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:110}]); expect(r.avgWaiting).toBe(0); });
  it("ps111", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:111}]); expect(r.avgWaiting).toBe(0); });
  it("ps112", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:112}]); expect(r.avgWaiting).toBe(0); });
  it("ps113", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:113}]); expect(r.avgWaiting).toBe(0); });
  it("ps114", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:114}]); expect(r.avgWaiting).toBe(0); });
  it("ps115", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:115}]); expect(r.avgWaiting).toBe(0); });
  it("ps116", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:116}]); expect(r.avgWaiting).toBe(0); });
  it("ps117", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:117}]); expect(r.avgWaiting).toBe(0); });
  it("ps118", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:118}]); expect(r.avgWaiting).toBe(0); });
  it("ps119", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:119}]); expect(r.avgWaiting).toBe(0); });
  it("ps120", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:120}]); expect(r.avgWaiting).toBe(0); });
  it("ps121", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:121}]); expect(r.avgWaiting).toBe(0); });
  it("ps122", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:122}]); expect(r.avgWaiting).toBe(0); });
  it("ps123", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:123}]); expect(r.avgWaiting).toBe(0); });
  it("ps124", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:124}]); expect(r.avgWaiting).toBe(0); });
  it("ps125", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:125}]); expect(r.avgWaiting).toBe(0); });
  it("ps126", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:126}]); expect(r.avgWaiting).toBe(0); });
  it("ps127", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:127}]); expect(r.avgWaiting).toBe(0); });
  it("ps128", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:128}]); expect(r.avgWaiting).toBe(0); });
  it("ps129", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:129}]); expect(r.avgWaiting).toBe(0); });
  it("ps130", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:130}]); expect(r.avgWaiting).toBe(0); });
  it("ps131", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:131}]); expect(r.avgWaiting).toBe(0); });
  it("ps132", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:132}]); expect(r.avgWaiting).toBe(0); });
  it("ps133", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:133}]); expect(r.avgWaiting).toBe(0); });
  it("ps134", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:134}]); expect(r.avgWaiting).toBe(0); });
  it("ps135", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:135}]); expect(r.avgWaiting).toBe(0); });
  it("ps136", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:136}]); expect(r.avgWaiting).toBe(0); });
  it("ps137", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:137}]); expect(r.avgWaiting).toBe(0); });
  it("ps138", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:138}]); expect(r.avgWaiting).toBe(0); });
  it("ps139", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:139}]); expect(r.avgWaiting).toBe(0); });
  it("ps140", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:140}]); expect(r.avgWaiting).toBe(0); });
  it("ps141", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:141}]); expect(r.avgWaiting).toBe(0); });
  it("ps142", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:142}]); expect(r.avgWaiting).toBe(0); });
  it("ps143", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:143}]); expect(r.avgWaiting).toBe(0); });
  it("ps144", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:144}]); expect(r.avgWaiting).toBe(0); });
  it("ps145", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:145}]); expect(r.avgWaiting).toBe(0); });
  it("ps146", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:146}]); expect(r.avgWaiting).toBe(0); });
  it("ps147", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:147}]); expect(r.avgWaiting).toBe(0); });
  it("ps148", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:148}]); expect(r.avgWaiting).toBe(0); });
  it("ps149", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:149}]); expect(r.avgWaiting).toBe(0); });
  it("ps150", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:150}]); expect(r.avgWaiting).toBe(0); });
  it("ps151", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:151}]); expect(r.avgWaiting).toBe(0); });
  it("ps152", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:152}]); expect(r.avgWaiting).toBe(0); });
  it("ps153", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:153}]); expect(r.avgWaiting).toBe(0); });
  it("ps154", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:154}]); expect(r.avgWaiting).toBe(0); });
  it("ps155", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:155}]); expect(r.avgWaiting).toBe(0); });
  it("ps156", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:156}]); expect(r.avgWaiting).toBe(0); });
  it("ps157", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:157}]); expect(r.avgWaiting).toBe(0); });
  it("ps158", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:158}]); expect(r.avgWaiting).toBe(0); });
  it("ps159", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:159}]); expect(r.avgWaiting).toBe(0); });
  it("ps160", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:160}]); expect(r.avgWaiting).toBe(0); });
  it("ps161", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:161}]); expect(r.avgWaiting).toBe(0); });
  it("ps162", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:162}]); expect(r.avgWaiting).toBe(0); });
  it("ps163", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:163}]); expect(r.avgWaiting).toBe(0); });
  it("ps164", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:164}]); expect(r.avgWaiting).toBe(0); });
  it("ps165", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:165}]); expect(r.avgWaiting).toBe(0); });
  it("ps166", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:166}]); expect(r.avgWaiting).toBe(0); });
  it("ps167", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:167}]); expect(r.avgWaiting).toBe(0); });
  it("ps168", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:168}]); expect(r.avgWaiting).toBe(0); });
  it("ps169", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:169}]); expect(r.avgWaiting).toBe(0); });
  it("ps170", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:170}]); expect(r.avgWaiting).toBe(0); });
  it("ps171", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:171}]); expect(r.avgWaiting).toBe(0); });
  it("ps172", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:172}]); expect(r.avgWaiting).toBe(0); });
  it("ps173", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:173}]); expect(r.avgWaiting).toBe(0); });
  it("ps174", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:174}]); expect(r.avgWaiting).toBe(0); });
  it("ps175", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:175}]); expect(r.avgWaiting).toBe(0); });
  it("ps176", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:176}]); expect(r.avgWaiting).toBe(0); });
  it("ps177", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:177}]); expect(r.avgWaiting).toBe(0); });
  it("ps178", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:178}]); expect(r.avgWaiting).toBe(0); });
  it("ps179", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:179}]); expect(r.avgWaiting).toBe(0); });
  it("ps180", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:180}]); expect(r.avgWaiting).toBe(0); });
  it("ps181", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:181}]); expect(r.avgWaiting).toBe(0); });
  it("ps182", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:182}]); expect(r.avgWaiting).toBe(0); });
  it("ps183", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:183}]); expect(r.avgWaiting).toBe(0); });
  it("ps184", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:184}]); expect(r.avgWaiting).toBe(0); });
  it("ps185", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:185}]); expect(r.avgWaiting).toBe(0); });
  it("ps186", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:186}]); expect(r.avgWaiting).toBe(0); });
  it("ps187", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:187}]); expect(r.avgWaiting).toBe(0); });
  it("ps188", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:188}]); expect(r.avgWaiting).toBe(0); });
  it("ps189", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:189}]); expect(r.avgWaiting).toBe(0); });
  it("ps190", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:190}]); expect(r.avgWaiting).toBe(0); });
  it("ps191", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:191}]); expect(r.avgWaiting).toBe(0); });
  it("ps192", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:192}]); expect(r.avgWaiting).toBe(0); });
  it("ps193", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:193}]); expect(r.avgWaiting).toBe(0); });
  it("ps194", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:194}]); expect(r.avgWaiting).toBe(0); });
  it("ps195", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:195}]); expect(r.avgWaiting).toBe(0); });
  it("ps196", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:196}]); expect(r.avgWaiting).toBe(0); });
  it("ps197", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:197}]); expect(r.avgWaiting).toBe(0); });
  it("ps198", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:198}]); expect(r.avgWaiting).toBe(0); });
  it("ps199", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:199}]); expect(r.avgWaiting).toBe(0); });
  it("ps200", () => { const r = priorityScheduling([{id:0,burst:5,arrival:0,priority:200}]); expect(r.avgWaiting).toBe(0); });
});

describe("roundRobin single job", () => {
  it("rr1", () => { const r = roundRobin([{id:0,burst:1,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr2", () => { const r = roundRobin([{id:0,burst:2,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr3", () => { const r = roundRobin([{id:0,burst:3,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr4", () => { const r = roundRobin([{id:0,burst:4,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr5", () => { const r = roundRobin([{id:0,burst:5,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr6", () => { const r = roundRobin([{id:0,burst:6,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr7", () => { const r = roundRobin([{id:0,burst:7,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr8", () => { const r = roundRobin([{id:0,burst:8,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr9", () => { const r = roundRobin([{id:0,burst:9,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr10", () => { const r = roundRobin([{id:0,burst:10,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr11", () => { const r = roundRobin([{id:0,burst:11,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr12", () => { const r = roundRobin([{id:0,burst:12,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr13", () => { const r = roundRobin([{id:0,burst:13,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr14", () => { const r = roundRobin([{id:0,burst:14,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr15", () => { const r = roundRobin([{id:0,burst:15,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr16", () => { const r = roundRobin([{id:0,burst:16,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr17", () => { const r = roundRobin([{id:0,burst:17,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr18", () => { const r = roundRobin([{id:0,burst:18,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr19", () => { const r = roundRobin([{id:0,burst:19,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr20", () => { const r = roundRobin([{id:0,burst:20,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr21", () => { const r = roundRobin([{id:0,burst:21,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr22", () => { const r = roundRobin([{id:0,burst:22,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr23", () => { const r = roundRobin([{id:0,burst:23,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr24", () => { const r = roundRobin([{id:0,burst:24,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr25", () => { const r = roundRobin([{id:0,burst:25,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr26", () => { const r = roundRobin([{id:0,burst:26,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr27", () => { const r = roundRobin([{id:0,burst:27,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr28", () => { const r = roundRobin([{id:0,burst:28,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr29", () => { const r = roundRobin([{id:0,burst:29,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr30", () => { const r = roundRobin([{id:0,burst:30,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr31", () => { const r = roundRobin([{id:0,burst:31,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr32", () => { const r = roundRobin([{id:0,burst:32,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr33", () => { const r = roundRobin([{id:0,burst:33,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr34", () => { const r = roundRobin([{id:0,burst:34,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr35", () => { const r = roundRobin([{id:0,burst:35,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr36", () => { const r = roundRobin([{id:0,burst:36,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr37", () => { const r = roundRobin([{id:0,burst:37,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr38", () => { const r = roundRobin([{id:0,burst:38,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr39", () => { const r = roundRobin([{id:0,burst:39,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr40", () => { const r = roundRobin([{id:0,burst:40,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr41", () => { const r = roundRobin([{id:0,burst:41,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr42", () => { const r = roundRobin([{id:0,burst:42,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr43", () => { const r = roundRobin([{id:0,burst:43,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr44", () => { const r = roundRobin([{id:0,burst:44,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr45", () => { const r = roundRobin([{id:0,burst:45,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr46", () => { const r = roundRobin([{id:0,burst:46,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr47", () => { const r = roundRobin([{id:0,burst:47,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr48", () => { const r = roundRobin([{id:0,burst:48,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr49", () => { const r = roundRobin([{id:0,burst:49,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr50", () => { const r = roundRobin([{id:0,burst:50,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr51", () => { const r = roundRobin([{id:0,burst:51,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr52", () => { const r = roundRobin([{id:0,burst:52,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr53", () => { const r = roundRobin([{id:0,burst:53,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr54", () => { const r = roundRobin([{id:0,burst:54,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr55", () => { const r = roundRobin([{id:0,burst:55,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr56", () => { const r = roundRobin([{id:0,burst:56,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr57", () => { const r = roundRobin([{id:0,burst:57,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr58", () => { const r = roundRobin([{id:0,burst:58,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr59", () => { const r = roundRobin([{id:0,burst:59,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr60", () => { const r = roundRobin([{id:0,burst:60,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr61", () => { const r = roundRobin([{id:0,burst:61,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr62", () => { const r = roundRobin([{id:0,burst:62,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr63", () => { const r = roundRobin([{id:0,burst:63,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr64", () => { const r = roundRobin([{id:0,burst:64,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr65", () => { const r = roundRobin([{id:0,burst:65,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr66", () => { const r = roundRobin([{id:0,burst:66,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr67", () => { const r = roundRobin([{id:0,burst:67,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr68", () => { const r = roundRobin([{id:0,burst:68,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr69", () => { const r = roundRobin([{id:0,burst:69,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr70", () => { const r = roundRobin([{id:0,burst:70,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr71", () => { const r = roundRobin([{id:0,burst:71,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr72", () => { const r = roundRobin([{id:0,burst:72,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr73", () => { const r = roundRobin([{id:0,burst:73,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr74", () => { const r = roundRobin([{id:0,burst:74,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr75", () => { const r = roundRobin([{id:0,burst:75,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr76", () => { const r = roundRobin([{id:0,burst:76,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr77", () => { const r = roundRobin([{id:0,burst:77,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr78", () => { const r = roundRobin([{id:0,burst:78,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr79", () => { const r = roundRobin([{id:0,burst:79,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr80", () => { const r = roundRobin([{id:0,burst:80,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr81", () => { const r = roundRobin([{id:0,burst:81,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr82", () => { const r = roundRobin([{id:0,burst:82,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr83", () => { const r = roundRobin([{id:0,burst:83,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr84", () => { const r = roundRobin([{id:0,burst:84,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr85", () => { const r = roundRobin([{id:0,burst:85,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr86", () => { const r = roundRobin([{id:0,burst:86,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr87", () => { const r = roundRobin([{id:0,burst:87,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr88", () => { const r = roundRobin([{id:0,burst:88,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr89", () => { const r = roundRobin([{id:0,burst:89,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr90", () => { const r = roundRobin([{id:0,burst:90,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr91", () => { const r = roundRobin([{id:0,burst:91,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr92", () => { const r = roundRobin([{id:0,burst:92,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr93", () => { const r = roundRobin([{id:0,burst:93,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr94", () => { const r = roundRobin([{id:0,burst:94,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr95", () => { const r = roundRobin([{id:0,burst:95,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr96", () => { const r = roundRobin([{id:0,burst:96,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr97", () => { const r = roundRobin([{id:0,burst:97,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr98", () => { const r = roundRobin([{id:0,burst:98,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr99", () => { const r = roundRobin([{id:0,burst:99,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr100", () => { const r = roundRobin([{id:0,burst:100,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr101", () => { const r = roundRobin([{id:0,burst:101,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr102", () => { const r = roundRobin([{id:0,burst:102,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr103", () => { const r = roundRobin([{id:0,burst:103,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr104", () => { const r = roundRobin([{id:0,burst:104,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr105", () => { const r = roundRobin([{id:0,burst:105,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr106", () => { const r = roundRobin([{id:0,burst:106,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr107", () => { const r = roundRobin([{id:0,burst:107,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr108", () => { const r = roundRobin([{id:0,burst:108,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr109", () => { const r = roundRobin([{id:0,burst:109,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr110", () => { const r = roundRobin([{id:0,burst:110,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr111", () => { const r = roundRobin([{id:0,burst:111,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr112", () => { const r = roundRobin([{id:0,burst:112,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr113", () => { const r = roundRobin([{id:0,burst:113,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr114", () => { const r = roundRobin([{id:0,burst:114,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr115", () => { const r = roundRobin([{id:0,burst:115,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr116", () => { const r = roundRobin([{id:0,burst:116,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr117", () => { const r = roundRobin([{id:0,burst:117,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr118", () => { const r = roundRobin([{id:0,burst:118,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr119", () => { const r = roundRobin([{id:0,burst:119,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr120", () => { const r = roundRobin([{id:0,burst:120,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr121", () => { const r = roundRobin([{id:0,burst:121,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr122", () => { const r = roundRobin([{id:0,burst:122,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr123", () => { const r = roundRobin([{id:0,burst:123,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr124", () => { const r = roundRobin([{id:0,burst:124,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr125", () => { const r = roundRobin([{id:0,burst:125,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr126", () => { const r = roundRobin([{id:0,burst:126,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr127", () => { const r = roundRobin([{id:0,burst:127,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr128", () => { const r = roundRobin([{id:0,burst:128,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr129", () => { const r = roundRobin([{id:0,burst:129,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr130", () => { const r = roundRobin([{id:0,burst:130,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr131", () => { const r = roundRobin([{id:0,burst:131,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr132", () => { const r = roundRobin([{id:0,burst:132,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr133", () => { const r = roundRobin([{id:0,burst:133,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr134", () => { const r = roundRobin([{id:0,burst:134,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr135", () => { const r = roundRobin([{id:0,burst:135,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr136", () => { const r = roundRobin([{id:0,burst:136,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr137", () => { const r = roundRobin([{id:0,burst:137,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr138", () => { const r = roundRobin([{id:0,burst:138,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr139", () => { const r = roundRobin([{id:0,burst:139,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr140", () => { const r = roundRobin([{id:0,burst:140,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr141", () => { const r = roundRobin([{id:0,burst:141,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr142", () => { const r = roundRobin([{id:0,burst:142,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr143", () => { const r = roundRobin([{id:0,burst:143,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr144", () => { const r = roundRobin([{id:0,burst:144,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr145", () => { const r = roundRobin([{id:0,burst:145,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr146", () => { const r = roundRobin([{id:0,burst:146,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr147", () => { const r = roundRobin([{id:0,burst:147,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr148", () => { const r = roundRobin([{id:0,burst:148,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr149", () => { const r = roundRobin([{id:0,burst:149,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr150", () => { const r = roundRobin([{id:0,burst:150,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr151", () => { const r = roundRobin([{id:0,burst:151,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr152", () => { const r = roundRobin([{id:0,burst:152,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr153", () => { const r = roundRobin([{id:0,burst:153,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr154", () => { const r = roundRobin([{id:0,burst:154,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr155", () => { const r = roundRobin([{id:0,burst:155,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr156", () => { const r = roundRobin([{id:0,burst:156,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr157", () => { const r = roundRobin([{id:0,burst:157,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr158", () => { const r = roundRobin([{id:0,burst:158,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr159", () => { const r = roundRobin([{id:0,burst:159,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr160", () => { const r = roundRobin([{id:0,burst:160,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr161", () => { const r = roundRobin([{id:0,burst:161,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr162", () => { const r = roundRobin([{id:0,burst:162,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr163", () => { const r = roundRobin([{id:0,burst:163,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr164", () => { const r = roundRobin([{id:0,burst:164,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr165", () => { const r = roundRobin([{id:0,burst:165,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr166", () => { const r = roundRobin([{id:0,burst:166,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr167", () => { const r = roundRobin([{id:0,burst:167,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr168", () => { const r = roundRobin([{id:0,burst:168,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr169", () => { const r = roundRobin([{id:0,burst:169,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr170", () => { const r = roundRobin([{id:0,burst:170,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr171", () => { const r = roundRobin([{id:0,burst:171,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr172", () => { const r = roundRobin([{id:0,burst:172,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr173", () => { const r = roundRobin([{id:0,burst:173,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr174", () => { const r = roundRobin([{id:0,burst:174,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr175", () => { const r = roundRobin([{id:0,burst:175,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr176", () => { const r = roundRobin([{id:0,burst:176,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr177", () => { const r = roundRobin([{id:0,burst:177,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr178", () => { const r = roundRobin([{id:0,burst:178,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr179", () => { const r = roundRobin([{id:0,burst:179,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr180", () => { const r = roundRobin([{id:0,burst:180,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr181", () => { const r = roundRobin([{id:0,burst:181,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr182", () => { const r = roundRobin([{id:0,burst:182,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr183", () => { const r = roundRobin([{id:0,burst:183,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr184", () => { const r = roundRobin([{id:0,burst:184,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr185", () => { const r = roundRobin([{id:0,burst:185,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr186", () => { const r = roundRobin([{id:0,burst:186,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr187", () => { const r = roundRobin([{id:0,burst:187,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr188", () => { const r = roundRobin([{id:0,burst:188,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr189", () => { const r = roundRobin([{id:0,burst:189,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr190", () => { const r = roundRobin([{id:0,burst:190,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr191", () => { const r = roundRobin([{id:0,burst:191,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr192", () => { const r = roundRobin([{id:0,burst:192,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr193", () => { const r = roundRobin([{id:0,burst:193,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr194", () => { const r = roundRobin([{id:0,burst:194,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr195", () => { const r = roundRobin([{id:0,burst:195,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr196", () => { const r = roundRobin([{id:0,burst:196,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr197", () => { const r = roundRobin([{id:0,burst:197,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr198", () => { const r = roundRobin([{id:0,burst:198,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr199", () => { const r = roundRobin([{id:0,burst:199,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
  it("rr200", () => { const r = roundRobin([{id:0,burst:200,arrival:0}], 4); expect(r.avgWaiting).toBe(0); });
});

describe("fcfs order count", () => {
  it("fo1", () => { const jobs = Array.from({length:1},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(1); });
  it("fo2", () => { const jobs = Array.from({length:2},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(2); });
  it("fo3", () => { const jobs = Array.from({length:3},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(3); });
  it("fo4", () => { const jobs = Array.from({length:4},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(4); });
  it("fo5", () => { const jobs = Array.from({length:5},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(5); });
  it("fo6", () => { const jobs = Array.from({length:6},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(6); });
  it("fo7", () => { const jobs = Array.from({length:7},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(7); });
  it("fo8", () => { const jobs = Array.from({length:8},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(8); });
  it("fo9", () => { const jobs = Array.from({length:9},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(9); });
  it("fo10", () => { const jobs = Array.from({length:10},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(10); });
  it("fo11", () => { const jobs = Array.from({length:11},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(11); });
  it("fo12", () => { const jobs = Array.from({length:12},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(12); });
  it("fo13", () => { const jobs = Array.from({length:13},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(13); });
  it("fo14", () => { const jobs = Array.from({length:14},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(14); });
  it("fo15", () => { const jobs = Array.from({length:15},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(15); });
  it("fo16", () => { const jobs = Array.from({length:16},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(16); });
  it("fo17", () => { const jobs = Array.from({length:17},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(17); });
  it("fo18", () => { const jobs = Array.from({length:18},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(18); });
  it("fo19", () => { const jobs = Array.from({length:19},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(19); });
  it("fo20", () => { const jobs = Array.from({length:20},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(20); });
  it("fo21", () => { const jobs = Array.from({length:21},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(21); });
  it("fo22", () => { const jobs = Array.from({length:22},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(22); });
  it("fo23", () => { const jobs = Array.from({length:23},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(23); });
  it("fo24", () => { const jobs = Array.from({length:24},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(24); });
  it("fo25", () => { const jobs = Array.from({length:25},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(25); });
  it("fo26", () => { const jobs = Array.from({length:26},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(26); });
  it("fo27", () => { const jobs = Array.from({length:27},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(27); });
  it("fo28", () => { const jobs = Array.from({length:28},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(28); });
  it("fo29", () => { const jobs = Array.from({length:29},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(29); });
  it("fo30", () => { const jobs = Array.from({length:30},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(30); });
  it("fo31", () => { const jobs = Array.from({length:31},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(31); });
  it("fo32", () => { const jobs = Array.from({length:32},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(32); });
  it("fo33", () => { const jobs = Array.from({length:33},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(33); });
  it("fo34", () => { const jobs = Array.from({length:34},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(34); });
  it("fo35", () => { const jobs = Array.from({length:35},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(35); });
  it("fo36", () => { const jobs = Array.from({length:36},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(36); });
  it("fo37", () => { const jobs = Array.from({length:37},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(37); });
  it("fo38", () => { const jobs = Array.from({length:38},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(38); });
  it("fo39", () => { const jobs = Array.from({length:39},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(39); });
  it("fo40", () => { const jobs = Array.from({length:40},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(40); });
  it("fo41", () => { const jobs = Array.from({length:41},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(41); });
  it("fo42", () => { const jobs = Array.from({length:42},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(42); });
  it("fo43", () => { const jobs = Array.from({length:43},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(43); });
  it("fo44", () => { const jobs = Array.from({length:44},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(44); });
  it("fo45", () => { const jobs = Array.from({length:45},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(45); });
  it("fo46", () => { const jobs = Array.from({length:46},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(46); });
  it("fo47", () => { const jobs = Array.from({length:47},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(47); });
  it("fo48", () => { const jobs = Array.from({length:48},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(48); });
  it("fo49", () => { const jobs = Array.from({length:49},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(49); });
  it("fo50", () => { const jobs = Array.from({length:50},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(50); });
  it("fo51", () => { const jobs = Array.from({length:51},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(51); });
  it("fo52", () => { const jobs = Array.from({length:52},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(52); });
  it("fo53", () => { const jobs = Array.from({length:53},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(53); });
  it("fo54", () => { const jobs = Array.from({length:54},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(54); });
  it("fo55", () => { const jobs = Array.from({length:55},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(55); });
  it("fo56", () => { const jobs = Array.from({length:56},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(56); });
  it("fo57", () => { const jobs = Array.from({length:57},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(57); });
  it("fo58", () => { const jobs = Array.from({length:58},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(58); });
  it("fo59", () => { const jobs = Array.from({length:59},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(59); });
  it("fo60", () => { const jobs = Array.from({length:60},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(60); });
  it("fo61", () => { const jobs = Array.from({length:61},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(61); });
  it("fo62", () => { const jobs = Array.from({length:62},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(62); });
  it("fo63", () => { const jobs = Array.from({length:63},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(63); });
  it("fo64", () => { const jobs = Array.from({length:64},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(64); });
  it("fo65", () => { const jobs = Array.from({length:65},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(65); });
  it("fo66", () => { const jobs = Array.from({length:66},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(66); });
  it("fo67", () => { const jobs = Array.from({length:67},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(67); });
  it("fo68", () => { const jobs = Array.from({length:68},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(68); });
  it("fo69", () => { const jobs = Array.from({length:69},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(69); });
  it("fo70", () => { const jobs = Array.from({length:70},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(70); });
  it("fo71", () => { const jobs = Array.from({length:71},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(71); });
  it("fo72", () => { const jobs = Array.from({length:72},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(72); });
  it("fo73", () => { const jobs = Array.from({length:73},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(73); });
  it("fo74", () => { const jobs = Array.from({length:74},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(74); });
  it("fo75", () => { const jobs = Array.from({length:75},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(75); });
  it("fo76", () => { const jobs = Array.from({length:76},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(76); });
  it("fo77", () => { const jobs = Array.from({length:77},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(77); });
  it("fo78", () => { const jobs = Array.from({length:78},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(78); });
  it("fo79", () => { const jobs = Array.from({length:79},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(79); });
  it("fo80", () => { const jobs = Array.from({length:80},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(80); });
  it("fo81", () => { const jobs = Array.from({length:81},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(81); });
  it("fo82", () => { const jobs = Array.from({length:82},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(82); });
  it("fo83", () => { const jobs = Array.from({length:83},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(83); });
  it("fo84", () => { const jobs = Array.from({length:84},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(84); });
  it("fo85", () => { const jobs = Array.from({length:85},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(85); });
  it("fo86", () => { const jobs = Array.from({length:86},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(86); });
  it("fo87", () => { const jobs = Array.from({length:87},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(87); });
  it("fo88", () => { const jobs = Array.from({length:88},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(88); });
  it("fo89", () => { const jobs = Array.from({length:89},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(89); });
  it("fo90", () => { const jobs = Array.from({length:90},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(90); });
  it("fo91", () => { const jobs = Array.from({length:91},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(91); });
  it("fo92", () => { const jobs = Array.from({length:92},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(92); });
  it("fo93", () => { const jobs = Array.from({length:93},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(93); });
  it("fo94", () => { const jobs = Array.from({length:94},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(94); });
  it("fo95", () => { const jobs = Array.from({length:95},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(95); });
  it("fo96", () => { const jobs = Array.from({length:96},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(96); });
  it("fo97", () => { const jobs = Array.from({length:97},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(97); });
  it("fo98", () => { const jobs = Array.from({length:98},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(98); });
  it("fo99", () => { const jobs = Array.from({length:99},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(99); });
  it("fo100", () => { const jobs = Array.from({length:100},(_,i)=>({id:i,burst:1,arrival:i})); const r = fcfs(jobs); expect(r.order.length).toBe(100); });
});
