// Copyright (c) 2026 Nexara DMCC. All rights reserved.
import { createStore, appendEvent, appendEvents, getEventsForAggregate, getEventsOfType, getEventsAfterVersion, rebuild, rebuildAggregate, createSnapshot, rebuildFromSnapshot, makeEvent, sortByVersion, latestVersion, totalEvents, uniqueAggregates, eventTypes, createProjection, filterEventsByDateRange } from '../store';
import type { DomainEvent, EventStore, Reducer } from '../types';

const mkEvent = (id: string, type: string, aggId: string, version: number): DomainEvent => ({
  id, type, aggregateId: aggId, aggregateType: 'TestAgg', version, timestamp: 1000000 + version, payload: { v: version }
});
const counterReducer: Reducer<number> = (state, _event) => state + 1;

describe('createStore', () => {
  it('creates empty store 1', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 2', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 3', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 4', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 5', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 6', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 7', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 8', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 9', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 10', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 11', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 12', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 13', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 14', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 15', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 16', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 17', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 18', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 19', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 20', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 21', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 22', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 23', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 24', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 25', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 26', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 27', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 28', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 29', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 30', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 31', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 32', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 33', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 34', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 35', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 36', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 37', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 38', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 39', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 40', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 41', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 42', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 43', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 44', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 45', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 46', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 47', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 48', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 49', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('creates empty store 50', () => { const s = createStore(); expect(s.events.length).toBe(0); });
  it('empty store is immutable 1', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e1','T','a1',1)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 2', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e2','T','a2',2)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 3', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e3','T','a3',3)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 4', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e4','T','a4',4)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 5', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e5','T','a5',5)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 6', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e6','T','a6',6)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 7', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e7','T','a7',7)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 8', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e8','T','a8',8)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 9', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e9','T','a9',9)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 10', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e10','T','a10',10)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 11', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e11','T','a11',11)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 12', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e12','T','a12',12)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 13', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e13','T','a13',13)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 14', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e14','T','a14',14)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 15', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e15','T','a15',15)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 16', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e16','T','a16',16)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 17', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e17','T','a17',17)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 18', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e18','T','a18',18)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 19', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e19','T','a19',19)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 20', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e20','T','a20',20)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 21', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e21','T','a21',21)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 22', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e22','T','a22',22)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 23', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e23','T','a23',23)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 24', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e24','T','a24',24)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 25', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e25','T','a25',25)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 26', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e26','T','a26',26)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 27', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e27','T','a27',27)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 28', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e28','T','a28',28)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 29', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e29','T','a29',29)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 30', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e30','T','a30',30)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 31', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e31','T','a31',31)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 32', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e32','T','a32',32)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 33', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e33','T','a33',33)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 34', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e34','T','a34',34)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 35', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e35','T','a35',35)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 36', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e36','T','a36',36)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 37', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e37','T','a37',37)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 38', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e38','T','a38',38)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 39', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e39','T','a39',39)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 40', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e40','T','a40',40)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 41', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e41','T','a41',41)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 42', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e42','T','a42',42)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 43', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e43','T','a43',43)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 44', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e44','T','a44',44)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 45', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e45','T','a45',45)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 46', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e46','T','a46',46)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 47', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e47','T','a47',47)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 48', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e48','T','a48',48)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 49', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e49','T','a49',49)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
  it('empty store is immutable 50', () => { const s = createStore(); const s2 = appendEvent(s, mkEvent('e50','T','a50',50)); expect(s.events.length).toBe(0); expect(s2.events.length).toBe(1); });
});

describe('appendEvent', () => {
  it('append event 1', () => { const s = appendEvent(createStore(), mkEvent('e1','T','a',1)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(1); });
  it('append event 2', () => { const s = appendEvent(createStore(), mkEvent('e2','T','a',2)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(2); });
  it('append event 3', () => { const s = appendEvent(createStore(), mkEvent('e3','T','a',3)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(3); });
  it('append event 4', () => { const s = appendEvent(createStore(), mkEvent('e4','T','a',4)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(4); });
  it('append event 5', () => { const s = appendEvent(createStore(), mkEvent('e5','T','a',5)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(5); });
  it('append event 6', () => { const s = appendEvent(createStore(), mkEvent('e6','T','a',6)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(6); });
  it('append event 7', () => { const s = appendEvent(createStore(), mkEvent('e7','T','a',7)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(7); });
  it('append event 8', () => { const s = appendEvent(createStore(), mkEvent('e8','T','a',8)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(8); });
  it('append event 9', () => { const s = appendEvent(createStore(), mkEvent('e9','T','a',9)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(9); });
  it('append event 10', () => { const s = appendEvent(createStore(), mkEvent('e10','T','a',10)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(10); });
  it('append event 11', () => { const s = appendEvent(createStore(), mkEvent('e11','T','a',11)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(11); });
  it('append event 12', () => { const s = appendEvent(createStore(), mkEvent('e12','T','a',12)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(12); });
  it('append event 13', () => { const s = appendEvent(createStore(), mkEvent('e13','T','a',13)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(13); });
  it('append event 14', () => { const s = appendEvent(createStore(), mkEvent('e14','T','a',14)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(14); });
  it('append event 15', () => { const s = appendEvent(createStore(), mkEvent('e15','T','a',15)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(15); });
  it('append event 16', () => { const s = appendEvent(createStore(), mkEvent('e16','T','a',16)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(16); });
  it('append event 17', () => { const s = appendEvent(createStore(), mkEvent('e17','T','a',17)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(17); });
  it('append event 18', () => { const s = appendEvent(createStore(), mkEvent('e18','T','a',18)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(18); });
  it('append event 19', () => { const s = appendEvent(createStore(), mkEvent('e19','T','a',19)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(19); });
  it('append event 20', () => { const s = appendEvent(createStore(), mkEvent('e20','T','a',20)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(20); });
  it('append event 21', () => { const s = appendEvent(createStore(), mkEvent('e21','T','a',21)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(21); });
  it('append event 22', () => { const s = appendEvent(createStore(), mkEvent('e22','T','a',22)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(22); });
  it('append event 23', () => { const s = appendEvent(createStore(), mkEvent('e23','T','a',23)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(23); });
  it('append event 24', () => { const s = appendEvent(createStore(), mkEvent('e24','T','a',24)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(24); });
  it('append event 25', () => { const s = appendEvent(createStore(), mkEvent('e25','T','a',25)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(25); });
  it('append event 26', () => { const s = appendEvent(createStore(), mkEvent('e26','T','a',26)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(26); });
  it('append event 27', () => { const s = appendEvent(createStore(), mkEvent('e27','T','a',27)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(27); });
  it('append event 28', () => { const s = appendEvent(createStore(), mkEvent('e28','T','a',28)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(28); });
  it('append event 29', () => { const s = appendEvent(createStore(), mkEvent('e29','T','a',29)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(29); });
  it('append event 30', () => { const s = appendEvent(createStore(), mkEvent('e30','T','a',30)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(30); });
  it('append event 31', () => { const s = appendEvent(createStore(), mkEvent('e31','T','a',31)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(31); });
  it('append event 32', () => { const s = appendEvent(createStore(), mkEvent('e32','T','a',32)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(32); });
  it('append event 33', () => { const s = appendEvent(createStore(), mkEvent('e33','T','a',33)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(33); });
  it('append event 34', () => { const s = appendEvent(createStore(), mkEvent('e34','T','a',34)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(34); });
  it('append event 35', () => { const s = appendEvent(createStore(), mkEvent('e35','T','a',35)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(35); });
  it('append event 36', () => { const s = appendEvent(createStore(), mkEvent('e36','T','a',36)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(36); });
  it('append event 37', () => { const s = appendEvent(createStore(), mkEvent('e37','T','a',37)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(37); });
  it('append event 38', () => { const s = appendEvent(createStore(), mkEvent('e38','T','a',38)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(38); });
  it('append event 39', () => { const s = appendEvent(createStore(), mkEvent('e39','T','a',39)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(39); });
  it('append event 40', () => { const s = appendEvent(createStore(), mkEvent('e40','T','a',40)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(40); });
  it('append event 41', () => { const s = appendEvent(createStore(), mkEvent('e41','T','a',41)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(41); });
  it('append event 42', () => { const s = appendEvent(createStore(), mkEvent('e42','T','a',42)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(42); });
  it('append event 43', () => { const s = appendEvent(createStore(), mkEvent('e43','T','a',43)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(43); });
  it('append event 44', () => { const s = appendEvent(createStore(), mkEvent('e44','T','a',44)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(44); });
  it('append event 45', () => { const s = appendEvent(createStore(), mkEvent('e45','T','a',45)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(45); });
  it('append event 46', () => { const s = appendEvent(createStore(), mkEvent('e46','T','a',46)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(46); });
  it('append event 47', () => { const s = appendEvent(createStore(), mkEvent('e47','T','a',47)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(47); });
  it('append event 48', () => { const s = appendEvent(createStore(), mkEvent('e48','T','a',48)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(48); });
  it('append event 49', () => { const s = appendEvent(createStore(), mkEvent('e49','T','a',49)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(49); });
  it('append event 50', () => { const s = appendEvent(createStore(), mkEvent('e50','T','a',50)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(50); });
  it('append event 51', () => { const s = appendEvent(createStore(), mkEvent('e51','T','a',51)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(51); });
  it('append event 52', () => { const s = appendEvent(createStore(), mkEvent('e52','T','a',52)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(52); });
  it('append event 53', () => { const s = appendEvent(createStore(), mkEvent('e53','T','a',53)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(53); });
  it('append event 54', () => { const s = appendEvent(createStore(), mkEvent('e54','T','a',54)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(54); });
  it('append event 55', () => { const s = appendEvent(createStore(), mkEvent('e55','T','a',55)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(55); });
  it('append event 56', () => { const s = appendEvent(createStore(), mkEvent('e56','T','a',56)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(56); });
  it('append event 57', () => { const s = appendEvent(createStore(), mkEvent('e57','T','a',57)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(57); });
  it('append event 58', () => { const s = appendEvent(createStore(), mkEvent('e58','T','a',58)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(58); });
  it('append event 59', () => { const s = appendEvent(createStore(), mkEvent('e59','T','a',59)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(59); });
  it('append event 60', () => { const s = appendEvent(createStore(), mkEvent('e60','T','a',60)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(60); });
  it('append event 61', () => { const s = appendEvent(createStore(), mkEvent('e61','T','a',61)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(61); });
  it('append event 62', () => { const s = appendEvent(createStore(), mkEvent('e62','T','a',62)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(62); });
  it('append event 63', () => { const s = appendEvent(createStore(), mkEvent('e63','T','a',63)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(63); });
  it('append event 64', () => { const s = appendEvent(createStore(), mkEvent('e64','T','a',64)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(64); });
  it('append event 65', () => { const s = appendEvent(createStore(), mkEvent('e65','T','a',65)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(65); });
  it('append event 66', () => { const s = appendEvent(createStore(), mkEvent('e66','T','a',66)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(66); });
  it('append event 67', () => { const s = appendEvent(createStore(), mkEvent('e67','T','a',67)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(67); });
  it('append event 68', () => { const s = appendEvent(createStore(), mkEvent('e68','T','a',68)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(68); });
  it('append event 69', () => { const s = appendEvent(createStore(), mkEvent('e69','T','a',69)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(69); });
  it('append event 70', () => { const s = appendEvent(createStore(), mkEvent('e70','T','a',70)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(70); });
  it('append event 71', () => { const s = appendEvent(createStore(), mkEvent('e71','T','a',71)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(71); });
  it('append event 72', () => { const s = appendEvent(createStore(), mkEvent('e72','T','a',72)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(72); });
  it('append event 73', () => { const s = appendEvent(createStore(), mkEvent('e73','T','a',73)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(73); });
  it('append event 74', () => { const s = appendEvent(createStore(), mkEvent('e74','T','a',74)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(74); });
  it('append event 75', () => { const s = appendEvent(createStore(), mkEvent('e75','T','a',75)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(75); });
  it('append event 76', () => { const s = appendEvent(createStore(), mkEvent('e76','T','a',76)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(76); });
  it('append event 77', () => { const s = appendEvent(createStore(), mkEvent('e77','T','a',77)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(77); });
  it('append event 78', () => { const s = appendEvent(createStore(), mkEvent('e78','T','a',78)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(78); });
  it('append event 79', () => { const s = appendEvent(createStore(), mkEvent('e79','T','a',79)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(79); });
  it('append event 80', () => { const s = appendEvent(createStore(), mkEvent('e80','T','a',80)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(80); });
  it('append event 81', () => { const s = appendEvent(createStore(), mkEvent('e81','T','a',81)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(81); });
  it('append event 82', () => { const s = appendEvent(createStore(), mkEvent('e82','T','a',82)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(82); });
  it('append event 83', () => { const s = appendEvent(createStore(), mkEvent('e83','T','a',83)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(83); });
  it('append event 84', () => { const s = appendEvent(createStore(), mkEvent('e84','T','a',84)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(84); });
  it('append event 85', () => { const s = appendEvent(createStore(), mkEvent('e85','T','a',85)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(85); });
  it('append event 86', () => { const s = appendEvent(createStore(), mkEvent('e86','T','a',86)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(86); });
  it('append event 87', () => { const s = appendEvent(createStore(), mkEvent('e87','T','a',87)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(87); });
  it('append event 88', () => { const s = appendEvent(createStore(), mkEvent('e88','T','a',88)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(88); });
  it('append event 89', () => { const s = appendEvent(createStore(), mkEvent('e89','T','a',89)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(89); });
  it('append event 90', () => { const s = appendEvent(createStore(), mkEvent('e90','T','a',90)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(90); });
  it('append event 91', () => { const s = appendEvent(createStore(), mkEvent('e91','T','a',91)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(91); });
  it('append event 92', () => { const s = appendEvent(createStore(), mkEvent('e92','T','a',92)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(92); });
  it('append event 93', () => { const s = appendEvent(createStore(), mkEvent('e93','T','a',93)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(93); });
  it('append event 94', () => { const s = appendEvent(createStore(), mkEvent('e94','T','a',94)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(94); });
  it('append event 95', () => { const s = appendEvent(createStore(), mkEvent('e95','T','a',95)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(95); });
  it('append event 96', () => { const s = appendEvent(createStore(), mkEvent('e96','T','a',96)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(96); });
  it('append event 97', () => { const s = appendEvent(createStore(), mkEvent('e97','T','a',97)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(97); });
  it('append event 98', () => { const s = appendEvent(createStore(), mkEvent('e98','T','a',98)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(98); });
  it('append event 99', () => { const s = appendEvent(createStore(), mkEvent('e99','T','a',99)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(99); });
  it('append event 100', () => { const s = appendEvent(createStore(), mkEvent('e100','T','a',100)); expect(s.events.length).toBe(1); expect(s.events[0].version).toBe(100); });
});

describe('getEventsForAggregate', () => {
  it('filters by aggregateId 1', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg1',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg1').length).toBe(1); });
  it('filters by aggregateId 2', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg2',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg2').length).toBe(1); });
  it('filters by aggregateId 3', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg3',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg3').length).toBe(1); });
  it('filters by aggregateId 4', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg4',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg4').length).toBe(1); });
  it('filters by aggregateId 5', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg5',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg5').length).toBe(1); });
  it('filters by aggregateId 6', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg6',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg6').length).toBe(1); });
  it('filters by aggregateId 7', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg7',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg7').length).toBe(1); });
  it('filters by aggregateId 8', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg8',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg8').length).toBe(1); });
  it('filters by aggregateId 9', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg9',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg9').length).toBe(1); });
  it('filters by aggregateId 10', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg10',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg10').length).toBe(1); });
  it('filters by aggregateId 11', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg11',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg11').length).toBe(1); });
  it('filters by aggregateId 12', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg12',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg12').length).toBe(1); });
  it('filters by aggregateId 13', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg13',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg13').length).toBe(1); });
  it('filters by aggregateId 14', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg14',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg14').length).toBe(1); });
  it('filters by aggregateId 15', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg15',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg15').length).toBe(1); });
  it('filters by aggregateId 16', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg16',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg16').length).toBe(1); });
  it('filters by aggregateId 17', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg17',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg17').length).toBe(1); });
  it('filters by aggregateId 18', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg18',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg18').length).toBe(1); });
  it('filters by aggregateId 19', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg19',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg19').length).toBe(1); });
  it('filters by aggregateId 20', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg20',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg20').length).toBe(1); });
  it('filters by aggregateId 21', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg21',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg21').length).toBe(1); });
  it('filters by aggregateId 22', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg22',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg22').length).toBe(1); });
  it('filters by aggregateId 23', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg23',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg23').length).toBe(1); });
  it('filters by aggregateId 24', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg24',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg24').length).toBe(1); });
  it('filters by aggregateId 25', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg25',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg25').length).toBe(1); });
  it('filters by aggregateId 26', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg26',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg26').length).toBe(1); });
  it('filters by aggregateId 27', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg27',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg27').length).toBe(1); });
  it('filters by aggregateId 28', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg28',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg28').length).toBe(1); });
  it('filters by aggregateId 29', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg29',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg29').length).toBe(1); });
  it('filters by aggregateId 30', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg30',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg30').length).toBe(1); });
  it('filters by aggregateId 31', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg31',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg31').length).toBe(1); });
  it('filters by aggregateId 32', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg32',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg32').length).toBe(1); });
  it('filters by aggregateId 33', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg33',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg33').length).toBe(1); });
  it('filters by aggregateId 34', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg34',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg34').length).toBe(1); });
  it('filters by aggregateId 35', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg35',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg35').length).toBe(1); });
  it('filters by aggregateId 36', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg36',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg36').length).toBe(1); });
  it('filters by aggregateId 37', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg37',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg37').length).toBe(1); });
  it('filters by aggregateId 38', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg38',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg38').length).toBe(1); });
  it('filters by aggregateId 39', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg39',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg39').length).toBe(1); });
  it('filters by aggregateId 40', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg40',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg40').length).toBe(1); });
  it('filters by aggregateId 41', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg41',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg41').length).toBe(1); });
  it('filters by aggregateId 42', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg42',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg42').length).toBe(1); });
  it('filters by aggregateId 43', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg43',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg43').length).toBe(1); });
  it('filters by aggregateId 44', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg44',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg44').length).toBe(1); });
  it('filters by aggregateId 45', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg45',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg45').length).toBe(1); });
  it('filters by aggregateId 46', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg46',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg46').length).toBe(1); });
  it('filters by aggregateId 47', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg47',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg47').length).toBe(1); });
  it('filters by aggregateId 48', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg48',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg48').length).toBe(1); });
  it('filters by aggregateId 49', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg49',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg49').length).toBe(1); });
  it('filters by aggregateId 50', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','agg50',1)); s = appendEvent(s, mkEvent('e2','T','other',2)); expect(getEventsForAggregate(s,'agg50').length).toBe(1); });
  it('returns empty for unknown id 1', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown1').length).toBe(0); });
  it('returns empty for unknown id 2', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown2').length).toBe(0); });
  it('returns empty for unknown id 3', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown3').length).toBe(0); });
  it('returns empty for unknown id 4', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown4').length).toBe(0); });
  it('returns empty for unknown id 5', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown5').length).toBe(0); });
  it('returns empty for unknown id 6', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown6').length).toBe(0); });
  it('returns empty for unknown id 7', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown7').length).toBe(0); });
  it('returns empty for unknown id 8', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown8').length).toBe(0); });
  it('returns empty for unknown id 9', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown9').length).toBe(0); });
  it('returns empty for unknown id 10', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown10').length).toBe(0); });
  it('returns empty for unknown id 11', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown11').length).toBe(0); });
  it('returns empty for unknown id 12', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown12').length).toBe(0); });
  it('returns empty for unknown id 13', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown13').length).toBe(0); });
  it('returns empty for unknown id 14', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown14').length).toBe(0); });
  it('returns empty for unknown id 15', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown15').length).toBe(0); });
  it('returns empty for unknown id 16', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown16').length).toBe(0); });
  it('returns empty for unknown id 17', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown17').length).toBe(0); });
  it('returns empty for unknown id 18', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown18').length).toBe(0); });
  it('returns empty for unknown id 19', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown19').length).toBe(0); });
  it('returns empty for unknown id 20', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown20').length).toBe(0); });
  it('returns empty for unknown id 21', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown21').length).toBe(0); });
  it('returns empty for unknown id 22', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown22').length).toBe(0); });
  it('returns empty for unknown id 23', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown23').length).toBe(0); });
  it('returns empty for unknown id 24', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown24').length).toBe(0); });
  it('returns empty for unknown id 25', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown25').length).toBe(0); });
  it('returns empty for unknown id 26', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown26').length).toBe(0); });
  it('returns empty for unknown id 27', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown27').length).toBe(0); });
  it('returns empty for unknown id 28', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown28').length).toBe(0); });
  it('returns empty for unknown id 29', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown29').length).toBe(0); });
  it('returns empty for unknown id 30', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown30').length).toBe(0); });
  it('returns empty for unknown id 31', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown31').length).toBe(0); });
  it('returns empty for unknown id 32', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown32').length).toBe(0); });
  it('returns empty for unknown id 33', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown33').length).toBe(0); });
  it('returns empty for unknown id 34', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown34').length).toBe(0); });
  it('returns empty for unknown id 35', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown35').length).toBe(0); });
  it('returns empty for unknown id 36', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown36').length).toBe(0); });
  it('returns empty for unknown id 37', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown37').length).toBe(0); });
  it('returns empty for unknown id 38', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown38').length).toBe(0); });
  it('returns empty for unknown id 39', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown39').length).toBe(0); });
  it('returns empty for unknown id 40', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown40').length).toBe(0); });
  it('returns empty for unknown id 41', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown41').length).toBe(0); });
  it('returns empty for unknown id 42', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown42').length).toBe(0); });
  it('returns empty for unknown id 43', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown43').length).toBe(0); });
  it('returns empty for unknown id 44', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown44').length).toBe(0); });
  it('returns empty for unknown id 45', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown45').length).toBe(0); });
  it('returns empty for unknown id 46', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown46').length).toBe(0); });
  it('returns empty for unknown id 47', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown47').length).toBe(0); });
  it('returns empty for unknown id 48', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown48').length).toBe(0); });
  it('returns empty for unknown id 49', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown49').length).toBe(0); });
  it('returns empty for unknown id 50', () => { const s = createStore(); expect(getEventsForAggregate(s,'unknown50').length).toBe(0); });
});

describe('getEventsOfType', () => {
  it('filters by type 1', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_1','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_1').length).toBe(1); });
  it('filters by type 2', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_2','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_2').length).toBe(1); });
  it('filters by type 3', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_3','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_3').length).toBe(1); });
  it('filters by type 4', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_4','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_4').length).toBe(1); });
  it('filters by type 5', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_5','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_5').length).toBe(1); });
  it('filters by type 6', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_6','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_6').length).toBe(1); });
  it('filters by type 7', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_7','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_7').length).toBe(1); });
  it('filters by type 8', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_8','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_8').length).toBe(1); });
  it('filters by type 9', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_9','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_9').length).toBe(1); });
  it('filters by type 10', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_10','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_10').length).toBe(1); });
  it('filters by type 11', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_11','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_11').length).toBe(1); });
  it('filters by type 12', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_12','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_12').length).toBe(1); });
  it('filters by type 13', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_13','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_13').length).toBe(1); });
  it('filters by type 14', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_14','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_14').length).toBe(1); });
  it('filters by type 15', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_15','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_15').length).toBe(1); });
  it('filters by type 16', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_16','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_16').length).toBe(1); });
  it('filters by type 17', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_17','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_17').length).toBe(1); });
  it('filters by type 18', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_18','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_18').length).toBe(1); });
  it('filters by type 19', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_19','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_19').length).toBe(1); });
  it('filters by type 20', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_20','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_20').length).toBe(1); });
  it('filters by type 21', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_21','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_21').length).toBe(1); });
  it('filters by type 22', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_22','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_22').length).toBe(1); });
  it('filters by type 23', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_23','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_23').length).toBe(1); });
  it('filters by type 24', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_24','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_24').length).toBe(1); });
  it('filters by type 25', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_25','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_25').length).toBe(1); });
  it('filters by type 26', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_26','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_26').length).toBe(1); });
  it('filters by type 27', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_27','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_27').length).toBe(1); });
  it('filters by type 28', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_28','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_28').length).toBe(1); });
  it('filters by type 29', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_29','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_29').length).toBe(1); });
  it('filters by type 30', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_30','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_30').length).toBe(1); });
  it('filters by type 31', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_31','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_31').length).toBe(1); });
  it('filters by type 32', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_32','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_32').length).toBe(1); });
  it('filters by type 33', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_33','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_33').length).toBe(1); });
  it('filters by type 34', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_34','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_34').length).toBe(1); });
  it('filters by type 35', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_35','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_35').length).toBe(1); });
  it('filters by type 36', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_36','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_36').length).toBe(1); });
  it('filters by type 37', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_37','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_37').length).toBe(1); });
  it('filters by type 38', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_38','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_38').length).toBe(1); });
  it('filters by type 39', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_39','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_39').length).toBe(1); });
  it('filters by type 40', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_40','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_40').length).toBe(1); });
  it('filters by type 41', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_41','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_41').length).toBe(1); });
  it('filters by type 42', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_42','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_42').length).toBe(1); });
  it('filters by type 43', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_43','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_43').length).toBe(1); });
  it('filters by type 44', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_44','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_44').length).toBe(1); });
  it('filters by type 45', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_45','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_45').length).toBe(1); });
  it('filters by type 46', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_46','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_46').length).toBe(1); });
  it('filters by type 47', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_47','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_47').length).toBe(1); });
  it('filters by type 48', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_48','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_48').length).toBe(1); });
  it('filters by type 49', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_49','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_49').length).toBe(1); });
  it('filters by type 50', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','TYPE_50','a',1)); s = appendEvent(s, mkEvent('e2','OTHER','a',2)); expect(getEventsOfType(s,'TYPE_50').length).toBe(1); });
  it('empty for missing type 1', () => { expect(getEventsOfType(createStore(),'MISSING_1').length).toBe(0); });
  it('empty for missing type 2', () => { expect(getEventsOfType(createStore(),'MISSING_2').length).toBe(0); });
  it('empty for missing type 3', () => { expect(getEventsOfType(createStore(),'MISSING_3').length).toBe(0); });
  it('empty for missing type 4', () => { expect(getEventsOfType(createStore(),'MISSING_4').length).toBe(0); });
  it('empty for missing type 5', () => { expect(getEventsOfType(createStore(),'MISSING_5').length).toBe(0); });
  it('empty for missing type 6', () => { expect(getEventsOfType(createStore(),'MISSING_6').length).toBe(0); });
  it('empty for missing type 7', () => { expect(getEventsOfType(createStore(),'MISSING_7').length).toBe(0); });
  it('empty for missing type 8', () => { expect(getEventsOfType(createStore(),'MISSING_8').length).toBe(0); });
  it('empty for missing type 9', () => { expect(getEventsOfType(createStore(),'MISSING_9').length).toBe(0); });
  it('empty for missing type 10', () => { expect(getEventsOfType(createStore(),'MISSING_10').length).toBe(0); });
  it('empty for missing type 11', () => { expect(getEventsOfType(createStore(),'MISSING_11').length).toBe(0); });
  it('empty for missing type 12', () => { expect(getEventsOfType(createStore(),'MISSING_12').length).toBe(0); });
  it('empty for missing type 13', () => { expect(getEventsOfType(createStore(),'MISSING_13').length).toBe(0); });
  it('empty for missing type 14', () => { expect(getEventsOfType(createStore(),'MISSING_14').length).toBe(0); });
  it('empty for missing type 15', () => { expect(getEventsOfType(createStore(),'MISSING_15').length).toBe(0); });
  it('empty for missing type 16', () => { expect(getEventsOfType(createStore(),'MISSING_16').length).toBe(0); });
  it('empty for missing type 17', () => { expect(getEventsOfType(createStore(),'MISSING_17').length).toBe(0); });
  it('empty for missing type 18', () => { expect(getEventsOfType(createStore(),'MISSING_18').length).toBe(0); });
  it('empty for missing type 19', () => { expect(getEventsOfType(createStore(),'MISSING_19').length).toBe(0); });
  it('empty for missing type 20', () => { expect(getEventsOfType(createStore(),'MISSING_20').length).toBe(0); });
  it('empty for missing type 21', () => { expect(getEventsOfType(createStore(),'MISSING_21').length).toBe(0); });
  it('empty for missing type 22', () => { expect(getEventsOfType(createStore(),'MISSING_22').length).toBe(0); });
  it('empty for missing type 23', () => { expect(getEventsOfType(createStore(),'MISSING_23').length).toBe(0); });
  it('empty for missing type 24', () => { expect(getEventsOfType(createStore(),'MISSING_24').length).toBe(0); });
  it('empty for missing type 25', () => { expect(getEventsOfType(createStore(),'MISSING_25').length).toBe(0); });
  it('empty for missing type 26', () => { expect(getEventsOfType(createStore(),'MISSING_26').length).toBe(0); });
  it('empty for missing type 27', () => { expect(getEventsOfType(createStore(),'MISSING_27').length).toBe(0); });
  it('empty for missing type 28', () => { expect(getEventsOfType(createStore(),'MISSING_28').length).toBe(0); });
  it('empty for missing type 29', () => { expect(getEventsOfType(createStore(),'MISSING_29').length).toBe(0); });
  it('empty for missing type 30', () => { expect(getEventsOfType(createStore(),'MISSING_30').length).toBe(0); });
  it('empty for missing type 31', () => { expect(getEventsOfType(createStore(),'MISSING_31').length).toBe(0); });
  it('empty for missing type 32', () => { expect(getEventsOfType(createStore(),'MISSING_32').length).toBe(0); });
  it('empty for missing type 33', () => { expect(getEventsOfType(createStore(),'MISSING_33').length).toBe(0); });
  it('empty for missing type 34', () => { expect(getEventsOfType(createStore(),'MISSING_34').length).toBe(0); });
  it('empty for missing type 35', () => { expect(getEventsOfType(createStore(),'MISSING_35').length).toBe(0); });
  it('empty for missing type 36', () => { expect(getEventsOfType(createStore(),'MISSING_36').length).toBe(0); });
  it('empty for missing type 37', () => { expect(getEventsOfType(createStore(),'MISSING_37').length).toBe(0); });
  it('empty for missing type 38', () => { expect(getEventsOfType(createStore(),'MISSING_38').length).toBe(0); });
  it('empty for missing type 39', () => { expect(getEventsOfType(createStore(),'MISSING_39').length).toBe(0); });
  it('empty for missing type 40', () => { expect(getEventsOfType(createStore(),'MISSING_40').length).toBe(0); });
  it('empty for missing type 41', () => { expect(getEventsOfType(createStore(),'MISSING_41').length).toBe(0); });
  it('empty for missing type 42', () => { expect(getEventsOfType(createStore(),'MISSING_42').length).toBe(0); });
  it('empty for missing type 43', () => { expect(getEventsOfType(createStore(),'MISSING_43').length).toBe(0); });
  it('empty for missing type 44', () => { expect(getEventsOfType(createStore(),'MISSING_44').length).toBe(0); });
  it('empty for missing type 45', () => { expect(getEventsOfType(createStore(),'MISSING_45').length).toBe(0); });
  it('empty for missing type 46', () => { expect(getEventsOfType(createStore(),'MISSING_46').length).toBe(0); });
  it('empty for missing type 47', () => { expect(getEventsOfType(createStore(),'MISSING_47').length).toBe(0); });
  it('empty for missing type 48', () => { expect(getEventsOfType(createStore(),'MISSING_48').length).toBe(0); });
  it('empty for missing type 49', () => { expect(getEventsOfType(createStore(),'MISSING_49').length).toBe(0); });
  it('empty for missing type 50', () => { expect(getEventsOfType(createStore(),'MISSING_50').length).toBe(0); });
});

describe('rebuild', () => {
  it('rebuilds state 1', () => { const events = Array.from({length:1}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(1); });
  it('rebuilds state 2', () => { const events = Array.from({length:2}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(2); });
  it('rebuilds state 3', () => { const events = Array.from({length:3}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(3); });
  it('rebuilds state 4', () => { const events = Array.from({length:4}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(4); });
  it('rebuilds state 5', () => { const events = Array.from({length:5}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(5); });
  it('rebuilds state 6', () => { const events = Array.from({length:6}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(6); });
  it('rebuilds state 7', () => { const events = Array.from({length:7}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(7); });
  it('rebuilds state 8', () => { const events = Array.from({length:8}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(8); });
  it('rebuilds state 9', () => { const events = Array.from({length:9}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(9); });
  it('rebuilds state 10', () => { const events = Array.from({length:10}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(10); });
  it('rebuilds state 11', () => { const events = Array.from({length:11}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(11); });
  it('rebuilds state 12', () => { const events = Array.from({length:12}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(12); });
  it('rebuilds state 13', () => { const events = Array.from({length:13}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(13); });
  it('rebuilds state 14', () => { const events = Array.from({length:14}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(14); });
  it('rebuilds state 15', () => { const events = Array.from({length:15}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(15); });
  it('rebuilds state 16', () => { const events = Array.from({length:16}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(16); });
  it('rebuilds state 17', () => { const events = Array.from({length:17}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(17); });
  it('rebuilds state 18', () => { const events = Array.from({length:18}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(18); });
  it('rebuilds state 19', () => { const events = Array.from({length:19}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(19); });
  it('rebuilds state 20', () => { const events = Array.from({length:20}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(20); });
  it('rebuilds state 21', () => { const events = Array.from({length:21}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(21); });
  it('rebuilds state 22', () => { const events = Array.from({length:22}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(22); });
  it('rebuilds state 23', () => { const events = Array.from({length:23}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(23); });
  it('rebuilds state 24', () => { const events = Array.from({length:24}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(24); });
  it('rebuilds state 25', () => { const events = Array.from({length:25}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(25); });
  it('rebuilds state 26', () => { const events = Array.from({length:26}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(26); });
  it('rebuilds state 27', () => { const events = Array.from({length:27}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(27); });
  it('rebuilds state 28', () => { const events = Array.from({length:28}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(28); });
  it('rebuilds state 29', () => { const events = Array.from({length:29}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(29); });
  it('rebuilds state 30', () => { const events = Array.from({length:30}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(30); });
  it('rebuilds state 31', () => { const events = Array.from({length:31}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(31); });
  it('rebuilds state 32', () => { const events = Array.from({length:32}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(32); });
  it('rebuilds state 33', () => { const events = Array.from({length:33}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(33); });
  it('rebuilds state 34', () => { const events = Array.from({length:34}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(34); });
  it('rebuilds state 35', () => { const events = Array.from({length:35}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(35); });
  it('rebuilds state 36', () => { const events = Array.from({length:36}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(36); });
  it('rebuilds state 37', () => { const events = Array.from({length:37}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(37); });
  it('rebuilds state 38', () => { const events = Array.from({length:38}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(38); });
  it('rebuilds state 39', () => { const events = Array.from({length:39}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(39); });
  it('rebuilds state 40', () => { const events = Array.from({length:40}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(40); });
  it('rebuilds state 41', () => { const events = Array.from({length:41}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(41); });
  it('rebuilds state 42', () => { const events = Array.from({length:42}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(42); });
  it('rebuilds state 43', () => { const events = Array.from({length:43}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(43); });
  it('rebuilds state 44', () => { const events = Array.from({length:44}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(44); });
  it('rebuilds state 45', () => { const events = Array.from({length:45}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(45); });
  it('rebuilds state 46', () => { const events = Array.from({length:46}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(46); });
  it('rebuilds state 47', () => { const events = Array.from({length:47}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(47); });
  it('rebuilds state 48', () => { const events = Array.from({length:48}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(48); });
  it('rebuilds state 49', () => { const events = Array.from({length:49}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(49); });
  it('rebuilds state 50', () => { const events = Array.from({length:50}, (_,k) => mkEvent(`e${k}`,'T','a',k+1)); expect(rebuild(0, events, counterReducer)).toBe(50); });
  it('rebuild with 0 events 1', () => { expect(rebuild(1, [], counterReducer)).toBe(1); });
  it('rebuild with 0 events 2', () => { expect(rebuild(2, [], counterReducer)).toBe(2); });
  it('rebuild with 0 events 3', () => { expect(rebuild(3, [], counterReducer)).toBe(3); });
  it('rebuild with 0 events 4', () => { expect(rebuild(4, [], counterReducer)).toBe(4); });
  it('rebuild with 0 events 5', () => { expect(rebuild(5, [], counterReducer)).toBe(5); });
  it('rebuild with 0 events 6', () => { expect(rebuild(6, [], counterReducer)).toBe(6); });
  it('rebuild with 0 events 7', () => { expect(rebuild(7, [], counterReducer)).toBe(7); });
  it('rebuild with 0 events 8', () => { expect(rebuild(8, [], counterReducer)).toBe(8); });
  it('rebuild with 0 events 9', () => { expect(rebuild(9, [], counterReducer)).toBe(9); });
  it('rebuild with 0 events 10', () => { expect(rebuild(10, [], counterReducer)).toBe(10); });
  it('rebuild with 0 events 11', () => { expect(rebuild(11, [], counterReducer)).toBe(11); });
  it('rebuild with 0 events 12', () => { expect(rebuild(12, [], counterReducer)).toBe(12); });
  it('rebuild with 0 events 13', () => { expect(rebuild(13, [], counterReducer)).toBe(13); });
  it('rebuild with 0 events 14', () => { expect(rebuild(14, [], counterReducer)).toBe(14); });
  it('rebuild with 0 events 15', () => { expect(rebuild(15, [], counterReducer)).toBe(15); });
  it('rebuild with 0 events 16', () => { expect(rebuild(16, [], counterReducer)).toBe(16); });
  it('rebuild with 0 events 17', () => { expect(rebuild(17, [], counterReducer)).toBe(17); });
  it('rebuild with 0 events 18', () => { expect(rebuild(18, [], counterReducer)).toBe(18); });
  it('rebuild with 0 events 19', () => { expect(rebuild(19, [], counterReducer)).toBe(19); });
  it('rebuild with 0 events 20', () => { expect(rebuild(20, [], counterReducer)).toBe(20); });
  it('rebuild with 0 events 21', () => { expect(rebuild(21, [], counterReducer)).toBe(21); });
  it('rebuild with 0 events 22', () => { expect(rebuild(22, [], counterReducer)).toBe(22); });
  it('rebuild with 0 events 23', () => { expect(rebuild(23, [], counterReducer)).toBe(23); });
  it('rebuild with 0 events 24', () => { expect(rebuild(24, [], counterReducer)).toBe(24); });
  it('rebuild with 0 events 25', () => { expect(rebuild(25, [], counterReducer)).toBe(25); });
  it('rebuild with 0 events 26', () => { expect(rebuild(26, [], counterReducer)).toBe(26); });
  it('rebuild with 0 events 27', () => { expect(rebuild(27, [], counterReducer)).toBe(27); });
  it('rebuild with 0 events 28', () => { expect(rebuild(28, [], counterReducer)).toBe(28); });
  it('rebuild with 0 events 29', () => { expect(rebuild(29, [], counterReducer)).toBe(29); });
  it('rebuild with 0 events 30', () => { expect(rebuild(30, [], counterReducer)).toBe(30); });
  it('rebuild with 0 events 31', () => { expect(rebuild(31, [], counterReducer)).toBe(31); });
  it('rebuild with 0 events 32', () => { expect(rebuild(32, [], counterReducer)).toBe(32); });
  it('rebuild with 0 events 33', () => { expect(rebuild(33, [], counterReducer)).toBe(33); });
  it('rebuild with 0 events 34', () => { expect(rebuild(34, [], counterReducer)).toBe(34); });
  it('rebuild with 0 events 35', () => { expect(rebuild(35, [], counterReducer)).toBe(35); });
  it('rebuild with 0 events 36', () => { expect(rebuild(36, [], counterReducer)).toBe(36); });
  it('rebuild with 0 events 37', () => { expect(rebuild(37, [], counterReducer)).toBe(37); });
  it('rebuild with 0 events 38', () => { expect(rebuild(38, [], counterReducer)).toBe(38); });
  it('rebuild with 0 events 39', () => { expect(rebuild(39, [], counterReducer)).toBe(39); });
  it('rebuild with 0 events 40', () => { expect(rebuild(40, [], counterReducer)).toBe(40); });
  it('rebuild with 0 events 41', () => { expect(rebuild(41, [], counterReducer)).toBe(41); });
  it('rebuild with 0 events 42', () => { expect(rebuild(42, [], counterReducer)).toBe(42); });
  it('rebuild with 0 events 43', () => { expect(rebuild(43, [], counterReducer)).toBe(43); });
  it('rebuild with 0 events 44', () => { expect(rebuild(44, [], counterReducer)).toBe(44); });
  it('rebuild with 0 events 45', () => { expect(rebuild(45, [], counterReducer)).toBe(45); });
  it('rebuild with 0 events 46', () => { expect(rebuild(46, [], counterReducer)).toBe(46); });
  it('rebuild with 0 events 47', () => { expect(rebuild(47, [], counterReducer)).toBe(47); });
  it('rebuild with 0 events 48', () => { expect(rebuild(48, [], counterReducer)).toBe(48); });
  it('rebuild with 0 events 49', () => { expect(rebuild(49, [], counterReducer)).toBe(49); });
  it('rebuild with 0 events 50', () => { expect(rebuild(50, [], counterReducer)).toBe(50); });
});

describe('makeEvent and sortByVersion', () => {
  it('makeEvent version 1', () => { const e = makeEvent('id1','T','agg1','Type',1,{}); expect(e.version).toBe(1); expect(e.aggregateId).toBe('agg1'); });
  it('makeEvent version 2', () => { const e = makeEvent('id2','T','agg2','Type',2,{}); expect(e.version).toBe(2); expect(e.aggregateId).toBe('agg2'); });
  it('makeEvent version 3', () => { const e = makeEvent('id3','T','agg3','Type',3,{}); expect(e.version).toBe(3); expect(e.aggregateId).toBe('agg3'); });
  it('makeEvent version 4', () => { const e = makeEvent('id4','T','agg4','Type',4,{}); expect(e.version).toBe(4); expect(e.aggregateId).toBe('agg4'); });
  it('makeEvent version 5', () => { const e = makeEvent('id5','T','agg5','Type',5,{}); expect(e.version).toBe(5); expect(e.aggregateId).toBe('agg5'); });
  it('makeEvent version 6', () => { const e = makeEvent('id6','T','agg6','Type',6,{}); expect(e.version).toBe(6); expect(e.aggregateId).toBe('agg6'); });
  it('makeEvent version 7', () => { const e = makeEvent('id7','T','agg7','Type',7,{}); expect(e.version).toBe(7); expect(e.aggregateId).toBe('agg7'); });
  it('makeEvent version 8', () => { const e = makeEvent('id8','T','agg8','Type',8,{}); expect(e.version).toBe(8); expect(e.aggregateId).toBe('agg8'); });
  it('makeEvent version 9', () => { const e = makeEvent('id9','T','agg9','Type',9,{}); expect(e.version).toBe(9); expect(e.aggregateId).toBe('agg9'); });
  it('makeEvent version 10', () => { const e = makeEvent('id10','T','agg10','Type',10,{}); expect(e.version).toBe(10); expect(e.aggregateId).toBe('agg10'); });
  it('makeEvent version 11', () => { const e = makeEvent('id11','T','agg11','Type',11,{}); expect(e.version).toBe(11); expect(e.aggregateId).toBe('agg11'); });
  it('makeEvent version 12', () => { const e = makeEvent('id12','T','agg12','Type',12,{}); expect(e.version).toBe(12); expect(e.aggregateId).toBe('agg12'); });
  it('makeEvent version 13', () => { const e = makeEvent('id13','T','agg13','Type',13,{}); expect(e.version).toBe(13); expect(e.aggregateId).toBe('agg13'); });
  it('makeEvent version 14', () => { const e = makeEvent('id14','T','agg14','Type',14,{}); expect(e.version).toBe(14); expect(e.aggregateId).toBe('agg14'); });
  it('makeEvent version 15', () => { const e = makeEvent('id15','T','agg15','Type',15,{}); expect(e.version).toBe(15); expect(e.aggregateId).toBe('agg15'); });
  it('makeEvent version 16', () => { const e = makeEvent('id16','T','agg16','Type',16,{}); expect(e.version).toBe(16); expect(e.aggregateId).toBe('agg16'); });
  it('makeEvent version 17', () => { const e = makeEvent('id17','T','agg17','Type',17,{}); expect(e.version).toBe(17); expect(e.aggregateId).toBe('agg17'); });
  it('makeEvent version 18', () => { const e = makeEvent('id18','T','agg18','Type',18,{}); expect(e.version).toBe(18); expect(e.aggregateId).toBe('agg18'); });
  it('makeEvent version 19', () => { const e = makeEvent('id19','T','agg19','Type',19,{}); expect(e.version).toBe(19); expect(e.aggregateId).toBe('agg19'); });
  it('makeEvent version 20', () => { const e = makeEvent('id20','T','agg20','Type',20,{}); expect(e.version).toBe(20); expect(e.aggregateId).toBe('agg20'); });
  it('makeEvent version 21', () => { const e = makeEvent('id21','T','agg21','Type',21,{}); expect(e.version).toBe(21); expect(e.aggregateId).toBe('agg21'); });
  it('makeEvent version 22', () => { const e = makeEvent('id22','T','agg22','Type',22,{}); expect(e.version).toBe(22); expect(e.aggregateId).toBe('agg22'); });
  it('makeEvent version 23', () => { const e = makeEvent('id23','T','agg23','Type',23,{}); expect(e.version).toBe(23); expect(e.aggregateId).toBe('agg23'); });
  it('makeEvent version 24', () => { const e = makeEvent('id24','T','agg24','Type',24,{}); expect(e.version).toBe(24); expect(e.aggregateId).toBe('agg24'); });
  it('makeEvent version 25', () => { const e = makeEvent('id25','T','agg25','Type',25,{}); expect(e.version).toBe(25); expect(e.aggregateId).toBe('agg25'); });
  it('makeEvent version 26', () => { const e = makeEvent('id26','T','agg26','Type',26,{}); expect(e.version).toBe(26); expect(e.aggregateId).toBe('agg26'); });
  it('makeEvent version 27', () => { const e = makeEvent('id27','T','agg27','Type',27,{}); expect(e.version).toBe(27); expect(e.aggregateId).toBe('agg27'); });
  it('makeEvent version 28', () => { const e = makeEvent('id28','T','agg28','Type',28,{}); expect(e.version).toBe(28); expect(e.aggregateId).toBe('agg28'); });
  it('makeEvent version 29', () => { const e = makeEvent('id29','T','agg29','Type',29,{}); expect(e.version).toBe(29); expect(e.aggregateId).toBe('agg29'); });
  it('makeEvent version 30', () => { const e = makeEvent('id30','T','agg30','Type',30,{}); expect(e.version).toBe(30); expect(e.aggregateId).toBe('agg30'); });
  it('makeEvent version 31', () => { const e = makeEvent('id31','T','agg31','Type',31,{}); expect(e.version).toBe(31); expect(e.aggregateId).toBe('agg31'); });
  it('makeEvent version 32', () => { const e = makeEvent('id32','T','agg32','Type',32,{}); expect(e.version).toBe(32); expect(e.aggregateId).toBe('agg32'); });
  it('makeEvent version 33', () => { const e = makeEvent('id33','T','agg33','Type',33,{}); expect(e.version).toBe(33); expect(e.aggregateId).toBe('agg33'); });
  it('makeEvent version 34', () => { const e = makeEvent('id34','T','agg34','Type',34,{}); expect(e.version).toBe(34); expect(e.aggregateId).toBe('agg34'); });
  it('makeEvent version 35', () => { const e = makeEvent('id35','T','agg35','Type',35,{}); expect(e.version).toBe(35); expect(e.aggregateId).toBe('agg35'); });
  it('makeEvent version 36', () => { const e = makeEvent('id36','T','agg36','Type',36,{}); expect(e.version).toBe(36); expect(e.aggregateId).toBe('agg36'); });
  it('makeEvent version 37', () => { const e = makeEvent('id37','T','agg37','Type',37,{}); expect(e.version).toBe(37); expect(e.aggregateId).toBe('agg37'); });
  it('makeEvent version 38', () => { const e = makeEvent('id38','T','agg38','Type',38,{}); expect(e.version).toBe(38); expect(e.aggregateId).toBe('agg38'); });
  it('makeEvent version 39', () => { const e = makeEvent('id39','T','agg39','Type',39,{}); expect(e.version).toBe(39); expect(e.aggregateId).toBe('agg39'); });
  it('makeEvent version 40', () => { const e = makeEvent('id40','T','agg40','Type',40,{}); expect(e.version).toBe(40); expect(e.aggregateId).toBe('agg40'); });
  it('makeEvent version 41', () => { const e = makeEvent('id41','T','agg41','Type',41,{}); expect(e.version).toBe(41); expect(e.aggregateId).toBe('agg41'); });
  it('makeEvent version 42', () => { const e = makeEvent('id42','T','agg42','Type',42,{}); expect(e.version).toBe(42); expect(e.aggregateId).toBe('agg42'); });
  it('makeEvent version 43', () => { const e = makeEvent('id43','T','agg43','Type',43,{}); expect(e.version).toBe(43); expect(e.aggregateId).toBe('agg43'); });
  it('makeEvent version 44', () => { const e = makeEvent('id44','T','agg44','Type',44,{}); expect(e.version).toBe(44); expect(e.aggregateId).toBe('agg44'); });
  it('makeEvent version 45', () => { const e = makeEvent('id45','T','agg45','Type',45,{}); expect(e.version).toBe(45); expect(e.aggregateId).toBe('agg45'); });
  it('makeEvent version 46', () => { const e = makeEvent('id46','T','agg46','Type',46,{}); expect(e.version).toBe(46); expect(e.aggregateId).toBe('agg46'); });
  it('makeEvent version 47', () => { const e = makeEvent('id47','T','agg47','Type',47,{}); expect(e.version).toBe(47); expect(e.aggregateId).toBe('agg47'); });
  it('makeEvent version 48', () => { const e = makeEvent('id48','T','agg48','Type',48,{}); expect(e.version).toBe(48); expect(e.aggregateId).toBe('agg48'); });
  it('makeEvent version 49', () => { const e = makeEvent('id49','T','agg49','Type',49,{}); expect(e.version).toBe(49); expect(e.aggregateId).toBe('agg49'); });
  it('makeEvent version 50', () => { const e = makeEvent('id50','T','agg50','Type',50,{}); expect(e.version).toBe(50); expect(e.aggregateId).toBe('agg50'); });
  it('sortByVersion sorts correctly', () => {
    const events = [mkEvent('e3','T','a',3), mkEvent('e1','T','a',1), mkEvent('e2','T','a',2)];
    const sorted = sortByVersion(events);
    expect(sorted.map(e => e.version)).toEqual([1,2,3]);
  });
  it('latestVersion 1', () => { let s = createStore(); for(let k=1;k<=1;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(1); });
  it('latestVersion 2', () => { let s = createStore(); for(let k=1;k<=2;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(2); });
  it('latestVersion 3', () => { let s = createStore(); for(let k=1;k<=3;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(3); });
  it('latestVersion 4', () => { let s = createStore(); for(let k=1;k<=4;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(4); });
  it('latestVersion 5', () => { let s = createStore(); for(let k=1;k<=5;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(5); });
  it('latestVersion 6', () => { let s = createStore(); for(let k=1;k<=6;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(6); });
  it('latestVersion 7', () => { let s = createStore(); for(let k=1;k<=7;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(7); });
  it('latestVersion 8', () => { let s = createStore(); for(let k=1;k<=8;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(8); });
  it('latestVersion 9', () => { let s = createStore(); for(let k=1;k<=9;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(9); });
  it('latestVersion 10', () => { let s = createStore(); for(let k=1;k<=10;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(10); });
  it('latestVersion 11', () => { let s = createStore(); for(let k=1;k<=11;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(11); });
  it('latestVersion 12', () => { let s = createStore(); for(let k=1;k<=12;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(12); });
  it('latestVersion 13', () => { let s = createStore(); for(let k=1;k<=13;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(13); });
  it('latestVersion 14', () => { let s = createStore(); for(let k=1;k<=14;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(14); });
  it('latestVersion 15', () => { let s = createStore(); for(let k=1;k<=15;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(15); });
  it('latestVersion 16', () => { let s = createStore(); for(let k=1;k<=16;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(16); });
  it('latestVersion 17', () => { let s = createStore(); for(let k=1;k<=17;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(17); });
  it('latestVersion 18', () => { let s = createStore(); for(let k=1;k<=18;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(18); });
  it('latestVersion 19', () => { let s = createStore(); for(let k=1;k<=19;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(19); });
  it('latestVersion 20', () => { let s = createStore(); for(let k=1;k<=20;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(20); });
  it('latestVersion 21', () => { let s = createStore(); for(let k=1;k<=21;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(21); });
  it('latestVersion 22', () => { let s = createStore(); for(let k=1;k<=22;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(22); });
  it('latestVersion 23', () => { let s = createStore(); for(let k=1;k<=23;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(23); });
  it('latestVersion 24', () => { let s = createStore(); for(let k=1;k<=24;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(24); });
  it('latestVersion 25', () => { let s = createStore(); for(let k=1;k<=25;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(25); });
  it('latestVersion 26', () => { let s = createStore(); for(let k=1;k<=26;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(26); });
  it('latestVersion 27', () => { let s = createStore(); for(let k=1;k<=27;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(27); });
  it('latestVersion 28', () => { let s = createStore(); for(let k=1;k<=28;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(28); });
  it('latestVersion 29', () => { let s = createStore(); for(let k=1;k<=29;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(29); });
  it('latestVersion 30', () => { let s = createStore(); for(let k=1;k<=30;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(30); });
  it('latestVersion 31', () => { let s = createStore(); for(let k=1;k<=31;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(31); });
  it('latestVersion 32', () => { let s = createStore(); for(let k=1;k<=32;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(32); });
  it('latestVersion 33', () => { let s = createStore(); for(let k=1;k<=33;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(33); });
  it('latestVersion 34', () => { let s = createStore(); for(let k=1;k<=34;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(34); });
  it('latestVersion 35', () => { let s = createStore(); for(let k=1;k<=35;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(35); });
  it('latestVersion 36', () => { let s = createStore(); for(let k=1;k<=36;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(36); });
  it('latestVersion 37', () => { let s = createStore(); for(let k=1;k<=37;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(37); });
  it('latestVersion 38', () => { let s = createStore(); for(let k=1;k<=38;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(38); });
  it('latestVersion 39', () => { let s = createStore(); for(let k=1;k<=39;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(39); });
  it('latestVersion 40', () => { let s = createStore(); for(let k=1;k<=40;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(40); });
  it('latestVersion 41', () => { let s = createStore(); for(let k=1;k<=41;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(41); });
  it('latestVersion 42', () => { let s = createStore(); for(let k=1;k<=42;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(42); });
  it('latestVersion 43', () => { let s = createStore(); for(let k=1;k<=43;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(43); });
  it('latestVersion 44', () => { let s = createStore(); for(let k=1;k<=44;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(44); });
  it('latestVersion 45', () => { let s = createStore(); for(let k=1;k<=45;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(45); });
  it('latestVersion 46', () => { let s = createStore(); for(let k=1;k<=46;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(46); });
  it('latestVersion 47', () => { let s = createStore(); for(let k=1;k<=47;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(47); });
  it('latestVersion 48', () => { let s = createStore(); for(let k=1;k<=48;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(latestVersion(s,'a')).toBe(48); });
});

describe('store queries', () => {
  it('totalEvents 1', () => { let s = createStore(); for(let k=0;k<1;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(1); });
  it('totalEvents 2', () => { let s = createStore(); for(let k=0;k<2;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(2); });
  it('totalEvents 3', () => { let s = createStore(); for(let k=0;k<3;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(3); });
  it('totalEvents 4', () => { let s = createStore(); for(let k=0;k<4;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(4); });
  it('totalEvents 5', () => { let s = createStore(); for(let k=0;k<5;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(5); });
  it('totalEvents 6', () => { let s = createStore(); for(let k=0;k<6;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(6); });
  it('totalEvents 7', () => { let s = createStore(); for(let k=0;k<7;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(7); });
  it('totalEvents 8', () => { let s = createStore(); for(let k=0;k<8;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(8); });
  it('totalEvents 9', () => { let s = createStore(); for(let k=0;k<9;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(9); });
  it('totalEvents 10', () => { let s = createStore(); for(let k=0;k<10;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(10); });
  it('totalEvents 11', () => { let s = createStore(); for(let k=0;k<11;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(11); });
  it('totalEvents 12', () => { let s = createStore(); for(let k=0;k<12;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(12); });
  it('totalEvents 13', () => { let s = createStore(); for(let k=0;k<13;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(13); });
  it('totalEvents 14', () => { let s = createStore(); for(let k=0;k<14;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(14); });
  it('totalEvents 15', () => { let s = createStore(); for(let k=0;k<15;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(15); });
  it('totalEvents 16', () => { let s = createStore(); for(let k=0;k<16;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(16); });
  it('totalEvents 17', () => { let s = createStore(); for(let k=0;k<17;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(17); });
  it('totalEvents 18', () => { let s = createStore(); for(let k=0;k<18;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(18); });
  it('totalEvents 19', () => { let s = createStore(); for(let k=0;k<19;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(19); });
  it('totalEvents 20', () => { let s = createStore(); for(let k=0;k<20;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(20); });
  it('totalEvents 21', () => { let s = createStore(); for(let k=0;k<21;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(21); });
  it('totalEvents 22', () => { let s = createStore(); for(let k=0;k<22;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(22); });
  it('totalEvents 23', () => { let s = createStore(); for(let k=0;k<23;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(23); });
  it('totalEvents 24', () => { let s = createStore(); for(let k=0;k<24;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(24); });
  it('totalEvents 25', () => { let s = createStore(); for(let k=0;k<25;k++) s = appendEvent(s, mkEvent(`e${k}`,'T','a',k)); expect(totalEvents(s)).toBe(25); });
  it('uniqueAggregates 1 distinct', () => { let s = createStore(); for(let k=0;k<1;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(1); });
  it('uniqueAggregates 2 distinct', () => { let s = createStore(); for(let k=0;k<2;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(2); });
  it('uniqueAggregates 3 distinct', () => { let s = createStore(); for(let k=0;k<3;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(3); });
  it('uniqueAggregates 4 distinct', () => { let s = createStore(); for(let k=0;k<4;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(4); });
  it('uniqueAggregates 5 distinct', () => { let s = createStore(); for(let k=0;k<5;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(5); });
  it('uniqueAggregates 6 distinct', () => { let s = createStore(); for(let k=0;k<6;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(6); });
  it('uniqueAggregates 7 distinct', () => { let s = createStore(); for(let k=0;k<7;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(7); });
  it('uniqueAggregates 8 distinct', () => { let s = createStore(); for(let k=0;k<8;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(8); });
  it('uniqueAggregates 9 distinct', () => { let s = createStore(); for(let k=0;k<9;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(9); });
  it('uniqueAggregates 10 distinct', () => { let s = createStore(); for(let k=0;k<10;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(10); });
  it('uniqueAggregates 11 distinct', () => { let s = createStore(); for(let k=0;k<11;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(11); });
  it('uniqueAggregates 12 distinct', () => { let s = createStore(); for(let k=0;k<12;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(12); });
  it('uniqueAggregates 13 distinct', () => { let s = createStore(); for(let k=0;k<13;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(13); });
  it('uniqueAggregates 14 distinct', () => { let s = createStore(); for(let k=0;k<14;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(14); });
  it('uniqueAggregates 15 distinct', () => { let s = createStore(); for(let k=0;k<15;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(15); });
  it('uniqueAggregates 16 distinct', () => { let s = createStore(); for(let k=0;k<16;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(16); });
  it('uniqueAggregates 17 distinct', () => { let s = createStore(); for(let k=0;k<17;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(17); });
  it('uniqueAggregates 18 distinct', () => { let s = createStore(); for(let k=0;k<18;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(18); });
  it('uniqueAggregates 19 distinct', () => { let s = createStore(); for(let k=0;k<19;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(19); });
  it('uniqueAggregates 20 distinct', () => { let s = createStore(); for(let k=0;k<20;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(20); });
  it('uniqueAggregates 21 distinct', () => { let s = createStore(); for(let k=0;k<21;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(21); });
  it('uniqueAggregates 22 distinct', () => { let s = createStore(); for(let k=0;k<22;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(22); });
  it('uniqueAggregates 23 distinct', () => { let s = createStore(); for(let k=0;k<23;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(23); });
  it('uniqueAggregates 24 distinct', () => { let s = createStore(); for(let k=0;k<24;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(24); });
  it('uniqueAggregates 25 distinct', () => { let s = createStore(); for(let k=0;k<25;k++) s = appendEvent(s, mkEvent(`e${k}`,'T',`agg${k}`,k)); expect(uniqueAggregates(s).length).toBe(25); });
  it('eventTypes 1 distinct', () => { let s = createStore(); for(let k=0;k<1;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(1); });
  it('eventTypes 2 distinct', () => { let s = createStore(); for(let k=0;k<2;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(2); });
  it('eventTypes 3 distinct', () => { let s = createStore(); for(let k=0;k<3;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(3); });
  it('eventTypes 4 distinct', () => { let s = createStore(); for(let k=0;k<4;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(4); });
  it('eventTypes 5 distinct', () => { let s = createStore(); for(let k=0;k<5;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(5); });
  it('eventTypes 6 distinct', () => { let s = createStore(); for(let k=0;k<6;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(6); });
  it('eventTypes 7 distinct', () => { let s = createStore(); for(let k=0;k<7;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(7); });
  it('eventTypes 8 distinct', () => { let s = createStore(); for(let k=0;k<8;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(8); });
  it('eventTypes 9 distinct', () => { let s = createStore(); for(let k=0;k<9;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(9); });
  it('eventTypes 10 distinct', () => { let s = createStore(); for(let k=0;k<10;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(10); });
  it('eventTypes 11 distinct', () => { let s = createStore(); for(let k=0;k<11;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(11); });
  it('eventTypes 12 distinct', () => { let s = createStore(); for(let k=0;k<12;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(12); });
  it('eventTypes 13 distinct', () => { let s = createStore(); for(let k=0;k<13;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(13); });
  it('eventTypes 14 distinct', () => { let s = createStore(); for(let k=0;k<14;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(14); });
  it('eventTypes 15 distinct', () => { let s = createStore(); for(let k=0;k<15;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(15); });
  it('eventTypes 16 distinct', () => { let s = createStore(); for(let k=0;k<16;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(16); });
  it('eventTypes 17 distinct', () => { let s = createStore(); for(let k=0;k<17;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(17); });
  it('eventTypes 18 distinct', () => { let s = createStore(); for(let k=0;k<18;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(18); });
  it('eventTypes 19 distinct', () => { let s = createStore(); for(let k=0;k<19;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(19); });
  it('eventTypes 20 distinct', () => { let s = createStore(); for(let k=0;k<20;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(20); });
  it('eventTypes 21 distinct', () => { let s = createStore(); for(let k=0;k<21;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(21); });
  it('eventTypes 22 distinct', () => { let s = createStore(); for(let k=0;k<22;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(22); });
  it('eventTypes 23 distinct', () => { let s = createStore(); for(let k=0;k<23;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(23); });
  it('eventTypes 24 distinct', () => { let s = createStore(); for(let k=0;k<24;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(24); });
  it('eventTypes 25 distinct', () => { let s = createStore(); for(let k=0;k<25;k++) s = appendEvent(s, mkEvent(`e${k}`,`T${k}`,'a',k)); expect(eventTypes(s).length).toBe(25); });
  it('filterEventsByDateRange 1', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',1)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 2', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',2)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 3', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',3)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 4', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',4)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 5', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',5)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 6', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',6)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 7', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',7)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 8', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',8)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 9', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',9)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 10', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',10)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 11', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',11)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 12', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',12)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 13', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',13)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 14', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',14)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 15', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',15)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 16', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',16)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 17', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',17)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 18', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',18)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 19', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',19)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 20', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',20)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 21', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',21)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 22', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',22)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 23', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',23)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 24', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',24)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
  it('filterEventsByDateRange 25', () => { let s = createStore(); s = appendEvent(s, mkEvent('e1','T','a',25)); const filtered = filterEventsByDateRange(s, 0, 9999999); expect(filtered.length).toBe(1); });
});

describe('snapshot', () => {
  it('createSnapshot version 1', () => { const snap = createSnapshot('a','T',1,{'state':true}); expect(snap.version).toBe(1); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 2', () => { const snap = createSnapshot('a','T',2,{'state':true}); expect(snap.version).toBe(2); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 3', () => { const snap = createSnapshot('a','T',3,{'state':true}); expect(snap.version).toBe(3); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 4', () => { const snap = createSnapshot('a','T',4,{'state':true}); expect(snap.version).toBe(4); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 5', () => { const snap = createSnapshot('a','T',5,{'state':true}); expect(snap.version).toBe(5); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 6', () => { const snap = createSnapshot('a','T',6,{'state':true}); expect(snap.version).toBe(6); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 7', () => { const snap = createSnapshot('a','T',7,{'state':true}); expect(snap.version).toBe(7); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 8', () => { const snap = createSnapshot('a','T',8,{'state':true}); expect(snap.version).toBe(8); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 9', () => { const snap = createSnapshot('a','T',9,{'state':true}); expect(snap.version).toBe(9); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 10', () => { const snap = createSnapshot('a','T',10,{'state':true}); expect(snap.version).toBe(10); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 11', () => { const snap = createSnapshot('a','T',11,{'state':true}); expect(snap.version).toBe(11); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 12', () => { const snap = createSnapshot('a','T',12,{'state':true}); expect(snap.version).toBe(12); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 13', () => { const snap = createSnapshot('a','T',13,{'state':true}); expect(snap.version).toBe(13); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 14', () => { const snap = createSnapshot('a','T',14,{'state':true}); expect(snap.version).toBe(14); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 15', () => { const snap = createSnapshot('a','T',15,{'state':true}); expect(snap.version).toBe(15); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 16', () => { const snap = createSnapshot('a','T',16,{'state':true}); expect(snap.version).toBe(16); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 17', () => { const snap = createSnapshot('a','T',17,{'state':true}); expect(snap.version).toBe(17); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 18', () => { const snap = createSnapshot('a','T',18,{'state':true}); expect(snap.version).toBe(18); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 19', () => { const snap = createSnapshot('a','T',19,{'state':true}); expect(snap.version).toBe(19); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 20', () => { const snap = createSnapshot('a','T',20,{'state':true}); expect(snap.version).toBe(20); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 21', () => { const snap = createSnapshot('a','T',21,{'state':true}); expect(snap.version).toBe(21); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 22', () => { const snap = createSnapshot('a','T',22,{'state':true}); expect(snap.version).toBe(22); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 23', () => { const snap = createSnapshot('a','T',23,{'state':true}); expect(snap.version).toBe(23); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 24', () => { const snap = createSnapshot('a','T',24,{'state':true}); expect(snap.version).toBe(24); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 25', () => { const snap = createSnapshot('a','T',25,{'state':true}); expect(snap.version).toBe(25); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 26', () => { const snap = createSnapshot('a','T',26,{'state':true}); expect(snap.version).toBe(26); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 27', () => { const snap = createSnapshot('a','T',27,{'state':true}); expect(snap.version).toBe(27); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 28', () => { const snap = createSnapshot('a','T',28,{'state':true}); expect(snap.version).toBe(28); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 29', () => { const snap = createSnapshot('a','T',29,{'state':true}); expect(snap.version).toBe(29); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 30', () => { const snap = createSnapshot('a','T',30,{'state':true}); expect(snap.version).toBe(30); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 31', () => { const snap = createSnapshot('a','T',31,{'state':true}); expect(snap.version).toBe(31); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 32', () => { const snap = createSnapshot('a','T',32,{'state':true}); expect(snap.version).toBe(32); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 33', () => { const snap = createSnapshot('a','T',33,{'state':true}); expect(snap.version).toBe(33); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 34', () => { const snap = createSnapshot('a','T',34,{'state':true}); expect(snap.version).toBe(34); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 35', () => { const snap = createSnapshot('a','T',35,{'state':true}); expect(snap.version).toBe(35); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 36', () => { const snap = createSnapshot('a','T',36,{'state':true}); expect(snap.version).toBe(36); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 37', () => { const snap = createSnapshot('a','T',37,{'state':true}); expect(snap.version).toBe(37); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 38', () => { const snap = createSnapshot('a','T',38,{'state':true}); expect(snap.version).toBe(38); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 39', () => { const snap = createSnapshot('a','T',39,{'state':true}); expect(snap.version).toBe(39); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 40', () => { const snap = createSnapshot('a','T',40,{'state':true}); expect(snap.version).toBe(40); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 41', () => { const snap = createSnapshot('a','T',41,{'state':true}); expect(snap.version).toBe(41); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 42', () => { const snap = createSnapshot('a','T',42,{'state':true}); expect(snap.version).toBe(42); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 43', () => { const snap = createSnapshot('a','T',43,{'state':true}); expect(snap.version).toBe(43); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 44', () => { const snap = createSnapshot('a','T',44,{'state':true}); expect(snap.version).toBe(44); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 45', () => { const snap = createSnapshot('a','T',45,{'state':true}); expect(snap.version).toBe(45); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 46', () => { const snap = createSnapshot('a','T',46,{'state':true}); expect(snap.version).toBe(46); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 47', () => { const snap = createSnapshot('a','T',47,{'state':true}); expect(snap.version).toBe(47); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 48', () => { const snap = createSnapshot('a','T',48,{'state':true}); expect(snap.version).toBe(48); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 49', () => { const snap = createSnapshot('a','T',49,{'state':true}); expect(snap.version).toBe(49); expect(snap.aggregateId).toBe('a'); });
  it('createSnapshot version 50', () => { const snap = createSnapshot('a','T',50,{'state':true}); expect(snap.version).toBe(50); expect(snap.aggregateId).toBe('a'); });
  it('rebuildFromSnapshot 1', () => { const snap = createSnapshot('a','T',0,1); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(2); });
  it('rebuildFromSnapshot 2', () => { const snap = createSnapshot('a','T',0,2); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(3); });
  it('rebuildFromSnapshot 3', () => { const snap = createSnapshot('a','T',0,3); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(4); });
  it('rebuildFromSnapshot 4', () => { const snap = createSnapshot('a','T',0,4); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(5); });
  it('rebuildFromSnapshot 5', () => { const snap = createSnapshot('a','T',0,5); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(6); });
  it('rebuildFromSnapshot 6', () => { const snap = createSnapshot('a','T',0,6); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(7); });
  it('rebuildFromSnapshot 7', () => { const snap = createSnapshot('a','T',0,7); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(8); });
  it('rebuildFromSnapshot 8', () => { const snap = createSnapshot('a','T',0,8); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(9); });
  it('rebuildFromSnapshot 9', () => { const snap = createSnapshot('a','T',0,9); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(10); });
  it('rebuildFromSnapshot 10', () => { const snap = createSnapshot('a','T',0,10); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(11); });
  it('rebuildFromSnapshot 11', () => { const snap = createSnapshot('a','T',0,11); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(12); });
  it('rebuildFromSnapshot 12', () => { const snap = createSnapshot('a','T',0,12); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(13); });
  it('rebuildFromSnapshot 13', () => { const snap = createSnapshot('a','T',0,13); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(14); });
  it('rebuildFromSnapshot 14', () => { const snap = createSnapshot('a','T',0,14); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(15); });
  it('rebuildFromSnapshot 15', () => { const snap = createSnapshot('a','T',0,15); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(16); });
  it('rebuildFromSnapshot 16', () => { const snap = createSnapshot('a','T',0,16); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(17); });
  it('rebuildFromSnapshot 17', () => { const snap = createSnapshot('a','T',0,17); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(18); });
  it('rebuildFromSnapshot 18', () => { const snap = createSnapshot('a','T',0,18); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(19); });
  it('rebuildFromSnapshot 19', () => { const snap = createSnapshot('a','T',0,19); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(20); });
  it('rebuildFromSnapshot 20', () => { const snap = createSnapshot('a','T',0,20); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(21); });
  it('rebuildFromSnapshot 21', () => { const snap = createSnapshot('a','T',0,21); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(22); });
  it('rebuildFromSnapshot 22', () => { const snap = createSnapshot('a','T',0,22); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(23); });
  it('rebuildFromSnapshot 23', () => { const snap = createSnapshot('a','T',0,23); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(24); });
  it('rebuildFromSnapshot 24', () => { const snap = createSnapshot('a','T',0,24); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(25); });
  it('rebuildFromSnapshot 25', () => { const snap = createSnapshot('a','T',0,25); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(26); });
  it('rebuildFromSnapshot 26', () => { const snap = createSnapshot('a','T',0,26); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(27); });
  it('rebuildFromSnapshot 27', () => { const snap = createSnapshot('a','T',0,27); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(28); });
  it('rebuildFromSnapshot 28', () => { const snap = createSnapshot('a','T',0,28); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(29); });
  it('rebuildFromSnapshot 29', () => { const snap = createSnapshot('a','T',0,29); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(30); });
  it('rebuildFromSnapshot 30', () => { const snap = createSnapshot('a','T',0,30); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(31); });
  it('rebuildFromSnapshot 31', () => { const snap = createSnapshot('a','T',0,31); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(32); });
  it('rebuildFromSnapshot 32', () => { const snap = createSnapshot('a','T',0,32); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(33); });
  it('rebuildFromSnapshot 33', () => { const snap = createSnapshot('a','T',0,33); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(34); });
  it('rebuildFromSnapshot 34', () => { const snap = createSnapshot('a','T',0,34); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(35); });
  it('rebuildFromSnapshot 35', () => { const snap = createSnapshot('a','T',0,35); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(36); });
  it('rebuildFromSnapshot 36', () => { const snap = createSnapshot('a','T',0,36); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(37); });
  it('rebuildFromSnapshot 37', () => { const snap = createSnapshot('a','T',0,37); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(38); });
  it('rebuildFromSnapshot 38', () => { const snap = createSnapshot('a','T',0,38); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(39); });
  it('rebuildFromSnapshot 39', () => { const snap = createSnapshot('a','T',0,39); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(40); });
  it('rebuildFromSnapshot 40', () => { const snap = createSnapshot('a','T',0,40); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(41); });
  it('rebuildFromSnapshot 41', () => { const snap = createSnapshot('a','T',0,41); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(42); });
  it('rebuildFromSnapshot 42', () => { const snap = createSnapshot('a','T',0,42); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(43); });
  it('rebuildFromSnapshot 43', () => { const snap = createSnapshot('a','T',0,43); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(44); });
  it('rebuildFromSnapshot 44', () => { const snap = createSnapshot('a','T',0,44); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(45); });
  it('rebuildFromSnapshot 45', () => { const snap = createSnapshot('a','T',0,45); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(46); });
  it('rebuildFromSnapshot 46', () => { const snap = createSnapshot('a','T',0,46); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(47); });
  it('rebuildFromSnapshot 47', () => { const snap = createSnapshot('a','T',0,47); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(48); });
  it('rebuildFromSnapshot 48', () => { const snap = createSnapshot('a','T',0,48); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(49); });
  it('rebuildFromSnapshot 49', () => { const snap = createSnapshot('a','T',0,49); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(50); });
  it('rebuildFromSnapshot 50', () => { const snap = createSnapshot('a','T',0,50); const events = [mkEvent('e1','T','a',1)]; expect(rebuildFromSnapshot(snap, events, counterReducer)).toBe(51); });
});

describe('extra event-sourcing coverage', () => {
  it('appendEvents bulk 1', () => { const events = [mkEvent('ea','T','agg',1), mkEvent('eb','T','agg',101)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 2', () => { const events = [mkEvent('ea','T','agg',2), mkEvent('eb','T','agg',102)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 3', () => { const events = [mkEvent('ea','T','agg',3), mkEvent('eb','T','agg',103)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 4', () => { const events = [mkEvent('ea','T','agg',4), mkEvent('eb','T','agg',104)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 5', () => { const events = [mkEvent('ea','T','agg',5), mkEvent('eb','T','agg',105)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 6', () => { const events = [mkEvent('ea','T','agg',6), mkEvent('eb','T','agg',106)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 7', () => { const events = [mkEvent('ea','T','agg',7), mkEvent('eb','T','agg',107)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 8', () => { const events = [mkEvent('ea','T','agg',8), mkEvent('eb','T','agg',108)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 9', () => { const events = [mkEvent('ea','T','agg',9), mkEvent('eb','T','agg',109)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 10', () => { const events = [mkEvent('ea','T','agg',10), mkEvent('eb','T','agg',110)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 11', () => { const events = [mkEvent('ea','T','agg',11), mkEvent('eb','T','agg',111)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 12', () => { const events = [mkEvent('ea','T','agg',12), mkEvent('eb','T','agg',112)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 13', () => { const events = [mkEvent('ea','T','agg',13), mkEvent('eb','T','agg',113)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 14', () => { const events = [mkEvent('ea','T','agg',14), mkEvent('eb','T','agg',114)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 15', () => { const events = [mkEvent('ea','T','agg',15), mkEvent('eb','T','agg',115)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 16', () => { const events = [mkEvent('ea','T','agg',16), mkEvent('eb','T','agg',116)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 17', () => { const events = [mkEvent('ea','T','agg',17), mkEvent('eb','T','agg',117)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 18', () => { const events = [mkEvent('ea','T','agg',18), mkEvent('eb','T','agg',118)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 19', () => { const events = [mkEvent('ea','T','agg',19), mkEvent('eb','T','agg',119)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 20', () => { const events = [mkEvent('ea','T','agg',20), mkEvent('eb','T','agg',120)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 21', () => { const events = [mkEvent('ea','T','agg',21), mkEvent('eb','T','agg',121)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 22', () => { const events = [mkEvent('ea','T','agg',22), mkEvent('eb','T','agg',122)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 23', () => { const events = [mkEvent('ea','T','agg',23), mkEvent('eb','T','agg',123)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 24', () => { const events = [mkEvent('ea','T','agg',24), mkEvent('eb','T','agg',124)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 25', () => { const events = [mkEvent('ea','T','agg',25), mkEvent('eb','T','agg',125)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 26', () => { const events = [mkEvent('ea','T','agg',26), mkEvent('eb','T','agg',126)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 27', () => { const events = [mkEvent('ea','T','agg',27), mkEvent('eb','T','agg',127)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 28', () => { const events = [mkEvent('ea','T','agg',28), mkEvent('eb','T','agg',128)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 29', () => { const events = [mkEvent('ea','T','agg',29), mkEvent('eb','T','agg',129)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 30', () => { const events = [mkEvent('ea','T','agg',30), mkEvent('eb','T','agg',130)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 31', () => { const events = [mkEvent('ea','T','agg',31), mkEvent('eb','T','agg',131)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 32', () => { const events = [mkEvent('ea','T','agg',32), mkEvent('eb','T','agg',132)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 33', () => { const events = [mkEvent('ea','T','agg',33), mkEvent('eb','T','agg',133)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 34', () => { const events = [mkEvent('ea','T','agg',34), mkEvent('eb','T','agg',134)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 35', () => { const events = [mkEvent('ea','T','agg',35), mkEvent('eb','T','agg',135)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 36', () => { const events = [mkEvent('ea','T','agg',36), mkEvent('eb','T','agg',136)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 37', () => { const events = [mkEvent('ea','T','agg',37), mkEvent('eb','T','agg',137)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 38', () => { const events = [mkEvent('ea','T','agg',38), mkEvent('eb','T','agg',138)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 39', () => { const events = [mkEvent('ea','T','agg',39), mkEvent('eb','T','agg',139)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 40', () => { const events = [mkEvent('ea','T','agg',40), mkEvent('eb','T','agg',140)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 41', () => { const events = [mkEvent('ea','T','agg',41), mkEvent('eb','T','agg',141)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 42', () => { const events = [mkEvent('ea','T','agg',42), mkEvent('eb','T','agg',142)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 43', () => { const events = [mkEvent('ea','T','agg',43), mkEvent('eb','T','agg',143)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 44', () => { const events = [mkEvent('ea','T','agg',44), mkEvent('eb','T','agg',144)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 45', () => { const events = [mkEvent('ea','T','agg',45), mkEvent('eb','T','agg',145)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 46', () => { const events = [mkEvent('ea','T','agg',46), mkEvent('eb','T','agg',146)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 47', () => { const events = [mkEvent('ea','T','agg',47), mkEvent('eb','T','agg',147)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 48', () => { const events = [mkEvent('ea','T','agg',48), mkEvent('eb','T','agg',148)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 49', () => { const events = [mkEvent('ea','T','agg',49), mkEvent('eb','T','agg',149)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 50', () => { const events = [mkEvent('ea','T','agg',50), mkEvent('eb','T','agg',150)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 51', () => { const events = [mkEvent('ea','T','agg',51), mkEvent('eb','T','agg',151)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 52', () => { const events = [mkEvent('ea','T','agg',52), mkEvent('eb','T','agg',152)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 53', () => { const events = [mkEvent('ea','T','agg',53), mkEvent('eb','T','agg',153)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 54', () => { const events = [mkEvent('ea','T','agg',54), mkEvent('eb','T','agg',154)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 55', () => { const events = [mkEvent('ea','T','agg',55), mkEvent('eb','T','agg',155)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 56', () => { const events = [mkEvent('ea','T','agg',56), mkEvent('eb','T','agg',156)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 57', () => { const events = [mkEvent('ea','T','agg',57), mkEvent('eb','T','agg',157)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 58', () => { const events = [mkEvent('ea','T','agg',58), mkEvent('eb','T','agg',158)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 59', () => { const events = [mkEvent('ea','T','agg',59), mkEvent('eb','T','agg',159)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 60', () => { const events = [mkEvent('ea','T','agg',60), mkEvent('eb','T','agg',160)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 61', () => { const events = [mkEvent('ea','T','agg',61), mkEvent('eb','T','agg',161)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 62', () => { const events = [mkEvent('ea','T','agg',62), mkEvent('eb','T','agg',162)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 63', () => { const events = [mkEvent('ea','T','agg',63), mkEvent('eb','T','agg',163)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 64', () => { const events = [mkEvent('ea','T','agg',64), mkEvent('eb','T','agg',164)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 65', () => { const events = [mkEvent('ea','T','agg',65), mkEvent('eb','T','agg',165)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 66', () => { const events = [mkEvent('ea','T','agg',66), mkEvent('eb','T','agg',166)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 67', () => { const events = [mkEvent('ea','T','agg',67), mkEvent('eb','T','agg',167)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 68', () => { const events = [mkEvent('ea','T','agg',68), mkEvent('eb','T','agg',168)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 69', () => { const events = [mkEvent('ea','T','agg',69), mkEvent('eb','T','agg',169)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 70', () => { const events = [mkEvent('ea','T','agg',70), mkEvent('eb','T','agg',170)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 71', () => { const events = [mkEvent('ea','T','agg',71), mkEvent('eb','T','agg',171)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 72', () => { const events = [mkEvent('ea','T','agg',72), mkEvent('eb','T','agg',172)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 73', () => { const events = [mkEvent('ea','T','agg',73), mkEvent('eb','T','agg',173)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 74', () => { const events = [mkEvent('ea','T','agg',74), mkEvent('eb','T','agg',174)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 75', () => { const events = [mkEvent('ea','T','agg',75), mkEvent('eb','T','agg',175)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 76', () => { const events = [mkEvent('ea','T','agg',76), mkEvent('eb','T','agg',176)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 77', () => { const events = [mkEvent('ea','T','agg',77), mkEvent('eb','T','agg',177)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 78', () => { const events = [mkEvent('ea','T','agg',78), mkEvent('eb','T','agg',178)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 79', () => { const events = [mkEvent('ea','T','agg',79), mkEvent('eb','T','agg',179)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 80', () => { const events = [mkEvent('ea','T','agg',80), mkEvent('eb','T','agg',180)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 81', () => { const events = [mkEvent('ea','T','agg',81), mkEvent('eb','T','agg',181)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 82', () => { const events = [mkEvent('ea','T','agg',82), mkEvent('eb','T','agg',182)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 83', () => { const events = [mkEvent('ea','T','agg',83), mkEvent('eb','T','agg',183)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 84', () => { const events = [mkEvent('ea','T','agg',84), mkEvent('eb','T','agg',184)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 85', () => { const events = [mkEvent('ea','T','agg',85), mkEvent('eb','T','agg',185)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 86', () => { const events = [mkEvent('ea','T','agg',86), mkEvent('eb','T','agg',186)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 87', () => { const events = [mkEvent('ea','T','agg',87), mkEvent('eb','T','agg',187)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 88', () => { const events = [mkEvent('ea','T','agg',88), mkEvent('eb','T','agg',188)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 89', () => { const events = [mkEvent('ea','T','agg',89), mkEvent('eb','T','agg',189)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 90', () => { const events = [mkEvent('ea','T','agg',90), mkEvent('eb','T','agg',190)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 91', () => { const events = [mkEvent('ea','T','agg',91), mkEvent('eb','T','agg',191)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 92', () => { const events = [mkEvent('ea','T','agg',92), mkEvent('eb','T','agg',192)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 93', () => { const events = [mkEvent('ea','T','agg',93), mkEvent('eb','T','agg',193)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 94', () => { const events = [mkEvent('ea','T','agg',94), mkEvent('eb','T','agg',194)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 95', () => { const events = [mkEvent('ea','T','agg',95), mkEvent('eb','T','agg',195)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 96', () => { const events = [mkEvent('ea','T','agg',96), mkEvent('eb','T','agg',196)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 97', () => { const events = [mkEvent('ea','T','agg',97), mkEvent('eb','T','agg',197)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 98', () => { const events = [mkEvent('ea','T','agg',98), mkEvent('eb','T','agg',198)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 99', () => { const events = [mkEvent('ea','T','agg',99), mkEvent('eb','T','agg',199)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('appendEvents bulk 100', () => { const events = [mkEvent('ea','T','agg',100), mkEvent('eb','T','agg',200)]; const s = appendEvents(createStore(), events); expect(s.events.length).toBe(2); });
  it('getEventsAfterVersion 1', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg1',k)); expect(getEventsAfterVersion(s,'agg1',1).length).toBe(4); });
  it('getEventsAfterVersion 2', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg2',k)); expect(getEventsAfterVersion(s,'agg2',2).length).toBe(3); });
  it('getEventsAfterVersion 3', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg3',k)); expect(getEventsAfterVersion(s,'agg3',3).length).toBe(2); });
  it('getEventsAfterVersion 4', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg4',k)); expect(getEventsAfterVersion(s,'agg4',3).length).toBe(2); });
  it('getEventsAfterVersion 5', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg5',k)); expect(getEventsAfterVersion(s,'agg5',3).length).toBe(2); });
  it('getEventsAfterVersion 6', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg6',k)); expect(getEventsAfterVersion(s,'agg6',3).length).toBe(2); });
  it('getEventsAfterVersion 7', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg7',k)); expect(getEventsAfterVersion(s,'agg7',3).length).toBe(2); });
  it('getEventsAfterVersion 8', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg8',k)); expect(getEventsAfterVersion(s,'agg8',3).length).toBe(2); });
  it('getEventsAfterVersion 9', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg9',k)); expect(getEventsAfterVersion(s,'agg9',3).length).toBe(2); });
  it('getEventsAfterVersion 10', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg10',k)); expect(getEventsAfterVersion(s,'agg10',3).length).toBe(2); });
  it('getEventsAfterVersion 11', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg11',k)); expect(getEventsAfterVersion(s,'agg11',3).length).toBe(2); });
  it('getEventsAfterVersion 12', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg12',k)); expect(getEventsAfterVersion(s,'agg12',3).length).toBe(2); });
  it('getEventsAfterVersion 13', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg13',k)); expect(getEventsAfterVersion(s,'agg13',3).length).toBe(2); });
  it('getEventsAfterVersion 14', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg14',k)); expect(getEventsAfterVersion(s,'agg14',3).length).toBe(2); });
  it('getEventsAfterVersion 15', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg15',k)); expect(getEventsAfterVersion(s,'agg15',3).length).toBe(2); });
  it('getEventsAfterVersion 16', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg16',k)); expect(getEventsAfterVersion(s,'agg16',3).length).toBe(2); });
  it('getEventsAfterVersion 17', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg17',k)); expect(getEventsAfterVersion(s,'agg17',3).length).toBe(2); });
  it('getEventsAfterVersion 18', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg18',k)); expect(getEventsAfterVersion(s,'agg18',3).length).toBe(2); });
  it('getEventsAfterVersion 19', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg19',k)); expect(getEventsAfterVersion(s,'agg19',3).length).toBe(2); });
  it('getEventsAfterVersion 20', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg20',k)); expect(getEventsAfterVersion(s,'agg20',3).length).toBe(2); });
  it('getEventsAfterVersion 21', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg21',k)); expect(getEventsAfterVersion(s,'agg21',3).length).toBe(2); });
  it('getEventsAfterVersion 22', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg22',k)); expect(getEventsAfterVersion(s,'agg22',3).length).toBe(2); });
  it('getEventsAfterVersion 23', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg23',k)); expect(getEventsAfterVersion(s,'agg23',3).length).toBe(2); });
  it('getEventsAfterVersion 24', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg24',k)); expect(getEventsAfterVersion(s,'agg24',3).length).toBe(2); });
  it('getEventsAfterVersion 25', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg25',k)); expect(getEventsAfterVersion(s,'agg25',3).length).toBe(2); });
  it('getEventsAfterVersion 26', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg26',k)); expect(getEventsAfterVersion(s,'agg26',3).length).toBe(2); });
  it('getEventsAfterVersion 27', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg27',k)); expect(getEventsAfterVersion(s,'agg27',3).length).toBe(2); });
  it('getEventsAfterVersion 28', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg28',k)); expect(getEventsAfterVersion(s,'agg28',3).length).toBe(2); });
  it('getEventsAfterVersion 29', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg29',k)); expect(getEventsAfterVersion(s,'agg29',3).length).toBe(2); });
  it('getEventsAfterVersion 30', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg30',k)); expect(getEventsAfterVersion(s,'agg30',3).length).toBe(2); });
  it('getEventsAfterVersion 31', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg31',k)); expect(getEventsAfterVersion(s,'agg31',3).length).toBe(2); });
  it('getEventsAfterVersion 32', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg32',k)); expect(getEventsAfterVersion(s,'agg32',3).length).toBe(2); });
  it('getEventsAfterVersion 33', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg33',k)); expect(getEventsAfterVersion(s,'agg33',3).length).toBe(2); });
  it('getEventsAfterVersion 34', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg34',k)); expect(getEventsAfterVersion(s,'agg34',3).length).toBe(2); });
  it('getEventsAfterVersion 35', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg35',k)); expect(getEventsAfterVersion(s,'agg35',3).length).toBe(2); });
  it('getEventsAfterVersion 36', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg36',k)); expect(getEventsAfterVersion(s,'agg36',3).length).toBe(2); });
  it('getEventsAfterVersion 37', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg37',k)); expect(getEventsAfterVersion(s,'agg37',3).length).toBe(2); });
  it('getEventsAfterVersion 38', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg38',k)); expect(getEventsAfterVersion(s,'agg38',3).length).toBe(2); });
  it('getEventsAfterVersion 39', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg39',k)); expect(getEventsAfterVersion(s,'agg39',3).length).toBe(2); });
  it('getEventsAfterVersion 40', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg40',k)); expect(getEventsAfterVersion(s,'agg40',3).length).toBe(2); });
  it('getEventsAfterVersion 41', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg41',k)); expect(getEventsAfterVersion(s,'agg41',3).length).toBe(2); });
  it('getEventsAfterVersion 42', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg42',k)); expect(getEventsAfterVersion(s,'agg42',3).length).toBe(2); });
  it('getEventsAfterVersion 43', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg43',k)); expect(getEventsAfterVersion(s,'agg43',3).length).toBe(2); });
  it('getEventsAfterVersion 44', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg44',k)); expect(getEventsAfterVersion(s,'agg44',3).length).toBe(2); });
  it('getEventsAfterVersion 45', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg45',k)); expect(getEventsAfterVersion(s,'agg45',3).length).toBe(2); });
  it('getEventsAfterVersion 46', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg46',k)); expect(getEventsAfterVersion(s,'agg46',3).length).toBe(2); });
  it('getEventsAfterVersion 47', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg47',k)); expect(getEventsAfterVersion(s,'agg47',3).length).toBe(2); });
  it('getEventsAfterVersion 48', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg48',k)); expect(getEventsAfterVersion(s,'agg48',3).length).toBe(2); });
  it('getEventsAfterVersion 49', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg49',k)); expect(getEventsAfterVersion(s,'agg49',3).length).toBe(2); });
  it('getEventsAfterVersion 50', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg50',k)); expect(getEventsAfterVersion(s,'agg50',3).length).toBe(2); });
  it('rebuildAggregate 1', () => { let s = createStore(); for(let k=1;k<=1;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg1',k)); expect(rebuildAggregate(s,'agg1',0,counterReducer)).toBe(1); });
  it('rebuildAggregate 2', () => { let s = createStore(); for(let k=1;k<=2;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg2',k)); expect(rebuildAggregate(s,'agg2',0,counterReducer)).toBe(2); });
  it('rebuildAggregate 3', () => { let s = createStore(); for(let k=1;k<=3;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg3',k)); expect(rebuildAggregate(s,'agg3',0,counterReducer)).toBe(3); });
  it('rebuildAggregate 4', () => { let s = createStore(); for(let k=1;k<=4;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg4',k)); expect(rebuildAggregate(s,'agg4',0,counterReducer)).toBe(4); });
  it('rebuildAggregate 5', () => { let s = createStore(); for(let k=1;k<=5;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg5',k)); expect(rebuildAggregate(s,'agg5',0,counterReducer)).toBe(5); });
  it('rebuildAggregate 6', () => { let s = createStore(); for(let k=1;k<=6;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg6',k)); expect(rebuildAggregate(s,'agg6',0,counterReducer)).toBe(6); });
  it('rebuildAggregate 7', () => { let s = createStore(); for(let k=1;k<=7;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg7',k)); expect(rebuildAggregate(s,'agg7',0,counterReducer)).toBe(7); });
  it('rebuildAggregate 8', () => { let s = createStore(); for(let k=1;k<=8;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg8',k)); expect(rebuildAggregate(s,'agg8',0,counterReducer)).toBe(8); });
  it('rebuildAggregate 9', () => { let s = createStore(); for(let k=1;k<=9;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg9',k)); expect(rebuildAggregate(s,'agg9',0,counterReducer)).toBe(9); });
  it('rebuildAggregate 10', () => { let s = createStore(); for(let k=1;k<=10;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg10',k)); expect(rebuildAggregate(s,'agg10',0,counterReducer)).toBe(10); });
  it('rebuildAggregate 11', () => { let s = createStore(); for(let k=1;k<=11;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg11',k)); expect(rebuildAggregate(s,'agg11',0,counterReducer)).toBe(11); });
  it('rebuildAggregate 12', () => { let s = createStore(); for(let k=1;k<=12;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg12',k)); expect(rebuildAggregate(s,'agg12',0,counterReducer)).toBe(12); });
  it('rebuildAggregate 13', () => { let s = createStore(); for(let k=1;k<=13;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg13',k)); expect(rebuildAggregate(s,'agg13',0,counterReducer)).toBe(13); });
  it('rebuildAggregate 14', () => { let s = createStore(); for(let k=1;k<=14;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg14',k)); expect(rebuildAggregate(s,'agg14',0,counterReducer)).toBe(14); });
  it('rebuildAggregate 15', () => { let s = createStore(); for(let k=1;k<=15;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg15',k)); expect(rebuildAggregate(s,'agg15',0,counterReducer)).toBe(15); });
  it('rebuildAggregate 16', () => { let s = createStore(); for(let k=1;k<=16;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg16',k)); expect(rebuildAggregate(s,'agg16',0,counterReducer)).toBe(16); });
  it('rebuildAggregate 17', () => { let s = createStore(); for(let k=1;k<=17;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg17',k)); expect(rebuildAggregate(s,'agg17',0,counterReducer)).toBe(17); });
  it('rebuildAggregate 18', () => { let s = createStore(); for(let k=1;k<=18;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg18',k)); expect(rebuildAggregate(s,'agg18',0,counterReducer)).toBe(18); });
  it('rebuildAggregate 19', () => { let s = createStore(); for(let k=1;k<=19;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg19',k)); expect(rebuildAggregate(s,'agg19',0,counterReducer)).toBe(19); });
  it('rebuildAggregate 20', () => { let s = createStore(); for(let k=1;k<=20;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg20',k)); expect(rebuildAggregate(s,'agg20',0,counterReducer)).toBe(20); });
  it('rebuildAggregate 21', () => { let s = createStore(); for(let k=1;k<=21;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg21',k)); expect(rebuildAggregate(s,'agg21',0,counterReducer)).toBe(21); });
  it('rebuildAggregate 22', () => { let s = createStore(); for(let k=1;k<=22;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg22',k)); expect(rebuildAggregate(s,'agg22',0,counterReducer)).toBe(22); });
  it('rebuildAggregate 23', () => { let s = createStore(); for(let k=1;k<=23;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg23',k)); expect(rebuildAggregate(s,'agg23',0,counterReducer)).toBe(23); });
  it('rebuildAggregate 24', () => { let s = createStore(); for(let k=1;k<=24;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg24',k)); expect(rebuildAggregate(s,'agg24',0,counterReducer)).toBe(24); });
  it('rebuildAggregate 25', () => { let s = createStore(); for(let k=1;k<=25;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg25',k)); expect(rebuildAggregate(s,'agg25',0,counterReducer)).toBe(25); });
  it('rebuildAggregate 26', () => { let s = createStore(); for(let k=1;k<=26;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg26',k)); expect(rebuildAggregate(s,'agg26',0,counterReducer)).toBe(26); });
  it('rebuildAggregate 27', () => { let s = createStore(); for(let k=1;k<=27;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg27',k)); expect(rebuildAggregate(s,'agg27',0,counterReducer)).toBe(27); });
  it('rebuildAggregate 28', () => { let s = createStore(); for(let k=1;k<=28;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg28',k)); expect(rebuildAggregate(s,'agg28',0,counterReducer)).toBe(28); });
  it('rebuildAggregate 29', () => { let s = createStore(); for(let k=1;k<=29;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg29',k)); expect(rebuildAggregate(s,'agg29',0,counterReducer)).toBe(29); });
  it('rebuildAggregate 30', () => { let s = createStore(); for(let k=1;k<=30;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg30',k)); expect(rebuildAggregate(s,'agg30',0,counterReducer)).toBe(30); });
  it('rebuildAggregate 31', () => { let s = createStore(); for(let k=1;k<=31;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg31',k)); expect(rebuildAggregate(s,'agg31',0,counterReducer)).toBe(31); });
  it('rebuildAggregate 32', () => { let s = createStore(); for(let k=1;k<=32;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg32',k)); expect(rebuildAggregate(s,'agg32',0,counterReducer)).toBe(32); });
  it('rebuildAggregate 33', () => { let s = createStore(); for(let k=1;k<=33;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg33',k)); expect(rebuildAggregate(s,'agg33',0,counterReducer)).toBe(33); });
  it('rebuildAggregate 34', () => { let s = createStore(); for(let k=1;k<=34;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg34',k)); expect(rebuildAggregate(s,'agg34',0,counterReducer)).toBe(34); });
  it('rebuildAggregate 35', () => { let s = createStore(); for(let k=1;k<=35;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg35',k)); expect(rebuildAggregate(s,'agg35',0,counterReducer)).toBe(35); });
  it('rebuildAggregate 36', () => { let s = createStore(); for(let k=1;k<=36;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg36',k)); expect(rebuildAggregate(s,'agg36',0,counterReducer)).toBe(36); });
  it('rebuildAggregate 37', () => { let s = createStore(); for(let k=1;k<=37;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg37',k)); expect(rebuildAggregate(s,'agg37',0,counterReducer)).toBe(37); });
  it('rebuildAggregate 38', () => { let s = createStore(); for(let k=1;k<=38;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg38',k)); expect(rebuildAggregate(s,'agg38',0,counterReducer)).toBe(38); });
  it('rebuildAggregate 39', () => { let s = createStore(); for(let k=1;k<=39;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg39',k)); expect(rebuildAggregate(s,'agg39',0,counterReducer)).toBe(39); });
  it('rebuildAggregate 40', () => { let s = createStore(); for(let k=1;k<=40;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg40',k)); expect(rebuildAggregate(s,'agg40',0,counterReducer)).toBe(40); });
  it('rebuildAggregate 41', () => { let s = createStore(); for(let k=1;k<=41;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg41',k)); expect(rebuildAggregate(s,'agg41',0,counterReducer)).toBe(41); });
  it('rebuildAggregate 42', () => { let s = createStore(); for(let k=1;k<=42;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg42',k)); expect(rebuildAggregate(s,'agg42',0,counterReducer)).toBe(42); });
  it('rebuildAggregate 43', () => { let s = createStore(); for(let k=1;k<=43;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg43',k)); expect(rebuildAggregate(s,'agg43',0,counterReducer)).toBe(43); });
  it('rebuildAggregate 44', () => { let s = createStore(); for(let k=1;k<=44;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg44',k)); expect(rebuildAggregate(s,'agg44',0,counterReducer)).toBe(44); });
  it('rebuildAggregate 45', () => { let s = createStore(); for(let k=1;k<=45;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg45',k)); expect(rebuildAggregate(s,'agg45',0,counterReducer)).toBe(45); });
  it('rebuildAggregate 46', () => { let s = createStore(); for(let k=1;k<=46;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg46',k)); expect(rebuildAggregate(s,'agg46',0,counterReducer)).toBe(46); });
  it('rebuildAggregate 47', () => { let s = createStore(); for(let k=1;k<=47;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg47',k)); expect(rebuildAggregate(s,'agg47',0,counterReducer)).toBe(47); });
  it('rebuildAggregate 48', () => { let s = createStore(); for(let k=1;k<=48;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg48',k)); expect(rebuildAggregate(s,'agg48',0,counterReducer)).toBe(48); });
  it('rebuildAggregate 49', () => { let s = createStore(); for(let k=1;k<=49;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg49',k)); expect(rebuildAggregate(s,'agg49',0,counterReducer)).toBe(49); });
  it('rebuildAggregate 50', () => { let s = createStore(); for(let k=1;k<=50;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg50',k)); expect(rebuildAggregate(s,'agg50',0,counterReducer)).toBe(50); });
  it('rebuildAggregate 51', () => { let s = createStore(); for(let k=1;k<=51;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg51',k)); expect(rebuildAggregate(s,'agg51',0,counterReducer)).toBe(51); });
  it('rebuildAggregate 52', () => { let s = createStore(); for(let k=1;k<=52;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg52',k)); expect(rebuildAggregate(s,'agg52',0,counterReducer)).toBe(52); });
  it('rebuildAggregate 53', () => { let s = createStore(); for(let k=1;k<=53;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg53',k)); expect(rebuildAggregate(s,'agg53',0,counterReducer)).toBe(53); });
  it('rebuildAggregate 54', () => { let s = createStore(); for(let k=1;k<=54;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg54',k)); expect(rebuildAggregate(s,'agg54',0,counterReducer)).toBe(54); });
  it('rebuildAggregate 55', () => { let s = createStore(); for(let k=1;k<=55;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg55',k)); expect(rebuildAggregate(s,'agg55',0,counterReducer)).toBe(55); });
  it('rebuildAggregate 56', () => { let s = createStore(); for(let k=1;k<=56;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg56',k)); expect(rebuildAggregate(s,'agg56',0,counterReducer)).toBe(56); });
  it('rebuildAggregate 57', () => { let s = createStore(); for(let k=1;k<=57;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg57',k)); expect(rebuildAggregate(s,'agg57',0,counterReducer)).toBe(57); });
  it('rebuildAggregate 58', () => { let s = createStore(); for(let k=1;k<=58;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg58',k)); expect(rebuildAggregate(s,'agg58',0,counterReducer)).toBe(58); });
  it('rebuildAggregate 59', () => { let s = createStore(); for(let k=1;k<=59;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg59',k)); expect(rebuildAggregate(s,'agg59',0,counterReducer)).toBe(59); });
  it('rebuildAggregate 60', () => { let s = createStore(); for(let k=1;k<=60;k++) s=appendEvent(s,mkEvent(`e${k}`,'T','agg60',k)); expect(rebuildAggregate(s,'agg60',0,counterReducer)).toBe(60); });
});
