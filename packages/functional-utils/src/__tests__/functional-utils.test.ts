// Copyright (c) 2026 Nexara DMCC. All rights reserved.
import { identity, constant, noop, always, curry2, curry3, partial, flip, memoize, once, tryCatch, some, none, fromNullable, mapMaybe, flatMapMaybe, getOrElse, isSome, isNone, left, right, mapRight, mapLeft, fold, isRight, isLeft, iterate, unfold, applyN, complement, juxt } from '../functional-utils';

describe('identity and constant', () => {
  it('identity returns value 1', () => { expect(identity(1)).toBe(1); });
  it('identity returns value 2', () => { expect(identity(2)).toBe(2); });
  it('identity returns value 3', () => { expect(identity(3)).toBe(3); });
  it('identity returns value 4', () => { expect(identity(4)).toBe(4); });
  it('identity returns value 5', () => { expect(identity(5)).toBe(5); });
  it('identity returns value 6', () => { expect(identity(6)).toBe(6); });
  it('identity returns value 7', () => { expect(identity(7)).toBe(7); });
  it('identity returns value 8', () => { expect(identity(8)).toBe(8); });
  it('identity returns value 9', () => { expect(identity(9)).toBe(9); });
  it('identity returns value 10', () => { expect(identity(10)).toBe(10); });
  it('identity returns value 11', () => { expect(identity(11)).toBe(11); });
  it('identity returns value 12', () => { expect(identity(12)).toBe(12); });
  it('identity returns value 13', () => { expect(identity(13)).toBe(13); });
  it('identity returns value 14', () => { expect(identity(14)).toBe(14); });
  it('identity returns value 15', () => { expect(identity(15)).toBe(15); });
  it('identity returns value 16', () => { expect(identity(16)).toBe(16); });
  it('identity returns value 17', () => { expect(identity(17)).toBe(17); });
  it('identity returns value 18', () => { expect(identity(18)).toBe(18); });
  it('identity returns value 19', () => { expect(identity(19)).toBe(19); });
  it('identity returns value 20', () => { expect(identity(20)).toBe(20); });
  it('identity returns value 21', () => { expect(identity(21)).toBe(21); });
  it('identity returns value 22', () => { expect(identity(22)).toBe(22); });
  it('identity returns value 23', () => { expect(identity(23)).toBe(23); });
  it('identity returns value 24', () => { expect(identity(24)).toBe(24); });
  it('identity returns value 25', () => { expect(identity(25)).toBe(25); });
  it('identity returns value 26', () => { expect(identity(26)).toBe(26); });
  it('identity returns value 27', () => { expect(identity(27)).toBe(27); });
  it('identity returns value 28', () => { expect(identity(28)).toBe(28); });
  it('identity returns value 29', () => { expect(identity(29)).toBe(29); });
  it('identity returns value 30', () => { expect(identity(30)).toBe(30); });
  it('identity returns value 31', () => { expect(identity(31)).toBe(31); });
  it('identity returns value 32', () => { expect(identity(32)).toBe(32); });
  it('identity returns value 33', () => { expect(identity(33)).toBe(33); });
  it('identity returns value 34', () => { expect(identity(34)).toBe(34); });
  it('identity returns value 35', () => { expect(identity(35)).toBe(35); });
  it('identity returns value 36', () => { expect(identity(36)).toBe(36); });
  it('identity returns value 37', () => { expect(identity(37)).toBe(37); });
  it('identity returns value 38', () => { expect(identity(38)).toBe(38); });
  it('identity returns value 39', () => { expect(identity(39)).toBe(39); });
  it('identity returns value 40', () => { expect(identity(40)).toBe(40); });
  it('identity returns value 41', () => { expect(identity(41)).toBe(41); });
  it('identity returns value 42', () => { expect(identity(42)).toBe(42); });
  it('identity returns value 43', () => { expect(identity(43)).toBe(43); });
  it('identity returns value 44', () => { expect(identity(44)).toBe(44); });
  it('identity returns value 45', () => { expect(identity(45)).toBe(45); });
  it('identity returns value 46', () => { expect(identity(46)).toBe(46); });
  it('identity returns value 47', () => { expect(identity(47)).toBe(47); });
  it('identity returns value 48', () => { expect(identity(48)).toBe(48); });
  it('identity returns value 49', () => { expect(identity(49)).toBe(49); });
  it('identity returns value 50', () => { expect(identity(50)).toBe(50); });
  it('constant returns fixed 1', () => { const c = constant(1); expect(c('anything')).toBe(1); expect(c(null)).toBe(1); });
  it('constant returns fixed 2', () => { const c = constant(2); expect(c('anything')).toBe(2); expect(c(null)).toBe(2); });
  it('constant returns fixed 3', () => { const c = constant(3); expect(c('anything')).toBe(3); expect(c(null)).toBe(3); });
  it('constant returns fixed 4', () => { const c = constant(4); expect(c('anything')).toBe(4); expect(c(null)).toBe(4); });
  it('constant returns fixed 5', () => { const c = constant(5); expect(c('anything')).toBe(5); expect(c(null)).toBe(5); });
  it('constant returns fixed 6', () => { const c = constant(6); expect(c('anything')).toBe(6); expect(c(null)).toBe(6); });
  it('constant returns fixed 7', () => { const c = constant(7); expect(c('anything')).toBe(7); expect(c(null)).toBe(7); });
  it('constant returns fixed 8', () => { const c = constant(8); expect(c('anything')).toBe(8); expect(c(null)).toBe(8); });
  it('constant returns fixed 9', () => { const c = constant(9); expect(c('anything')).toBe(9); expect(c(null)).toBe(9); });
  it('constant returns fixed 10', () => { const c = constant(10); expect(c('anything')).toBe(10); expect(c(null)).toBe(10); });
  it('constant returns fixed 11', () => { const c = constant(11); expect(c('anything')).toBe(11); expect(c(null)).toBe(11); });
  it('constant returns fixed 12', () => { const c = constant(12); expect(c('anything')).toBe(12); expect(c(null)).toBe(12); });
  it('constant returns fixed 13', () => { const c = constant(13); expect(c('anything')).toBe(13); expect(c(null)).toBe(13); });
  it('constant returns fixed 14', () => { const c = constant(14); expect(c('anything')).toBe(14); expect(c(null)).toBe(14); });
  it('constant returns fixed 15', () => { const c = constant(15); expect(c('anything')).toBe(15); expect(c(null)).toBe(15); });
  it('constant returns fixed 16', () => { const c = constant(16); expect(c('anything')).toBe(16); expect(c(null)).toBe(16); });
  it('constant returns fixed 17', () => { const c = constant(17); expect(c('anything')).toBe(17); expect(c(null)).toBe(17); });
  it('constant returns fixed 18', () => { const c = constant(18); expect(c('anything')).toBe(18); expect(c(null)).toBe(18); });
  it('constant returns fixed 19', () => { const c = constant(19); expect(c('anything')).toBe(19); expect(c(null)).toBe(19); });
  it('constant returns fixed 20', () => { const c = constant(20); expect(c('anything')).toBe(20); expect(c(null)).toBe(20); });
  it('constant returns fixed 21', () => { const c = constant(21); expect(c('anything')).toBe(21); expect(c(null)).toBe(21); });
  it('constant returns fixed 22', () => { const c = constant(22); expect(c('anything')).toBe(22); expect(c(null)).toBe(22); });
  it('constant returns fixed 23', () => { const c = constant(23); expect(c('anything')).toBe(23); expect(c(null)).toBe(23); });
  it('constant returns fixed 24', () => { const c = constant(24); expect(c('anything')).toBe(24); expect(c(null)).toBe(24); });
  it('constant returns fixed 25', () => { const c = constant(25); expect(c('anything')).toBe(25); expect(c(null)).toBe(25); });
  it('always returns fixed 1', () => { const a = always(1); expect(a()).toBe(1); });
  it('always returns fixed 2', () => { const a = always(2); expect(a()).toBe(2); });
  it('always returns fixed 3', () => { const a = always(3); expect(a()).toBe(3); });
  it('always returns fixed 4', () => { const a = always(4); expect(a()).toBe(4); });
  it('always returns fixed 5', () => { const a = always(5); expect(a()).toBe(5); });
  it('always returns fixed 6', () => { const a = always(6); expect(a()).toBe(6); });
  it('always returns fixed 7', () => { const a = always(7); expect(a()).toBe(7); });
  it('always returns fixed 8', () => { const a = always(8); expect(a()).toBe(8); });
  it('always returns fixed 9', () => { const a = always(9); expect(a()).toBe(9); });
  it('always returns fixed 10', () => { const a = always(10); expect(a()).toBe(10); });
  it('always returns fixed 11', () => { const a = always(11); expect(a()).toBe(11); });
  it('always returns fixed 12', () => { const a = always(12); expect(a()).toBe(12); });
  it('always returns fixed 13', () => { const a = always(13); expect(a()).toBe(13); });
  it('always returns fixed 14', () => { const a = always(14); expect(a()).toBe(14); });
  it('always returns fixed 15', () => { const a = always(15); expect(a()).toBe(15); });
  it('always returns fixed 16', () => { const a = always(16); expect(a()).toBe(16); });
  it('always returns fixed 17', () => { const a = always(17); expect(a()).toBe(17); });
  it('always returns fixed 18', () => { const a = always(18); expect(a()).toBe(18); });
  it('always returns fixed 19', () => { const a = always(19); expect(a()).toBe(19); });
  it('always returns fixed 20', () => { const a = always(20); expect(a()).toBe(20); });
  it('always returns fixed 21', () => { const a = always(21); expect(a()).toBe(21); });
  it('always returns fixed 22', () => { const a = always(22); expect(a()).toBe(22); });
  it('always returns fixed 23', () => { const a = always(23); expect(a()).toBe(23); });
  it('always returns fixed 24', () => { const a = always(24); expect(a()).toBe(24); });
  it('always returns fixed 25', () => { const a = always(25); expect(a()).toBe(25); });
});

describe('curry and partial', () => {
  it('curry2 1', () => { const add = curry2((a: number, b: number) => a + b); expect(add(1)(2)).toBe(3); });
  it('curry2 2', () => { const add = curry2((a: number, b: number) => a + b); expect(add(2)(3)).toBe(5); });
  it('curry2 3', () => { const add = curry2((a: number, b: number) => a + b); expect(add(3)(4)).toBe(7); });
  it('curry2 4', () => { const add = curry2((a: number, b: number) => a + b); expect(add(4)(5)).toBe(9); });
  it('curry2 5', () => { const add = curry2((a: number, b: number) => a + b); expect(add(5)(6)).toBe(11); });
  it('curry2 6', () => { const add = curry2((a: number, b: number) => a + b); expect(add(6)(7)).toBe(13); });
  it('curry2 7', () => { const add = curry2((a: number, b: number) => a + b); expect(add(7)(8)).toBe(15); });
  it('curry2 8', () => { const add = curry2((a: number, b: number) => a + b); expect(add(8)(9)).toBe(17); });
  it('curry2 9', () => { const add = curry2((a: number, b: number) => a + b); expect(add(9)(10)).toBe(19); });
  it('curry2 10', () => { const add = curry2((a: number, b: number) => a + b); expect(add(10)(11)).toBe(21); });
  it('curry2 11', () => { const add = curry2((a: number, b: number) => a + b); expect(add(11)(12)).toBe(23); });
  it('curry2 12', () => { const add = curry2((a: number, b: number) => a + b); expect(add(12)(13)).toBe(25); });
  it('curry2 13', () => { const add = curry2((a: number, b: number) => a + b); expect(add(13)(14)).toBe(27); });
  it('curry2 14', () => { const add = curry2((a: number, b: number) => a + b); expect(add(14)(15)).toBe(29); });
  it('curry2 15', () => { const add = curry2((a: number, b: number) => a + b); expect(add(15)(16)).toBe(31); });
  it('curry2 16', () => { const add = curry2((a: number, b: number) => a + b); expect(add(16)(17)).toBe(33); });
  it('curry2 17', () => { const add = curry2((a: number, b: number) => a + b); expect(add(17)(18)).toBe(35); });
  it('curry2 18', () => { const add = curry2((a: number, b: number) => a + b); expect(add(18)(19)).toBe(37); });
  it('curry2 19', () => { const add = curry2((a: number, b: number) => a + b); expect(add(19)(20)).toBe(39); });
  it('curry2 20', () => { const add = curry2((a: number, b: number) => a + b); expect(add(20)(21)).toBe(41); });
  it('curry2 21', () => { const add = curry2((a: number, b: number) => a + b); expect(add(21)(22)).toBe(43); });
  it('curry2 22', () => { const add = curry2((a: number, b: number) => a + b); expect(add(22)(23)).toBe(45); });
  it('curry2 23', () => { const add = curry2((a: number, b: number) => a + b); expect(add(23)(24)).toBe(47); });
  it('curry2 24', () => { const add = curry2((a: number, b: number) => a + b); expect(add(24)(25)).toBe(49); });
  it('curry2 25', () => { const add = curry2((a: number, b: number) => a + b); expect(add(25)(26)).toBe(51); });
  it('curry2 26', () => { const add = curry2((a: number, b: number) => a + b); expect(add(26)(27)).toBe(53); });
  it('curry2 27', () => { const add = curry2((a: number, b: number) => a + b); expect(add(27)(28)).toBe(55); });
  it('curry2 28', () => { const add = curry2((a: number, b: number) => a + b); expect(add(28)(29)).toBe(57); });
  it('curry2 29', () => { const add = curry2((a: number, b: number) => a + b); expect(add(29)(30)).toBe(59); });
  it('curry2 30', () => { const add = curry2((a: number, b: number) => a + b); expect(add(30)(31)).toBe(61); });
  it('curry2 31', () => { const add = curry2((a: number, b: number) => a + b); expect(add(31)(32)).toBe(63); });
  it('curry2 32', () => { const add = curry2((a: number, b: number) => a + b); expect(add(32)(33)).toBe(65); });
  it('curry2 33', () => { const add = curry2((a: number, b: number) => a + b); expect(add(33)(34)).toBe(67); });
  it('curry2 34', () => { const add = curry2((a: number, b: number) => a + b); expect(add(34)(35)).toBe(69); });
  it('curry2 35', () => { const add = curry2((a: number, b: number) => a + b); expect(add(35)(36)).toBe(71); });
  it('curry2 36', () => { const add = curry2((a: number, b: number) => a + b); expect(add(36)(37)).toBe(73); });
  it('curry2 37', () => { const add = curry2((a: number, b: number) => a + b); expect(add(37)(38)).toBe(75); });
  it('curry2 38', () => { const add = curry2((a: number, b: number) => a + b); expect(add(38)(39)).toBe(77); });
  it('curry2 39', () => { const add = curry2((a: number, b: number) => a + b); expect(add(39)(40)).toBe(79); });
  it('curry2 40', () => { const add = curry2((a: number, b: number) => a + b); expect(add(40)(41)).toBe(81); });
  it('curry2 41', () => { const add = curry2((a: number, b: number) => a + b); expect(add(41)(42)).toBe(83); });
  it('curry2 42', () => { const add = curry2((a: number, b: number) => a + b); expect(add(42)(43)).toBe(85); });
  it('curry2 43', () => { const add = curry2((a: number, b: number) => a + b); expect(add(43)(44)).toBe(87); });
  it('curry2 44', () => { const add = curry2((a: number, b: number) => a + b); expect(add(44)(45)).toBe(89); });
  it('curry2 45', () => { const add = curry2((a: number, b: number) => a + b); expect(add(45)(46)).toBe(91); });
  it('curry2 46', () => { const add = curry2((a: number, b: number) => a + b); expect(add(46)(47)).toBe(93); });
  it('curry2 47', () => { const add = curry2((a: number, b: number) => a + b); expect(add(47)(48)).toBe(95); });
  it('curry2 48', () => { const add = curry2((a: number, b: number) => a + b); expect(add(48)(49)).toBe(97); });
  it('curry2 49', () => { const add = curry2((a: number, b: number) => a + b); expect(add(49)(50)).toBe(99); });
  it('curry2 50', () => { const add = curry2((a: number, b: number) => a + b); expect(add(50)(51)).toBe(101); });
  it('curry3 1', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(1)(2)(3)).toBe(6); });
  it('curry3 2', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(2)(3)(4)).toBe(9); });
  it('curry3 3', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(3)(4)(5)).toBe(12); });
  it('curry3 4', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(4)(5)(6)).toBe(15); });
  it('curry3 5', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(5)(6)(7)).toBe(18); });
  it('curry3 6', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(6)(7)(8)).toBe(21); });
  it('curry3 7', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(7)(8)(9)).toBe(24); });
  it('curry3 8', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(8)(9)(10)).toBe(27); });
  it('curry3 9', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(9)(10)(11)).toBe(30); });
  it('curry3 10', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(10)(11)(12)).toBe(33); });
  it('curry3 11', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(11)(12)(13)).toBe(36); });
  it('curry3 12', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(12)(13)(14)).toBe(39); });
  it('curry3 13', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(13)(14)(15)).toBe(42); });
  it('curry3 14', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(14)(15)(16)).toBe(45); });
  it('curry3 15', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(15)(16)(17)).toBe(48); });
  it('curry3 16', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(16)(17)(18)).toBe(51); });
  it('curry3 17', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(17)(18)(19)).toBe(54); });
  it('curry3 18', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(18)(19)(20)).toBe(57); });
  it('curry3 19', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(19)(20)(21)).toBe(60); });
  it('curry3 20', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(20)(21)(22)).toBe(63); });
  it('curry3 21', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(21)(22)(23)).toBe(66); });
  it('curry3 22', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(22)(23)(24)).toBe(69); });
  it('curry3 23', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(23)(24)(25)).toBe(72); });
  it('curry3 24', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(24)(25)(26)).toBe(75); });
  it('curry3 25', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(25)(26)(27)).toBe(78); });
  it('curry3 26', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(26)(27)(28)).toBe(81); });
  it('curry3 27', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(27)(28)(29)).toBe(84); });
  it('curry3 28', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(28)(29)(30)).toBe(87); });
  it('curry3 29', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(29)(30)(31)).toBe(90); });
  it('curry3 30', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(30)(31)(32)).toBe(93); });
  it('curry3 31', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(31)(32)(33)).toBe(96); });
  it('curry3 32', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(32)(33)(34)).toBe(99); });
  it('curry3 33', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(33)(34)(35)).toBe(102); });
  it('curry3 34', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(34)(35)(36)).toBe(105); });
  it('curry3 35', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(35)(36)(37)).toBe(108); });
  it('curry3 36', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(36)(37)(38)).toBe(111); });
  it('curry3 37', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(37)(38)(39)).toBe(114); });
  it('curry3 38', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(38)(39)(40)).toBe(117); });
  it('curry3 39', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(39)(40)(41)).toBe(120); });
  it('curry3 40', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(40)(41)(42)).toBe(123); });
  it('curry3 41', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(41)(42)(43)).toBe(126); });
  it('curry3 42', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(42)(43)(44)).toBe(129); });
  it('curry3 43', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(43)(44)(45)).toBe(132); });
  it('curry3 44', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(44)(45)(46)).toBe(135); });
  it('curry3 45', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(45)(46)(47)).toBe(138); });
  it('curry3 46', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(46)(47)(48)).toBe(141); });
  it('curry3 47', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(47)(48)(49)).toBe(144); });
  it('curry3 48', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(48)(49)(50)).toBe(147); });
  it('curry3 49', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(49)(50)(51)).toBe(150); });
  it('curry3 50', () => { const add3 = curry3((a: number, b: number, c: number) => a+b+c); expect(add3(50)(51)(52)).toBe(153); });
  it('partial 1', () => { const add = partial((a: number, b: number) => a+b, 1); expect(add(2)).toBe(3); });
  it('partial 2', () => { const add = partial((a: number, b: number) => a+b, 2); expect(add(3)).toBe(5); });
  it('partial 3', () => { const add = partial((a: number, b: number) => a+b, 3); expect(add(4)).toBe(7); });
  it('partial 4', () => { const add = partial((a: number, b: number) => a+b, 4); expect(add(5)).toBe(9); });
  it('partial 5', () => { const add = partial((a: number, b: number) => a+b, 5); expect(add(6)).toBe(11); });
  it('partial 6', () => { const add = partial((a: number, b: number) => a+b, 6); expect(add(7)).toBe(13); });
  it('partial 7', () => { const add = partial((a: number, b: number) => a+b, 7); expect(add(8)).toBe(15); });
  it('partial 8', () => { const add = partial((a: number, b: number) => a+b, 8); expect(add(9)).toBe(17); });
  it('partial 9', () => { const add = partial((a: number, b: number) => a+b, 9); expect(add(10)).toBe(19); });
  it('partial 10', () => { const add = partial((a: number, b: number) => a+b, 10); expect(add(11)).toBe(21); });
  it('partial 11', () => { const add = partial((a: number, b: number) => a+b, 11); expect(add(12)).toBe(23); });
  it('partial 12', () => { const add = partial((a: number, b: number) => a+b, 12); expect(add(13)).toBe(25); });
  it('partial 13', () => { const add = partial((a: number, b: number) => a+b, 13); expect(add(14)).toBe(27); });
  it('partial 14', () => { const add = partial((a: number, b: number) => a+b, 14); expect(add(15)).toBe(29); });
  it('partial 15', () => { const add = partial((a: number, b: number) => a+b, 15); expect(add(16)).toBe(31); });
  it('partial 16', () => { const add = partial((a: number, b: number) => a+b, 16); expect(add(17)).toBe(33); });
  it('partial 17', () => { const add = partial((a: number, b: number) => a+b, 17); expect(add(18)).toBe(35); });
  it('partial 18', () => { const add = partial((a: number, b: number) => a+b, 18); expect(add(19)).toBe(37); });
  it('partial 19', () => { const add = partial((a: number, b: number) => a+b, 19); expect(add(20)).toBe(39); });
  it('partial 20', () => { const add = partial((a: number, b: number) => a+b, 20); expect(add(21)).toBe(41); });
  it('partial 21', () => { const add = partial((a: number, b: number) => a+b, 21); expect(add(22)).toBe(43); });
  it('partial 22', () => { const add = partial((a: number, b: number) => a+b, 22); expect(add(23)).toBe(45); });
  it('partial 23', () => { const add = partial((a: number, b: number) => a+b, 23); expect(add(24)).toBe(47); });
  it('partial 24', () => { const add = partial((a: number, b: number) => a+b, 24); expect(add(25)).toBe(49); });
  it('partial 25', () => { const add = partial((a: number, b: number) => a+b, 25); expect(add(26)).toBe(51); });
  it('flip 1', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(1,2)).toBe(1); });
  it('flip 2', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(2,3)).toBe(1); });
  it('flip 3', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(3,4)).toBe(1); });
  it('flip 4', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(4,5)).toBe(1); });
  it('flip 5', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(5,6)).toBe(1); });
  it('flip 6', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(6,7)).toBe(1); });
  it('flip 7', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(7,8)).toBe(1); });
  it('flip 8', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(8,9)).toBe(1); });
  it('flip 9', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(9,10)).toBe(1); });
  it('flip 10', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(10,11)).toBe(1); });
  it('flip 11', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(11,12)).toBe(1); });
  it('flip 12', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(12,13)).toBe(1); });
  it('flip 13', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(13,14)).toBe(1); });
  it('flip 14', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(14,15)).toBe(1); });
  it('flip 15', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(15,16)).toBe(1); });
  it('flip 16', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(16,17)).toBe(1); });
  it('flip 17', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(17,18)).toBe(1); });
  it('flip 18', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(18,19)).toBe(1); });
  it('flip 19', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(19,20)).toBe(1); });
  it('flip 20', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(20,21)).toBe(1); });
  it('flip 21', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(21,22)).toBe(1); });
  it('flip 22', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(22,23)).toBe(1); });
  it('flip 23', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(23,24)).toBe(1); });
  it('flip 24', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(24,25)).toBe(1); });
  it('flip 25', () => { const sub = flip((a: number, b: number) => a-b); expect(sub(25,26)).toBe(1); });
});

describe('memoize and once', () => {
  it('memoize returns cached 1', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*1; }); fn(1); fn(1); expect(calls).toBe(1); expect(fn(1)).toBe(1); });
  it('memoize returns cached 2', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*2; }); fn(2); fn(2); expect(calls).toBe(1); expect(fn(2)).toBe(4); });
  it('memoize returns cached 3', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*3; }); fn(3); fn(3); expect(calls).toBe(1); expect(fn(3)).toBe(9); });
  it('memoize returns cached 4', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*4; }); fn(4); fn(4); expect(calls).toBe(1); expect(fn(4)).toBe(16); });
  it('memoize returns cached 5', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*5; }); fn(5); fn(5); expect(calls).toBe(1); expect(fn(5)).toBe(25); });
  it('memoize returns cached 6', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*6; }); fn(6); fn(6); expect(calls).toBe(1); expect(fn(6)).toBe(36); });
  it('memoize returns cached 7', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*7; }); fn(7); fn(7); expect(calls).toBe(1); expect(fn(7)).toBe(49); });
  it('memoize returns cached 8', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*8; }); fn(8); fn(8); expect(calls).toBe(1); expect(fn(8)).toBe(64); });
  it('memoize returns cached 9', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*9; }); fn(9); fn(9); expect(calls).toBe(1); expect(fn(9)).toBe(81); });
  it('memoize returns cached 10', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*10; }); fn(10); fn(10); expect(calls).toBe(1); expect(fn(10)).toBe(100); });
  it('memoize returns cached 11', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*11; }); fn(11); fn(11); expect(calls).toBe(1); expect(fn(11)).toBe(121); });
  it('memoize returns cached 12', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*12; }); fn(12); fn(12); expect(calls).toBe(1); expect(fn(12)).toBe(144); });
  it('memoize returns cached 13', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*13; }); fn(13); fn(13); expect(calls).toBe(1); expect(fn(13)).toBe(169); });
  it('memoize returns cached 14', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*14; }); fn(14); fn(14); expect(calls).toBe(1); expect(fn(14)).toBe(196); });
  it('memoize returns cached 15', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*15; }); fn(15); fn(15); expect(calls).toBe(1); expect(fn(15)).toBe(225); });
  it('memoize returns cached 16', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*16; }); fn(16); fn(16); expect(calls).toBe(1); expect(fn(16)).toBe(256); });
  it('memoize returns cached 17', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*17; }); fn(17); fn(17); expect(calls).toBe(1); expect(fn(17)).toBe(289); });
  it('memoize returns cached 18', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*18; }); fn(18); fn(18); expect(calls).toBe(1); expect(fn(18)).toBe(324); });
  it('memoize returns cached 19', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*19; }); fn(19); fn(19); expect(calls).toBe(1); expect(fn(19)).toBe(361); });
  it('memoize returns cached 20', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*20; }); fn(20); fn(20); expect(calls).toBe(1); expect(fn(20)).toBe(400); });
  it('memoize returns cached 21', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*21; }); fn(21); fn(21); expect(calls).toBe(1); expect(fn(21)).toBe(441); });
  it('memoize returns cached 22', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*22; }); fn(22); fn(22); expect(calls).toBe(1); expect(fn(22)).toBe(484); });
  it('memoize returns cached 23', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*23; }); fn(23); fn(23); expect(calls).toBe(1); expect(fn(23)).toBe(529); });
  it('memoize returns cached 24', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*24; }); fn(24); fn(24); expect(calls).toBe(1); expect(fn(24)).toBe(576); });
  it('memoize returns cached 25', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*25; }); fn(25); fn(25); expect(calls).toBe(1); expect(fn(25)).toBe(625); });
  it('memoize returns cached 26', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*26; }); fn(26); fn(26); expect(calls).toBe(1); expect(fn(26)).toBe(676); });
  it('memoize returns cached 27', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*27; }); fn(27); fn(27); expect(calls).toBe(1); expect(fn(27)).toBe(729); });
  it('memoize returns cached 28', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*28; }); fn(28); fn(28); expect(calls).toBe(1); expect(fn(28)).toBe(784); });
  it('memoize returns cached 29', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*29; }); fn(29); fn(29); expect(calls).toBe(1); expect(fn(29)).toBe(841); });
  it('memoize returns cached 30', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*30; }); fn(30); fn(30); expect(calls).toBe(1); expect(fn(30)).toBe(900); });
  it('memoize returns cached 31', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*31; }); fn(31); fn(31); expect(calls).toBe(1); expect(fn(31)).toBe(961); });
  it('memoize returns cached 32', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*32; }); fn(32); fn(32); expect(calls).toBe(1); expect(fn(32)).toBe(1024); });
  it('memoize returns cached 33', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*33; }); fn(33); fn(33); expect(calls).toBe(1); expect(fn(33)).toBe(1089); });
  it('memoize returns cached 34', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*34; }); fn(34); fn(34); expect(calls).toBe(1); expect(fn(34)).toBe(1156); });
  it('memoize returns cached 35', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*35; }); fn(35); fn(35); expect(calls).toBe(1); expect(fn(35)).toBe(1225); });
  it('memoize returns cached 36', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*36; }); fn(36); fn(36); expect(calls).toBe(1); expect(fn(36)).toBe(1296); });
  it('memoize returns cached 37', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*37; }); fn(37); fn(37); expect(calls).toBe(1); expect(fn(37)).toBe(1369); });
  it('memoize returns cached 38', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*38; }); fn(38); fn(38); expect(calls).toBe(1); expect(fn(38)).toBe(1444); });
  it('memoize returns cached 39', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*39; }); fn(39); fn(39); expect(calls).toBe(1); expect(fn(39)).toBe(1521); });
  it('memoize returns cached 40', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*40; }); fn(40); fn(40); expect(calls).toBe(1); expect(fn(40)).toBe(1600); });
  it('memoize returns cached 41', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*41; }); fn(41); fn(41); expect(calls).toBe(1); expect(fn(41)).toBe(1681); });
  it('memoize returns cached 42', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*42; }); fn(42); fn(42); expect(calls).toBe(1); expect(fn(42)).toBe(1764); });
  it('memoize returns cached 43', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*43; }); fn(43); fn(43); expect(calls).toBe(1); expect(fn(43)).toBe(1849); });
  it('memoize returns cached 44', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*44; }); fn(44); fn(44); expect(calls).toBe(1); expect(fn(44)).toBe(1936); });
  it('memoize returns cached 45', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*45; }); fn(45); fn(45); expect(calls).toBe(1); expect(fn(45)).toBe(2025); });
  it('memoize returns cached 46', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*46; }); fn(46); fn(46); expect(calls).toBe(1); expect(fn(46)).toBe(2116); });
  it('memoize returns cached 47', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*47; }); fn(47); fn(47); expect(calls).toBe(1); expect(fn(47)).toBe(2209); });
  it('memoize returns cached 48', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*48; }); fn(48); fn(48); expect(calls).toBe(1); expect(fn(48)).toBe(2304); });
  it('memoize returns cached 49', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*49; }); fn(49); fn(49); expect(calls).toBe(1); expect(fn(49)).toBe(2401); });
  it('memoize returns cached 50', () => { let calls=0; const fn = memoize((x: number) => { calls++; return x*50; }); fn(50); fn(50); expect(calls).toBe(1); expect(fn(50)).toBe(2500); });
  it('once runs once 1', () => { let n=0; const fn = once(() => { n++; return 1; }); expect(fn()).toBe(1); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 2', () => { let n=0; const fn = once(() => { n++; return 2; }); expect(fn()).toBe(2); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 3', () => { let n=0; const fn = once(() => { n++; return 3; }); expect(fn()).toBe(3); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 4', () => { let n=0; const fn = once(() => { n++; return 4; }); expect(fn()).toBe(4); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 5', () => { let n=0; const fn = once(() => { n++; return 5; }); expect(fn()).toBe(5); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 6', () => { let n=0; const fn = once(() => { n++; return 6; }); expect(fn()).toBe(6); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 7', () => { let n=0; const fn = once(() => { n++; return 7; }); expect(fn()).toBe(7); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 8', () => { let n=0; const fn = once(() => { n++; return 8; }); expect(fn()).toBe(8); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 9', () => { let n=0; const fn = once(() => { n++; return 9; }); expect(fn()).toBe(9); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 10', () => { let n=0; const fn = once(() => { n++; return 10; }); expect(fn()).toBe(10); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 11', () => { let n=0; const fn = once(() => { n++; return 11; }); expect(fn()).toBe(11); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 12', () => { let n=0; const fn = once(() => { n++; return 12; }); expect(fn()).toBe(12); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 13', () => { let n=0; const fn = once(() => { n++; return 13; }); expect(fn()).toBe(13); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 14', () => { let n=0; const fn = once(() => { n++; return 14; }); expect(fn()).toBe(14); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 15', () => { let n=0; const fn = once(() => { n++; return 15; }); expect(fn()).toBe(15); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 16', () => { let n=0; const fn = once(() => { n++; return 16; }); expect(fn()).toBe(16); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 17', () => { let n=0; const fn = once(() => { n++; return 17; }); expect(fn()).toBe(17); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 18', () => { let n=0; const fn = once(() => { n++; return 18; }); expect(fn()).toBe(18); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 19', () => { let n=0; const fn = once(() => { n++; return 19; }); expect(fn()).toBe(19); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 20', () => { let n=0; const fn = once(() => { n++; return 20; }); expect(fn()).toBe(20); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 21', () => { let n=0; const fn = once(() => { n++; return 21; }); expect(fn()).toBe(21); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 22', () => { let n=0; const fn = once(() => { n++; return 22; }); expect(fn()).toBe(22); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 23', () => { let n=0; const fn = once(() => { n++; return 23; }); expect(fn()).toBe(23); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 24', () => { let n=0; const fn = once(() => { n++; return 24; }); expect(fn()).toBe(24); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 25', () => { let n=0; const fn = once(() => { n++; return 25; }); expect(fn()).toBe(25); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 26', () => { let n=0; const fn = once(() => { n++; return 26; }); expect(fn()).toBe(26); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 27', () => { let n=0; const fn = once(() => { n++; return 27; }); expect(fn()).toBe(27); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 28', () => { let n=0; const fn = once(() => { n++; return 28; }); expect(fn()).toBe(28); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 29', () => { let n=0; const fn = once(() => { n++; return 29; }); expect(fn()).toBe(29); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 30', () => { let n=0; const fn = once(() => { n++; return 30; }); expect(fn()).toBe(30); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 31', () => { let n=0; const fn = once(() => { n++; return 31; }); expect(fn()).toBe(31); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 32', () => { let n=0; const fn = once(() => { n++; return 32; }); expect(fn()).toBe(32); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 33', () => { let n=0; const fn = once(() => { n++; return 33; }); expect(fn()).toBe(33); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 34', () => { let n=0; const fn = once(() => { n++; return 34; }); expect(fn()).toBe(34); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 35', () => { let n=0; const fn = once(() => { n++; return 35; }); expect(fn()).toBe(35); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 36', () => { let n=0; const fn = once(() => { n++; return 36; }); expect(fn()).toBe(36); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 37', () => { let n=0; const fn = once(() => { n++; return 37; }); expect(fn()).toBe(37); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 38', () => { let n=0; const fn = once(() => { n++; return 38; }); expect(fn()).toBe(38); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 39', () => { let n=0; const fn = once(() => { n++; return 39; }); expect(fn()).toBe(39); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 40', () => { let n=0; const fn = once(() => { n++; return 40; }); expect(fn()).toBe(40); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 41', () => { let n=0; const fn = once(() => { n++; return 41; }); expect(fn()).toBe(41); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 42', () => { let n=0; const fn = once(() => { n++; return 42; }); expect(fn()).toBe(42); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 43', () => { let n=0; const fn = once(() => { n++; return 43; }); expect(fn()).toBe(43); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 44', () => { let n=0; const fn = once(() => { n++; return 44; }); expect(fn()).toBe(44); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 45', () => { let n=0; const fn = once(() => { n++; return 45; }); expect(fn()).toBe(45); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 46', () => { let n=0; const fn = once(() => { n++; return 46; }); expect(fn()).toBe(46); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 47', () => { let n=0; const fn = once(() => { n++; return 47; }); expect(fn()).toBe(47); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 48', () => { let n=0; const fn = once(() => { n++; return 48; }); expect(fn()).toBe(48); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 49', () => { let n=0; const fn = once(() => { n++; return 49; }); expect(fn()).toBe(49); fn(); fn(); expect(n).toBe(1); });
  it('once runs once 50', () => { let n=0; const fn = once(() => { n++; return 50; }); expect(fn()).toBe(50); fn(); fn(); expect(n).toBe(1); });
});

describe('tryCatch', () => {
  it('tryCatch success 1', () => { const r = tryCatch(() => 1); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(1); });
  it('tryCatch success 2', () => { const r = tryCatch(() => 2); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(2); });
  it('tryCatch success 3', () => { const r = tryCatch(() => 3); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(3); });
  it('tryCatch success 4', () => { const r = tryCatch(() => 4); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(4); });
  it('tryCatch success 5', () => { const r = tryCatch(() => 5); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(5); });
  it('tryCatch success 6', () => { const r = tryCatch(() => 6); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(6); });
  it('tryCatch success 7', () => { const r = tryCatch(() => 7); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(7); });
  it('tryCatch success 8', () => { const r = tryCatch(() => 8); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(8); });
  it('tryCatch success 9', () => { const r = tryCatch(() => 9); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(9); });
  it('tryCatch success 10', () => { const r = tryCatch(() => 10); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(10); });
  it('tryCatch success 11', () => { const r = tryCatch(() => 11); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(11); });
  it('tryCatch success 12', () => { const r = tryCatch(() => 12); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(12); });
  it('tryCatch success 13', () => { const r = tryCatch(() => 13); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(13); });
  it('tryCatch success 14', () => { const r = tryCatch(() => 14); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(14); });
  it('tryCatch success 15', () => { const r = tryCatch(() => 15); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(15); });
  it('tryCatch success 16', () => { const r = tryCatch(() => 16); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(16); });
  it('tryCatch success 17', () => { const r = tryCatch(() => 17); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(17); });
  it('tryCatch success 18', () => { const r = tryCatch(() => 18); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(18); });
  it('tryCatch success 19', () => { const r = tryCatch(() => 19); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(19); });
  it('tryCatch success 20', () => { const r = tryCatch(() => 20); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(20); });
  it('tryCatch success 21', () => { const r = tryCatch(() => 21); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(21); });
  it('tryCatch success 22', () => { const r = tryCatch(() => 22); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(22); });
  it('tryCatch success 23', () => { const r = tryCatch(() => 23); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(23); });
  it('tryCatch success 24', () => { const r = tryCatch(() => 24); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(24); });
  it('tryCatch success 25', () => { const r = tryCatch(() => 25); expect(r.success).toBe(true); if(r.success) expect(r.value).toBe(25); });
  it('tryCatch failure 1', () => { const r = tryCatch(() => { throw new Error('e1'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 2', () => { const r = tryCatch(() => { throw new Error('e2'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 3', () => { const r = tryCatch(() => { throw new Error('e3'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 4', () => { const r = tryCatch(() => { throw new Error('e4'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 5', () => { const r = tryCatch(() => { throw new Error('e5'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 6', () => { const r = tryCatch(() => { throw new Error('e6'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 7', () => { const r = tryCatch(() => { throw new Error('e7'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 8', () => { const r = tryCatch(() => { throw new Error('e8'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 9', () => { const r = tryCatch(() => { throw new Error('e9'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 10', () => { const r = tryCatch(() => { throw new Error('e10'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 11', () => { const r = tryCatch(() => { throw new Error('e11'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 12', () => { const r = tryCatch(() => { throw new Error('e12'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 13', () => { const r = tryCatch(() => { throw new Error('e13'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 14', () => { const r = tryCatch(() => { throw new Error('e14'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 15', () => { const r = tryCatch(() => { throw new Error('e15'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 16', () => { const r = tryCatch(() => { throw new Error('e16'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 17', () => { const r = tryCatch(() => { throw new Error('e17'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 18', () => { const r = tryCatch(() => { throw new Error('e18'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 19', () => { const r = tryCatch(() => { throw new Error('e19'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 20', () => { const r = tryCatch(() => { throw new Error('e20'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 21', () => { const r = tryCatch(() => { throw new Error('e21'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 22', () => { const r = tryCatch(() => { throw new Error('e22'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 23', () => { const r = tryCatch(() => { throw new Error('e23'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 24', () => { const r = tryCatch(() => { throw new Error('e24'); }); expect(r.success).toBe(false); });
  it('tryCatch failure 25', () => { const r = tryCatch(() => { throw new Error('e25'); }); expect(r.success).toBe(false); });
});

describe('Maybe monad', () => {
  it('some 1', () => { const m = some(1); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 2', () => { const m = some(2); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 3', () => { const m = some(3); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 4', () => { const m = some(4); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 5', () => { const m = some(5); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 6', () => { const m = some(6); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 7', () => { const m = some(7); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 8', () => { const m = some(8); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 9', () => { const m = some(9); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 10', () => { const m = some(10); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 11', () => { const m = some(11); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 12', () => { const m = some(12); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 13', () => { const m = some(13); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 14', () => { const m = some(14); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 15', () => { const m = some(15); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 16', () => { const m = some(16); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 17', () => { const m = some(17); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 18', () => { const m = some(18); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 19', () => { const m = some(19); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 20', () => { const m = some(20); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 21', () => { const m = some(21); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 22', () => { const m = some(22); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 23', () => { const m = some(23); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 24', () => { const m = some(24); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 25', () => { const m = some(25); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 26', () => { const m = some(26); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 27', () => { const m = some(27); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 28', () => { const m = some(28); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 29', () => { const m = some(29); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 30', () => { const m = some(30); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 31', () => { const m = some(31); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 32', () => { const m = some(32); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 33', () => { const m = some(33); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 34', () => { const m = some(34); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 35', () => { const m = some(35); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 36', () => { const m = some(36); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 37', () => { const m = some(37); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 38', () => { const m = some(38); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 39', () => { const m = some(39); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 40', () => { const m = some(40); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 41', () => { const m = some(41); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 42', () => { const m = some(42); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 43', () => { const m = some(43); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 44', () => { const m = some(44); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 45', () => { const m = some(45); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 46', () => { const m = some(46); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 47', () => { const m = some(47); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 48', () => { const m = some(48); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 49', () => { const m = some(49); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('some 50', () => { const m = some(50); expect(isSome(m)).toBe(true); expect(isNone(m)).toBe(false); });
  it('none 1', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 2', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 3', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 4', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 5', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 6', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 7', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 8', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 9', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 10', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 11', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 12', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 13', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 14', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 15', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 16', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 17', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 18', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 19', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 20', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 21', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 22', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 23', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 24', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 25', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 26', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 27', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 28', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 29', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 30', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 31', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 32', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 33', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 34', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 35', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 36', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 37', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 38', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 39', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 40', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 41', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 42', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 43', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 44', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 45', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 46', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 47', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 48', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 49', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('none 50', () => { const m = none(); expect(isNone(m)).toBe(true); expect(isSome(m)).toBe(false); });
  it('mapMaybe some 1', () => { const m = mapMaybe(some(1), x => x*2); expect(getOrElse(m, 0)).toBe(2); });
  it('mapMaybe some 2', () => { const m = mapMaybe(some(2), x => x*2); expect(getOrElse(m, 0)).toBe(4); });
  it('mapMaybe some 3', () => { const m = mapMaybe(some(3), x => x*2); expect(getOrElse(m, 0)).toBe(6); });
  it('mapMaybe some 4', () => { const m = mapMaybe(some(4), x => x*2); expect(getOrElse(m, 0)).toBe(8); });
  it('mapMaybe some 5', () => { const m = mapMaybe(some(5), x => x*2); expect(getOrElse(m, 0)).toBe(10); });
  it('mapMaybe some 6', () => { const m = mapMaybe(some(6), x => x*2); expect(getOrElse(m, 0)).toBe(12); });
  it('mapMaybe some 7', () => { const m = mapMaybe(some(7), x => x*2); expect(getOrElse(m, 0)).toBe(14); });
  it('mapMaybe some 8', () => { const m = mapMaybe(some(8), x => x*2); expect(getOrElse(m, 0)).toBe(16); });
  it('mapMaybe some 9', () => { const m = mapMaybe(some(9), x => x*2); expect(getOrElse(m, 0)).toBe(18); });
  it('mapMaybe some 10', () => { const m = mapMaybe(some(10), x => x*2); expect(getOrElse(m, 0)).toBe(20); });
  it('mapMaybe some 11', () => { const m = mapMaybe(some(11), x => x*2); expect(getOrElse(m, 0)).toBe(22); });
  it('mapMaybe some 12', () => { const m = mapMaybe(some(12), x => x*2); expect(getOrElse(m, 0)).toBe(24); });
  it('mapMaybe some 13', () => { const m = mapMaybe(some(13), x => x*2); expect(getOrElse(m, 0)).toBe(26); });
  it('mapMaybe some 14', () => { const m = mapMaybe(some(14), x => x*2); expect(getOrElse(m, 0)).toBe(28); });
  it('mapMaybe some 15', () => { const m = mapMaybe(some(15), x => x*2); expect(getOrElse(m, 0)).toBe(30); });
  it('mapMaybe some 16', () => { const m = mapMaybe(some(16), x => x*2); expect(getOrElse(m, 0)).toBe(32); });
  it('mapMaybe some 17', () => { const m = mapMaybe(some(17), x => x*2); expect(getOrElse(m, 0)).toBe(34); });
  it('mapMaybe some 18', () => { const m = mapMaybe(some(18), x => x*2); expect(getOrElse(m, 0)).toBe(36); });
  it('mapMaybe some 19', () => { const m = mapMaybe(some(19), x => x*2); expect(getOrElse(m, 0)).toBe(38); });
  it('mapMaybe some 20', () => { const m = mapMaybe(some(20), x => x*2); expect(getOrElse(m, 0)).toBe(40); });
  it('mapMaybe some 21', () => { const m = mapMaybe(some(21), x => x*2); expect(getOrElse(m, 0)).toBe(42); });
  it('mapMaybe some 22', () => { const m = mapMaybe(some(22), x => x*2); expect(getOrElse(m, 0)).toBe(44); });
  it('mapMaybe some 23', () => { const m = mapMaybe(some(23), x => x*2); expect(getOrElse(m, 0)).toBe(46); });
  it('mapMaybe some 24', () => { const m = mapMaybe(some(24), x => x*2); expect(getOrElse(m, 0)).toBe(48); });
  it('mapMaybe some 25', () => { const m = mapMaybe(some(25), x => x*2); expect(getOrElse(m, 0)).toBe(50); });
  it('mapMaybe some 26', () => { const m = mapMaybe(some(26), x => x*2); expect(getOrElse(m, 0)).toBe(52); });
  it('mapMaybe some 27', () => { const m = mapMaybe(some(27), x => x*2); expect(getOrElse(m, 0)).toBe(54); });
  it('mapMaybe some 28', () => { const m = mapMaybe(some(28), x => x*2); expect(getOrElse(m, 0)).toBe(56); });
  it('mapMaybe some 29', () => { const m = mapMaybe(some(29), x => x*2); expect(getOrElse(m, 0)).toBe(58); });
  it('mapMaybe some 30', () => { const m = mapMaybe(some(30), x => x*2); expect(getOrElse(m, 0)).toBe(60); });
  it('mapMaybe some 31', () => { const m = mapMaybe(some(31), x => x*2); expect(getOrElse(m, 0)).toBe(62); });
  it('mapMaybe some 32', () => { const m = mapMaybe(some(32), x => x*2); expect(getOrElse(m, 0)).toBe(64); });
  it('mapMaybe some 33', () => { const m = mapMaybe(some(33), x => x*2); expect(getOrElse(m, 0)).toBe(66); });
  it('mapMaybe some 34', () => { const m = mapMaybe(some(34), x => x*2); expect(getOrElse(m, 0)).toBe(68); });
  it('mapMaybe some 35', () => { const m = mapMaybe(some(35), x => x*2); expect(getOrElse(m, 0)).toBe(70); });
  it('mapMaybe some 36', () => { const m = mapMaybe(some(36), x => x*2); expect(getOrElse(m, 0)).toBe(72); });
  it('mapMaybe some 37', () => { const m = mapMaybe(some(37), x => x*2); expect(getOrElse(m, 0)).toBe(74); });
  it('mapMaybe some 38', () => { const m = mapMaybe(some(38), x => x*2); expect(getOrElse(m, 0)).toBe(76); });
  it('mapMaybe some 39', () => { const m = mapMaybe(some(39), x => x*2); expect(getOrElse(m, 0)).toBe(78); });
  it('mapMaybe some 40', () => { const m = mapMaybe(some(40), x => x*2); expect(getOrElse(m, 0)).toBe(80); });
  it('mapMaybe some 41', () => { const m = mapMaybe(some(41), x => x*2); expect(getOrElse(m, 0)).toBe(82); });
  it('mapMaybe some 42', () => { const m = mapMaybe(some(42), x => x*2); expect(getOrElse(m, 0)).toBe(84); });
  it('mapMaybe some 43', () => { const m = mapMaybe(some(43), x => x*2); expect(getOrElse(m, 0)).toBe(86); });
  it('mapMaybe some 44', () => { const m = mapMaybe(some(44), x => x*2); expect(getOrElse(m, 0)).toBe(88); });
  it('mapMaybe some 45', () => { const m = mapMaybe(some(45), x => x*2); expect(getOrElse(m, 0)).toBe(90); });
  it('mapMaybe some 46', () => { const m = mapMaybe(some(46), x => x*2); expect(getOrElse(m, 0)).toBe(92); });
  it('mapMaybe some 47', () => { const m = mapMaybe(some(47), x => x*2); expect(getOrElse(m, 0)).toBe(94); });
  it('mapMaybe some 48', () => { const m = mapMaybe(some(48), x => x*2); expect(getOrElse(m, 0)).toBe(96); });
  it('mapMaybe some 49', () => { const m = mapMaybe(some(49), x => x*2); expect(getOrElse(m, 0)).toBe(98); });
  it('mapMaybe some 50', () => { const m = mapMaybe(some(50), x => x*2); expect(getOrElse(m, 0)).toBe(100); });
  it('fromNullable non-null 1', () => { const m = fromNullable(1); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 2', () => { const m = fromNullable(2); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 3', () => { const m = fromNullable(3); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 4', () => { const m = fromNullable(4); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 5', () => { const m = fromNullable(5); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 6', () => { const m = fromNullable(6); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 7', () => { const m = fromNullable(7); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 8', () => { const m = fromNullable(8); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 9', () => { const m = fromNullable(9); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 10', () => { const m = fromNullable(10); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 11', () => { const m = fromNullable(11); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 12', () => { const m = fromNullable(12); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 13', () => { const m = fromNullable(13); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 14', () => { const m = fromNullable(14); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 15', () => { const m = fromNullable(15); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 16', () => { const m = fromNullable(16); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 17', () => { const m = fromNullable(17); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 18', () => { const m = fromNullable(18); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 19', () => { const m = fromNullable(19); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 20', () => { const m = fromNullable(20); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 21', () => { const m = fromNullable(21); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 22', () => { const m = fromNullable(22); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 23', () => { const m = fromNullable(23); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 24', () => { const m = fromNullable(24); expect(isSome(m)).toBe(true); });
  it('fromNullable non-null 25', () => { const m = fromNullable(25); expect(isSome(m)).toBe(true); });
  it('fromNullable null 1', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 2', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 3', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 4', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 5', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 6', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 7', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 8', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 9', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 10', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 11', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 12', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 13', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 14', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 15', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 16', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 17', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 18', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 19', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 20', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 21', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 22', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 23', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 24', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
  it('fromNullable null 25', () => { const m = fromNullable(null); expect(isNone(m)).toBe(true); });
});

describe('Either monad', () => {
  it('right 1', () => { const e = right(1); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 2', () => { const e = right(2); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 3', () => { const e = right(3); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 4', () => { const e = right(4); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 5', () => { const e = right(5); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 6', () => { const e = right(6); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 7', () => { const e = right(7); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 8', () => { const e = right(8); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 9', () => { const e = right(9); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 10', () => { const e = right(10); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 11', () => { const e = right(11); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 12', () => { const e = right(12); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 13', () => { const e = right(13); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 14', () => { const e = right(14); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 15', () => { const e = right(15); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 16', () => { const e = right(16); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 17', () => { const e = right(17); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 18', () => { const e = right(18); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 19', () => { const e = right(19); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 20', () => { const e = right(20); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 21', () => { const e = right(21); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 22', () => { const e = right(22); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 23', () => { const e = right(23); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 24', () => { const e = right(24); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 25', () => { const e = right(25); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 26', () => { const e = right(26); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 27', () => { const e = right(27); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 28', () => { const e = right(28); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 29', () => { const e = right(29); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 30', () => { const e = right(30); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 31', () => { const e = right(31); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 32', () => { const e = right(32); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 33', () => { const e = right(33); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 34', () => { const e = right(34); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 35', () => { const e = right(35); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 36', () => { const e = right(36); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 37', () => { const e = right(37); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 38', () => { const e = right(38); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 39', () => { const e = right(39); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 40', () => { const e = right(40); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 41', () => { const e = right(41); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 42', () => { const e = right(42); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 43', () => { const e = right(43); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 44', () => { const e = right(44); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 45', () => { const e = right(45); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 46', () => { const e = right(46); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 47', () => { const e = right(47); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 48', () => { const e = right(48); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 49', () => { const e = right(49); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('right 50', () => { const e = right(50); expect(isRight(e)).toBe(true); expect(isLeft(e)).toBe(false); });
  it('left 1', () => { const e = left('err1'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 2', () => { const e = left('err2'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 3', () => { const e = left('err3'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 4', () => { const e = left('err4'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 5', () => { const e = left('err5'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 6', () => { const e = left('err6'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 7', () => { const e = left('err7'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 8', () => { const e = left('err8'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 9', () => { const e = left('err9'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 10', () => { const e = left('err10'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 11', () => { const e = left('err11'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 12', () => { const e = left('err12'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 13', () => { const e = left('err13'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 14', () => { const e = left('err14'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 15', () => { const e = left('err15'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 16', () => { const e = left('err16'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 17', () => { const e = left('err17'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 18', () => { const e = left('err18'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 19', () => { const e = left('err19'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 20', () => { const e = left('err20'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 21', () => { const e = left('err21'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 22', () => { const e = left('err22'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 23', () => { const e = left('err23'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 24', () => { const e = left('err24'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 25', () => { const e = left('err25'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 26', () => { const e = left('err26'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 27', () => { const e = left('err27'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 28', () => { const e = left('err28'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 29', () => { const e = left('err29'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 30', () => { const e = left('err30'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 31', () => { const e = left('err31'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 32', () => { const e = left('err32'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 33', () => { const e = left('err33'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 34', () => { const e = left('err34'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 35', () => { const e = left('err35'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 36', () => { const e = left('err36'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 37', () => { const e = left('err37'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 38', () => { const e = left('err38'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 39', () => { const e = left('err39'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 40', () => { const e = left('err40'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 41', () => { const e = left('err41'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 42', () => { const e = left('err42'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 43', () => { const e = left('err43'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 44', () => { const e = left('err44'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 45', () => { const e = left('err45'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 46', () => { const e = left('err46'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 47', () => { const e = left('err47'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 48', () => { const e = left('err48'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 49', () => { const e = left('err49'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('left 50', () => { const e = left('err50'); expect(isLeft(e)).toBe(true); expect(isRight(e)).toBe(false); });
  it('mapRight 1', () => { const e = mapRight(right(1), x => x*2); if(isRight(e)) expect(e.right).toBe(2); });
  it('mapRight 2', () => { const e = mapRight(right(2), x => x*2); if(isRight(e)) expect(e.right).toBe(4); });
  it('mapRight 3', () => { const e = mapRight(right(3), x => x*2); if(isRight(e)) expect(e.right).toBe(6); });
  it('mapRight 4', () => { const e = mapRight(right(4), x => x*2); if(isRight(e)) expect(e.right).toBe(8); });
  it('mapRight 5', () => { const e = mapRight(right(5), x => x*2); if(isRight(e)) expect(e.right).toBe(10); });
  it('mapRight 6', () => { const e = mapRight(right(6), x => x*2); if(isRight(e)) expect(e.right).toBe(12); });
  it('mapRight 7', () => { const e = mapRight(right(7), x => x*2); if(isRight(e)) expect(e.right).toBe(14); });
  it('mapRight 8', () => { const e = mapRight(right(8), x => x*2); if(isRight(e)) expect(e.right).toBe(16); });
  it('mapRight 9', () => { const e = mapRight(right(9), x => x*2); if(isRight(e)) expect(e.right).toBe(18); });
  it('mapRight 10', () => { const e = mapRight(right(10), x => x*2); if(isRight(e)) expect(e.right).toBe(20); });
  it('mapRight 11', () => { const e = mapRight(right(11), x => x*2); if(isRight(e)) expect(e.right).toBe(22); });
  it('mapRight 12', () => { const e = mapRight(right(12), x => x*2); if(isRight(e)) expect(e.right).toBe(24); });
  it('mapRight 13', () => { const e = mapRight(right(13), x => x*2); if(isRight(e)) expect(e.right).toBe(26); });
  it('mapRight 14', () => { const e = mapRight(right(14), x => x*2); if(isRight(e)) expect(e.right).toBe(28); });
  it('mapRight 15', () => { const e = mapRight(right(15), x => x*2); if(isRight(e)) expect(e.right).toBe(30); });
  it('mapRight 16', () => { const e = mapRight(right(16), x => x*2); if(isRight(e)) expect(e.right).toBe(32); });
  it('mapRight 17', () => { const e = mapRight(right(17), x => x*2); if(isRight(e)) expect(e.right).toBe(34); });
  it('mapRight 18', () => { const e = mapRight(right(18), x => x*2); if(isRight(e)) expect(e.right).toBe(36); });
  it('mapRight 19', () => { const e = mapRight(right(19), x => x*2); if(isRight(e)) expect(e.right).toBe(38); });
  it('mapRight 20', () => { const e = mapRight(right(20), x => x*2); if(isRight(e)) expect(e.right).toBe(40); });
  it('mapRight 21', () => { const e = mapRight(right(21), x => x*2); if(isRight(e)) expect(e.right).toBe(42); });
  it('mapRight 22', () => { const e = mapRight(right(22), x => x*2); if(isRight(e)) expect(e.right).toBe(44); });
  it('mapRight 23', () => { const e = mapRight(right(23), x => x*2); if(isRight(e)) expect(e.right).toBe(46); });
  it('mapRight 24', () => { const e = mapRight(right(24), x => x*2); if(isRight(e)) expect(e.right).toBe(48); });
  it('mapRight 25', () => { const e = mapRight(right(25), x => x*2); if(isRight(e)) expect(e.right).toBe(50); });
  it('mapRight 26', () => { const e = mapRight(right(26), x => x*2); if(isRight(e)) expect(e.right).toBe(52); });
  it('mapRight 27', () => { const e = mapRight(right(27), x => x*2); if(isRight(e)) expect(e.right).toBe(54); });
  it('mapRight 28', () => { const e = mapRight(right(28), x => x*2); if(isRight(e)) expect(e.right).toBe(56); });
  it('mapRight 29', () => { const e = mapRight(right(29), x => x*2); if(isRight(e)) expect(e.right).toBe(58); });
  it('mapRight 30', () => { const e = mapRight(right(30), x => x*2); if(isRight(e)) expect(e.right).toBe(60); });
  it('mapRight 31', () => { const e = mapRight(right(31), x => x*2); if(isRight(e)) expect(e.right).toBe(62); });
  it('mapRight 32', () => { const e = mapRight(right(32), x => x*2); if(isRight(e)) expect(e.right).toBe(64); });
  it('mapRight 33', () => { const e = mapRight(right(33), x => x*2); if(isRight(e)) expect(e.right).toBe(66); });
  it('mapRight 34', () => { const e = mapRight(right(34), x => x*2); if(isRight(e)) expect(e.right).toBe(68); });
  it('mapRight 35', () => { const e = mapRight(right(35), x => x*2); if(isRight(e)) expect(e.right).toBe(70); });
  it('mapRight 36', () => { const e = mapRight(right(36), x => x*2); if(isRight(e)) expect(e.right).toBe(72); });
  it('mapRight 37', () => { const e = mapRight(right(37), x => x*2); if(isRight(e)) expect(e.right).toBe(74); });
  it('mapRight 38', () => { const e = mapRight(right(38), x => x*2); if(isRight(e)) expect(e.right).toBe(76); });
  it('mapRight 39', () => { const e = mapRight(right(39), x => x*2); if(isRight(e)) expect(e.right).toBe(78); });
  it('mapRight 40', () => { const e = mapRight(right(40), x => x*2); if(isRight(e)) expect(e.right).toBe(80); });
  it('mapRight 41', () => { const e = mapRight(right(41), x => x*2); if(isRight(e)) expect(e.right).toBe(82); });
  it('mapRight 42', () => { const e = mapRight(right(42), x => x*2); if(isRight(e)) expect(e.right).toBe(84); });
  it('mapRight 43', () => { const e = mapRight(right(43), x => x*2); if(isRight(e)) expect(e.right).toBe(86); });
  it('mapRight 44', () => { const e = mapRight(right(44), x => x*2); if(isRight(e)) expect(e.right).toBe(88); });
  it('mapRight 45', () => { const e = mapRight(right(45), x => x*2); if(isRight(e)) expect(e.right).toBe(90); });
  it('mapRight 46', () => { const e = mapRight(right(46), x => x*2); if(isRight(e)) expect(e.right).toBe(92); });
  it('mapRight 47', () => { const e = mapRight(right(47), x => x*2); if(isRight(e)) expect(e.right).toBe(94); });
  it('mapRight 48', () => { const e = mapRight(right(48), x => x*2); if(isRight(e)) expect(e.right).toBe(96); });
  it('mapRight 49', () => { const e = mapRight(right(49), x => x*2); if(isRight(e)) expect(e.right).toBe(98); });
  it('mapRight 50', () => { const e = mapRight(right(50), x => x*2); if(isRight(e)) expect(e.right).toBe(100); });
  it('fold right 1', () => { const r = fold(right(1), _ => 0, v => v); expect(r).toBe(1); });
  it('fold right 2', () => { const r = fold(right(2), _ => 0, v => v); expect(r).toBe(2); });
  it('fold right 3', () => { const r = fold(right(3), _ => 0, v => v); expect(r).toBe(3); });
  it('fold right 4', () => { const r = fold(right(4), _ => 0, v => v); expect(r).toBe(4); });
  it('fold right 5', () => { const r = fold(right(5), _ => 0, v => v); expect(r).toBe(5); });
  it('fold right 6', () => { const r = fold(right(6), _ => 0, v => v); expect(r).toBe(6); });
  it('fold right 7', () => { const r = fold(right(7), _ => 0, v => v); expect(r).toBe(7); });
  it('fold right 8', () => { const r = fold(right(8), _ => 0, v => v); expect(r).toBe(8); });
  it('fold right 9', () => { const r = fold(right(9), _ => 0, v => v); expect(r).toBe(9); });
  it('fold right 10', () => { const r = fold(right(10), _ => 0, v => v); expect(r).toBe(10); });
  it('fold right 11', () => { const r = fold(right(11), _ => 0, v => v); expect(r).toBe(11); });
  it('fold right 12', () => { const r = fold(right(12), _ => 0, v => v); expect(r).toBe(12); });
  it('fold right 13', () => { const r = fold(right(13), _ => 0, v => v); expect(r).toBe(13); });
  it('fold right 14', () => { const r = fold(right(14), _ => 0, v => v); expect(r).toBe(14); });
  it('fold right 15', () => { const r = fold(right(15), _ => 0, v => v); expect(r).toBe(15); });
  it('fold right 16', () => { const r = fold(right(16), _ => 0, v => v); expect(r).toBe(16); });
  it('fold right 17', () => { const r = fold(right(17), _ => 0, v => v); expect(r).toBe(17); });
  it('fold right 18', () => { const r = fold(right(18), _ => 0, v => v); expect(r).toBe(18); });
  it('fold right 19', () => { const r = fold(right(19), _ => 0, v => v); expect(r).toBe(19); });
  it('fold right 20', () => { const r = fold(right(20), _ => 0, v => v); expect(r).toBe(20); });
  it('fold right 21', () => { const r = fold(right(21), _ => 0, v => v); expect(r).toBe(21); });
  it('fold right 22', () => { const r = fold(right(22), _ => 0, v => v); expect(r).toBe(22); });
  it('fold right 23', () => { const r = fold(right(23), _ => 0, v => v); expect(r).toBe(23); });
  it('fold right 24', () => { const r = fold(right(24), _ => 0, v => v); expect(r).toBe(24); });
  it('fold right 25', () => { const r = fold(right(25), _ => 0, v => v); expect(r).toBe(25); });
  it('fold left 1', () => { const r = fold(left(1), v => v, _ => 0); expect(r).toBe(1); });
  it('fold left 2', () => { const r = fold(left(2), v => v, _ => 0); expect(r).toBe(2); });
  it('fold left 3', () => { const r = fold(left(3), v => v, _ => 0); expect(r).toBe(3); });
  it('fold left 4', () => { const r = fold(left(4), v => v, _ => 0); expect(r).toBe(4); });
  it('fold left 5', () => { const r = fold(left(5), v => v, _ => 0); expect(r).toBe(5); });
  it('fold left 6', () => { const r = fold(left(6), v => v, _ => 0); expect(r).toBe(6); });
  it('fold left 7', () => { const r = fold(left(7), v => v, _ => 0); expect(r).toBe(7); });
  it('fold left 8', () => { const r = fold(left(8), v => v, _ => 0); expect(r).toBe(8); });
  it('fold left 9', () => { const r = fold(left(9), v => v, _ => 0); expect(r).toBe(9); });
  it('fold left 10', () => { const r = fold(left(10), v => v, _ => 0); expect(r).toBe(10); });
  it('fold left 11', () => { const r = fold(left(11), v => v, _ => 0); expect(r).toBe(11); });
  it('fold left 12', () => { const r = fold(left(12), v => v, _ => 0); expect(r).toBe(12); });
  it('fold left 13', () => { const r = fold(left(13), v => v, _ => 0); expect(r).toBe(13); });
  it('fold left 14', () => { const r = fold(left(14), v => v, _ => 0); expect(r).toBe(14); });
  it('fold left 15', () => { const r = fold(left(15), v => v, _ => 0); expect(r).toBe(15); });
  it('fold left 16', () => { const r = fold(left(16), v => v, _ => 0); expect(r).toBe(16); });
  it('fold left 17', () => { const r = fold(left(17), v => v, _ => 0); expect(r).toBe(17); });
  it('fold left 18', () => { const r = fold(left(18), v => v, _ => 0); expect(r).toBe(18); });
  it('fold left 19', () => { const r = fold(left(19), v => v, _ => 0); expect(r).toBe(19); });
  it('fold left 20', () => { const r = fold(left(20), v => v, _ => 0); expect(r).toBe(20); });
  it('fold left 21', () => { const r = fold(left(21), v => v, _ => 0); expect(r).toBe(21); });
  it('fold left 22', () => { const r = fold(left(22), v => v, _ => 0); expect(r).toBe(22); });
  it('fold left 23', () => { const r = fold(left(23), v => v, _ => 0); expect(r).toBe(23); });
  it('fold left 24', () => { const r = fold(left(24), v => v, _ => 0); expect(r).toBe(24); });
  it('fold left 25', () => { const r = fold(left(25), v => v, _ => 0); expect(r).toBe(25); });
});

describe('iteration helpers', () => {
  it('iterate count 1', () => { const arr = iterate(0, x => x+1, 1); expect(arr.length).toBe(1); expect(arr[0]).toBe(0); });
  it('iterate count 2', () => { const arr = iterate(0, x => x+1, 2); expect(arr.length).toBe(2); expect(arr[1]).toBe(1); });
  it('iterate count 3', () => { const arr = iterate(0, x => x+1, 3); expect(arr.length).toBe(3); expect(arr[2]).toBe(2); });
  it('iterate count 4', () => { const arr = iterate(0, x => x+1, 4); expect(arr.length).toBe(4); expect(arr[3]).toBe(3); });
  it('iterate count 5', () => { const arr = iterate(0, x => x+1, 5); expect(arr.length).toBe(5); expect(arr[4]).toBe(4); });
  it('iterate count 6', () => { const arr = iterate(0, x => x+1, 6); expect(arr.length).toBe(6); expect(arr[5]).toBe(5); });
  it('iterate count 7', () => { const arr = iterate(0, x => x+1, 7); expect(arr.length).toBe(7); expect(arr[6]).toBe(6); });
  it('iterate count 8', () => { const arr = iterate(0, x => x+1, 8); expect(arr.length).toBe(8); expect(arr[7]).toBe(7); });
  it('iterate count 9', () => { const arr = iterate(0, x => x+1, 9); expect(arr.length).toBe(9); expect(arr[8]).toBe(8); });
  it('iterate count 10', () => { const arr = iterate(0, x => x+1, 10); expect(arr.length).toBe(10); expect(arr[9]).toBe(9); });
  it('iterate count 11', () => { const arr = iterate(0, x => x+1, 11); expect(arr.length).toBe(11); expect(arr[10]).toBe(10); });
  it('iterate count 12', () => { const arr = iterate(0, x => x+1, 12); expect(arr.length).toBe(12); expect(arr[11]).toBe(11); });
  it('iterate count 13', () => { const arr = iterate(0, x => x+1, 13); expect(arr.length).toBe(13); expect(arr[12]).toBe(12); });
  it('iterate count 14', () => { const arr = iterate(0, x => x+1, 14); expect(arr.length).toBe(14); expect(arr[13]).toBe(13); });
  it('iterate count 15', () => { const arr = iterate(0, x => x+1, 15); expect(arr.length).toBe(15); expect(arr[14]).toBe(14); });
  it('iterate count 16', () => { const arr = iterate(0, x => x+1, 16); expect(arr.length).toBe(16); expect(arr[15]).toBe(15); });
  it('iterate count 17', () => { const arr = iterate(0, x => x+1, 17); expect(arr.length).toBe(17); expect(arr[16]).toBe(16); });
  it('iterate count 18', () => { const arr = iterate(0, x => x+1, 18); expect(arr.length).toBe(18); expect(arr[17]).toBe(17); });
  it('iterate count 19', () => { const arr = iterate(0, x => x+1, 19); expect(arr.length).toBe(19); expect(arr[18]).toBe(18); });
  it('iterate count 20', () => { const arr = iterate(0, x => x+1, 20); expect(arr.length).toBe(20); expect(arr[19]).toBe(19); });
  it('iterate count 21', () => { const arr = iterate(0, x => x+1, 21); expect(arr.length).toBe(21); expect(arr[20]).toBe(20); });
  it('iterate count 22', () => { const arr = iterate(0, x => x+1, 22); expect(arr.length).toBe(22); expect(arr[21]).toBe(21); });
  it('iterate count 23', () => { const arr = iterate(0, x => x+1, 23); expect(arr.length).toBe(23); expect(arr[22]).toBe(22); });
  it('iterate count 24', () => { const arr = iterate(0, x => x+1, 24); expect(arr.length).toBe(24); expect(arr[23]).toBe(23); });
  it('iterate count 25', () => { const arr = iterate(0, x => x+1, 25); expect(arr.length).toBe(25); expect(arr[24]).toBe(24); });
  it('iterate count 26', () => { const arr = iterate(0, x => x+1, 26); expect(arr.length).toBe(26); expect(arr[25]).toBe(25); });
  it('iterate count 27', () => { const arr = iterate(0, x => x+1, 27); expect(arr.length).toBe(27); expect(arr[26]).toBe(26); });
  it('iterate count 28', () => { const arr = iterate(0, x => x+1, 28); expect(arr.length).toBe(28); expect(arr[27]).toBe(27); });
  it('iterate count 29', () => { const arr = iterate(0, x => x+1, 29); expect(arr.length).toBe(29); expect(arr[28]).toBe(28); });
  it('iterate count 30', () => { const arr = iterate(0, x => x+1, 30); expect(arr.length).toBe(30); expect(arr[29]).toBe(29); });
  it('iterate count 31', () => { const arr = iterate(0, x => x+1, 31); expect(arr.length).toBe(31); expect(arr[30]).toBe(30); });
  it('iterate count 32', () => { const arr = iterate(0, x => x+1, 32); expect(arr.length).toBe(32); expect(arr[31]).toBe(31); });
  it('iterate count 33', () => { const arr = iterate(0, x => x+1, 33); expect(arr.length).toBe(33); expect(arr[32]).toBe(32); });
  it('iterate count 34', () => { const arr = iterate(0, x => x+1, 34); expect(arr.length).toBe(34); expect(arr[33]).toBe(33); });
  it('iterate count 35', () => { const arr = iterate(0, x => x+1, 35); expect(arr.length).toBe(35); expect(arr[34]).toBe(34); });
  it('iterate count 36', () => { const arr = iterate(0, x => x+1, 36); expect(arr.length).toBe(36); expect(arr[35]).toBe(35); });
  it('iterate count 37', () => { const arr = iterate(0, x => x+1, 37); expect(arr.length).toBe(37); expect(arr[36]).toBe(36); });
  it('iterate count 38', () => { const arr = iterate(0, x => x+1, 38); expect(arr.length).toBe(38); expect(arr[37]).toBe(37); });
  it('iterate count 39', () => { const arr = iterate(0, x => x+1, 39); expect(arr.length).toBe(39); expect(arr[38]).toBe(38); });
  it('iterate count 40', () => { const arr = iterate(0, x => x+1, 40); expect(arr.length).toBe(40); expect(arr[39]).toBe(39); });
  it('iterate count 41', () => { const arr = iterate(0, x => x+1, 41); expect(arr.length).toBe(41); expect(arr[40]).toBe(40); });
  it('iterate count 42', () => { const arr = iterate(0, x => x+1, 42); expect(arr.length).toBe(42); expect(arr[41]).toBe(41); });
  it('iterate count 43', () => { const arr = iterate(0, x => x+1, 43); expect(arr.length).toBe(43); expect(arr[42]).toBe(42); });
  it('iterate count 44', () => { const arr = iterate(0, x => x+1, 44); expect(arr.length).toBe(44); expect(arr[43]).toBe(43); });
  it('iterate count 45', () => { const arr = iterate(0, x => x+1, 45); expect(arr.length).toBe(45); expect(arr[44]).toBe(44); });
  it('iterate count 46', () => { const arr = iterate(0, x => x+1, 46); expect(arr.length).toBe(46); expect(arr[45]).toBe(45); });
  it('iterate count 47', () => { const arr = iterate(0, x => x+1, 47); expect(arr.length).toBe(47); expect(arr[46]).toBe(46); });
  it('iterate count 48', () => { const arr = iterate(0, x => x+1, 48); expect(arr.length).toBe(48); expect(arr[47]).toBe(47); });
  it('iterate count 49', () => { const arr = iterate(0, x => x+1, 49); expect(arr.length).toBe(49); expect(arr[48]).toBe(48); });
  it('iterate count 50', () => { const arr = iterate(0, x => x+1, 50); expect(arr.length).toBe(50); expect(arr[49]).toBe(49); });
  it('applyN 1 times', () => { const r = applyN((x: number) => x+1, 1, 0); expect(r).toBe(1); });
  it('applyN 2 times', () => { const r = applyN((x: number) => x+1, 2, 0); expect(r).toBe(2); });
  it('applyN 3 times', () => { const r = applyN((x: number) => x+1, 3, 0); expect(r).toBe(3); });
  it('applyN 4 times', () => { const r = applyN((x: number) => x+1, 4, 0); expect(r).toBe(4); });
  it('applyN 5 times', () => { const r = applyN((x: number) => x+1, 5, 0); expect(r).toBe(5); });
  it('applyN 6 times', () => { const r = applyN((x: number) => x+1, 6, 0); expect(r).toBe(6); });
  it('applyN 7 times', () => { const r = applyN((x: number) => x+1, 7, 0); expect(r).toBe(7); });
  it('applyN 8 times', () => { const r = applyN((x: number) => x+1, 8, 0); expect(r).toBe(8); });
  it('applyN 9 times', () => { const r = applyN((x: number) => x+1, 9, 0); expect(r).toBe(9); });
  it('applyN 10 times', () => { const r = applyN((x: number) => x+1, 10, 0); expect(r).toBe(10); });
  it('applyN 11 times', () => { const r = applyN((x: number) => x+1, 11, 0); expect(r).toBe(11); });
  it('applyN 12 times', () => { const r = applyN((x: number) => x+1, 12, 0); expect(r).toBe(12); });
  it('applyN 13 times', () => { const r = applyN((x: number) => x+1, 13, 0); expect(r).toBe(13); });
  it('applyN 14 times', () => { const r = applyN((x: number) => x+1, 14, 0); expect(r).toBe(14); });
  it('applyN 15 times', () => { const r = applyN((x: number) => x+1, 15, 0); expect(r).toBe(15); });
  it('applyN 16 times', () => { const r = applyN((x: number) => x+1, 16, 0); expect(r).toBe(16); });
  it('applyN 17 times', () => { const r = applyN((x: number) => x+1, 17, 0); expect(r).toBe(17); });
  it('applyN 18 times', () => { const r = applyN((x: number) => x+1, 18, 0); expect(r).toBe(18); });
  it('applyN 19 times', () => { const r = applyN((x: number) => x+1, 19, 0); expect(r).toBe(19); });
  it('applyN 20 times', () => { const r = applyN((x: number) => x+1, 20, 0); expect(r).toBe(20); });
  it('applyN 21 times', () => { const r = applyN((x: number) => x+1, 21, 0); expect(r).toBe(21); });
  it('applyN 22 times', () => { const r = applyN((x: number) => x+1, 22, 0); expect(r).toBe(22); });
  it('applyN 23 times', () => { const r = applyN((x: number) => x+1, 23, 0); expect(r).toBe(23); });
  it('applyN 24 times', () => { const r = applyN((x: number) => x+1, 24, 0); expect(r).toBe(24); });
  it('applyN 25 times', () => { const r = applyN((x: number) => x+1, 25, 0); expect(r).toBe(25); });
  it('applyN 26 times', () => { const r = applyN((x: number) => x+1, 26, 0); expect(r).toBe(26); });
  it('applyN 27 times', () => { const r = applyN((x: number) => x+1, 27, 0); expect(r).toBe(27); });
  it('applyN 28 times', () => { const r = applyN((x: number) => x+1, 28, 0); expect(r).toBe(28); });
  it('applyN 29 times', () => { const r = applyN((x: number) => x+1, 29, 0); expect(r).toBe(29); });
  it('applyN 30 times', () => { const r = applyN((x: number) => x+1, 30, 0); expect(r).toBe(30); });
  it('applyN 31 times', () => { const r = applyN((x: number) => x+1, 31, 0); expect(r).toBe(31); });
  it('applyN 32 times', () => { const r = applyN((x: number) => x+1, 32, 0); expect(r).toBe(32); });
  it('applyN 33 times', () => { const r = applyN((x: number) => x+1, 33, 0); expect(r).toBe(33); });
  it('applyN 34 times', () => { const r = applyN((x: number) => x+1, 34, 0); expect(r).toBe(34); });
  it('applyN 35 times', () => { const r = applyN((x: number) => x+1, 35, 0); expect(r).toBe(35); });
  it('applyN 36 times', () => { const r = applyN((x: number) => x+1, 36, 0); expect(r).toBe(36); });
  it('applyN 37 times', () => { const r = applyN((x: number) => x+1, 37, 0); expect(r).toBe(37); });
  it('applyN 38 times', () => { const r = applyN((x: number) => x+1, 38, 0); expect(r).toBe(38); });
  it('applyN 39 times', () => { const r = applyN((x: number) => x+1, 39, 0); expect(r).toBe(39); });
  it('applyN 40 times', () => { const r = applyN((x: number) => x+1, 40, 0); expect(r).toBe(40); });
  it('applyN 41 times', () => { const r = applyN((x: number) => x+1, 41, 0); expect(r).toBe(41); });
  it('applyN 42 times', () => { const r = applyN((x: number) => x+1, 42, 0); expect(r).toBe(42); });
  it('applyN 43 times', () => { const r = applyN((x: number) => x+1, 43, 0); expect(r).toBe(43); });
  it('applyN 44 times', () => { const r = applyN((x: number) => x+1, 44, 0); expect(r).toBe(44); });
  it('applyN 45 times', () => { const r = applyN((x: number) => x+1, 45, 0); expect(r).toBe(45); });
  it('applyN 46 times', () => { const r = applyN((x: number) => x+1, 46, 0); expect(r).toBe(46); });
  it('applyN 47 times', () => { const r = applyN((x: number) => x+1, 47, 0); expect(r).toBe(47); });
  it('applyN 48 times', () => { const r = applyN((x: number) => x+1, 48, 0); expect(r).toBe(48); });
  it('applyN 49 times', () => { const r = applyN((x: number) => x+1, 49, 0); expect(r).toBe(49); });
  it('applyN 50 times', () => { const r = applyN((x: number) => x+1, 50, 0); expect(r).toBe(50); });
  it('complement 1', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(3)).toBe(true); expect(isEven(2)).toBe(false); });
  it('complement 2', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(5)).toBe(true); expect(isEven(4)).toBe(false); });
  it('complement 3', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(7)).toBe(true); expect(isEven(6)).toBe(false); });
  it('complement 4', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(9)).toBe(true); expect(isEven(8)).toBe(false); });
  it('complement 5', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(11)).toBe(true); expect(isEven(10)).toBe(false); });
  it('complement 6', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(13)).toBe(true); expect(isEven(12)).toBe(false); });
  it('complement 7', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(15)).toBe(true); expect(isEven(14)).toBe(false); });
  it('complement 8', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(17)).toBe(true); expect(isEven(16)).toBe(false); });
  it('complement 9', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(19)).toBe(true); expect(isEven(18)).toBe(false); });
  it('complement 10', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(21)).toBe(true); expect(isEven(20)).toBe(false); });
  it('complement 11', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(23)).toBe(true); expect(isEven(22)).toBe(false); });
  it('complement 12', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(25)).toBe(true); expect(isEven(24)).toBe(false); });
  it('complement 13', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(27)).toBe(true); expect(isEven(26)).toBe(false); });
  it('complement 14', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(29)).toBe(true); expect(isEven(28)).toBe(false); });
  it('complement 15', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(31)).toBe(true); expect(isEven(30)).toBe(false); });
  it('complement 16', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(33)).toBe(true); expect(isEven(32)).toBe(false); });
  it('complement 17', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(35)).toBe(true); expect(isEven(34)).toBe(false); });
  it('complement 18', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(37)).toBe(true); expect(isEven(36)).toBe(false); });
  it('complement 19', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(39)).toBe(true); expect(isEven(38)).toBe(false); });
  it('complement 20', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(41)).toBe(true); expect(isEven(40)).toBe(false); });
  it('complement 21', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(43)).toBe(true); expect(isEven(42)).toBe(false); });
  it('complement 22', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(45)).toBe(true); expect(isEven(44)).toBe(false); });
  it('complement 23', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(47)).toBe(true); expect(isEven(46)).toBe(false); });
  it('complement 24', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(49)).toBe(true); expect(isEven(48)).toBe(false); });
  it('complement 25', () => { const isEven = complement((x: number) => x % 2 === 0); expect(isEven(51)).toBe(true); expect(isEven(50)).toBe(false); });
  it('juxt 1', () => { const fns = [(x: number) => x+1, (x: number) => x*1]; const r = juxt(fns)(1); expect(r[0]).toBe(2); expect(r[1]).toBe(1); });
  it('juxt 2', () => { const fns = [(x: number) => x+2, (x: number) => x*2]; const r = juxt(fns)(2); expect(r[0]).toBe(4); expect(r[1]).toBe(4); });
  it('juxt 3', () => { const fns = [(x: number) => x+3, (x: number) => x*3]; const r = juxt(fns)(3); expect(r[0]).toBe(6); expect(r[1]).toBe(9); });
  it('juxt 4', () => { const fns = [(x: number) => x+4, (x: number) => x*4]; const r = juxt(fns)(4); expect(r[0]).toBe(8); expect(r[1]).toBe(16); });
  it('juxt 5', () => { const fns = [(x: number) => x+5, (x: number) => x*5]; const r = juxt(fns)(5); expect(r[0]).toBe(10); expect(r[1]).toBe(25); });
  it('juxt 6', () => { const fns = [(x: number) => x+6, (x: number) => x*6]; const r = juxt(fns)(6); expect(r[0]).toBe(12); expect(r[1]).toBe(36); });
  it('juxt 7', () => { const fns = [(x: number) => x+7, (x: number) => x*7]; const r = juxt(fns)(7); expect(r[0]).toBe(14); expect(r[1]).toBe(49); });
  it('juxt 8', () => { const fns = [(x: number) => x+8, (x: number) => x*8]; const r = juxt(fns)(8); expect(r[0]).toBe(16); expect(r[1]).toBe(64); });
  it('juxt 9', () => { const fns = [(x: number) => x+9, (x: number) => x*9]; const r = juxt(fns)(9); expect(r[0]).toBe(18); expect(r[1]).toBe(81); });
  it('juxt 10', () => { const fns = [(x: number) => x+10, (x: number) => x*10]; const r = juxt(fns)(10); expect(r[0]).toBe(20); expect(r[1]).toBe(100); });
  it('juxt 11', () => { const fns = [(x: number) => x+11, (x: number) => x*11]; const r = juxt(fns)(11); expect(r[0]).toBe(22); expect(r[1]).toBe(121); });
  it('juxt 12', () => { const fns = [(x: number) => x+12, (x: number) => x*12]; const r = juxt(fns)(12); expect(r[0]).toBe(24); expect(r[1]).toBe(144); });
  it('juxt 13', () => { const fns = [(x: number) => x+13, (x: number) => x*13]; const r = juxt(fns)(13); expect(r[0]).toBe(26); expect(r[1]).toBe(169); });
  it('juxt 14', () => { const fns = [(x: number) => x+14, (x: number) => x*14]; const r = juxt(fns)(14); expect(r[0]).toBe(28); expect(r[1]).toBe(196); });
  it('juxt 15', () => { const fns = [(x: number) => x+15, (x: number) => x*15]; const r = juxt(fns)(15); expect(r[0]).toBe(30); expect(r[1]).toBe(225); });
  it('juxt 16', () => { const fns = [(x: number) => x+16, (x: number) => x*16]; const r = juxt(fns)(16); expect(r[0]).toBe(32); expect(r[1]).toBe(256); });
  it('juxt 17', () => { const fns = [(x: number) => x+17, (x: number) => x*17]; const r = juxt(fns)(17); expect(r[0]).toBe(34); expect(r[1]).toBe(289); });
  it('juxt 18', () => { const fns = [(x: number) => x+18, (x: number) => x*18]; const r = juxt(fns)(18); expect(r[0]).toBe(36); expect(r[1]).toBe(324); });
  it('juxt 19', () => { const fns = [(x: number) => x+19, (x: number) => x*19]; const r = juxt(fns)(19); expect(r[0]).toBe(38); expect(r[1]).toBe(361); });
  it('juxt 20', () => { const fns = [(x: number) => x+20, (x: number) => x*20]; const r = juxt(fns)(20); expect(r[0]).toBe(40); expect(r[1]).toBe(400); });
  it('juxt 21', () => { const fns = [(x: number) => x+21, (x: number) => x*21]; const r = juxt(fns)(21); expect(r[0]).toBe(42); expect(r[1]).toBe(441); });
  it('juxt 22', () => { const fns = [(x: number) => x+22, (x: number) => x*22]; const r = juxt(fns)(22); expect(r[0]).toBe(44); expect(r[1]).toBe(484); });
  it('juxt 23', () => { const fns = [(x: number) => x+23, (x: number) => x*23]; const r = juxt(fns)(23); expect(r[0]).toBe(46); expect(r[1]).toBe(529); });
  it('juxt 24', () => { const fns = [(x: number) => x+24, (x: number) => x*24]; const r = juxt(fns)(24); expect(r[0]).toBe(48); expect(r[1]).toBe(576); });
  it('juxt 25', () => { const fns = [(x: number) => x+25, (x: number) => x*25]; const r = juxt(fns)(25); expect(r[0]).toBe(50); expect(r[1]).toBe(625); });
});

describe('extra functional-utils coverage', () => {
  it('flatMapMaybe some 1', () => { const m = flatMapMaybe(some(1), x => some(x*2)); expect(getOrElse(m, 0)).toBe(2); });
  it('flatMapMaybe some 2', () => { const m = flatMapMaybe(some(2), x => some(x*2)); expect(getOrElse(m, 0)).toBe(4); });
  it('flatMapMaybe some 3', () => { const m = flatMapMaybe(some(3), x => some(x*2)); expect(getOrElse(m, 0)).toBe(6); });
  it('flatMapMaybe some 4', () => { const m = flatMapMaybe(some(4), x => some(x*2)); expect(getOrElse(m, 0)).toBe(8); });
  it('flatMapMaybe some 5', () => { const m = flatMapMaybe(some(5), x => some(x*2)); expect(getOrElse(m, 0)).toBe(10); });
  it('flatMapMaybe some 6', () => { const m = flatMapMaybe(some(6), x => some(x*2)); expect(getOrElse(m, 0)).toBe(12); });
  it('flatMapMaybe some 7', () => { const m = flatMapMaybe(some(7), x => some(x*2)); expect(getOrElse(m, 0)).toBe(14); });
  it('flatMapMaybe some 8', () => { const m = flatMapMaybe(some(8), x => some(x*2)); expect(getOrElse(m, 0)).toBe(16); });
  it('flatMapMaybe some 9', () => { const m = flatMapMaybe(some(9), x => some(x*2)); expect(getOrElse(m, 0)).toBe(18); });
  it('flatMapMaybe some 10', () => { const m = flatMapMaybe(some(10), x => some(x*2)); expect(getOrElse(m, 0)).toBe(20); });
  it('flatMapMaybe some 11', () => { const m = flatMapMaybe(some(11), x => some(x*2)); expect(getOrElse(m, 0)).toBe(22); });
  it('flatMapMaybe some 12', () => { const m = flatMapMaybe(some(12), x => some(x*2)); expect(getOrElse(m, 0)).toBe(24); });
  it('flatMapMaybe some 13', () => { const m = flatMapMaybe(some(13), x => some(x*2)); expect(getOrElse(m, 0)).toBe(26); });
  it('flatMapMaybe some 14', () => { const m = flatMapMaybe(some(14), x => some(x*2)); expect(getOrElse(m, 0)).toBe(28); });
  it('flatMapMaybe some 15', () => { const m = flatMapMaybe(some(15), x => some(x*2)); expect(getOrElse(m, 0)).toBe(30); });
  it('flatMapMaybe some 16', () => { const m = flatMapMaybe(some(16), x => some(x*2)); expect(getOrElse(m, 0)).toBe(32); });
  it('flatMapMaybe some 17', () => { const m = flatMapMaybe(some(17), x => some(x*2)); expect(getOrElse(m, 0)).toBe(34); });
  it('flatMapMaybe some 18', () => { const m = flatMapMaybe(some(18), x => some(x*2)); expect(getOrElse(m, 0)).toBe(36); });
  it('flatMapMaybe some 19', () => { const m = flatMapMaybe(some(19), x => some(x*2)); expect(getOrElse(m, 0)).toBe(38); });
  it('flatMapMaybe some 20', () => { const m = flatMapMaybe(some(20), x => some(x*2)); expect(getOrElse(m, 0)).toBe(40); });
  it('flatMapMaybe some 21', () => { const m = flatMapMaybe(some(21), x => some(x*2)); expect(getOrElse(m, 0)).toBe(42); });
  it('flatMapMaybe some 22', () => { const m = flatMapMaybe(some(22), x => some(x*2)); expect(getOrElse(m, 0)).toBe(44); });
  it('flatMapMaybe some 23', () => { const m = flatMapMaybe(some(23), x => some(x*2)); expect(getOrElse(m, 0)).toBe(46); });
  it('flatMapMaybe some 24', () => { const m = flatMapMaybe(some(24), x => some(x*2)); expect(getOrElse(m, 0)).toBe(48); });
  it('flatMapMaybe some 25', () => { const m = flatMapMaybe(some(25), x => some(x*2)); expect(getOrElse(m, 0)).toBe(50); });
  it('flatMapMaybe some 26', () => { const m = flatMapMaybe(some(26), x => some(x*2)); expect(getOrElse(m, 0)).toBe(52); });
  it('flatMapMaybe some 27', () => { const m = flatMapMaybe(some(27), x => some(x*2)); expect(getOrElse(m, 0)).toBe(54); });
  it('flatMapMaybe some 28', () => { const m = flatMapMaybe(some(28), x => some(x*2)); expect(getOrElse(m, 0)).toBe(56); });
  it('flatMapMaybe some 29', () => { const m = flatMapMaybe(some(29), x => some(x*2)); expect(getOrElse(m, 0)).toBe(58); });
  it('flatMapMaybe some 30', () => { const m = flatMapMaybe(some(30), x => some(x*2)); expect(getOrElse(m, 0)).toBe(60); });
  it('flatMapMaybe some 31', () => { const m = flatMapMaybe(some(31), x => some(x*2)); expect(getOrElse(m, 0)).toBe(62); });
  it('flatMapMaybe some 32', () => { const m = flatMapMaybe(some(32), x => some(x*2)); expect(getOrElse(m, 0)).toBe(64); });
  it('flatMapMaybe some 33', () => { const m = flatMapMaybe(some(33), x => some(x*2)); expect(getOrElse(m, 0)).toBe(66); });
  it('flatMapMaybe some 34', () => { const m = flatMapMaybe(some(34), x => some(x*2)); expect(getOrElse(m, 0)).toBe(68); });
  it('flatMapMaybe some 35', () => { const m = flatMapMaybe(some(35), x => some(x*2)); expect(getOrElse(m, 0)).toBe(70); });
  it('flatMapMaybe some 36', () => { const m = flatMapMaybe(some(36), x => some(x*2)); expect(getOrElse(m, 0)).toBe(72); });
  it('flatMapMaybe some 37', () => { const m = flatMapMaybe(some(37), x => some(x*2)); expect(getOrElse(m, 0)).toBe(74); });
  it('flatMapMaybe some 38', () => { const m = flatMapMaybe(some(38), x => some(x*2)); expect(getOrElse(m, 0)).toBe(76); });
  it('flatMapMaybe some 39', () => { const m = flatMapMaybe(some(39), x => some(x*2)); expect(getOrElse(m, 0)).toBe(78); });
  it('flatMapMaybe some 40', () => { const m = flatMapMaybe(some(40), x => some(x*2)); expect(getOrElse(m, 0)).toBe(80); });
  it('flatMapMaybe some 41', () => { const m = flatMapMaybe(some(41), x => some(x*2)); expect(getOrElse(m, 0)).toBe(82); });
  it('flatMapMaybe some 42', () => { const m = flatMapMaybe(some(42), x => some(x*2)); expect(getOrElse(m, 0)).toBe(84); });
  it('flatMapMaybe some 43', () => { const m = flatMapMaybe(some(43), x => some(x*2)); expect(getOrElse(m, 0)).toBe(86); });
  it('flatMapMaybe some 44', () => { const m = flatMapMaybe(some(44), x => some(x*2)); expect(getOrElse(m, 0)).toBe(88); });
  it('flatMapMaybe some 45', () => { const m = flatMapMaybe(some(45), x => some(x*2)); expect(getOrElse(m, 0)).toBe(90); });
  it('flatMapMaybe some 46', () => { const m = flatMapMaybe(some(46), x => some(x*2)); expect(getOrElse(m, 0)).toBe(92); });
  it('flatMapMaybe some 47', () => { const m = flatMapMaybe(some(47), x => some(x*2)); expect(getOrElse(m, 0)).toBe(94); });
  it('flatMapMaybe some 48', () => { const m = flatMapMaybe(some(48), x => some(x*2)); expect(getOrElse(m, 0)).toBe(96); });
  it('flatMapMaybe some 49', () => { const m = flatMapMaybe(some(49), x => some(x*2)); expect(getOrElse(m, 0)).toBe(98); });
  it('flatMapMaybe some 50', () => { const m = flatMapMaybe(some(50), x => some(x*2)); expect(getOrElse(m, 0)).toBe(100); });
  it('mapMaybe none 1', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 2', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 3', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 4', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 5', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 6', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 7', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 8', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 9', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 10', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 11', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 12', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 13', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 14', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 15', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 16', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 17', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 18', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 19', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 20', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 21', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 22', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 23', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 24', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('mapMaybe none 25', () => { const m = mapMaybe(none(), (x: number) => x*2); expect(isNone(m)).toBe(true); });
  it('noop does nothing 1', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 2', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 3', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 4', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 5', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 6', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 7', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 8', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 9', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 10', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 11', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 12', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 13', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 14', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 15', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 16', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 17', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 18', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 19', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 20', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 21', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 22', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 23', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 24', () => { expect(() => noop()).not.toThrow(); });
  it('noop does nothing 25', () => { expect(() => noop()).not.toThrow(); });
});
