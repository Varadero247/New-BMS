// Copyright (c) 2026 Nexara DMCC. All rights reserved.
import { createStore, set, setIn, getIn, deleteKey, merge, produce, createSelector, createComputedSelector, createStateMachine, createHistory, pushHistory, undoHistory, redoHistory, canUndo, canRedo } from '../state-utils';

describe('createStore', () => {
  it('stores initial state 1', () => { const s = createStore(1); expect(s.getState()).toBe(1); });
  it('stores initial state 2', () => { const s = createStore(2); expect(s.getState()).toBe(2); });
  it('stores initial state 3', () => { const s = createStore(3); expect(s.getState()).toBe(3); });
  it('stores initial state 4', () => { const s = createStore(4); expect(s.getState()).toBe(4); });
  it('stores initial state 5', () => { const s = createStore(5); expect(s.getState()).toBe(5); });
  it('stores initial state 6', () => { const s = createStore(6); expect(s.getState()).toBe(6); });
  it('stores initial state 7', () => { const s = createStore(7); expect(s.getState()).toBe(7); });
  it('stores initial state 8', () => { const s = createStore(8); expect(s.getState()).toBe(8); });
  it('stores initial state 9', () => { const s = createStore(9); expect(s.getState()).toBe(9); });
  it('stores initial state 10', () => { const s = createStore(10); expect(s.getState()).toBe(10); });
  it('stores initial state 11', () => { const s = createStore(11); expect(s.getState()).toBe(11); });
  it('stores initial state 12', () => { const s = createStore(12); expect(s.getState()).toBe(12); });
  it('stores initial state 13', () => { const s = createStore(13); expect(s.getState()).toBe(13); });
  it('stores initial state 14', () => { const s = createStore(14); expect(s.getState()).toBe(14); });
  it('stores initial state 15', () => { const s = createStore(15); expect(s.getState()).toBe(15); });
  it('stores initial state 16', () => { const s = createStore(16); expect(s.getState()).toBe(16); });
  it('stores initial state 17', () => { const s = createStore(17); expect(s.getState()).toBe(17); });
  it('stores initial state 18', () => { const s = createStore(18); expect(s.getState()).toBe(18); });
  it('stores initial state 19', () => { const s = createStore(19); expect(s.getState()).toBe(19); });
  it('stores initial state 20', () => { const s = createStore(20); expect(s.getState()).toBe(20); });
  it('stores initial state 21', () => { const s = createStore(21); expect(s.getState()).toBe(21); });
  it('stores initial state 22', () => { const s = createStore(22); expect(s.getState()).toBe(22); });
  it('stores initial state 23', () => { const s = createStore(23); expect(s.getState()).toBe(23); });
  it('stores initial state 24', () => { const s = createStore(24); expect(s.getState()).toBe(24); });
  it('stores initial state 25', () => { const s = createStore(25); expect(s.getState()).toBe(25); });
  it('stores initial state 26', () => { const s = createStore(26); expect(s.getState()).toBe(26); });
  it('stores initial state 27', () => { const s = createStore(27); expect(s.getState()).toBe(27); });
  it('stores initial state 28', () => { const s = createStore(28); expect(s.getState()).toBe(28); });
  it('stores initial state 29', () => { const s = createStore(29); expect(s.getState()).toBe(29); });
  it('stores initial state 30', () => { const s = createStore(30); expect(s.getState()).toBe(30); });
  it('stores initial state 31', () => { const s = createStore(31); expect(s.getState()).toBe(31); });
  it('stores initial state 32', () => { const s = createStore(32); expect(s.getState()).toBe(32); });
  it('stores initial state 33', () => { const s = createStore(33); expect(s.getState()).toBe(33); });
  it('stores initial state 34', () => { const s = createStore(34); expect(s.getState()).toBe(34); });
  it('stores initial state 35', () => { const s = createStore(35); expect(s.getState()).toBe(35); });
  it('stores initial state 36', () => { const s = createStore(36); expect(s.getState()).toBe(36); });
  it('stores initial state 37', () => { const s = createStore(37); expect(s.getState()).toBe(37); });
  it('stores initial state 38', () => { const s = createStore(38); expect(s.getState()).toBe(38); });
  it('stores initial state 39', () => { const s = createStore(39); expect(s.getState()).toBe(39); });
  it('stores initial state 40', () => { const s = createStore(40); expect(s.getState()).toBe(40); });
  it('stores initial state 41', () => { const s = createStore(41); expect(s.getState()).toBe(41); });
  it('stores initial state 42', () => { const s = createStore(42); expect(s.getState()).toBe(42); });
  it('stores initial state 43', () => { const s = createStore(43); expect(s.getState()).toBe(43); });
  it('stores initial state 44', () => { const s = createStore(44); expect(s.getState()).toBe(44); });
  it('stores initial state 45', () => { const s = createStore(45); expect(s.getState()).toBe(45); });
  it('stores initial state 46', () => { const s = createStore(46); expect(s.getState()).toBe(46); });
  it('stores initial state 47', () => { const s = createStore(47); expect(s.getState()).toBe(47); });
  it('stores initial state 48', () => { const s = createStore(48); expect(s.getState()).toBe(48); });
  it('stores initial state 49', () => { const s = createStore(49); expect(s.getState()).toBe(49); });
  it('stores initial state 50', () => { const s = createStore(50); expect(s.getState()).toBe(50); });
  it('setState value 1', () => { const s = createStore(0); s.setState(2); expect(s.getState()).toBe(2); });
  it('setState value 2', () => { const s = createStore(0); s.setState(4); expect(s.getState()).toBe(4); });
  it('setState value 3', () => { const s = createStore(0); s.setState(6); expect(s.getState()).toBe(6); });
  it('setState value 4', () => { const s = createStore(0); s.setState(8); expect(s.getState()).toBe(8); });
  it('setState value 5', () => { const s = createStore(0); s.setState(10); expect(s.getState()).toBe(10); });
  it('setState value 6', () => { const s = createStore(0); s.setState(12); expect(s.getState()).toBe(12); });
  it('setState value 7', () => { const s = createStore(0); s.setState(14); expect(s.getState()).toBe(14); });
  it('setState value 8', () => { const s = createStore(0); s.setState(16); expect(s.getState()).toBe(16); });
  it('setState value 9', () => { const s = createStore(0); s.setState(18); expect(s.getState()).toBe(18); });
  it('setState value 10', () => { const s = createStore(0); s.setState(20); expect(s.getState()).toBe(20); });
  it('setState value 11', () => { const s = createStore(0); s.setState(22); expect(s.getState()).toBe(22); });
  it('setState value 12', () => { const s = createStore(0); s.setState(24); expect(s.getState()).toBe(24); });
  it('setState value 13', () => { const s = createStore(0); s.setState(26); expect(s.getState()).toBe(26); });
  it('setState value 14', () => { const s = createStore(0); s.setState(28); expect(s.getState()).toBe(28); });
  it('setState value 15', () => { const s = createStore(0); s.setState(30); expect(s.getState()).toBe(30); });
  it('setState value 16', () => { const s = createStore(0); s.setState(32); expect(s.getState()).toBe(32); });
  it('setState value 17', () => { const s = createStore(0); s.setState(34); expect(s.getState()).toBe(34); });
  it('setState value 18', () => { const s = createStore(0); s.setState(36); expect(s.getState()).toBe(36); });
  it('setState value 19', () => { const s = createStore(0); s.setState(38); expect(s.getState()).toBe(38); });
  it('setState value 20', () => { const s = createStore(0); s.setState(40); expect(s.getState()).toBe(40); });
  it('setState value 21', () => { const s = createStore(0); s.setState(42); expect(s.getState()).toBe(42); });
  it('setState value 22', () => { const s = createStore(0); s.setState(44); expect(s.getState()).toBe(44); });
  it('setState value 23', () => { const s = createStore(0); s.setState(46); expect(s.getState()).toBe(46); });
  it('setState value 24', () => { const s = createStore(0); s.setState(48); expect(s.getState()).toBe(48); });
  it('setState value 25', () => { const s = createStore(0); s.setState(50); expect(s.getState()).toBe(50); });
  it('setState value 26', () => { const s = createStore(0); s.setState(52); expect(s.getState()).toBe(52); });
  it('setState value 27', () => { const s = createStore(0); s.setState(54); expect(s.getState()).toBe(54); });
  it('setState value 28', () => { const s = createStore(0); s.setState(56); expect(s.getState()).toBe(56); });
  it('setState value 29', () => { const s = createStore(0); s.setState(58); expect(s.getState()).toBe(58); });
  it('setState value 30', () => { const s = createStore(0); s.setState(60); expect(s.getState()).toBe(60); });
  it('setState value 31', () => { const s = createStore(0); s.setState(62); expect(s.getState()).toBe(62); });
  it('setState value 32', () => { const s = createStore(0); s.setState(64); expect(s.getState()).toBe(64); });
  it('setState value 33', () => { const s = createStore(0); s.setState(66); expect(s.getState()).toBe(66); });
  it('setState value 34', () => { const s = createStore(0); s.setState(68); expect(s.getState()).toBe(68); });
  it('setState value 35', () => { const s = createStore(0); s.setState(70); expect(s.getState()).toBe(70); });
  it('setState value 36', () => { const s = createStore(0); s.setState(72); expect(s.getState()).toBe(72); });
  it('setState value 37', () => { const s = createStore(0); s.setState(74); expect(s.getState()).toBe(74); });
  it('setState value 38', () => { const s = createStore(0); s.setState(76); expect(s.getState()).toBe(76); });
  it('setState value 39', () => { const s = createStore(0); s.setState(78); expect(s.getState()).toBe(78); });
  it('setState value 40', () => { const s = createStore(0); s.setState(80); expect(s.getState()).toBe(80); });
  it('setState value 41', () => { const s = createStore(0); s.setState(82); expect(s.getState()).toBe(82); });
  it('setState value 42', () => { const s = createStore(0); s.setState(84); expect(s.getState()).toBe(84); });
  it('setState value 43', () => { const s = createStore(0); s.setState(86); expect(s.getState()).toBe(86); });
  it('setState value 44', () => { const s = createStore(0); s.setState(88); expect(s.getState()).toBe(88); });
  it('setState value 45', () => { const s = createStore(0); s.setState(90); expect(s.getState()).toBe(90); });
  it('setState value 46', () => { const s = createStore(0); s.setState(92); expect(s.getState()).toBe(92); });
  it('setState value 47', () => { const s = createStore(0); s.setState(94); expect(s.getState()).toBe(94); });
  it('setState value 48', () => { const s = createStore(0); s.setState(96); expect(s.getState()).toBe(96); });
  it('setState value 49', () => { const s = createStore(0); s.setState(98); expect(s.getState()).toBe(98); });
  it('setState value 50', () => { const s = createStore(0); s.setState(100); expect(s.getState()).toBe(100); });
  it('setState updater 1', () => { const s = createStore(1); s.setState(x => x + 1); expect(s.getState()).toBe(2); });
  it('setState updater 2', () => { const s = createStore(2); s.setState(x => x + 1); expect(s.getState()).toBe(3); });
  it('setState updater 3', () => { const s = createStore(3); s.setState(x => x + 1); expect(s.getState()).toBe(4); });
  it('setState updater 4', () => { const s = createStore(4); s.setState(x => x + 1); expect(s.getState()).toBe(5); });
  it('setState updater 5', () => { const s = createStore(5); s.setState(x => x + 1); expect(s.getState()).toBe(6); });
  it('setState updater 6', () => { const s = createStore(6); s.setState(x => x + 1); expect(s.getState()).toBe(7); });
  it('setState updater 7', () => { const s = createStore(7); s.setState(x => x + 1); expect(s.getState()).toBe(8); });
  it('setState updater 8', () => { const s = createStore(8); s.setState(x => x + 1); expect(s.getState()).toBe(9); });
  it('setState updater 9', () => { const s = createStore(9); s.setState(x => x + 1); expect(s.getState()).toBe(10); });
  it('setState updater 10', () => { const s = createStore(10); s.setState(x => x + 1); expect(s.getState()).toBe(11); });
  it('setState updater 11', () => { const s = createStore(11); s.setState(x => x + 1); expect(s.getState()).toBe(12); });
  it('setState updater 12', () => { const s = createStore(12); s.setState(x => x + 1); expect(s.getState()).toBe(13); });
  it('setState updater 13', () => { const s = createStore(13); s.setState(x => x + 1); expect(s.getState()).toBe(14); });
  it('setState updater 14', () => { const s = createStore(14); s.setState(x => x + 1); expect(s.getState()).toBe(15); });
  it('setState updater 15', () => { const s = createStore(15); s.setState(x => x + 1); expect(s.getState()).toBe(16); });
  it('setState updater 16', () => { const s = createStore(16); s.setState(x => x + 1); expect(s.getState()).toBe(17); });
  it('setState updater 17', () => { const s = createStore(17); s.setState(x => x + 1); expect(s.getState()).toBe(18); });
  it('setState updater 18', () => { const s = createStore(18); s.setState(x => x + 1); expect(s.getState()).toBe(19); });
  it('setState updater 19', () => { const s = createStore(19); s.setState(x => x + 1); expect(s.getState()).toBe(20); });
  it('setState updater 20', () => { const s = createStore(20); s.setState(x => x + 1); expect(s.getState()).toBe(21); });
  it('setState updater 21', () => { const s = createStore(21); s.setState(x => x + 1); expect(s.getState()).toBe(22); });
  it('setState updater 22', () => { const s = createStore(22); s.setState(x => x + 1); expect(s.getState()).toBe(23); });
  it('setState updater 23', () => { const s = createStore(23); s.setState(x => x + 1); expect(s.getState()).toBe(24); });
  it('setState updater 24', () => { const s = createStore(24); s.setState(x => x + 1); expect(s.getState()).toBe(25); });
  it('setState updater 25', () => { const s = createStore(25); s.setState(x => x + 1); expect(s.getState()).toBe(26); });
  it('setState updater 26', () => { const s = createStore(26); s.setState(x => x + 1); expect(s.getState()).toBe(27); });
  it('setState updater 27', () => { const s = createStore(27); s.setState(x => x + 1); expect(s.getState()).toBe(28); });
  it('setState updater 28', () => { const s = createStore(28); s.setState(x => x + 1); expect(s.getState()).toBe(29); });
  it('setState updater 29', () => { const s = createStore(29); s.setState(x => x + 1); expect(s.getState()).toBe(30); });
  it('setState updater 30', () => { const s = createStore(30); s.setState(x => x + 1); expect(s.getState()).toBe(31); });
  it('setState updater 31', () => { const s = createStore(31); s.setState(x => x + 1); expect(s.getState()).toBe(32); });
  it('setState updater 32', () => { const s = createStore(32); s.setState(x => x + 1); expect(s.getState()).toBe(33); });
  it('setState updater 33', () => { const s = createStore(33); s.setState(x => x + 1); expect(s.getState()).toBe(34); });
  it('setState updater 34', () => { const s = createStore(34); s.setState(x => x + 1); expect(s.getState()).toBe(35); });
  it('setState updater 35', () => { const s = createStore(35); s.setState(x => x + 1); expect(s.getState()).toBe(36); });
  it('setState updater 36', () => { const s = createStore(36); s.setState(x => x + 1); expect(s.getState()).toBe(37); });
  it('setState updater 37', () => { const s = createStore(37); s.setState(x => x + 1); expect(s.getState()).toBe(38); });
  it('setState updater 38', () => { const s = createStore(38); s.setState(x => x + 1); expect(s.getState()).toBe(39); });
  it('setState updater 39', () => { const s = createStore(39); s.setState(x => x + 1); expect(s.getState()).toBe(40); });
  it('setState updater 40', () => { const s = createStore(40); s.setState(x => x + 1); expect(s.getState()).toBe(41); });
  it('setState updater 41', () => { const s = createStore(41); s.setState(x => x + 1); expect(s.getState()).toBe(42); });
  it('setState updater 42', () => { const s = createStore(42); s.setState(x => x + 1); expect(s.getState()).toBe(43); });
  it('setState updater 43', () => { const s = createStore(43); s.setState(x => x + 1); expect(s.getState()).toBe(44); });
  it('setState updater 44', () => { const s = createStore(44); s.setState(x => x + 1); expect(s.getState()).toBe(45); });
  it('setState updater 45', () => { const s = createStore(45); s.setState(x => x + 1); expect(s.getState()).toBe(46); });
  it('setState updater 46', () => { const s = createStore(46); s.setState(x => x + 1); expect(s.getState()).toBe(47); });
  it('setState updater 47', () => { const s = createStore(47); s.setState(x => x + 1); expect(s.getState()).toBe(48); });
  it('setState updater 48', () => { const s = createStore(48); s.setState(x => x + 1); expect(s.getState()).toBe(49); });
  it('setState updater 49', () => { const s = createStore(49); s.setState(x => x + 1); expect(s.getState()).toBe(50); });
  it('setState updater 50', () => { const s = createStore(50); s.setState(x => x + 1); expect(s.getState()).toBe(51); });
  it('subscribe calls listener 1', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(1); expect(calls).toContain(1); });
  it('subscribe calls listener 2', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(2); expect(calls).toContain(2); });
  it('subscribe calls listener 3', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(3); expect(calls).toContain(3); });
  it('subscribe calls listener 4', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(4); expect(calls).toContain(4); });
  it('subscribe calls listener 5', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(5); expect(calls).toContain(5); });
  it('subscribe calls listener 6', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(6); expect(calls).toContain(6); });
  it('subscribe calls listener 7', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(7); expect(calls).toContain(7); });
  it('subscribe calls listener 8', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(8); expect(calls).toContain(8); });
  it('subscribe calls listener 9', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(9); expect(calls).toContain(9); });
  it('subscribe calls listener 10', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(10); expect(calls).toContain(10); });
  it('subscribe calls listener 11', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(11); expect(calls).toContain(11); });
  it('subscribe calls listener 12', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(12); expect(calls).toContain(12); });
  it('subscribe calls listener 13', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(13); expect(calls).toContain(13); });
  it('subscribe calls listener 14', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(14); expect(calls).toContain(14); });
  it('subscribe calls listener 15', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(15); expect(calls).toContain(15); });
  it('subscribe calls listener 16', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(16); expect(calls).toContain(16); });
  it('subscribe calls listener 17', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(17); expect(calls).toContain(17); });
  it('subscribe calls listener 18', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(18); expect(calls).toContain(18); });
  it('subscribe calls listener 19', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(19); expect(calls).toContain(19); });
  it('subscribe calls listener 20', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(20); expect(calls).toContain(20); });
  it('subscribe calls listener 21', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(21); expect(calls).toContain(21); });
  it('subscribe calls listener 22', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(22); expect(calls).toContain(22); });
  it('subscribe calls listener 23', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(23); expect(calls).toContain(23); });
  it('subscribe calls listener 24', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(24); expect(calls).toContain(24); });
  it('subscribe calls listener 25', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(25); expect(calls).toContain(25); });
  it('subscribe calls listener 26', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(26); expect(calls).toContain(26); });
  it('subscribe calls listener 27', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(27); expect(calls).toContain(27); });
  it('subscribe calls listener 28', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(28); expect(calls).toContain(28); });
  it('subscribe calls listener 29', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(29); expect(calls).toContain(29); });
  it('subscribe calls listener 30', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(30); expect(calls).toContain(30); });
  it('subscribe calls listener 31', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(31); expect(calls).toContain(31); });
  it('subscribe calls listener 32', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(32); expect(calls).toContain(32); });
  it('subscribe calls listener 33', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(33); expect(calls).toContain(33); });
  it('subscribe calls listener 34', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(34); expect(calls).toContain(34); });
  it('subscribe calls listener 35', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(35); expect(calls).toContain(35); });
  it('subscribe calls listener 36', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(36); expect(calls).toContain(36); });
  it('subscribe calls listener 37', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(37); expect(calls).toContain(37); });
  it('subscribe calls listener 38', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(38); expect(calls).toContain(38); });
  it('subscribe calls listener 39', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(39); expect(calls).toContain(39); });
  it('subscribe calls listener 40', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(40); expect(calls).toContain(40); });
  it('subscribe calls listener 41', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(41); expect(calls).toContain(41); });
  it('subscribe calls listener 42', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(42); expect(calls).toContain(42); });
  it('subscribe calls listener 43', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(43); expect(calls).toContain(43); });
  it('subscribe calls listener 44', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(44); expect(calls).toContain(44); });
  it('subscribe calls listener 45', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(45); expect(calls).toContain(45); });
  it('subscribe calls listener 46', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(46); expect(calls).toContain(46); });
  it('subscribe calls listener 47', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(47); expect(calls).toContain(47); });
  it('subscribe calls listener 48', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(48); expect(calls).toContain(48); });
  it('subscribe calls listener 49', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(49); expect(calls).toContain(49); });
  it('subscribe calls listener 50', () => { const s = createStore(0); const calls: number[] = []; s.subscribe(v => calls.push(v)); s.setState(50); expect(calls).toContain(50); });
});

describe('immutable helpers', () => {
  it('set key 1', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 1); expect(r.a).toBe(1); expect(o.a).toBe(0); });
  it('set key 2', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 2); expect(r.a).toBe(2); expect(o.a).toBe(0); });
  it('set key 3', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 3); expect(r.a).toBe(3); expect(o.a).toBe(0); });
  it('set key 4', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 4); expect(r.a).toBe(4); expect(o.a).toBe(0); });
  it('set key 5', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 5); expect(r.a).toBe(5); expect(o.a).toBe(0); });
  it('set key 6', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 6); expect(r.a).toBe(6); expect(o.a).toBe(0); });
  it('set key 7', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 7); expect(r.a).toBe(7); expect(o.a).toBe(0); });
  it('set key 8', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 8); expect(r.a).toBe(8); expect(o.a).toBe(0); });
  it('set key 9', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 9); expect(r.a).toBe(9); expect(o.a).toBe(0); });
  it('set key 10', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 10); expect(r.a).toBe(10); expect(o.a).toBe(0); });
  it('set key 11', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 11); expect(r.a).toBe(11); expect(o.a).toBe(0); });
  it('set key 12', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 12); expect(r.a).toBe(12); expect(o.a).toBe(0); });
  it('set key 13', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 13); expect(r.a).toBe(13); expect(o.a).toBe(0); });
  it('set key 14', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 14); expect(r.a).toBe(14); expect(o.a).toBe(0); });
  it('set key 15', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 15); expect(r.a).toBe(15); expect(o.a).toBe(0); });
  it('set key 16', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 16); expect(r.a).toBe(16); expect(o.a).toBe(0); });
  it('set key 17', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 17); expect(r.a).toBe(17); expect(o.a).toBe(0); });
  it('set key 18', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 18); expect(r.a).toBe(18); expect(o.a).toBe(0); });
  it('set key 19', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 19); expect(r.a).toBe(19); expect(o.a).toBe(0); });
  it('set key 20', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 20); expect(r.a).toBe(20); expect(o.a).toBe(0); });
  it('set key 21', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 21); expect(r.a).toBe(21); expect(o.a).toBe(0); });
  it('set key 22', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 22); expect(r.a).toBe(22); expect(o.a).toBe(0); });
  it('set key 23', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 23); expect(r.a).toBe(23); expect(o.a).toBe(0); });
  it('set key 24', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 24); expect(r.a).toBe(24); expect(o.a).toBe(0); });
  it('set key 25', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 25); expect(r.a).toBe(25); expect(o.a).toBe(0); });
  it('set key 26', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 26); expect(r.a).toBe(26); expect(o.a).toBe(0); });
  it('set key 27', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 27); expect(r.a).toBe(27); expect(o.a).toBe(0); });
  it('set key 28', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 28); expect(r.a).toBe(28); expect(o.a).toBe(0); });
  it('set key 29', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 29); expect(r.a).toBe(29); expect(o.a).toBe(0); });
  it('set key 30', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 30); expect(r.a).toBe(30); expect(o.a).toBe(0); });
  it('set key 31', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 31); expect(r.a).toBe(31); expect(o.a).toBe(0); });
  it('set key 32', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 32); expect(r.a).toBe(32); expect(o.a).toBe(0); });
  it('set key 33', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 33); expect(r.a).toBe(33); expect(o.a).toBe(0); });
  it('set key 34', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 34); expect(r.a).toBe(34); expect(o.a).toBe(0); });
  it('set key 35', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 35); expect(r.a).toBe(35); expect(o.a).toBe(0); });
  it('set key 36', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 36); expect(r.a).toBe(36); expect(o.a).toBe(0); });
  it('set key 37', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 37); expect(r.a).toBe(37); expect(o.a).toBe(0); });
  it('set key 38', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 38); expect(r.a).toBe(38); expect(o.a).toBe(0); });
  it('set key 39', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 39); expect(r.a).toBe(39); expect(o.a).toBe(0); });
  it('set key 40', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 40); expect(r.a).toBe(40); expect(o.a).toBe(0); });
  it('set key 41', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 41); expect(r.a).toBe(41); expect(o.a).toBe(0); });
  it('set key 42', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 42); expect(r.a).toBe(42); expect(o.a).toBe(0); });
  it('set key 43', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 43); expect(r.a).toBe(43); expect(o.a).toBe(0); });
  it('set key 44', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 44); expect(r.a).toBe(44); expect(o.a).toBe(0); });
  it('set key 45', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 45); expect(r.a).toBe(45); expect(o.a).toBe(0); });
  it('set key 46', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 46); expect(r.a).toBe(46); expect(o.a).toBe(0); });
  it('set key 47', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 47); expect(r.a).toBe(47); expect(o.a).toBe(0); });
  it('set key 48', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 48); expect(r.a).toBe(48); expect(o.a).toBe(0); });
  it('set key 49', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 49); expect(r.a).toBe(49); expect(o.a).toBe(0); });
  it('set key 50', () => { const o = { a: 0, b: 0 }; const r = set(o, 'a', 50); expect(r.a).toBe(50); expect(o.a).toBe(0); });
  it('setIn depth1 1', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 1); expect((r as any).x).toBe(1); });
  it('setIn depth1 2', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 2); expect((r as any).x).toBe(2); });
  it('setIn depth1 3', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 3); expect((r as any).x).toBe(3); });
  it('setIn depth1 4', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 4); expect((r as any).x).toBe(4); });
  it('setIn depth1 5', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 5); expect((r as any).x).toBe(5); });
  it('setIn depth1 6', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 6); expect((r as any).x).toBe(6); });
  it('setIn depth1 7', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 7); expect((r as any).x).toBe(7); });
  it('setIn depth1 8', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 8); expect((r as any).x).toBe(8); });
  it('setIn depth1 9', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 9); expect((r as any).x).toBe(9); });
  it('setIn depth1 10', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 10); expect((r as any).x).toBe(10); });
  it('setIn depth1 11', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 11); expect((r as any).x).toBe(11); });
  it('setIn depth1 12', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 12); expect((r as any).x).toBe(12); });
  it('setIn depth1 13', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 13); expect((r as any).x).toBe(13); });
  it('setIn depth1 14', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 14); expect((r as any).x).toBe(14); });
  it('setIn depth1 15', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 15); expect((r as any).x).toBe(15); });
  it('setIn depth1 16', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 16); expect((r as any).x).toBe(16); });
  it('setIn depth1 17', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 17); expect((r as any).x).toBe(17); });
  it('setIn depth1 18', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 18); expect((r as any).x).toBe(18); });
  it('setIn depth1 19', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 19); expect((r as any).x).toBe(19); });
  it('setIn depth1 20', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 20); expect((r as any).x).toBe(20); });
  it('setIn depth1 21', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 21); expect((r as any).x).toBe(21); });
  it('setIn depth1 22', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 22); expect((r as any).x).toBe(22); });
  it('setIn depth1 23', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 23); expect((r as any).x).toBe(23); });
  it('setIn depth1 24', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 24); expect((r as any).x).toBe(24); });
  it('setIn depth1 25', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 25); expect((r as any).x).toBe(25); });
  it('setIn depth1 26', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 26); expect((r as any).x).toBe(26); });
  it('setIn depth1 27', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 27); expect((r as any).x).toBe(27); });
  it('setIn depth1 28', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 28); expect((r as any).x).toBe(28); });
  it('setIn depth1 29', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 29); expect((r as any).x).toBe(29); });
  it('setIn depth1 30', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 30); expect((r as any).x).toBe(30); });
  it('setIn depth1 31', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 31); expect((r as any).x).toBe(31); });
  it('setIn depth1 32', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 32); expect((r as any).x).toBe(32); });
  it('setIn depth1 33', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 33); expect((r as any).x).toBe(33); });
  it('setIn depth1 34', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 34); expect((r as any).x).toBe(34); });
  it('setIn depth1 35', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 35); expect((r as any).x).toBe(35); });
  it('setIn depth1 36', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 36); expect((r as any).x).toBe(36); });
  it('setIn depth1 37', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 37); expect((r as any).x).toBe(37); });
  it('setIn depth1 38', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 38); expect((r as any).x).toBe(38); });
  it('setIn depth1 39', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 39); expect((r as any).x).toBe(39); });
  it('setIn depth1 40', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 40); expect((r as any).x).toBe(40); });
  it('setIn depth1 41', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 41); expect((r as any).x).toBe(41); });
  it('setIn depth1 42', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 42); expect((r as any).x).toBe(42); });
  it('setIn depth1 43', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 43); expect((r as any).x).toBe(43); });
  it('setIn depth1 44', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 44); expect((r as any).x).toBe(44); });
  it('setIn depth1 45', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 45); expect((r as any).x).toBe(45); });
  it('setIn depth1 46', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 46); expect((r as any).x).toBe(46); });
  it('setIn depth1 47', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 47); expect((r as any).x).toBe(47); });
  it('setIn depth1 48', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 48); expect((r as any).x).toBe(48); });
  it('setIn depth1 49', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 49); expect((r as any).x).toBe(49); });
  it('setIn depth1 50', () => { const o = { x: 0 }; const r = setIn(o, ['x'], 50); expect((r as any).x).toBe(50); });
  it('getIn depth1 1', () => { const o = { n: 1 }; expect(getIn(o, ['n'])).toBe(1); });
  it('getIn depth1 2', () => { const o = { n: 2 }; expect(getIn(o, ['n'])).toBe(2); });
  it('getIn depth1 3', () => { const o = { n: 3 }; expect(getIn(o, ['n'])).toBe(3); });
  it('getIn depth1 4', () => { const o = { n: 4 }; expect(getIn(o, ['n'])).toBe(4); });
  it('getIn depth1 5', () => { const o = { n: 5 }; expect(getIn(o, ['n'])).toBe(5); });
  it('getIn depth1 6', () => { const o = { n: 6 }; expect(getIn(o, ['n'])).toBe(6); });
  it('getIn depth1 7', () => { const o = { n: 7 }; expect(getIn(o, ['n'])).toBe(7); });
  it('getIn depth1 8', () => { const o = { n: 8 }; expect(getIn(o, ['n'])).toBe(8); });
  it('getIn depth1 9', () => { const o = { n: 9 }; expect(getIn(o, ['n'])).toBe(9); });
  it('getIn depth1 10', () => { const o = { n: 10 }; expect(getIn(o, ['n'])).toBe(10); });
  it('getIn depth1 11', () => { const o = { n: 11 }; expect(getIn(o, ['n'])).toBe(11); });
  it('getIn depth1 12', () => { const o = { n: 12 }; expect(getIn(o, ['n'])).toBe(12); });
  it('getIn depth1 13', () => { const o = { n: 13 }; expect(getIn(o, ['n'])).toBe(13); });
  it('getIn depth1 14', () => { const o = { n: 14 }; expect(getIn(o, ['n'])).toBe(14); });
  it('getIn depth1 15', () => { const o = { n: 15 }; expect(getIn(o, ['n'])).toBe(15); });
  it('getIn depth1 16', () => { const o = { n: 16 }; expect(getIn(o, ['n'])).toBe(16); });
  it('getIn depth1 17', () => { const o = { n: 17 }; expect(getIn(o, ['n'])).toBe(17); });
  it('getIn depth1 18', () => { const o = { n: 18 }; expect(getIn(o, ['n'])).toBe(18); });
  it('getIn depth1 19', () => { const o = { n: 19 }; expect(getIn(o, ['n'])).toBe(19); });
  it('getIn depth1 20', () => { const o = { n: 20 }; expect(getIn(o, ['n'])).toBe(20); });
  it('getIn depth1 21', () => { const o = { n: 21 }; expect(getIn(o, ['n'])).toBe(21); });
  it('getIn depth1 22', () => { const o = { n: 22 }; expect(getIn(o, ['n'])).toBe(22); });
  it('getIn depth1 23', () => { const o = { n: 23 }; expect(getIn(o, ['n'])).toBe(23); });
  it('getIn depth1 24', () => { const o = { n: 24 }; expect(getIn(o, ['n'])).toBe(24); });
  it('getIn depth1 25', () => { const o = { n: 25 }; expect(getIn(o, ['n'])).toBe(25); });
  it('deleteKey 1', () => { const o: any = { a: 1, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 2', () => { const o: any = { a: 2, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 3', () => { const o: any = { a: 3, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 4', () => { const o: any = { a: 4, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 5', () => { const o: any = { a: 5, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 6', () => { const o: any = { a: 6, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 7', () => { const o: any = { a: 7, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 8', () => { const o: any = { a: 8, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 9', () => { const o: any = { a: 9, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 10', () => { const o: any = { a: 10, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 11', () => { const o: any = { a: 11, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 12', () => { const o: any = { a: 12, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 13', () => { const o: any = { a: 13, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 14', () => { const o: any = { a: 14, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 15', () => { const o: any = { a: 15, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 16', () => { const o: any = { a: 16, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 17', () => { const o: any = { a: 17, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 18', () => { const o: any = { a: 18, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 19', () => { const o: any = { a: 19, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 20', () => { const o: any = { a: 20, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 21', () => { const o: any = { a: 21, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 22', () => { const o: any = { a: 22, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 23', () => { const o: any = { a: 23, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 24', () => { const o: any = { a: 24, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
  it('deleteKey 25', () => { const o: any = { a: 25, b: 'x' }; const r = deleteKey(o, 'a'); expect(r).not.toHaveProperty('a'); });
});

describe('merge and produce', () => {
  it('merge override 1', () => { const a = { n: 0, m: 1 }; const b = { n: 1 }; const r = merge(a, b); expect(r.n).toBe(1); expect(r.m).toBe(1); });
  it('merge override 2', () => { const a = { n: 0, m: 2 }; const b = { n: 2 }; const r = merge(a, b); expect(r.n).toBe(2); expect(r.m).toBe(2); });
  it('merge override 3', () => { const a = { n: 0, m: 3 }; const b = { n: 3 }; const r = merge(a, b); expect(r.n).toBe(3); expect(r.m).toBe(3); });
  it('merge override 4', () => { const a = { n: 0, m: 4 }; const b = { n: 4 }; const r = merge(a, b); expect(r.n).toBe(4); expect(r.m).toBe(4); });
  it('merge override 5', () => { const a = { n: 0, m: 5 }; const b = { n: 5 }; const r = merge(a, b); expect(r.n).toBe(5); expect(r.m).toBe(5); });
  it('merge override 6', () => { const a = { n: 0, m: 6 }; const b = { n: 6 }; const r = merge(a, b); expect(r.n).toBe(6); expect(r.m).toBe(6); });
  it('merge override 7', () => { const a = { n: 0, m: 7 }; const b = { n: 7 }; const r = merge(a, b); expect(r.n).toBe(7); expect(r.m).toBe(7); });
  it('merge override 8', () => { const a = { n: 0, m: 8 }; const b = { n: 8 }; const r = merge(a, b); expect(r.n).toBe(8); expect(r.m).toBe(8); });
  it('merge override 9', () => { const a = { n: 0, m: 9 }; const b = { n: 9 }; const r = merge(a, b); expect(r.n).toBe(9); expect(r.m).toBe(9); });
  it('merge override 10', () => { const a = { n: 0, m: 10 }; const b = { n: 10 }; const r = merge(a, b); expect(r.n).toBe(10); expect(r.m).toBe(10); });
  it('merge override 11', () => { const a = { n: 0, m: 11 }; const b = { n: 11 }; const r = merge(a, b); expect(r.n).toBe(11); expect(r.m).toBe(11); });
  it('merge override 12', () => { const a = { n: 0, m: 12 }; const b = { n: 12 }; const r = merge(a, b); expect(r.n).toBe(12); expect(r.m).toBe(12); });
  it('merge override 13', () => { const a = { n: 0, m: 13 }; const b = { n: 13 }; const r = merge(a, b); expect(r.n).toBe(13); expect(r.m).toBe(13); });
  it('merge override 14', () => { const a = { n: 0, m: 14 }; const b = { n: 14 }; const r = merge(a, b); expect(r.n).toBe(14); expect(r.m).toBe(14); });
  it('merge override 15', () => { const a = { n: 0, m: 15 }; const b = { n: 15 }; const r = merge(a, b); expect(r.n).toBe(15); expect(r.m).toBe(15); });
  it('merge override 16', () => { const a = { n: 0, m: 16 }; const b = { n: 16 }; const r = merge(a, b); expect(r.n).toBe(16); expect(r.m).toBe(16); });
  it('merge override 17', () => { const a = { n: 0, m: 17 }; const b = { n: 17 }; const r = merge(a, b); expect(r.n).toBe(17); expect(r.m).toBe(17); });
  it('merge override 18', () => { const a = { n: 0, m: 18 }; const b = { n: 18 }; const r = merge(a, b); expect(r.n).toBe(18); expect(r.m).toBe(18); });
  it('merge override 19', () => { const a = { n: 0, m: 19 }; const b = { n: 19 }; const r = merge(a, b); expect(r.n).toBe(19); expect(r.m).toBe(19); });
  it('merge override 20', () => { const a = { n: 0, m: 20 }; const b = { n: 20 }; const r = merge(a, b); expect(r.n).toBe(20); expect(r.m).toBe(20); });
  it('merge override 21', () => { const a = { n: 0, m: 21 }; const b = { n: 21 }; const r = merge(a, b); expect(r.n).toBe(21); expect(r.m).toBe(21); });
  it('merge override 22', () => { const a = { n: 0, m: 22 }; const b = { n: 22 }; const r = merge(a, b); expect(r.n).toBe(22); expect(r.m).toBe(22); });
  it('merge override 23', () => { const a = { n: 0, m: 23 }; const b = { n: 23 }; const r = merge(a, b); expect(r.n).toBe(23); expect(r.m).toBe(23); });
  it('merge override 24', () => { const a = { n: 0, m: 24 }; const b = { n: 24 }; const r = merge(a, b); expect(r.n).toBe(24); expect(r.m).toBe(24); });
  it('merge override 25', () => { const a = { n: 0, m: 25 }; const b = { n: 25 }; const r = merge(a, b); expect(r.n).toBe(25); expect(r.m).toBe(25); });
  it('merge override 26', () => { const a = { n: 0, m: 26 }; const b = { n: 26 }; const r = merge(a, b); expect(r.n).toBe(26); expect(r.m).toBe(26); });
  it('merge override 27', () => { const a = { n: 0, m: 27 }; const b = { n: 27 }; const r = merge(a, b); expect(r.n).toBe(27); expect(r.m).toBe(27); });
  it('merge override 28', () => { const a = { n: 0, m: 28 }; const b = { n: 28 }; const r = merge(a, b); expect(r.n).toBe(28); expect(r.m).toBe(28); });
  it('merge override 29', () => { const a = { n: 0, m: 29 }; const b = { n: 29 }; const r = merge(a, b); expect(r.n).toBe(29); expect(r.m).toBe(29); });
  it('merge override 30', () => { const a = { n: 0, m: 30 }; const b = { n: 30 }; const r = merge(a, b); expect(r.n).toBe(30); expect(r.m).toBe(30); });
  it('merge override 31', () => { const a = { n: 0, m: 31 }; const b = { n: 31 }; const r = merge(a, b); expect(r.n).toBe(31); expect(r.m).toBe(31); });
  it('merge override 32', () => { const a = { n: 0, m: 32 }; const b = { n: 32 }; const r = merge(a, b); expect(r.n).toBe(32); expect(r.m).toBe(32); });
  it('merge override 33', () => { const a = { n: 0, m: 33 }; const b = { n: 33 }; const r = merge(a, b); expect(r.n).toBe(33); expect(r.m).toBe(33); });
  it('merge override 34', () => { const a = { n: 0, m: 34 }; const b = { n: 34 }; const r = merge(a, b); expect(r.n).toBe(34); expect(r.m).toBe(34); });
  it('merge override 35', () => { const a = { n: 0, m: 35 }; const b = { n: 35 }; const r = merge(a, b); expect(r.n).toBe(35); expect(r.m).toBe(35); });
  it('merge override 36', () => { const a = { n: 0, m: 36 }; const b = { n: 36 }; const r = merge(a, b); expect(r.n).toBe(36); expect(r.m).toBe(36); });
  it('merge override 37', () => { const a = { n: 0, m: 37 }; const b = { n: 37 }; const r = merge(a, b); expect(r.n).toBe(37); expect(r.m).toBe(37); });
  it('merge override 38', () => { const a = { n: 0, m: 38 }; const b = { n: 38 }; const r = merge(a, b); expect(r.n).toBe(38); expect(r.m).toBe(38); });
  it('merge override 39', () => { const a = { n: 0, m: 39 }; const b = { n: 39 }; const r = merge(a, b); expect(r.n).toBe(39); expect(r.m).toBe(39); });
  it('merge override 40', () => { const a = { n: 0, m: 40 }; const b = { n: 40 }; const r = merge(a, b); expect(r.n).toBe(40); expect(r.m).toBe(40); });
  it('merge override 41', () => { const a = { n: 0, m: 41 }; const b = { n: 41 }; const r = merge(a, b); expect(r.n).toBe(41); expect(r.m).toBe(41); });
  it('merge override 42', () => { const a = { n: 0, m: 42 }; const b = { n: 42 }; const r = merge(a, b); expect(r.n).toBe(42); expect(r.m).toBe(42); });
  it('merge override 43', () => { const a = { n: 0, m: 43 }; const b = { n: 43 }; const r = merge(a, b); expect(r.n).toBe(43); expect(r.m).toBe(43); });
  it('merge override 44', () => { const a = { n: 0, m: 44 }; const b = { n: 44 }; const r = merge(a, b); expect(r.n).toBe(44); expect(r.m).toBe(44); });
  it('merge override 45', () => { const a = { n: 0, m: 45 }; const b = { n: 45 }; const r = merge(a, b); expect(r.n).toBe(45); expect(r.m).toBe(45); });
  it('merge override 46', () => { const a = { n: 0, m: 46 }; const b = { n: 46 }; const r = merge(a, b); expect(r.n).toBe(46); expect(r.m).toBe(46); });
  it('merge override 47', () => { const a = { n: 0, m: 47 }; const b = { n: 47 }; const r = merge(a, b); expect(r.n).toBe(47); expect(r.m).toBe(47); });
  it('merge override 48', () => { const a = { n: 0, m: 48 }; const b = { n: 48 }; const r = merge(a, b); expect(r.n).toBe(48); expect(r.m).toBe(48); });
  it('merge override 49', () => { const a = { n: 0, m: 49 }; const b = { n: 49 }; const r = merge(a, b); expect(r.n).toBe(49); expect(r.m).toBe(49); });
  it('merge override 50', () => { const a = { n: 0, m: 50 }; const b = { n: 50 }; const r = merge(a, b); expect(r.n).toBe(50); expect(r.m).toBe(50); });
  it('produce mutate 1', () => { const base = { v: 1 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(2); expect(base.v).toBe(1); });
  it('produce mutate 2', () => { const base = { v: 2 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(4); expect(base.v).toBe(2); });
  it('produce mutate 3', () => { const base = { v: 3 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(6); expect(base.v).toBe(3); });
  it('produce mutate 4', () => { const base = { v: 4 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(8); expect(base.v).toBe(4); });
  it('produce mutate 5', () => { const base = { v: 5 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(10); expect(base.v).toBe(5); });
  it('produce mutate 6', () => { const base = { v: 6 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(12); expect(base.v).toBe(6); });
  it('produce mutate 7', () => { const base = { v: 7 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(14); expect(base.v).toBe(7); });
  it('produce mutate 8', () => { const base = { v: 8 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(16); expect(base.v).toBe(8); });
  it('produce mutate 9', () => { const base = { v: 9 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(18); expect(base.v).toBe(9); });
  it('produce mutate 10', () => { const base = { v: 10 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(20); expect(base.v).toBe(10); });
  it('produce mutate 11', () => { const base = { v: 11 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(22); expect(base.v).toBe(11); });
  it('produce mutate 12', () => { const base = { v: 12 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(24); expect(base.v).toBe(12); });
  it('produce mutate 13', () => { const base = { v: 13 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(26); expect(base.v).toBe(13); });
  it('produce mutate 14', () => { const base = { v: 14 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(28); expect(base.v).toBe(14); });
  it('produce mutate 15', () => { const base = { v: 15 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(30); expect(base.v).toBe(15); });
  it('produce mutate 16', () => { const base = { v: 16 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(32); expect(base.v).toBe(16); });
  it('produce mutate 17', () => { const base = { v: 17 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(34); expect(base.v).toBe(17); });
  it('produce mutate 18', () => { const base = { v: 18 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(36); expect(base.v).toBe(18); });
  it('produce mutate 19', () => { const base = { v: 19 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(38); expect(base.v).toBe(19); });
  it('produce mutate 20', () => { const base = { v: 20 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(40); expect(base.v).toBe(20); });
  it('produce mutate 21', () => { const base = { v: 21 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(42); expect(base.v).toBe(21); });
  it('produce mutate 22', () => { const base = { v: 22 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(44); expect(base.v).toBe(22); });
  it('produce mutate 23', () => { const base = { v: 23 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(46); expect(base.v).toBe(23); });
  it('produce mutate 24', () => { const base = { v: 24 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(48); expect(base.v).toBe(24); });
  it('produce mutate 25', () => { const base = { v: 25 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(50); expect(base.v).toBe(25); });
  it('produce mutate 26', () => { const base = { v: 26 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(52); expect(base.v).toBe(26); });
  it('produce mutate 27', () => { const base = { v: 27 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(54); expect(base.v).toBe(27); });
  it('produce mutate 28', () => { const base = { v: 28 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(56); expect(base.v).toBe(28); });
  it('produce mutate 29', () => { const base = { v: 29 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(58); expect(base.v).toBe(29); });
  it('produce mutate 30', () => { const base = { v: 30 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(60); expect(base.v).toBe(30); });
  it('produce mutate 31', () => { const base = { v: 31 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(62); expect(base.v).toBe(31); });
  it('produce mutate 32', () => { const base = { v: 32 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(64); expect(base.v).toBe(32); });
  it('produce mutate 33', () => { const base = { v: 33 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(66); expect(base.v).toBe(33); });
  it('produce mutate 34', () => { const base = { v: 34 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(68); expect(base.v).toBe(34); });
  it('produce mutate 35', () => { const base = { v: 35 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(70); expect(base.v).toBe(35); });
  it('produce mutate 36', () => { const base = { v: 36 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(72); expect(base.v).toBe(36); });
  it('produce mutate 37', () => { const base = { v: 37 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(74); expect(base.v).toBe(37); });
  it('produce mutate 38', () => { const base = { v: 38 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(76); expect(base.v).toBe(38); });
  it('produce mutate 39', () => { const base = { v: 39 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(78); expect(base.v).toBe(39); });
  it('produce mutate 40', () => { const base = { v: 40 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(80); expect(base.v).toBe(40); });
  it('produce mutate 41', () => { const base = { v: 41 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(82); expect(base.v).toBe(41); });
  it('produce mutate 42', () => { const base = { v: 42 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(84); expect(base.v).toBe(42); });
  it('produce mutate 43', () => { const base = { v: 43 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(86); expect(base.v).toBe(43); });
  it('produce mutate 44', () => { const base = { v: 44 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(88); expect(base.v).toBe(44); });
  it('produce mutate 45', () => { const base = { v: 45 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(90); expect(base.v).toBe(45); });
  it('produce mutate 46', () => { const base = { v: 46 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(92); expect(base.v).toBe(46); });
  it('produce mutate 47', () => { const base = { v: 47 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(94); expect(base.v).toBe(47); });
  it('produce mutate 48', () => { const base = { v: 48 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(96); expect(base.v).toBe(48); });
  it('produce mutate 49', () => { const base = { v: 49 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(98); expect(base.v).toBe(49); });
  it('produce mutate 50', () => { const base = { v: 50 }; const next = produce(base, d => { d.v = d.v * 2; }); expect(next.v).toBe(100); expect(base.v).toBe(50); });
});

describe('selectors', () => {
  it('createSelector returns value 1', () => { const sel = createSelector((s: number) => s * 1); expect(sel(1)).toBe(1); });
  it('createSelector returns value 2', () => { const sel = createSelector((s: number) => s * 2); expect(sel(2)).toBe(4); });
  it('createSelector returns value 3', () => { const sel = createSelector((s: number) => s * 3); expect(sel(3)).toBe(9); });
  it('createSelector returns value 4', () => { const sel = createSelector((s: number) => s * 4); expect(sel(4)).toBe(16); });
  it('createSelector returns value 5', () => { const sel = createSelector((s: number) => s * 5); expect(sel(5)).toBe(25); });
  it('createSelector returns value 6', () => { const sel = createSelector((s: number) => s * 6); expect(sel(6)).toBe(36); });
  it('createSelector returns value 7', () => { const sel = createSelector((s: number) => s * 7); expect(sel(7)).toBe(49); });
  it('createSelector returns value 8', () => { const sel = createSelector((s: number) => s * 8); expect(sel(8)).toBe(64); });
  it('createSelector returns value 9', () => { const sel = createSelector((s: number) => s * 9); expect(sel(9)).toBe(81); });
  it('createSelector returns value 10', () => { const sel = createSelector((s: number) => s * 10); expect(sel(10)).toBe(100); });
  it('createSelector returns value 11', () => { const sel = createSelector((s: number) => s * 11); expect(sel(11)).toBe(121); });
  it('createSelector returns value 12', () => { const sel = createSelector((s: number) => s * 12); expect(sel(12)).toBe(144); });
  it('createSelector returns value 13', () => { const sel = createSelector((s: number) => s * 13); expect(sel(13)).toBe(169); });
  it('createSelector returns value 14', () => { const sel = createSelector((s: number) => s * 14); expect(sel(14)).toBe(196); });
  it('createSelector returns value 15', () => { const sel = createSelector((s: number) => s * 15); expect(sel(15)).toBe(225); });
  it('createSelector returns value 16', () => { const sel = createSelector((s: number) => s * 16); expect(sel(16)).toBe(256); });
  it('createSelector returns value 17', () => { const sel = createSelector((s: number) => s * 17); expect(sel(17)).toBe(289); });
  it('createSelector returns value 18', () => { const sel = createSelector((s: number) => s * 18); expect(sel(18)).toBe(324); });
  it('createSelector returns value 19', () => { const sel = createSelector((s: number) => s * 19); expect(sel(19)).toBe(361); });
  it('createSelector returns value 20', () => { const sel = createSelector((s: number) => s * 20); expect(sel(20)).toBe(400); });
  it('createSelector returns value 21', () => { const sel = createSelector((s: number) => s * 21); expect(sel(21)).toBe(441); });
  it('createSelector returns value 22', () => { const sel = createSelector((s: number) => s * 22); expect(sel(22)).toBe(484); });
  it('createSelector returns value 23', () => { const sel = createSelector((s: number) => s * 23); expect(sel(23)).toBe(529); });
  it('createSelector returns value 24', () => { const sel = createSelector((s: number) => s * 24); expect(sel(24)).toBe(576); });
  it('createSelector returns value 25', () => { const sel = createSelector((s: number) => s * 25); expect(sel(25)).toBe(625); });
  it('createSelector returns value 26', () => { const sel = createSelector((s: number) => s * 26); expect(sel(26)).toBe(676); });
  it('createSelector returns value 27', () => { const sel = createSelector((s: number) => s * 27); expect(sel(27)).toBe(729); });
  it('createSelector returns value 28', () => { const sel = createSelector((s: number) => s * 28); expect(sel(28)).toBe(784); });
  it('createSelector returns value 29', () => { const sel = createSelector((s: number) => s * 29); expect(sel(29)).toBe(841); });
  it('createSelector returns value 30', () => { const sel = createSelector((s: number) => s * 30); expect(sel(30)).toBe(900); });
  it('createSelector returns value 31', () => { const sel = createSelector((s: number) => s * 31); expect(sel(31)).toBe(961); });
  it('createSelector returns value 32', () => { const sel = createSelector((s: number) => s * 32); expect(sel(32)).toBe(1024); });
  it('createSelector returns value 33', () => { const sel = createSelector((s: number) => s * 33); expect(sel(33)).toBe(1089); });
  it('createSelector returns value 34', () => { const sel = createSelector((s: number) => s * 34); expect(sel(34)).toBe(1156); });
  it('createSelector returns value 35', () => { const sel = createSelector((s: number) => s * 35); expect(sel(35)).toBe(1225); });
  it('createSelector returns value 36', () => { const sel = createSelector((s: number) => s * 36); expect(sel(36)).toBe(1296); });
  it('createSelector returns value 37', () => { const sel = createSelector((s: number) => s * 37); expect(sel(37)).toBe(1369); });
  it('createSelector returns value 38', () => { const sel = createSelector((s: number) => s * 38); expect(sel(38)).toBe(1444); });
  it('createSelector returns value 39', () => { const sel = createSelector((s: number) => s * 39); expect(sel(39)).toBe(1521); });
  it('createSelector returns value 40', () => { const sel = createSelector((s: number) => s * 40); expect(sel(40)).toBe(1600); });
  it('createSelector returns value 41', () => { const sel = createSelector((s: number) => s * 41); expect(sel(41)).toBe(1681); });
  it('createSelector returns value 42', () => { const sel = createSelector((s: number) => s * 42); expect(sel(42)).toBe(1764); });
  it('createSelector returns value 43', () => { const sel = createSelector((s: number) => s * 43); expect(sel(43)).toBe(1849); });
  it('createSelector returns value 44', () => { const sel = createSelector((s: number) => s * 44); expect(sel(44)).toBe(1936); });
  it('createSelector returns value 45', () => { const sel = createSelector((s: number) => s * 45); expect(sel(45)).toBe(2025); });
  it('createSelector returns value 46', () => { const sel = createSelector((s: number) => s * 46); expect(sel(46)).toBe(2116); });
  it('createSelector returns value 47', () => { const sel = createSelector((s: number) => s * 47); expect(sel(47)).toBe(2209); });
  it('createSelector returns value 48', () => { const sel = createSelector((s: number) => s * 48); expect(sel(48)).toBe(2304); });
  it('createSelector returns value 49', () => { const sel = createSelector((s: number) => s * 49); expect(sel(49)).toBe(2401); });
  it('createSelector returns value 50', () => { const sel = createSelector((s: number) => s * 50); expect(sel(50)).toBe(2500); });
  it('createSelector memoizes 1', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(1); sel(1); expect(calls).toBe(1); });
  it('createSelector memoizes 2', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(2); sel(2); expect(calls).toBe(1); });
  it('createSelector memoizes 3', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(3); sel(3); expect(calls).toBe(1); });
  it('createSelector memoizes 4', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(4); sel(4); expect(calls).toBe(1); });
  it('createSelector memoizes 5', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(5); sel(5); expect(calls).toBe(1); });
  it('createSelector memoizes 6', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(6); sel(6); expect(calls).toBe(1); });
  it('createSelector memoizes 7', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(7); sel(7); expect(calls).toBe(1); });
  it('createSelector memoizes 8', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(8); sel(8); expect(calls).toBe(1); });
  it('createSelector memoizes 9', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(9); sel(9); expect(calls).toBe(1); });
  it('createSelector memoizes 10', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(10); sel(10); expect(calls).toBe(1); });
  it('createSelector memoizes 11', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(11); sel(11); expect(calls).toBe(1); });
  it('createSelector memoizes 12', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(12); sel(12); expect(calls).toBe(1); });
  it('createSelector memoizes 13', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(13); sel(13); expect(calls).toBe(1); });
  it('createSelector memoizes 14', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(14); sel(14); expect(calls).toBe(1); });
  it('createSelector memoizes 15', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(15); sel(15); expect(calls).toBe(1); });
  it('createSelector memoizes 16', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(16); sel(16); expect(calls).toBe(1); });
  it('createSelector memoizes 17', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(17); sel(17); expect(calls).toBe(1); });
  it('createSelector memoizes 18', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(18); sel(18); expect(calls).toBe(1); });
  it('createSelector memoizes 19', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(19); sel(19); expect(calls).toBe(1); });
  it('createSelector memoizes 20', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(20); sel(20); expect(calls).toBe(1); });
  it('createSelector memoizes 21', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(21); sel(21); expect(calls).toBe(1); });
  it('createSelector memoizes 22', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(22); sel(22); expect(calls).toBe(1); });
  it('createSelector memoizes 23', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(23); sel(23); expect(calls).toBe(1); });
  it('createSelector memoizes 24', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(24); sel(24); expect(calls).toBe(1); });
  it('createSelector memoizes 25', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(25); sel(25); expect(calls).toBe(1); });
  it('createSelector memoizes 26', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(26); sel(26); expect(calls).toBe(1); });
  it('createSelector memoizes 27', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(27); sel(27); expect(calls).toBe(1); });
  it('createSelector memoizes 28', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(28); sel(28); expect(calls).toBe(1); });
  it('createSelector memoizes 29', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(29); sel(29); expect(calls).toBe(1); });
  it('createSelector memoizes 30', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(30); sel(30); expect(calls).toBe(1); });
  it('createSelector memoizes 31', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(31); sel(31); expect(calls).toBe(1); });
  it('createSelector memoizes 32', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(32); sel(32); expect(calls).toBe(1); });
  it('createSelector memoizes 33', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(33); sel(33); expect(calls).toBe(1); });
  it('createSelector memoizes 34', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(34); sel(34); expect(calls).toBe(1); });
  it('createSelector memoizes 35', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(35); sel(35); expect(calls).toBe(1); });
  it('createSelector memoizes 36', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(36); sel(36); expect(calls).toBe(1); });
  it('createSelector memoizes 37', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(37); sel(37); expect(calls).toBe(1); });
  it('createSelector memoizes 38', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(38); sel(38); expect(calls).toBe(1); });
  it('createSelector memoizes 39', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(39); sel(39); expect(calls).toBe(1); });
  it('createSelector memoizes 40', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(40); sel(40); expect(calls).toBe(1); });
  it('createSelector memoizes 41', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(41); sel(41); expect(calls).toBe(1); });
  it('createSelector memoizes 42', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(42); sel(42); expect(calls).toBe(1); });
  it('createSelector memoizes 43', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(43); sel(43); expect(calls).toBe(1); });
  it('createSelector memoizes 44', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(44); sel(44); expect(calls).toBe(1); });
  it('createSelector memoizes 45', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(45); sel(45); expect(calls).toBe(1); });
  it('createSelector memoizes 46', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(46); sel(46); expect(calls).toBe(1); });
  it('createSelector memoizes 47', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(47); sel(47); expect(calls).toBe(1); });
  it('createSelector memoizes 48', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(48); sel(48); expect(calls).toBe(1); });
  it('createSelector memoizes 49', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(49); sel(49); expect(calls).toBe(1); });
  it('createSelector memoizes 50', () => { let calls = 0; const sel = createSelector((s: number) => { calls++; return s; }); sel(50); sel(50); expect(calls).toBe(1); });
});

describe('createStateMachine', () => {
  const config = { initial: 'idle' as 'idle'|'running'|'done', transitions: [ { from: 'idle' as 'idle'|'running'|'done', event: 'start' as 'start'|'finish'|'reset', to: 'running' as 'idle'|'running'|'done' }, { from: 'running' as 'idle'|'running'|'done', event: 'finish' as 'start'|'finish'|'reset', to: 'done' as 'idle'|'running'|'done' }, { from: 'done' as 'idle'|'running'|'done', event: 'reset' as 'start'|'finish'|'reset', to: 'idle' as 'idle'|'running'|'done' } ] };
  it('initial state 1', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 2', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 3', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 4', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 5', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 6', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 7', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 8', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 9', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 10', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 11', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 12', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 13', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 14', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 15', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 16', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 17', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 18', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 19', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 20', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 21', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 22', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 23', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 24', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 25', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 26', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 27', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 28', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 29', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 30', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 31', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 32', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 33', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 34', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 35', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 36', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 37', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 38', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 39', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 40', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 41', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 42', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 43', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 44', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 45', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 46', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 47', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 48', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 49', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('initial state 50', () => { const m = createStateMachine(config); expect(m.current).toBe('idle'); });
  it('transition to running 1', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 2', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 3', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 4', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 5', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 6', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 7', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 8', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 9', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 10', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 11', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 12', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 13', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 14', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 15', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 16', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 17', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 18', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 19', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 20', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 21', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 22', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 23', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 24', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 25', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 26', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 27', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 28', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 29', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 30', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 31', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 32', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 33', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 34', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 35', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 36', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 37', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 38', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 39', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 40', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 41', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 42', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 43', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 44', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 45', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 46', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 47', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 48', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 49', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('transition to running 50', () => { const m = createStateMachine(config).send('start'); expect(m.current).toBe('running'); });
  it('can check 1', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 2', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 3', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 4', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 5', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 6', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 7', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 8', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 9', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 10', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 11', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 12', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 13', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 14', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 15', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 16', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 17', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 18', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 19', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 20', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 21', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 22', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 23', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 24', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 25', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 26', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 27', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 28', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 29', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 30', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 31', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 32', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 33', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 34', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 35', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 36', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 37', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 38', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 39', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 40', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 41', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 42', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 43', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 44', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 45', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 46', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 47', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 48', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 49', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
  it('can check 50', () => { const m = createStateMachine(config); expect(m.can('start')).toBe(true); expect(m.can('finish')).toBe(false); });
});

describe('history', () => {
  it('createHistory initial 1', () => { const h = createHistory(1); expect(h.present).toBe(1); expect(h.past.length).toBe(0); });
  it('createHistory initial 2', () => { const h = createHistory(2); expect(h.present).toBe(2); expect(h.past.length).toBe(0); });
  it('createHistory initial 3', () => { const h = createHistory(3); expect(h.present).toBe(3); expect(h.past.length).toBe(0); });
  it('createHistory initial 4', () => { const h = createHistory(4); expect(h.present).toBe(4); expect(h.past.length).toBe(0); });
  it('createHistory initial 5', () => { const h = createHistory(5); expect(h.present).toBe(5); expect(h.past.length).toBe(0); });
  it('createHistory initial 6', () => { const h = createHistory(6); expect(h.present).toBe(6); expect(h.past.length).toBe(0); });
  it('createHistory initial 7', () => { const h = createHistory(7); expect(h.present).toBe(7); expect(h.past.length).toBe(0); });
  it('createHistory initial 8', () => { const h = createHistory(8); expect(h.present).toBe(8); expect(h.past.length).toBe(0); });
  it('createHistory initial 9', () => { const h = createHistory(9); expect(h.present).toBe(9); expect(h.past.length).toBe(0); });
  it('createHistory initial 10', () => { const h = createHistory(10); expect(h.present).toBe(10); expect(h.past.length).toBe(0); });
  it('createHistory initial 11', () => { const h = createHistory(11); expect(h.present).toBe(11); expect(h.past.length).toBe(0); });
  it('createHistory initial 12', () => { const h = createHistory(12); expect(h.present).toBe(12); expect(h.past.length).toBe(0); });
  it('createHistory initial 13', () => { const h = createHistory(13); expect(h.present).toBe(13); expect(h.past.length).toBe(0); });
  it('createHistory initial 14', () => { const h = createHistory(14); expect(h.present).toBe(14); expect(h.past.length).toBe(0); });
  it('createHistory initial 15', () => { const h = createHistory(15); expect(h.present).toBe(15); expect(h.past.length).toBe(0); });
  it('createHistory initial 16', () => { const h = createHistory(16); expect(h.present).toBe(16); expect(h.past.length).toBe(0); });
  it('createHistory initial 17', () => { const h = createHistory(17); expect(h.present).toBe(17); expect(h.past.length).toBe(0); });
  it('createHistory initial 18', () => { const h = createHistory(18); expect(h.present).toBe(18); expect(h.past.length).toBe(0); });
  it('createHistory initial 19', () => { const h = createHistory(19); expect(h.present).toBe(19); expect(h.past.length).toBe(0); });
  it('createHistory initial 20', () => { const h = createHistory(20); expect(h.present).toBe(20); expect(h.past.length).toBe(0); });
  it('createHistory initial 21', () => { const h = createHistory(21); expect(h.present).toBe(21); expect(h.past.length).toBe(0); });
  it('createHistory initial 22', () => { const h = createHistory(22); expect(h.present).toBe(22); expect(h.past.length).toBe(0); });
  it('createHistory initial 23', () => { const h = createHistory(23); expect(h.present).toBe(23); expect(h.past.length).toBe(0); });
  it('createHistory initial 24', () => { const h = createHistory(24); expect(h.present).toBe(24); expect(h.past.length).toBe(0); });
  it('createHistory initial 25', () => { const h = createHistory(25); expect(h.present).toBe(25); expect(h.past.length).toBe(0); });
  it('createHistory initial 26', () => { const h = createHistory(26); expect(h.present).toBe(26); expect(h.past.length).toBe(0); });
  it('createHistory initial 27', () => { const h = createHistory(27); expect(h.present).toBe(27); expect(h.past.length).toBe(0); });
  it('createHistory initial 28', () => { const h = createHistory(28); expect(h.present).toBe(28); expect(h.past.length).toBe(0); });
  it('createHistory initial 29', () => { const h = createHistory(29); expect(h.present).toBe(29); expect(h.past.length).toBe(0); });
  it('createHistory initial 30', () => { const h = createHistory(30); expect(h.present).toBe(30); expect(h.past.length).toBe(0); });
  it('createHistory initial 31', () => { const h = createHistory(31); expect(h.present).toBe(31); expect(h.past.length).toBe(0); });
  it('createHistory initial 32', () => { const h = createHistory(32); expect(h.present).toBe(32); expect(h.past.length).toBe(0); });
  it('createHistory initial 33', () => { const h = createHistory(33); expect(h.present).toBe(33); expect(h.past.length).toBe(0); });
  it('createHistory initial 34', () => { const h = createHistory(34); expect(h.present).toBe(34); expect(h.past.length).toBe(0); });
  it('createHistory initial 35', () => { const h = createHistory(35); expect(h.present).toBe(35); expect(h.past.length).toBe(0); });
  it('createHistory initial 36', () => { const h = createHistory(36); expect(h.present).toBe(36); expect(h.past.length).toBe(0); });
  it('createHistory initial 37', () => { const h = createHistory(37); expect(h.present).toBe(37); expect(h.past.length).toBe(0); });
  it('createHistory initial 38', () => { const h = createHistory(38); expect(h.present).toBe(38); expect(h.past.length).toBe(0); });
  it('createHistory initial 39', () => { const h = createHistory(39); expect(h.present).toBe(39); expect(h.past.length).toBe(0); });
  it('createHistory initial 40', () => { const h = createHistory(40); expect(h.present).toBe(40); expect(h.past.length).toBe(0); });
  it('createHistory initial 41', () => { const h = createHistory(41); expect(h.present).toBe(41); expect(h.past.length).toBe(0); });
  it('createHistory initial 42', () => { const h = createHistory(42); expect(h.present).toBe(42); expect(h.past.length).toBe(0); });
  it('createHistory initial 43', () => { const h = createHistory(43); expect(h.present).toBe(43); expect(h.past.length).toBe(0); });
  it('createHistory initial 44', () => { const h = createHistory(44); expect(h.present).toBe(44); expect(h.past.length).toBe(0); });
  it('createHistory initial 45', () => { const h = createHistory(45); expect(h.present).toBe(45); expect(h.past.length).toBe(0); });
  it('createHistory initial 46', () => { const h = createHistory(46); expect(h.present).toBe(46); expect(h.past.length).toBe(0); });
  it('createHistory initial 47', () => { const h = createHistory(47); expect(h.present).toBe(47); expect(h.past.length).toBe(0); });
  it('createHistory initial 48', () => { const h = createHistory(48); expect(h.present).toBe(48); expect(h.past.length).toBe(0); });
  it('createHistory initial 49', () => { const h = createHistory(49); expect(h.present).toBe(49); expect(h.past.length).toBe(0); });
  it('createHistory initial 50', () => { const h = createHistory(50); expect(h.present).toBe(50); expect(h.past.length).toBe(0); });
  it('pushHistory 1', () => { const h = pushHistory(createHistory(1), 2); expect(h.present).toBe(2); expect(h.past).toContain(1); });
  it('pushHistory 2', () => { const h = pushHistory(createHistory(2), 3); expect(h.present).toBe(3); expect(h.past).toContain(2); });
  it('pushHistory 3', () => { const h = pushHistory(createHistory(3), 4); expect(h.present).toBe(4); expect(h.past).toContain(3); });
  it('pushHistory 4', () => { const h = pushHistory(createHistory(4), 5); expect(h.present).toBe(5); expect(h.past).toContain(4); });
  it('pushHistory 5', () => { const h = pushHistory(createHistory(5), 6); expect(h.present).toBe(6); expect(h.past).toContain(5); });
  it('pushHistory 6', () => { const h = pushHistory(createHistory(6), 7); expect(h.present).toBe(7); expect(h.past).toContain(6); });
  it('pushHistory 7', () => { const h = pushHistory(createHistory(7), 8); expect(h.present).toBe(8); expect(h.past).toContain(7); });
  it('pushHistory 8', () => { const h = pushHistory(createHistory(8), 9); expect(h.present).toBe(9); expect(h.past).toContain(8); });
  it('pushHistory 9', () => { const h = pushHistory(createHistory(9), 10); expect(h.present).toBe(10); expect(h.past).toContain(9); });
  it('pushHistory 10', () => { const h = pushHistory(createHistory(10), 11); expect(h.present).toBe(11); expect(h.past).toContain(10); });
  it('pushHistory 11', () => { const h = pushHistory(createHistory(11), 12); expect(h.present).toBe(12); expect(h.past).toContain(11); });
  it('pushHistory 12', () => { const h = pushHistory(createHistory(12), 13); expect(h.present).toBe(13); expect(h.past).toContain(12); });
  it('pushHistory 13', () => { const h = pushHistory(createHistory(13), 14); expect(h.present).toBe(14); expect(h.past).toContain(13); });
  it('pushHistory 14', () => { const h = pushHistory(createHistory(14), 15); expect(h.present).toBe(15); expect(h.past).toContain(14); });
  it('pushHistory 15', () => { const h = pushHistory(createHistory(15), 16); expect(h.present).toBe(16); expect(h.past).toContain(15); });
  it('pushHistory 16', () => { const h = pushHistory(createHistory(16), 17); expect(h.present).toBe(17); expect(h.past).toContain(16); });
  it('pushHistory 17', () => { const h = pushHistory(createHistory(17), 18); expect(h.present).toBe(18); expect(h.past).toContain(17); });
  it('pushHistory 18', () => { const h = pushHistory(createHistory(18), 19); expect(h.present).toBe(19); expect(h.past).toContain(18); });
  it('pushHistory 19', () => { const h = pushHistory(createHistory(19), 20); expect(h.present).toBe(20); expect(h.past).toContain(19); });
  it('pushHistory 20', () => { const h = pushHistory(createHistory(20), 21); expect(h.present).toBe(21); expect(h.past).toContain(20); });
  it('pushHistory 21', () => { const h = pushHistory(createHistory(21), 22); expect(h.present).toBe(22); expect(h.past).toContain(21); });
  it('pushHistory 22', () => { const h = pushHistory(createHistory(22), 23); expect(h.present).toBe(23); expect(h.past).toContain(22); });
  it('pushHistory 23', () => { const h = pushHistory(createHistory(23), 24); expect(h.present).toBe(24); expect(h.past).toContain(23); });
  it('pushHistory 24', () => { const h = pushHistory(createHistory(24), 25); expect(h.present).toBe(25); expect(h.past).toContain(24); });
  it('pushHistory 25', () => { const h = pushHistory(createHistory(25), 26); expect(h.present).toBe(26); expect(h.past).toContain(25); });
  it('pushHistory 26', () => { const h = pushHistory(createHistory(26), 27); expect(h.present).toBe(27); expect(h.past).toContain(26); });
  it('pushHistory 27', () => { const h = pushHistory(createHistory(27), 28); expect(h.present).toBe(28); expect(h.past).toContain(27); });
  it('pushHistory 28', () => { const h = pushHistory(createHistory(28), 29); expect(h.present).toBe(29); expect(h.past).toContain(28); });
  it('pushHistory 29', () => { const h = pushHistory(createHistory(29), 30); expect(h.present).toBe(30); expect(h.past).toContain(29); });
  it('pushHistory 30', () => { const h = pushHistory(createHistory(30), 31); expect(h.present).toBe(31); expect(h.past).toContain(30); });
  it('pushHistory 31', () => { const h = pushHistory(createHistory(31), 32); expect(h.present).toBe(32); expect(h.past).toContain(31); });
  it('pushHistory 32', () => { const h = pushHistory(createHistory(32), 33); expect(h.present).toBe(33); expect(h.past).toContain(32); });
  it('pushHistory 33', () => { const h = pushHistory(createHistory(33), 34); expect(h.present).toBe(34); expect(h.past).toContain(33); });
  it('pushHistory 34', () => { const h = pushHistory(createHistory(34), 35); expect(h.present).toBe(35); expect(h.past).toContain(34); });
  it('pushHistory 35', () => { const h = pushHistory(createHistory(35), 36); expect(h.present).toBe(36); expect(h.past).toContain(35); });
  it('pushHistory 36', () => { const h = pushHistory(createHistory(36), 37); expect(h.present).toBe(37); expect(h.past).toContain(36); });
  it('pushHistory 37', () => { const h = pushHistory(createHistory(37), 38); expect(h.present).toBe(38); expect(h.past).toContain(37); });
  it('pushHistory 38', () => { const h = pushHistory(createHistory(38), 39); expect(h.present).toBe(39); expect(h.past).toContain(38); });
  it('pushHistory 39', () => { const h = pushHistory(createHistory(39), 40); expect(h.present).toBe(40); expect(h.past).toContain(39); });
  it('pushHistory 40', () => { const h = pushHistory(createHistory(40), 41); expect(h.present).toBe(41); expect(h.past).toContain(40); });
  it('pushHistory 41', () => { const h = pushHistory(createHistory(41), 42); expect(h.present).toBe(42); expect(h.past).toContain(41); });
  it('pushHistory 42', () => { const h = pushHistory(createHistory(42), 43); expect(h.present).toBe(43); expect(h.past).toContain(42); });
  it('pushHistory 43', () => { const h = pushHistory(createHistory(43), 44); expect(h.present).toBe(44); expect(h.past).toContain(43); });
  it('pushHistory 44', () => { const h = pushHistory(createHistory(44), 45); expect(h.present).toBe(45); expect(h.past).toContain(44); });
  it('pushHistory 45', () => { const h = pushHistory(createHistory(45), 46); expect(h.present).toBe(46); expect(h.past).toContain(45); });
  it('pushHistory 46', () => { const h = pushHistory(createHistory(46), 47); expect(h.present).toBe(47); expect(h.past).toContain(46); });
  it('pushHistory 47', () => { const h = pushHistory(createHistory(47), 48); expect(h.present).toBe(48); expect(h.past).toContain(47); });
  it('pushHistory 48', () => { const h = pushHistory(createHistory(48), 49); expect(h.present).toBe(49); expect(h.past).toContain(48); });
  it('pushHistory 49', () => { const h = pushHistory(createHistory(49), 50); expect(h.present).toBe(50); expect(h.past).toContain(49); });
  it('pushHistory 50', () => { const h = pushHistory(createHistory(50), 51); expect(h.present).toBe(51); expect(h.past).toContain(50); });
  it('undoHistory 1', () => { const h = undoHistory(pushHistory(createHistory(1), 2)); expect(h.present).toBe(1); });
  it('undoHistory 2', () => { const h = undoHistory(pushHistory(createHistory(2), 3)); expect(h.present).toBe(2); });
  it('undoHistory 3', () => { const h = undoHistory(pushHistory(createHistory(3), 4)); expect(h.present).toBe(3); });
  it('undoHistory 4', () => { const h = undoHistory(pushHistory(createHistory(4), 5)); expect(h.present).toBe(4); });
  it('undoHistory 5', () => { const h = undoHistory(pushHistory(createHistory(5), 6)); expect(h.present).toBe(5); });
  it('undoHistory 6', () => { const h = undoHistory(pushHistory(createHistory(6), 7)); expect(h.present).toBe(6); });
  it('undoHistory 7', () => { const h = undoHistory(pushHistory(createHistory(7), 8)); expect(h.present).toBe(7); });
  it('undoHistory 8', () => { const h = undoHistory(pushHistory(createHistory(8), 9)); expect(h.present).toBe(8); });
  it('undoHistory 9', () => { const h = undoHistory(pushHistory(createHistory(9), 10)); expect(h.present).toBe(9); });
  it('undoHistory 10', () => { const h = undoHistory(pushHistory(createHistory(10), 11)); expect(h.present).toBe(10); });
  it('undoHistory 11', () => { const h = undoHistory(pushHistory(createHistory(11), 12)); expect(h.present).toBe(11); });
  it('undoHistory 12', () => { const h = undoHistory(pushHistory(createHistory(12), 13)); expect(h.present).toBe(12); });
  it('undoHistory 13', () => { const h = undoHistory(pushHistory(createHistory(13), 14)); expect(h.present).toBe(13); });
  it('undoHistory 14', () => { const h = undoHistory(pushHistory(createHistory(14), 15)); expect(h.present).toBe(14); });
  it('undoHistory 15', () => { const h = undoHistory(pushHistory(createHistory(15), 16)); expect(h.present).toBe(15); });
  it('undoHistory 16', () => { const h = undoHistory(pushHistory(createHistory(16), 17)); expect(h.present).toBe(16); });
  it('undoHistory 17', () => { const h = undoHistory(pushHistory(createHistory(17), 18)); expect(h.present).toBe(17); });
  it('undoHistory 18', () => { const h = undoHistory(pushHistory(createHistory(18), 19)); expect(h.present).toBe(18); });
  it('undoHistory 19', () => { const h = undoHistory(pushHistory(createHistory(19), 20)); expect(h.present).toBe(19); });
  it('undoHistory 20', () => { const h = undoHistory(pushHistory(createHistory(20), 21)); expect(h.present).toBe(20); });
  it('undoHistory 21', () => { const h = undoHistory(pushHistory(createHistory(21), 22)); expect(h.present).toBe(21); });
  it('undoHistory 22', () => { const h = undoHistory(pushHistory(createHistory(22), 23)); expect(h.present).toBe(22); });
  it('undoHistory 23', () => { const h = undoHistory(pushHistory(createHistory(23), 24)); expect(h.present).toBe(23); });
  it('undoHistory 24', () => { const h = undoHistory(pushHistory(createHistory(24), 25)); expect(h.present).toBe(24); });
  it('undoHistory 25', () => { const h = undoHistory(pushHistory(createHistory(25), 26)); expect(h.present).toBe(25); });
  it('redoHistory 1', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(1), 2))); expect(h.present).toBe(2); });
  it('redoHistory 2', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(2), 3))); expect(h.present).toBe(3); });
  it('redoHistory 3', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(3), 4))); expect(h.present).toBe(4); });
  it('redoHistory 4', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(4), 5))); expect(h.present).toBe(5); });
  it('redoHistory 5', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(5), 6))); expect(h.present).toBe(6); });
  it('redoHistory 6', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(6), 7))); expect(h.present).toBe(7); });
  it('redoHistory 7', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(7), 8))); expect(h.present).toBe(8); });
  it('redoHistory 8', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(8), 9))); expect(h.present).toBe(9); });
  it('redoHistory 9', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(9), 10))); expect(h.present).toBe(10); });
  it('redoHistory 10', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(10), 11))); expect(h.present).toBe(11); });
  it('redoHistory 11', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(11), 12))); expect(h.present).toBe(12); });
  it('redoHistory 12', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(12), 13))); expect(h.present).toBe(13); });
  it('redoHistory 13', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(13), 14))); expect(h.present).toBe(14); });
  it('redoHistory 14', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(14), 15))); expect(h.present).toBe(15); });
  it('redoHistory 15', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(15), 16))); expect(h.present).toBe(16); });
  it('redoHistory 16', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(16), 17))); expect(h.present).toBe(17); });
  it('redoHistory 17', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(17), 18))); expect(h.present).toBe(18); });
  it('redoHistory 18', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(18), 19))); expect(h.present).toBe(19); });
  it('redoHistory 19', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(19), 20))); expect(h.present).toBe(20); });
  it('redoHistory 20', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(20), 21))); expect(h.present).toBe(21); });
  it('redoHistory 21', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(21), 22))); expect(h.present).toBe(22); });
  it('redoHistory 22', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(22), 23))); expect(h.present).toBe(23); });
  it('redoHistory 23', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(23), 24))); expect(h.present).toBe(24); });
  it('redoHistory 24', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(24), 25))); expect(h.present).toBe(25); });
  it('redoHistory 25', () => { const h = redoHistory(undoHistory(pushHistory(createHistory(25), 26))); expect(h.present).toBe(26); });
  it('canUndo false on fresh', () => { expect(canUndo(createHistory(0))).toBe(false); });
  it('canRedo false on fresh', () => { expect(canRedo(createHistory(0))).toBe(false); });
  it('canUndo true after push', () => { expect(canUndo(pushHistory(createHistory(0), 1))).toBe(true); });
});

describe('extra state-utils coverage', () => {
  it('set preserves other keys 1', () => { const o = { a: 1, b: 2, c: 3 }; const r = set(o, 'b', 3); expect(r.a).toBe(1); expect(r.c).toBe(3); });
  it('set preserves other keys 2', () => { const o = { a: 2, b: 3, c: 4 }; const r = set(o, 'b', 6); expect(r.a).toBe(2); expect(r.c).toBe(4); });
  it('set preserves other keys 3', () => { const o = { a: 3, b: 4, c: 5 }; const r = set(o, 'b', 9); expect(r.a).toBe(3); expect(r.c).toBe(5); });
  it('set preserves other keys 4', () => { const o = { a: 4, b: 5, c: 6 }; const r = set(o, 'b', 12); expect(r.a).toBe(4); expect(r.c).toBe(6); });
  it('set preserves other keys 5', () => { const o = { a: 5, b: 6, c: 7 }; const r = set(o, 'b', 15); expect(r.a).toBe(5); expect(r.c).toBe(7); });
  it('set preserves other keys 6', () => { const o = { a: 6, b: 7, c: 8 }; const r = set(o, 'b', 18); expect(r.a).toBe(6); expect(r.c).toBe(8); });
  it('set preserves other keys 7', () => { const o = { a: 7, b: 8, c: 9 }; const r = set(o, 'b', 21); expect(r.a).toBe(7); expect(r.c).toBe(9); });
  it('set preserves other keys 8', () => { const o = { a: 8, b: 9, c: 10 }; const r = set(o, 'b', 24); expect(r.a).toBe(8); expect(r.c).toBe(10); });
  it('set preserves other keys 9', () => { const o = { a: 9, b: 10, c: 11 }; const r = set(o, 'b', 27); expect(r.a).toBe(9); expect(r.c).toBe(11); });
  it('set preserves other keys 10', () => { const o = { a: 10, b: 11, c: 12 }; const r = set(o, 'b', 30); expect(r.a).toBe(10); expect(r.c).toBe(12); });
  it('set preserves other keys 11', () => { const o = { a: 11, b: 12, c: 13 }; const r = set(o, 'b', 33); expect(r.a).toBe(11); expect(r.c).toBe(13); });
  it('set preserves other keys 12', () => { const o = { a: 12, b: 13, c: 14 }; const r = set(o, 'b', 36); expect(r.a).toBe(12); expect(r.c).toBe(14); });
  it('set preserves other keys 13', () => { const o = { a: 13, b: 14, c: 15 }; const r = set(o, 'b', 39); expect(r.a).toBe(13); expect(r.c).toBe(15); });
  it('set preserves other keys 14', () => { const o = { a: 14, b: 15, c: 16 }; const r = set(o, 'b', 42); expect(r.a).toBe(14); expect(r.c).toBe(16); });
  it('set preserves other keys 15', () => { const o = { a: 15, b: 16, c: 17 }; const r = set(o, 'b', 45); expect(r.a).toBe(15); expect(r.c).toBe(17); });
  it('set preserves other keys 16', () => { const o = { a: 16, b: 17, c: 18 }; const r = set(o, 'b', 48); expect(r.a).toBe(16); expect(r.c).toBe(18); });
  it('set preserves other keys 17', () => { const o = { a: 17, b: 18, c: 19 }; const r = set(o, 'b', 51); expect(r.a).toBe(17); expect(r.c).toBe(19); });
  it('set preserves other keys 18', () => { const o = { a: 18, b: 19, c: 20 }; const r = set(o, 'b', 54); expect(r.a).toBe(18); expect(r.c).toBe(20); });
  it('set preserves other keys 19', () => { const o = { a: 19, b: 20, c: 21 }; const r = set(o, 'b', 57); expect(r.a).toBe(19); expect(r.c).toBe(21); });
  it('set preserves other keys 20', () => { const o = { a: 20, b: 21, c: 22 }; const r = set(o, 'b', 60); expect(r.a).toBe(20); expect(r.c).toBe(22); });
  it('set preserves other keys 21', () => { const o = { a: 21, b: 22, c: 23 }; const r = set(o, 'b', 63); expect(r.a).toBe(21); expect(r.c).toBe(23); });
  it('set preserves other keys 22', () => { const o = { a: 22, b: 23, c: 24 }; const r = set(o, 'b', 66); expect(r.a).toBe(22); expect(r.c).toBe(24); });
  it('set preserves other keys 23', () => { const o = { a: 23, b: 24, c: 25 }; const r = set(o, 'b', 69); expect(r.a).toBe(23); expect(r.c).toBe(25); });
  it('set preserves other keys 24', () => { const o = { a: 24, b: 25, c: 26 }; const r = set(o, 'b', 72); expect(r.a).toBe(24); expect(r.c).toBe(26); });
  it('set preserves other keys 25', () => { const o = { a: 25, b: 26, c: 27 }; const r = set(o, 'b', 75); expect(r.a).toBe(25); expect(r.c).toBe(27); });
  it('set preserves other keys 26', () => { const o = { a: 26, b: 27, c: 28 }; const r = set(o, 'b', 78); expect(r.a).toBe(26); expect(r.c).toBe(28); });
  it('set preserves other keys 27', () => { const o = { a: 27, b: 28, c: 29 }; const r = set(o, 'b', 81); expect(r.a).toBe(27); expect(r.c).toBe(29); });
  it('set preserves other keys 28', () => { const o = { a: 28, b: 29, c: 30 }; const r = set(o, 'b', 84); expect(r.a).toBe(28); expect(r.c).toBe(30); });
  it('set preserves other keys 29', () => { const o = { a: 29, b: 30, c: 31 }; const r = set(o, 'b', 87); expect(r.a).toBe(29); expect(r.c).toBe(31); });
  it('set preserves other keys 30', () => { const o = { a: 30, b: 31, c: 32 }; const r = set(o, 'b', 90); expect(r.a).toBe(30); expect(r.c).toBe(32); });
  it('set preserves other keys 31', () => { const o = { a: 31, b: 32, c: 33 }; const r = set(o, 'b', 93); expect(r.a).toBe(31); expect(r.c).toBe(33); });
  it('set preserves other keys 32', () => { const o = { a: 32, b: 33, c: 34 }; const r = set(o, 'b', 96); expect(r.a).toBe(32); expect(r.c).toBe(34); });
  it('set preserves other keys 33', () => { const o = { a: 33, b: 34, c: 35 }; const r = set(o, 'b', 99); expect(r.a).toBe(33); expect(r.c).toBe(35); });
  it('set preserves other keys 34', () => { const o = { a: 34, b: 35, c: 36 }; const r = set(o, 'b', 102); expect(r.a).toBe(34); expect(r.c).toBe(36); });
  it('set preserves other keys 35', () => { const o = { a: 35, b: 36, c: 37 }; const r = set(o, 'b', 105); expect(r.a).toBe(35); expect(r.c).toBe(37); });
  it('set preserves other keys 36', () => { const o = { a: 36, b: 37, c: 38 }; const r = set(o, 'b', 108); expect(r.a).toBe(36); expect(r.c).toBe(38); });
  it('set preserves other keys 37', () => { const o = { a: 37, b: 38, c: 39 }; const r = set(o, 'b', 111); expect(r.a).toBe(37); expect(r.c).toBe(39); });
  it('set preserves other keys 38', () => { const o = { a: 38, b: 39, c: 40 }; const r = set(o, 'b', 114); expect(r.a).toBe(38); expect(r.c).toBe(40); });
  it('set preserves other keys 39', () => { const o = { a: 39, b: 40, c: 41 }; const r = set(o, 'b', 117); expect(r.a).toBe(39); expect(r.c).toBe(41); });
  it('set preserves other keys 40', () => { const o = { a: 40, b: 41, c: 42 }; const r = set(o, 'b', 120); expect(r.a).toBe(40); expect(r.c).toBe(42); });
  it('set preserves other keys 41', () => { const o = { a: 41, b: 42, c: 43 }; const r = set(o, 'b', 123); expect(r.a).toBe(41); expect(r.c).toBe(43); });
  it('set preserves other keys 42', () => { const o = { a: 42, b: 43, c: 44 }; const r = set(o, 'b', 126); expect(r.a).toBe(42); expect(r.c).toBe(44); });
  it('set preserves other keys 43', () => { const o = { a: 43, b: 44, c: 45 }; const r = set(o, 'b', 129); expect(r.a).toBe(43); expect(r.c).toBe(45); });
  it('set preserves other keys 44', () => { const o = { a: 44, b: 45, c: 46 }; const r = set(o, 'b', 132); expect(r.a).toBe(44); expect(r.c).toBe(46); });
  it('set preserves other keys 45', () => { const o = { a: 45, b: 46, c: 47 }; const r = set(o, 'b', 135); expect(r.a).toBe(45); expect(r.c).toBe(47); });
  it('set preserves other keys 46', () => { const o = { a: 46, b: 47, c: 48 }; const r = set(o, 'b', 138); expect(r.a).toBe(46); expect(r.c).toBe(48); });
  it('set preserves other keys 47', () => { const o = { a: 47, b: 48, c: 49 }; const r = set(o, 'b', 141); expect(r.a).toBe(47); expect(r.c).toBe(49); });
  it('set preserves other keys 48', () => { const o = { a: 48, b: 49, c: 50 }; const r = set(o, 'b', 144); expect(r.a).toBe(48); expect(r.c).toBe(50); });
  it('set preserves other keys 49', () => { const o = { a: 49, b: 50, c: 51 }; const r = set(o, 'b', 147); expect(r.a).toBe(49); expect(r.c).toBe(51); });
  it('set preserves other keys 50', () => { const o = { a: 50, b: 51, c: 52 }; const r = set(o, 'b', 150); expect(r.a).toBe(50); expect(r.c).toBe(52); });
  it('set preserves other keys 51', () => { const o = { a: 51, b: 52, c: 53 }; const r = set(o, 'b', 153); expect(r.a).toBe(51); expect(r.c).toBe(53); });
  it('set preserves other keys 52', () => { const o = { a: 52, b: 53, c: 54 }; const r = set(o, 'b', 156); expect(r.a).toBe(52); expect(r.c).toBe(54); });
  it('set preserves other keys 53', () => { const o = { a: 53, b: 54, c: 55 }; const r = set(o, 'b', 159); expect(r.a).toBe(53); expect(r.c).toBe(55); });
  it('set preserves other keys 54', () => { const o = { a: 54, b: 55, c: 56 }; const r = set(o, 'b', 162); expect(r.a).toBe(54); expect(r.c).toBe(56); });
  it('set preserves other keys 55', () => { const o = { a: 55, b: 56, c: 57 }; const r = set(o, 'b', 165); expect(r.a).toBe(55); expect(r.c).toBe(57); });
  it('set preserves other keys 56', () => { const o = { a: 56, b: 57, c: 58 }; const r = set(o, 'b', 168); expect(r.a).toBe(56); expect(r.c).toBe(58); });
  it('set preserves other keys 57', () => { const o = { a: 57, b: 58, c: 59 }; const r = set(o, 'b', 171); expect(r.a).toBe(57); expect(r.c).toBe(59); });
  it('set preserves other keys 58', () => { const o = { a: 58, b: 59, c: 60 }; const r = set(o, 'b', 174); expect(r.a).toBe(58); expect(r.c).toBe(60); });
  it('set preserves other keys 59', () => { const o = { a: 59, b: 60, c: 61 }; const r = set(o, 'b', 177); expect(r.a).toBe(59); expect(r.c).toBe(61); });
  it('set preserves other keys 60', () => { const o = { a: 60, b: 61, c: 62 }; const r = set(o, 'b', 180); expect(r.a).toBe(60); expect(r.c).toBe(62); });
  it('set preserves other keys 61', () => { const o = { a: 61, b: 62, c: 63 }; const r = set(o, 'b', 183); expect(r.a).toBe(61); expect(r.c).toBe(63); });
  it('set preserves other keys 62', () => { const o = { a: 62, b: 63, c: 64 }; const r = set(o, 'b', 186); expect(r.a).toBe(62); expect(r.c).toBe(64); });
  it('set preserves other keys 63', () => { const o = { a: 63, b: 64, c: 65 }; const r = set(o, 'b', 189); expect(r.a).toBe(63); expect(r.c).toBe(65); });
  it('set preserves other keys 64', () => { const o = { a: 64, b: 65, c: 66 }; const r = set(o, 'b', 192); expect(r.a).toBe(64); expect(r.c).toBe(66); });
  it('set preserves other keys 65', () => { const o = { a: 65, b: 66, c: 67 }; const r = set(o, 'b', 195); expect(r.a).toBe(65); expect(r.c).toBe(67); });
  it('set preserves other keys 66', () => { const o = { a: 66, b: 67, c: 68 }; const r = set(o, 'b', 198); expect(r.a).toBe(66); expect(r.c).toBe(68); });
  it('set preserves other keys 67', () => { const o = { a: 67, b: 68, c: 69 }; const r = set(o, 'b', 201); expect(r.a).toBe(67); expect(r.c).toBe(69); });
  it('set preserves other keys 68', () => { const o = { a: 68, b: 69, c: 70 }; const r = set(o, 'b', 204); expect(r.a).toBe(68); expect(r.c).toBe(70); });
  it('set preserves other keys 69', () => { const o = { a: 69, b: 70, c: 71 }; const r = set(o, 'b', 207); expect(r.a).toBe(69); expect(r.c).toBe(71); });
  it('set preserves other keys 70', () => { const o = { a: 70, b: 71, c: 72 }; const r = set(o, 'b', 210); expect(r.a).toBe(70); expect(r.c).toBe(72); });
  it('set preserves other keys 71', () => { const o = { a: 71, b: 72, c: 73 }; const r = set(o, 'b', 213); expect(r.a).toBe(71); expect(r.c).toBe(73); });
  it('set preserves other keys 72', () => { const o = { a: 72, b: 73, c: 74 }; const r = set(o, 'b', 216); expect(r.a).toBe(72); expect(r.c).toBe(74); });
  it('set preserves other keys 73', () => { const o = { a: 73, b: 74, c: 75 }; const r = set(o, 'b', 219); expect(r.a).toBe(73); expect(r.c).toBe(75); });
  it('set preserves other keys 74', () => { const o = { a: 74, b: 75, c: 76 }; const r = set(o, 'b', 222); expect(r.a).toBe(74); expect(r.c).toBe(76); });
  it('set preserves other keys 75', () => { const o = { a: 75, b: 76, c: 77 }; const r = set(o, 'b', 225); expect(r.a).toBe(75); expect(r.c).toBe(77); });
  it('set preserves other keys 76', () => { const o = { a: 76, b: 77, c: 78 }; const r = set(o, 'b', 228); expect(r.a).toBe(76); expect(r.c).toBe(78); });
  it('set preserves other keys 77', () => { const o = { a: 77, b: 78, c: 79 }; const r = set(o, 'b', 231); expect(r.a).toBe(77); expect(r.c).toBe(79); });
  it('set preserves other keys 78', () => { const o = { a: 78, b: 79, c: 80 }; const r = set(o, 'b', 234); expect(r.a).toBe(78); expect(r.c).toBe(80); });
  it('set preserves other keys 79', () => { const o = { a: 79, b: 80, c: 81 }; const r = set(o, 'b', 237); expect(r.a).toBe(79); expect(r.c).toBe(81); });
  it('set preserves other keys 80', () => { const o = { a: 80, b: 81, c: 82 }; const r = set(o, 'b', 240); expect(r.a).toBe(80); expect(r.c).toBe(82); });
  it('getIn returns undefined for missing path 1', () => { const o = { x: { y: 1 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 2', () => { const o = { x: { y: 2 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 3', () => { const o = { x: { y: 3 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 4', () => { const o = { x: { y: 4 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 5', () => { const o = { x: { y: 5 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 6', () => { const o = { x: { y: 6 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 7', () => { const o = { x: { y: 7 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 8', () => { const o = { x: { y: 8 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 9', () => { const o = { x: { y: 9 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 10', () => { const o = { x: { y: 10 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 11', () => { const o = { x: { y: 11 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 12', () => { const o = { x: { y: 12 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 13', () => { const o = { x: { y: 13 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 14', () => { const o = { x: { y: 14 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 15', () => { const o = { x: { y: 15 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 16', () => { const o = { x: { y: 16 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 17', () => { const o = { x: { y: 17 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 18', () => { const o = { x: { y: 18 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 19', () => { const o = { x: { y: 19 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 20', () => { const o = { x: { y: 20 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 21', () => { const o = { x: { y: 21 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 22', () => { const o = { x: { y: 22 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 23', () => { const o = { x: { y: 23 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 24', () => { const o = { x: { y: 24 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 25', () => { const o = { x: { y: 25 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 26', () => { const o = { x: { y: 26 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 27', () => { const o = { x: { y: 27 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 28', () => { const o = { x: { y: 28 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 29', () => { const o = { x: { y: 29 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 30', () => { const o = { x: { y: 30 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 31', () => { const o = { x: { y: 31 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 32', () => { const o = { x: { y: 32 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 33', () => { const o = { x: { y: 33 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 34', () => { const o = { x: { y: 34 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 35', () => { const o = { x: { y: 35 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 36', () => { const o = { x: { y: 36 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 37', () => { const o = { x: { y: 37 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 38', () => { const o = { x: { y: 38 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 39', () => { const o = { x: { y: 39 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('getIn returns undefined for missing path 40', () => { const o = { x: { y: 40 } }; expect(getIn(o, ['x','z'])).toBeUndefined(); });
  it('merge preserves original 1', () => { const a = { n: 1 }; const b = { m: 2 }; const r = merge(a, b as any); expect(r.n).toBe(1); });
  it('merge preserves original 2', () => { const a = { n: 2 }; const b = { m: 3 }; const r = merge(a, b as any); expect(r.n).toBe(2); });
  it('merge preserves original 3', () => { const a = { n: 3 }; const b = { m: 4 }; const r = merge(a, b as any); expect(r.n).toBe(3); });
  it('merge preserves original 4', () => { const a = { n: 4 }; const b = { m: 5 }; const r = merge(a, b as any); expect(r.n).toBe(4); });
  it('merge preserves original 5', () => { const a = { n: 5 }; const b = { m: 6 }; const r = merge(a, b as any); expect(r.n).toBe(5); });
  it('merge preserves original 6', () => { const a = { n: 6 }; const b = { m: 7 }; const r = merge(a, b as any); expect(r.n).toBe(6); });
  it('merge preserves original 7', () => { const a = { n: 7 }; const b = { m: 8 }; const r = merge(a, b as any); expect(r.n).toBe(7); });
  it('merge preserves original 8', () => { const a = { n: 8 }; const b = { m: 9 }; const r = merge(a, b as any); expect(r.n).toBe(8); });
  it('merge preserves original 9', () => { const a = { n: 9 }; const b = { m: 10 }; const r = merge(a, b as any); expect(r.n).toBe(9); });
  it('merge preserves original 10', () => { const a = { n: 10 }; const b = { m: 11 }; const r = merge(a, b as any); expect(r.n).toBe(10); });
  it('merge preserves original 11', () => { const a = { n: 11 }; const b = { m: 12 }; const r = merge(a, b as any); expect(r.n).toBe(11); });
  it('merge preserves original 12', () => { const a = { n: 12 }; const b = { m: 13 }; const r = merge(a, b as any); expect(r.n).toBe(12); });
  it('merge preserves original 13', () => { const a = { n: 13 }; const b = { m: 14 }; const r = merge(a, b as any); expect(r.n).toBe(13); });
  it('merge preserves original 14', () => { const a = { n: 14 }; const b = { m: 15 }; const r = merge(a, b as any); expect(r.n).toBe(14); });
  it('merge preserves original 15', () => { const a = { n: 15 }; const b = { m: 16 }; const r = merge(a, b as any); expect(r.n).toBe(15); });
  it('merge preserves original 16', () => { const a = { n: 16 }; const b = { m: 17 }; const r = merge(a, b as any); expect(r.n).toBe(16); });
  it('merge preserves original 17', () => { const a = { n: 17 }; const b = { m: 18 }; const r = merge(a, b as any); expect(r.n).toBe(17); });
  it('merge preserves original 18', () => { const a = { n: 18 }; const b = { m: 19 }; const r = merge(a, b as any); expect(r.n).toBe(18); });
  it('merge preserves original 19', () => { const a = { n: 19 }; const b = { m: 20 }; const r = merge(a, b as any); expect(r.n).toBe(19); });
  it('merge preserves original 20', () => { const a = { n: 20 }; const b = { m: 21 }; const r = merge(a, b as any); expect(r.n).toBe(20); });
  it('merge preserves original 21', () => { const a = { n: 21 }; const b = { m: 22 }; const r = merge(a, b as any); expect(r.n).toBe(21); });
  it('merge preserves original 22', () => { const a = { n: 22 }; const b = { m: 23 }; const r = merge(a, b as any); expect(r.n).toBe(22); });
  it('merge preserves original 23', () => { const a = { n: 23 }; const b = { m: 24 }; const r = merge(a, b as any); expect(r.n).toBe(23); });
  it('merge preserves original 24', () => { const a = { n: 24 }; const b = { m: 25 }; const r = merge(a, b as any); expect(r.n).toBe(24); });
  it('merge preserves original 25', () => { const a = { n: 25 }; const b = { m: 26 }; const r = merge(a, b as any); expect(r.n).toBe(25); });
  it('merge preserves original 26', () => { const a = { n: 26 }; const b = { m: 27 }; const r = merge(a, b as any); expect(r.n).toBe(26); });
  it('merge preserves original 27', () => { const a = { n: 27 }; const b = { m: 28 }; const r = merge(a, b as any); expect(r.n).toBe(27); });
  it('merge preserves original 28', () => { const a = { n: 28 }; const b = { m: 29 }; const r = merge(a, b as any); expect(r.n).toBe(28); });
  it('merge preserves original 29', () => { const a = { n: 29 }; const b = { m: 30 }; const r = merge(a, b as any); expect(r.n).toBe(29); });
  it('merge preserves original 30', () => { const a = { n: 30 }; const b = { m: 31 }; const r = merge(a, b as any); expect(r.n).toBe(30); });
  it('merge preserves original 31', () => { const a = { n: 31 }; const b = { m: 32 }; const r = merge(a, b as any); expect(r.n).toBe(31); });
  it('merge preserves original 32', () => { const a = { n: 32 }; const b = { m: 33 }; const r = merge(a, b as any); expect(r.n).toBe(32); });
  it('merge preserves original 33', () => { const a = { n: 33 }; const b = { m: 34 }; const r = merge(a, b as any); expect(r.n).toBe(33); });
  it('merge preserves original 34', () => { const a = { n: 34 }; const b = { m: 35 }; const r = merge(a, b as any); expect(r.n).toBe(34); });
  it('merge preserves original 35', () => { const a = { n: 35 }; const b = { m: 36 }; const r = merge(a, b as any); expect(r.n).toBe(35); });
  it('merge preserves original 36', () => { const a = { n: 36 }; const b = { m: 37 }; const r = merge(a, b as any); expect(r.n).toBe(36); });
  it('merge preserves original 37', () => { const a = { n: 37 }; const b = { m: 38 }; const r = merge(a, b as any); expect(r.n).toBe(37); });
  it('merge preserves original 38', () => { const a = { n: 38 }; const b = { m: 39 }; const r = merge(a, b as any); expect(r.n).toBe(38); });
  it('merge preserves original 39', () => { const a = { n: 39 }; const b = { m: 40 }; const r = merge(a, b as any); expect(r.n).toBe(39); });
  it('merge preserves original 40', () => { const a = { n: 40 }; const b = { m: 41 }; const r = merge(a, b as any); expect(r.n).toBe(40); });
});
