// Copyright (c) 2026 Nexara DMCC. All rights reserved.
import { sleep, retry, parallel, sequential, memoize, once, Semaphore, EventQueue, createDeferred, allSettled, batch, mapConcurrent } from "../concurrency-utils";

describe("sleep", () => {
  it("resolves after delay", async () => { const start = Date.now(); await sleep(10); expect(Date.now() - start).toBeGreaterThanOrEqual(9); });
  it("sleep promise 1", () => { expect(sleep(1) instanceof Promise).toBe(true); });
  it("sleep promise 2", () => { expect(sleep(2) instanceof Promise).toBe(true); });
  it("sleep promise 3", () => { expect(sleep(3) instanceof Promise).toBe(true); });
  it("sleep promise 4", () => { expect(sleep(4) instanceof Promise).toBe(true); });
  it("sleep promise 5", () => { expect(sleep(5) instanceof Promise).toBe(true); });
  it("sleep promise 6", () => { expect(sleep(6) instanceof Promise).toBe(true); });
  it("sleep promise 7", () => { expect(sleep(7) instanceof Promise).toBe(true); });
  it("sleep promise 8", () => { expect(sleep(8) instanceof Promise).toBe(true); });
  it("sleep promise 9", () => { expect(sleep(9) instanceof Promise).toBe(true); });
  it("sleep promise 10", () => { expect(sleep(10) instanceof Promise).toBe(true); });
  it("sleep promise 11", () => { expect(sleep(11) instanceof Promise).toBe(true); });
  it("sleep promise 12", () => { expect(sleep(12) instanceof Promise).toBe(true); });
  it("sleep promise 13", () => { expect(sleep(13) instanceof Promise).toBe(true); });
  it("sleep promise 14", () => { expect(sleep(14) instanceof Promise).toBe(true); });
  it("sleep promise 15", () => { expect(sleep(15) instanceof Promise).toBe(true); });
  it("sleep promise 16", () => { expect(sleep(16) instanceof Promise).toBe(true); });
  it("sleep promise 17", () => { expect(sleep(17) instanceof Promise).toBe(true); });
  it("sleep promise 18", () => { expect(sleep(18) instanceof Promise).toBe(true); });
  it("sleep promise 19", () => { expect(sleep(19) instanceof Promise).toBe(true); });
  it("sleep promise 20", () => { expect(sleep(20) instanceof Promise).toBe(true); });
  it("sleep promise 21", () => { expect(sleep(21) instanceof Promise).toBe(true); });
  it("sleep promise 22", () => { expect(sleep(22) instanceof Promise).toBe(true); });
  it("sleep promise 23", () => { expect(sleep(23) instanceof Promise).toBe(true); });
  it("sleep promise 24", () => { expect(sleep(24) instanceof Promise).toBe(true); });
  it("sleep promise 25", () => { expect(sleep(25) instanceof Promise).toBe(true); });
  it("sleep promise 26", () => { expect(sleep(26) instanceof Promise).toBe(true); });
  it("sleep promise 27", () => { expect(sleep(27) instanceof Promise).toBe(true); });
  it("sleep promise 28", () => { expect(sleep(28) instanceof Promise).toBe(true); });
  it("sleep promise 29", () => { expect(sleep(29) instanceof Promise).toBe(true); });
  it("sleep promise 30", () => { expect(sleep(30) instanceof Promise).toBe(true); });
  it("sleep promise 31", () => { expect(sleep(31) instanceof Promise).toBe(true); });
  it("sleep promise 32", () => { expect(sleep(32) instanceof Promise).toBe(true); });
  it("sleep promise 33", () => { expect(sleep(33) instanceof Promise).toBe(true); });
  it("sleep promise 34", () => { expect(sleep(34) instanceof Promise).toBe(true); });
  it("sleep promise 35", () => { expect(sleep(35) instanceof Promise).toBe(true); });
  it("sleep promise 36", () => { expect(sleep(36) instanceof Promise).toBe(true); });
  it("sleep promise 37", () => { expect(sleep(37) instanceof Promise).toBe(true); });
  it("sleep promise 38", () => { expect(sleep(38) instanceof Promise).toBe(true); });
  it("sleep promise 39", () => { expect(sleep(39) instanceof Promise).toBe(true); });
  it("sleep promise 40", () => { expect(sleep(40) instanceof Promise).toBe(true); });
  it("sleep promise 41", () => { expect(sleep(41) instanceof Promise).toBe(true); });
  it("sleep promise 42", () => { expect(sleep(42) instanceof Promise).toBe(true); });
  it("sleep promise 43", () => { expect(sleep(43) instanceof Promise).toBe(true); });
  it("sleep promise 44", () => { expect(sleep(44) instanceof Promise).toBe(true); });
  it("sleep promise 45", () => { expect(sleep(45) instanceof Promise).toBe(true); });
  it("sleep promise 46", () => { expect(sleep(46) instanceof Promise).toBe(true); });
  it("sleep promise 47", () => { expect(sleep(47) instanceof Promise).toBe(true); });
  it("sleep promise 48", () => { expect(sleep(48) instanceof Promise).toBe(true); });
  it("sleep promise 49", () => { expect(sleep(49) instanceof Promise).toBe(true); });
  it("sleep promise 50", () => { expect(sleep(50) instanceof Promise).toBe(true); });
});

describe("retry", () => {
  it("succeeds on first try", async () => { const r = await retry(() => Promise.resolve(42), 3); expect(r).toBe(42); });
  it("retries on failure", async () => { let a=0; const r = await retry(() => { a++; if(a<3) return Promise.reject("e"); return Promise.resolve("ok"); }, 5); expect(r).toBe("ok"); });
  it("throws after max", async () => { await expect(retry(() => Promise.reject("fail"), 3)).rejects.toBe("fail"); });
  it("retry val 1", async () => { expect(await retry(() => Promise.resolve(1), 1)).toBe(1); });
  it("retry val 2", async () => { expect(await retry(() => Promise.resolve(2), 1)).toBe(2); });
  it("retry val 3", async () => { expect(await retry(() => Promise.resolve(3), 1)).toBe(3); });
  it("retry val 4", async () => { expect(await retry(() => Promise.resolve(4), 1)).toBe(4); });
  it("retry val 5", async () => { expect(await retry(() => Promise.resolve(5), 1)).toBe(5); });
  it("retry val 6", async () => { expect(await retry(() => Promise.resolve(6), 1)).toBe(6); });
  it("retry val 7", async () => { expect(await retry(() => Promise.resolve(7), 1)).toBe(7); });
  it("retry val 8", async () => { expect(await retry(() => Promise.resolve(8), 1)).toBe(8); });
  it("retry val 9", async () => { expect(await retry(() => Promise.resolve(9), 1)).toBe(9); });
  it("retry val 10", async () => { expect(await retry(() => Promise.resolve(10), 1)).toBe(10); });
  it("retry val 11", async () => { expect(await retry(() => Promise.resolve(11), 1)).toBe(11); });
  it("retry val 12", async () => { expect(await retry(() => Promise.resolve(12), 1)).toBe(12); });
  it("retry val 13", async () => { expect(await retry(() => Promise.resolve(13), 1)).toBe(13); });
  it("retry val 14", async () => { expect(await retry(() => Promise.resolve(14), 1)).toBe(14); });
  it("retry val 15", async () => { expect(await retry(() => Promise.resolve(15), 1)).toBe(15); });
  it("retry val 16", async () => { expect(await retry(() => Promise.resolve(16), 1)).toBe(16); });
  it("retry val 17", async () => { expect(await retry(() => Promise.resolve(17), 1)).toBe(17); });
  it("retry val 18", async () => { expect(await retry(() => Promise.resolve(18), 1)).toBe(18); });
  it("retry val 19", async () => { expect(await retry(() => Promise.resolve(19), 1)).toBe(19); });
  it("retry val 20", async () => { expect(await retry(() => Promise.resolve(20), 1)).toBe(20); });
  it("retry val 21", async () => { expect(await retry(() => Promise.resolve(21), 1)).toBe(21); });
  it("retry val 22", async () => { expect(await retry(() => Promise.resolve(22), 1)).toBe(22); });
  it("retry val 23", async () => { expect(await retry(() => Promise.resolve(23), 1)).toBe(23); });
  it("retry val 24", async () => { expect(await retry(() => Promise.resolve(24), 1)).toBe(24); });
  it("retry val 25", async () => { expect(await retry(() => Promise.resolve(25), 1)).toBe(25); });
  it("retry val 26", async () => { expect(await retry(() => Promise.resolve(26), 1)).toBe(26); });
  it("retry val 27", async () => { expect(await retry(() => Promise.resolve(27), 1)).toBe(27); });
  it("retry val 28", async () => { expect(await retry(() => Promise.resolve(28), 1)).toBe(28); });
  it("retry val 29", async () => { expect(await retry(() => Promise.resolve(29), 1)).toBe(29); });
  it("retry val 30", async () => { expect(await retry(() => Promise.resolve(30), 1)).toBe(30); });
  it("retry val 31", async () => { expect(await retry(() => Promise.resolve(31), 1)).toBe(31); });
  it("retry val 32", async () => { expect(await retry(() => Promise.resolve(32), 1)).toBe(32); });
  it("retry val 33", async () => { expect(await retry(() => Promise.resolve(33), 1)).toBe(33); });
  it("retry val 34", async () => { expect(await retry(() => Promise.resolve(34), 1)).toBe(34); });
  it("retry val 35", async () => { expect(await retry(() => Promise.resolve(35), 1)).toBe(35); });
  it("retry val 36", async () => { expect(await retry(() => Promise.resolve(36), 1)).toBe(36); });
  it("retry val 37", async () => { expect(await retry(() => Promise.resolve(37), 1)).toBe(37); });
  it("retry val 38", async () => { expect(await retry(() => Promise.resolve(38), 1)).toBe(38); });
  it("retry val 39", async () => { expect(await retry(() => Promise.resolve(39), 1)).toBe(39); });
  it("retry val 40", async () => { expect(await retry(() => Promise.resolve(40), 1)).toBe(40); });
  it("retry val 41", async () => { expect(await retry(() => Promise.resolve(41), 1)).toBe(41); });
  it("retry val 42", async () => { expect(await retry(() => Promise.resolve(42), 1)).toBe(42); });
  it("retry val 43", async () => { expect(await retry(() => Promise.resolve(43), 1)).toBe(43); });
  it("retry val 44", async () => { expect(await retry(() => Promise.resolve(44), 1)).toBe(44); });
  it("retry val 45", async () => { expect(await retry(() => Promise.resolve(45), 1)).toBe(45); });
  it("retry val 46", async () => { expect(await retry(() => Promise.resolve(46), 1)).toBe(46); });
  it("retry val 47", async () => { expect(await retry(() => Promise.resolve(47), 1)).toBe(47); });
  it("retry val 48", async () => { expect(await retry(() => Promise.resolve(48), 1)).toBe(48); });
  it("retry val 49", async () => { expect(await retry(() => Promise.resolve(49), 1)).toBe(49); });
  it("retry val 50", async () => { expect(await retry(() => Promise.resolve(50), 1)).toBe(50); });
  it("retry val 51", async () => { expect(await retry(() => Promise.resolve(51), 1)).toBe(51); });
  it("retry val 52", async () => { expect(await retry(() => Promise.resolve(52), 1)).toBe(52); });
  it("retry val 53", async () => { expect(await retry(() => Promise.resolve(53), 1)).toBe(53); });
  it("retry val 54", async () => { expect(await retry(() => Promise.resolve(54), 1)).toBe(54); });
  it("retry val 55", async () => { expect(await retry(() => Promise.resolve(55), 1)).toBe(55); });
  it("retry val 56", async () => { expect(await retry(() => Promise.resolve(56), 1)).toBe(56); });
  it("retry val 57", async () => { expect(await retry(() => Promise.resolve(57), 1)).toBe(57); });
  it("retry val 58", async () => { expect(await retry(() => Promise.resolve(58), 1)).toBe(58); });
  it("retry val 59", async () => { expect(await retry(() => Promise.resolve(59), 1)).toBe(59); });
  it("retry val 60", async () => { expect(await retry(() => Promise.resolve(60), 1)).toBe(60); });
  it("retry val 61", async () => { expect(await retry(() => Promise.resolve(61), 1)).toBe(61); });
  it("retry val 62", async () => { expect(await retry(() => Promise.resolve(62), 1)).toBe(62); });
  it("retry val 63", async () => { expect(await retry(() => Promise.resolve(63), 1)).toBe(63); });
  it("retry val 64", async () => { expect(await retry(() => Promise.resolve(64), 1)).toBe(64); });
  it("retry val 65", async () => { expect(await retry(() => Promise.resolve(65), 1)).toBe(65); });
  it("retry val 66", async () => { expect(await retry(() => Promise.resolve(66), 1)).toBe(66); });
  it("retry val 67", async () => { expect(await retry(() => Promise.resolve(67), 1)).toBe(67); });
  it("retry val 68", async () => { expect(await retry(() => Promise.resolve(68), 1)).toBe(68); });
  it("retry val 69", async () => { expect(await retry(() => Promise.resolve(69), 1)).toBe(69); });
  it("retry val 70", async () => { expect(await retry(() => Promise.resolve(70), 1)).toBe(70); });
  it("retry val 71", async () => { expect(await retry(() => Promise.resolve(71), 1)).toBe(71); });
  it("retry val 72", async () => { expect(await retry(() => Promise.resolve(72), 1)).toBe(72); });
  it("retry val 73", async () => { expect(await retry(() => Promise.resolve(73), 1)).toBe(73); });
  it("retry val 74", async () => { expect(await retry(() => Promise.resolve(74), 1)).toBe(74); });
  it("retry val 75", async () => { expect(await retry(() => Promise.resolve(75), 1)).toBe(75); });
  it("retry val 76", async () => { expect(await retry(() => Promise.resolve(76), 1)).toBe(76); });
  it("retry val 77", async () => { expect(await retry(() => Promise.resolve(77), 1)).toBe(77); });
  it("retry val 78", async () => { expect(await retry(() => Promise.resolve(78), 1)).toBe(78); });
  it("retry val 79", async () => { expect(await retry(() => Promise.resolve(79), 1)).toBe(79); });
  it("retry val 80", async () => { expect(await retry(() => Promise.resolve(80), 1)).toBe(80); });
  it("retry val 81", async () => { expect(await retry(() => Promise.resolve(81), 1)).toBe(81); });
  it("retry val 82", async () => { expect(await retry(() => Promise.resolve(82), 1)).toBe(82); });
  it("retry val 83", async () => { expect(await retry(() => Promise.resolve(83), 1)).toBe(83); });
  it("retry val 84", async () => { expect(await retry(() => Promise.resolve(84), 1)).toBe(84); });
  it("retry val 85", async () => { expect(await retry(() => Promise.resolve(85), 1)).toBe(85); });
  it("retry val 86", async () => { expect(await retry(() => Promise.resolve(86), 1)).toBe(86); });
  it("retry val 87", async () => { expect(await retry(() => Promise.resolve(87), 1)).toBe(87); });
  it("retry val 88", async () => { expect(await retry(() => Promise.resolve(88), 1)).toBe(88); });
  it("retry val 89", async () => { expect(await retry(() => Promise.resolve(89), 1)).toBe(89); });
  it("retry val 90", async () => { expect(await retry(() => Promise.resolve(90), 1)).toBe(90); });
  it("retry val 91", async () => { expect(await retry(() => Promise.resolve(91), 1)).toBe(91); });
  it("retry val 92", async () => { expect(await retry(() => Promise.resolve(92), 1)).toBe(92); });
  it("retry val 93", async () => { expect(await retry(() => Promise.resolve(93), 1)).toBe(93); });
  it("retry val 94", async () => { expect(await retry(() => Promise.resolve(94), 1)).toBe(94); });
  it("retry val 95", async () => { expect(await retry(() => Promise.resolve(95), 1)).toBe(95); });
  it("retry val 96", async () => { expect(await retry(() => Promise.resolve(96), 1)).toBe(96); });
  it("retry val 97", async () => { expect(await retry(() => Promise.resolve(97), 1)).toBe(97); });
  it("retry val 98", async () => { expect(await retry(() => Promise.resolve(98), 1)).toBe(98); });
  it("retry val 99", async () => { expect(await retry(() => Promise.resolve(99), 1)).toBe(99); });
  it("retry val 100", async () => { expect(await retry(() => Promise.resolve(100), 1)).toBe(100); });
});

// parallel tests
describe("parallel", () => {
  it("par1", async () => { expect((await parallel([() => Promise.resolve(1)], 1))[0]).toBe(1); });
  it("par2", async () => { const t = Array.from({length:2}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(2); });
  it("par3", async () => { const t = Array.from({length:3}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(3); });
  it("par4", async () => { const t = Array.from({length:4}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(4); });
  it("par5", async () => { const t = Array.from({length:5}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(5); });
  it("par6", async () => { const t = Array.from({length:6}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(6); });
  it("par7", async () => { const t = Array.from({length:7}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(7); });
  it("par8", async () => { const t = Array.from({length:8}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(8); });
  it("par9", async () => { const t = Array.from({length:9}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(9); });
  it("par10", async () => { const t = Array.from({length:10}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(10); });
  it("par11", async () => { const t = Array.from({length:11}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(11); });
  it("par12", async () => { const t = Array.from({length:12}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(12); });
  it("par13", async () => { const t = Array.from({length:13}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(13); });
  it("par14", async () => { const t = Array.from({length:14}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(14); });
  it("par15", async () => { const t = Array.from({length:15}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(15); });
  it("par16", async () => { const t = Array.from({length:16}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(16); });
  it("par17", async () => { const t = Array.from({length:17}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(17); });
  it("par18", async () => { const t = Array.from({length:18}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(18); });
  it("par19", async () => { const t = Array.from({length:19}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(19); });
  it("par20", async () => { const t = Array.from({length:20}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(20); });
  it("par21", async () => { const t = Array.from({length:21}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(21); });
  it("par22", async () => { const t = Array.from({length:22}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(22); });
  it("par23", async () => { const t = Array.from({length:23}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(23); });
  it("par24", async () => { const t = Array.from({length:24}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(24); });
  it("par25", async () => { const t = Array.from({length:25}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(25); });
  it("par26", async () => { const t = Array.from({length:26}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(26); });
  it("par27", async () => { const t = Array.from({length:27}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(27); });
  it("par28", async () => { const t = Array.from({length:28}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(28); });
  it("par29", async () => { const t = Array.from({length:29}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(29); });
  it("par30", async () => { const t = Array.from({length:30}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(30); });
  it("par31", async () => { const t = Array.from({length:31}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(31); });
  it("par32", async () => { const t = Array.from({length:32}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(32); });
  it("par33", async () => { const t = Array.from({length:33}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(33); });
  it("par34", async () => { const t = Array.from({length:34}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(34); });
  it("par35", async () => { const t = Array.from({length:35}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(35); });
  it("par36", async () => { const t = Array.from({length:36}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(36); });
  it("par37", async () => { const t = Array.from({length:37}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(37); });
  it("par38", async () => { const t = Array.from({length:38}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(38); });
  it("par39", async () => { const t = Array.from({length:39}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(39); });
  it("par40", async () => { const t = Array.from({length:40}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(40); });
  it("par41", async () => { const t = Array.from({length:41}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(41); });
  it("par42", async () => { const t = Array.from({length:42}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(42); });
  it("par43", async () => { const t = Array.from({length:43}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(43); });
  it("par44", async () => { const t = Array.from({length:44}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(44); });
  it("par45", async () => { const t = Array.from({length:45}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(45); });
  it("par46", async () => { const t = Array.from({length:46}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(46); });
  it("par47", async () => { const t = Array.from({length:47}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(47); });
  it("par48", async () => { const t = Array.from({length:48}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(48); });
  it("par49", async () => { const t = Array.from({length:49}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(49); });
  it("par50", async () => { const t = Array.from({length:50}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(50); });
  it("par51", async () => { const t = Array.from({length:51}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(51); });
  it("par52", async () => { const t = Array.from({length:52}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(52); });
  it("par53", async () => { const t = Array.from({length:53}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(53); });
  it("par54", async () => { const t = Array.from({length:54}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(54); });
  it("par55", async () => { const t = Array.from({length:55}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(55); });
  it("par56", async () => { const t = Array.from({length:56}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(56); });
  it("par57", async () => { const t = Array.from({length:57}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(57); });
  it("par58", async () => { const t = Array.from({length:58}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(58); });
  it("par59", async () => { const t = Array.from({length:59}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(59); });
  it("par60", async () => { const t = Array.from({length:60}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(60); });
  it("par61", async () => { const t = Array.from({length:61}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(61); });
  it("par62", async () => { const t = Array.from({length:62}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(62); });
  it("par63", async () => { const t = Array.from({length:63}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(63); });
  it("par64", async () => { const t = Array.from({length:64}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(64); });
  it("par65", async () => { const t = Array.from({length:65}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(65); });
  it("par66", async () => { const t = Array.from({length:66}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(66); });
  it("par67", async () => { const t = Array.from({length:67}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(67); });
  it("par68", async () => { const t = Array.from({length:68}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(68); });
  it("par69", async () => { const t = Array.from({length:69}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(69); });
  it("par70", async () => { const t = Array.from({length:70}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(70); });
  it("par71", async () => { const t = Array.from({length:71}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(71); });
  it("par72", async () => { const t = Array.from({length:72}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(72); });
  it("par73", async () => { const t = Array.from({length:73}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(73); });
  it("par74", async () => { const t = Array.from({length:74}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(74); });
  it("par75", async () => { const t = Array.from({length:75}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(75); });
  it("par76", async () => { const t = Array.from({length:76}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(76); });
  it("par77", async () => { const t = Array.from({length:77}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(77); });
  it("par78", async () => { const t = Array.from({length:78}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(78); });
  it("par79", async () => { const t = Array.from({length:79}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(79); });
  it("par80", async () => { const t = Array.from({length:80}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(80); });
  it("par81", async () => { const t = Array.from({length:81}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(81); });
  it("par82", async () => { const t = Array.from({length:82}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(82); });
  it("par83", async () => { const t = Array.from({length:83}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(83); });
  it("par84", async () => { const t = Array.from({length:84}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(84); });
  it("par85", async () => { const t = Array.from({length:85}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(85); });
  it("par86", async () => { const t = Array.from({length:86}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(86); });
  it("par87", async () => { const t = Array.from({length:87}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(87); });
  it("par88", async () => { const t = Array.from({length:88}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(88); });
  it("par89", async () => { const t = Array.from({length:89}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(89); });
  it("par90", async () => { const t = Array.from({length:90}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(90); });
  it("par91", async () => { const t = Array.from({length:91}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(91); });
  it("par92", async () => { const t = Array.from({length:92}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(92); });
  it("par93", async () => { const t = Array.from({length:93}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(93); });
  it("par94", async () => { const t = Array.from({length:94}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(94); });
  it("par95", async () => { const t = Array.from({length:95}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(95); });
  it("par96", async () => { const t = Array.from({length:96}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(96); });
  it("par97", async () => { const t = Array.from({length:97}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(97); });
  it("par98", async () => { const t = Array.from({length:98}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(98); });
  it("par99", async () => { const t = Array.from({length:99}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(99); });
  it("par100", async () => { const t = Array.from({length:100}, (_, k) => () => Promise.resolve(k)); expect((await parallel(t,2)).length).toBe(100); });
});

describe("sequential", () => {
  it("seq1", async () => { expect((await sequential([() => Promise.resolve(1)])).length).toBe(1); });
  it("seq2", async () => { expect((await sequential([() => Promise.resolve(1), () => Promise.resolve(2)]))[0]).toBe(1); });
  it("seq3", async () => { const r = await sequential([() => Promise.resolve(3)]); expect(r[0]).toBe(3); });
  it("seq4", async () => { const r = await sequential([() => Promise.resolve(4), () => Promise.resolve(8)]); expect(r[1]).toBe(8); });
  it("seq5", async () => { const r = await sequential([() => Promise.resolve(5)]); expect(r.length).toBe(1); });
  it("seq6", async () => { const r = await sequential([() => Promise.resolve(6)]); expect(r[0]).toBe(6); });
  it("seq7", async () => { const r = await sequential([() => Promise.resolve(7)]); expect(r[0]).toBe(7); });
  it("seq8", async () => { const r = await sequential([() => Promise.resolve(8)]); expect(r[0]).toBe(8); });
  it("seq9", async () => { const r = await sequential([() => Promise.resolve(9)]); expect(r[0]).toBe(9); });
  it("seq10", async () => { const r = await sequential([() => Promise.resolve(10)]); expect(r[0]).toBe(10); });
  it("seq11", async () => { const r = await sequential([() => Promise.resolve(11)]); expect(r[0]).toBe(11); });
  it("seq12", async () => { const r = await sequential([() => Promise.resolve(12)]); expect(r[0]).toBe(12); });
  it("seq13", async () => { const r = await sequential([() => Promise.resolve(13)]); expect(r[0]).toBe(13); });
  it("seq14", async () => { const r = await sequential([() => Promise.resolve(14)]); expect(r[0]).toBe(14); });
  it("seq15", async () => { const r = await sequential([() => Promise.resolve(15)]); expect(r[0]).toBe(15); });
  it("seq16", async () => { const r = await sequential([() => Promise.resolve(16)]); expect(r[0]).toBe(16); });
  it("seq17", async () => { const r = await sequential([() => Promise.resolve(17)]); expect(r[0]).toBe(17); });
  it("seq18", async () => { const r = await sequential([() => Promise.resolve(18)]); expect(r[0]).toBe(18); });
  it("seq19", async () => { const r = await sequential([() => Promise.resolve(19)]); expect(r[0]).toBe(19); });
  it("seq20", async () => { const r = await sequential([() => Promise.resolve(20)]); expect(r[0]).toBe(20); });
  it("seq21", async () => { const r = await sequential([() => Promise.resolve(21)]); expect(r[0]).toBe(21); });
  it("seq22", async () => { const r = await sequential([() => Promise.resolve(22)]); expect(r[0]).toBe(22); });
  it("seq23", async () => { const r = await sequential([() => Promise.resolve(23)]); expect(r[0]).toBe(23); });
  it("seq24", async () => { const r = await sequential([() => Promise.resolve(24)]); expect(r[0]).toBe(24); });
  it("seq25", async () => { const r = await sequential([() => Promise.resolve(25)]); expect(r[0]).toBe(25); });
  it("seq26", async () => { const r = await sequential([() => Promise.resolve(26)]); expect(r[0]).toBe(26); });
  it("seq27", async () => { const r = await sequential([() => Promise.resolve(27)]); expect(r[0]).toBe(27); });
  it("seq28", async () => { const r = await sequential([() => Promise.resolve(28)]); expect(r[0]).toBe(28); });
  it("seq29", async () => { const r = await sequential([() => Promise.resolve(29)]); expect(r[0]).toBe(29); });
  it("seq30", async () => { const r = await sequential([() => Promise.resolve(30)]); expect(r[0]).toBe(30); });
  it("seq31", async () => { const r = await sequential([() => Promise.resolve(31)]); expect(r[0]).toBe(31); });
  it("seq32", async () => { const r = await sequential([() => Promise.resolve(32)]); expect(r[0]).toBe(32); });
  it("seq33", async () => { const r = await sequential([() => Promise.resolve(33)]); expect(r[0]).toBe(33); });
  it("seq34", async () => { const r = await sequential([() => Promise.resolve(34)]); expect(r[0]).toBe(34); });
  it("seq35", async () => { const r = await sequential([() => Promise.resolve(35)]); expect(r[0]).toBe(35); });
  it("seq36", async () => { const r = await sequential([() => Promise.resolve(36)]); expect(r[0]).toBe(36); });
  it("seq37", async () => { const r = await sequential([() => Promise.resolve(37)]); expect(r[0]).toBe(37); });
  it("seq38", async () => { const r = await sequential([() => Promise.resolve(38)]); expect(r[0]).toBe(38); });
  it("seq39", async () => { const r = await sequential([() => Promise.resolve(39)]); expect(r[0]).toBe(39); });
  it("seq40", async () => { const r = await sequential([() => Promise.resolve(40)]); expect(r[0]).toBe(40); });
  it("seq41", async () => { const r = await sequential([() => Promise.resolve(41)]); expect(r[0]).toBe(41); });
  it("seq42", async () => { const r = await sequential([() => Promise.resolve(42)]); expect(r[0]).toBe(42); });
  it("seq43", async () => { const r = await sequential([() => Promise.resolve(43)]); expect(r[0]).toBe(43); });
  it("seq44", async () => { const r = await sequential([() => Promise.resolve(44)]); expect(r[0]).toBe(44); });
  it("seq45", async () => { const r = await sequential([() => Promise.resolve(45)]); expect(r[0]).toBe(45); });
  it("seq46", async () => { const r = await sequential([() => Promise.resolve(46)]); expect(r[0]).toBe(46); });
  it("seq47", async () => { const r = await sequential([() => Promise.resolve(47)]); expect(r[0]).toBe(47); });
  it("seq48", async () => { const r = await sequential([() => Promise.resolve(48)]); expect(r[0]).toBe(48); });
  it("seq49", async () => { const r = await sequential([() => Promise.resolve(49)]); expect(r[0]).toBe(49); });
  it("seq50", async () => { const r = await sequential([() => Promise.resolve(50)]); expect(r[0]).toBe(50); });
  it("seq51", async () => { const r = await sequential([() => Promise.resolve(51)]); expect(r[0]).toBe(51); });
  it("seq52", async () => { const r = await sequential([() => Promise.resolve(52)]); expect(r[0]).toBe(52); });
  it("seq53", async () => { const r = await sequential([() => Promise.resolve(53)]); expect(r[0]).toBe(53); });
  it("seq54", async () => { const r = await sequential([() => Promise.resolve(54)]); expect(r[0]).toBe(54); });
  it("seq55", async () => { const r = await sequential([() => Promise.resolve(55)]); expect(r[0]).toBe(55); });
  it("seq56", async () => { const r = await sequential([() => Promise.resolve(56)]); expect(r[0]).toBe(56); });
  it("seq57", async () => { const r = await sequential([() => Promise.resolve(57)]); expect(r[0]).toBe(57); });
  it("seq58", async () => { const r = await sequential([() => Promise.resolve(58)]); expect(r[0]).toBe(58); });
  it("seq59", async () => { const r = await sequential([() => Promise.resolve(59)]); expect(r[0]).toBe(59); });
  it("seq60", async () => { const r = await sequential([() => Promise.resolve(60)]); expect(r[0]).toBe(60); });
  it("seq61", async () => { const r = await sequential([() => Promise.resolve(61)]); expect(r[0]).toBe(61); });
  it("seq62", async () => { const r = await sequential([() => Promise.resolve(62)]); expect(r[0]).toBe(62); });
  it("seq63", async () => { const r = await sequential([() => Promise.resolve(63)]); expect(r[0]).toBe(63); });
  it("seq64", async () => { const r = await sequential([() => Promise.resolve(64)]); expect(r[0]).toBe(64); });
  it("seq65", async () => { const r = await sequential([() => Promise.resolve(65)]); expect(r[0]).toBe(65); });
  it("seq66", async () => { const r = await sequential([() => Promise.resolve(66)]); expect(r[0]).toBe(66); });
  it("seq67", async () => { const r = await sequential([() => Promise.resolve(67)]); expect(r[0]).toBe(67); });
  it("seq68", async () => { const r = await sequential([() => Promise.resolve(68)]); expect(r[0]).toBe(68); });
  it("seq69", async () => { const r = await sequential([() => Promise.resolve(69)]); expect(r[0]).toBe(69); });
  it("seq70", async () => { const r = await sequential([() => Promise.resolve(70)]); expect(r[0]).toBe(70); });
  it("seq71", async () => { const r = await sequential([() => Promise.resolve(71)]); expect(r[0]).toBe(71); });
  it("seq72", async () => { const r = await sequential([() => Promise.resolve(72)]); expect(r[0]).toBe(72); });
  it("seq73", async () => { const r = await sequential([() => Promise.resolve(73)]); expect(r[0]).toBe(73); });
  it("seq74", async () => { const r = await sequential([() => Promise.resolve(74)]); expect(r[0]).toBe(74); });
  it("seq75", async () => { const r = await sequential([() => Promise.resolve(75)]); expect(r[0]).toBe(75); });
  it("seq76", async () => { const r = await sequential([() => Promise.resolve(76)]); expect(r[0]).toBe(76); });
  it("seq77", async () => { const r = await sequential([() => Promise.resolve(77)]); expect(r[0]).toBe(77); });
  it("seq78", async () => { const r = await sequential([() => Promise.resolve(78)]); expect(r[0]).toBe(78); });
  it("seq79", async () => { const r = await sequential([() => Promise.resolve(79)]); expect(r[0]).toBe(79); });
  it("seq80", async () => { const r = await sequential([() => Promise.resolve(80)]); expect(r[0]).toBe(80); });
  it("seq81", async () => { const r = await sequential([() => Promise.resolve(81)]); expect(r[0]).toBe(81); });
  it("seq82", async () => { const r = await sequential([() => Promise.resolve(82)]); expect(r[0]).toBe(82); });
  it("seq83", async () => { const r = await sequential([() => Promise.resolve(83)]); expect(r[0]).toBe(83); });
  it("seq84", async () => { const r = await sequential([() => Promise.resolve(84)]); expect(r[0]).toBe(84); });
  it("seq85", async () => { const r = await sequential([() => Promise.resolve(85)]); expect(r[0]).toBe(85); });
  it("seq86", async () => { const r = await sequential([() => Promise.resolve(86)]); expect(r[0]).toBe(86); });
  it("seq87", async () => { const r = await sequential([() => Promise.resolve(87)]); expect(r[0]).toBe(87); });
  it("seq88", async () => { const r = await sequential([() => Promise.resolve(88)]); expect(r[0]).toBe(88); });
  it("seq89", async () => { const r = await sequential([() => Promise.resolve(89)]); expect(r[0]).toBe(89); });
  it("seq90", async () => { const r = await sequential([() => Promise.resolve(90)]); expect(r[0]).toBe(90); });
  it("seq91", async () => { const r = await sequential([() => Promise.resolve(91)]); expect(r[0]).toBe(91); });
  it("seq92", async () => { const r = await sequential([() => Promise.resolve(92)]); expect(r[0]).toBe(92); });
  it("seq93", async () => { const r = await sequential([() => Promise.resolve(93)]); expect(r[0]).toBe(93); });
  it("seq94", async () => { const r = await sequential([() => Promise.resolve(94)]); expect(r[0]).toBe(94); });
  it("seq95", async () => { const r = await sequential([() => Promise.resolve(95)]); expect(r[0]).toBe(95); });
  it("seq96", async () => { const r = await sequential([() => Promise.resolve(96)]); expect(r[0]).toBe(96); });
  it("seq97", async () => { const r = await sequential([() => Promise.resolve(97)]); expect(r[0]).toBe(97); });
  it("seq98", async () => { const r = await sequential([() => Promise.resolve(98)]); expect(r[0]).toBe(98); });
  it("seq99", async () => { const r = await sequential([() => Promise.resolve(99)]); expect(r[0]).toBe(99); });
  it("seq100", async () => { const r = await sequential([() => Promise.resolve(100)]); expect(r[0]).toBe(100); });
});

describe("memoize", () => {
  it("caches", () => { let c=0; const fn = memoize((x: number) => { c++; return x*2; }); expect(fn(5)).toBe(10); expect(fn(5)).toBe(10); expect(c).toBe(1); });
  it("m1", () => { let c=0; const fn = memoize((x: number) => { c++; return x+1; }); fn(1); fn(1); expect(c).toBe(1); expect(fn(1)).toBe(2); });
  it("m2", () => { let c=0; const fn = memoize((x: number) => { c++; return x+2; }); fn(2); fn(2); expect(c).toBe(1); expect(fn(2)).toBe(4); });
  it("m3", () => { let c=0; const fn = memoize((x: number) => { c++; return x+3; }); fn(3); fn(3); expect(c).toBe(1); expect(fn(3)).toBe(6); });
  it("m4", () => { let c=0; const fn = memoize((x: number) => { c++; return x+4; }); fn(4); fn(4); expect(c).toBe(1); expect(fn(4)).toBe(8); });
  it("m5", () => { let c=0; const fn = memoize((x: number) => { c++; return x+5; }); fn(5); fn(5); expect(c).toBe(1); expect(fn(5)).toBe(10); });
  it("m6", () => { let c=0; const fn = memoize((x: number) => { c++; return x+6; }); fn(6); fn(6); expect(c).toBe(1); expect(fn(6)).toBe(12); });
  it("m7", () => { let c=0; const fn = memoize((x: number) => { c++; return x+7; }); fn(7); fn(7); expect(c).toBe(1); expect(fn(7)).toBe(14); });
  it("m8", () => { let c=0; const fn = memoize((x: number) => { c++; return x+8; }); fn(8); fn(8); expect(c).toBe(1); expect(fn(8)).toBe(16); });
  it("m9", () => { let c=0; const fn = memoize((x: number) => { c++; return x+9; }); fn(9); fn(9); expect(c).toBe(1); expect(fn(9)).toBe(18); });
  it("m10", () => { let c=0; const fn = memoize((x: number) => { c++; return x+10; }); fn(10); fn(10); expect(c).toBe(1); expect(fn(10)).toBe(20); });
  it("m11", () => { let c=0; const fn = memoize((x: number) => { c++; return x+11; }); fn(11); fn(11); expect(c).toBe(1); expect(fn(11)).toBe(22); });
  it("m12", () => { let c=0; const fn = memoize((x: number) => { c++; return x+12; }); fn(12); fn(12); expect(c).toBe(1); expect(fn(12)).toBe(24); });
  it("m13", () => { let c=0; const fn = memoize((x: number) => { c++; return x+13; }); fn(13); fn(13); expect(c).toBe(1); expect(fn(13)).toBe(26); });
  it("m14", () => { let c=0; const fn = memoize((x: number) => { c++; return x+14; }); fn(14); fn(14); expect(c).toBe(1); expect(fn(14)).toBe(28); });
  it("m15", () => { let c=0; const fn = memoize((x: number) => { c++; return x+15; }); fn(15); fn(15); expect(c).toBe(1); expect(fn(15)).toBe(30); });
  it("m16", () => { let c=0; const fn = memoize((x: number) => { c++; return x+16; }); fn(16); fn(16); expect(c).toBe(1); expect(fn(16)).toBe(32); });
  it("m17", () => { let c=0; const fn = memoize((x: number) => { c++; return x+17; }); fn(17); fn(17); expect(c).toBe(1); expect(fn(17)).toBe(34); });
  it("m18", () => { let c=0; const fn = memoize((x: number) => { c++; return x+18; }); fn(18); fn(18); expect(c).toBe(1); expect(fn(18)).toBe(36); });
  it("m19", () => { let c=0; const fn = memoize((x: number) => { c++; return x+19; }); fn(19); fn(19); expect(c).toBe(1); expect(fn(19)).toBe(38); });
  it("m20", () => { let c=0; const fn = memoize((x: number) => { c++; return x+20; }); fn(20); fn(20); expect(c).toBe(1); expect(fn(20)).toBe(40); });
  it("m21", () => { let c=0; const fn = memoize((x: number) => { c++; return x+21; }); fn(21); fn(21); expect(c).toBe(1); expect(fn(21)).toBe(42); });
  it("m22", () => { let c=0; const fn = memoize((x: number) => { c++; return x+22; }); fn(22); fn(22); expect(c).toBe(1); expect(fn(22)).toBe(44); });
  it("m23", () => { let c=0; const fn = memoize((x: number) => { c++; return x+23; }); fn(23); fn(23); expect(c).toBe(1); expect(fn(23)).toBe(46); });
  it("m24", () => { let c=0; const fn = memoize((x: number) => { c++; return x+24; }); fn(24); fn(24); expect(c).toBe(1); expect(fn(24)).toBe(48); });
  it("m25", () => { let c=0; const fn = memoize((x: number) => { c++; return x+25; }); fn(25); fn(25); expect(c).toBe(1); expect(fn(25)).toBe(50); });
  it("m26", () => { let c=0; const fn = memoize((x: number) => { c++; return x+26; }); fn(26); fn(26); expect(c).toBe(1); expect(fn(26)).toBe(52); });
  it("m27", () => { let c=0; const fn = memoize((x: number) => { c++; return x+27; }); fn(27); fn(27); expect(c).toBe(1); expect(fn(27)).toBe(54); });
  it("m28", () => { let c=0; const fn = memoize((x: number) => { c++; return x+28; }); fn(28); fn(28); expect(c).toBe(1); expect(fn(28)).toBe(56); });
  it("m29", () => { let c=0; const fn = memoize((x: number) => { c++; return x+29; }); fn(29); fn(29); expect(c).toBe(1); expect(fn(29)).toBe(58); });
  it("m30", () => { let c=0; const fn = memoize((x: number) => { c++; return x+30; }); fn(30); fn(30); expect(c).toBe(1); expect(fn(30)).toBe(60); });
  it("m31", () => { let c=0; const fn = memoize((x: number) => { c++; return x+31; }); fn(31); fn(31); expect(c).toBe(1); expect(fn(31)).toBe(62); });
  it("m32", () => { let c=0; const fn = memoize((x: number) => { c++; return x+32; }); fn(32); fn(32); expect(c).toBe(1); expect(fn(32)).toBe(64); });
  it("m33", () => { let c=0; const fn = memoize((x: number) => { c++; return x+33; }); fn(33); fn(33); expect(c).toBe(1); expect(fn(33)).toBe(66); });
  it("m34", () => { let c=0; const fn = memoize((x: number) => { c++; return x+34; }); fn(34); fn(34); expect(c).toBe(1); expect(fn(34)).toBe(68); });
  it("m35", () => { let c=0; const fn = memoize((x: number) => { c++; return x+35; }); fn(35); fn(35); expect(c).toBe(1); expect(fn(35)).toBe(70); });
  it("m36", () => { let c=0; const fn = memoize((x: number) => { c++; return x+36; }); fn(36); fn(36); expect(c).toBe(1); expect(fn(36)).toBe(72); });
  it("m37", () => { let c=0; const fn = memoize((x: number) => { c++; return x+37; }); fn(37); fn(37); expect(c).toBe(1); expect(fn(37)).toBe(74); });
  it("m38", () => { let c=0; const fn = memoize((x: number) => { c++; return x+38; }); fn(38); fn(38); expect(c).toBe(1); expect(fn(38)).toBe(76); });
  it("m39", () => { let c=0; const fn = memoize((x: number) => { c++; return x+39; }); fn(39); fn(39); expect(c).toBe(1); expect(fn(39)).toBe(78); });
  it("m40", () => { let c=0; const fn = memoize((x: number) => { c++; return x+40; }); fn(40); fn(40); expect(c).toBe(1); expect(fn(40)).toBe(80); });
  it("m41", () => { let c=0; const fn = memoize((x: number) => { c++; return x+41; }); fn(41); fn(41); expect(c).toBe(1); expect(fn(41)).toBe(82); });
  it("m42", () => { let c=0; const fn = memoize((x: number) => { c++; return x+42; }); fn(42); fn(42); expect(c).toBe(1); expect(fn(42)).toBe(84); });
  it("m43", () => { let c=0; const fn = memoize((x: number) => { c++; return x+43; }); fn(43); fn(43); expect(c).toBe(1); expect(fn(43)).toBe(86); });
  it("m44", () => { let c=0; const fn = memoize((x: number) => { c++; return x+44; }); fn(44); fn(44); expect(c).toBe(1); expect(fn(44)).toBe(88); });
  it("m45", () => { let c=0; const fn = memoize((x: number) => { c++; return x+45; }); fn(45); fn(45); expect(c).toBe(1); expect(fn(45)).toBe(90); });
  it("m46", () => { let c=0; const fn = memoize((x: number) => { c++; return x+46; }); fn(46); fn(46); expect(c).toBe(1); expect(fn(46)).toBe(92); });
  it("m47", () => { let c=0; const fn = memoize((x: number) => { c++; return x+47; }); fn(47); fn(47); expect(c).toBe(1); expect(fn(47)).toBe(94); });
  it("m48", () => { let c=0; const fn = memoize((x: number) => { c++; return x+48; }); fn(48); fn(48); expect(c).toBe(1); expect(fn(48)).toBe(96); });
  it("m49", () => { let c=0; const fn = memoize((x: number) => { c++; return x+49; }); fn(49); fn(49); expect(c).toBe(1); expect(fn(49)).toBe(98); });
  it("m50", () => { let c=0; const fn = memoize((x: number) => { c++; return x+50; }); fn(50); fn(50); expect(c).toBe(1); expect(fn(50)).toBe(100); });
  it("m51", () => { let c=0; const fn = memoize((x: number) => { c++; return x+51; }); fn(51); fn(51); expect(c).toBe(1); expect(fn(51)).toBe(102); });
  it("m52", () => { let c=0; const fn = memoize((x: number) => { c++; return x+52; }); fn(52); fn(52); expect(c).toBe(1); expect(fn(52)).toBe(104); });
  it("m53", () => { let c=0; const fn = memoize((x: number) => { c++; return x+53; }); fn(53); fn(53); expect(c).toBe(1); expect(fn(53)).toBe(106); });
  it("m54", () => { let c=0; const fn = memoize((x: number) => { c++; return x+54; }); fn(54); fn(54); expect(c).toBe(1); expect(fn(54)).toBe(108); });
  it("m55", () => { let c=0; const fn = memoize((x: number) => { c++; return x+55; }); fn(55); fn(55); expect(c).toBe(1); expect(fn(55)).toBe(110); });
  it("m56", () => { let c=0; const fn = memoize((x: number) => { c++; return x+56; }); fn(56); fn(56); expect(c).toBe(1); expect(fn(56)).toBe(112); });
  it("m57", () => { let c=0; const fn = memoize((x: number) => { c++; return x+57; }); fn(57); fn(57); expect(c).toBe(1); expect(fn(57)).toBe(114); });
  it("m58", () => { let c=0; const fn = memoize((x: number) => { c++; return x+58; }); fn(58); fn(58); expect(c).toBe(1); expect(fn(58)).toBe(116); });
  it("m59", () => { let c=0; const fn = memoize((x: number) => { c++; return x+59; }); fn(59); fn(59); expect(c).toBe(1); expect(fn(59)).toBe(118); });
  it("m60", () => { let c=0; const fn = memoize((x: number) => { c++; return x+60; }); fn(60); fn(60); expect(c).toBe(1); expect(fn(60)).toBe(120); });
  it("m61", () => { let c=0; const fn = memoize((x: number) => { c++; return x+61; }); fn(61); fn(61); expect(c).toBe(1); expect(fn(61)).toBe(122); });
  it("m62", () => { let c=0; const fn = memoize((x: number) => { c++; return x+62; }); fn(62); fn(62); expect(c).toBe(1); expect(fn(62)).toBe(124); });
  it("m63", () => { let c=0; const fn = memoize((x: number) => { c++; return x+63; }); fn(63); fn(63); expect(c).toBe(1); expect(fn(63)).toBe(126); });
  it("m64", () => { let c=0; const fn = memoize((x: number) => { c++; return x+64; }); fn(64); fn(64); expect(c).toBe(1); expect(fn(64)).toBe(128); });
  it("m65", () => { let c=0; const fn = memoize((x: number) => { c++; return x+65; }); fn(65); fn(65); expect(c).toBe(1); expect(fn(65)).toBe(130); });
  it("m66", () => { let c=0; const fn = memoize((x: number) => { c++; return x+66; }); fn(66); fn(66); expect(c).toBe(1); expect(fn(66)).toBe(132); });
  it("m67", () => { let c=0; const fn = memoize((x: number) => { c++; return x+67; }); fn(67); fn(67); expect(c).toBe(1); expect(fn(67)).toBe(134); });
  it("m68", () => { let c=0; const fn = memoize((x: number) => { c++; return x+68; }); fn(68); fn(68); expect(c).toBe(1); expect(fn(68)).toBe(136); });
  it("m69", () => { let c=0; const fn = memoize((x: number) => { c++; return x+69; }); fn(69); fn(69); expect(c).toBe(1); expect(fn(69)).toBe(138); });
  it("m70", () => { let c=0; const fn = memoize((x: number) => { c++; return x+70; }); fn(70); fn(70); expect(c).toBe(1); expect(fn(70)).toBe(140); });
  it("m71", () => { let c=0; const fn = memoize((x: number) => { c++; return x+71; }); fn(71); fn(71); expect(c).toBe(1); expect(fn(71)).toBe(142); });
  it("m72", () => { let c=0; const fn = memoize((x: number) => { c++; return x+72; }); fn(72); fn(72); expect(c).toBe(1); expect(fn(72)).toBe(144); });
  it("m73", () => { let c=0; const fn = memoize((x: number) => { c++; return x+73; }); fn(73); fn(73); expect(c).toBe(1); expect(fn(73)).toBe(146); });
  it("m74", () => { let c=0; const fn = memoize((x: number) => { c++; return x+74; }); fn(74); fn(74); expect(c).toBe(1); expect(fn(74)).toBe(148); });
  it("m75", () => { let c=0; const fn = memoize((x: number) => { c++; return x+75; }); fn(75); fn(75); expect(c).toBe(1); expect(fn(75)).toBe(150); });
  it("m76", () => { let c=0; const fn = memoize((x: number) => { c++; return x+76; }); fn(76); fn(76); expect(c).toBe(1); expect(fn(76)).toBe(152); });
  it("m77", () => { let c=0; const fn = memoize((x: number) => { c++; return x+77; }); fn(77); fn(77); expect(c).toBe(1); expect(fn(77)).toBe(154); });
  it("m78", () => { let c=0; const fn = memoize((x: number) => { c++; return x+78; }); fn(78); fn(78); expect(c).toBe(1); expect(fn(78)).toBe(156); });
  it("m79", () => { let c=0; const fn = memoize((x: number) => { c++; return x+79; }); fn(79); fn(79); expect(c).toBe(1); expect(fn(79)).toBe(158); });
  it("m80", () => { let c=0; const fn = memoize((x: number) => { c++; return x+80; }); fn(80); fn(80); expect(c).toBe(1); expect(fn(80)).toBe(160); });
  it("m81", () => { let c=0; const fn = memoize((x: number) => { c++; return x+81; }); fn(81); fn(81); expect(c).toBe(1); expect(fn(81)).toBe(162); });
  it("m82", () => { let c=0; const fn = memoize((x: number) => { c++; return x+82; }); fn(82); fn(82); expect(c).toBe(1); expect(fn(82)).toBe(164); });
  it("m83", () => { let c=0; const fn = memoize((x: number) => { c++; return x+83; }); fn(83); fn(83); expect(c).toBe(1); expect(fn(83)).toBe(166); });
  it("m84", () => { let c=0; const fn = memoize((x: number) => { c++; return x+84; }); fn(84); fn(84); expect(c).toBe(1); expect(fn(84)).toBe(168); });
  it("m85", () => { let c=0; const fn = memoize((x: number) => { c++; return x+85; }); fn(85); fn(85); expect(c).toBe(1); expect(fn(85)).toBe(170); });
  it("m86", () => { let c=0; const fn = memoize((x: number) => { c++; return x+86; }); fn(86); fn(86); expect(c).toBe(1); expect(fn(86)).toBe(172); });
  it("m87", () => { let c=0; const fn = memoize((x: number) => { c++; return x+87; }); fn(87); fn(87); expect(c).toBe(1); expect(fn(87)).toBe(174); });
  it("m88", () => { let c=0; const fn = memoize((x: number) => { c++; return x+88; }); fn(88); fn(88); expect(c).toBe(1); expect(fn(88)).toBe(176); });
  it("m89", () => { let c=0; const fn = memoize((x: number) => { c++; return x+89; }); fn(89); fn(89); expect(c).toBe(1); expect(fn(89)).toBe(178); });
  it("m90", () => { let c=0; const fn = memoize((x: number) => { c++; return x+90; }); fn(90); fn(90); expect(c).toBe(1); expect(fn(90)).toBe(180); });
  it("m91", () => { let c=0; const fn = memoize((x: number) => { c++; return x+91; }); fn(91); fn(91); expect(c).toBe(1); expect(fn(91)).toBe(182); });
  it("m92", () => { let c=0; const fn = memoize((x: number) => { c++; return x+92; }); fn(92); fn(92); expect(c).toBe(1); expect(fn(92)).toBe(184); });
  it("m93", () => { let c=0; const fn = memoize((x: number) => { c++; return x+93; }); fn(93); fn(93); expect(c).toBe(1); expect(fn(93)).toBe(186); });
  it("m94", () => { let c=0; const fn = memoize((x: number) => { c++; return x+94; }); fn(94); fn(94); expect(c).toBe(1); expect(fn(94)).toBe(188); });
  it("m95", () => { let c=0; const fn = memoize((x: number) => { c++; return x+95; }); fn(95); fn(95); expect(c).toBe(1); expect(fn(95)).toBe(190); });
  it("m96", () => { let c=0; const fn = memoize((x: number) => { c++; return x+96; }); fn(96); fn(96); expect(c).toBe(1); expect(fn(96)).toBe(192); });
  it("m97", () => { let c=0; const fn = memoize((x: number) => { c++; return x+97; }); fn(97); fn(97); expect(c).toBe(1); expect(fn(97)).toBe(194); });
  it("m98", () => { let c=0; const fn = memoize((x: number) => { c++; return x+98; }); fn(98); fn(98); expect(c).toBe(1); expect(fn(98)).toBe(196); });
  it("m99", () => { let c=0; const fn = memoize((x: number) => { c++; return x+99; }); fn(99); fn(99); expect(c).toBe(1); expect(fn(99)).toBe(198); });
  it("m100", () => { let c=0; const fn = memoize((x: number) => { c++; return x+100; }); fn(100); fn(100); expect(c).toBe(1); expect(fn(100)).toBe(200); });
});

describe("once", () => {
  it("only calls once", () => { let c=0; const fn = once(() => { c++; return 42; }); fn(); fn(); fn(); expect(c).toBe(1); });
  it("o1", () => { let c=0; const fn = once(() => { c++; return 1; }); expect(fn()).toBe(1); expect(fn()).toBe(1); expect(c).toBe(1); });
  it("o2", () => { let c=0; const fn = once(() => { c++; return 2; }); expect(fn()).toBe(2); expect(fn()).toBe(2); expect(c).toBe(1); });
  it("o3", () => { let c=0; const fn = once(() => { c++; return 3; }); expect(fn()).toBe(3); expect(fn()).toBe(3); expect(c).toBe(1); });
  it("o4", () => { let c=0; const fn = once(() => { c++; return 4; }); expect(fn()).toBe(4); expect(fn()).toBe(4); expect(c).toBe(1); });
  it("o5", () => { let c=0; const fn = once(() => { c++; return 5; }); expect(fn()).toBe(5); expect(fn()).toBe(5); expect(c).toBe(1); });
  it("o6", () => { let c=0; const fn = once(() => { c++; return 6; }); expect(fn()).toBe(6); expect(fn()).toBe(6); expect(c).toBe(1); });
  it("o7", () => { let c=0; const fn = once(() => { c++; return 7; }); expect(fn()).toBe(7); expect(fn()).toBe(7); expect(c).toBe(1); });
  it("o8", () => { let c=0; const fn = once(() => { c++; return 8; }); expect(fn()).toBe(8); expect(fn()).toBe(8); expect(c).toBe(1); });
  it("o9", () => { let c=0; const fn = once(() => { c++; return 9; }); expect(fn()).toBe(9); expect(fn()).toBe(9); expect(c).toBe(1); });
  it("o10", () => { let c=0; const fn = once(() => { c++; return 10; }); expect(fn()).toBe(10); expect(fn()).toBe(10); expect(c).toBe(1); });
  it("o11", () => { let c=0; const fn = once(() => { c++; return 11; }); expect(fn()).toBe(11); expect(fn()).toBe(11); expect(c).toBe(1); });
  it("o12", () => { let c=0; const fn = once(() => { c++; return 12; }); expect(fn()).toBe(12); expect(fn()).toBe(12); expect(c).toBe(1); });
  it("o13", () => { let c=0; const fn = once(() => { c++; return 13; }); expect(fn()).toBe(13); expect(fn()).toBe(13); expect(c).toBe(1); });
  it("o14", () => { let c=0; const fn = once(() => { c++; return 14; }); expect(fn()).toBe(14); expect(fn()).toBe(14); expect(c).toBe(1); });
  it("o15", () => { let c=0; const fn = once(() => { c++; return 15; }); expect(fn()).toBe(15); expect(fn()).toBe(15); expect(c).toBe(1); });
  it("o16", () => { let c=0; const fn = once(() => { c++; return 16; }); expect(fn()).toBe(16); expect(fn()).toBe(16); expect(c).toBe(1); });
  it("o17", () => { let c=0; const fn = once(() => { c++; return 17; }); expect(fn()).toBe(17); expect(fn()).toBe(17); expect(c).toBe(1); });
  it("o18", () => { let c=0; const fn = once(() => { c++; return 18; }); expect(fn()).toBe(18); expect(fn()).toBe(18); expect(c).toBe(1); });
  it("o19", () => { let c=0; const fn = once(() => { c++; return 19; }); expect(fn()).toBe(19); expect(fn()).toBe(19); expect(c).toBe(1); });
  it("o20", () => { let c=0; const fn = once(() => { c++; return 20; }); expect(fn()).toBe(20); expect(fn()).toBe(20); expect(c).toBe(1); });
  it("o21", () => { let c=0; const fn = once(() => { c++; return 21; }); expect(fn()).toBe(21); expect(fn()).toBe(21); expect(c).toBe(1); });
  it("o22", () => { let c=0; const fn = once(() => { c++; return 22; }); expect(fn()).toBe(22); expect(fn()).toBe(22); expect(c).toBe(1); });
  it("o23", () => { let c=0; const fn = once(() => { c++; return 23; }); expect(fn()).toBe(23); expect(fn()).toBe(23); expect(c).toBe(1); });
  it("o24", () => { let c=0; const fn = once(() => { c++; return 24; }); expect(fn()).toBe(24); expect(fn()).toBe(24); expect(c).toBe(1); });
  it("o25", () => { let c=0; const fn = once(() => { c++; return 25; }); expect(fn()).toBe(25); expect(fn()).toBe(25); expect(c).toBe(1); });
  it("o26", () => { let c=0; const fn = once(() => { c++; return 26; }); expect(fn()).toBe(26); expect(fn()).toBe(26); expect(c).toBe(1); });
  it("o27", () => { let c=0; const fn = once(() => { c++; return 27; }); expect(fn()).toBe(27); expect(fn()).toBe(27); expect(c).toBe(1); });
  it("o28", () => { let c=0; const fn = once(() => { c++; return 28; }); expect(fn()).toBe(28); expect(fn()).toBe(28); expect(c).toBe(1); });
  it("o29", () => { let c=0; const fn = once(() => { c++; return 29; }); expect(fn()).toBe(29); expect(fn()).toBe(29); expect(c).toBe(1); });
  it("o30", () => { let c=0; const fn = once(() => { c++; return 30; }); expect(fn()).toBe(30); expect(fn()).toBe(30); expect(c).toBe(1); });
  it("o31", () => { let c=0; const fn = once(() => { c++; return 31; }); expect(fn()).toBe(31); expect(fn()).toBe(31); expect(c).toBe(1); });
  it("o32", () => { let c=0; const fn = once(() => { c++; return 32; }); expect(fn()).toBe(32); expect(fn()).toBe(32); expect(c).toBe(1); });
  it("o33", () => { let c=0; const fn = once(() => { c++; return 33; }); expect(fn()).toBe(33); expect(fn()).toBe(33); expect(c).toBe(1); });
  it("o34", () => { let c=0; const fn = once(() => { c++; return 34; }); expect(fn()).toBe(34); expect(fn()).toBe(34); expect(c).toBe(1); });
  it("o35", () => { let c=0; const fn = once(() => { c++; return 35; }); expect(fn()).toBe(35); expect(fn()).toBe(35); expect(c).toBe(1); });
  it("o36", () => { let c=0; const fn = once(() => { c++; return 36; }); expect(fn()).toBe(36); expect(fn()).toBe(36); expect(c).toBe(1); });
  it("o37", () => { let c=0; const fn = once(() => { c++; return 37; }); expect(fn()).toBe(37); expect(fn()).toBe(37); expect(c).toBe(1); });
  it("o38", () => { let c=0; const fn = once(() => { c++; return 38; }); expect(fn()).toBe(38); expect(fn()).toBe(38); expect(c).toBe(1); });
  it("o39", () => { let c=0; const fn = once(() => { c++; return 39; }); expect(fn()).toBe(39); expect(fn()).toBe(39); expect(c).toBe(1); });
  it("o40", () => { let c=0; const fn = once(() => { c++; return 40; }); expect(fn()).toBe(40); expect(fn()).toBe(40); expect(c).toBe(1); });
  it("o41", () => { let c=0; const fn = once(() => { c++; return 41; }); expect(fn()).toBe(41); expect(fn()).toBe(41); expect(c).toBe(1); });
  it("o42", () => { let c=0; const fn = once(() => { c++; return 42; }); expect(fn()).toBe(42); expect(fn()).toBe(42); expect(c).toBe(1); });
  it("o43", () => { let c=0; const fn = once(() => { c++; return 43; }); expect(fn()).toBe(43); expect(fn()).toBe(43); expect(c).toBe(1); });
  it("o44", () => { let c=0; const fn = once(() => { c++; return 44; }); expect(fn()).toBe(44); expect(fn()).toBe(44); expect(c).toBe(1); });
  it("o45", () => { let c=0; const fn = once(() => { c++; return 45; }); expect(fn()).toBe(45); expect(fn()).toBe(45); expect(c).toBe(1); });
  it("o46", () => { let c=0; const fn = once(() => { c++; return 46; }); expect(fn()).toBe(46); expect(fn()).toBe(46); expect(c).toBe(1); });
  it("o47", () => { let c=0; const fn = once(() => { c++; return 47; }); expect(fn()).toBe(47); expect(fn()).toBe(47); expect(c).toBe(1); });
  it("o48", () => { let c=0; const fn = once(() => { c++; return 48; }); expect(fn()).toBe(48); expect(fn()).toBe(48); expect(c).toBe(1); });
  it("o49", () => { let c=0; const fn = once(() => { c++; return 49; }); expect(fn()).toBe(49); expect(fn()).toBe(49); expect(c).toBe(1); });
  it("o50", () => { let c=0; const fn = once(() => { c++; return 50; }); expect(fn()).toBe(50); expect(fn()).toBe(50); expect(c).toBe(1); });
  it("o51", () => { let c=0; const fn = once(() => { c++; return 51; }); expect(fn()).toBe(51); expect(fn()).toBe(51); expect(c).toBe(1); });
  it("o52", () => { let c=0; const fn = once(() => { c++; return 52; }); expect(fn()).toBe(52); expect(fn()).toBe(52); expect(c).toBe(1); });
  it("o53", () => { let c=0; const fn = once(() => { c++; return 53; }); expect(fn()).toBe(53); expect(fn()).toBe(53); expect(c).toBe(1); });
  it("o54", () => { let c=0; const fn = once(() => { c++; return 54; }); expect(fn()).toBe(54); expect(fn()).toBe(54); expect(c).toBe(1); });
  it("o55", () => { let c=0; const fn = once(() => { c++; return 55; }); expect(fn()).toBe(55); expect(fn()).toBe(55); expect(c).toBe(1); });
  it("o56", () => { let c=0; const fn = once(() => { c++; return 56; }); expect(fn()).toBe(56); expect(fn()).toBe(56); expect(c).toBe(1); });
  it("o57", () => { let c=0; const fn = once(() => { c++; return 57; }); expect(fn()).toBe(57); expect(fn()).toBe(57); expect(c).toBe(1); });
  it("o58", () => { let c=0; const fn = once(() => { c++; return 58; }); expect(fn()).toBe(58); expect(fn()).toBe(58); expect(c).toBe(1); });
  it("o59", () => { let c=0; const fn = once(() => { c++; return 59; }); expect(fn()).toBe(59); expect(fn()).toBe(59); expect(c).toBe(1); });
  it("o60", () => { let c=0; const fn = once(() => { c++; return 60; }); expect(fn()).toBe(60); expect(fn()).toBe(60); expect(c).toBe(1); });
  it("o61", () => { let c=0; const fn = once(() => { c++; return 61; }); expect(fn()).toBe(61); expect(fn()).toBe(61); expect(c).toBe(1); });
  it("o62", () => { let c=0; const fn = once(() => { c++; return 62; }); expect(fn()).toBe(62); expect(fn()).toBe(62); expect(c).toBe(1); });
  it("o63", () => { let c=0; const fn = once(() => { c++; return 63; }); expect(fn()).toBe(63); expect(fn()).toBe(63); expect(c).toBe(1); });
  it("o64", () => { let c=0; const fn = once(() => { c++; return 64; }); expect(fn()).toBe(64); expect(fn()).toBe(64); expect(c).toBe(1); });
  it("o65", () => { let c=0; const fn = once(() => { c++; return 65; }); expect(fn()).toBe(65); expect(fn()).toBe(65); expect(c).toBe(1); });
  it("o66", () => { let c=0; const fn = once(() => { c++; return 66; }); expect(fn()).toBe(66); expect(fn()).toBe(66); expect(c).toBe(1); });
  it("o67", () => { let c=0; const fn = once(() => { c++; return 67; }); expect(fn()).toBe(67); expect(fn()).toBe(67); expect(c).toBe(1); });
  it("o68", () => { let c=0; const fn = once(() => { c++; return 68; }); expect(fn()).toBe(68); expect(fn()).toBe(68); expect(c).toBe(1); });
  it("o69", () => { let c=0; const fn = once(() => { c++; return 69; }); expect(fn()).toBe(69); expect(fn()).toBe(69); expect(c).toBe(1); });
  it("o70", () => { let c=0; const fn = once(() => { c++; return 70; }); expect(fn()).toBe(70); expect(fn()).toBe(70); expect(c).toBe(1); });
  it("o71", () => { let c=0; const fn = once(() => { c++; return 71; }); expect(fn()).toBe(71); expect(fn()).toBe(71); expect(c).toBe(1); });
  it("o72", () => { let c=0; const fn = once(() => { c++; return 72; }); expect(fn()).toBe(72); expect(fn()).toBe(72); expect(c).toBe(1); });
  it("o73", () => { let c=0; const fn = once(() => { c++; return 73; }); expect(fn()).toBe(73); expect(fn()).toBe(73); expect(c).toBe(1); });
  it("o74", () => { let c=0; const fn = once(() => { c++; return 74; }); expect(fn()).toBe(74); expect(fn()).toBe(74); expect(c).toBe(1); });
  it("o75", () => { let c=0; const fn = once(() => { c++; return 75; }); expect(fn()).toBe(75); expect(fn()).toBe(75); expect(c).toBe(1); });
  it("o76", () => { let c=0; const fn = once(() => { c++; return 76; }); expect(fn()).toBe(76); expect(fn()).toBe(76); expect(c).toBe(1); });
  it("o77", () => { let c=0; const fn = once(() => { c++; return 77; }); expect(fn()).toBe(77); expect(fn()).toBe(77); expect(c).toBe(1); });
  it("o78", () => { let c=0; const fn = once(() => { c++; return 78; }); expect(fn()).toBe(78); expect(fn()).toBe(78); expect(c).toBe(1); });
  it("o79", () => { let c=0; const fn = once(() => { c++; return 79; }); expect(fn()).toBe(79); expect(fn()).toBe(79); expect(c).toBe(1); });
  it("o80", () => { let c=0; const fn = once(() => { c++; return 80; }); expect(fn()).toBe(80); expect(fn()).toBe(80); expect(c).toBe(1); });
  it("o81", () => { let c=0; const fn = once(() => { c++; return 81; }); expect(fn()).toBe(81); expect(fn()).toBe(81); expect(c).toBe(1); });
  it("o82", () => { let c=0; const fn = once(() => { c++; return 82; }); expect(fn()).toBe(82); expect(fn()).toBe(82); expect(c).toBe(1); });
  it("o83", () => { let c=0; const fn = once(() => { c++; return 83; }); expect(fn()).toBe(83); expect(fn()).toBe(83); expect(c).toBe(1); });
  it("o84", () => { let c=0; const fn = once(() => { c++; return 84; }); expect(fn()).toBe(84); expect(fn()).toBe(84); expect(c).toBe(1); });
  it("o85", () => { let c=0; const fn = once(() => { c++; return 85; }); expect(fn()).toBe(85); expect(fn()).toBe(85); expect(c).toBe(1); });
  it("o86", () => { let c=0; const fn = once(() => { c++; return 86; }); expect(fn()).toBe(86); expect(fn()).toBe(86); expect(c).toBe(1); });
  it("o87", () => { let c=0; const fn = once(() => { c++; return 87; }); expect(fn()).toBe(87); expect(fn()).toBe(87); expect(c).toBe(1); });
  it("o88", () => { let c=0; const fn = once(() => { c++; return 88; }); expect(fn()).toBe(88); expect(fn()).toBe(88); expect(c).toBe(1); });
  it("o89", () => { let c=0; const fn = once(() => { c++; return 89; }); expect(fn()).toBe(89); expect(fn()).toBe(89); expect(c).toBe(1); });
  it("o90", () => { let c=0; const fn = once(() => { c++; return 90; }); expect(fn()).toBe(90); expect(fn()).toBe(90); expect(c).toBe(1); });
  it("o91", () => { let c=0; const fn = once(() => { c++; return 91; }); expect(fn()).toBe(91); expect(fn()).toBe(91); expect(c).toBe(1); });
  it("o92", () => { let c=0; const fn = once(() => { c++; return 92; }); expect(fn()).toBe(92); expect(fn()).toBe(92); expect(c).toBe(1); });
  it("o93", () => { let c=0; const fn = once(() => { c++; return 93; }); expect(fn()).toBe(93); expect(fn()).toBe(93); expect(c).toBe(1); });
  it("o94", () => { let c=0; const fn = once(() => { c++; return 94; }); expect(fn()).toBe(94); expect(fn()).toBe(94); expect(c).toBe(1); });
  it("o95", () => { let c=0; const fn = once(() => { c++; return 95; }); expect(fn()).toBe(95); expect(fn()).toBe(95); expect(c).toBe(1); });
  it("o96", () => { let c=0; const fn = once(() => { c++; return 96; }); expect(fn()).toBe(96); expect(fn()).toBe(96); expect(c).toBe(1); });
  it("o97", () => { let c=0; const fn = once(() => { c++; return 97; }); expect(fn()).toBe(97); expect(fn()).toBe(97); expect(c).toBe(1); });
  it("o98", () => { let c=0; const fn = once(() => { c++; return 98; }); expect(fn()).toBe(98); expect(fn()).toBe(98); expect(c).toBe(1); });
  it("o99", () => { let c=0; const fn = once(() => { c++; return 99; }); expect(fn()).toBe(99); expect(fn()).toBe(99); expect(c).toBe(1); });
  it("o100", () => { let c=0; const fn = once(() => { c++; return 100; }); expect(fn()).toBe(100); expect(fn()).toBe(100); expect(c).toBe(1); });
});

describe("Semaphore", () => {
  it("sem1", () => { expect(new Semaphore(1).available).toBe(1); });
  it("sem2", () => { expect(new Semaphore(2).available).toBe(2); });
  it("sem3", () => { expect(new Semaphore(3).available).toBe(3); });
  it("sem4", () => { expect(new Semaphore(4).available).toBe(4); });
  it("sem5", () => { expect(new Semaphore(5).available).toBe(5); });
  it("sem6", () => { expect(new Semaphore(6).available).toBe(6); });
  it("sem7", () => { expect(new Semaphore(7).available).toBe(7); });
  it("sem8", () => { expect(new Semaphore(8).available).toBe(8); });
  it("sem9", () => { expect(new Semaphore(9).available).toBe(9); });
  it("sem10", () => { expect(new Semaphore(10).available).toBe(10); });
  it("sem11", () => { expect(new Semaphore(11).available).toBe(11); });
  it("sem12", () => { expect(new Semaphore(12).available).toBe(12); });
  it("sem13", () => { expect(new Semaphore(13).available).toBe(13); });
  it("sem14", () => { expect(new Semaphore(14).available).toBe(14); });
  it("sem15", () => { expect(new Semaphore(15).available).toBe(15); });
  it("sem16", () => { expect(new Semaphore(16).available).toBe(16); });
  it("sem17", () => { expect(new Semaphore(17).available).toBe(17); });
  it("sem18", () => { expect(new Semaphore(18).available).toBe(18); });
  it("sem19", () => { expect(new Semaphore(19).available).toBe(19); });
  it("sem20", () => { expect(new Semaphore(20).available).toBe(20); });
  it("sem-acquire", async () => { const s = new Semaphore(2); await s.acquire(); expect(s.available).toBe(1); });
  it("sem-release", async () => { const s = new Semaphore(1); await s.acquire(); s.release(); expect(s.available).toBe(1); });
  it("sem21", () => { expect(new Semaphore(21).available).toBe(21); });
  it("sem22", () => { expect(new Semaphore(22).available).toBe(22); });
  it("sem23", () => { expect(new Semaphore(23).available).toBe(23); });
  it("sem24", () => { expect(new Semaphore(24).available).toBe(24); });
  it("sem25", () => { expect(new Semaphore(25).available).toBe(25); });
  it("sem26", () => { expect(new Semaphore(26).available).toBe(26); });
  it("sem27", () => { expect(new Semaphore(27).available).toBe(27); });
  it("sem28", () => { expect(new Semaphore(28).available).toBe(28); });
  it("sem29", () => { expect(new Semaphore(29).available).toBe(29); });
  it("sem30", () => { expect(new Semaphore(30).available).toBe(30); });
  it("sem31", () => { expect(new Semaphore(31).available).toBe(31); });
  it("sem32", () => { expect(new Semaphore(32).available).toBe(32); });
  it("sem33", () => { expect(new Semaphore(33).available).toBe(33); });
  it("sem34", () => { expect(new Semaphore(34).available).toBe(34); });
  it("sem35", () => { expect(new Semaphore(35).available).toBe(35); });
  it("sem36", () => { expect(new Semaphore(36).available).toBe(36); });
  it("sem37", () => { expect(new Semaphore(37).available).toBe(37); });
  it("sem38", () => { expect(new Semaphore(38).available).toBe(38); });
  it("sem39", () => { expect(new Semaphore(39).available).toBe(39); });
  it("sem40", () => { expect(new Semaphore(40).available).toBe(40); });
  it("sem41", () => { expect(new Semaphore(41).available).toBe(41); });
  it("sem42", () => { expect(new Semaphore(42).available).toBe(42); });
  it("sem43", () => { expect(new Semaphore(43).available).toBe(43); });
  it("sem44", () => { expect(new Semaphore(44).available).toBe(44); });
  it("sem45", () => { expect(new Semaphore(45).available).toBe(45); });
  it("sem46", () => { expect(new Semaphore(46).available).toBe(46); });
  it("sem47", () => { expect(new Semaphore(47).available).toBe(47); });
  it("sem48", () => { expect(new Semaphore(48).available).toBe(48); });
  it("sem49", () => { expect(new Semaphore(49).available).toBe(49); });
  it("sem50", () => { expect(new Semaphore(50).available).toBe(50); });
});

describe("batch", () => {
  it("batch0", () => { expect(batch([], 5)).toEqual([]); });
  it("batch1", () => { expect(batch([1], 5)).toEqual([[1]]); });
  it("batch5", () => { expect(batch([1,2,3,4,5], 5)).toEqual([[1,2,3,4,5]]); });
  it("batch6", () => { expect(batch([1,2,3,4,5,6], 5).length).toBe(2); });
  it("b1", () => { expect(batch(Array.from({length:1}, (_,k) => k), 5).flat().length).toBe(1); });
  it("b2", () => { expect(batch(Array.from({length:2}, (_,k) => k), 5).flat().length).toBe(2); });
  it("b3", () => { expect(batch(Array.from({length:3}, (_,k) => k), 5).flat().length).toBe(3); });
  it("b4", () => { expect(batch(Array.from({length:4}, (_,k) => k), 5).flat().length).toBe(4); });
  it("b7", () => { expect(batch(Array.from({length:7}, (_,k) => k), 5).flat().length).toBe(7); });
  it("b8", () => { expect(batch(Array.from({length:8}, (_,k) => k), 5).flat().length).toBe(8); });
  it("b9", () => { expect(batch(Array.from({length:9}, (_,k) => k), 5).flat().length).toBe(9); });
  it("b10", () => { expect(batch(Array.from({length:10}, (_,k) => k), 5).flat().length).toBe(10); });
  it("b11", () => { expect(batch(Array.from({length:11}, (_,k) => k), 5).flat().length).toBe(11); });
  it("b12", () => { expect(batch(Array.from({length:12}, (_,k) => k), 5).flat().length).toBe(12); });
  it("b13", () => { expect(batch(Array.from({length:13}, (_,k) => k), 5).flat().length).toBe(13); });
  it("b14", () => { expect(batch(Array.from({length:14}, (_,k) => k), 5).flat().length).toBe(14); });
  it("b15", () => { expect(batch(Array.from({length:15}, (_,k) => k), 5).flat().length).toBe(15); });
  it("b16", () => { expect(batch(Array.from({length:16}, (_,k) => k), 5).flat().length).toBe(16); });
  it("b17", () => { expect(batch(Array.from({length:17}, (_,k) => k), 5).flat().length).toBe(17); });
  it("b18", () => { expect(batch(Array.from({length:18}, (_,k) => k), 5).flat().length).toBe(18); });
  it("b19", () => { expect(batch(Array.from({length:19}, (_,k) => k), 5).flat().length).toBe(19); });
  it("b20", () => { expect(batch(Array.from({length:20}, (_,k) => k), 5).flat().length).toBe(20); });
  it("b21", () => { expect(batch(Array.from({length:21}, (_,k) => k), 5).flat().length).toBe(21); });
  it("b22", () => { expect(batch(Array.from({length:22}, (_,k) => k), 5).flat().length).toBe(22); });
  it("b23", () => { expect(batch(Array.from({length:23}, (_,k) => k), 5).flat().length).toBe(23); });
  it("b24", () => { expect(batch(Array.from({length:24}, (_,k) => k), 5).flat().length).toBe(24); });
  it("b25", () => { expect(batch(Array.from({length:25}, (_,k) => k), 5).flat().length).toBe(25); });
  it("b26", () => { expect(batch(Array.from({length:26}, (_,k) => k), 5).flat().length).toBe(26); });
  it("b27", () => { expect(batch(Array.from({length:27}, (_,k) => k), 5).flat().length).toBe(27); });
  it("b28", () => { expect(batch(Array.from({length:28}, (_,k) => k), 5).flat().length).toBe(28); });
  it("b29", () => { expect(batch(Array.from({length:29}, (_,k) => k), 5).flat().length).toBe(29); });
  it("b30", () => { expect(batch(Array.from({length:30}, (_,k) => k), 5).flat().length).toBe(30); });
  it("b31", () => { expect(batch(Array.from({length:31}, (_,k) => k), 5).flat().length).toBe(31); });
  it("b32", () => { expect(batch(Array.from({length:32}, (_,k) => k), 5).flat().length).toBe(32); });
  it("b33", () => { expect(batch(Array.from({length:33}, (_,k) => k), 5).flat().length).toBe(33); });
  it("b34", () => { expect(batch(Array.from({length:34}, (_,k) => k), 5).flat().length).toBe(34); });
  it("b35", () => { expect(batch(Array.from({length:35}, (_,k) => k), 5).flat().length).toBe(35); });
  it("b36", () => { expect(batch(Array.from({length:36}, (_,k) => k), 5).flat().length).toBe(36); });
  it("b37", () => { expect(batch(Array.from({length:37}, (_,k) => k), 5).flat().length).toBe(37); });
  it("b38", () => { expect(batch(Array.from({length:38}, (_,k) => k), 5).flat().length).toBe(38); });
  it("b39", () => { expect(batch(Array.from({length:39}, (_,k) => k), 5).flat().length).toBe(39); });
  it("b40", () => { expect(batch(Array.from({length:40}, (_,k) => k), 5).flat().length).toBe(40); });
  it("b41", () => { expect(batch(Array.from({length:41}, (_,k) => k), 5).flat().length).toBe(41); });
  it("b42", () => { expect(batch(Array.from({length:42}, (_,k) => k), 5).flat().length).toBe(42); });
  it("b43", () => { expect(batch(Array.from({length:43}, (_,k) => k), 5).flat().length).toBe(43); });
  it("b44", () => { expect(batch(Array.from({length:44}, (_,k) => k), 5).flat().length).toBe(44); });
  it("b45", () => { expect(batch(Array.from({length:45}, (_,k) => k), 5).flat().length).toBe(45); });
  it("b46", () => { expect(batch(Array.from({length:46}, (_,k) => k), 5).flat().length).toBe(46); });
  it("b47", () => { expect(batch(Array.from({length:47}, (_,k) => k), 5).flat().length).toBe(47); });
  it("b48", () => { expect(batch(Array.from({length:48}, (_,k) => k), 5).flat().length).toBe(48); });
  it("b49", () => { expect(batch(Array.from({length:49}, (_,k) => k), 5).flat().length).toBe(49); });
  it("b50", () => { expect(batch(Array.from({length:50}, (_,k) => k), 5).flat().length).toBe(50); });
  it("b51", () => { expect(batch(Array.from({length:51}, (_,k) => k), 5).flat().length).toBe(51); });
  it("b52", () => { expect(batch(Array.from({length:52}, (_,k) => k), 5).flat().length).toBe(52); });
  it("b53", () => { expect(batch(Array.from({length:53}, (_,k) => k), 5).flat().length).toBe(53); });
  it("b54", () => { expect(batch(Array.from({length:54}, (_,k) => k), 5).flat().length).toBe(54); });
  it("b55", () => { expect(batch(Array.from({length:55}, (_,k) => k), 5).flat().length).toBe(55); });
  it("b56", () => { expect(batch(Array.from({length:56}, (_,k) => k), 5).flat().length).toBe(56); });
  it("b57", () => { expect(batch(Array.from({length:57}, (_,k) => k), 5).flat().length).toBe(57); });
  it("b58", () => { expect(batch(Array.from({length:58}, (_,k) => k), 5).flat().length).toBe(58); });
  it("b59", () => { expect(batch(Array.from({length:59}, (_,k) => k), 5).flat().length).toBe(59); });
  it("b60", () => { expect(batch(Array.from({length:60}, (_,k) => k), 5).flat().length).toBe(60); });
  it("b61", () => { expect(batch(Array.from({length:61}, (_,k) => k), 5).flat().length).toBe(61); });
  it("b62", () => { expect(batch(Array.from({length:62}, (_,k) => k), 5).flat().length).toBe(62); });
  it("b63", () => { expect(batch(Array.from({length:63}, (_,k) => k), 5).flat().length).toBe(63); });
  it("b64", () => { expect(batch(Array.from({length:64}, (_,k) => k), 5).flat().length).toBe(64); });
  it("b65", () => { expect(batch(Array.from({length:65}, (_,k) => k), 5).flat().length).toBe(65); });
  it("b66", () => { expect(batch(Array.from({length:66}, (_,k) => k), 5).flat().length).toBe(66); });
});

describe("EventQueue", () => {
  it("eq-size0", () => { const q = new EventQueue<number>(); expect(q.size).toBe(0); });
  it("eq-size1", () => { const q = new EventQueue<number>(); q.enqueue(1); expect(q.size).toBe(1); });
  it("eq-size2", () => { const q = new EventQueue<number>(); q.enqueue(1); q.enqueue(2); expect(q.size).toBe(2); });
  it("eq-dequeue", async () => { const q = new EventQueue<number>(); q.enqueue(42); expect(await q.dequeue()).toBe(42); });
  it("eq-pending", () => { const q = new EventQueue<number>(); expect(q.pendingHandlers).toBe(0); });
  it("eq1", () => { const q = new EventQueue<number>(); q.enqueue(1); expect(q.size).toBe(1); });
  it("eq2", () => { const q = new EventQueue<number>(); q.enqueue(2); expect(q.size).toBe(1); });
  it("eq3", () => { const q = new EventQueue<number>(); q.enqueue(3); expect(q.size).toBe(1); });
  it("eq4", () => { const q = new EventQueue<number>(); q.enqueue(4); expect(q.size).toBe(1); });
  it("eq5", () => { const q = new EventQueue<number>(); q.enqueue(5); expect(q.size).toBe(1); });
  it("eq6", () => { const q = new EventQueue<number>(); q.enqueue(6); expect(q.size).toBe(1); });
  it("eq7", () => { const q = new EventQueue<number>(); q.enqueue(7); expect(q.size).toBe(1); });
  it("eq8", () => { const q = new EventQueue<number>(); q.enqueue(8); expect(q.size).toBe(1); });
  it("eq9", () => { const q = new EventQueue<number>(); q.enqueue(9); expect(q.size).toBe(1); });
  it("eq10", () => { const q = new EventQueue<number>(); q.enqueue(10); expect(q.size).toBe(1); });
  it("eq11", () => { const q = new EventQueue<number>(); q.enqueue(11); expect(q.size).toBe(1); });
  it("eq12", () => { const q = new EventQueue<number>(); q.enqueue(12); expect(q.size).toBe(1); });
  it("eq13", () => { const q = new EventQueue<number>(); q.enqueue(13); expect(q.size).toBe(1); });
  it("eq14", () => { const q = new EventQueue<number>(); q.enqueue(14); expect(q.size).toBe(1); });
  it("eq15", () => { const q = new EventQueue<number>(); q.enqueue(15); expect(q.size).toBe(1); });
  it("eq16", () => { const q = new EventQueue<number>(); q.enqueue(16); expect(q.size).toBe(1); });
  it("eq17", () => { const q = new EventQueue<number>(); q.enqueue(17); expect(q.size).toBe(1); });
  it("eq18", () => { const q = new EventQueue<number>(); q.enqueue(18); expect(q.size).toBe(1); });
  it("eq19", () => { const q = new EventQueue<number>(); q.enqueue(19); expect(q.size).toBe(1); });
  it("eq20", () => { const q = new EventQueue<number>(); q.enqueue(20); expect(q.size).toBe(1); });
  it("eq21", () => { const q = new EventQueue<number>(); q.enqueue(21); expect(q.size).toBe(1); });
  it("eq22", () => { const q = new EventQueue<number>(); q.enqueue(22); expect(q.size).toBe(1); });
  it("eq23", () => { const q = new EventQueue<number>(); q.enqueue(23); expect(q.size).toBe(1); });
  it("eq24", () => { const q = new EventQueue<number>(); q.enqueue(24); expect(q.size).toBe(1); });
  it("eq25", () => { const q = new EventQueue<number>(); q.enqueue(25); expect(q.size).toBe(1); });
  it("eq26", () => { const q = new EventQueue<number>(); q.enqueue(26); expect(q.size).toBe(1); });
  it("eq27", () => { const q = new EventQueue<number>(); q.enqueue(27); expect(q.size).toBe(1); });
  it("eq28", () => { const q = new EventQueue<number>(); q.enqueue(28); expect(q.size).toBe(1); });
  it("eq29", () => { const q = new EventQueue<number>(); q.enqueue(29); expect(q.size).toBe(1); });
  it("eq30", () => { const q = new EventQueue<number>(); q.enqueue(30); expect(q.size).toBe(1); });
  it("eq31", () => { const q = new EventQueue<number>(); q.enqueue(31); expect(q.size).toBe(1); });
  it("eq32", () => { const q = new EventQueue<number>(); q.enqueue(32); expect(q.size).toBe(1); });
  it("eq33", () => { const q = new EventQueue<number>(); q.enqueue(33); expect(q.size).toBe(1); });
  it("eq34", () => { const q = new EventQueue<number>(); q.enqueue(34); expect(q.size).toBe(1); });
  it("eq35", () => { const q = new EventQueue<number>(); q.enqueue(35); expect(q.size).toBe(1); });
  it("eq36", () => { const q = new EventQueue<number>(); q.enqueue(36); expect(q.size).toBe(1); });
  it("eq37", () => { const q = new EventQueue<number>(); q.enqueue(37); expect(q.size).toBe(1); });
  it("eq38", () => { const q = new EventQueue<number>(); q.enqueue(38); expect(q.size).toBe(1); });
  it("eq39", () => { const q = new EventQueue<number>(); q.enqueue(39); expect(q.size).toBe(1); });
  it("eq40", () => { const q = new EventQueue<number>(); q.enqueue(40); expect(q.size).toBe(1); });
  it("eq41", () => { const q = new EventQueue<number>(); q.enqueue(41); expect(q.size).toBe(1); });
  it("eq42", () => { const q = new EventQueue<number>(); q.enqueue(42); expect(q.size).toBe(1); });
  it("eq43", () => { const q = new EventQueue<number>(); q.enqueue(43); expect(q.size).toBe(1); });
  it("eq44", () => { const q = new EventQueue<number>(); q.enqueue(44); expect(q.size).toBe(1); });
  it("eq45", () => { const q = new EventQueue<number>(); q.enqueue(45); expect(q.size).toBe(1); });
});

describe("allSettled", () => {
  it("as0", async () => { const r = await allSettled([]); expect(r.length).toBe(0); });
  it("as1", async () => { const r = await allSettled([Promise.resolve(1)]); expect(r[0].status).toBe("fulfilled"); });
  it("as2", async () => { const r = await allSettled([Promise.reject("e")]); expect(r[0].status).toBe("rejected"); });
  it("as3", async () => { const r = await allSettled([Promise.resolve(3)]); expect(r.length).toBe(1); });
  it("as4", async () => { const r = await allSettled([Promise.resolve(4), Promise.resolve(5)]); expect(r.length).toBe(2); });
  it("as5", async () => { const r = await allSettled([Promise.resolve(5)]); expect((r[0] as any).value).toBe(5); });
  it("as6", async () => { const r = await allSettled([Promise.resolve(6), Promise.reject("e")]); expect(r.filter(x => x.status === "fulfilled").length).toBe(1); });
  it("as7", async () => { const r = await allSettled(Array.from({length:7}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(7); });
  it("as8", async () => { const r = await allSettled(Array.from({length:8}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(8); });
  it("as9", async () => { const r = await allSettled(Array.from({length:9}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(9); });
  it("as10", async () => { const r = await allSettled(Array.from({length:10}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(10); });
  it("as11", async () => { const r = await allSettled(Array.from({length:11}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(11); });
  it("as12", async () => { const r = await allSettled(Array.from({length:12}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(12); });
  it("as13", async () => { const r = await allSettled(Array.from({length:13}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(13); });
  it("as14", async () => { const r = await allSettled(Array.from({length:14}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(14); });
  it("as15", async () => { const r = await allSettled(Array.from({length:15}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(15); });
  it("as16", async () => { const r = await allSettled(Array.from({length:16}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(16); });
  it("as17", async () => { const r = await allSettled(Array.from({length:17}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(17); });
  it("as18", async () => { const r = await allSettled(Array.from({length:18}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(18); });
  it("as19", async () => { const r = await allSettled(Array.from({length:19}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(19); });
  it("as20", async () => { const r = await allSettled(Array.from({length:20}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(20); });
  it("as21", async () => { const r = await allSettled(Array.from({length:21}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(21); });
  it("as22", async () => { const r = await allSettled(Array.from({length:22}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(22); });
  it("as23", async () => { const r = await allSettled(Array.from({length:23}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(23); });
  it("as24", async () => { const r = await allSettled(Array.from({length:24}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(24); });
  it("as25", async () => { const r = await allSettled(Array.from({length:25}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(25); });
  it("as26", async () => { const r = await allSettled(Array.from({length:26}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(26); });
  it("as27", async () => { const r = await allSettled(Array.from({length:27}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(27); });
  it("as28", async () => { const r = await allSettled(Array.from({length:28}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(28); });
  it("as29", async () => { const r = await allSettled(Array.from({length:29}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(29); });
  it("as30", async () => { const r = await allSettled(Array.from({length:30}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(30); });
  it("as31", async () => { const r = await allSettled(Array.from({length:31}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(31); });
  it("as32", async () => { const r = await allSettled(Array.from({length:32}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(32); });
  it("as33", async () => { const r = await allSettled(Array.from({length:33}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(33); });
  it("as34", async () => { const r = await allSettled(Array.from({length:34}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(34); });
  it("as35", async () => { const r = await allSettled(Array.from({length:35}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(35); });
  it("as36", async () => { const r = await allSettled(Array.from({length:36}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(36); });
  it("as37", async () => { const r = await allSettled(Array.from({length:37}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(37); });
  it("as38", async () => { const r = await allSettled(Array.from({length:38}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(38); });
  it("as39", async () => { const r = await allSettled(Array.from({length:39}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(39); });
  it("as40", async () => { const r = await allSettled(Array.from({length:40}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(40); });
  it("as41", async () => { const r = await allSettled(Array.from({length:41}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(41); });
  it("as42", async () => { const r = await allSettled(Array.from({length:42}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(42); });
  it("as43", async () => { const r = await allSettled(Array.from({length:43}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(43); });
  it("as44", async () => { const r = await allSettled(Array.from({length:44}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(44); });
  it("as45", async () => { const r = await allSettled(Array.from({length:45}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(45); });
  it("as46", async () => { const r = await allSettled(Array.from({length:46}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(46); });
  it("as47", async () => { const r = await allSettled(Array.from({length:47}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(47); });
  it("as48", async () => { const r = await allSettled(Array.from({length:48}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(48); });
  it("as49", async () => { const r = await allSettled(Array.from({length:49}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(49); });
  it("as50", async () => { const r = await allSettled(Array.from({length:50}, (_,k) => Promise.resolve(k))); expect(r.length).toBe(50); });
});

describe("mapConcurrent", () => {
  it("mc0", async () => { const r = await mapConcurrent([], async x => x, 2); expect(r.length).toBe(0); });
  it("mc1", async () => { const r = await mapConcurrent([1], async x => x*2, 1); expect(r[0]).toBe(2); });
  it("mc2", async () => { const r = await mapConcurrent([1,2], async x => x*2, 2); expect(r).toEqual([2,4]); });
  it("mc3", async () => { const r = await mapConcurrent([1,2,3], async x => x+1, 2); expect(r.length).toBe(3); });
  it("mc4", async () => { const r = await mapConcurrent([4], async x => x*x, 1); expect(r[0]).toBe(16); });
  it("mc5", async () => { const r = await mapConcurrent([5], async x => x+5, 1); expect(r[0]).toBe(10); });
  it("mc6", async () => { const r = await mapConcurrent([6], async x => x-1, 1); expect(r[0]).toBe(5); });
  it("mc7", async () => { const r = await mapConcurrent([7], async x => x*3, 1); expect(r[0]).toBe(21); });
  it("mc8", async () => { const r = await mapConcurrent([8], async x => x/2, 1); expect(r[0]).toBe(4); });
  it("mc9", async () => { const r = await mapConcurrent([9], async x => x+1, 1); expect(r[0]).toBe(10); });
  it("mc10", async () => { const r = await mapConcurrent(Array.from({length:10}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(10); });
  it("mc11", async () => { const r = await mapConcurrent(Array.from({length:11}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(11); });
  it("mc12", async () => { const r = await mapConcurrent(Array.from({length:12}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(12); });
  it("mc13", async () => { const r = await mapConcurrent(Array.from({length:13}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(13); });
  it("mc14", async () => { const r = await mapConcurrent(Array.from({length:14}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(14); });
  it("mc15", async () => { const r = await mapConcurrent(Array.from({length:15}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(15); });
  it("mc16", async () => { const r = await mapConcurrent(Array.from({length:16}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(16); });
  it("mc17", async () => { const r = await mapConcurrent(Array.from({length:17}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(17); });
  it("mc18", async () => { const r = await mapConcurrent(Array.from({length:18}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(18); });
  it("mc19", async () => { const r = await mapConcurrent(Array.from({length:19}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(19); });
  it("mc20", async () => { const r = await mapConcurrent(Array.from({length:20}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(20); });
  it("mc21", async () => { const r = await mapConcurrent(Array.from({length:21}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(21); });
  it("mc22", async () => { const r = await mapConcurrent(Array.from({length:22}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(22); });
  it("mc23", async () => { const r = await mapConcurrent(Array.from({length:23}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(23); });
  it("mc24", async () => { const r = await mapConcurrent(Array.from({length:24}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(24); });
  it("mc25", async () => { const r = await mapConcurrent(Array.from({length:25}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(25); });
  it("mc26", async () => { const r = await mapConcurrent(Array.from({length:26}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(26); });
  it("mc27", async () => { const r = await mapConcurrent(Array.from({length:27}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(27); });
  it("mc28", async () => { const r = await mapConcurrent(Array.from({length:28}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(28); });
  it("mc29", async () => { const r = await mapConcurrent(Array.from({length:29}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(29); });
  it("mc30", async () => { const r = await mapConcurrent(Array.from({length:30}, (_,k) => k), async x => x*2, 3); expect(r.length).toBe(30); });
});

describe("createDeferred", () => {
  it("cd-resolve", async () => { const d = createDeferred(); d.resolve(1); expect(await d.promise).toBe(1); });
  it("cd-reject", async () => { const d = createDeferred(); d.reject(new Error("e")); await expect(d.promise).rejects.toThrow("e"); });
  it("cd1", async () => { const d = createDeferred(); d.resolve(1); expect(await d.promise).toBe(1); });
  it("cd2", async () => { const d = createDeferred(); d.resolve(2); expect(await d.promise).toBe(2); });
  it("cd3", async () => { const d = createDeferred(); d.resolve(3); expect(await d.promise).toBe(3); });
  it("cd4", async () => { const d = createDeferred(); d.resolve(4); expect(await d.promise).toBe(4); });
  it("cd5", async () => { const d = createDeferred(); d.resolve(5); expect(await d.promise).toBe(5); });
  it("cd6", async () => { const d = createDeferred(); d.resolve(6); expect(await d.promise).toBe(6); });
  it("cd7", async () => { const d = createDeferred(); d.resolve(7); expect(await d.promise).toBe(7); });
  it("cd8", async () => { const d = createDeferred(); d.resolve(8); expect(await d.promise).toBe(8); });
  it("cd9", async () => { const d = createDeferred(); d.resolve(9); expect(await d.promise).toBe(9); });
  it("cd10", async () => { const d = createDeferred(); d.resolve(10); expect(await d.promise).toBe(10); });
  it("cd11", async () => { const d = createDeferred(); d.resolve(11); expect(await d.promise).toBe(11); });
  it("cd12", async () => { const d = createDeferred(); d.resolve(12); expect(await d.promise).toBe(12); });
  it("cd13", async () => { const d = createDeferred(); d.resolve(13); expect(await d.promise).toBe(13); });
  it("cd14", async () => { const d = createDeferred(); d.resolve(14); expect(await d.promise).toBe(14); });
  it("cd15", async () => { const d = createDeferred(); d.resolve(15); expect(await d.promise).toBe(15); });
  it("cd16", async () => { const d = createDeferred(); d.resolve(16); expect(await d.promise).toBe(16); });
  it("cd17", async () => { const d = createDeferred(); d.resolve(17); expect(await d.promise).toBe(17); });
  it("cd18", async () => { const d = createDeferred(); d.resolve(18); expect(await d.promise).toBe(18); });
  it("cd19", async () => { const d = createDeferred(); d.resolve(19); expect(await d.promise).toBe(19); });
  it("cd20", async () => { const d = createDeferred(); d.resolve(20); expect(await d.promise).toBe(20); });
  it("cd21", async () => { const d = createDeferred(); d.resolve(21); expect(await d.promise).toBe(21); });
  it("cd22", async () => { const d = createDeferred(); d.resolve(22); expect(await d.promise).toBe(22); });
  it("cd23", async () => { const d = createDeferred(); d.resolve(23); expect(await d.promise).toBe(23); });
  it("cd24", async () => { const d = createDeferred(); d.resolve(24); expect(await d.promise).toBe(24); });
  it("cd25", async () => { const d = createDeferred(); d.resolve(25); expect(await d.promise).toBe(25); });
  it("cd26", async () => { const d = createDeferred(); d.resolve(26); expect(await d.promise).toBe(26); });
  it("cd27", async () => { const d = createDeferred(); d.resolve(27); expect(await d.promise).toBe(27); });
  it("cd28", async () => { const d = createDeferred(); d.resolve(28); expect(await d.promise).toBe(28); });
  it("cd29", async () => { const d = createDeferred(); d.resolve(29); expect(await d.promise).toBe(29); });
  it("cd30", async () => { const d = createDeferred(); d.resolve(30); expect(await d.promise).toBe(30); });
  it("cd31", async () => { const d = createDeferred(); d.resolve(31); expect(await d.promise).toBe(31); });
  it("cd32", async () => { const d = createDeferred(); d.resolve(32); expect(await d.promise).toBe(32); });
  it("cd33", async () => { const d = createDeferred(); d.resolve(33); expect(await d.promise).toBe(33); });
  it("cd34", async () => { const d = createDeferred(); d.resolve(34); expect(await d.promise).toBe(34); });
  it("cd35", async () => { const d = createDeferred(); d.resolve(35); expect(await d.promise).toBe(35); });
  it("cd36", async () => { const d = createDeferred(); d.resolve(36); expect(await d.promise).toBe(36); });
  it("cd37", async () => { const d = createDeferred(); d.resolve(37); expect(await d.promise).toBe(37); });
  it("cd38", async () => { const d = createDeferred(); d.resolve(38); expect(await d.promise).toBe(38); });
  it("cd39", async () => { const d = createDeferred(); d.resolve(39); expect(await d.promise).toBe(39); });
  it("cd40", async () => { const d = createDeferred(); d.resolve(40); expect(await d.promise).toBe(40); });
  it("cd41", async () => { const d = createDeferred(); d.resolve(41); expect(await d.promise).toBe(41); });
  it("cd42", async () => { const d = createDeferred(); d.resolve(42); expect(await d.promise).toBe(42); });
  it("cd43", async () => { const d = createDeferred(); d.resolve(43); expect(await d.promise).toBe(43); });
  it("cd44", async () => { const d = createDeferred(); d.resolve(44); expect(await d.promise).toBe(44); });
  it("cd45", async () => { const d = createDeferred(); d.resolve(45); expect(await d.promise).toBe(45); });
  it("cd46", async () => { const d = createDeferred(); d.resolve(46); expect(await d.promise).toBe(46); });
  it("cd47", async () => { const d = createDeferred(); d.resolve(47); expect(await d.promise).toBe(47); });
  it("cd48", async () => { const d = createDeferred(); d.resolve(48); expect(await d.promise).toBe(48); });
  it("cd49", async () => { const d = createDeferred(); d.resolve(49); expect(await d.promise).toBe(49); });
  it("cd50", async () => { const d = createDeferred(); d.resolve(50); expect(await d.promise).toBe(50); });
});

describe("extra-sleep", () => {
  it("xs1", () => { expect(sleep(1) instanceof Promise).toBe(true); });
  it("xs2", () => { expect(sleep(2) instanceof Promise).toBe(true); });
  it("xs3", () => { expect(sleep(3) instanceof Promise).toBe(true); });
  it("xs4", () => { expect(sleep(4) instanceof Promise).toBe(true); });
  it("xs5", () => { expect(sleep(5) instanceof Promise).toBe(true); });
  it("xs6", () => { expect(sleep(6) instanceof Promise).toBe(true); });
  it("xs7", () => { expect(sleep(7) instanceof Promise).toBe(true); });
  it("xs8", () => { expect(sleep(8) instanceof Promise).toBe(true); });
  it("xs9", () => { expect(sleep(9) instanceof Promise).toBe(true); });
  it("xs10", () => { expect(sleep(10) instanceof Promise).toBe(true); });
  it("xs11", () => { expect(sleep(11) instanceof Promise).toBe(true); });
  it("xs12", () => { expect(sleep(12) instanceof Promise).toBe(true); });
  it("xs13", () => { expect(sleep(13) instanceof Promise).toBe(true); });
  it("xs14", () => { expect(sleep(14) instanceof Promise).toBe(true); });
  it("xs15", () => { expect(sleep(15) instanceof Promise).toBe(true); });
  it("xs16", () => { expect(sleep(16) instanceof Promise).toBe(true); });
  it("xs17", () => { expect(sleep(17) instanceof Promise).toBe(true); });
  it("xs18", () => { expect(sleep(18) instanceof Promise).toBe(true); });
  it("xs19", () => { expect(sleep(19) instanceof Promise).toBe(true); });
  it("xs20", () => { expect(sleep(20) instanceof Promise).toBe(true); });
  it("xs21", () => { expect(sleep(21) instanceof Promise).toBe(true); });
  it("xs22", () => { expect(sleep(22) instanceof Promise).toBe(true); });
  it("xs23", () => { expect(sleep(23) instanceof Promise).toBe(true); });
  it("xs24", () => { expect(sleep(24) instanceof Promise).toBe(true); });
  it("xs25", () => { expect(sleep(25) instanceof Promise).toBe(true); });
  it("xs26", () => { expect(sleep(26) instanceof Promise).toBe(true); });
  it("xs27", () => { expect(sleep(27) instanceof Promise).toBe(true); });
  it("xs28", () => { expect(sleep(28) instanceof Promise).toBe(true); });
  it("xs29", () => { expect(sleep(29) instanceof Promise).toBe(true); });
  it("xs30", () => { expect(sleep(30) instanceof Promise).toBe(true); });
  it("xs31", () => { expect(sleep(31) instanceof Promise).toBe(true); });
  it("xs32", () => { expect(sleep(32) instanceof Promise).toBe(true); });
  it("xs33", () => { expect(sleep(33) instanceof Promise).toBe(true); });
  it("xs34", () => { expect(sleep(34) instanceof Promise).toBe(true); });
  it("xs35", () => { expect(sleep(35) instanceof Promise).toBe(true); });
  it("xs36", () => { expect(sleep(36) instanceof Promise).toBe(true); });
  it("xs37", () => { expect(sleep(37) instanceof Promise).toBe(true); });
  it("xs38", () => { expect(sleep(38) instanceof Promise).toBe(true); });
  it("xs39", () => { expect(sleep(39) instanceof Promise).toBe(true); });
  it("xs40", () => { expect(sleep(40) instanceof Promise).toBe(true); });
  it("xs41", () => { expect(sleep(41) instanceof Promise).toBe(true); });
  it("xs42", () => { expect(sleep(42) instanceof Promise).toBe(true); });
  it("xs43", () => { expect(sleep(43) instanceof Promise).toBe(true); });
  it("xs44", () => { expect(sleep(44) instanceof Promise).toBe(true); });
  it("xs45", () => { expect(sleep(45) instanceof Promise).toBe(true); });
  it("xs46", () => { expect(sleep(46) instanceof Promise).toBe(true); });
  it("xs47", () => { expect(sleep(47) instanceof Promise).toBe(true); });
  it("xs48", () => { expect(sleep(48) instanceof Promise).toBe(true); });
  it("xs49", () => { expect(sleep(49) instanceof Promise).toBe(true); });
  it("xs50", () => { expect(sleep(50) instanceof Promise).toBe(true); });
});

describe("extra-retry", () => {
  it("xr1", async () => { expect(await retry(() => Promise.resolve("a"), 1)).toBe("a"); });
  it("xr2", async () => { expect(await retry(() => Promise.resolve("b"), 2)).toBe("b"); });
  it("xr3", async () => { expect(await retry(() => Promise.resolve("c"), 3)).toBe("c"); });
  it("xr4", async () => { expect(await retry(() => Promise.resolve("d"), 4)).toBe("d"); });
  it("xr5", async () => { expect(await retry(() => Promise.resolve("e"), 5)).toBe("e"); });
  it("xr6", async () => { expect(await retry(() => Promise.resolve("f"), 1)).toBe("f"); });
  it("xr7", async () => { expect(await retry(() => Promise.resolve("g"), 1)).toBe("g"); });
  it("xr8", async () => { expect(await retry(() => Promise.resolve("h"), 1)).toBe("h"); });
  it("xr9", async () => { expect(await retry(() => Promise.resolve("i"), 1)).toBe("i"); });
  it("xr10", async () => { expect(await retry(() => Promise.resolve("j"), 1)).toBe("j"); });
  it("xr11", async () => { expect(await retry(() => Promise.resolve("k"), 1)).toBe("k"); });
  it("xr12", async () => { expect(await retry(() => Promise.resolve("l"), 1)).toBe("l"); });
  it("xr13", async () => { expect(await retry(() => Promise.resolve("m"), 1)).toBe("m"); });
  it("xr14", async () => { expect(await retry(() => Promise.resolve("n"), 1)).toBe("n"); });
  it("xr15", async () => { expect(await retry(() => Promise.resolve("o"), 1)).toBe("o"); });
  it("xr16", async () => { expect(await retry(() => Promise.resolve("p"), 1)).toBe("p"); });
  it("xr17", async () => { expect(await retry(() => Promise.resolve("q"), 1)).toBe("q"); });
  it("xr18", async () => { expect(await retry(() => Promise.resolve("r"), 1)).toBe("r"); });
  it("xr19", async () => { expect(await retry(() => Promise.resolve("s"), 1)).toBe("s"); });
  it("xr20", async () => { expect(await retry(() => Promise.resolve("t"), 1)).toBe("t"); });
  it("xr21", async () => { expect(await retry(() => Promise.resolve("u"), 1)).toBe("u"); });
  it("xr22", async () => { expect(await retry(() => Promise.resolve("v"), 1)).toBe("v"); });
  it("xr23", async () => { expect(await retry(() => Promise.resolve("w"), 1)).toBe("w"); });
  it("xr24", async () => { expect(await retry(() => Promise.resolve("x"), 1)).toBe("x"); });
  it("xr25", async () => { expect(await retry(() => Promise.resolve("y"), 1)).toBe("y"); });
  it("xr26", async () => { expect(await retry(() => Promise.resolve("z"), 1)).toBe("z"); });
  it("xr27", async () => { expect(await retry(() => Promise.resolve(true), 1)).toBe(true); });
  it("xr28", async () => { expect(await retry(() => Promise.resolve(false), 1)).toBe(false); });
  it("xr29", async () => { expect(await retry(() => Promise.resolve(null), 1)).toBe(null); });
  it("xr30", async () => { expect(await retry(() => Promise.resolve(0), 1)).toBe(0); });
  it("xr31", async () => { let a=0; await retry(() => { a++; if(a<2) throw new Error("e"); return Promise.resolve("ok"); }, 5); expect(a).toBe(2); });
  it("xr32", async () => { let a=0; await retry(() => { a++; return Promise.resolve(1); }, 3); expect(a).toBe(1); });
  it("xr33", async () => { expect(await retry(async () => 33, 1)).toBe(33); });
  it("xr34", async () => { expect(await retry(async () => 34, 1)).toBe(34); });
  it("xr35", async () => { expect(await retry(async () => 35, 1)).toBe(35); });
  it("xr36", async () => { expect(await retry(async () => 36, 1)).toBe(36); });
  it("xr37", async () => { expect(await retry(async () => 37, 1)).toBe(37); });
  it("xr38", async () => { expect(await retry(async () => 38, 1)).toBe(38); });
  it("xr39", async () => { expect(await retry(async () => 39, 1)).toBe(39); });
  it("xr40", async () => { expect(await retry(async () => 40, 1)).toBe(40); });
  it("xr41", async () => { expect(await retry(async () => 41, 1)).toBe(41); });
  it("xr42", async () => { expect(await retry(async () => 42, 1)).toBe(42); });
  it("xr43", async () => { expect(await retry(async () => 43, 1)).toBe(43); });
  it("xr44", async () => { expect(await retry(async () => 44, 1)).toBe(44); });
  it("xr45", async () => { expect(await retry(async () => 45, 1)).toBe(45); });
  it("xr46", async () => { expect(await retry(async () => 46, 1)).toBe(46); });
  it("xr47", async () => { expect(await retry(async () => 47, 1)).toBe(47); });
  it("xr48", async () => { expect(await retry(async () => 48, 1)).toBe(48); });
  it("xr49", async () => { expect(await retry(async () => 49, 1)).toBe(49); });
  it("xr50", async () => { expect(await retry(async () => 50, 1)).toBe(50); });
  it("xr51", async () => { expect(await retry(async () => 51, 1)).toBe(51); });
  it("xr52", async () => { expect(await retry(async () => 52, 1)).toBe(52); });
  it("xr53", async () => { expect(await retry(async () => 53, 1)).toBe(53); });
  it("xr54", async () => { expect(await retry(async () => 54, 1)).toBe(54); });
  it("xr55", async () => { expect(await retry(async () => 55, 1)).toBe(55); });
  it("xr56", async () => { expect(await retry(async () => 56, 1)).toBe(56); });
  it("xr57", async () => { expect(await retry(async () => 57, 1)).toBe(57); });
  it("xr58", async () => { expect(await retry(async () => 58, 1)).toBe(58); });
  it("xr59", async () => { expect(await retry(async () => 59, 1)).toBe(59); });
  it("xr60", async () => { expect(await retry(async () => 60, 1)).toBe(60); });
  it("xr61", async () => { expect(await retry(async () => 61, 1)).toBe(61); });
  it("xr62", async () => { expect(await retry(async () => 62, 1)).toBe(62); });
  it("xr63", async () => { expect(await retry(async () => 63, 1)).toBe(63); });
  it("xr64", async () => { expect(await retry(async () => 64, 1)).toBe(64); });
  it("xr65", async () => { expect(await retry(async () => 65, 1)).toBe(65); });
  it("xr66", async () => { expect(await retry(async () => 66, 1)).toBe(66); });
  it("xr67", async () => { expect(await retry(async () => 67, 1)).toBe(67); });
  it("xr68", async () => { expect(await retry(async () => 68, 1)).toBe(68); });
  it("xr69", async () => { expect(await retry(async () => 69, 1)).toBe(69); });
  it("xr70", async () => { expect(await retry(async () => 70, 1)).toBe(70); });
  it("xr71", async () => { expect(await retry(async () => 71, 1)).toBe(71); });
  it("xr72", async () => { expect(await retry(async () => 72, 1)).toBe(72); });
  it("xr73", async () => { expect(await retry(async () => 73, 1)).toBe(73); });
  it("xr74", async () => { expect(await retry(async () => 74, 1)).toBe(74); });
  it("xr75", async () => { expect(await retry(async () => 75, 1)).toBe(75); });
  it("xr76", async () => { expect(await retry(async () => 76, 1)).toBe(76); });
  it("xr77", async () => { expect(await retry(async () => 77, 1)).toBe(77); });
  it("xr78", async () => { expect(await retry(async () => 78, 1)).toBe(78); });
  it("xr79", async () => { expect(await retry(async () => 79, 1)).toBe(79); });
  it("xr80", async () => { expect(await retry(async () => 80, 1)).toBe(80); });
  it("xr81", async () => { expect(await retry(async () => 81, 1)).toBe(81); });
  it("xr82", async () => { expect(await retry(async () => 82, 1)).toBe(82); });
  it("xr83", async () => { expect(await retry(async () => 83, 1)).toBe(83); });
  it("xr84", async () => { expect(await retry(async () => 84, 1)).toBe(84); });
  it("xr85", async () => { expect(await retry(async () => 85, 1)).toBe(85); });
  it("xr86", async () => { expect(await retry(async () => 86, 1)).toBe(86); });
  it("xr87", async () => { expect(await retry(async () => 87, 1)).toBe(87); });
  it("xr88", async () => { expect(await retry(async () => 88, 1)).toBe(88); });
  it("xr89", async () => { expect(await retry(async () => 89, 1)).toBe(89); });
  it("xr90", async () => { expect(await retry(async () => 90, 1)).toBe(90); });
});
