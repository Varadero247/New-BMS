// Copyright (c) 2026 Nexara DMCC. All rights reserved.
// Proprietary and confidential. Unauthorised copying prohibited.
// See LICENCE file for details.

import { dijkstra, bellmanFord, floydWarshall, topologicalSort, stronglyConnected } from "../graph-algorithms-2";

describe("dijkstra single node", () => {
  it("d1", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d2", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d3", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d4", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d5", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d6", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d7", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d8", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d9", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d10", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d11", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d12", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d13", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d14", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d15", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d16", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d17", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d18", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d19", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d20", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d21", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d22", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d23", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d24", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d25", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d26", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d27", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d28", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d29", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d30", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d31", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d32", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d33", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d34", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d35", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d36", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d37", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d38", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d39", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d40", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d41", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d42", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d43", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d44", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d45", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d46", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d47", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d48", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d49", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d50", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d51", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d52", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d53", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d54", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d55", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d56", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d57", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d58", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d59", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d60", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d61", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d62", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d63", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d64", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d65", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d66", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d67", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d68", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d69", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d70", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d71", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d72", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d73", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d74", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d75", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d76", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d77", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d78", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d79", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d80", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d81", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d82", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d83", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d84", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d85", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d86", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d87", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d88", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d89", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d90", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d91", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d92", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d93", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d94", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d95", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d96", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d97", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d98", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d99", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d100", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d101", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d102", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d103", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d104", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d105", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d106", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d107", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d108", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d109", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d110", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d111", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d112", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d113", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d114", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d115", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d116", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d117", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d118", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d119", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d120", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d121", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d122", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d123", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d124", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d125", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d126", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d127", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d128", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d129", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d130", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d131", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d132", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d133", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d134", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d135", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d136", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d137", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d138", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d139", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d140", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d141", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d142", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d143", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d144", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d145", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d146", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d147", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d148", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d149", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d150", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d151", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d152", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d153", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d154", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d155", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d156", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d157", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d158", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d159", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d160", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d161", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d162", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d163", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d164", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d165", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d166", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d167", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d168", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d169", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d170", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d171", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d172", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d173", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d174", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d175", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d176", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d177", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d178", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d179", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d180", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d181", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d182", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d183", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d184", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d185", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d186", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d187", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d188", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d189", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d190", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d191", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d192", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d193", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d194", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d195", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d196", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d197", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d198", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d199", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
  it("d200", () => { const g = new Map([[0, []]]); const d = dijkstra(g, 0); expect(d.get(0)).toBe(0); });
});

describe("dijkstra two nodes", () => {
  it("d2n1", () => { const g = new Map([[0, [[1, 1]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(1); });
  it("d2n2", () => { const g = new Map([[0, [[1, 2]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(2); });
  it("d2n3", () => { const g = new Map([[0, [[1, 3]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(3); });
  it("d2n4", () => { const g = new Map([[0, [[1, 4]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(4); });
  it("d2n5", () => { const g = new Map([[0, [[1, 5]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(5); });
  it("d2n6", () => { const g = new Map([[0, [[1, 6]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(6); });
  it("d2n7", () => { const g = new Map([[0, [[1, 7]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(7); });
  it("d2n8", () => { const g = new Map([[0, [[1, 8]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(8); });
  it("d2n9", () => { const g = new Map([[0, [[1, 9]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(9); });
  it("d2n10", () => { const g = new Map([[0, [[1, 10]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(10); });
  it("d2n11", () => { const g = new Map([[0, [[1, 11]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(11); });
  it("d2n12", () => { const g = new Map([[0, [[1, 12]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(12); });
  it("d2n13", () => { const g = new Map([[0, [[1, 13]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(13); });
  it("d2n14", () => { const g = new Map([[0, [[1, 14]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(14); });
  it("d2n15", () => { const g = new Map([[0, [[1, 15]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(15); });
  it("d2n16", () => { const g = new Map([[0, [[1, 16]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(16); });
  it("d2n17", () => { const g = new Map([[0, [[1, 17]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(17); });
  it("d2n18", () => { const g = new Map([[0, [[1, 18]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(18); });
  it("d2n19", () => { const g = new Map([[0, [[1, 19]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(19); });
  it("d2n20", () => { const g = new Map([[0, [[1, 20]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(20); });
  it("d2n21", () => { const g = new Map([[0, [[1, 21]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(21); });
  it("d2n22", () => { const g = new Map([[0, [[1, 22]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(22); });
  it("d2n23", () => { const g = new Map([[0, [[1, 23]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(23); });
  it("d2n24", () => { const g = new Map([[0, [[1, 24]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(24); });
  it("d2n25", () => { const g = new Map([[0, [[1, 25]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(25); });
  it("d2n26", () => { const g = new Map([[0, [[1, 26]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(26); });
  it("d2n27", () => { const g = new Map([[0, [[1, 27]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(27); });
  it("d2n28", () => { const g = new Map([[0, [[1, 28]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(28); });
  it("d2n29", () => { const g = new Map([[0, [[1, 29]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(29); });
  it("d2n30", () => { const g = new Map([[0, [[1, 30]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(30); });
  it("d2n31", () => { const g = new Map([[0, [[1, 31]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(31); });
  it("d2n32", () => { const g = new Map([[0, [[1, 32]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(32); });
  it("d2n33", () => { const g = new Map([[0, [[1, 33]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(33); });
  it("d2n34", () => { const g = new Map([[0, [[1, 34]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(34); });
  it("d2n35", () => { const g = new Map([[0, [[1, 35]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(35); });
  it("d2n36", () => { const g = new Map([[0, [[1, 36]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(36); });
  it("d2n37", () => { const g = new Map([[0, [[1, 37]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(37); });
  it("d2n38", () => { const g = new Map([[0, [[1, 38]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(38); });
  it("d2n39", () => { const g = new Map([[0, [[1, 39]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(39); });
  it("d2n40", () => { const g = new Map([[0, [[1, 40]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(40); });
  it("d2n41", () => { const g = new Map([[0, [[1, 41]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(41); });
  it("d2n42", () => { const g = new Map([[0, [[1, 42]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(42); });
  it("d2n43", () => { const g = new Map([[0, [[1, 43]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(43); });
  it("d2n44", () => { const g = new Map([[0, [[1, 44]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(44); });
  it("d2n45", () => { const g = new Map([[0, [[1, 45]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(45); });
  it("d2n46", () => { const g = new Map([[0, [[1, 46]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(46); });
  it("d2n47", () => { const g = new Map([[0, [[1, 47]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(47); });
  it("d2n48", () => { const g = new Map([[0, [[1, 48]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(48); });
  it("d2n49", () => { const g = new Map([[0, [[1, 49]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(49); });
  it("d2n50", () => { const g = new Map([[0, [[1, 50]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(50); });
  it("d2n51", () => { const g = new Map([[0, [[1, 51]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(51); });
  it("d2n52", () => { const g = new Map([[0, [[1, 52]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(52); });
  it("d2n53", () => { const g = new Map([[0, [[1, 53]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(53); });
  it("d2n54", () => { const g = new Map([[0, [[1, 54]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(54); });
  it("d2n55", () => { const g = new Map([[0, [[1, 55]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(55); });
  it("d2n56", () => { const g = new Map([[0, [[1, 56]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(56); });
  it("d2n57", () => { const g = new Map([[0, [[1, 57]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(57); });
  it("d2n58", () => { const g = new Map([[0, [[1, 58]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(58); });
  it("d2n59", () => { const g = new Map([[0, [[1, 59]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(59); });
  it("d2n60", () => { const g = new Map([[0, [[1, 60]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(60); });
  it("d2n61", () => { const g = new Map([[0, [[1, 61]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(61); });
  it("d2n62", () => { const g = new Map([[0, [[1, 62]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(62); });
  it("d2n63", () => { const g = new Map([[0, [[1, 63]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(63); });
  it("d2n64", () => { const g = new Map([[0, [[1, 64]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(64); });
  it("d2n65", () => { const g = new Map([[0, [[1, 65]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(65); });
  it("d2n66", () => { const g = new Map([[0, [[1, 66]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(66); });
  it("d2n67", () => { const g = new Map([[0, [[1, 67]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(67); });
  it("d2n68", () => { const g = new Map([[0, [[1, 68]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(68); });
  it("d2n69", () => { const g = new Map([[0, [[1, 69]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(69); });
  it("d2n70", () => { const g = new Map([[0, [[1, 70]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(70); });
  it("d2n71", () => { const g = new Map([[0, [[1, 71]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(71); });
  it("d2n72", () => { const g = new Map([[0, [[1, 72]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(72); });
  it("d2n73", () => { const g = new Map([[0, [[1, 73]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(73); });
  it("d2n74", () => { const g = new Map([[0, [[1, 74]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(74); });
  it("d2n75", () => { const g = new Map([[0, [[1, 75]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(75); });
  it("d2n76", () => { const g = new Map([[0, [[1, 76]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(76); });
  it("d2n77", () => { const g = new Map([[0, [[1, 77]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(77); });
  it("d2n78", () => { const g = new Map([[0, [[1, 78]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(78); });
  it("d2n79", () => { const g = new Map([[0, [[1, 79]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(79); });
  it("d2n80", () => { const g = new Map([[0, [[1, 80]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(80); });
  it("d2n81", () => { const g = new Map([[0, [[1, 81]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(81); });
  it("d2n82", () => { const g = new Map([[0, [[1, 82]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(82); });
  it("d2n83", () => { const g = new Map([[0, [[1, 83]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(83); });
  it("d2n84", () => { const g = new Map([[0, [[1, 84]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(84); });
  it("d2n85", () => { const g = new Map([[0, [[1, 85]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(85); });
  it("d2n86", () => { const g = new Map([[0, [[1, 86]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(86); });
  it("d2n87", () => { const g = new Map([[0, [[1, 87]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(87); });
  it("d2n88", () => { const g = new Map([[0, [[1, 88]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(88); });
  it("d2n89", () => { const g = new Map([[0, [[1, 89]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(89); });
  it("d2n90", () => { const g = new Map([[0, [[1, 90]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(90); });
  it("d2n91", () => { const g = new Map([[0, [[1, 91]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(91); });
  it("d2n92", () => { const g = new Map([[0, [[1, 92]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(92); });
  it("d2n93", () => { const g = new Map([[0, [[1, 93]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(93); });
  it("d2n94", () => { const g = new Map([[0, [[1, 94]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(94); });
  it("d2n95", () => { const g = new Map([[0, [[1, 95]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(95); });
  it("d2n96", () => { const g = new Map([[0, [[1, 96]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(96); });
  it("d2n97", () => { const g = new Map([[0, [[1, 97]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(97); });
  it("d2n98", () => { const g = new Map([[0, [[1, 98]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(98); });
  it("d2n99", () => { const g = new Map([[0, [[1, 99]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(99); });
  it("d2n100", () => { const g = new Map([[0, [[1, 100]]], [1, []]]); const d = dijkstra(g, 0); expect(d.get(1)).toBe(100); });
});

describe("bellmanFord basic", () => {
  it("bf1", () => { const d = bellmanFord([[0, 1, 1]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(1); });
  it("bf2", () => { const d = bellmanFord([[0, 1, 2]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(2); });
  it("bf3", () => { const d = bellmanFord([[0, 1, 3]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(3); });
  it("bf4", () => { const d = bellmanFord([[0, 1, 4]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(4); });
  it("bf5", () => { const d = bellmanFord([[0, 1, 5]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(5); });
  it("bf6", () => { const d = bellmanFord([[0, 1, 6]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(6); });
  it("bf7", () => { const d = bellmanFord([[0, 1, 7]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(7); });
  it("bf8", () => { const d = bellmanFord([[0, 1, 8]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(8); });
  it("bf9", () => { const d = bellmanFord([[0, 1, 9]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(9); });
  it("bf10", () => { const d = bellmanFord([[0, 1, 10]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(10); });
  it("bf11", () => { const d = bellmanFord([[0, 1, 11]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(11); });
  it("bf12", () => { const d = bellmanFord([[0, 1, 12]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(12); });
  it("bf13", () => { const d = bellmanFord([[0, 1, 13]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(13); });
  it("bf14", () => { const d = bellmanFord([[0, 1, 14]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(14); });
  it("bf15", () => { const d = bellmanFord([[0, 1, 15]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(15); });
  it("bf16", () => { const d = bellmanFord([[0, 1, 16]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(16); });
  it("bf17", () => { const d = bellmanFord([[0, 1, 17]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(17); });
  it("bf18", () => { const d = bellmanFord([[0, 1, 18]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(18); });
  it("bf19", () => { const d = bellmanFord([[0, 1, 19]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(19); });
  it("bf20", () => { const d = bellmanFord([[0, 1, 20]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(20); });
  it("bf21", () => { const d = bellmanFord([[0, 1, 21]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(21); });
  it("bf22", () => { const d = bellmanFord([[0, 1, 22]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(22); });
  it("bf23", () => { const d = bellmanFord([[0, 1, 23]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(23); });
  it("bf24", () => { const d = bellmanFord([[0, 1, 24]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(24); });
  it("bf25", () => { const d = bellmanFord([[0, 1, 25]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(25); });
  it("bf26", () => { const d = bellmanFord([[0, 1, 26]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(26); });
  it("bf27", () => { const d = bellmanFord([[0, 1, 27]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(27); });
  it("bf28", () => { const d = bellmanFord([[0, 1, 28]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(28); });
  it("bf29", () => { const d = bellmanFord([[0, 1, 29]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(29); });
  it("bf30", () => { const d = bellmanFord([[0, 1, 30]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(30); });
  it("bf31", () => { const d = bellmanFord([[0, 1, 31]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(31); });
  it("bf32", () => { const d = bellmanFord([[0, 1, 32]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(32); });
  it("bf33", () => { const d = bellmanFord([[0, 1, 33]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(33); });
  it("bf34", () => { const d = bellmanFord([[0, 1, 34]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(34); });
  it("bf35", () => { const d = bellmanFord([[0, 1, 35]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(35); });
  it("bf36", () => { const d = bellmanFord([[0, 1, 36]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(36); });
  it("bf37", () => { const d = bellmanFord([[0, 1, 37]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(37); });
  it("bf38", () => { const d = bellmanFord([[0, 1, 38]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(38); });
  it("bf39", () => { const d = bellmanFord([[0, 1, 39]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(39); });
  it("bf40", () => { const d = bellmanFord([[0, 1, 40]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(40); });
  it("bf41", () => { const d = bellmanFord([[0, 1, 41]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(41); });
  it("bf42", () => { const d = bellmanFord([[0, 1, 42]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(42); });
  it("bf43", () => { const d = bellmanFord([[0, 1, 43]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(43); });
  it("bf44", () => { const d = bellmanFord([[0, 1, 44]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(44); });
  it("bf45", () => { const d = bellmanFord([[0, 1, 45]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(45); });
  it("bf46", () => { const d = bellmanFord([[0, 1, 46]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(46); });
  it("bf47", () => { const d = bellmanFord([[0, 1, 47]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(47); });
  it("bf48", () => { const d = bellmanFord([[0, 1, 48]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(48); });
  it("bf49", () => { const d = bellmanFord([[0, 1, 49]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(49); });
  it("bf50", () => { const d = bellmanFord([[0, 1, 50]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(50); });
  it("bf51", () => { const d = bellmanFord([[0, 1, 51]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(51); });
  it("bf52", () => { const d = bellmanFord([[0, 1, 52]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(52); });
  it("bf53", () => { const d = bellmanFord([[0, 1, 53]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(53); });
  it("bf54", () => { const d = bellmanFord([[0, 1, 54]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(54); });
  it("bf55", () => { const d = bellmanFord([[0, 1, 55]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(55); });
  it("bf56", () => { const d = bellmanFord([[0, 1, 56]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(56); });
  it("bf57", () => { const d = bellmanFord([[0, 1, 57]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(57); });
  it("bf58", () => { const d = bellmanFord([[0, 1, 58]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(58); });
  it("bf59", () => { const d = bellmanFord([[0, 1, 59]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(59); });
  it("bf60", () => { const d = bellmanFord([[0, 1, 60]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(60); });
  it("bf61", () => { const d = bellmanFord([[0, 1, 61]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(61); });
  it("bf62", () => { const d = bellmanFord([[0, 1, 62]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(62); });
  it("bf63", () => { const d = bellmanFord([[0, 1, 63]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(63); });
  it("bf64", () => { const d = bellmanFord([[0, 1, 64]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(64); });
  it("bf65", () => { const d = bellmanFord([[0, 1, 65]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(65); });
  it("bf66", () => { const d = bellmanFord([[0, 1, 66]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(66); });
  it("bf67", () => { const d = bellmanFord([[0, 1, 67]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(67); });
  it("bf68", () => { const d = bellmanFord([[0, 1, 68]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(68); });
  it("bf69", () => { const d = bellmanFord([[0, 1, 69]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(69); });
  it("bf70", () => { const d = bellmanFord([[0, 1, 70]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(70); });
  it("bf71", () => { const d = bellmanFord([[0, 1, 71]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(71); });
  it("bf72", () => { const d = bellmanFord([[0, 1, 72]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(72); });
  it("bf73", () => { const d = bellmanFord([[0, 1, 73]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(73); });
  it("bf74", () => { const d = bellmanFord([[0, 1, 74]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(74); });
  it("bf75", () => { const d = bellmanFord([[0, 1, 75]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(75); });
  it("bf76", () => { const d = bellmanFord([[0, 1, 76]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(76); });
  it("bf77", () => { const d = bellmanFord([[0, 1, 77]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(77); });
  it("bf78", () => { const d = bellmanFord([[0, 1, 78]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(78); });
  it("bf79", () => { const d = bellmanFord([[0, 1, 79]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(79); });
  it("bf80", () => { const d = bellmanFord([[0, 1, 80]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(80); });
  it("bf81", () => { const d = bellmanFord([[0, 1, 81]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(81); });
  it("bf82", () => { const d = bellmanFord([[0, 1, 82]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(82); });
  it("bf83", () => { const d = bellmanFord([[0, 1, 83]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(83); });
  it("bf84", () => { const d = bellmanFord([[0, 1, 84]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(84); });
  it("bf85", () => { const d = bellmanFord([[0, 1, 85]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(85); });
  it("bf86", () => { const d = bellmanFord([[0, 1, 86]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(86); });
  it("bf87", () => { const d = bellmanFord([[0, 1, 87]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(87); });
  it("bf88", () => { const d = bellmanFord([[0, 1, 88]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(88); });
  it("bf89", () => { const d = bellmanFord([[0, 1, 89]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(89); });
  it("bf90", () => { const d = bellmanFord([[0, 1, 90]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(90); });
  it("bf91", () => { const d = bellmanFord([[0, 1, 91]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(91); });
  it("bf92", () => { const d = bellmanFord([[0, 1, 92]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(92); });
  it("bf93", () => { const d = bellmanFord([[0, 1, 93]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(93); });
  it("bf94", () => { const d = bellmanFord([[0, 1, 94]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(94); });
  it("bf95", () => { const d = bellmanFord([[0, 1, 95]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(95); });
  it("bf96", () => { const d = bellmanFord([[0, 1, 96]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(96); });
  it("bf97", () => { const d = bellmanFord([[0, 1, 97]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(97); });
  it("bf98", () => { const d = bellmanFord([[0, 1, 98]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(98); });
  it("bf99", () => { const d = bellmanFord([[0, 1, 99]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(99); });
  it("bf100", () => { const d = bellmanFord([[0, 1, 100]], 2, 0); expect(d).not.toBeNull(); expect(d!.get(1)).toBe(100); });
});

describe("floydWarshall diagonal", () => {
  it("fw1", () => { const m = Array.from({length:1}, (_,i) => Array.from({length:1}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw2", () => { const m = Array.from({length:2}, (_,i) => Array.from({length:2}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw3", () => { const m = Array.from({length:3}, (_,i) => Array.from({length:3}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw4", () => { const m = Array.from({length:4}, (_,i) => Array.from({length:4}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw5", () => { const m = Array.from({length:5}, (_,i) => Array.from({length:5}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw6", () => { const m = Array.from({length:6}, (_,i) => Array.from({length:6}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw7", () => { const m = Array.from({length:7}, (_,i) => Array.from({length:7}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw8", () => { const m = Array.from({length:8}, (_,i) => Array.from({length:8}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw9", () => { const m = Array.from({length:9}, (_,i) => Array.from({length:9}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw10", () => { const m = Array.from({length:10}, (_,i) => Array.from({length:10}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw11", () => { const m = Array.from({length:11}, (_,i) => Array.from({length:11}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw12", () => { const m = Array.from({length:12}, (_,i) => Array.from({length:12}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw13", () => { const m = Array.from({length:13}, (_,i) => Array.from({length:13}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw14", () => { const m = Array.from({length:14}, (_,i) => Array.from({length:14}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw15", () => { const m = Array.from({length:15}, (_,i) => Array.from({length:15}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw16", () => { const m = Array.from({length:16}, (_,i) => Array.from({length:16}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw17", () => { const m = Array.from({length:17}, (_,i) => Array.from({length:17}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw18", () => { const m = Array.from({length:18}, (_,i) => Array.from({length:18}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw19", () => { const m = Array.from({length:19}, (_,i) => Array.from({length:19}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw20", () => { const m = Array.from({length:20}, (_,i) => Array.from({length:20}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw21", () => { const m = Array.from({length:21}, (_,i) => Array.from({length:21}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw22", () => { const m = Array.from({length:22}, (_,i) => Array.from({length:22}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw23", () => { const m = Array.from({length:23}, (_,i) => Array.from({length:23}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw24", () => { const m = Array.from({length:24}, (_,i) => Array.from({length:24}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw25", () => { const m = Array.from({length:25}, (_,i) => Array.from({length:25}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw26", () => { const m = Array.from({length:26}, (_,i) => Array.from({length:26}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw27", () => { const m = Array.from({length:27}, (_,i) => Array.from({length:27}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw28", () => { const m = Array.from({length:28}, (_,i) => Array.from({length:28}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw29", () => { const m = Array.from({length:29}, (_,i) => Array.from({length:29}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw30", () => { const m = Array.from({length:30}, (_,i) => Array.from({length:30}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw31", () => { const m = Array.from({length:31}, (_,i) => Array.from({length:31}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw32", () => { const m = Array.from({length:32}, (_,i) => Array.from({length:32}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw33", () => { const m = Array.from({length:33}, (_,i) => Array.from({length:33}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw34", () => { const m = Array.from({length:34}, (_,i) => Array.from({length:34}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw35", () => { const m = Array.from({length:35}, (_,i) => Array.from({length:35}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw36", () => { const m = Array.from({length:36}, (_,i) => Array.from({length:36}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw37", () => { const m = Array.from({length:37}, (_,i) => Array.from({length:37}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw38", () => { const m = Array.from({length:38}, (_,i) => Array.from({length:38}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw39", () => { const m = Array.from({length:39}, (_,i) => Array.from({length:39}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw40", () => { const m = Array.from({length:40}, (_,i) => Array.from({length:40}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw41", () => { const m = Array.from({length:41}, (_,i) => Array.from({length:41}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw42", () => { const m = Array.from({length:42}, (_,i) => Array.from({length:42}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw43", () => { const m = Array.from({length:43}, (_,i) => Array.from({length:43}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw44", () => { const m = Array.from({length:44}, (_,i) => Array.from({length:44}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw45", () => { const m = Array.from({length:45}, (_,i) => Array.from({length:45}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw46", () => { const m = Array.from({length:46}, (_,i) => Array.from({length:46}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw47", () => { const m = Array.from({length:47}, (_,i) => Array.from({length:47}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw48", () => { const m = Array.from({length:48}, (_,i) => Array.from({length:48}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw49", () => { const m = Array.from({length:49}, (_,i) => Array.from({length:49}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw50", () => { const m = Array.from({length:50}, (_,i) => Array.from({length:50}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw51", () => { const m = Array.from({length:51}, (_,i) => Array.from({length:51}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw52", () => { const m = Array.from({length:52}, (_,i) => Array.from({length:52}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw53", () => { const m = Array.from({length:53}, (_,i) => Array.from({length:53}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw54", () => { const m = Array.from({length:54}, (_,i) => Array.from({length:54}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw55", () => { const m = Array.from({length:55}, (_,i) => Array.from({length:55}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw56", () => { const m = Array.from({length:56}, (_,i) => Array.from({length:56}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw57", () => { const m = Array.from({length:57}, (_,i) => Array.from({length:57}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw58", () => { const m = Array.from({length:58}, (_,i) => Array.from({length:58}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw59", () => { const m = Array.from({length:59}, (_,i) => Array.from({length:59}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw60", () => { const m = Array.from({length:60}, (_,i) => Array.from({length:60}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw61", () => { const m = Array.from({length:61}, (_,i) => Array.from({length:61}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw62", () => { const m = Array.from({length:62}, (_,i) => Array.from({length:62}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw63", () => { const m = Array.from({length:63}, (_,i) => Array.from({length:63}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw64", () => { const m = Array.from({length:64}, (_,i) => Array.from({length:64}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw65", () => { const m = Array.from({length:65}, (_,i) => Array.from({length:65}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw66", () => { const m = Array.from({length:66}, (_,i) => Array.from({length:66}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw67", () => { const m = Array.from({length:67}, (_,i) => Array.from({length:67}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw68", () => { const m = Array.from({length:68}, (_,i) => Array.from({length:68}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw69", () => { const m = Array.from({length:69}, (_,i) => Array.from({length:69}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw70", () => { const m = Array.from({length:70}, (_,i) => Array.from({length:70}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw71", () => { const m = Array.from({length:71}, (_,i) => Array.from({length:71}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw72", () => { const m = Array.from({length:72}, (_,i) => Array.from({length:72}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw73", () => { const m = Array.from({length:73}, (_,i) => Array.from({length:73}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw74", () => { const m = Array.from({length:74}, (_,i) => Array.from({length:74}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw75", () => { const m = Array.from({length:75}, (_,i) => Array.from({length:75}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw76", () => { const m = Array.from({length:76}, (_,i) => Array.from({length:76}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw77", () => { const m = Array.from({length:77}, (_,i) => Array.from({length:77}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw78", () => { const m = Array.from({length:78}, (_,i) => Array.from({length:78}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw79", () => { const m = Array.from({length:79}, (_,i) => Array.from({length:79}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw80", () => { const m = Array.from({length:80}, (_,i) => Array.from({length:80}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw81", () => { const m = Array.from({length:81}, (_,i) => Array.from({length:81}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw82", () => { const m = Array.from({length:82}, (_,i) => Array.from({length:82}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw83", () => { const m = Array.from({length:83}, (_,i) => Array.from({length:83}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw84", () => { const m = Array.from({length:84}, (_,i) => Array.from({length:84}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw85", () => { const m = Array.from({length:85}, (_,i) => Array.from({length:85}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw86", () => { const m = Array.from({length:86}, (_,i) => Array.from({length:86}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw87", () => { const m = Array.from({length:87}, (_,i) => Array.from({length:87}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw88", () => { const m = Array.from({length:88}, (_,i) => Array.from({length:88}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw89", () => { const m = Array.from({length:89}, (_,i) => Array.from({length:89}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw90", () => { const m = Array.from({length:90}, (_,i) => Array.from({length:90}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw91", () => { const m = Array.from({length:91}, (_,i) => Array.from({length:91}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw92", () => { const m = Array.from({length:92}, (_,i) => Array.from({length:92}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw93", () => { const m = Array.from({length:93}, (_,i) => Array.from({length:93}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw94", () => { const m = Array.from({length:94}, (_,i) => Array.from({length:94}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw95", () => { const m = Array.from({length:95}, (_,i) => Array.from({length:95}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw96", () => { const m = Array.from({length:96}, (_,i) => Array.from({length:96}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw97", () => { const m = Array.from({length:97}, (_,i) => Array.from({length:97}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw98", () => { const m = Array.from({length:98}, (_,i) => Array.from({length:98}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw99", () => { const m = Array.from({length:99}, (_,i) => Array.from({length:99}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
  it("fw100", () => { const m = Array.from({length:100}, (_,i) => Array.from({length:100}, (_,j) => i===j ? 0 : Infinity)); const r = floydWarshall(m); expect(r[0][0]).toBe(0); });
});

describe("topologicalSort linear chain", () => {
  it("ts2", () => { const g = new Map(Array.from({length:2}, (_,i) => [i, i<2-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(2); });
  it("ts3", () => { const g = new Map(Array.from({length:3}, (_,i) => [i, i<3-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(3); });
  it("ts4", () => { const g = new Map(Array.from({length:4}, (_,i) => [i, i<4-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(4); });
  it("ts5", () => { const g = new Map(Array.from({length:5}, (_,i) => [i, i<5-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(5); });
  it("ts6", () => { const g = new Map(Array.from({length:6}, (_,i) => [i, i<6-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(6); });
  it("ts7", () => { const g = new Map(Array.from({length:7}, (_,i) => [i, i<7-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(7); });
  it("ts8", () => { const g = new Map(Array.from({length:8}, (_,i) => [i, i<8-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(8); });
  it("ts9", () => { const g = new Map(Array.from({length:9}, (_,i) => [i, i<9-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(9); });
  it("ts10", () => { const g = new Map(Array.from({length:10}, (_,i) => [i, i<10-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(10); });
  it("ts11", () => { const g = new Map(Array.from({length:11}, (_,i) => [i, i<11-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(11); });
  it("ts12", () => { const g = new Map(Array.from({length:12}, (_,i) => [i, i<12-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(12); });
  it("ts13", () => { const g = new Map(Array.from({length:13}, (_,i) => [i, i<13-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(13); });
  it("ts14", () => { const g = new Map(Array.from({length:14}, (_,i) => [i, i<14-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(14); });
  it("ts15", () => { const g = new Map(Array.from({length:15}, (_,i) => [i, i<15-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(15); });
  it("ts16", () => { const g = new Map(Array.from({length:16}, (_,i) => [i, i<16-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(16); });
  it("ts17", () => { const g = new Map(Array.from({length:17}, (_,i) => [i, i<17-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(17); });
  it("ts18", () => { const g = new Map(Array.from({length:18}, (_,i) => [i, i<18-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(18); });
  it("ts19", () => { const g = new Map(Array.from({length:19}, (_,i) => [i, i<19-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(19); });
  it("ts20", () => { const g = new Map(Array.from({length:20}, (_,i) => [i, i<20-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(20); });
  it("ts21", () => { const g = new Map(Array.from({length:21}, (_,i) => [i, i<21-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(21); });
  it("ts22", () => { const g = new Map(Array.from({length:22}, (_,i) => [i, i<22-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(22); });
  it("ts23", () => { const g = new Map(Array.from({length:23}, (_,i) => [i, i<23-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(23); });
  it("ts24", () => { const g = new Map(Array.from({length:24}, (_,i) => [i, i<24-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(24); });
  it("ts25", () => { const g = new Map(Array.from({length:25}, (_,i) => [i, i<25-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(25); });
  it("ts26", () => { const g = new Map(Array.from({length:26}, (_,i) => [i, i<26-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(26); });
  it("ts27", () => { const g = new Map(Array.from({length:27}, (_,i) => [i, i<27-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(27); });
  it("ts28", () => { const g = new Map(Array.from({length:28}, (_,i) => [i, i<28-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(28); });
  it("ts29", () => { const g = new Map(Array.from({length:29}, (_,i) => [i, i<29-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(29); });
  it("ts30", () => { const g = new Map(Array.from({length:30}, (_,i) => [i, i<30-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(30); });
  it("ts31", () => { const g = new Map(Array.from({length:31}, (_,i) => [i, i<31-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(31); });
  it("ts32", () => { const g = new Map(Array.from({length:32}, (_,i) => [i, i<32-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(32); });
  it("ts33", () => { const g = new Map(Array.from({length:33}, (_,i) => [i, i<33-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(33); });
  it("ts34", () => { const g = new Map(Array.from({length:34}, (_,i) => [i, i<34-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(34); });
  it("ts35", () => { const g = new Map(Array.from({length:35}, (_,i) => [i, i<35-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(35); });
  it("ts36", () => { const g = new Map(Array.from({length:36}, (_,i) => [i, i<36-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(36); });
  it("ts37", () => { const g = new Map(Array.from({length:37}, (_,i) => [i, i<37-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(37); });
  it("ts38", () => { const g = new Map(Array.from({length:38}, (_,i) => [i, i<38-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(38); });
  it("ts39", () => { const g = new Map(Array.from({length:39}, (_,i) => [i, i<39-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(39); });
  it("ts40", () => { const g = new Map(Array.from({length:40}, (_,i) => [i, i<40-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(40); });
  it("ts41", () => { const g = new Map(Array.from({length:41}, (_,i) => [i, i<41-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(41); });
  it("ts42", () => { const g = new Map(Array.from({length:42}, (_,i) => [i, i<42-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(42); });
  it("ts43", () => { const g = new Map(Array.from({length:43}, (_,i) => [i, i<43-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(43); });
  it("ts44", () => { const g = new Map(Array.from({length:44}, (_,i) => [i, i<44-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(44); });
  it("ts45", () => { const g = new Map(Array.from({length:45}, (_,i) => [i, i<45-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(45); });
  it("ts46", () => { const g = new Map(Array.from({length:46}, (_,i) => [i, i<46-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(46); });
  it("ts47", () => { const g = new Map(Array.from({length:47}, (_,i) => [i, i<47-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(47); });
  it("ts48", () => { const g = new Map(Array.from({length:48}, (_,i) => [i, i<48-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(48); });
  it("ts49", () => { const g = new Map(Array.from({length:49}, (_,i) => [i, i<49-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(49); });
  it("ts50", () => { const g = new Map(Array.from({length:50}, (_,i) => [i, i<50-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(50); });
  it("ts51", () => { const g = new Map(Array.from({length:51}, (_,i) => [i, i<51-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(51); });
  it("ts52", () => { const g = new Map(Array.from({length:52}, (_,i) => [i, i<52-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(52); });
  it("ts53", () => { const g = new Map(Array.from({length:53}, (_,i) => [i, i<53-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(53); });
  it("ts54", () => { const g = new Map(Array.from({length:54}, (_,i) => [i, i<54-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(54); });
  it("ts55", () => { const g = new Map(Array.from({length:55}, (_,i) => [i, i<55-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(55); });
  it("ts56", () => { const g = new Map(Array.from({length:56}, (_,i) => [i, i<56-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(56); });
  it("ts57", () => { const g = new Map(Array.from({length:57}, (_,i) => [i, i<57-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(57); });
  it("ts58", () => { const g = new Map(Array.from({length:58}, (_,i) => [i, i<58-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(58); });
  it("ts59", () => { const g = new Map(Array.from({length:59}, (_,i) => [i, i<59-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(59); });
  it("ts60", () => { const g = new Map(Array.from({length:60}, (_,i) => [i, i<60-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(60); });
  it("ts61", () => { const g = new Map(Array.from({length:61}, (_,i) => [i, i<61-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(61); });
  it("ts62", () => { const g = new Map(Array.from({length:62}, (_,i) => [i, i<62-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(62); });
  it("ts63", () => { const g = new Map(Array.from({length:63}, (_,i) => [i, i<63-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(63); });
  it("ts64", () => { const g = new Map(Array.from({length:64}, (_,i) => [i, i<64-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(64); });
  it("ts65", () => { const g = new Map(Array.from({length:65}, (_,i) => [i, i<65-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(65); });
  it("ts66", () => { const g = new Map(Array.from({length:66}, (_,i) => [i, i<66-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(66); });
  it("ts67", () => { const g = new Map(Array.from({length:67}, (_,i) => [i, i<67-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(67); });
  it("ts68", () => { const g = new Map(Array.from({length:68}, (_,i) => [i, i<68-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(68); });
  it("ts69", () => { const g = new Map(Array.from({length:69}, (_,i) => [i, i<69-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(69); });
  it("ts70", () => { const g = new Map(Array.from({length:70}, (_,i) => [i, i<70-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(70); });
  it("ts71", () => { const g = new Map(Array.from({length:71}, (_,i) => [i, i<71-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(71); });
  it("ts72", () => { const g = new Map(Array.from({length:72}, (_,i) => [i, i<72-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(72); });
  it("ts73", () => { const g = new Map(Array.from({length:73}, (_,i) => [i, i<73-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(73); });
  it("ts74", () => { const g = new Map(Array.from({length:74}, (_,i) => [i, i<74-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(74); });
  it("ts75", () => { const g = new Map(Array.from({length:75}, (_,i) => [i, i<75-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(75); });
  it("ts76", () => { const g = new Map(Array.from({length:76}, (_,i) => [i, i<76-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(76); });
  it("ts77", () => { const g = new Map(Array.from({length:77}, (_,i) => [i, i<77-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(77); });
  it("ts78", () => { const g = new Map(Array.from({length:78}, (_,i) => [i, i<78-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(78); });
  it("ts79", () => { const g = new Map(Array.from({length:79}, (_,i) => [i, i<79-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(79); });
  it("ts80", () => { const g = new Map(Array.from({length:80}, (_,i) => [i, i<80-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(80); });
  it("ts81", () => { const g = new Map(Array.from({length:81}, (_,i) => [i, i<81-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(81); });
  it("ts82", () => { const g = new Map(Array.from({length:82}, (_,i) => [i, i<82-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(82); });
  it("ts83", () => { const g = new Map(Array.from({length:83}, (_,i) => [i, i<83-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(83); });
  it("ts84", () => { const g = new Map(Array.from({length:84}, (_,i) => [i, i<84-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(84); });
  it("ts85", () => { const g = new Map(Array.from({length:85}, (_,i) => [i, i<85-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(85); });
  it("ts86", () => { const g = new Map(Array.from({length:86}, (_,i) => [i, i<86-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(86); });
  it("ts87", () => { const g = new Map(Array.from({length:87}, (_,i) => [i, i<87-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(87); });
  it("ts88", () => { const g = new Map(Array.from({length:88}, (_,i) => [i, i<88-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(88); });
  it("ts89", () => { const g = new Map(Array.from({length:89}, (_,i) => [i, i<89-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(89); });
  it("ts90", () => { const g = new Map(Array.from({length:90}, (_,i) => [i, i<90-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(90); });
  it("ts91", () => { const g = new Map(Array.from({length:91}, (_,i) => [i, i<91-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(91); });
  it("ts92", () => { const g = new Map(Array.from({length:92}, (_,i) => [i, i<92-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(92); });
  it("ts93", () => { const g = new Map(Array.from({length:93}, (_,i) => [i, i<93-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(93); });
  it("ts94", () => { const g = new Map(Array.from({length:94}, (_,i) => [i, i<94-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(94); });
  it("ts95", () => { const g = new Map(Array.from({length:95}, (_,i) => [i, i<95-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(95); });
  it("ts96", () => { const g = new Map(Array.from({length:96}, (_,i) => [i, i<96-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(96); });
  it("ts97", () => { const g = new Map(Array.from({length:97}, (_,i) => [i, i<97-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(97); });
  it("ts98", () => { const g = new Map(Array.from({length:98}, (_,i) => [i, i<98-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(98); });
  it("ts99", () => { const g = new Map(Array.from({length:99}, (_,i) => [i, i<99-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(99); });
  it("ts100", () => { const g = new Map(Array.from({length:100}, (_,i) => [i, i<100-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(100); });
  it("ts101", () => { const g = new Map(Array.from({length:101}, (_,i) => [i, i<101-1 ? [i+1] : []])); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(101); });
});

describe("stronglyConnected self-loop", () => {
  it("sc1", () => { const g = new Map([[1, [1]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc2", () => { const g = new Map([[2, [2]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc3", () => { const g = new Map([[3, [3]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc4", () => { const g = new Map([[4, [4]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc5", () => { const g = new Map([[5, [5]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc6", () => { const g = new Map([[6, [6]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc7", () => { const g = new Map([[7, [7]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc8", () => { const g = new Map([[8, [8]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc9", () => { const g = new Map([[9, [9]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc10", () => { const g = new Map([[10, [10]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc11", () => { const g = new Map([[11, [11]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc12", () => { const g = new Map([[12, [12]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc13", () => { const g = new Map([[13, [13]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc14", () => { const g = new Map([[14, [14]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc15", () => { const g = new Map([[15, [15]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc16", () => { const g = new Map([[16, [16]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc17", () => { const g = new Map([[17, [17]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc18", () => { const g = new Map([[18, [18]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc19", () => { const g = new Map([[19, [19]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc20", () => { const g = new Map([[20, [20]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc21", () => { const g = new Map([[21, [21]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc22", () => { const g = new Map([[22, [22]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc23", () => { const g = new Map([[23, [23]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc24", () => { const g = new Map([[24, [24]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc25", () => { const g = new Map([[25, [25]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc26", () => { const g = new Map([[26, [26]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc27", () => { const g = new Map([[27, [27]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc28", () => { const g = new Map([[28, [28]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc29", () => { const g = new Map([[29, [29]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc30", () => { const g = new Map([[30, [30]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc31", () => { const g = new Map([[31, [31]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc32", () => { const g = new Map([[32, [32]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc33", () => { const g = new Map([[33, [33]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc34", () => { const g = new Map([[34, [34]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc35", () => { const g = new Map([[35, [35]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc36", () => { const g = new Map([[36, [36]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc37", () => { const g = new Map([[37, [37]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc38", () => { const g = new Map([[38, [38]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc39", () => { const g = new Map([[39, [39]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc40", () => { const g = new Map([[40, [40]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc41", () => { const g = new Map([[41, [41]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc42", () => { const g = new Map([[42, [42]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc43", () => { const g = new Map([[43, [43]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc44", () => { const g = new Map([[44, [44]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc45", () => { const g = new Map([[45, [45]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc46", () => { const g = new Map([[46, [46]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc47", () => { const g = new Map([[47, [47]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc48", () => { const g = new Map([[48, [48]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc49", () => { const g = new Map([[49, [49]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc50", () => { const g = new Map([[50, [50]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc51", () => { const g = new Map([[51, [51]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc52", () => { const g = new Map([[52, [52]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc53", () => { const g = new Map([[53, [53]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc54", () => { const g = new Map([[54, [54]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc55", () => { const g = new Map([[55, [55]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc56", () => { const g = new Map([[56, [56]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc57", () => { const g = new Map([[57, [57]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc58", () => { const g = new Map([[58, [58]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc59", () => { const g = new Map([[59, [59]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc60", () => { const g = new Map([[60, [60]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc61", () => { const g = new Map([[61, [61]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc62", () => { const g = new Map([[62, [62]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc63", () => { const g = new Map([[63, [63]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc64", () => { const g = new Map([[64, [64]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc65", () => { const g = new Map([[65, [65]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc66", () => { const g = new Map([[66, [66]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc67", () => { const g = new Map([[67, [67]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc68", () => { const g = new Map([[68, [68]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc69", () => { const g = new Map([[69, [69]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc70", () => { const g = new Map([[70, [70]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc71", () => { const g = new Map([[71, [71]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc72", () => { const g = new Map([[72, [72]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc73", () => { const g = new Map([[73, [73]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc74", () => { const g = new Map([[74, [74]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc75", () => { const g = new Map([[75, [75]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc76", () => { const g = new Map([[76, [76]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc77", () => { const g = new Map([[77, [77]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc78", () => { const g = new Map([[78, [78]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc79", () => { const g = new Map([[79, [79]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc80", () => { const g = new Map([[80, [80]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc81", () => { const g = new Map([[81, [81]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc82", () => { const g = new Map([[82, [82]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc83", () => { const g = new Map([[83, [83]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc84", () => { const g = new Map([[84, [84]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc85", () => { const g = new Map([[85, [85]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc86", () => { const g = new Map([[86, [86]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc87", () => { const g = new Map([[87, [87]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc88", () => { const g = new Map([[88, [88]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc89", () => { const g = new Map([[89, [89]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc90", () => { const g = new Map([[90, [90]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc91", () => { const g = new Map([[91, [91]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc92", () => { const g = new Map([[92, [92]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc93", () => { const g = new Map([[93, [93]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc94", () => { const g = new Map([[94, [94]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc95", () => { const g = new Map([[95, [95]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc96", () => { const g = new Map([[96, [96]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc97", () => { const g = new Map([[97, [97]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc98", () => { const g = new Map([[98, [98]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc99", () => { const g = new Map([[99, [99]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
  it("sc100", () => { const g = new Map([[100, [100]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBeGreaterThan(0); });
});

describe("dijkstra unreachable node", () => {
  it("dun1", () => { const g = new Map([[0, []], [10, []]]); const d = dijkstra(g, 0); expect(d.get(10)).toBe(Infinity); });
  it("dun2", () => { const g = new Map([[0, []], [20, []]]); const d = dijkstra(g, 0); expect(d.get(20)).toBe(Infinity); });
  it("dun3", () => { const g = new Map([[0, []], [30, []]]); const d = dijkstra(g, 0); expect(d.get(30)).toBe(Infinity); });
  it("dun4", () => { const g = new Map([[0, []], [40, []]]); const d = dijkstra(g, 0); expect(d.get(40)).toBe(Infinity); });
  it("dun5", () => { const g = new Map([[0, []], [50, []]]); const d = dijkstra(g, 0); expect(d.get(50)).toBe(Infinity); });
  it("dun6", () => { const g = new Map([[0, []], [60, []]]); const d = dijkstra(g, 0); expect(d.get(60)).toBe(Infinity); });
  it("dun7", () => { const g = new Map([[0, []], [70, []]]); const d = dijkstra(g, 0); expect(d.get(70)).toBe(Infinity); });
  it("dun8", () => { const g = new Map([[0, []], [80, []]]); const d = dijkstra(g, 0); expect(d.get(80)).toBe(Infinity); });
  it("dun9", () => { const g = new Map([[0, []], [90, []]]); const d = dijkstra(g, 0); expect(d.get(90)).toBe(Infinity); });
  it("dun10", () => { const g = new Map([[0, []], [100, []]]); const d = dijkstra(g, 0); expect(d.get(100)).toBe(Infinity); });
  it("dun11", () => { const g = new Map([[0, []], [110, []]]); const d = dijkstra(g, 0); expect(d.get(110)).toBe(Infinity); });
  it("dun12", () => { const g = new Map([[0, []], [120, []]]); const d = dijkstra(g, 0); expect(d.get(120)).toBe(Infinity); });
  it("dun13", () => { const g = new Map([[0, []], [130, []]]); const d = dijkstra(g, 0); expect(d.get(130)).toBe(Infinity); });
  it("dun14", () => { const g = new Map([[0, []], [140, []]]); const d = dijkstra(g, 0); expect(d.get(140)).toBe(Infinity); });
  it("dun15", () => { const g = new Map([[0, []], [150, []]]); const d = dijkstra(g, 0); expect(d.get(150)).toBe(Infinity); });
  it("dun16", () => { const g = new Map([[0, []], [160, []]]); const d = dijkstra(g, 0); expect(d.get(160)).toBe(Infinity); });
  it("dun17", () => { const g = new Map([[0, []], [170, []]]); const d = dijkstra(g, 0); expect(d.get(170)).toBe(Infinity); });
  it("dun18", () => { const g = new Map([[0, []], [180, []]]); const d = dijkstra(g, 0); expect(d.get(180)).toBe(Infinity); });
  it("dun19", () => { const g = new Map([[0, []], [190, []]]); const d = dijkstra(g, 0); expect(d.get(190)).toBe(Infinity); });
  it("dun20", () => { const g = new Map([[0, []], [200, []]]); const d = dijkstra(g, 0); expect(d.get(200)).toBe(Infinity); });
  it("dun21", () => { const g = new Map([[0, []], [210, []]]); const d = dijkstra(g, 0); expect(d.get(210)).toBe(Infinity); });
  it("dun22", () => { const g = new Map([[0, []], [220, []]]); const d = dijkstra(g, 0); expect(d.get(220)).toBe(Infinity); });
  it("dun23", () => { const g = new Map([[0, []], [230, []]]); const d = dijkstra(g, 0); expect(d.get(230)).toBe(Infinity); });
  it("dun24", () => { const g = new Map([[0, []], [240, []]]); const d = dijkstra(g, 0); expect(d.get(240)).toBe(Infinity); });
  it("dun25", () => { const g = new Map([[0, []], [250, []]]); const d = dijkstra(g, 0); expect(d.get(250)).toBe(Infinity); });
  it("dun26", () => { const g = new Map([[0, []], [260, []]]); const d = dijkstra(g, 0); expect(d.get(260)).toBe(Infinity); });
  it("dun27", () => { const g = new Map([[0, []], [270, []]]); const d = dijkstra(g, 0); expect(d.get(270)).toBe(Infinity); });
  it("dun28", () => { const g = new Map([[0, []], [280, []]]); const d = dijkstra(g, 0); expect(d.get(280)).toBe(Infinity); });
  it("dun29", () => { const g = new Map([[0, []], [290, []]]); const d = dijkstra(g, 0); expect(d.get(290)).toBe(Infinity); });
  it("dun30", () => { const g = new Map([[0, []], [300, []]]); const d = dijkstra(g, 0); expect(d.get(300)).toBe(Infinity); });
  it("dun31", () => { const g = new Map([[0, []], [310, []]]); const d = dijkstra(g, 0); expect(d.get(310)).toBe(Infinity); });
  it("dun32", () => { const g = new Map([[0, []], [320, []]]); const d = dijkstra(g, 0); expect(d.get(320)).toBe(Infinity); });
  it("dun33", () => { const g = new Map([[0, []], [330, []]]); const d = dijkstra(g, 0); expect(d.get(330)).toBe(Infinity); });
  it("dun34", () => { const g = new Map([[0, []], [340, []]]); const d = dijkstra(g, 0); expect(d.get(340)).toBe(Infinity); });
  it("dun35", () => { const g = new Map([[0, []], [350, []]]); const d = dijkstra(g, 0); expect(d.get(350)).toBe(Infinity); });
  it("dun36", () => { const g = new Map([[0, []], [360, []]]); const d = dijkstra(g, 0); expect(d.get(360)).toBe(Infinity); });
  it("dun37", () => { const g = new Map([[0, []], [370, []]]); const d = dijkstra(g, 0); expect(d.get(370)).toBe(Infinity); });
  it("dun38", () => { const g = new Map([[0, []], [380, []]]); const d = dijkstra(g, 0); expect(d.get(380)).toBe(Infinity); });
  it("dun39", () => { const g = new Map([[0, []], [390, []]]); const d = dijkstra(g, 0); expect(d.get(390)).toBe(Infinity); });
  it("dun40", () => { const g = new Map([[0, []], [400, []]]); const d = dijkstra(g, 0); expect(d.get(400)).toBe(Infinity); });
  it("dun41", () => { const g = new Map([[0, []], [410, []]]); const d = dijkstra(g, 0); expect(d.get(410)).toBe(Infinity); });
  it("dun42", () => { const g = new Map([[0, []], [420, []]]); const d = dijkstra(g, 0); expect(d.get(420)).toBe(Infinity); });
  it("dun43", () => { const g = new Map([[0, []], [430, []]]); const d = dijkstra(g, 0); expect(d.get(430)).toBe(Infinity); });
  it("dun44", () => { const g = new Map([[0, []], [440, []]]); const d = dijkstra(g, 0); expect(d.get(440)).toBe(Infinity); });
  it("dun45", () => { const g = new Map([[0, []], [450, []]]); const d = dijkstra(g, 0); expect(d.get(450)).toBe(Infinity); });
  it("dun46", () => { const g = new Map([[0, []], [460, []]]); const d = dijkstra(g, 0); expect(d.get(460)).toBe(Infinity); });
  it("dun47", () => { const g = new Map([[0, []], [470, []]]); const d = dijkstra(g, 0); expect(d.get(470)).toBe(Infinity); });
  it("dun48", () => { const g = new Map([[0, []], [480, []]]); const d = dijkstra(g, 0); expect(d.get(480)).toBe(Infinity); });
  it("dun49", () => { const g = new Map([[0, []], [490, []]]); const d = dijkstra(g, 0); expect(d.get(490)).toBe(Infinity); });
  it("dun50", () => { const g = new Map([[0, []], [500, []]]); const d = dijkstra(g, 0); expect(d.get(500)).toBe(Infinity); });
  it("dun51", () => { const g = new Map([[0, []], [510, []]]); const d = dijkstra(g, 0); expect(d.get(510)).toBe(Infinity); });
  it("dun52", () => { const g = new Map([[0, []], [520, []]]); const d = dijkstra(g, 0); expect(d.get(520)).toBe(Infinity); });
  it("dun53", () => { const g = new Map([[0, []], [530, []]]); const d = dijkstra(g, 0); expect(d.get(530)).toBe(Infinity); });
  it("dun54", () => { const g = new Map([[0, []], [540, []]]); const d = dijkstra(g, 0); expect(d.get(540)).toBe(Infinity); });
  it("dun55", () => { const g = new Map([[0, []], [550, []]]); const d = dijkstra(g, 0); expect(d.get(550)).toBe(Infinity); });
  it("dun56", () => { const g = new Map([[0, []], [560, []]]); const d = dijkstra(g, 0); expect(d.get(560)).toBe(Infinity); });
  it("dun57", () => { const g = new Map([[0, []], [570, []]]); const d = dijkstra(g, 0); expect(d.get(570)).toBe(Infinity); });
  it("dun58", () => { const g = new Map([[0, []], [580, []]]); const d = dijkstra(g, 0); expect(d.get(580)).toBe(Infinity); });
  it("dun59", () => { const g = new Map([[0, []], [590, []]]); const d = dijkstra(g, 0); expect(d.get(590)).toBe(Infinity); });
  it("dun60", () => { const g = new Map([[0, []], [600, []]]); const d = dijkstra(g, 0); expect(d.get(600)).toBe(Infinity); });
  it("dun61", () => { const g = new Map([[0, []], [610, []]]); const d = dijkstra(g, 0); expect(d.get(610)).toBe(Infinity); });
  it("dun62", () => { const g = new Map([[0, []], [620, []]]); const d = dijkstra(g, 0); expect(d.get(620)).toBe(Infinity); });
  it("dun63", () => { const g = new Map([[0, []], [630, []]]); const d = dijkstra(g, 0); expect(d.get(630)).toBe(Infinity); });
  it("dun64", () => { const g = new Map([[0, []], [640, []]]); const d = dijkstra(g, 0); expect(d.get(640)).toBe(Infinity); });
  it("dun65", () => { const g = new Map([[0, []], [650, []]]); const d = dijkstra(g, 0); expect(d.get(650)).toBe(Infinity); });
  it("dun66", () => { const g = new Map([[0, []], [660, []]]); const d = dijkstra(g, 0); expect(d.get(660)).toBe(Infinity); });
  it("dun67", () => { const g = new Map([[0, []], [670, []]]); const d = dijkstra(g, 0); expect(d.get(670)).toBe(Infinity); });
  it("dun68", () => { const g = new Map([[0, []], [680, []]]); const d = dijkstra(g, 0); expect(d.get(680)).toBe(Infinity); });
  it("dun69", () => { const g = new Map([[0, []], [690, []]]); const d = dijkstra(g, 0); expect(d.get(690)).toBe(Infinity); });
  it("dun70", () => { const g = new Map([[0, []], [700, []]]); const d = dijkstra(g, 0); expect(d.get(700)).toBe(Infinity); });
  it("dun71", () => { const g = new Map([[0, []], [710, []]]); const d = dijkstra(g, 0); expect(d.get(710)).toBe(Infinity); });
  it("dun72", () => { const g = new Map([[0, []], [720, []]]); const d = dijkstra(g, 0); expect(d.get(720)).toBe(Infinity); });
  it("dun73", () => { const g = new Map([[0, []], [730, []]]); const d = dijkstra(g, 0); expect(d.get(730)).toBe(Infinity); });
  it("dun74", () => { const g = new Map([[0, []], [740, []]]); const d = dijkstra(g, 0); expect(d.get(740)).toBe(Infinity); });
  it("dun75", () => { const g = new Map([[0, []], [750, []]]); const d = dijkstra(g, 0); expect(d.get(750)).toBe(Infinity); });
  it("dun76", () => { const g = new Map([[0, []], [760, []]]); const d = dijkstra(g, 0); expect(d.get(760)).toBe(Infinity); });
  it("dun77", () => { const g = new Map([[0, []], [770, []]]); const d = dijkstra(g, 0); expect(d.get(770)).toBe(Infinity); });
  it("dun78", () => { const g = new Map([[0, []], [780, []]]); const d = dijkstra(g, 0); expect(d.get(780)).toBe(Infinity); });
  it("dun79", () => { const g = new Map([[0, []], [790, []]]); const d = dijkstra(g, 0); expect(d.get(790)).toBe(Infinity); });
  it("dun80", () => { const g = new Map([[0, []], [800, []]]); const d = dijkstra(g, 0); expect(d.get(800)).toBe(Infinity); });
  it("dun81", () => { const g = new Map([[0, []], [810, []]]); const d = dijkstra(g, 0); expect(d.get(810)).toBe(Infinity); });
  it("dun82", () => { const g = new Map([[0, []], [820, []]]); const d = dijkstra(g, 0); expect(d.get(820)).toBe(Infinity); });
  it("dun83", () => { const g = new Map([[0, []], [830, []]]); const d = dijkstra(g, 0); expect(d.get(830)).toBe(Infinity); });
  it("dun84", () => { const g = new Map([[0, []], [840, []]]); const d = dijkstra(g, 0); expect(d.get(840)).toBe(Infinity); });
  it("dun85", () => { const g = new Map([[0, []], [850, []]]); const d = dijkstra(g, 0); expect(d.get(850)).toBe(Infinity); });
  it("dun86", () => { const g = new Map([[0, []], [860, []]]); const d = dijkstra(g, 0); expect(d.get(860)).toBe(Infinity); });
  it("dun87", () => { const g = new Map([[0, []], [870, []]]); const d = dijkstra(g, 0); expect(d.get(870)).toBe(Infinity); });
  it("dun88", () => { const g = new Map([[0, []], [880, []]]); const d = dijkstra(g, 0); expect(d.get(880)).toBe(Infinity); });
  it("dun89", () => { const g = new Map([[0, []], [890, []]]); const d = dijkstra(g, 0); expect(d.get(890)).toBe(Infinity); });
  it("dun90", () => { const g = new Map([[0, []], [900, []]]); const d = dijkstra(g, 0); expect(d.get(900)).toBe(Infinity); });
  it("dun91", () => { const g = new Map([[0, []], [910, []]]); const d = dijkstra(g, 0); expect(d.get(910)).toBe(Infinity); });
  it("dun92", () => { const g = new Map([[0, []], [920, []]]); const d = dijkstra(g, 0); expect(d.get(920)).toBe(Infinity); });
  it("dun93", () => { const g = new Map([[0, []], [930, []]]); const d = dijkstra(g, 0); expect(d.get(930)).toBe(Infinity); });
  it("dun94", () => { const g = new Map([[0, []], [940, []]]); const d = dijkstra(g, 0); expect(d.get(940)).toBe(Infinity); });
  it("dun95", () => { const g = new Map([[0, []], [950, []]]); const d = dijkstra(g, 0); expect(d.get(950)).toBe(Infinity); });
  it("dun96", () => { const g = new Map([[0, []], [960, []]]); const d = dijkstra(g, 0); expect(d.get(960)).toBe(Infinity); });
  it("dun97", () => { const g = new Map([[0, []], [970, []]]); const d = dijkstra(g, 0); expect(d.get(970)).toBe(Infinity); });
  it("dun98", () => { const g = new Map([[0, []], [980, []]]); const d = dijkstra(g, 0); expect(d.get(980)).toBe(Infinity); });
  it("dun99", () => { const g = new Map([[0, []], [990, []]]); const d = dijkstra(g, 0); expect(d.get(990)).toBe(Infinity); });
  it("dun100", () => { const g = new Map([[0, []], [1000, []]]); const d = dijkstra(g, 0); expect(d.get(1000)).toBe(Infinity); });
});

describe("bellmanFord negative cycle", () => {
  it("bfnc1", () => { const d = bellmanFord([[0, 1, -1], [1, 0, -1]], 2, 0); expect(d).toBeNull(); });
  it("bfnc2", () => { const d = bellmanFord([[0, 1, -2], [1, 0, -2]], 2, 0); expect(d).toBeNull(); });
  it("bfnc3", () => { const d = bellmanFord([[0, 1, -3], [1, 0, -3]], 2, 0); expect(d).toBeNull(); });
  it("bfnc4", () => { const d = bellmanFord([[0, 1, -4], [1, 0, -4]], 2, 0); expect(d).toBeNull(); });
  it("bfnc5", () => { const d = bellmanFord([[0, 1, -5], [1, 0, -5]], 2, 0); expect(d).toBeNull(); });
  it("bfnc6", () => { const d = bellmanFord([[0, 1, -6], [1, 0, -6]], 2, 0); expect(d).toBeNull(); });
  it("bfnc7", () => { const d = bellmanFord([[0, 1, -7], [1, 0, -7]], 2, 0); expect(d).toBeNull(); });
  it("bfnc8", () => { const d = bellmanFord([[0, 1, -8], [1, 0, -8]], 2, 0); expect(d).toBeNull(); });
  it("bfnc9", () => { const d = bellmanFord([[0, 1, -9], [1, 0, -9]], 2, 0); expect(d).toBeNull(); });
  it("bfnc10", () => { const d = bellmanFord([[0, 1, -10], [1, 0, -10]], 2, 0); expect(d).toBeNull(); });
  it("bfnc11", () => { const d = bellmanFord([[0, 1, -11], [1, 0, -11]], 2, 0); expect(d).toBeNull(); });
  it("bfnc12", () => { const d = bellmanFord([[0, 1, -12], [1, 0, -12]], 2, 0); expect(d).toBeNull(); });
  it("bfnc13", () => { const d = bellmanFord([[0, 1, -13], [1, 0, -13]], 2, 0); expect(d).toBeNull(); });
  it("bfnc14", () => { const d = bellmanFord([[0, 1, -14], [1, 0, -14]], 2, 0); expect(d).toBeNull(); });
  it("bfnc15", () => { const d = bellmanFord([[0, 1, -15], [1, 0, -15]], 2, 0); expect(d).toBeNull(); });
  it("bfnc16", () => { const d = bellmanFord([[0, 1, -16], [1, 0, -16]], 2, 0); expect(d).toBeNull(); });
  it("bfnc17", () => { const d = bellmanFord([[0, 1, -17], [1, 0, -17]], 2, 0); expect(d).toBeNull(); });
  it("bfnc18", () => { const d = bellmanFord([[0, 1, -18], [1, 0, -18]], 2, 0); expect(d).toBeNull(); });
  it("bfnc19", () => { const d = bellmanFord([[0, 1, -19], [1, 0, -19]], 2, 0); expect(d).toBeNull(); });
  it("bfnc20", () => { const d = bellmanFord([[0, 1, -20], [1, 0, -20]], 2, 0); expect(d).toBeNull(); });
  it("bfnc21", () => { const d = bellmanFord([[0, 1, -21], [1, 0, -21]], 2, 0); expect(d).toBeNull(); });
  it("bfnc22", () => { const d = bellmanFord([[0, 1, -22], [1, 0, -22]], 2, 0); expect(d).toBeNull(); });
  it("bfnc23", () => { const d = bellmanFord([[0, 1, -23], [1, 0, -23]], 2, 0); expect(d).toBeNull(); });
  it("bfnc24", () => { const d = bellmanFord([[0, 1, -24], [1, 0, -24]], 2, 0); expect(d).toBeNull(); });
  it("bfnc25", () => { const d = bellmanFord([[0, 1, -25], [1, 0, -25]], 2, 0); expect(d).toBeNull(); });
  it("bfnc26", () => { const d = bellmanFord([[0, 1, -26], [1, 0, -26]], 2, 0); expect(d).toBeNull(); });
  it("bfnc27", () => { const d = bellmanFord([[0, 1, -27], [1, 0, -27]], 2, 0); expect(d).toBeNull(); });
  it("bfnc28", () => { const d = bellmanFord([[0, 1, -28], [1, 0, -28]], 2, 0); expect(d).toBeNull(); });
  it("bfnc29", () => { const d = bellmanFord([[0, 1, -29], [1, 0, -29]], 2, 0); expect(d).toBeNull(); });
  it("bfnc30", () => { const d = bellmanFord([[0, 1, -30], [1, 0, -30]], 2, 0); expect(d).toBeNull(); });
  it("bfnc31", () => { const d = bellmanFord([[0, 1, -31], [1, 0, -31]], 2, 0); expect(d).toBeNull(); });
  it("bfnc32", () => { const d = bellmanFord([[0, 1, -32], [1, 0, -32]], 2, 0); expect(d).toBeNull(); });
  it("bfnc33", () => { const d = bellmanFord([[0, 1, -33], [1, 0, -33]], 2, 0); expect(d).toBeNull(); });
  it("bfnc34", () => { const d = bellmanFord([[0, 1, -34], [1, 0, -34]], 2, 0); expect(d).toBeNull(); });
  it("bfnc35", () => { const d = bellmanFord([[0, 1, -35], [1, 0, -35]], 2, 0); expect(d).toBeNull(); });
  it("bfnc36", () => { const d = bellmanFord([[0, 1, -36], [1, 0, -36]], 2, 0); expect(d).toBeNull(); });
  it("bfnc37", () => { const d = bellmanFord([[0, 1, -37], [1, 0, -37]], 2, 0); expect(d).toBeNull(); });
  it("bfnc38", () => { const d = bellmanFord([[0, 1, -38], [1, 0, -38]], 2, 0); expect(d).toBeNull(); });
  it("bfnc39", () => { const d = bellmanFord([[0, 1, -39], [1, 0, -39]], 2, 0); expect(d).toBeNull(); });
  it("bfnc40", () => { const d = bellmanFord([[0, 1, -40], [1, 0, -40]], 2, 0); expect(d).toBeNull(); });
  it("bfnc41", () => { const d = bellmanFord([[0, 1, -41], [1, 0, -41]], 2, 0); expect(d).toBeNull(); });
  it("bfnc42", () => { const d = bellmanFord([[0, 1, -42], [1, 0, -42]], 2, 0); expect(d).toBeNull(); });
  it("bfnc43", () => { const d = bellmanFord([[0, 1, -43], [1, 0, -43]], 2, 0); expect(d).toBeNull(); });
  it("bfnc44", () => { const d = bellmanFord([[0, 1, -44], [1, 0, -44]], 2, 0); expect(d).toBeNull(); });
  it("bfnc45", () => { const d = bellmanFord([[0, 1, -45], [1, 0, -45]], 2, 0); expect(d).toBeNull(); });
  it("bfnc46", () => { const d = bellmanFord([[0, 1, -46], [1, 0, -46]], 2, 0); expect(d).toBeNull(); });
  it("bfnc47", () => { const d = bellmanFord([[0, 1, -47], [1, 0, -47]], 2, 0); expect(d).toBeNull(); });
  it("bfnc48", () => { const d = bellmanFord([[0, 1, -48], [1, 0, -48]], 2, 0); expect(d).toBeNull(); });
  it("bfnc49", () => { const d = bellmanFord([[0, 1, -49], [1, 0, -49]], 2, 0); expect(d).toBeNull(); });
  it("bfnc50", () => { const d = bellmanFord([[0, 1, -50], [1, 0, -50]], 2, 0); expect(d).toBeNull(); });
  it("bfnc51", () => { const d = bellmanFord([[0, 1, -51], [1, 0, -51]], 2, 0); expect(d).toBeNull(); });
  it("bfnc52", () => { const d = bellmanFord([[0, 1, -52], [1, 0, -52]], 2, 0); expect(d).toBeNull(); });
  it("bfnc53", () => { const d = bellmanFord([[0, 1, -53], [1, 0, -53]], 2, 0); expect(d).toBeNull(); });
  it("bfnc54", () => { const d = bellmanFord([[0, 1, -54], [1, 0, -54]], 2, 0); expect(d).toBeNull(); });
  it("bfnc55", () => { const d = bellmanFord([[0, 1, -55], [1, 0, -55]], 2, 0); expect(d).toBeNull(); });
  it("bfnc56", () => { const d = bellmanFord([[0, 1, -56], [1, 0, -56]], 2, 0); expect(d).toBeNull(); });
  it("bfnc57", () => { const d = bellmanFord([[0, 1, -57], [1, 0, -57]], 2, 0); expect(d).toBeNull(); });
  it("bfnc58", () => { const d = bellmanFord([[0, 1, -58], [1, 0, -58]], 2, 0); expect(d).toBeNull(); });
  it("bfnc59", () => { const d = bellmanFord([[0, 1, -59], [1, 0, -59]], 2, 0); expect(d).toBeNull(); });
  it("bfnc60", () => { const d = bellmanFord([[0, 1, -60], [1, 0, -60]], 2, 0); expect(d).toBeNull(); });
  it("bfnc61", () => { const d = bellmanFord([[0, 1, -61], [1, 0, -61]], 2, 0); expect(d).toBeNull(); });
  it("bfnc62", () => { const d = bellmanFord([[0, 1, -62], [1, 0, -62]], 2, 0); expect(d).toBeNull(); });
  it("bfnc63", () => { const d = bellmanFord([[0, 1, -63], [1, 0, -63]], 2, 0); expect(d).toBeNull(); });
  it("bfnc64", () => { const d = bellmanFord([[0, 1, -64], [1, 0, -64]], 2, 0); expect(d).toBeNull(); });
  it("bfnc65", () => { const d = bellmanFord([[0, 1, -65], [1, 0, -65]], 2, 0); expect(d).toBeNull(); });
  it("bfnc66", () => { const d = bellmanFord([[0, 1, -66], [1, 0, -66]], 2, 0); expect(d).toBeNull(); });
  it("bfnc67", () => { const d = bellmanFord([[0, 1, -67], [1, 0, -67]], 2, 0); expect(d).toBeNull(); });
  it("bfnc68", () => { const d = bellmanFord([[0, 1, -68], [1, 0, -68]], 2, 0); expect(d).toBeNull(); });
  it("bfnc69", () => { const d = bellmanFord([[0, 1, -69], [1, 0, -69]], 2, 0); expect(d).toBeNull(); });
  it("bfnc70", () => { const d = bellmanFord([[0, 1, -70], [1, 0, -70]], 2, 0); expect(d).toBeNull(); });
  it("bfnc71", () => { const d = bellmanFord([[0, 1, -71], [1, 0, -71]], 2, 0); expect(d).toBeNull(); });
  it("bfnc72", () => { const d = bellmanFord([[0, 1, -72], [1, 0, -72]], 2, 0); expect(d).toBeNull(); });
  it("bfnc73", () => { const d = bellmanFord([[0, 1, -73], [1, 0, -73]], 2, 0); expect(d).toBeNull(); });
  it("bfnc74", () => { const d = bellmanFord([[0, 1, -74], [1, 0, -74]], 2, 0); expect(d).toBeNull(); });
  it("bfnc75", () => { const d = bellmanFord([[0, 1, -75], [1, 0, -75]], 2, 0); expect(d).toBeNull(); });
  it("bfnc76", () => { const d = bellmanFord([[0, 1, -76], [1, 0, -76]], 2, 0); expect(d).toBeNull(); });
  it("bfnc77", () => { const d = bellmanFord([[0, 1, -77], [1, 0, -77]], 2, 0); expect(d).toBeNull(); });
  it("bfnc78", () => { const d = bellmanFord([[0, 1, -78], [1, 0, -78]], 2, 0); expect(d).toBeNull(); });
  it("bfnc79", () => { const d = bellmanFord([[0, 1, -79], [1, 0, -79]], 2, 0); expect(d).toBeNull(); });
  it("bfnc80", () => { const d = bellmanFord([[0, 1, -80], [1, 0, -80]], 2, 0); expect(d).toBeNull(); });
  it("bfnc81", () => { const d = bellmanFord([[0, 1, -81], [1, 0, -81]], 2, 0); expect(d).toBeNull(); });
  it("bfnc82", () => { const d = bellmanFord([[0, 1, -82], [1, 0, -82]], 2, 0); expect(d).toBeNull(); });
  it("bfnc83", () => { const d = bellmanFord([[0, 1, -83], [1, 0, -83]], 2, 0); expect(d).toBeNull(); });
  it("bfnc84", () => { const d = bellmanFord([[0, 1, -84], [1, 0, -84]], 2, 0); expect(d).toBeNull(); });
  it("bfnc85", () => { const d = bellmanFord([[0, 1, -85], [1, 0, -85]], 2, 0); expect(d).toBeNull(); });
  it("bfnc86", () => { const d = bellmanFord([[0, 1, -86], [1, 0, -86]], 2, 0); expect(d).toBeNull(); });
  it("bfnc87", () => { const d = bellmanFord([[0, 1, -87], [1, 0, -87]], 2, 0); expect(d).toBeNull(); });
  it("bfnc88", () => { const d = bellmanFord([[0, 1, -88], [1, 0, -88]], 2, 0); expect(d).toBeNull(); });
  it("bfnc89", () => { const d = bellmanFord([[0, 1, -89], [1, 0, -89]], 2, 0); expect(d).toBeNull(); });
  it("bfnc90", () => { const d = bellmanFord([[0, 1, -90], [1, 0, -90]], 2, 0); expect(d).toBeNull(); });
  it("bfnc91", () => { const d = bellmanFord([[0, 1, -91], [1, 0, -91]], 2, 0); expect(d).toBeNull(); });
  it("bfnc92", () => { const d = bellmanFord([[0, 1, -92], [1, 0, -92]], 2, 0); expect(d).toBeNull(); });
  it("bfnc93", () => { const d = bellmanFord([[0, 1, -93], [1, 0, -93]], 2, 0); expect(d).toBeNull(); });
  it("bfnc94", () => { const d = bellmanFord([[0, 1, -94], [1, 0, -94]], 2, 0); expect(d).toBeNull(); });
  it("bfnc95", () => { const d = bellmanFord([[0, 1, -95], [1, 0, -95]], 2, 0); expect(d).toBeNull(); });
  it("bfnc96", () => { const d = bellmanFord([[0, 1, -96], [1, 0, -96]], 2, 0); expect(d).toBeNull(); });
  it("bfnc97", () => { const d = bellmanFord([[0, 1, -97], [1, 0, -97]], 2, 0); expect(d).toBeNull(); });
  it("bfnc98", () => { const d = bellmanFord([[0, 1, -98], [1, 0, -98]], 2, 0); expect(d).toBeNull(); });
  it("bfnc99", () => { const d = bellmanFord([[0, 1, -99], [1, 0, -99]], 2, 0); expect(d).toBeNull(); });
  it("bfnc100", () => { const d = bellmanFord([[0, 1, -100], [1, 0, -100]], 2, 0); expect(d).toBeNull(); });
});

describe("floydWarshall two node", () => {
  it("fw2n1", () => { const m = [[0, 1], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(1); });
  it("fw2n2", () => { const m = [[0, 2], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(2); });
  it("fw2n3", () => { const m = [[0, 3], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(3); });
  it("fw2n4", () => { const m = [[0, 4], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(4); });
  it("fw2n5", () => { const m = [[0, 5], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(5); });
  it("fw2n6", () => { const m = [[0, 6], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(6); });
  it("fw2n7", () => { const m = [[0, 7], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(7); });
  it("fw2n8", () => { const m = [[0, 8], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(8); });
  it("fw2n9", () => { const m = [[0, 9], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(9); });
  it("fw2n10", () => { const m = [[0, 10], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(10); });
  it("fw2n11", () => { const m = [[0, 11], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(11); });
  it("fw2n12", () => { const m = [[0, 12], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(12); });
  it("fw2n13", () => { const m = [[0, 13], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(13); });
  it("fw2n14", () => { const m = [[0, 14], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(14); });
  it("fw2n15", () => { const m = [[0, 15], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(15); });
  it("fw2n16", () => { const m = [[0, 16], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(16); });
  it("fw2n17", () => { const m = [[0, 17], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(17); });
  it("fw2n18", () => { const m = [[0, 18], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(18); });
  it("fw2n19", () => { const m = [[0, 19], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(19); });
  it("fw2n20", () => { const m = [[0, 20], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(20); });
  it("fw2n21", () => { const m = [[0, 21], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(21); });
  it("fw2n22", () => { const m = [[0, 22], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(22); });
  it("fw2n23", () => { const m = [[0, 23], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(23); });
  it("fw2n24", () => { const m = [[0, 24], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(24); });
  it("fw2n25", () => { const m = [[0, 25], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(25); });
  it("fw2n26", () => { const m = [[0, 26], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(26); });
  it("fw2n27", () => { const m = [[0, 27], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(27); });
  it("fw2n28", () => { const m = [[0, 28], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(28); });
  it("fw2n29", () => { const m = [[0, 29], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(29); });
  it("fw2n30", () => { const m = [[0, 30], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(30); });
  it("fw2n31", () => { const m = [[0, 31], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(31); });
  it("fw2n32", () => { const m = [[0, 32], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(32); });
  it("fw2n33", () => { const m = [[0, 33], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(33); });
  it("fw2n34", () => { const m = [[0, 34], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(34); });
  it("fw2n35", () => { const m = [[0, 35], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(35); });
  it("fw2n36", () => { const m = [[0, 36], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(36); });
  it("fw2n37", () => { const m = [[0, 37], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(37); });
  it("fw2n38", () => { const m = [[0, 38], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(38); });
  it("fw2n39", () => { const m = [[0, 39], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(39); });
  it("fw2n40", () => { const m = [[0, 40], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(40); });
  it("fw2n41", () => { const m = [[0, 41], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(41); });
  it("fw2n42", () => { const m = [[0, 42], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(42); });
  it("fw2n43", () => { const m = [[0, 43], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(43); });
  it("fw2n44", () => { const m = [[0, 44], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(44); });
  it("fw2n45", () => { const m = [[0, 45], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(45); });
  it("fw2n46", () => { const m = [[0, 46], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(46); });
  it("fw2n47", () => { const m = [[0, 47], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(47); });
  it("fw2n48", () => { const m = [[0, 48], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(48); });
  it("fw2n49", () => { const m = [[0, 49], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(49); });
  it("fw2n50", () => { const m = [[0, 50], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(50); });
  it("fw2n51", () => { const m = [[0, 51], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(51); });
  it("fw2n52", () => { const m = [[0, 52], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(52); });
  it("fw2n53", () => { const m = [[0, 53], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(53); });
  it("fw2n54", () => { const m = [[0, 54], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(54); });
  it("fw2n55", () => { const m = [[0, 55], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(55); });
  it("fw2n56", () => { const m = [[0, 56], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(56); });
  it("fw2n57", () => { const m = [[0, 57], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(57); });
  it("fw2n58", () => { const m = [[0, 58], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(58); });
  it("fw2n59", () => { const m = [[0, 59], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(59); });
  it("fw2n60", () => { const m = [[0, 60], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(60); });
  it("fw2n61", () => { const m = [[0, 61], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(61); });
  it("fw2n62", () => { const m = [[0, 62], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(62); });
  it("fw2n63", () => { const m = [[0, 63], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(63); });
  it("fw2n64", () => { const m = [[0, 64], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(64); });
  it("fw2n65", () => { const m = [[0, 65], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(65); });
  it("fw2n66", () => { const m = [[0, 66], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(66); });
  it("fw2n67", () => { const m = [[0, 67], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(67); });
  it("fw2n68", () => { const m = [[0, 68], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(68); });
  it("fw2n69", () => { const m = [[0, 69], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(69); });
  it("fw2n70", () => { const m = [[0, 70], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(70); });
  it("fw2n71", () => { const m = [[0, 71], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(71); });
  it("fw2n72", () => { const m = [[0, 72], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(72); });
  it("fw2n73", () => { const m = [[0, 73], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(73); });
  it("fw2n74", () => { const m = [[0, 74], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(74); });
  it("fw2n75", () => { const m = [[0, 75], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(75); });
  it("fw2n76", () => { const m = [[0, 76], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(76); });
  it("fw2n77", () => { const m = [[0, 77], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(77); });
  it("fw2n78", () => { const m = [[0, 78], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(78); });
  it("fw2n79", () => { const m = [[0, 79], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(79); });
  it("fw2n80", () => { const m = [[0, 80], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(80); });
  it("fw2n81", () => { const m = [[0, 81], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(81); });
  it("fw2n82", () => { const m = [[0, 82], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(82); });
  it("fw2n83", () => { const m = [[0, 83], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(83); });
  it("fw2n84", () => { const m = [[0, 84], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(84); });
  it("fw2n85", () => { const m = [[0, 85], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(85); });
  it("fw2n86", () => { const m = [[0, 86], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(86); });
  it("fw2n87", () => { const m = [[0, 87], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(87); });
  it("fw2n88", () => { const m = [[0, 88], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(88); });
  it("fw2n89", () => { const m = [[0, 89], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(89); });
  it("fw2n90", () => { const m = [[0, 90], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(90); });
  it("fw2n91", () => { const m = [[0, 91], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(91); });
  it("fw2n92", () => { const m = [[0, 92], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(92); });
  it("fw2n93", () => { const m = [[0, 93], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(93); });
  it("fw2n94", () => { const m = [[0, 94], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(94); });
  it("fw2n95", () => { const m = [[0, 95], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(95); });
  it("fw2n96", () => { const m = [[0, 96], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(96); });
  it("fw2n97", () => { const m = [[0, 97], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(97); });
  it("fw2n98", () => { const m = [[0, 98], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(98); });
  it("fw2n99", () => { const m = [[0, 99], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(99); });
  it("fw2n100", () => { const m = [[0, 100], [Infinity, 0]]; const r = floydWarshall(m); expect(r[0][1]).toBe(100); });
});

describe("topologicalSort single node", () => {
  it("tss0", () => { const g = new Map([[0, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss1", () => { const g = new Map([[1, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss2", () => { const g = new Map([[2, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss3", () => { const g = new Map([[3, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss4", () => { const g = new Map([[4, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss5", () => { const g = new Map([[5, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss6", () => { const g = new Map([[6, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss7", () => { const g = new Map([[7, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss8", () => { const g = new Map([[8, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss9", () => { const g = new Map([[9, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss10", () => { const g = new Map([[10, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss11", () => { const g = new Map([[11, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss12", () => { const g = new Map([[12, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss13", () => { const g = new Map([[13, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss14", () => { const g = new Map([[14, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss15", () => { const g = new Map([[15, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss16", () => { const g = new Map([[16, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss17", () => { const g = new Map([[17, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss18", () => { const g = new Map([[18, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss19", () => { const g = new Map([[19, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss20", () => { const g = new Map([[20, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss21", () => { const g = new Map([[21, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss22", () => { const g = new Map([[22, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss23", () => { const g = new Map([[23, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss24", () => { const g = new Map([[24, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss25", () => { const g = new Map([[25, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss26", () => { const g = new Map([[26, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss27", () => { const g = new Map([[27, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss28", () => { const g = new Map([[28, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss29", () => { const g = new Map([[29, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss30", () => { const g = new Map([[30, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss31", () => { const g = new Map([[31, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss32", () => { const g = new Map([[32, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss33", () => { const g = new Map([[33, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss34", () => { const g = new Map([[34, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss35", () => { const g = new Map([[35, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss36", () => { const g = new Map([[36, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss37", () => { const g = new Map([[37, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss38", () => { const g = new Map([[38, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss39", () => { const g = new Map([[39, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss40", () => { const g = new Map([[40, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss41", () => { const g = new Map([[41, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss42", () => { const g = new Map([[42, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss43", () => { const g = new Map([[43, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss44", () => { const g = new Map([[44, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss45", () => { const g = new Map([[45, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss46", () => { const g = new Map([[46, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss47", () => { const g = new Map([[47, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss48", () => { const g = new Map([[48, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss49", () => { const g = new Map([[49, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss50", () => { const g = new Map([[50, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss51", () => { const g = new Map([[51, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss52", () => { const g = new Map([[52, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss53", () => { const g = new Map([[53, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss54", () => { const g = new Map([[54, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss55", () => { const g = new Map([[55, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss56", () => { const g = new Map([[56, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss57", () => { const g = new Map([[57, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss58", () => { const g = new Map([[58, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss59", () => { const g = new Map([[59, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss60", () => { const g = new Map([[60, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss61", () => { const g = new Map([[61, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss62", () => { const g = new Map([[62, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss63", () => { const g = new Map([[63, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss64", () => { const g = new Map([[64, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss65", () => { const g = new Map([[65, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss66", () => { const g = new Map([[66, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss67", () => { const g = new Map([[67, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss68", () => { const g = new Map([[68, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss69", () => { const g = new Map([[69, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss70", () => { const g = new Map([[70, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss71", () => { const g = new Map([[71, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss72", () => { const g = new Map([[72, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss73", () => { const g = new Map([[73, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss74", () => { const g = new Map([[74, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss75", () => { const g = new Map([[75, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss76", () => { const g = new Map([[76, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss77", () => { const g = new Map([[77, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss78", () => { const g = new Map([[78, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss79", () => { const g = new Map([[79, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss80", () => { const g = new Map([[80, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss81", () => { const g = new Map([[81, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss82", () => { const g = new Map([[82, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss83", () => { const g = new Map([[83, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss84", () => { const g = new Map([[84, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss85", () => { const g = new Map([[85, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss86", () => { const g = new Map([[86, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss87", () => { const g = new Map([[87, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss88", () => { const g = new Map([[88, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss89", () => { const g = new Map([[89, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss90", () => { const g = new Map([[90, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss91", () => { const g = new Map([[91, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss92", () => { const g = new Map([[92, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss93", () => { const g = new Map([[93, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss94", () => { const g = new Map([[94, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss95", () => { const g = new Map([[95, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss96", () => { const g = new Map([[96, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss97", () => { const g = new Map([[97, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss98", () => { const g = new Map([[98, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
  it("tss99", () => { const g = new Map([[99, []]]); const r = topologicalSort(g); expect(r).not.toBeNull(); expect(r!.length).toBe(1); });
});

describe("stronglyConnected mutual", () => {
  it("scm1", () => { const g = new Map([[1, [1001]], [1001, [1]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm2", () => { const g = new Map([[2, [1002]], [1002, [2]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm3", () => { const g = new Map([[3, [1003]], [1003, [3]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm4", () => { const g = new Map([[4, [1004]], [1004, [4]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm5", () => { const g = new Map([[5, [1005]], [1005, [5]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm6", () => { const g = new Map([[6, [1006]], [1006, [6]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm7", () => { const g = new Map([[7, [1007]], [1007, [7]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm8", () => { const g = new Map([[8, [1008]], [1008, [8]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm9", () => { const g = new Map([[9, [1009]], [1009, [9]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm10", () => { const g = new Map([[10, [1010]], [1010, [10]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm11", () => { const g = new Map([[11, [1011]], [1011, [11]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm12", () => { const g = new Map([[12, [1012]], [1012, [12]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm13", () => { const g = new Map([[13, [1013]], [1013, [13]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm14", () => { const g = new Map([[14, [1014]], [1014, [14]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm15", () => { const g = new Map([[15, [1015]], [1015, [15]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm16", () => { const g = new Map([[16, [1016]], [1016, [16]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm17", () => { const g = new Map([[17, [1017]], [1017, [17]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm18", () => { const g = new Map([[18, [1018]], [1018, [18]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm19", () => { const g = new Map([[19, [1019]], [1019, [19]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm20", () => { const g = new Map([[20, [1020]], [1020, [20]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm21", () => { const g = new Map([[21, [1021]], [1021, [21]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm22", () => { const g = new Map([[22, [1022]], [1022, [22]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm23", () => { const g = new Map([[23, [1023]], [1023, [23]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm24", () => { const g = new Map([[24, [1024]], [1024, [24]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm25", () => { const g = new Map([[25, [1025]], [1025, [25]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm26", () => { const g = new Map([[26, [1026]], [1026, [26]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm27", () => { const g = new Map([[27, [1027]], [1027, [27]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm28", () => { const g = new Map([[28, [1028]], [1028, [28]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm29", () => { const g = new Map([[29, [1029]], [1029, [29]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm30", () => { const g = new Map([[30, [1030]], [1030, [30]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm31", () => { const g = new Map([[31, [1031]], [1031, [31]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm32", () => { const g = new Map([[32, [1032]], [1032, [32]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm33", () => { const g = new Map([[33, [1033]], [1033, [33]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm34", () => { const g = new Map([[34, [1034]], [1034, [34]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm35", () => { const g = new Map([[35, [1035]], [1035, [35]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm36", () => { const g = new Map([[36, [1036]], [1036, [36]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm37", () => { const g = new Map([[37, [1037]], [1037, [37]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm38", () => { const g = new Map([[38, [1038]], [1038, [38]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm39", () => { const g = new Map([[39, [1039]], [1039, [39]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm40", () => { const g = new Map([[40, [1040]], [1040, [40]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm41", () => { const g = new Map([[41, [1041]], [1041, [41]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm42", () => { const g = new Map([[42, [1042]], [1042, [42]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm43", () => { const g = new Map([[43, [1043]], [1043, [43]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm44", () => { const g = new Map([[44, [1044]], [1044, [44]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm45", () => { const g = new Map([[45, [1045]], [1045, [45]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm46", () => { const g = new Map([[46, [1046]], [1046, [46]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm47", () => { const g = new Map([[47, [1047]], [1047, [47]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm48", () => { const g = new Map([[48, [1048]], [1048, [48]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm49", () => { const g = new Map([[49, [1049]], [1049, [49]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm50", () => { const g = new Map([[50, [1050]], [1050, [50]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm51", () => { const g = new Map([[51, [1051]], [1051, [51]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm52", () => { const g = new Map([[52, [1052]], [1052, [52]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm53", () => { const g = new Map([[53, [1053]], [1053, [53]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm54", () => { const g = new Map([[54, [1054]], [1054, [54]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm55", () => { const g = new Map([[55, [1055]], [1055, [55]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm56", () => { const g = new Map([[56, [1056]], [1056, [56]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm57", () => { const g = new Map([[57, [1057]], [1057, [57]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm58", () => { const g = new Map([[58, [1058]], [1058, [58]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm59", () => { const g = new Map([[59, [1059]], [1059, [59]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm60", () => { const g = new Map([[60, [1060]], [1060, [60]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm61", () => { const g = new Map([[61, [1061]], [1061, [61]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm62", () => { const g = new Map([[62, [1062]], [1062, [62]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm63", () => { const g = new Map([[63, [1063]], [1063, [63]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm64", () => { const g = new Map([[64, [1064]], [1064, [64]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm65", () => { const g = new Map([[65, [1065]], [1065, [65]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm66", () => { const g = new Map([[66, [1066]], [1066, [66]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm67", () => { const g = new Map([[67, [1067]], [1067, [67]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm68", () => { const g = new Map([[68, [1068]], [1068, [68]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm69", () => { const g = new Map([[69, [1069]], [1069, [69]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm70", () => { const g = new Map([[70, [1070]], [1070, [70]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm71", () => { const g = new Map([[71, [1071]], [1071, [71]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm72", () => { const g = new Map([[72, [1072]], [1072, [72]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm73", () => { const g = new Map([[73, [1073]], [1073, [73]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm74", () => { const g = new Map([[74, [1074]], [1074, [74]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm75", () => { const g = new Map([[75, [1075]], [1075, [75]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm76", () => { const g = new Map([[76, [1076]], [1076, [76]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm77", () => { const g = new Map([[77, [1077]], [1077, [77]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm78", () => { const g = new Map([[78, [1078]], [1078, [78]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm79", () => { const g = new Map([[79, [1079]], [1079, [79]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm80", () => { const g = new Map([[80, [1080]], [1080, [80]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm81", () => { const g = new Map([[81, [1081]], [1081, [81]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm82", () => { const g = new Map([[82, [1082]], [1082, [82]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm83", () => { const g = new Map([[83, [1083]], [1083, [83]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm84", () => { const g = new Map([[84, [1084]], [1084, [84]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm85", () => { const g = new Map([[85, [1085]], [1085, [85]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm86", () => { const g = new Map([[86, [1086]], [1086, [86]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm87", () => { const g = new Map([[87, [1087]], [1087, [87]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm88", () => { const g = new Map([[88, [1088]], [1088, [88]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm89", () => { const g = new Map([[89, [1089]], [1089, [89]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm90", () => { const g = new Map([[90, [1090]], [1090, [90]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm91", () => { const g = new Map([[91, [1091]], [1091, [91]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm92", () => { const g = new Map([[92, [1092]], [1092, [92]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm93", () => { const g = new Map([[93, [1093]], [1093, [93]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm94", () => { const g = new Map([[94, [1094]], [1094, [94]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm95", () => { const g = new Map([[95, [1095]], [1095, [95]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm96", () => { const g = new Map([[96, [1096]], [1096, [96]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm97", () => { const g = new Map([[97, [1097]], [1097, [97]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm98", () => { const g = new Map([[98, [1098]], [1098, [98]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm99", () => { const g = new Map([[99, [1099]], [1099, [99]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
  it("scm100", () => { const g = new Map([[100, [1100]], [1100, [100]]]); const sccs = stronglyConnected(g); expect(sccs.length).toBe(1); });
});

describe("dijkstra three node chain", () => {
  it("d3n1", () => { const g = new Map([[0, [[1, 1]]], [1, [[2, 1]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(2); });
  it("d3n2", () => { const g = new Map([[0, [[1, 2]]], [1, [[2, 2]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(4); });
  it("d3n3", () => { const g = new Map([[0, [[1, 3]]], [1, [[2, 3]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(6); });
  it("d3n4", () => { const g = new Map([[0, [[1, 4]]], [1, [[2, 4]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(8); });
  it("d3n5", () => { const g = new Map([[0, [[1, 5]]], [1, [[2, 5]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(10); });
  it("d3n6", () => { const g = new Map([[0, [[1, 6]]], [1, [[2, 6]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(12); });
  it("d3n7", () => { const g = new Map([[0, [[1, 7]]], [1, [[2, 7]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(14); });
  it("d3n8", () => { const g = new Map([[0, [[1, 8]]], [1, [[2, 8]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(16); });
  it("d3n9", () => { const g = new Map([[0, [[1, 9]]], [1, [[2, 9]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(18); });
  it("d3n10", () => { const g = new Map([[0, [[1, 10]]], [1, [[2, 10]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(20); });
  it("d3n11", () => { const g = new Map([[0, [[1, 11]]], [1, [[2, 11]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(22); });
  it("d3n12", () => { const g = new Map([[0, [[1, 12]]], [1, [[2, 12]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(24); });
  it("d3n13", () => { const g = new Map([[0, [[1, 13]]], [1, [[2, 13]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(26); });
  it("d3n14", () => { const g = new Map([[0, [[1, 14]]], [1, [[2, 14]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(28); });
  it("d3n15", () => { const g = new Map([[0, [[1, 15]]], [1, [[2, 15]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(30); });
  it("d3n16", () => { const g = new Map([[0, [[1, 16]]], [1, [[2, 16]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(32); });
  it("d3n17", () => { const g = new Map([[0, [[1, 17]]], [1, [[2, 17]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(34); });
  it("d3n18", () => { const g = new Map([[0, [[1, 18]]], [1, [[2, 18]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(36); });
  it("d3n19", () => { const g = new Map([[0, [[1, 19]]], [1, [[2, 19]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(38); });
  it("d3n20", () => { const g = new Map([[0, [[1, 20]]], [1, [[2, 20]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(40); });
  it("d3n21", () => { const g = new Map([[0, [[1, 21]]], [1, [[2, 21]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(42); });
  it("d3n22", () => { const g = new Map([[0, [[1, 22]]], [1, [[2, 22]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(44); });
  it("d3n23", () => { const g = new Map([[0, [[1, 23]]], [1, [[2, 23]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(46); });
  it("d3n24", () => { const g = new Map([[0, [[1, 24]]], [1, [[2, 24]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(48); });
  it("d3n25", () => { const g = new Map([[0, [[1, 25]]], [1, [[2, 25]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(50); });
  it("d3n26", () => { const g = new Map([[0, [[1, 26]]], [1, [[2, 26]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(52); });
  it("d3n27", () => { const g = new Map([[0, [[1, 27]]], [1, [[2, 27]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(54); });
  it("d3n28", () => { const g = new Map([[0, [[1, 28]]], [1, [[2, 28]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(56); });
  it("d3n29", () => { const g = new Map([[0, [[1, 29]]], [1, [[2, 29]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(58); });
  it("d3n30", () => { const g = new Map([[0, [[1, 30]]], [1, [[2, 30]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(60); });
  it("d3n31", () => { const g = new Map([[0, [[1, 31]]], [1, [[2, 31]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(62); });
  it("d3n32", () => { const g = new Map([[0, [[1, 32]]], [1, [[2, 32]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(64); });
  it("d3n33", () => { const g = new Map([[0, [[1, 33]]], [1, [[2, 33]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(66); });
  it("d3n34", () => { const g = new Map([[0, [[1, 34]]], [1, [[2, 34]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(68); });
  it("d3n35", () => { const g = new Map([[0, [[1, 35]]], [1, [[2, 35]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(70); });
  it("d3n36", () => { const g = new Map([[0, [[1, 36]]], [1, [[2, 36]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(72); });
  it("d3n37", () => { const g = new Map([[0, [[1, 37]]], [1, [[2, 37]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(74); });
  it("d3n38", () => { const g = new Map([[0, [[1, 38]]], [1, [[2, 38]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(76); });
  it("d3n39", () => { const g = new Map([[0, [[1, 39]]], [1, [[2, 39]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(78); });
  it("d3n40", () => { const g = new Map([[0, [[1, 40]]], [1, [[2, 40]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(80); });
  it("d3n41", () => { const g = new Map([[0, [[1, 41]]], [1, [[2, 41]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(82); });
  it("d3n42", () => { const g = new Map([[0, [[1, 42]]], [1, [[2, 42]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(84); });
  it("d3n43", () => { const g = new Map([[0, [[1, 43]]], [1, [[2, 43]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(86); });
  it("d3n44", () => { const g = new Map([[0, [[1, 44]]], [1, [[2, 44]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(88); });
  it("d3n45", () => { const g = new Map([[0, [[1, 45]]], [1, [[2, 45]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(90); });
  it("d3n46", () => { const g = new Map([[0, [[1, 46]]], [1, [[2, 46]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(92); });
  it("d3n47", () => { const g = new Map([[0, [[1, 47]]], [1, [[2, 47]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(94); });
  it("d3n48", () => { const g = new Map([[0, [[1, 48]]], [1, [[2, 48]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(96); });
  it("d3n49", () => { const g = new Map([[0, [[1, 49]]], [1, [[2, 49]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(98); });
  it("d3n50", () => { const g = new Map([[0, [[1, 50]]], [1, [[2, 50]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(100); });
  it("d3n51", () => { const g = new Map([[0, [[1, 51]]], [1, [[2, 51]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(102); });
  it("d3n52", () => { const g = new Map([[0, [[1, 52]]], [1, [[2, 52]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(104); });
  it("d3n53", () => { const g = new Map([[0, [[1, 53]]], [1, [[2, 53]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(106); });
  it("d3n54", () => { const g = new Map([[0, [[1, 54]]], [1, [[2, 54]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(108); });
  it("d3n55", () => { const g = new Map([[0, [[1, 55]]], [1, [[2, 55]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(110); });
  it("d3n56", () => { const g = new Map([[0, [[1, 56]]], [1, [[2, 56]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(112); });
  it("d3n57", () => { const g = new Map([[0, [[1, 57]]], [1, [[2, 57]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(114); });
  it("d3n58", () => { const g = new Map([[0, [[1, 58]]], [1, [[2, 58]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(116); });
  it("d3n59", () => { const g = new Map([[0, [[1, 59]]], [1, [[2, 59]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(118); });
  it("d3n60", () => { const g = new Map([[0, [[1, 60]]], [1, [[2, 60]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(120); });
  it("d3n61", () => { const g = new Map([[0, [[1, 61]]], [1, [[2, 61]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(122); });
  it("d3n62", () => { const g = new Map([[0, [[1, 62]]], [1, [[2, 62]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(124); });
  it("d3n63", () => { const g = new Map([[0, [[1, 63]]], [1, [[2, 63]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(126); });
  it("d3n64", () => { const g = new Map([[0, [[1, 64]]], [1, [[2, 64]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(128); });
  it("d3n65", () => { const g = new Map([[0, [[1, 65]]], [1, [[2, 65]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(130); });
  it("d3n66", () => { const g = new Map([[0, [[1, 66]]], [1, [[2, 66]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(132); });
  it("d3n67", () => { const g = new Map([[0, [[1, 67]]], [1, [[2, 67]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(134); });
  it("d3n68", () => { const g = new Map([[0, [[1, 68]]], [1, [[2, 68]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(136); });
  it("d3n69", () => { const g = new Map([[0, [[1, 69]]], [1, [[2, 69]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(138); });
  it("d3n70", () => { const g = new Map([[0, [[1, 70]]], [1, [[2, 70]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(140); });
  it("d3n71", () => { const g = new Map([[0, [[1, 71]]], [1, [[2, 71]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(142); });
  it("d3n72", () => { const g = new Map([[0, [[1, 72]]], [1, [[2, 72]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(144); });
  it("d3n73", () => { const g = new Map([[0, [[1, 73]]], [1, [[2, 73]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(146); });
  it("d3n74", () => { const g = new Map([[0, [[1, 74]]], [1, [[2, 74]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(148); });
  it("d3n75", () => { const g = new Map([[0, [[1, 75]]], [1, [[2, 75]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(150); });
  it("d3n76", () => { const g = new Map([[0, [[1, 76]]], [1, [[2, 76]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(152); });
  it("d3n77", () => { const g = new Map([[0, [[1, 77]]], [1, [[2, 77]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(154); });
  it("d3n78", () => { const g = new Map([[0, [[1, 78]]], [1, [[2, 78]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(156); });
  it("d3n79", () => { const g = new Map([[0, [[1, 79]]], [1, [[2, 79]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(158); });
  it("d3n80", () => { const g = new Map([[0, [[1, 80]]], [1, [[2, 80]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(160); });
  it("d3n81", () => { const g = new Map([[0, [[1, 81]]], [1, [[2, 81]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(162); });
  it("d3n82", () => { const g = new Map([[0, [[1, 82]]], [1, [[2, 82]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(164); });
  it("d3n83", () => { const g = new Map([[0, [[1, 83]]], [1, [[2, 83]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(166); });
  it("d3n84", () => { const g = new Map([[0, [[1, 84]]], [1, [[2, 84]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(168); });
  it("d3n85", () => { const g = new Map([[0, [[1, 85]]], [1, [[2, 85]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(170); });
  it("d3n86", () => { const g = new Map([[0, [[1, 86]]], [1, [[2, 86]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(172); });
  it("d3n87", () => { const g = new Map([[0, [[1, 87]]], [1, [[2, 87]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(174); });
  it("d3n88", () => { const g = new Map([[0, [[1, 88]]], [1, [[2, 88]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(176); });
  it("d3n89", () => { const g = new Map([[0, [[1, 89]]], [1, [[2, 89]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(178); });
  it("d3n90", () => { const g = new Map([[0, [[1, 90]]], [1, [[2, 90]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(180); });
  it("d3n91", () => { const g = new Map([[0, [[1, 91]]], [1, [[2, 91]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(182); });
  it("d3n92", () => { const g = new Map([[0, [[1, 92]]], [1, [[2, 92]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(184); });
  it("d3n93", () => { const g = new Map([[0, [[1, 93]]], [1, [[2, 93]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(186); });
  it("d3n94", () => { const g = new Map([[0, [[1, 94]]], [1, [[2, 94]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(188); });
  it("d3n95", () => { const g = new Map([[0, [[1, 95]]], [1, [[2, 95]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(190); });
  it("d3n96", () => { const g = new Map([[0, [[1, 96]]], [1, [[2, 96]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(192); });
  it("d3n97", () => { const g = new Map([[0, [[1, 97]]], [1, [[2, 97]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(194); });
  it("d3n98", () => { const g = new Map([[0, [[1, 98]]], [1, [[2, 98]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(196); });
  it("d3n99", () => { const g = new Map([[0, [[1, 99]]], [1, [[2, 99]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(198); });
  it("d3n100", () => { const g = new Map([[0, [[1, 100]]], [1, [[2, 100]]], [2, []]]); const d = dijkstra(g, 0); expect(d.get(2)).toBe(200); });
});

