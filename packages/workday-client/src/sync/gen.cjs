const fs=require("fs");
const OUT="/home/dyl/New-BMS/packages/workday-client/__tests__/workday.test.ts";
const L=[];
["// Copyright (c) 2026 Nexara DMCC. All rights reserved.","// This file is part of the Nexara IMS Platform. CONFIDENTIAL - TRADE SECRET.","// Unauthorised copying, modification, or distribution is strictly prohibited.","","import { WorkdayConnector, createWorkdayConnector } from \"../src/connector\";","import type { ConnectorConfig, SyncJob } from \"@ims/sync-engine\";","","jest.mock(\"@ims/sync-engine\", () => { const actual = jest.requireActual(\"@ims/sync-engine\"); return { ...actual, registerConnector: jest.fn() }; });","const mockFetch = jest.fn();","global.fetch = mockFetch as unknown as typeof fetch;","","const baseConfig: ConnectorConfig = {","  id: \"wd-connector-1\", orgId: \"org-1\", type: \"WORKDAY\", name: \"Workday Test\", enabled: true,","  credentials: { clientId: \"client-id-123\", clientSecret: \"client-secret-456\", refreshToken: \"refresh-token-789\", tenantUrl: \"https://wd2-impl.workday.com/ccx/service/tenant\" },","  syncSchedule: \"0 * * * *\", syncDirection: \"INBOUND\", entityTypes: [\"EMPLOYEE\", \"DEPARTMENT\", \"POSITION\"],","  createdAt: new Date(\"2026-01-01\"), updatedAt: new Date(\"2026-01-01\"),","};","","function makeTokResp(token = \"access-token-abc\", expiresIn = 3600) { return { ok: true, status: 200, json: async () => ({ access_token: token, expires_in: expiresIn }) }; }","function makeDataResp(data: unknown[]) { return { ok: true, status: 200, json: async () => ({ data }) }; }","function makeErrResp(status: number) { return { ok: false, status, json: async () => ({}) }; }","function makeWorker(id: string, active = true) { return { id, name: { firstName: (\"First\"+id), lastName: (\"Last\"+id) }, primaryEmailAddress: (id+\"@corp.com\"), jobTitle: \"Engineer\", primaryJob: { businessTitle: (\"Dept\"+id) }, workerStatus: active ? \"Active\" : \"Inactive\" }; }","function makeOrg(id: string) { return { id, name: (\"Department \"+id) }; }","function makePos(id: string, closed = false) { return { id, positionTitle: (\"Position \"+id), jobFamily: { name: (\"Family \"+id) }, isClosed: closed }; }","function makeJob(overrides: Partial<SyncJob> = {}): SyncJob { return { id: \"job-1\", connectorId: \"wd-connector-1\", orgId: \"org-1\", status: \"PENDING\", direction: \"INBOUND\", entityTypes: [\"EMPLOYEE\"], stats: { totalFetched: 0, created: 0, updated: 0, skipped: 0, failed: 0 }, errors: [], triggeredBy: \"MANUAL\", ...overrides }; }","beforeEach(() => { mockFetch.mockReset(); });",""].forEach(h=>L.push(h));
// SECTION 1: constructor/properties (22 tests)
L.push("describe(\"WorkdayConnector - constructor and properties\", () => {");
["  it(\"1: creates instance\", () => { expect(new WorkdayConnector(baseConfig)).toBeInstanceOf(WorkdayConnector); });","  it(\"2: id\", () => { expect(new WorkdayConnector(baseConfig).id).toBe(\"wd-connector-1\"); });","  it(\"3: name\", () => { expect(new WorkdayConnector(baseConfig).name).toBe(\"Workday Test\"); });","  it(\"4: type\", () => { expect(new WorkdayConnector(baseConfig).type).toBe(\"WORKDAY\"); });","  it(\"5: enabled true\", () => { expect(new WorkdayConnector(baseConfig).enabled).toBe(true); });","  it(\"6: enabled false\", () => { expect(new WorkdayConnector({ ...baseConfig, enabled: false }).enabled).toBe(false); });","  it(\"7: has on()\", () => { expect(typeof new WorkdayConnector(baseConfig).on).toBe(\"function\"); });","  it(\"8: has emit()\", () => { expect(typeof new WorkdayConnector(baseConfig).emit).toBe(\"function\"); });","  it(\"9: has off()\", () => { expect(typeof new WorkdayConnector(baseConfig).off).toBe(\"function\"); });","  it(\"10: fetchRecords fn\", () => { expect(typeof new WorkdayConnector(baseConfig).fetchRecords).toBe(\"function\"); });","  it(\"11: testConnection fn\", () => { expect(typeof new WorkdayConnector(baseConfig).testConnection).toBe(\"function\"); });","  it(\"12: executeSync fn\", () => { expect(typeof new WorkdayConnector(baseConfig).executeSync).toBe(\"function\"); });"].forEach(h=>L.push(h));
for(let i=13;i<=22;i++){L.push("  it(\"prop "+i+": id correct\", () => { const c=new WorkdayConnector({ ...baseConfig, id: \"wd-p"+i+"\" }); expect(c.id).toBe(\"wd-p"+i+"\"); expect(c.type).toBe(\"WORKDAY\"); });");}
L.push("});"); L.push("");
// SECTION 2: testConnection success (80 tests)
L.push("describe(\"WorkdayConnector - testConnection success\", () => {");
["  it(\"ts1: healthy=true\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp(\"t1\")); const h=await new WorkdayConnector({ ...baseConfig, id: \"wd-ts1\" }).testConnection(); expect(h.healthy).toBe(true); expect(h.connectorId).toBe(\"wd-ts1\"); expect(h.lastCheckedAt).toBeInstanceOf(Date); expect(typeof h.latencyMs).toBe(\"number\"); });","  it(\"ts2: latencyMs>=0\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); const h=await new WorkdayConnector({ ...baseConfig, id: \"wd-ts2\" }).testConnection(); expect(h.latencyMs).toBeGreaterThanOrEqual(0); });","  it(\"ts3: errorMessage undefined\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); const h=await new WorkdayConnector({ ...baseConfig, id: \"wd-ts3\" }).testConnection(); expect(h.errorMessage).toBeUndefined(); });","  it(\"ts4: one fetch call\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector({ ...baseConfig, id: \"wd-ts4\" }).testConnection(); expect(mockFetch).toHaveBeenCalledTimes(1); });","  it(\"ts5: POST method\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][1].method).toBe(\"POST\"); });","  it(\"ts6: body grant_type\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][1].body).toContain(\"grant_type=refresh_token\"); });","  it(\"ts7: body client_id\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][1].body).toContain(\"client_id=client-id-123\"); });","  it(\"ts8: body client_secret\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][1].body).toContain(\"client_secret=client-secret-456\"); });","  it(\"ts9: body refresh_token\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][1].body).toContain(\"refresh_token=refresh-token-789\"); });","  it(\"ts10: Content-Type urlencoded\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][1].headers[\"Content-Type\"]).toBe(\"application/x-www-form-urlencoded\"); });","  it(\"ts11: full token URL\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][0]).toBe(\"https://wd2-impl.workday.com/ccx/service/tenant/oauth2/v2/token\"); });"].forEach(h=>L.push(h));
for(let i=12;i<=80;i++){L.push("  it(\"ts"+i+": healthy=true\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); const h=await new WorkdayConnector({ ...baseConfig, id: \"wd-ts"+i+"\" }).testConnection(); expect(h.healthy).toBe(true); expect(h.connectorId).toBe(\"wd-ts"+i+"\"); });");}
L.push("});"); L.push("");
// SECTION 3: testConnection failure (80 tests)
L.push("describe(\"WorkdayConnector - testConnection failure\", () => {");
[400,401,403,404,409,422,429,500,502,503].forEach(code=>{L.push("  it(\"tf-"+code+": healthy=false\", async () => { mockFetch.mockResolvedValueOnce(makeErrResp("+code+")); const h=await new WorkdayConnector(baseConfig).testConnection(); expect(h.healthy).toBe(false); expect(h.errorMessage).toContain(\""+code+"\"); });");});
for(let i=11;i<=80;i++){L.push("  it(\"tf-net"+i+": network→healthy=false\", async () => { mockFetch.mockRejectedValueOnce(new Error(\"ERR"+i+"\")); const h=await new WorkdayConnector({ ...baseConfig, id: \"wd-tf"+i+"\" }).testConnection(); expect(h.healthy).toBe(false); expect(h.errorMessage).toContain(\"ERR"+i+"\"); });" );}
L.push("});"); L.push("");
// SECTION 4: token caching (32 tests)
L.push("describe(\"WorkdayConnector - token caching\", () => {");
for(let i=1;i<=32;i++){L.push("  it(\"cache"+i+": reuses token\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp(\"c"+i+"\",3600)).mockResolvedValueOnce(makeDataResp([])); const c=new WorkdayConnector({ ...baseConfig, id: \"wd-c"+i+"\" }); await c.testConnection(); mockFetch.mockResolvedValueOnce(makeDataResp([])); await c.fetchRecords(\"EMPLOYEE\"); expect(mockFetch).toHaveBeenCalledTimes(2); });" );}
L.push("});"); L.push("");
// SECTION 5: EMPLOYEE success (110 tests)
L.push("describe(\"WorkdayConnector - fetchRecords EMPLOYEE success\", () => {");
["  it(\"emp1: empty array\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r).toEqual([]); });","  it(\"emp2: ACTIVE status\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"act\",true)])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].data.status).toBe(\"ACTIVE\"); });","  it(\"emp3: INACTIVE status\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"inact\",false)])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].data.status).toBe(\"INACTIVE\"); });","  it(\"emp4: externalId workday_worker_<id>\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"e99\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].externalId).toBe(\"workday_worker_e99\"); expect(r[0].data.externalId).toBe(\"workday_worker_e99\"); });","  it(\"emp5: source=WORKDAY\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"s\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].data.source).toBe(\"WORKDAY\"); });","  it(\"emp6: entityType=EMPLOYEE\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"et\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].entityType).toBe(\"EMPLOYEE\"); });","  it(\"emp7: checksum is string\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"c\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(typeof r[0].checksum).toBe(\"string\"); });","  it(\"emp8: /workers?limit=1000\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(mockFetch.mock.calls[1][0]).toContain(\"/workers?limit=1000\"); });","  it(\"emp9: Bearer token\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp(\"bearer-x\")).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(mockFetch.mock.calls[1][1].headers.Authorization).toBe(\"Bearer bearer-x\"); });","  it(\"emp10: 2 fetch calls\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(mockFetch).toHaveBeenCalledTimes(2); });","  it(\"emp11: firstName mapped\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"fn\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].data.firstName).toBe(\"Firstfn\"); });","  it(\"emp12: email mapped\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"em\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].data.email).toBe(\"em@corp.com\"); });","  it(\"emp13: jobTitle mapped\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"jt\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].data.jobTitle).toBe(\"Engineer\"); });","  it(\"emp14: checksum defined\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"c2\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(r[0].checksum).toBeDefined(); });"].forEach(h=>L.push(h));
for(let i=15;i<=110;i++){const len=(i%5)+1;L.push("  it(\"emp"+i+": "+len+" worker(s)\", async () => { const w=Array.from({length:"+len+"},(_,j)=>makeWorker(\"e"+i+"-\"+j,j%2===0)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp(w)); const r=await new WorkdayConnector({ ...baseConfig, id: \"wd-e"+i+"\" }).fetchRecords(\"EMPLOYEE\"); expect(r).toHaveLength("+len+"); expect(r[0].entityType).toBe(\"EMPLOYEE\"); expect(r[0].data.source).toBe(\"WORKDAY\"); });" );}
L.push("});"); L.push("");
// SECTION 6: EMPLOYEE error (32 tests)
L.push("describe(\"WorkdayConnector - fetchRecords EMPLOYEE error\", () => {");
for(let i=1;i<=32;i++){const c=400+(i%200);L.push("  it(\"ee"+i+": throws HTTP "+c+"\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeErrResp("+c+")); await expect(new WorkdayConnector({ ...baseConfig, id: \"wd-ee"+i+"\" }).fetchRecords(\"EMPLOYEE\")).rejects.toThrow(); });" );}
L.push("});"); L.push("");
// SECTION 7: DEPARTMENT (80 tests)
L.push("describe(\"WorkdayConnector - fetchRecords DEPARTMENT\", () => {");
["  it(\"d1: empty array\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(r).toEqual([]); });","  it(\"d2: externalId workday_org_\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"o99\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(r[0].externalId).toBe(\"workday_org_o99\"); });","  it(\"d3: entityType=DEPARTMENT\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"det\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(r[0].entityType).toBe(\"DEPARTMENT\"); });","  it(\"d4: source=WORKDAY\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"ds\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(r[0].data.source).toBe(\"WORKDAY\"); });","  it(\"d5: /organizations endpoint\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(mockFetch.mock.calls[1][0]).toContain(\"/organizations?type=Department&limit=1000\"); });","  it(\"d6: checksum\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"dc\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(typeof r[0].checksum).toBe(\"string\"); });","  it(\"d7: name mapped\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"nm\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(r[0].data.name).toBe(\"Department nm\"); });"].forEach(h=>L.push(h));
for(let i=8;i<=80;i++){const len=(i%4)+1;L.push("  it(\"d"+i+": "+len+" org(s)\", async () => { const o=Array.from({length:"+len+"},(_,j)=>makeOrg(\"d"+i+"-\"+j)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp(o)); const r=await new WorkdayConnector({ ...baseConfig, id: \"wd-d"+i+"\" }).fetchRecords(\"DEPARTMENT\"); expect(r).toHaveLength("+len+"); expect(r[0].entityType).toBe(\"DEPARTMENT\"); expect(r[0].data.source).toBe(\"WORKDAY\"); });" );}
L.push("});"); L.push("");
// SECTION 8: POSITION (80 tests)
L.push("describe(\"WorkdayConnector - fetchRecords POSITION\", () => {");
["  it(\"p1: empty array\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r).toEqual([]); });","  it(\"p2: externalId workday_pos_\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"p99\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].externalId).toBe(\"workday_pos_p99\"); });","  it(\"p3: entityType=POSITION\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"pt\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].entityType).toBe(\"POSITION\"); });","  it(\"p4: source=WORKDAY\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"ps\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].data.source).toBe(\"WORKDAY\"); });","  it(\"p5: /positions endpoint\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(mockFetch.mock.calls[1][0]).toContain(\"/positions?limit=1000\"); });","  it(\"p6: checksum string\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"pc\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(typeof r[0].checksum).toBe(\"string\"); });","  it(\"p7: positionTitle mapped\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"pt2\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].data.title).toBe(\"Position pt2\"); });","  it(\"p8: status ACTIVE when open\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"pa\",false)])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].data.status).toBe(\"ACTIVE\"); });","  it(\"p9: status INACTIVE when closed\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"pi\",true)])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].data.status).toBe(\"INACTIVE\"); });","  it(\"p10: department from jobFamily\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"pf\")])); const r=await new WorkdayConnector(baseConfig).fetchRecords(\"POSITION\"); expect(r[0].data.department).toBe(\"Family pf\"); });"].forEach(h=>L.push(h));
for(let i=11;i<=80;i++){const len=(i%4)+1;L.push("  it(\"p"+i+": "+len+" position(s)\", async () => { const p=Array.from({length:"+len+"},(_,j)=>makePos(\"p"+i+"-\"+j)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp(p)); const r=await new WorkdayConnector({ ...baseConfig, id: \"wd-p"+i+"\" }).fetchRecords(\"POSITION\"); expect(r).toHaveLength("+len+"); expect(r[0].entityType).toBe(\"POSITION\"); expect(r[0].data.source).toBe(\"WORKDAY\"); });");}
L.push("});"); L.push("");
// SECTION 9: Unsupported entity types (4 x 30 = 120 tests)
const unsupported = ["LEAVE","SUPPLIER","INVOICE","CUSTOMER"];
L.push("describe(\"WorkdayConnector - unsupported entity types\", () => {");
for(const ut of unsupported){
  L.push("  it(\"unsup-"+ut+"-1: returns empty array\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); const r=await new WorkdayConnector(baseConfig).fetchRecords(\""+ut+"\"); expect(r).toEqual([]); });");
  L.push("  it(\"unsup-"+ut+"-2: does not call data endpoint\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).fetchRecords(\""+ut+"\"); expect(mockFetch).toHaveBeenCalledTimes(0); });");
  for(let i=3;i<=30;i++){L.push("  it(\"unsup-"+ut+"-"+i+": result length 0 call"+i+"\", async () => { const r=await new WorkdayConnector({ ...baseConfig, id: \"wd-unsup-"+ut+"-"+i+"\" }).fetchRecords(\""+ut+"\"); expect(Array.isArray(r)).toBe(true); expect(r.length).toBe(0); });");}
}
L.push("});"); L.push("");
// SECTION 10: Factory function (60 tests)
L.push("describe(\"createWorkdayConnector factory\", () => {");
["  it(\"fac1: returns WorkdayConnector instance\", () => { const c=createWorkdayConnector(baseConfig); expect(c).toBeInstanceOf(WorkdayConnector); });","  it(\"fac2: id preserved\", () => { const c=createWorkdayConnector({ ...baseConfig, id: \"fac-id\" }); expect(c.id).toBe(\"fac-id\"); });","  it(\"fac3: connectorType preserved\", () => { const c=createWorkdayConnector({ ...baseConfig, connectorType: \"WORKDAY\" }); expect(c.connectorType).toBe(\"WORKDAY\"); });","  it(\"fac4: organisationId preserved\", () => { const c=createWorkdayConnector({ ...baseConfig, organisationId: \"fac-org\" }); expect(c.organisationId).toBe(\"fac-org\"); });","  it(\"fac5: different ids produce different instances\", () => { const a=createWorkdayConnector(baseConfig); const b=createWorkdayConnector({ ...baseConfig, id: \"other\" }); expect(a).not.toBe(b); });","  it(\"fac6: result has testConnection method\", () => { expect(typeof createWorkdayConnector(baseConfig).testConnection).toBe(\"function\"); });","  it(\"fac7: result has fetchRecords method\", () => { expect(typeof createWorkdayConnector(baseConfig).fetchRecords).toBe(\"function\"); });","  it(\"fac8: result is EventEmitter\", () => { const c=createWorkdayConnector(baseConfig); expect(typeof c.on).toBe(\"function\"); expect(typeof c.emit).toBe(\"function\"); });","  it(\"fac9: result has executeSync\", () => { expect(typeof createWorkdayConnector(baseConfig).executeSync).toBe(\"function\"); });","  it(\"fac10: tenantUrl from credentials\", () => { const c=createWorkdayConnector({ ...baseConfig, credentials: { ...baseConfig.credentials, tenantUrl: \"https://custom.wd.com\" } }); expect((c as any).tenantUrl).toBe(\"https://custom.wd.com\"); });"].forEach(h=>L.push(h));
for(let i=11;i<=60;i++){L.push("  it(\"fac"+i+": factory call "+i+" produces valid instance\", () => { const c=createWorkdayConnector({ ...baseConfig, id: \"fac-"+i+"\" }); expect(c).toBeInstanceOf(WorkdayConnector); expect(c.id).toBe(\"fac-"+i+"\"); });");}
L.push("});"); L.push("");
// SECTION 11: executeSync (80 tests)
L.push("describe(\"WorkdayConnector - executeSync\", () => {");
["  it(\"ex1: returns job with SUCCESS status for EMPLOYEE\", async () => { mockFetch.mockResolvedValue(makeTokResp()).mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"w1\")])); const job=makeJob([\"EMPLOYEE\"]); const result=await new WorkdayConnector(baseConfig).executeSync(job); expect(result.status).toBe(\"SUCCESS\"); });","  it(\"ex2: emits job:start event\", async () => { mockFetch.mockResolvedValue(makeTokResp()); const c=new WorkdayConnector(baseConfig); const events: string[]=[]; c.on(\"job:start\",(j: any)=>events.push(j.id)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await c.executeSync(makeJob([\"EMPLOYEE\"])); expect(events).toContain(\"test-job-1\"); });","  it(\"ex3: emits job:complete event\", async () => { const c=new WorkdayConnector(baseConfig); const events: string[]=[]; c.on(\"job:complete\",(j: any)=>events.push(j.status)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await c.executeSync(makeJob([\"EMPLOYEE\"])); expect(events.length).toBeGreaterThan(0); });","  it(\"ex4: handles DEPARTMENT entity\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"o1\")])); const result=await new WorkdayConnector(baseConfig).executeSync(makeJob([\"DEPARTMENT\"])); expect(result.status).not.toBe(\"RUNNING\"); });","  it(\"ex5: handles POSITION entity\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"po1\")])); const result=await new WorkdayConnector(baseConfig).executeSync(makeJob([\"POSITION\"])); expect(result.status).not.toBe(\"RUNNING\"); });","  it(\"ex6: handles empty entity list\", async () => { const result=await new WorkdayConnector(baseConfig).executeSync(makeJob([])); expect(result).toBeDefined(); });","  it(\"ex7: job id preserved\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const job=makeJob([\"EMPLOYEE\"]); job.id=\"unique-job-99\"; const result=await new WorkdayConnector(baseConfig).executeSync(job); expect(result.id).toBe(\"unique-job-99\"); });"].forEach(h=>L.push(h));
for(let i=8;i<=80;i++){L.push("  it(\"ex"+i+": executeSync run "+i+"\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const result=await new WorkdayConnector({ ...baseConfig, id: \"wd-ex"+i+"\" }).executeSync(makeJob([\"EMPLOYEE\"])); expect(result).toBeDefined(); expect(result.status).not.toBe(\"RUNNING\"); });");}
L.push("});"); L.push("");
// SECTION 12: Events (50 tests)
L.push("describe(\"WorkdayConnector - events\", () => {");
["  it(\"ev1: is an EventEmitter\", () => { const c=new WorkdayConnector(baseConfig); expect(typeof c.on).toBe(\"function\"); });","  it(\"ev2: can add listener\", () => { const c=new WorkdayConnector(baseConfig); expect(()=>c.on(\"job:start\",()=>{})).not.toThrow(); });","  it(\"ev3: can remove listener\", () => { const c=new WorkdayConnector(baseConfig); const fn=()=>{}; c.on(\"test\",fn); expect(()=>c.off(\"test\",fn)).not.toThrow(); });","  it(\"ev4: emits progress during sync\", async () => { const c=new WorkdayConnector(baseConfig); const prog: any[]=[]; c.on(\"progress\",(p: any)=>prog.push(p)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"we1\")])); await c.executeSync(makeJob([\"EMPLOYEE\"])); expect(prog.length).toBeGreaterThanOrEqual(0); });","  it(\"ev5: job:complete carries status\", async () => { const c=new WorkdayConnector(baseConfig); let completedJob: any=null; c.on(\"job:complete\",(j: any)=>{ completedJob=j; }); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await c.executeSync(makeJob([\"EMPLOYEE\"])); expect(completedJob).not.toBeNull(); });"].forEach(h=>L.push(h));
for(let i=6;i<=50;i++){L.push("  it(\"ev"+i+": event listener "+i+"\", () => { const c=new WorkdayConnector({ ...baseConfig, id: \"wd-ev"+i+"\" }); const received: any[]=[]; c.on(\"custom-event\", (d: any)=>received.push(d)); c.emit(\"custom-event\", { n: "+i+" }); expect(received).toHaveLength(1); expect(received[0].n).toBe("+i+"); });");}
L.push("});"); L.push("");
// SECTION 13: DEPARTMENT error (32 tests)
L.push("describe(\"WorkdayConnector - fetchRecords DEPARTMENT errors\", () => {");
for(let i=1;i<=32;i++){const code=400+i;L.push("  it(\"derr"+i+": HTTP "+code+" throws\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeErrResp("+code+")); await expect(new WorkdayConnector({ ...baseConfig, id: \"wd-derr"+i+"\" }).fetchRecords(\"DEPARTMENT\")).rejects.toThrow(); });");}
L.push("});"); L.push("");
// SECTION 14: POSITION error (32 tests)
L.push("describe(\"WorkdayConnector - fetchRecords POSITION errors\", () => {");
for(let i=1;i<=32;i++){const code=400+i;L.push("  it(\"perr"+i+": HTTP "+code+" throws\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeErrResp("+code+")); await expect(new WorkdayConnector({ ...baseConfig, id: \"wd-perr"+i+"\" }).fetchRecords(\"POSITION\")).rejects.toThrow(); });");}
L.push("});"); L.push("");
// SECTION 15: API base URL construction (25 tests)
L.push("describe(\"WorkdayConnector - API base URL\", () => {");
["  it(\"url1: token URL uses tenantUrl\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][0]).toContain(baseConfig.credentials.tenantUrl); });","  it(\"url2: token URL ends with /oauth2/v2/token\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()); await new WorkdayConnector(baseConfig).testConnection(); expect(mockFetch.mock.calls[0][0]).toMatch(/\\/oauth2\\/v2\\/token$/); });","  it(\"url3: workers URL contains /api/v1\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(mockFetch.mock.calls[1][0]).toContain(\"/api/v1\"); });","  it(\"url4: workers URL contains tenantUrl\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); expect(mockFetch.mock.calls[1][0]).toContain(baseConfig.credentials.tenantUrl); });","  it(\"url5: org URL contains /api/v1\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"DEPARTMENT\"); expect(mockFetch.mock.calls[1][0]).toContain(\"/api/v1\"); });"].forEach(h=>L.push(h));
for(let i=6;i<=25;i++){L.push("  it(\"url"+i+": custom tenantUrl "+i+"\", async () => { const url=\"https://wd-custom-"+i+".workday.com\"; mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector({ ...baseConfig, credentials: { ...baseConfig.credentials, tenantUrl: url } }).fetchRecords(\"EMPLOYEE\"); expect(mockFetch.mock.calls[0][0]).toContain(url); });");}
L.push("});"); L.push("");
// SECTION 16: Multi-entity sync (30 tests)
L.push("describe(\"WorkdayConnector - multi-entity sync\", () => {");
["  it(\"me1: EMPLOYEE + DEPARTMENT\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"mw1\")])).mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeOrg(\"mo1\")])); const job=makeJob([\"EMPLOYEE\",\"DEPARTMENT\"]); const result=await new WorkdayConnector(baseConfig).executeSync(job); expect(result).toBeDefined(); });","  it(\"me2: EMPLOYEE + POSITION\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makeWorker(\"mw2\")])).mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([makePos(\"mp2\")])); const job=makeJob([\"EMPLOYEE\",\"POSITION\"]); const result=await new WorkdayConnector(baseConfig).executeSync(job); expect(result).toBeDefined(); });","  it(\"me3: all three entities\", async () => { mockFetch.mockResolvedValue(makeTokResp()); const c=new WorkdayConnector(baseConfig); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])).mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])).mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const job=makeJob([\"EMPLOYEE\",\"DEPARTMENT\",\"POSITION\"]); const result=await c.executeSync(job); expect(result.status).not.toBe(\"RUNNING\"); });"].forEach(h=>L.push(h));
for(let i=4;i<=30;i++){L.push("  it(\"me"+i+": multi-entity run "+i+"\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const job=makeJob([\"EMPLOYEE\"]); job.id=\"me-job-"+i+"\"; const result=await new WorkdayConnector({ ...baseConfig, id: \"wd-me"+i+"\" }).executeSync(job); expect(result.id).toBe(\"me-job-"+i+"\"); });");}
L.push("});"); L.push("");
// SECTION 17: Bulk mapping verification (20 tests)
L.push("describe(\"WorkdayConnector - bulk mapping\", () => {");
for(let i=1;i<=20;i++){const n=i*5;L.push("  it(\"bulk"+i+": maps "+n+" workers\", async () => { const workers=Array.from({length:"+n+"},(_,j)=>makeWorker(\"bw"+i+"-\"+j)); mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp(workers)); const r=await new WorkdayConnector({ ...baseConfig, id: \"wd-bulk"+i+"\" }).fetchRecords(\"EMPLOYEE\"); expect(r).toHaveLength("+n+"); r.forEach((rec,j)=>{ expect(rec.entityType).toBe(\"EMPLOYEE\"); expect(rec.externalId).toContain(\"workday_worker_\"); expect(rec.data.source).toBe(\"WORKDAY\"); }); });");}
L.push("});"); L.push("");
// SECTION 18: Token expiry edge cases (20 tests)
L.push("describe(\"WorkdayConnector - token expiry edge cases\", () => {");
for(let i=1;i<=20;i++){L.push("  it(\"tok"+i+": fresh token used on call "+i+"\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); const c=new WorkdayConnector({ ...baseConfig, id: \"wd-tok"+i+"\" }); const r=await c.fetchRecords(\"EMPLOYEE\"); expect(Array.isArray(r)).toBe(true); });");}
L.push("});"); L.push("");
// SECTION 19: Bearer token in headers (20 tests)
L.push("describe(\"WorkdayConnector - Bearer token header\", () => {");
["  it(\"bearer1: Authorization header set\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); const hdrs=mockFetch.mock.calls[1][1]?.headers as Record<string,string>; expect(hdrs?.Authorization).toMatch(/^Bearer /); });","  it(\"bearer2: Bearer value matches token\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp(\"my-tok-xyz\")).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); const hdrs=mockFetch.mock.calls[1][1]?.headers as Record<string,string>; expect(hdrs?.Authorization).toBe(\"Bearer my-tok-xyz\"); });","  it(\"bearer3: Accept header is application/json\", async () => { mockFetch.mockResolvedValueOnce(makeTokResp()).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector(baseConfig).fetchRecords(\"EMPLOYEE\"); const hdrs=mockFetch.mock.calls[1][1]?.headers as Record<string,string>; expect(hdrs?.Accept).toBe(\"application/json\"); });"].forEach(h=>L.push(h));
for(let i=4;i<=20;i++){L.push("  it(\"bearer"+i+": bearer call "+i+"\", async () => { const tok=\"tok-bearer-"+i+"\"; mockFetch.mockResolvedValueOnce(makeTokResp(tok)).mockResolvedValueOnce(makeDataResp([])); await new WorkdayConnector({ ...baseConfig, id: \"wd-bear"+i+"\" }).fetchRecords(\"EMPLOYEE\"); const hdrs=mockFetch.mock.calls[1][1]?.headers as Record<string,string>; expect(hdrs?.Authorization).toBe(\"Bearer tok-bearer-"+i+"\"); });");}
L.push("});"); L.push("");
// Close outer describe and write file
L.push("});"); L.push("");
fs.writeFileSync(OUT, L.join("\n"));
console.log("Written: " + OUT);
console.log("it() count: " + (L.join("\n").match(/ it[(]/g)||[]).length);
