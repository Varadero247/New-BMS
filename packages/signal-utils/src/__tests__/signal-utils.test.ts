// Copyright (c) 2026 Nexara DMCC. All rights reserved.

import { createSignal, createComputed, createEffect, batch, untrack, createMemo, isSignal, peek, derive } from '../signal-utils';
describe('signal-utils', () => {
  describe('createSignal', () => {
    it('signal get initial 0', () => { const s = createSignal(0); expect(s.get()).toBe(0); });
    it('signal get initial 1', () => { const s = createSignal(1); expect(s.get()).toBe(1); });
    it('signal get initial 2', () => { const s = createSignal(2); expect(s.get()).toBe(2); });
    it('signal get initial 3', () => { const s = createSignal(3); expect(s.get()).toBe(3); });
    it('signal get initial 4', () => { const s = createSignal(4); expect(s.get()).toBe(4); });
    it('signal get initial 5', () => { const s = createSignal(5); expect(s.get()).toBe(5); });
    it('signal get initial 6', () => { const s = createSignal(6); expect(s.get()).toBe(6); });
    it('signal get initial 7', () => { const s = createSignal(7); expect(s.get()).toBe(7); });
    it('signal get initial 8', () => { const s = createSignal(8); expect(s.get()).toBe(8); });
    it('signal get initial 9', () => { const s = createSignal(9); expect(s.get()).toBe(9); });
    it('signal get initial 10', () => { const s = createSignal(10); expect(s.get()).toBe(10); });
    it('signal get initial 11', () => { const s = createSignal(11); expect(s.get()).toBe(11); });
    it('signal get initial 12', () => { const s = createSignal(12); expect(s.get()).toBe(12); });
    it('signal get initial 13', () => { const s = createSignal(13); expect(s.get()).toBe(13); });
    it('signal get initial 14', () => { const s = createSignal(14); expect(s.get()).toBe(14); });
    it('signal get initial 15', () => { const s = createSignal(15); expect(s.get()).toBe(15); });
    it('signal get initial 16', () => { const s = createSignal(16); expect(s.get()).toBe(16); });
    it('signal get initial 17', () => { const s = createSignal(17); expect(s.get()).toBe(17); });
    it('signal get initial 18', () => { const s = createSignal(18); expect(s.get()).toBe(18); });
    it('signal get initial 19', () => { const s = createSignal(19); expect(s.get()).toBe(19); });
    it('signal get initial 20', () => { const s = createSignal(20); expect(s.get()).toBe(20); });
    it('signal get initial 21', () => { const s = createSignal(21); expect(s.get()).toBe(21); });
    it('signal get initial 22', () => { const s = createSignal(22); expect(s.get()).toBe(22); });
    it('signal get initial 23', () => { const s = createSignal(23); expect(s.get()).toBe(23); });
    it('signal get initial 24', () => { const s = createSignal(24); expect(s.get()).toBe(24); });
    it('signal get initial 25', () => { const s = createSignal(25); expect(s.get()).toBe(25); });
    it('signal get initial 26', () => { const s = createSignal(26); expect(s.get()).toBe(26); });
    it('signal get initial 27', () => { const s = createSignal(27); expect(s.get()).toBe(27); });
    it('signal get initial 28', () => { const s = createSignal(28); expect(s.get()).toBe(28); });
    it('signal get initial 29', () => { const s = createSignal(29); expect(s.get()).toBe(29); });
    it('signal get initial 30', () => { const s = createSignal(30); expect(s.get()).toBe(30); });
    it('signal get initial 31', () => { const s = createSignal(31); expect(s.get()).toBe(31); });
    it('signal get initial 32', () => { const s = createSignal(32); expect(s.get()).toBe(32); });
    it('signal get initial 33', () => { const s = createSignal(33); expect(s.get()).toBe(33); });
    it('signal get initial 34', () => { const s = createSignal(34); expect(s.get()).toBe(34); });
    it('signal get initial 35', () => { const s = createSignal(35); expect(s.get()).toBe(35); });
    it('signal get initial 36', () => { const s = createSignal(36); expect(s.get()).toBe(36); });
    it('signal get initial 37', () => { const s = createSignal(37); expect(s.get()).toBe(37); });
    it('signal get initial 38', () => { const s = createSignal(38); expect(s.get()).toBe(38); });
    it('signal get initial 39', () => { const s = createSignal(39); expect(s.get()).toBe(39); });
    it('signal get initial 40', () => { const s = createSignal(40); expect(s.get()).toBe(40); });
    it('signal get initial 41', () => { const s = createSignal(41); expect(s.get()).toBe(41); });
    it('signal get initial 42', () => { const s = createSignal(42); expect(s.get()).toBe(42); });
    it('signal get initial 43', () => { const s = createSignal(43); expect(s.get()).toBe(43); });
    it('signal get initial 44', () => { const s = createSignal(44); expect(s.get()).toBe(44); });
    it('signal get initial 45', () => { const s = createSignal(45); expect(s.get()).toBe(45); });
    it('signal get initial 46', () => { const s = createSignal(46); expect(s.get()).toBe(46); });
    it('signal get initial 47', () => { const s = createSignal(47); expect(s.get()).toBe(47); });
    it('signal get initial 48', () => { const s = createSignal(48); expect(s.get()).toBe(48); });
    it('signal get initial 49', () => { const s = createSignal(49); expect(s.get()).toBe(49); });
    it('signal get initial 50', () => { const s = createSignal(50); expect(s.get()).toBe(50); });
    it('signal get initial 51', () => { const s = createSignal(51); expect(s.get()).toBe(51); });
    it('signal get initial 52', () => { const s = createSignal(52); expect(s.get()).toBe(52); });
    it('signal get initial 53', () => { const s = createSignal(53); expect(s.get()).toBe(53); });
    it('signal get initial 54', () => { const s = createSignal(54); expect(s.get()).toBe(54); });
    it('signal get initial 55', () => { const s = createSignal(55); expect(s.get()).toBe(55); });
    it('signal get initial 56', () => { const s = createSignal(56); expect(s.get()).toBe(56); });
    it('signal get initial 57', () => { const s = createSignal(57); expect(s.get()).toBe(57); });
    it('signal get initial 58', () => { const s = createSignal(58); expect(s.get()).toBe(58); });
    it('signal get initial 59', () => { const s = createSignal(59); expect(s.get()).toBe(59); });
    it('signal get initial 60', () => { const s = createSignal(60); expect(s.get()).toBe(60); });
    it('signal get initial 61', () => { const s = createSignal(61); expect(s.get()).toBe(61); });
    it('signal get initial 62', () => { const s = createSignal(62); expect(s.get()).toBe(62); });
    it('signal get initial 63', () => { const s = createSignal(63); expect(s.get()).toBe(63); });
    it('signal get initial 64', () => { const s = createSignal(64); expect(s.get()).toBe(64); });
    it('signal get initial 65', () => { const s = createSignal(65); expect(s.get()).toBe(65); });
    it('signal get initial 66', () => { const s = createSignal(66); expect(s.get()).toBe(66); });
    it('signal get initial 67', () => { const s = createSignal(67); expect(s.get()).toBe(67); });
    it('signal get initial 68', () => { const s = createSignal(68); expect(s.get()).toBe(68); });
    it('signal get initial 69', () => { const s = createSignal(69); expect(s.get()).toBe(69); });
    it('signal get initial 70', () => { const s = createSignal(70); expect(s.get()).toBe(70); });
    it('signal get initial 71', () => { const s = createSignal(71); expect(s.get()).toBe(71); });
    it('signal get initial 72', () => { const s = createSignal(72); expect(s.get()).toBe(72); });
    it('signal get initial 73', () => { const s = createSignal(73); expect(s.get()).toBe(73); });
    it('signal get initial 74', () => { const s = createSignal(74); expect(s.get()).toBe(74); });
    it('signal get initial 75', () => { const s = createSignal(75); expect(s.get()).toBe(75); });
    it('signal get initial 76', () => { const s = createSignal(76); expect(s.get()).toBe(76); });
    it('signal get initial 77', () => { const s = createSignal(77); expect(s.get()).toBe(77); });
    it('signal get initial 78', () => { const s = createSignal(78); expect(s.get()).toBe(78); });
    it('signal get initial 79', () => { const s = createSignal(79); expect(s.get()).toBe(79); });
    it('signal get initial 80', () => { const s = createSignal(80); expect(s.get()).toBe(80); });
    it('signal get initial 81', () => { const s = createSignal(81); expect(s.get()).toBe(81); });
    it('signal get initial 82', () => { const s = createSignal(82); expect(s.get()).toBe(82); });
    it('signal get initial 83', () => { const s = createSignal(83); expect(s.get()).toBe(83); });
    it('signal get initial 84', () => { const s = createSignal(84); expect(s.get()).toBe(84); });
    it('signal get initial 85', () => { const s = createSignal(85); expect(s.get()).toBe(85); });
    it('signal get initial 86', () => { const s = createSignal(86); expect(s.get()).toBe(86); });
    it('signal get initial 87', () => { const s = createSignal(87); expect(s.get()).toBe(87); });
    it('signal get initial 88', () => { const s = createSignal(88); expect(s.get()).toBe(88); });
    it('signal get initial 89', () => { const s = createSignal(89); expect(s.get()).toBe(89); });
    it('signal get initial 90', () => { const s = createSignal(90); expect(s.get()).toBe(90); });
    it('signal get initial 91', () => { const s = createSignal(91); expect(s.get()).toBe(91); });
    it('signal get initial 92', () => { const s = createSignal(92); expect(s.get()).toBe(92); });
    it('signal get initial 93', () => { const s = createSignal(93); expect(s.get()).toBe(93); });
    it('signal get initial 94', () => { const s = createSignal(94); expect(s.get()).toBe(94); });
    it('signal get initial 95', () => { const s = createSignal(95); expect(s.get()).toBe(95); });
    it('signal get initial 96', () => { const s = createSignal(96); expect(s.get()).toBe(96); });
    it('signal get initial 97', () => { const s = createSignal(97); expect(s.get()).toBe(97); });
    it('signal get initial 98', () => { const s = createSignal(98); expect(s.get()).toBe(98); });
    it('signal get initial 99', () => { const s = createSignal(99); expect(s.get()).toBe(99); });
    it('signal set 0', () => { const s = createSignal(0); s.set(0); expect(s.get()).toBe(0); });
    it('signal set 1', () => { const s = createSignal(0); s.set(1); expect(s.get()).toBe(1); });
    it('signal set 2', () => { const s = createSignal(0); s.set(2); expect(s.get()).toBe(2); });
    it('signal set 3', () => { const s = createSignal(0); s.set(3); expect(s.get()).toBe(3); });
    it('signal set 4', () => { const s = createSignal(0); s.set(4); expect(s.get()).toBe(4); });
    it('signal set 5', () => { const s = createSignal(0); s.set(5); expect(s.get()).toBe(5); });
    it('signal set 6', () => { const s = createSignal(0); s.set(6); expect(s.get()).toBe(6); });
    it('signal set 7', () => { const s = createSignal(0); s.set(7); expect(s.get()).toBe(7); });
    it('signal set 8', () => { const s = createSignal(0); s.set(8); expect(s.get()).toBe(8); });
    it('signal set 9', () => { const s = createSignal(0); s.set(9); expect(s.get()).toBe(9); });
    it('signal set 10', () => { const s = createSignal(0); s.set(10); expect(s.get()).toBe(10); });
    it('signal set 11', () => { const s = createSignal(0); s.set(11); expect(s.get()).toBe(11); });
    it('signal set 12', () => { const s = createSignal(0); s.set(12); expect(s.get()).toBe(12); });
    it('signal set 13', () => { const s = createSignal(0); s.set(13); expect(s.get()).toBe(13); });
    it('signal set 14', () => { const s = createSignal(0); s.set(14); expect(s.get()).toBe(14); });
    it('signal set 15', () => { const s = createSignal(0); s.set(15); expect(s.get()).toBe(15); });
    it('signal set 16', () => { const s = createSignal(0); s.set(16); expect(s.get()).toBe(16); });
    it('signal set 17', () => { const s = createSignal(0); s.set(17); expect(s.get()).toBe(17); });
    it('signal set 18', () => { const s = createSignal(0); s.set(18); expect(s.get()).toBe(18); });
    it('signal set 19', () => { const s = createSignal(0); s.set(19); expect(s.get()).toBe(19); });
    it('signal set 20', () => { const s = createSignal(0); s.set(20); expect(s.get()).toBe(20); });
    it('signal set 21', () => { const s = createSignal(0); s.set(21); expect(s.get()).toBe(21); });
    it('signal set 22', () => { const s = createSignal(0); s.set(22); expect(s.get()).toBe(22); });
    it('signal set 23', () => { const s = createSignal(0); s.set(23); expect(s.get()).toBe(23); });
    it('signal set 24', () => { const s = createSignal(0); s.set(24); expect(s.get()).toBe(24); });
    it('signal set 25', () => { const s = createSignal(0); s.set(25); expect(s.get()).toBe(25); });
    it('signal set 26', () => { const s = createSignal(0); s.set(26); expect(s.get()).toBe(26); });
    it('signal set 27', () => { const s = createSignal(0); s.set(27); expect(s.get()).toBe(27); });
    it('signal set 28', () => { const s = createSignal(0); s.set(28); expect(s.get()).toBe(28); });
    it('signal set 29', () => { const s = createSignal(0); s.set(29); expect(s.get()).toBe(29); });
    it('signal set 30', () => { const s = createSignal(0); s.set(30); expect(s.get()).toBe(30); });
    it('signal set 31', () => { const s = createSignal(0); s.set(31); expect(s.get()).toBe(31); });
    it('signal set 32', () => { const s = createSignal(0); s.set(32); expect(s.get()).toBe(32); });
    it('signal set 33', () => { const s = createSignal(0); s.set(33); expect(s.get()).toBe(33); });
    it('signal set 34', () => { const s = createSignal(0); s.set(34); expect(s.get()).toBe(34); });
    it('signal set 35', () => { const s = createSignal(0); s.set(35); expect(s.get()).toBe(35); });
    it('signal set 36', () => { const s = createSignal(0); s.set(36); expect(s.get()).toBe(36); });
    it('signal set 37', () => { const s = createSignal(0); s.set(37); expect(s.get()).toBe(37); });
    it('signal set 38', () => { const s = createSignal(0); s.set(38); expect(s.get()).toBe(38); });
    it('signal set 39', () => { const s = createSignal(0); s.set(39); expect(s.get()).toBe(39); });
    it('signal set 40', () => { const s = createSignal(0); s.set(40); expect(s.get()).toBe(40); });
    it('signal set 41', () => { const s = createSignal(0); s.set(41); expect(s.get()).toBe(41); });
    it('signal set 42', () => { const s = createSignal(0); s.set(42); expect(s.get()).toBe(42); });
    it('signal set 43', () => { const s = createSignal(0); s.set(43); expect(s.get()).toBe(43); });
    it('signal set 44', () => { const s = createSignal(0); s.set(44); expect(s.get()).toBe(44); });
    it('signal set 45', () => { const s = createSignal(0); s.set(45); expect(s.get()).toBe(45); });
    it('signal set 46', () => { const s = createSignal(0); s.set(46); expect(s.get()).toBe(46); });
    it('signal set 47', () => { const s = createSignal(0); s.set(47); expect(s.get()).toBe(47); });
    it('signal set 48', () => { const s = createSignal(0); s.set(48); expect(s.get()).toBe(48); });
    it('signal set 49', () => { const s = createSignal(0); s.set(49); expect(s.get()).toBe(49); });
    it('signal set 50', () => { const s = createSignal(0); s.set(50); expect(s.get()).toBe(50); });
    it('signal set 51', () => { const s = createSignal(0); s.set(51); expect(s.get()).toBe(51); });
    it('signal set 52', () => { const s = createSignal(0); s.set(52); expect(s.get()).toBe(52); });
    it('signal set 53', () => { const s = createSignal(0); s.set(53); expect(s.get()).toBe(53); });
    it('signal set 54', () => { const s = createSignal(0); s.set(54); expect(s.get()).toBe(54); });
    it('signal set 55', () => { const s = createSignal(0); s.set(55); expect(s.get()).toBe(55); });
    it('signal set 56', () => { const s = createSignal(0); s.set(56); expect(s.get()).toBe(56); });
    it('signal set 57', () => { const s = createSignal(0); s.set(57); expect(s.get()).toBe(57); });
    it('signal set 58', () => { const s = createSignal(0); s.set(58); expect(s.get()).toBe(58); });
    it('signal set 59', () => { const s = createSignal(0); s.set(59); expect(s.get()).toBe(59); });
    it('signal set 60', () => { const s = createSignal(0); s.set(60); expect(s.get()).toBe(60); });
    it('signal set 61', () => { const s = createSignal(0); s.set(61); expect(s.get()).toBe(61); });
    it('signal set 62', () => { const s = createSignal(0); s.set(62); expect(s.get()).toBe(62); });
    it('signal set 63', () => { const s = createSignal(0); s.set(63); expect(s.get()).toBe(63); });
    it('signal set 64', () => { const s = createSignal(0); s.set(64); expect(s.get()).toBe(64); });
    it('signal set 65', () => { const s = createSignal(0); s.set(65); expect(s.get()).toBe(65); });
    it('signal set 66', () => { const s = createSignal(0); s.set(66); expect(s.get()).toBe(66); });
    it('signal set 67', () => { const s = createSignal(0); s.set(67); expect(s.get()).toBe(67); });
    it('signal set 68', () => { const s = createSignal(0); s.set(68); expect(s.get()).toBe(68); });
    it('signal set 69', () => { const s = createSignal(0); s.set(69); expect(s.get()).toBe(69); });
    it('signal set 70', () => { const s = createSignal(0); s.set(70); expect(s.get()).toBe(70); });
    it('signal set 71', () => { const s = createSignal(0); s.set(71); expect(s.get()).toBe(71); });
    it('signal set 72', () => { const s = createSignal(0); s.set(72); expect(s.get()).toBe(72); });
    it('signal set 73', () => { const s = createSignal(0); s.set(73); expect(s.get()).toBe(73); });
    it('signal set 74', () => { const s = createSignal(0); s.set(74); expect(s.get()).toBe(74); });
    it('signal set 75', () => { const s = createSignal(0); s.set(75); expect(s.get()).toBe(75); });
    it('signal set 76', () => { const s = createSignal(0); s.set(76); expect(s.get()).toBe(76); });
    it('signal set 77', () => { const s = createSignal(0); s.set(77); expect(s.get()).toBe(77); });
    it('signal set 78', () => { const s = createSignal(0); s.set(78); expect(s.get()).toBe(78); });
    it('signal set 79', () => { const s = createSignal(0); s.set(79); expect(s.get()).toBe(79); });
    it('signal set 80', () => { const s = createSignal(0); s.set(80); expect(s.get()).toBe(80); });
    it('signal set 81', () => { const s = createSignal(0); s.set(81); expect(s.get()).toBe(81); });
    it('signal set 82', () => { const s = createSignal(0); s.set(82); expect(s.get()).toBe(82); });
    it('signal set 83', () => { const s = createSignal(0); s.set(83); expect(s.get()).toBe(83); });
    it('signal set 84', () => { const s = createSignal(0); s.set(84); expect(s.get()).toBe(84); });
    it('signal set 85', () => { const s = createSignal(0); s.set(85); expect(s.get()).toBe(85); });
    it('signal set 86', () => { const s = createSignal(0); s.set(86); expect(s.get()).toBe(86); });
    it('signal set 87', () => { const s = createSignal(0); s.set(87); expect(s.get()).toBe(87); });
    it('signal set 88', () => { const s = createSignal(0); s.set(88); expect(s.get()).toBe(88); });
    it('signal set 89', () => { const s = createSignal(0); s.set(89); expect(s.get()).toBe(89); });
    it('signal set 90', () => { const s = createSignal(0); s.set(90); expect(s.get()).toBe(90); });
    it('signal set 91', () => { const s = createSignal(0); s.set(91); expect(s.get()).toBe(91); });
    it('signal set 92', () => { const s = createSignal(0); s.set(92); expect(s.get()).toBe(92); });
    it('signal set 93', () => { const s = createSignal(0); s.set(93); expect(s.get()).toBe(93); });
    it('signal set 94', () => { const s = createSignal(0); s.set(94); expect(s.get()).toBe(94); });
    it('signal set 95', () => { const s = createSignal(0); s.set(95); expect(s.get()).toBe(95); });
    it('signal set 96', () => { const s = createSignal(0); s.set(96); expect(s.get()).toBe(96); });
    it('signal set 97', () => { const s = createSignal(0); s.set(97); expect(s.get()).toBe(97); });
    it('signal set 98', () => { const s = createSignal(0); s.set(98); expect(s.get()).toBe(98); });
    it('signal set 99', () => { const s = createSignal(0); s.set(99); expect(s.get()).toBe(99); });
    it('signal with string', () => { const s = createSignal('hello'); expect(s.get()).toBe('hello'); });
    it('signal with null', () => { const s = createSignal<null>(null); expect(s.get()).toBeNull(); });
    it('signal updates', () => { const s = createSignal(0); s.set(1); s.set(2); expect(s.get()).toBe(2); });
  });
  describe('createComputed', () => {
    it('computed derives 0', () => { const s = createSignal(0); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(0); });
    it('computed derives 1', () => { const s = createSignal(1); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(2); });
    it('computed derives 2', () => { const s = createSignal(2); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(4); });
    it('computed derives 3', () => { const s = createSignal(3); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(6); });
    it('computed derives 4', () => { const s = createSignal(4); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(8); });
    it('computed derives 5', () => { const s = createSignal(5); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(10); });
    it('computed derives 6', () => { const s = createSignal(6); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(12); });
    it('computed derives 7', () => { const s = createSignal(7); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(14); });
    it('computed derives 8', () => { const s = createSignal(8); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(16); });
    it('computed derives 9', () => { const s = createSignal(9); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(18); });
    it('computed derives 10', () => { const s = createSignal(10); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(20); });
    it('computed derives 11', () => { const s = createSignal(11); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(22); });
    it('computed derives 12', () => { const s = createSignal(12); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(24); });
    it('computed derives 13', () => { const s = createSignal(13); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(26); });
    it('computed derives 14', () => { const s = createSignal(14); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(28); });
    it('computed derives 15', () => { const s = createSignal(15); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(30); });
    it('computed derives 16', () => { const s = createSignal(16); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(32); });
    it('computed derives 17', () => { const s = createSignal(17); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(34); });
    it('computed derives 18', () => { const s = createSignal(18); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(36); });
    it('computed derives 19', () => { const s = createSignal(19); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(38); });
    it('computed derives 20', () => { const s = createSignal(20); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(40); });
    it('computed derives 21', () => { const s = createSignal(21); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(42); });
    it('computed derives 22', () => { const s = createSignal(22); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(44); });
    it('computed derives 23', () => { const s = createSignal(23); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(46); });
    it('computed derives 24', () => { const s = createSignal(24); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(48); });
    it('computed derives 25', () => { const s = createSignal(25); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(50); });
    it('computed derives 26', () => { const s = createSignal(26); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(52); });
    it('computed derives 27', () => { const s = createSignal(27); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(54); });
    it('computed derives 28', () => { const s = createSignal(28); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(56); });
    it('computed derives 29', () => { const s = createSignal(29); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(58); });
    it('computed derives 30', () => { const s = createSignal(30); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(60); });
    it('computed derives 31', () => { const s = createSignal(31); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(62); });
    it('computed derives 32', () => { const s = createSignal(32); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(64); });
    it('computed derives 33', () => { const s = createSignal(33); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(66); });
    it('computed derives 34', () => { const s = createSignal(34); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(68); });
    it('computed derives 35', () => { const s = createSignal(35); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(70); });
    it('computed derives 36', () => { const s = createSignal(36); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(72); });
    it('computed derives 37', () => { const s = createSignal(37); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(74); });
    it('computed derives 38', () => { const s = createSignal(38); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(76); });
    it('computed derives 39', () => { const s = createSignal(39); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(78); });
    it('computed derives 40', () => { const s = createSignal(40); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(80); });
    it('computed derives 41', () => { const s = createSignal(41); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(82); });
    it('computed derives 42', () => { const s = createSignal(42); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(84); });
    it('computed derives 43', () => { const s = createSignal(43); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(86); });
    it('computed derives 44', () => { const s = createSignal(44); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(88); });
    it('computed derives 45', () => { const s = createSignal(45); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(90); });
    it('computed derives 46', () => { const s = createSignal(46); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(92); });
    it('computed derives 47', () => { const s = createSignal(47); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(94); });
    it('computed derives 48', () => { const s = createSignal(48); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(96); });
    it('computed derives 49', () => { const s = createSignal(49); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(98); });
    it('computed derives 50', () => { const s = createSignal(50); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(100); });
    it('computed derives 51', () => { const s = createSignal(51); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(102); });
    it('computed derives 52', () => { const s = createSignal(52); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(104); });
    it('computed derives 53', () => { const s = createSignal(53); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(106); });
    it('computed derives 54', () => { const s = createSignal(54); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(108); });
    it('computed derives 55', () => { const s = createSignal(55); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(110); });
    it('computed derives 56', () => { const s = createSignal(56); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(112); });
    it('computed derives 57', () => { const s = createSignal(57); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(114); });
    it('computed derives 58', () => { const s = createSignal(58); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(116); });
    it('computed derives 59', () => { const s = createSignal(59); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(118); });
    it('computed derives 60', () => { const s = createSignal(60); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(120); });
    it('computed derives 61', () => { const s = createSignal(61); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(122); });
    it('computed derives 62', () => { const s = createSignal(62); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(124); });
    it('computed derives 63', () => { const s = createSignal(63); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(126); });
    it('computed derives 64', () => { const s = createSignal(64); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(128); });
    it('computed derives 65', () => { const s = createSignal(65); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(130); });
    it('computed derives 66', () => { const s = createSignal(66); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(132); });
    it('computed derives 67', () => { const s = createSignal(67); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(134); });
    it('computed derives 68', () => { const s = createSignal(68); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(136); });
    it('computed derives 69', () => { const s = createSignal(69); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(138); });
    it('computed derives 70', () => { const s = createSignal(70); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(140); });
    it('computed derives 71', () => { const s = createSignal(71); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(142); });
    it('computed derives 72', () => { const s = createSignal(72); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(144); });
    it('computed derives 73', () => { const s = createSignal(73); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(146); });
    it('computed derives 74', () => { const s = createSignal(74); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(148); });
    it('computed derives 75', () => { const s = createSignal(75); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(150); });
    it('computed derives 76', () => { const s = createSignal(76); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(152); });
    it('computed derives 77', () => { const s = createSignal(77); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(154); });
    it('computed derives 78', () => { const s = createSignal(78); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(156); });
    it('computed derives 79', () => { const s = createSignal(79); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(158); });
    it('computed derives 80', () => { const s = createSignal(80); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(160); });
    it('computed derives 81', () => { const s = createSignal(81); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(162); });
    it('computed derives 82', () => { const s = createSignal(82); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(164); });
    it('computed derives 83', () => { const s = createSignal(83); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(166); });
    it('computed derives 84', () => { const s = createSignal(84); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(168); });
    it('computed derives 85', () => { const s = createSignal(85); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(170); });
    it('computed derives 86', () => { const s = createSignal(86); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(172); });
    it('computed derives 87', () => { const s = createSignal(87); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(174); });
    it('computed derives 88', () => { const s = createSignal(88); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(176); });
    it('computed derives 89', () => { const s = createSignal(89); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(178); });
    it('computed derives 90', () => { const s = createSignal(90); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(180); });
    it('computed derives 91', () => { const s = createSignal(91); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(182); });
    it('computed derives 92', () => { const s = createSignal(92); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(184); });
    it('computed derives 93', () => { const s = createSignal(93); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(186); });
    it('computed derives 94', () => { const s = createSignal(94); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(188); });
    it('computed derives 95', () => { const s = createSignal(95); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(190); });
    it('computed derives 96', () => { const s = createSignal(96); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(192); });
    it('computed derives 97', () => { const s = createSignal(97); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(194); });
    it('computed derives 98', () => { const s = createSignal(98); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(196); });
    it('computed derives 99', () => { const s = createSignal(99); const c = createComputed(() => s.get() * 2); expect(c.get()).toBe(198); });
    it('computed reacts 0', () => { const s = createSignal(0); const c = createComputed(() => s.get() + 10); s.set(5); expect(c.get()).toBe(15); });
    it('computed reacts 1', () => { const s = createSignal(1); const c = createComputed(() => s.get() + 10); s.set(6); expect(c.get()).toBe(16); });
    it('computed reacts 2', () => { const s = createSignal(2); const c = createComputed(() => s.get() + 10); s.set(7); expect(c.get()).toBe(17); });
    it('computed reacts 3', () => { const s = createSignal(3); const c = createComputed(() => s.get() + 10); s.set(8); expect(c.get()).toBe(18); });
    it('computed reacts 4', () => { const s = createSignal(4); const c = createComputed(() => s.get() + 10); s.set(9); expect(c.get()).toBe(19); });
    it('computed reacts 5', () => { const s = createSignal(5); const c = createComputed(() => s.get() + 10); s.set(10); expect(c.get()).toBe(20); });
    it('computed reacts 6', () => { const s = createSignal(6); const c = createComputed(() => s.get() + 10); s.set(11); expect(c.get()).toBe(21); });
    it('computed reacts 7', () => { const s = createSignal(7); const c = createComputed(() => s.get() + 10); s.set(12); expect(c.get()).toBe(22); });
    it('computed reacts 8', () => { const s = createSignal(8); const c = createComputed(() => s.get() + 10); s.set(13); expect(c.get()).toBe(23); });
    it('computed reacts 9', () => { const s = createSignal(9); const c = createComputed(() => s.get() + 10); s.set(14); expect(c.get()).toBe(24); });
    it('computed reacts 10', () => { const s = createSignal(10); const c = createComputed(() => s.get() + 10); s.set(15); expect(c.get()).toBe(25); });
    it('computed reacts 11', () => { const s = createSignal(11); const c = createComputed(() => s.get() + 10); s.set(16); expect(c.get()).toBe(26); });
    it('computed reacts 12', () => { const s = createSignal(12); const c = createComputed(() => s.get() + 10); s.set(17); expect(c.get()).toBe(27); });
    it('computed reacts 13', () => { const s = createSignal(13); const c = createComputed(() => s.get() + 10); s.set(18); expect(c.get()).toBe(28); });
    it('computed reacts 14', () => { const s = createSignal(14); const c = createComputed(() => s.get() + 10); s.set(19); expect(c.get()).toBe(29); });
    it('computed reacts 15', () => { const s = createSignal(15); const c = createComputed(() => s.get() + 10); s.set(20); expect(c.get()).toBe(30); });
    it('computed reacts 16', () => { const s = createSignal(16); const c = createComputed(() => s.get() + 10); s.set(21); expect(c.get()).toBe(31); });
    it('computed reacts 17', () => { const s = createSignal(17); const c = createComputed(() => s.get() + 10); s.set(22); expect(c.get()).toBe(32); });
    it('computed reacts 18', () => { const s = createSignal(18); const c = createComputed(() => s.get() + 10); s.set(23); expect(c.get()).toBe(33); });
    it('computed reacts 19', () => { const s = createSignal(19); const c = createComputed(() => s.get() + 10); s.set(24); expect(c.get()).toBe(34); });
    it('computed reacts 20', () => { const s = createSignal(20); const c = createComputed(() => s.get() + 10); s.set(25); expect(c.get()).toBe(35); });
    it('computed reacts 21', () => { const s = createSignal(21); const c = createComputed(() => s.get() + 10); s.set(26); expect(c.get()).toBe(36); });
    it('computed reacts 22', () => { const s = createSignal(22); const c = createComputed(() => s.get() + 10); s.set(27); expect(c.get()).toBe(37); });
    it('computed reacts 23', () => { const s = createSignal(23); const c = createComputed(() => s.get() + 10); s.set(28); expect(c.get()).toBe(38); });
    it('computed reacts 24', () => { const s = createSignal(24); const c = createComputed(() => s.get() + 10); s.set(29); expect(c.get()).toBe(39); });
    it('computed reacts 25', () => { const s = createSignal(25); const c = createComputed(() => s.get() + 10); s.set(30); expect(c.get()).toBe(40); });
    it('computed reacts 26', () => { const s = createSignal(26); const c = createComputed(() => s.get() + 10); s.set(31); expect(c.get()).toBe(41); });
    it('computed reacts 27', () => { const s = createSignal(27); const c = createComputed(() => s.get() + 10); s.set(32); expect(c.get()).toBe(42); });
    it('computed reacts 28', () => { const s = createSignal(28); const c = createComputed(() => s.get() + 10); s.set(33); expect(c.get()).toBe(43); });
    it('computed reacts 29', () => { const s = createSignal(29); const c = createComputed(() => s.get() + 10); s.set(34); expect(c.get()).toBe(44); });
    it('computed reacts 30', () => { const s = createSignal(30); const c = createComputed(() => s.get() + 10); s.set(35); expect(c.get()).toBe(45); });
    it('computed reacts 31', () => { const s = createSignal(31); const c = createComputed(() => s.get() + 10); s.set(36); expect(c.get()).toBe(46); });
    it('computed reacts 32', () => { const s = createSignal(32); const c = createComputed(() => s.get() + 10); s.set(37); expect(c.get()).toBe(47); });
    it('computed reacts 33', () => { const s = createSignal(33); const c = createComputed(() => s.get() + 10); s.set(38); expect(c.get()).toBe(48); });
    it('computed reacts 34', () => { const s = createSignal(34); const c = createComputed(() => s.get() + 10); s.set(39); expect(c.get()).toBe(49); });
    it('computed reacts 35', () => { const s = createSignal(35); const c = createComputed(() => s.get() + 10); s.set(40); expect(c.get()).toBe(50); });
    it('computed reacts 36', () => { const s = createSignal(36); const c = createComputed(() => s.get() + 10); s.set(41); expect(c.get()).toBe(51); });
    it('computed reacts 37', () => { const s = createSignal(37); const c = createComputed(() => s.get() + 10); s.set(42); expect(c.get()).toBe(52); });
    it('computed reacts 38', () => { const s = createSignal(38); const c = createComputed(() => s.get() + 10); s.set(43); expect(c.get()).toBe(53); });
    it('computed reacts 39', () => { const s = createSignal(39); const c = createComputed(() => s.get() + 10); s.set(44); expect(c.get()).toBe(54); });
    it('computed reacts 40', () => { const s = createSignal(40); const c = createComputed(() => s.get() + 10); s.set(45); expect(c.get()).toBe(55); });
    it('computed reacts 41', () => { const s = createSignal(41); const c = createComputed(() => s.get() + 10); s.set(46); expect(c.get()).toBe(56); });
    it('computed reacts 42', () => { const s = createSignal(42); const c = createComputed(() => s.get() + 10); s.set(47); expect(c.get()).toBe(57); });
    it('computed reacts 43', () => { const s = createSignal(43); const c = createComputed(() => s.get() + 10); s.set(48); expect(c.get()).toBe(58); });
    it('computed reacts 44', () => { const s = createSignal(44); const c = createComputed(() => s.get() + 10); s.set(49); expect(c.get()).toBe(59); });
    it('computed reacts 45', () => { const s = createSignal(45); const c = createComputed(() => s.get() + 10); s.set(50); expect(c.get()).toBe(60); });
    it('computed reacts 46', () => { const s = createSignal(46); const c = createComputed(() => s.get() + 10); s.set(51); expect(c.get()).toBe(61); });
    it('computed reacts 47', () => { const s = createSignal(47); const c = createComputed(() => s.get() + 10); s.set(52); expect(c.get()).toBe(62); });
    it('computed reacts 48', () => { const s = createSignal(48); const c = createComputed(() => s.get() + 10); s.set(53); expect(c.get()).toBe(63); });
    it('computed reacts 49', () => { const s = createSignal(49); const c = createComputed(() => s.get() + 10); s.set(54); expect(c.get()).toBe(64); });
    it('computed reacts 50', () => { const s = createSignal(50); const c = createComputed(() => s.get() + 10); s.set(55); expect(c.get()).toBe(65); });
    it('computed reacts 51', () => { const s = createSignal(51); const c = createComputed(() => s.get() + 10); s.set(56); expect(c.get()).toBe(66); });
    it('computed reacts 52', () => { const s = createSignal(52); const c = createComputed(() => s.get() + 10); s.set(57); expect(c.get()).toBe(67); });
    it('computed reacts 53', () => { const s = createSignal(53); const c = createComputed(() => s.get() + 10); s.set(58); expect(c.get()).toBe(68); });
    it('computed reacts 54', () => { const s = createSignal(54); const c = createComputed(() => s.get() + 10); s.set(59); expect(c.get()).toBe(69); });
    it('computed reacts 55', () => { const s = createSignal(55); const c = createComputed(() => s.get() + 10); s.set(60); expect(c.get()).toBe(70); });
    it('computed reacts 56', () => { const s = createSignal(56); const c = createComputed(() => s.get() + 10); s.set(61); expect(c.get()).toBe(71); });
    it('computed reacts 57', () => { const s = createSignal(57); const c = createComputed(() => s.get() + 10); s.set(62); expect(c.get()).toBe(72); });
    it('computed reacts 58', () => { const s = createSignal(58); const c = createComputed(() => s.get() + 10); s.set(63); expect(c.get()).toBe(73); });
    it('computed reacts 59', () => { const s = createSignal(59); const c = createComputed(() => s.get() + 10); s.set(64); expect(c.get()).toBe(74); });
    it('computed reacts 60', () => { const s = createSignal(60); const c = createComputed(() => s.get() + 10); s.set(65); expect(c.get()).toBe(75); });
    it('computed reacts 61', () => { const s = createSignal(61); const c = createComputed(() => s.get() + 10); s.set(66); expect(c.get()).toBe(76); });
    it('computed reacts 62', () => { const s = createSignal(62); const c = createComputed(() => s.get() + 10); s.set(67); expect(c.get()).toBe(77); });
    it('computed reacts 63', () => { const s = createSignal(63); const c = createComputed(() => s.get() + 10); s.set(68); expect(c.get()).toBe(78); });
    it('computed reacts 64', () => { const s = createSignal(64); const c = createComputed(() => s.get() + 10); s.set(69); expect(c.get()).toBe(79); });
    it('computed reacts 65', () => { const s = createSignal(65); const c = createComputed(() => s.get() + 10); s.set(70); expect(c.get()).toBe(80); });
    it('computed reacts 66', () => { const s = createSignal(66); const c = createComputed(() => s.get() + 10); s.set(71); expect(c.get()).toBe(81); });
    it('computed reacts 67', () => { const s = createSignal(67); const c = createComputed(() => s.get() + 10); s.set(72); expect(c.get()).toBe(82); });
    it('computed reacts 68', () => { const s = createSignal(68); const c = createComputed(() => s.get() + 10); s.set(73); expect(c.get()).toBe(83); });
    it('computed reacts 69', () => { const s = createSignal(69); const c = createComputed(() => s.get() + 10); s.set(74); expect(c.get()).toBe(84); });
    it('computed reacts 70', () => { const s = createSignal(70); const c = createComputed(() => s.get() + 10); s.set(75); expect(c.get()).toBe(85); });
    it('computed reacts 71', () => { const s = createSignal(71); const c = createComputed(() => s.get() + 10); s.set(76); expect(c.get()).toBe(86); });
    it('computed reacts 72', () => { const s = createSignal(72); const c = createComputed(() => s.get() + 10); s.set(77); expect(c.get()).toBe(87); });
    it('computed reacts 73', () => { const s = createSignal(73); const c = createComputed(() => s.get() + 10); s.set(78); expect(c.get()).toBe(88); });
    it('computed reacts 74', () => { const s = createSignal(74); const c = createComputed(() => s.get() + 10); s.set(79); expect(c.get()).toBe(89); });
    it('computed reacts 75', () => { const s = createSignal(75); const c = createComputed(() => s.get() + 10); s.set(80); expect(c.get()).toBe(90); });
    it('computed reacts 76', () => { const s = createSignal(76); const c = createComputed(() => s.get() + 10); s.set(81); expect(c.get()).toBe(91); });
    it('computed reacts 77', () => { const s = createSignal(77); const c = createComputed(() => s.get() + 10); s.set(82); expect(c.get()).toBe(92); });
    it('computed reacts 78', () => { const s = createSignal(78); const c = createComputed(() => s.get() + 10); s.set(83); expect(c.get()).toBe(93); });
    it('computed reacts 79', () => { const s = createSignal(79); const c = createComputed(() => s.get() + 10); s.set(84); expect(c.get()).toBe(94); });
    it('computed reacts 80', () => { const s = createSignal(80); const c = createComputed(() => s.get() + 10); s.set(85); expect(c.get()).toBe(95); });
    it('computed reacts 81', () => { const s = createSignal(81); const c = createComputed(() => s.get() + 10); s.set(86); expect(c.get()).toBe(96); });
    it('computed reacts 82', () => { const s = createSignal(82); const c = createComputed(() => s.get() + 10); s.set(87); expect(c.get()).toBe(97); });
    it('computed reacts 83', () => { const s = createSignal(83); const c = createComputed(() => s.get() + 10); s.set(88); expect(c.get()).toBe(98); });
    it('computed reacts 84', () => { const s = createSignal(84); const c = createComputed(() => s.get() + 10); s.set(89); expect(c.get()).toBe(99); });
    it('computed reacts 85', () => { const s = createSignal(85); const c = createComputed(() => s.get() + 10); s.set(90); expect(c.get()).toBe(100); });
    it('computed reacts 86', () => { const s = createSignal(86); const c = createComputed(() => s.get() + 10); s.set(91); expect(c.get()).toBe(101); });
    it('computed reacts 87', () => { const s = createSignal(87); const c = createComputed(() => s.get() + 10); s.set(92); expect(c.get()).toBe(102); });
    it('computed reacts 88', () => { const s = createSignal(88); const c = createComputed(() => s.get() + 10); s.set(93); expect(c.get()).toBe(103); });
    it('computed reacts 89', () => { const s = createSignal(89); const c = createComputed(() => s.get() + 10); s.set(94); expect(c.get()).toBe(104); });
    it('computed reacts 90', () => { const s = createSignal(90); const c = createComputed(() => s.get() + 10); s.set(95); expect(c.get()).toBe(105); });
    it('computed reacts 91', () => { const s = createSignal(91); const c = createComputed(() => s.get() + 10); s.set(96); expect(c.get()).toBe(106); });
    it('computed reacts 92', () => { const s = createSignal(92); const c = createComputed(() => s.get() + 10); s.set(97); expect(c.get()).toBe(107); });
    it('computed reacts 93', () => { const s = createSignal(93); const c = createComputed(() => s.get() + 10); s.set(98); expect(c.get()).toBe(108); });
    it('computed reacts 94', () => { const s = createSignal(94); const c = createComputed(() => s.get() + 10); s.set(99); expect(c.get()).toBe(109); });
    it('computed reacts 95', () => { const s = createSignal(95); const c = createComputed(() => s.get() + 10); s.set(100); expect(c.get()).toBe(110); });
    it('computed reacts 96', () => { const s = createSignal(96); const c = createComputed(() => s.get() + 10); s.set(101); expect(c.get()).toBe(111); });
    it('computed reacts 97', () => { const s = createSignal(97); const c = createComputed(() => s.get() + 10); s.set(102); expect(c.get()).toBe(112); });
    it('computed reacts 98', () => { const s = createSignal(98); const c = createComputed(() => s.get() + 10); s.set(103); expect(c.get()).toBe(113); });
    it('computed reacts 99', () => { const s = createSignal(99); const c = createComputed(() => s.get() + 10); s.set(104); expect(c.get()).toBe(114); });
    it('computed chain', () => { const a = createSignal(2); const b = createComputed(() => a.get()*3); const c = createComputed(() => b.get()+1); expect(c.get()).toBe(7); });
  });
  describe('createEffect', () => {
    it('effect runs 0', () => { let count = 0; const s = createSignal(0); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 1', () => { let count = 0; const s = createSignal(1); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 2', () => { let count = 0; const s = createSignal(2); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 3', () => { let count = 0; const s = createSignal(3); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 4', () => { let count = 0; const s = createSignal(4); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 5', () => { let count = 0; const s = createSignal(5); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 6', () => { let count = 0; const s = createSignal(6); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 7', () => { let count = 0; const s = createSignal(7); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 8', () => { let count = 0; const s = createSignal(8); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 9', () => { let count = 0; const s = createSignal(9); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 10', () => { let count = 0; const s = createSignal(10); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 11', () => { let count = 0; const s = createSignal(11); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 12', () => { let count = 0; const s = createSignal(12); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 13', () => { let count = 0; const s = createSignal(13); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 14', () => { let count = 0; const s = createSignal(14); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 15', () => { let count = 0; const s = createSignal(15); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 16', () => { let count = 0; const s = createSignal(16); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 17', () => { let count = 0; const s = createSignal(17); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 18', () => { let count = 0; const s = createSignal(18); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 19', () => { let count = 0; const s = createSignal(19); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 20', () => { let count = 0; const s = createSignal(20); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 21', () => { let count = 0; const s = createSignal(21); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 22', () => { let count = 0; const s = createSignal(22); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 23', () => { let count = 0; const s = createSignal(23); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 24', () => { let count = 0; const s = createSignal(24); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 25', () => { let count = 0; const s = createSignal(25); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 26', () => { let count = 0; const s = createSignal(26); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 27', () => { let count = 0; const s = createSignal(27); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 28', () => { let count = 0; const s = createSignal(28); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 29', () => { let count = 0; const s = createSignal(29); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 30', () => { let count = 0; const s = createSignal(30); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 31', () => { let count = 0; const s = createSignal(31); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 32', () => { let count = 0; const s = createSignal(32); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 33', () => { let count = 0; const s = createSignal(33); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 34', () => { let count = 0; const s = createSignal(34); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 35', () => { let count = 0; const s = createSignal(35); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 36', () => { let count = 0; const s = createSignal(36); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 37', () => { let count = 0; const s = createSignal(37); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 38', () => { let count = 0; const s = createSignal(38); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 39', () => { let count = 0; const s = createSignal(39); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 40', () => { let count = 0; const s = createSignal(40); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 41', () => { let count = 0; const s = createSignal(41); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 42', () => { let count = 0; const s = createSignal(42); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 43', () => { let count = 0; const s = createSignal(43); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 44', () => { let count = 0; const s = createSignal(44); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 45', () => { let count = 0; const s = createSignal(45); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 46', () => { let count = 0; const s = createSignal(46); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 47', () => { let count = 0; const s = createSignal(47); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 48', () => { let count = 0; const s = createSignal(48); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 49', () => { let count = 0; const s = createSignal(49); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 50', () => { let count = 0; const s = createSignal(50); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 51', () => { let count = 0; const s = createSignal(51); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 52', () => { let count = 0; const s = createSignal(52); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 53', () => { let count = 0; const s = createSignal(53); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 54', () => { let count = 0; const s = createSignal(54); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 55', () => { let count = 0; const s = createSignal(55); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 56', () => { let count = 0; const s = createSignal(56); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 57', () => { let count = 0; const s = createSignal(57); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 58', () => { let count = 0; const s = createSignal(58); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 59', () => { let count = 0; const s = createSignal(59); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 60', () => { let count = 0; const s = createSignal(60); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 61', () => { let count = 0; const s = createSignal(61); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 62', () => { let count = 0; const s = createSignal(62); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 63', () => { let count = 0; const s = createSignal(63); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 64', () => { let count = 0; const s = createSignal(64); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 65', () => { let count = 0; const s = createSignal(65); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 66', () => { let count = 0; const s = createSignal(66); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 67', () => { let count = 0; const s = createSignal(67); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 68', () => { let count = 0; const s = createSignal(68); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 69', () => { let count = 0; const s = createSignal(69); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 70', () => { let count = 0; const s = createSignal(70); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 71', () => { let count = 0; const s = createSignal(71); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 72', () => { let count = 0; const s = createSignal(72); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 73', () => { let count = 0; const s = createSignal(73); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 74', () => { let count = 0; const s = createSignal(74); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 75', () => { let count = 0; const s = createSignal(75); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 76', () => { let count = 0; const s = createSignal(76); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 77', () => { let count = 0; const s = createSignal(77); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 78', () => { let count = 0; const s = createSignal(78); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 79', () => { let count = 0; const s = createSignal(79); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 80', () => { let count = 0; const s = createSignal(80); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 81', () => { let count = 0; const s = createSignal(81); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 82', () => { let count = 0; const s = createSignal(82); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 83', () => { let count = 0; const s = createSignal(83); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 84', () => { let count = 0; const s = createSignal(84); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 85', () => { let count = 0; const s = createSignal(85); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 86', () => { let count = 0; const s = createSignal(86); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 87', () => { let count = 0; const s = createSignal(87); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 88', () => { let count = 0; const s = createSignal(88); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 89', () => { let count = 0; const s = createSignal(89); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 90', () => { let count = 0; const s = createSignal(90); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 91', () => { let count = 0; const s = createSignal(91); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 92', () => { let count = 0; const s = createSignal(92); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 93', () => { let count = 0; const s = createSignal(93); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 94', () => { let count = 0; const s = createSignal(94); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 95', () => { let count = 0; const s = createSignal(95); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 96', () => { let count = 0; const s = createSignal(96); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 97', () => { let count = 0; const s = createSignal(97); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 98', () => { let count = 0; const s = createSignal(98); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect runs 99', () => { let count = 0; const s = createSignal(99); createEffect(() => { s.get(); count++; }); expect(count).toBeGreaterThan(0); });
    it('effect returns dispose', () => { const e = createEffect(() => {}); expect(typeof e.dispose).toBe('function'); });
  });
  describe('createMemo', () => {
    it('memo 0', () => { const s = createSignal(0); const m = createMemo(() => s.get() * 3); expect(m()).toBe(0); });
    it('memo 1', () => { const s = createSignal(1); const m = createMemo(() => s.get() * 3); expect(m()).toBe(3); });
    it('memo 2', () => { const s = createSignal(2); const m = createMemo(() => s.get() * 3); expect(m()).toBe(6); });
    it('memo 3', () => { const s = createSignal(3); const m = createMemo(() => s.get() * 3); expect(m()).toBe(9); });
    it('memo 4', () => { const s = createSignal(4); const m = createMemo(() => s.get() * 3); expect(m()).toBe(12); });
    it('memo 5', () => { const s = createSignal(5); const m = createMemo(() => s.get() * 3); expect(m()).toBe(15); });
    it('memo 6', () => { const s = createSignal(6); const m = createMemo(() => s.get() * 3); expect(m()).toBe(18); });
    it('memo 7', () => { const s = createSignal(7); const m = createMemo(() => s.get() * 3); expect(m()).toBe(21); });
    it('memo 8', () => { const s = createSignal(8); const m = createMemo(() => s.get() * 3); expect(m()).toBe(24); });
    it('memo 9', () => { const s = createSignal(9); const m = createMemo(() => s.get() * 3); expect(m()).toBe(27); });
    it('memo 10', () => { const s = createSignal(10); const m = createMemo(() => s.get() * 3); expect(m()).toBe(30); });
    it('memo 11', () => { const s = createSignal(11); const m = createMemo(() => s.get() * 3); expect(m()).toBe(33); });
    it('memo 12', () => { const s = createSignal(12); const m = createMemo(() => s.get() * 3); expect(m()).toBe(36); });
    it('memo 13', () => { const s = createSignal(13); const m = createMemo(() => s.get() * 3); expect(m()).toBe(39); });
    it('memo 14', () => { const s = createSignal(14); const m = createMemo(() => s.get() * 3); expect(m()).toBe(42); });
    it('memo 15', () => { const s = createSignal(15); const m = createMemo(() => s.get() * 3); expect(m()).toBe(45); });
    it('memo 16', () => { const s = createSignal(16); const m = createMemo(() => s.get() * 3); expect(m()).toBe(48); });
    it('memo 17', () => { const s = createSignal(17); const m = createMemo(() => s.get() * 3); expect(m()).toBe(51); });
    it('memo 18', () => { const s = createSignal(18); const m = createMemo(() => s.get() * 3); expect(m()).toBe(54); });
    it('memo 19', () => { const s = createSignal(19); const m = createMemo(() => s.get() * 3); expect(m()).toBe(57); });
    it('memo 20', () => { const s = createSignal(20); const m = createMemo(() => s.get() * 3); expect(m()).toBe(60); });
    it('memo 21', () => { const s = createSignal(21); const m = createMemo(() => s.get() * 3); expect(m()).toBe(63); });
    it('memo 22', () => { const s = createSignal(22); const m = createMemo(() => s.get() * 3); expect(m()).toBe(66); });
    it('memo 23', () => { const s = createSignal(23); const m = createMemo(() => s.get() * 3); expect(m()).toBe(69); });
    it('memo 24', () => { const s = createSignal(24); const m = createMemo(() => s.get() * 3); expect(m()).toBe(72); });
    it('memo 25', () => { const s = createSignal(25); const m = createMemo(() => s.get() * 3); expect(m()).toBe(75); });
    it('memo 26', () => { const s = createSignal(26); const m = createMemo(() => s.get() * 3); expect(m()).toBe(78); });
    it('memo 27', () => { const s = createSignal(27); const m = createMemo(() => s.get() * 3); expect(m()).toBe(81); });
    it('memo 28', () => { const s = createSignal(28); const m = createMemo(() => s.get() * 3); expect(m()).toBe(84); });
    it('memo 29', () => { const s = createSignal(29); const m = createMemo(() => s.get() * 3); expect(m()).toBe(87); });
    it('memo 30', () => { const s = createSignal(30); const m = createMemo(() => s.get() * 3); expect(m()).toBe(90); });
    it('memo 31', () => { const s = createSignal(31); const m = createMemo(() => s.get() * 3); expect(m()).toBe(93); });
    it('memo 32', () => { const s = createSignal(32); const m = createMemo(() => s.get() * 3); expect(m()).toBe(96); });
    it('memo 33', () => { const s = createSignal(33); const m = createMemo(() => s.get() * 3); expect(m()).toBe(99); });
    it('memo 34', () => { const s = createSignal(34); const m = createMemo(() => s.get() * 3); expect(m()).toBe(102); });
    it('memo 35', () => { const s = createSignal(35); const m = createMemo(() => s.get() * 3); expect(m()).toBe(105); });
    it('memo 36', () => { const s = createSignal(36); const m = createMemo(() => s.get() * 3); expect(m()).toBe(108); });
    it('memo 37', () => { const s = createSignal(37); const m = createMemo(() => s.get() * 3); expect(m()).toBe(111); });
    it('memo 38', () => { const s = createSignal(38); const m = createMemo(() => s.get() * 3); expect(m()).toBe(114); });
    it('memo 39', () => { const s = createSignal(39); const m = createMemo(() => s.get() * 3); expect(m()).toBe(117); });
    it('memo 40', () => { const s = createSignal(40); const m = createMemo(() => s.get() * 3); expect(m()).toBe(120); });
    it('memo 41', () => { const s = createSignal(41); const m = createMemo(() => s.get() * 3); expect(m()).toBe(123); });
    it('memo 42', () => { const s = createSignal(42); const m = createMemo(() => s.get() * 3); expect(m()).toBe(126); });
    it('memo 43', () => { const s = createSignal(43); const m = createMemo(() => s.get() * 3); expect(m()).toBe(129); });
    it('memo 44', () => { const s = createSignal(44); const m = createMemo(() => s.get() * 3); expect(m()).toBe(132); });
    it('memo 45', () => { const s = createSignal(45); const m = createMemo(() => s.get() * 3); expect(m()).toBe(135); });
    it('memo 46', () => { const s = createSignal(46); const m = createMemo(() => s.get() * 3); expect(m()).toBe(138); });
    it('memo 47', () => { const s = createSignal(47); const m = createMemo(() => s.get() * 3); expect(m()).toBe(141); });
    it('memo 48', () => { const s = createSignal(48); const m = createMemo(() => s.get() * 3); expect(m()).toBe(144); });
    it('memo 49', () => { const s = createSignal(49); const m = createMemo(() => s.get() * 3); expect(m()).toBe(147); });
    it('memo 50', () => { const s = createSignal(50); const m = createMemo(() => s.get() * 3); expect(m()).toBe(150); });
    it('memo 51', () => { const s = createSignal(51); const m = createMemo(() => s.get() * 3); expect(m()).toBe(153); });
    it('memo 52', () => { const s = createSignal(52); const m = createMemo(() => s.get() * 3); expect(m()).toBe(156); });
    it('memo 53', () => { const s = createSignal(53); const m = createMemo(() => s.get() * 3); expect(m()).toBe(159); });
    it('memo 54', () => { const s = createSignal(54); const m = createMemo(() => s.get() * 3); expect(m()).toBe(162); });
    it('memo 55', () => { const s = createSignal(55); const m = createMemo(() => s.get() * 3); expect(m()).toBe(165); });
    it('memo 56', () => { const s = createSignal(56); const m = createMemo(() => s.get() * 3); expect(m()).toBe(168); });
    it('memo 57', () => { const s = createSignal(57); const m = createMemo(() => s.get() * 3); expect(m()).toBe(171); });
    it('memo 58', () => { const s = createSignal(58); const m = createMemo(() => s.get() * 3); expect(m()).toBe(174); });
    it('memo 59', () => { const s = createSignal(59); const m = createMemo(() => s.get() * 3); expect(m()).toBe(177); });
    it('memo 60', () => { const s = createSignal(60); const m = createMemo(() => s.get() * 3); expect(m()).toBe(180); });
    it('memo 61', () => { const s = createSignal(61); const m = createMemo(() => s.get() * 3); expect(m()).toBe(183); });
    it('memo 62', () => { const s = createSignal(62); const m = createMemo(() => s.get() * 3); expect(m()).toBe(186); });
    it('memo 63', () => { const s = createSignal(63); const m = createMemo(() => s.get() * 3); expect(m()).toBe(189); });
    it('memo 64', () => { const s = createSignal(64); const m = createMemo(() => s.get() * 3); expect(m()).toBe(192); });
    it('memo 65', () => { const s = createSignal(65); const m = createMemo(() => s.get() * 3); expect(m()).toBe(195); });
    it('memo 66', () => { const s = createSignal(66); const m = createMemo(() => s.get() * 3); expect(m()).toBe(198); });
    it('memo 67', () => { const s = createSignal(67); const m = createMemo(() => s.get() * 3); expect(m()).toBe(201); });
    it('memo 68', () => { const s = createSignal(68); const m = createMemo(() => s.get() * 3); expect(m()).toBe(204); });
    it('memo 69', () => { const s = createSignal(69); const m = createMemo(() => s.get() * 3); expect(m()).toBe(207); });
    it('memo 70', () => { const s = createSignal(70); const m = createMemo(() => s.get() * 3); expect(m()).toBe(210); });
    it('memo 71', () => { const s = createSignal(71); const m = createMemo(() => s.get() * 3); expect(m()).toBe(213); });
    it('memo 72', () => { const s = createSignal(72); const m = createMemo(() => s.get() * 3); expect(m()).toBe(216); });
    it('memo 73', () => { const s = createSignal(73); const m = createMemo(() => s.get() * 3); expect(m()).toBe(219); });
    it('memo 74', () => { const s = createSignal(74); const m = createMemo(() => s.get() * 3); expect(m()).toBe(222); });
    it('memo 75', () => { const s = createSignal(75); const m = createMemo(() => s.get() * 3); expect(m()).toBe(225); });
    it('memo 76', () => { const s = createSignal(76); const m = createMemo(() => s.get() * 3); expect(m()).toBe(228); });
    it('memo 77', () => { const s = createSignal(77); const m = createMemo(() => s.get() * 3); expect(m()).toBe(231); });
    it('memo 78', () => { const s = createSignal(78); const m = createMemo(() => s.get() * 3); expect(m()).toBe(234); });
    it('memo 79', () => { const s = createSignal(79); const m = createMemo(() => s.get() * 3); expect(m()).toBe(237); });
    it('memo 80', () => { const s = createSignal(80); const m = createMemo(() => s.get() * 3); expect(m()).toBe(240); });
    it('memo 81', () => { const s = createSignal(81); const m = createMemo(() => s.get() * 3); expect(m()).toBe(243); });
    it('memo 82', () => { const s = createSignal(82); const m = createMemo(() => s.get() * 3); expect(m()).toBe(246); });
    it('memo 83', () => { const s = createSignal(83); const m = createMemo(() => s.get() * 3); expect(m()).toBe(249); });
    it('memo 84', () => { const s = createSignal(84); const m = createMemo(() => s.get() * 3); expect(m()).toBe(252); });
    it('memo 85', () => { const s = createSignal(85); const m = createMemo(() => s.get() * 3); expect(m()).toBe(255); });
    it('memo 86', () => { const s = createSignal(86); const m = createMemo(() => s.get() * 3); expect(m()).toBe(258); });
    it('memo 87', () => { const s = createSignal(87); const m = createMemo(() => s.get() * 3); expect(m()).toBe(261); });
    it('memo 88', () => { const s = createSignal(88); const m = createMemo(() => s.get() * 3); expect(m()).toBe(264); });
    it('memo 89', () => { const s = createSignal(89); const m = createMemo(() => s.get() * 3); expect(m()).toBe(267); });
    it('memo 90', () => { const s = createSignal(90); const m = createMemo(() => s.get() * 3); expect(m()).toBe(270); });
    it('memo 91', () => { const s = createSignal(91); const m = createMemo(() => s.get() * 3); expect(m()).toBe(273); });
    it('memo 92', () => { const s = createSignal(92); const m = createMemo(() => s.get() * 3); expect(m()).toBe(276); });
    it('memo 93', () => { const s = createSignal(93); const m = createMemo(() => s.get() * 3); expect(m()).toBe(279); });
    it('memo 94', () => { const s = createSignal(94); const m = createMemo(() => s.get() * 3); expect(m()).toBe(282); });
    it('memo 95', () => { const s = createSignal(95); const m = createMemo(() => s.get() * 3); expect(m()).toBe(285); });
    it('memo 96', () => { const s = createSignal(96); const m = createMemo(() => s.get() * 3); expect(m()).toBe(288); });
    it('memo 97', () => { const s = createSignal(97); const m = createMemo(() => s.get() * 3); expect(m()).toBe(291); });
    it('memo 98', () => { const s = createSignal(98); const m = createMemo(() => s.get() * 3); expect(m()).toBe(294); });
    it('memo 99', () => { const s = createSignal(99); const m = createMemo(() => s.get() * 3); expect(m()).toBe(297); });
  });
  describe('untrack', () => {
    it('untrack 0', () => { const result = untrack(() => 0 + 0); expect(result).toBe(0); });
    it('untrack 1', () => { const result = untrack(() => 1 + 1); expect(result).toBe(2); });
    it('untrack 2', () => { const result = untrack(() => 2 + 2); expect(result).toBe(4); });
    it('untrack 3', () => { const result = untrack(() => 3 + 3); expect(result).toBe(6); });
    it('untrack 4', () => { const result = untrack(() => 4 + 4); expect(result).toBe(8); });
    it('untrack 5', () => { const result = untrack(() => 5 + 5); expect(result).toBe(10); });
    it('untrack 6', () => { const result = untrack(() => 6 + 6); expect(result).toBe(12); });
    it('untrack 7', () => { const result = untrack(() => 7 + 7); expect(result).toBe(14); });
    it('untrack 8', () => { const result = untrack(() => 8 + 8); expect(result).toBe(16); });
    it('untrack 9', () => { const result = untrack(() => 9 + 9); expect(result).toBe(18); });
    it('untrack 10', () => { const result = untrack(() => 10 + 10); expect(result).toBe(20); });
    it('untrack 11', () => { const result = untrack(() => 11 + 11); expect(result).toBe(22); });
    it('untrack 12', () => { const result = untrack(() => 12 + 12); expect(result).toBe(24); });
    it('untrack 13', () => { const result = untrack(() => 13 + 13); expect(result).toBe(26); });
    it('untrack 14', () => { const result = untrack(() => 14 + 14); expect(result).toBe(28); });
    it('untrack 15', () => { const result = untrack(() => 15 + 15); expect(result).toBe(30); });
    it('untrack 16', () => { const result = untrack(() => 16 + 16); expect(result).toBe(32); });
    it('untrack 17', () => { const result = untrack(() => 17 + 17); expect(result).toBe(34); });
    it('untrack 18', () => { const result = untrack(() => 18 + 18); expect(result).toBe(36); });
    it('untrack 19', () => { const result = untrack(() => 19 + 19); expect(result).toBe(38); });
    it('untrack 20', () => { const result = untrack(() => 20 + 20); expect(result).toBe(40); });
    it('untrack 21', () => { const result = untrack(() => 21 + 21); expect(result).toBe(42); });
    it('untrack 22', () => { const result = untrack(() => 22 + 22); expect(result).toBe(44); });
    it('untrack 23', () => { const result = untrack(() => 23 + 23); expect(result).toBe(46); });
    it('untrack 24', () => { const result = untrack(() => 24 + 24); expect(result).toBe(48); });
    it('untrack 25', () => { const result = untrack(() => 25 + 25); expect(result).toBe(50); });
    it('untrack 26', () => { const result = untrack(() => 26 + 26); expect(result).toBe(52); });
    it('untrack 27', () => { const result = untrack(() => 27 + 27); expect(result).toBe(54); });
    it('untrack 28', () => { const result = untrack(() => 28 + 28); expect(result).toBe(56); });
    it('untrack 29', () => { const result = untrack(() => 29 + 29); expect(result).toBe(58); });
    it('untrack 30', () => { const result = untrack(() => 30 + 30); expect(result).toBe(60); });
    it('untrack 31', () => { const result = untrack(() => 31 + 31); expect(result).toBe(62); });
    it('untrack 32', () => { const result = untrack(() => 32 + 32); expect(result).toBe(64); });
    it('untrack 33', () => { const result = untrack(() => 33 + 33); expect(result).toBe(66); });
    it('untrack 34', () => { const result = untrack(() => 34 + 34); expect(result).toBe(68); });
    it('untrack 35', () => { const result = untrack(() => 35 + 35); expect(result).toBe(70); });
    it('untrack 36', () => { const result = untrack(() => 36 + 36); expect(result).toBe(72); });
    it('untrack 37', () => { const result = untrack(() => 37 + 37); expect(result).toBe(74); });
    it('untrack 38', () => { const result = untrack(() => 38 + 38); expect(result).toBe(76); });
    it('untrack 39', () => { const result = untrack(() => 39 + 39); expect(result).toBe(78); });
    it('untrack 40', () => { const result = untrack(() => 40 + 40); expect(result).toBe(80); });
    it('untrack 41', () => { const result = untrack(() => 41 + 41); expect(result).toBe(82); });
    it('untrack 42', () => { const result = untrack(() => 42 + 42); expect(result).toBe(84); });
    it('untrack 43', () => { const result = untrack(() => 43 + 43); expect(result).toBe(86); });
    it('untrack 44', () => { const result = untrack(() => 44 + 44); expect(result).toBe(88); });
    it('untrack 45', () => { const result = untrack(() => 45 + 45); expect(result).toBe(90); });
    it('untrack 46', () => { const result = untrack(() => 46 + 46); expect(result).toBe(92); });
    it('untrack 47', () => { const result = untrack(() => 47 + 47); expect(result).toBe(94); });
    it('untrack 48', () => { const result = untrack(() => 48 + 48); expect(result).toBe(96); });
    it('untrack 49', () => { const result = untrack(() => 49 + 49); expect(result).toBe(98); });
    it('untrack 50', () => { const result = untrack(() => 50 + 50); expect(result).toBe(100); });
    it('untrack 51', () => { const result = untrack(() => 51 + 51); expect(result).toBe(102); });
    it('untrack 52', () => { const result = untrack(() => 52 + 52); expect(result).toBe(104); });
    it('untrack 53', () => { const result = untrack(() => 53 + 53); expect(result).toBe(106); });
    it('untrack 54', () => { const result = untrack(() => 54 + 54); expect(result).toBe(108); });
    it('untrack 55', () => { const result = untrack(() => 55 + 55); expect(result).toBe(110); });
    it('untrack 56', () => { const result = untrack(() => 56 + 56); expect(result).toBe(112); });
    it('untrack 57', () => { const result = untrack(() => 57 + 57); expect(result).toBe(114); });
    it('untrack 58', () => { const result = untrack(() => 58 + 58); expect(result).toBe(116); });
    it('untrack 59', () => { const result = untrack(() => 59 + 59); expect(result).toBe(118); });
    it('untrack 60', () => { const result = untrack(() => 60 + 60); expect(result).toBe(120); });
    it('untrack 61', () => { const result = untrack(() => 61 + 61); expect(result).toBe(122); });
    it('untrack 62', () => { const result = untrack(() => 62 + 62); expect(result).toBe(124); });
    it('untrack 63', () => { const result = untrack(() => 63 + 63); expect(result).toBe(126); });
    it('untrack 64', () => { const result = untrack(() => 64 + 64); expect(result).toBe(128); });
    it('untrack 65', () => { const result = untrack(() => 65 + 65); expect(result).toBe(130); });
    it('untrack 66', () => { const result = untrack(() => 66 + 66); expect(result).toBe(132); });
    it('untrack 67', () => { const result = untrack(() => 67 + 67); expect(result).toBe(134); });
    it('untrack 68', () => { const result = untrack(() => 68 + 68); expect(result).toBe(136); });
    it('untrack 69', () => { const result = untrack(() => 69 + 69); expect(result).toBe(138); });
    it('untrack 70', () => { const result = untrack(() => 70 + 70); expect(result).toBe(140); });
    it('untrack 71', () => { const result = untrack(() => 71 + 71); expect(result).toBe(142); });
    it('untrack 72', () => { const result = untrack(() => 72 + 72); expect(result).toBe(144); });
    it('untrack 73', () => { const result = untrack(() => 73 + 73); expect(result).toBe(146); });
    it('untrack 74', () => { const result = untrack(() => 74 + 74); expect(result).toBe(148); });
    it('untrack 75', () => { const result = untrack(() => 75 + 75); expect(result).toBe(150); });
    it('untrack 76', () => { const result = untrack(() => 76 + 76); expect(result).toBe(152); });
    it('untrack 77', () => { const result = untrack(() => 77 + 77); expect(result).toBe(154); });
    it('untrack 78', () => { const result = untrack(() => 78 + 78); expect(result).toBe(156); });
    it('untrack 79', () => { const result = untrack(() => 79 + 79); expect(result).toBe(158); });
  });
  describe('isSignal', () => {
    it('isSignal signal 0', () => { const s = createSignal(0); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 1', () => { const s = createSignal(1); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 2', () => { const s = createSignal(2); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 3', () => { const s = createSignal(3); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 4', () => { const s = createSignal(4); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 5', () => { const s = createSignal(5); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 6', () => { const s = createSignal(6); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 7', () => { const s = createSignal(7); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 8', () => { const s = createSignal(8); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 9', () => { const s = createSignal(9); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 10', () => { const s = createSignal(10); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 11', () => { const s = createSignal(11); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 12', () => { const s = createSignal(12); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 13', () => { const s = createSignal(13); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 14', () => { const s = createSignal(14); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 15', () => { const s = createSignal(15); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 16', () => { const s = createSignal(16); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 17', () => { const s = createSignal(17); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 18', () => { const s = createSignal(18); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 19', () => { const s = createSignal(19); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 20', () => { const s = createSignal(20); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 21', () => { const s = createSignal(21); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 22', () => { const s = createSignal(22); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 23', () => { const s = createSignal(23); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 24', () => { const s = createSignal(24); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 25', () => { const s = createSignal(25); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 26', () => { const s = createSignal(26); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 27', () => { const s = createSignal(27); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 28', () => { const s = createSignal(28); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 29', () => { const s = createSignal(29); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 30', () => { const s = createSignal(30); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 31', () => { const s = createSignal(31); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 32', () => { const s = createSignal(32); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 33', () => { const s = createSignal(33); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 34', () => { const s = createSignal(34); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 35', () => { const s = createSignal(35); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 36', () => { const s = createSignal(36); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 37', () => { const s = createSignal(37); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 38', () => { const s = createSignal(38); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 39', () => { const s = createSignal(39); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 40', () => { const s = createSignal(40); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 41', () => { const s = createSignal(41); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 42', () => { const s = createSignal(42); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 43', () => { const s = createSignal(43); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 44', () => { const s = createSignal(44); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 45', () => { const s = createSignal(45); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 46', () => { const s = createSignal(46); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 47', () => { const s = createSignal(47); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 48', () => { const s = createSignal(48); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 49', () => { const s = createSignal(49); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 50', () => { const s = createSignal(50); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 51', () => { const s = createSignal(51); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 52', () => { const s = createSignal(52); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 53', () => { const s = createSignal(53); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 54', () => { const s = createSignal(54); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 55', () => { const s = createSignal(55); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 56', () => { const s = createSignal(56); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 57', () => { const s = createSignal(57); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 58', () => { const s = createSignal(58); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 59', () => { const s = createSignal(59); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 60', () => { const s = createSignal(60); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 61', () => { const s = createSignal(61); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 62', () => { const s = createSignal(62); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 63', () => { const s = createSignal(63); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 64', () => { const s = createSignal(64); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 65', () => { const s = createSignal(65); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 66', () => { const s = createSignal(66); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 67', () => { const s = createSignal(67); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 68', () => { const s = createSignal(68); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 69', () => { const s = createSignal(69); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 70', () => { const s = createSignal(70); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 71', () => { const s = createSignal(71); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 72', () => { const s = createSignal(72); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 73', () => { const s = createSignal(73); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 74', () => { const s = createSignal(74); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 75', () => { const s = createSignal(75); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 76', () => { const s = createSignal(76); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 77', () => { const s = createSignal(77); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 78', () => { const s = createSignal(78); expect(isSignal(s)).toBe(true); });
    it('isSignal signal 79', () => { const s = createSignal(79); expect(isSignal(s)).toBe(true); });
    it('isSignal non-signal', () => { expect(isSignal(42)).toBe(false); });
    it('isSignal null', () => { expect(isSignal(null)).toBe(false); });
  });
  describe('peek', () => {
    it('peek 0', () => { const s = createSignal(0); expect(peek(s)).toBe(0); });
    it('peek 1', () => { const s = createSignal(1); expect(peek(s)).toBe(1); });
    it('peek 2', () => { const s = createSignal(2); expect(peek(s)).toBe(2); });
    it('peek 3', () => { const s = createSignal(3); expect(peek(s)).toBe(3); });
    it('peek 4', () => { const s = createSignal(4); expect(peek(s)).toBe(4); });
    it('peek 5', () => { const s = createSignal(5); expect(peek(s)).toBe(5); });
    it('peek 6', () => { const s = createSignal(6); expect(peek(s)).toBe(6); });
    it('peek 7', () => { const s = createSignal(7); expect(peek(s)).toBe(7); });
    it('peek 8', () => { const s = createSignal(8); expect(peek(s)).toBe(8); });
    it('peek 9', () => { const s = createSignal(9); expect(peek(s)).toBe(9); });
    it('peek 10', () => { const s = createSignal(10); expect(peek(s)).toBe(10); });
    it('peek 11', () => { const s = createSignal(11); expect(peek(s)).toBe(11); });
    it('peek 12', () => { const s = createSignal(12); expect(peek(s)).toBe(12); });
    it('peek 13', () => { const s = createSignal(13); expect(peek(s)).toBe(13); });
    it('peek 14', () => { const s = createSignal(14); expect(peek(s)).toBe(14); });
    it('peek 15', () => { const s = createSignal(15); expect(peek(s)).toBe(15); });
    it('peek 16', () => { const s = createSignal(16); expect(peek(s)).toBe(16); });
    it('peek 17', () => { const s = createSignal(17); expect(peek(s)).toBe(17); });
    it('peek 18', () => { const s = createSignal(18); expect(peek(s)).toBe(18); });
    it('peek 19', () => { const s = createSignal(19); expect(peek(s)).toBe(19); });
    it('peek 20', () => { const s = createSignal(20); expect(peek(s)).toBe(20); });
    it('peek 21', () => { const s = createSignal(21); expect(peek(s)).toBe(21); });
    it('peek 22', () => { const s = createSignal(22); expect(peek(s)).toBe(22); });
    it('peek 23', () => { const s = createSignal(23); expect(peek(s)).toBe(23); });
    it('peek 24', () => { const s = createSignal(24); expect(peek(s)).toBe(24); });
    it('peek 25', () => { const s = createSignal(25); expect(peek(s)).toBe(25); });
    it('peek 26', () => { const s = createSignal(26); expect(peek(s)).toBe(26); });
    it('peek 27', () => { const s = createSignal(27); expect(peek(s)).toBe(27); });
    it('peek 28', () => { const s = createSignal(28); expect(peek(s)).toBe(28); });
    it('peek 29', () => { const s = createSignal(29); expect(peek(s)).toBe(29); });
    it('peek 30', () => { const s = createSignal(30); expect(peek(s)).toBe(30); });
    it('peek 31', () => { const s = createSignal(31); expect(peek(s)).toBe(31); });
    it('peek 32', () => { const s = createSignal(32); expect(peek(s)).toBe(32); });
    it('peek 33', () => { const s = createSignal(33); expect(peek(s)).toBe(33); });
    it('peek 34', () => { const s = createSignal(34); expect(peek(s)).toBe(34); });
    it('peek 35', () => { const s = createSignal(35); expect(peek(s)).toBe(35); });
    it('peek 36', () => { const s = createSignal(36); expect(peek(s)).toBe(36); });
    it('peek 37', () => { const s = createSignal(37); expect(peek(s)).toBe(37); });
    it('peek 38', () => { const s = createSignal(38); expect(peek(s)).toBe(38); });
    it('peek 39', () => { const s = createSignal(39); expect(peek(s)).toBe(39); });
    it('peek 40', () => { const s = createSignal(40); expect(peek(s)).toBe(40); });
    it('peek 41', () => { const s = createSignal(41); expect(peek(s)).toBe(41); });
    it('peek 42', () => { const s = createSignal(42); expect(peek(s)).toBe(42); });
    it('peek 43', () => { const s = createSignal(43); expect(peek(s)).toBe(43); });
    it('peek 44', () => { const s = createSignal(44); expect(peek(s)).toBe(44); });
    it('peek 45', () => { const s = createSignal(45); expect(peek(s)).toBe(45); });
    it('peek 46', () => { const s = createSignal(46); expect(peek(s)).toBe(46); });
    it('peek 47', () => { const s = createSignal(47); expect(peek(s)).toBe(47); });
    it('peek 48', () => { const s = createSignal(48); expect(peek(s)).toBe(48); });
    it('peek 49', () => { const s = createSignal(49); expect(peek(s)).toBe(49); });
    it('peek 50', () => { const s = createSignal(50); expect(peek(s)).toBe(50); });
    it('peek 51', () => { const s = createSignal(51); expect(peek(s)).toBe(51); });
    it('peek 52', () => { const s = createSignal(52); expect(peek(s)).toBe(52); });
    it('peek 53', () => { const s = createSignal(53); expect(peek(s)).toBe(53); });
    it('peek 54', () => { const s = createSignal(54); expect(peek(s)).toBe(54); });
    it('peek 55', () => { const s = createSignal(55); expect(peek(s)).toBe(55); });
    it('peek 56', () => { const s = createSignal(56); expect(peek(s)).toBe(56); });
    it('peek 57', () => { const s = createSignal(57); expect(peek(s)).toBe(57); });
    it('peek 58', () => { const s = createSignal(58); expect(peek(s)).toBe(58); });
    it('peek 59', () => { const s = createSignal(59); expect(peek(s)).toBe(59); });
    it('peek 60', () => { const s = createSignal(60); expect(peek(s)).toBe(60); });
    it('peek 61', () => { const s = createSignal(61); expect(peek(s)).toBe(61); });
    it('peek 62', () => { const s = createSignal(62); expect(peek(s)).toBe(62); });
    it('peek 63', () => { const s = createSignal(63); expect(peek(s)).toBe(63); });
    it('peek 64', () => { const s = createSignal(64); expect(peek(s)).toBe(64); });
    it('peek 65', () => { const s = createSignal(65); expect(peek(s)).toBe(65); });
    it('peek 66', () => { const s = createSignal(66); expect(peek(s)).toBe(66); });
    it('peek 67', () => { const s = createSignal(67); expect(peek(s)).toBe(67); });
    it('peek 68', () => { const s = createSignal(68); expect(peek(s)).toBe(68); });
    it('peek 69', () => { const s = createSignal(69); expect(peek(s)).toBe(69); });
    it('peek 70', () => { const s = createSignal(70); expect(peek(s)).toBe(70); });
    it('peek 71', () => { const s = createSignal(71); expect(peek(s)).toBe(71); });
    it('peek 72', () => { const s = createSignal(72); expect(peek(s)).toBe(72); });
    it('peek 73', () => { const s = createSignal(73); expect(peek(s)).toBe(73); });
    it('peek 74', () => { const s = createSignal(74); expect(peek(s)).toBe(74); });
    it('peek 75', () => { const s = createSignal(75); expect(peek(s)).toBe(75); });
    it('peek 76', () => { const s = createSignal(76); expect(peek(s)).toBe(76); });
    it('peek 77', () => { const s = createSignal(77); expect(peek(s)).toBe(77); });
    it('peek 78', () => { const s = createSignal(78); expect(peek(s)).toBe(78); });
    it('peek 79', () => { const s = createSignal(79); expect(peek(s)).toBe(79); });
  });
  describe('derive', () => {
    it('derive 0', () => { const s = createSignal(0); const d = derive(s, v => v * 2); expect(d.get()).toBe(0); });
    it('derive 1', () => { const s = createSignal(1); const d = derive(s, v => v * 2); expect(d.get()).toBe(2); });
    it('derive 2', () => { const s = createSignal(2); const d = derive(s, v => v * 2); expect(d.get()).toBe(4); });
    it('derive 3', () => { const s = createSignal(3); const d = derive(s, v => v * 2); expect(d.get()).toBe(6); });
    it('derive 4', () => { const s = createSignal(4); const d = derive(s, v => v * 2); expect(d.get()).toBe(8); });
    it('derive 5', () => { const s = createSignal(5); const d = derive(s, v => v * 2); expect(d.get()).toBe(10); });
    it('derive 6', () => { const s = createSignal(6); const d = derive(s, v => v * 2); expect(d.get()).toBe(12); });
    it('derive 7', () => { const s = createSignal(7); const d = derive(s, v => v * 2); expect(d.get()).toBe(14); });
    it('derive 8', () => { const s = createSignal(8); const d = derive(s, v => v * 2); expect(d.get()).toBe(16); });
    it('derive 9', () => { const s = createSignal(9); const d = derive(s, v => v * 2); expect(d.get()).toBe(18); });
    it('derive 10', () => { const s = createSignal(10); const d = derive(s, v => v * 2); expect(d.get()).toBe(20); });
    it('derive 11', () => { const s = createSignal(11); const d = derive(s, v => v * 2); expect(d.get()).toBe(22); });
    it('derive 12', () => { const s = createSignal(12); const d = derive(s, v => v * 2); expect(d.get()).toBe(24); });
    it('derive 13', () => { const s = createSignal(13); const d = derive(s, v => v * 2); expect(d.get()).toBe(26); });
    it('derive 14', () => { const s = createSignal(14); const d = derive(s, v => v * 2); expect(d.get()).toBe(28); });
    it('derive 15', () => { const s = createSignal(15); const d = derive(s, v => v * 2); expect(d.get()).toBe(30); });
    it('derive 16', () => { const s = createSignal(16); const d = derive(s, v => v * 2); expect(d.get()).toBe(32); });
    it('derive 17', () => { const s = createSignal(17); const d = derive(s, v => v * 2); expect(d.get()).toBe(34); });
    it('derive 18', () => { const s = createSignal(18); const d = derive(s, v => v * 2); expect(d.get()).toBe(36); });
    it('derive 19', () => { const s = createSignal(19); const d = derive(s, v => v * 2); expect(d.get()).toBe(38); });
    it('derive 20', () => { const s = createSignal(20); const d = derive(s, v => v * 2); expect(d.get()).toBe(40); });
    it('derive 21', () => { const s = createSignal(21); const d = derive(s, v => v * 2); expect(d.get()).toBe(42); });
    it('derive 22', () => { const s = createSignal(22); const d = derive(s, v => v * 2); expect(d.get()).toBe(44); });
    it('derive 23', () => { const s = createSignal(23); const d = derive(s, v => v * 2); expect(d.get()).toBe(46); });
    it('derive 24', () => { const s = createSignal(24); const d = derive(s, v => v * 2); expect(d.get()).toBe(48); });
    it('derive 25', () => { const s = createSignal(25); const d = derive(s, v => v * 2); expect(d.get()).toBe(50); });
    it('derive 26', () => { const s = createSignal(26); const d = derive(s, v => v * 2); expect(d.get()).toBe(52); });
    it('derive 27', () => { const s = createSignal(27); const d = derive(s, v => v * 2); expect(d.get()).toBe(54); });
    it('derive 28', () => { const s = createSignal(28); const d = derive(s, v => v * 2); expect(d.get()).toBe(56); });
    it('derive 29', () => { const s = createSignal(29); const d = derive(s, v => v * 2); expect(d.get()).toBe(58); });
    it('derive 30', () => { const s = createSignal(30); const d = derive(s, v => v * 2); expect(d.get()).toBe(60); });
    it('derive 31', () => { const s = createSignal(31); const d = derive(s, v => v * 2); expect(d.get()).toBe(62); });
    it('derive 32', () => { const s = createSignal(32); const d = derive(s, v => v * 2); expect(d.get()).toBe(64); });
    it('derive 33', () => { const s = createSignal(33); const d = derive(s, v => v * 2); expect(d.get()).toBe(66); });
    it('derive 34', () => { const s = createSignal(34); const d = derive(s, v => v * 2); expect(d.get()).toBe(68); });
    it('derive 35', () => { const s = createSignal(35); const d = derive(s, v => v * 2); expect(d.get()).toBe(70); });
    it('derive 36', () => { const s = createSignal(36); const d = derive(s, v => v * 2); expect(d.get()).toBe(72); });
    it('derive 37', () => { const s = createSignal(37); const d = derive(s, v => v * 2); expect(d.get()).toBe(74); });
    it('derive 38', () => { const s = createSignal(38); const d = derive(s, v => v * 2); expect(d.get()).toBe(76); });
    it('derive 39', () => { const s = createSignal(39); const d = derive(s, v => v * 2); expect(d.get()).toBe(78); });
    it('derive 40', () => { const s = createSignal(40); const d = derive(s, v => v * 2); expect(d.get()).toBe(80); });
    it('derive 41', () => { const s = createSignal(41); const d = derive(s, v => v * 2); expect(d.get()).toBe(82); });
    it('derive 42', () => { const s = createSignal(42); const d = derive(s, v => v * 2); expect(d.get()).toBe(84); });
    it('derive 43', () => { const s = createSignal(43); const d = derive(s, v => v * 2); expect(d.get()).toBe(86); });
    it('derive 44', () => { const s = createSignal(44); const d = derive(s, v => v * 2); expect(d.get()).toBe(88); });
    it('derive 45', () => { const s = createSignal(45); const d = derive(s, v => v * 2); expect(d.get()).toBe(90); });
    it('derive 46', () => { const s = createSignal(46); const d = derive(s, v => v * 2); expect(d.get()).toBe(92); });
    it('derive 47', () => { const s = createSignal(47); const d = derive(s, v => v * 2); expect(d.get()).toBe(94); });
    it('derive 48', () => { const s = createSignal(48); const d = derive(s, v => v * 2); expect(d.get()).toBe(96); });
    it('derive 49', () => { const s = createSignal(49); const d = derive(s, v => v * 2); expect(d.get()).toBe(98); });
    it('derive 50', () => { const s = createSignal(50); const d = derive(s, v => v * 2); expect(d.get()).toBe(100); });
    it('derive 51', () => { const s = createSignal(51); const d = derive(s, v => v * 2); expect(d.get()).toBe(102); });
    it('derive 52', () => { const s = createSignal(52); const d = derive(s, v => v * 2); expect(d.get()).toBe(104); });
    it('derive 53', () => { const s = createSignal(53); const d = derive(s, v => v * 2); expect(d.get()).toBe(106); });
    it('derive 54', () => { const s = createSignal(54); const d = derive(s, v => v * 2); expect(d.get()).toBe(108); });
    it('derive 55', () => { const s = createSignal(55); const d = derive(s, v => v * 2); expect(d.get()).toBe(110); });
    it('derive 56', () => { const s = createSignal(56); const d = derive(s, v => v * 2); expect(d.get()).toBe(112); });
    it('derive 57', () => { const s = createSignal(57); const d = derive(s, v => v * 2); expect(d.get()).toBe(114); });
    it('derive 58', () => { const s = createSignal(58); const d = derive(s, v => v * 2); expect(d.get()).toBe(116); });
    it('derive 59', () => { const s = createSignal(59); const d = derive(s, v => v * 2); expect(d.get()).toBe(118); });
    it('derive 60', () => { const s = createSignal(60); const d = derive(s, v => v * 2); expect(d.get()).toBe(120); });
    it('derive 61', () => { const s = createSignal(61); const d = derive(s, v => v * 2); expect(d.get()).toBe(122); });
    it('derive 62', () => { const s = createSignal(62); const d = derive(s, v => v * 2); expect(d.get()).toBe(124); });
    it('derive 63', () => { const s = createSignal(63); const d = derive(s, v => v * 2); expect(d.get()).toBe(126); });
    it('derive 64', () => { const s = createSignal(64); const d = derive(s, v => v * 2); expect(d.get()).toBe(128); });
    it('derive 65', () => { const s = createSignal(65); const d = derive(s, v => v * 2); expect(d.get()).toBe(130); });
    it('derive 66', () => { const s = createSignal(66); const d = derive(s, v => v * 2); expect(d.get()).toBe(132); });
    it('derive 67', () => { const s = createSignal(67); const d = derive(s, v => v * 2); expect(d.get()).toBe(134); });
    it('derive 68', () => { const s = createSignal(68); const d = derive(s, v => v * 2); expect(d.get()).toBe(136); });
    it('derive 69', () => { const s = createSignal(69); const d = derive(s, v => v * 2); expect(d.get()).toBe(138); });
    it('derive 70', () => { const s = createSignal(70); const d = derive(s, v => v * 2); expect(d.get()).toBe(140); });
    it('derive 71', () => { const s = createSignal(71); const d = derive(s, v => v * 2); expect(d.get()).toBe(142); });
    it('derive 72', () => { const s = createSignal(72); const d = derive(s, v => v * 2); expect(d.get()).toBe(144); });
    it('derive 73', () => { const s = createSignal(73); const d = derive(s, v => v * 2); expect(d.get()).toBe(146); });
    it('derive 74', () => { const s = createSignal(74); const d = derive(s, v => v * 2); expect(d.get()).toBe(148); });
    it('derive 75', () => { const s = createSignal(75); const d = derive(s, v => v * 2); expect(d.get()).toBe(150); });
    it('derive 76', () => { const s = createSignal(76); const d = derive(s, v => v * 2); expect(d.get()).toBe(152); });
    it('derive 77', () => { const s = createSignal(77); const d = derive(s, v => v * 2); expect(d.get()).toBe(154); });
    it('derive 78', () => { const s = createSignal(78); const d = derive(s, v => v * 2); expect(d.get()).toBe(156); });
    it('derive 79', () => { const s = createSignal(79); const d = derive(s, v => v * 2); expect(d.get()).toBe(158); });
  });
  describe('batch', () => {
    it('batch 0', () => { const s = createSignal(0); batch(() => { s.set(1); s.set(2); }); expect(s.get()).toBe(2); });
    it('batch 1', () => { const s = createSignal(1); batch(() => { s.set(2); s.set(3); }); expect(s.get()).toBe(3); });
    it('batch 2', () => { const s = createSignal(2); batch(() => { s.set(3); s.set(4); }); expect(s.get()).toBe(4); });
    it('batch 3', () => { const s = createSignal(3); batch(() => { s.set(4); s.set(5); }); expect(s.get()).toBe(5); });
    it('batch 4', () => { const s = createSignal(4); batch(() => { s.set(5); s.set(6); }); expect(s.get()).toBe(6); });
    it('batch 5', () => { const s = createSignal(5); batch(() => { s.set(6); s.set(7); }); expect(s.get()).toBe(7); });
    it('batch 6', () => { const s = createSignal(6); batch(() => { s.set(7); s.set(8); }); expect(s.get()).toBe(8); });
    it('batch 7', () => { const s = createSignal(7); batch(() => { s.set(8); s.set(9); }); expect(s.get()).toBe(9); });
    it('batch 8', () => { const s = createSignal(8); batch(() => { s.set(9); s.set(10); }); expect(s.get()).toBe(10); });
    it('batch 9', () => { const s = createSignal(9); batch(() => { s.set(10); s.set(11); }); expect(s.get()).toBe(11); });
    it('batch 10', () => { const s = createSignal(10); batch(() => { s.set(11); s.set(12); }); expect(s.get()).toBe(12); });
    it('batch 11', () => { const s = createSignal(11); batch(() => { s.set(12); s.set(13); }); expect(s.get()).toBe(13); });
    it('batch 12', () => { const s = createSignal(12); batch(() => { s.set(13); s.set(14); }); expect(s.get()).toBe(14); });
    it('batch 13', () => { const s = createSignal(13); batch(() => { s.set(14); s.set(15); }); expect(s.get()).toBe(15); });
    it('batch 14', () => { const s = createSignal(14); batch(() => { s.set(15); s.set(16); }); expect(s.get()).toBe(16); });
    it('batch 15', () => { const s = createSignal(15); batch(() => { s.set(16); s.set(17); }); expect(s.get()).toBe(17); });
    it('batch 16', () => { const s = createSignal(16); batch(() => { s.set(17); s.set(18); }); expect(s.get()).toBe(18); });
    it('batch 17', () => { const s = createSignal(17); batch(() => { s.set(18); s.set(19); }); expect(s.get()).toBe(19); });
    it('batch 18', () => { const s = createSignal(18); batch(() => { s.set(19); s.set(20); }); expect(s.get()).toBe(20); });
    it('batch 19', () => { const s = createSignal(19); batch(() => { s.set(20); s.set(21); }); expect(s.get()).toBe(21); });
    it('batch 20', () => { const s = createSignal(20); batch(() => { s.set(21); s.set(22); }); expect(s.get()).toBe(22); });
    it('batch 21', () => { const s = createSignal(21); batch(() => { s.set(22); s.set(23); }); expect(s.get()).toBe(23); });
    it('batch 22', () => { const s = createSignal(22); batch(() => { s.set(23); s.set(24); }); expect(s.get()).toBe(24); });
    it('batch 23', () => { const s = createSignal(23); batch(() => { s.set(24); s.set(25); }); expect(s.get()).toBe(25); });
    it('batch 24', () => { const s = createSignal(24); batch(() => { s.set(25); s.set(26); }); expect(s.get()).toBe(26); });
    it('batch 25', () => { const s = createSignal(25); batch(() => { s.set(26); s.set(27); }); expect(s.get()).toBe(27); });
    it('batch 26', () => { const s = createSignal(26); batch(() => { s.set(27); s.set(28); }); expect(s.get()).toBe(28); });
    it('batch 27', () => { const s = createSignal(27); batch(() => { s.set(28); s.set(29); }); expect(s.get()).toBe(29); });
    it('batch 28', () => { const s = createSignal(28); batch(() => { s.set(29); s.set(30); }); expect(s.get()).toBe(30); });
    it('batch 29', () => { const s = createSignal(29); batch(() => { s.set(30); s.set(31); }); expect(s.get()).toBe(31); });
    it('batch 30', () => { const s = createSignal(30); batch(() => { s.set(31); s.set(32); }); expect(s.get()).toBe(32); });
    it('batch 31', () => { const s = createSignal(31); batch(() => { s.set(32); s.set(33); }); expect(s.get()).toBe(33); });
    it('batch 32', () => { const s = createSignal(32); batch(() => { s.set(33); s.set(34); }); expect(s.get()).toBe(34); });
    it('batch 33', () => { const s = createSignal(33); batch(() => { s.set(34); s.set(35); }); expect(s.get()).toBe(35); });
    it('batch 34', () => { const s = createSignal(34); batch(() => { s.set(35); s.set(36); }); expect(s.get()).toBe(36); });
    it('batch 35', () => { const s = createSignal(35); batch(() => { s.set(36); s.set(37); }); expect(s.get()).toBe(37); });
    it('batch 36', () => { const s = createSignal(36); batch(() => { s.set(37); s.set(38); }); expect(s.get()).toBe(38); });
    it('batch 37', () => { const s = createSignal(37); batch(() => { s.set(38); s.set(39); }); expect(s.get()).toBe(39); });
    it('batch 38', () => { const s = createSignal(38); batch(() => { s.set(39); s.set(40); }); expect(s.get()).toBe(40); });
    it('batch 39', () => { const s = createSignal(39); batch(() => { s.set(40); s.set(41); }); expect(s.get()).toBe(41); });
    it('batch 40', () => { const s = createSignal(40); batch(() => { s.set(41); s.set(42); }); expect(s.get()).toBe(42); });
    it('batch 41', () => { const s = createSignal(41); batch(() => { s.set(42); s.set(43); }); expect(s.get()).toBe(43); });
    it('batch 42', () => { const s = createSignal(42); batch(() => { s.set(43); s.set(44); }); expect(s.get()).toBe(44); });
    it('batch 43', () => { const s = createSignal(43); batch(() => { s.set(44); s.set(45); }); expect(s.get()).toBe(45); });
    it('batch 44', () => { const s = createSignal(44); batch(() => { s.set(45); s.set(46); }); expect(s.get()).toBe(46); });
    it('batch 45', () => { const s = createSignal(45); batch(() => { s.set(46); s.set(47); }); expect(s.get()).toBe(47); });
    it('batch 46', () => { const s = createSignal(46); batch(() => { s.set(47); s.set(48); }); expect(s.get()).toBe(48); });
    it('batch 47', () => { const s = createSignal(47); batch(() => { s.set(48); s.set(49); }); expect(s.get()).toBe(49); });
    it('batch 48', () => { const s = createSignal(48); batch(() => { s.set(49); s.set(50); }); expect(s.get()).toBe(50); });
    it('batch 49', () => { const s = createSignal(49); batch(() => { s.set(50); s.set(51); }); expect(s.get()).toBe(51); });
    it('batch 50', () => { const s = createSignal(50); batch(() => { s.set(51); s.set(52); }); expect(s.get()).toBe(52); });
    it('batch 51', () => { const s = createSignal(51); batch(() => { s.set(52); s.set(53); }); expect(s.get()).toBe(53); });
    it('batch 52', () => { const s = createSignal(52); batch(() => { s.set(53); s.set(54); }); expect(s.get()).toBe(54); });
    it('batch 53', () => { const s = createSignal(53); batch(() => { s.set(54); s.set(55); }); expect(s.get()).toBe(55); });
    it('batch 54', () => { const s = createSignal(54); batch(() => { s.set(55); s.set(56); }); expect(s.get()).toBe(56); });
    it('batch 55', () => { const s = createSignal(55); batch(() => { s.set(56); s.set(57); }); expect(s.get()).toBe(57); });
    it('batch 56', () => { const s = createSignal(56); batch(() => { s.set(57); s.set(58); }); expect(s.get()).toBe(58); });
    it('batch 57', () => { const s = createSignal(57); batch(() => { s.set(58); s.set(59); }); expect(s.get()).toBe(59); });
    it('batch 58', () => { const s = createSignal(58); batch(() => { s.set(59); s.set(60); }); expect(s.get()).toBe(60); });
    it('batch 59', () => { const s = createSignal(59); batch(() => { s.set(60); s.set(61); }); expect(s.get()).toBe(61); });
  });

  describe('signal-utils extra', () => {
    it('signal extra 0: set and get', () => { const s = createSignal(0); s.set(0); expect(s.get()).toBe(0); });
    it('signal extra 1: set and get', () => { const s = createSignal(7); s.set(3); expect(s.get()).toBe(3); });
    it('signal extra 2: set and get', () => { const s = createSignal(14); s.set(6); expect(s.get()).toBe(6); });
    it('signal extra 3: set and get', () => { const s = createSignal(21); s.set(9); expect(s.get()).toBe(9); });
    it('signal extra 4: set and get', () => { const s = createSignal(28); s.set(12); expect(s.get()).toBe(12); });
    it('signal extra 5: set and get', () => { const s = createSignal(35); s.set(15); expect(s.get()).toBe(15); });
    it('signal extra 6: set and get', () => { const s = createSignal(42); s.set(18); expect(s.get()).toBe(18); });
    it('signal extra 7: set and get', () => { const s = createSignal(49); s.set(21); expect(s.get()).toBe(21); });
    it('signal extra 8: set and get', () => { const s = createSignal(56); s.set(24); expect(s.get()).toBe(24); });
    it('signal extra 9: set and get', () => { const s = createSignal(63); s.set(27); expect(s.get()).toBe(27); });
    it('signal extra 10: set and get', () => { const s = createSignal(70); s.set(30); expect(s.get()).toBe(30); });
    it('signal extra 11: set and get', () => { const s = createSignal(77); s.set(33); expect(s.get()).toBe(33); });
    it('signal extra 12: set and get', () => { const s = createSignal(84); s.set(36); expect(s.get()).toBe(36); });
    it('signal extra 13: set and get', () => { const s = createSignal(91); s.set(39); expect(s.get()).toBe(39); });
    it('signal extra 14: set and get', () => { const s = createSignal(98); s.set(42); expect(s.get()).toBe(42); });
    it('signal extra 15: set and get', () => { const s = createSignal(105); s.set(45); expect(s.get()).toBe(45); });
    it('signal extra 16: set and get', () => { const s = createSignal(112); s.set(48); expect(s.get()).toBe(48); });
    it('signal extra 17: set and get', () => { const s = createSignal(119); s.set(51); expect(s.get()).toBe(51); });
    it('signal extra 18: set and get', () => { const s = createSignal(126); s.set(54); expect(s.get()).toBe(54); });
    it('signal extra 19: set and get', () => { const s = createSignal(133); s.set(57); expect(s.get()).toBe(57); });
    it('signal extra 20: set and get', () => { const s = createSignal(140); s.set(60); expect(s.get()).toBe(60); });
    it('signal extra 21: set and get', () => { const s = createSignal(147); s.set(63); expect(s.get()).toBe(63); });
    it('signal extra 22: set and get', () => { const s = createSignal(154); s.set(66); expect(s.get()).toBe(66); });
    it('signal extra 23: set and get', () => { const s = createSignal(161); s.set(69); expect(s.get()).toBe(69); });
    it('signal extra 24: set and get', () => { const s = createSignal(168); s.set(72); expect(s.get()).toBe(72); });
    it('signal extra 25: set and get', () => { const s = createSignal(175); s.set(75); expect(s.get()).toBe(75); });
    it('signal extra 26: set and get', () => { const s = createSignal(182); s.set(78); expect(s.get()).toBe(78); });
    it('signal extra 27: set and get', () => { const s = createSignal(189); s.set(81); expect(s.get()).toBe(81); });
    it('signal extra 28: set and get', () => { const s = createSignal(196); s.set(84); expect(s.get()).toBe(84); });
    it('signal extra 29: set and get', () => { const s = createSignal(203); s.set(87); expect(s.get()).toBe(87); });
    it('signal extra 30: set and get', () => { const s = createSignal(210); s.set(90); expect(s.get()).toBe(90); });
    it('signal extra 31: set and get', () => { const s = createSignal(217); s.set(93); expect(s.get()).toBe(93); });
    it('signal extra 32: set and get', () => { const s = createSignal(224); s.set(96); expect(s.get()).toBe(96); });
    it('signal extra 33: set and get', () => { const s = createSignal(231); s.set(99); expect(s.get()).toBe(99); });
    it('signal extra 34: set and get', () => { const s = createSignal(238); s.set(102); expect(s.get()).toBe(102); });
    it('signal extra 35: set and get', () => { const s = createSignal(245); s.set(105); expect(s.get()).toBe(105); });
    it('signal extra 36: set and get', () => { const s = createSignal(252); s.set(108); expect(s.get()).toBe(108); });
    it('signal extra 37: set and get', () => { const s = createSignal(259); s.set(111); expect(s.get()).toBe(111); });
    it('signal extra 38: set and get', () => { const s = createSignal(266); s.set(114); expect(s.get()).toBe(114); });
    it('signal extra 39: set and get', () => { const s = createSignal(273); s.set(117); expect(s.get()).toBe(117); });
    it('signal extra 40: set and get', () => { const s = createSignal(280); s.set(120); expect(s.get()).toBe(120); });
    it('signal extra 41: set and get', () => { const s = createSignal(287); s.set(123); expect(s.get()).toBe(123); });
    it('signal extra 42: set and get', () => { const s = createSignal(294); s.set(126); expect(s.get()).toBe(126); });
    it('signal extra 43: set and get', () => { const s = createSignal(301); s.set(129); expect(s.get()).toBe(129); });
    it('signal extra 44: set and get', () => { const s = createSignal(308); s.set(132); expect(s.get()).toBe(132); });
    it('signal extra 45: set and get', () => { const s = createSignal(315); s.set(135); expect(s.get()).toBe(135); });
    it('signal extra 46: set and get', () => { const s = createSignal(322); s.set(138); expect(s.get()).toBe(138); });
    it('signal extra 47: set and get', () => { const s = createSignal(329); s.set(141); expect(s.get()).toBe(141); });
    it('signal extra 48: set and get', () => { const s = createSignal(336); s.set(144); expect(s.get()).toBe(144); });
    it('signal extra 49: set and get', () => { const s = createSignal(343); s.set(147); expect(s.get()).toBe(147); });
  });
});
