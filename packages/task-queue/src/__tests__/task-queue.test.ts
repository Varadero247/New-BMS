// Copyright (c) 2026 Nexara DMCC. All rights reserved.
import { PriorityQueue, FIFOQueue } from '../task-queue';
describe('task-queue', () => {
  describe('PriorityQueue enqueue/dequeue', () => {
    it('enqueue/dequeue 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t0',0,0); const t=q.dequeue(); expect(t?.data).toBe(0); });
    it('enqueue/dequeue 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t1',1,1); const t=q.dequeue(); expect(t?.data).toBe(1); });
    it('enqueue/dequeue 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t2',2,2); const t=q.dequeue(); expect(t?.data).toBe(2); });
    it('enqueue/dequeue 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t3',3,3); const t=q.dequeue(); expect(t?.data).toBe(3); });
    it('enqueue/dequeue 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t4',4,4); const t=q.dequeue(); expect(t?.data).toBe(4); });
    it('enqueue/dequeue 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t5',5,5); const t=q.dequeue(); expect(t?.data).toBe(5); });
    it('enqueue/dequeue 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t6',6,6); const t=q.dequeue(); expect(t?.data).toBe(6); });
    it('enqueue/dequeue 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t7',7,7); const t=q.dequeue(); expect(t?.data).toBe(7); });
    it('enqueue/dequeue 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t8',8,8); const t=q.dequeue(); expect(t?.data).toBe(8); });
    it('enqueue/dequeue 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t9',9,9); const t=q.dequeue(); expect(t?.data).toBe(9); });
    it('enqueue/dequeue 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t10',10,0); const t=q.dequeue(); expect(t?.data).toBe(10); });
    it('enqueue/dequeue 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t11',11,1); const t=q.dequeue(); expect(t?.data).toBe(11); });
    it('enqueue/dequeue 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t12',12,2); const t=q.dequeue(); expect(t?.data).toBe(12); });
    it('enqueue/dequeue 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t13',13,3); const t=q.dequeue(); expect(t?.data).toBe(13); });
    it('enqueue/dequeue 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t14',14,4); const t=q.dequeue(); expect(t?.data).toBe(14); });
    it('enqueue/dequeue 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t15',15,5); const t=q.dequeue(); expect(t?.data).toBe(15); });
    it('enqueue/dequeue 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t16',16,6); const t=q.dequeue(); expect(t?.data).toBe(16); });
    it('enqueue/dequeue 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t17',17,7); const t=q.dequeue(); expect(t?.data).toBe(17); });
    it('enqueue/dequeue 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t18',18,8); const t=q.dequeue(); expect(t?.data).toBe(18); });
    it('enqueue/dequeue 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t19',19,9); const t=q.dequeue(); expect(t?.data).toBe(19); });
    it('enqueue/dequeue 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t20',20,0); const t=q.dequeue(); expect(t?.data).toBe(20); });
    it('enqueue/dequeue 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t21',21,1); const t=q.dequeue(); expect(t?.data).toBe(21); });
    it('enqueue/dequeue 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t22',22,2); const t=q.dequeue(); expect(t?.data).toBe(22); });
    it('enqueue/dequeue 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t23',23,3); const t=q.dequeue(); expect(t?.data).toBe(23); });
    it('enqueue/dequeue 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t24',24,4); const t=q.dequeue(); expect(t?.data).toBe(24); });
    it('enqueue/dequeue 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t25',25,5); const t=q.dequeue(); expect(t?.data).toBe(25); });
    it('enqueue/dequeue 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t26',26,6); const t=q.dequeue(); expect(t?.data).toBe(26); });
    it('enqueue/dequeue 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t27',27,7); const t=q.dequeue(); expect(t?.data).toBe(27); });
    it('enqueue/dequeue 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t28',28,8); const t=q.dequeue(); expect(t?.data).toBe(28); });
    it('enqueue/dequeue 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t29',29,9); const t=q.dequeue(); expect(t?.data).toBe(29); });
    it('enqueue/dequeue 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t30',30,0); const t=q.dequeue(); expect(t?.data).toBe(30); });
    it('enqueue/dequeue 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t31',31,1); const t=q.dequeue(); expect(t?.data).toBe(31); });
    it('enqueue/dequeue 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t32',32,2); const t=q.dequeue(); expect(t?.data).toBe(32); });
    it('enqueue/dequeue 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t33',33,3); const t=q.dequeue(); expect(t?.data).toBe(33); });
    it('enqueue/dequeue 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t34',34,4); const t=q.dequeue(); expect(t?.data).toBe(34); });
    it('enqueue/dequeue 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t35',35,5); const t=q.dequeue(); expect(t?.data).toBe(35); });
    it('enqueue/dequeue 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t36',36,6); const t=q.dequeue(); expect(t?.data).toBe(36); });
    it('enqueue/dequeue 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t37',37,7); const t=q.dequeue(); expect(t?.data).toBe(37); });
    it('enqueue/dequeue 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t38',38,8); const t=q.dequeue(); expect(t?.data).toBe(38); });
    it('enqueue/dequeue 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t39',39,9); const t=q.dequeue(); expect(t?.data).toBe(39); });
    it('enqueue/dequeue 40', () => { const q = new PriorityQueue<number>(); q.enqueue('t40',40,0); const t=q.dequeue(); expect(t?.data).toBe(40); });
    it('enqueue/dequeue 41', () => { const q = new PriorityQueue<number>(); q.enqueue('t41',41,1); const t=q.dequeue(); expect(t?.data).toBe(41); });
    it('enqueue/dequeue 42', () => { const q = new PriorityQueue<number>(); q.enqueue('t42',42,2); const t=q.dequeue(); expect(t?.data).toBe(42); });
    it('enqueue/dequeue 43', () => { const q = new PriorityQueue<number>(); q.enqueue('t43',43,3); const t=q.dequeue(); expect(t?.data).toBe(43); });
    it('enqueue/dequeue 44', () => { const q = new PriorityQueue<number>(); q.enqueue('t44',44,4); const t=q.dequeue(); expect(t?.data).toBe(44); });
    it('enqueue/dequeue 45', () => { const q = new PriorityQueue<number>(); q.enqueue('t45',45,5); const t=q.dequeue(); expect(t?.data).toBe(45); });
    it('enqueue/dequeue 46', () => { const q = new PriorityQueue<number>(); q.enqueue('t46',46,6); const t=q.dequeue(); expect(t?.data).toBe(46); });
    it('enqueue/dequeue 47', () => { const q = new PriorityQueue<number>(); q.enqueue('t47',47,7); const t=q.dequeue(); expect(t?.data).toBe(47); });
    it('enqueue/dequeue 48', () => { const q = new PriorityQueue<number>(); q.enqueue('t48',48,8); const t=q.dequeue(); expect(t?.data).toBe(48); });
    it('enqueue/dequeue 49', () => { const q = new PriorityQueue<number>(); q.enqueue('t49',49,9); const t=q.dequeue(); expect(t?.data).toBe(49); });
    it('enqueue/dequeue 50', () => { const q = new PriorityQueue<number>(); q.enqueue('t50',50,0); const t=q.dequeue(); expect(t?.data).toBe(50); });
    it('enqueue/dequeue 51', () => { const q = new PriorityQueue<number>(); q.enqueue('t51',51,1); const t=q.dequeue(); expect(t?.data).toBe(51); });
    it('enqueue/dequeue 52', () => { const q = new PriorityQueue<number>(); q.enqueue('t52',52,2); const t=q.dequeue(); expect(t?.data).toBe(52); });
    it('enqueue/dequeue 53', () => { const q = new PriorityQueue<number>(); q.enqueue('t53',53,3); const t=q.dequeue(); expect(t?.data).toBe(53); });
    it('enqueue/dequeue 54', () => { const q = new PriorityQueue<number>(); q.enqueue('t54',54,4); const t=q.dequeue(); expect(t?.data).toBe(54); });
    it('enqueue/dequeue 55', () => { const q = new PriorityQueue<number>(); q.enqueue('t55',55,5); const t=q.dequeue(); expect(t?.data).toBe(55); });
    it('enqueue/dequeue 56', () => { const q = new PriorityQueue<number>(); q.enqueue('t56',56,6); const t=q.dequeue(); expect(t?.data).toBe(56); });
    it('enqueue/dequeue 57', () => { const q = new PriorityQueue<number>(); q.enqueue('t57',57,7); const t=q.dequeue(); expect(t?.data).toBe(57); });
    it('enqueue/dequeue 58', () => { const q = new PriorityQueue<number>(); q.enqueue('t58',58,8); const t=q.dequeue(); expect(t?.data).toBe(58); });
    it('enqueue/dequeue 59', () => { const q = new PriorityQueue<number>(); q.enqueue('t59',59,9); const t=q.dequeue(); expect(t?.data).toBe(59); });
    it('enqueue/dequeue 60', () => { const q = new PriorityQueue<number>(); q.enqueue('t60',60,0); const t=q.dequeue(); expect(t?.data).toBe(60); });
    it('enqueue/dequeue 61', () => { const q = new PriorityQueue<number>(); q.enqueue('t61',61,1); const t=q.dequeue(); expect(t?.data).toBe(61); });
    it('enqueue/dequeue 62', () => { const q = new PriorityQueue<number>(); q.enqueue('t62',62,2); const t=q.dequeue(); expect(t?.data).toBe(62); });
    it('enqueue/dequeue 63', () => { const q = new PriorityQueue<number>(); q.enqueue('t63',63,3); const t=q.dequeue(); expect(t?.data).toBe(63); });
    it('enqueue/dequeue 64', () => { const q = new PriorityQueue<number>(); q.enqueue('t64',64,4); const t=q.dequeue(); expect(t?.data).toBe(64); });
    it('enqueue/dequeue 65', () => { const q = new PriorityQueue<number>(); q.enqueue('t65',65,5); const t=q.dequeue(); expect(t?.data).toBe(65); });
    it('enqueue/dequeue 66', () => { const q = new PriorityQueue<number>(); q.enqueue('t66',66,6); const t=q.dequeue(); expect(t?.data).toBe(66); });
    it('enqueue/dequeue 67', () => { const q = new PriorityQueue<number>(); q.enqueue('t67',67,7); const t=q.dequeue(); expect(t?.data).toBe(67); });
    it('enqueue/dequeue 68', () => { const q = new PriorityQueue<number>(); q.enqueue('t68',68,8); const t=q.dequeue(); expect(t?.data).toBe(68); });
    it('enqueue/dequeue 69', () => { const q = new PriorityQueue<number>(); q.enqueue('t69',69,9); const t=q.dequeue(); expect(t?.data).toBe(69); });
    it('enqueue/dequeue 70', () => { const q = new PriorityQueue<number>(); q.enqueue('t70',70,0); const t=q.dequeue(); expect(t?.data).toBe(70); });
    it('enqueue/dequeue 71', () => { const q = new PriorityQueue<number>(); q.enqueue('t71',71,1); const t=q.dequeue(); expect(t?.data).toBe(71); });
    it('enqueue/dequeue 72', () => { const q = new PriorityQueue<number>(); q.enqueue('t72',72,2); const t=q.dequeue(); expect(t?.data).toBe(72); });
    it('enqueue/dequeue 73', () => { const q = new PriorityQueue<number>(); q.enqueue('t73',73,3); const t=q.dequeue(); expect(t?.data).toBe(73); });
    it('enqueue/dequeue 74', () => { const q = new PriorityQueue<number>(); q.enqueue('t74',74,4); const t=q.dequeue(); expect(t?.data).toBe(74); });
    it('enqueue/dequeue 75', () => { const q = new PriorityQueue<number>(); q.enqueue('t75',75,5); const t=q.dequeue(); expect(t?.data).toBe(75); });
    it('enqueue/dequeue 76', () => { const q = new PriorityQueue<number>(); q.enqueue('t76',76,6); const t=q.dequeue(); expect(t?.data).toBe(76); });
    it('enqueue/dequeue 77', () => { const q = new PriorityQueue<number>(); q.enqueue('t77',77,7); const t=q.dequeue(); expect(t?.data).toBe(77); });
    it('enqueue/dequeue 78', () => { const q = new PriorityQueue<number>(); q.enqueue('t78',78,8); const t=q.dequeue(); expect(t?.data).toBe(78); });
    it('enqueue/dequeue 79', () => { const q = new PriorityQueue<number>(); q.enqueue('t79',79,9); const t=q.dequeue(); expect(t?.data).toBe(79); });
    it('enqueue/dequeue 80', () => { const q = new PriorityQueue<number>(); q.enqueue('t80',80,0); const t=q.dequeue(); expect(t?.data).toBe(80); });
    it('enqueue/dequeue 81', () => { const q = new PriorityQueue<number>(); q.enqueue('t81',81,1); const t=q.dequeue(); expect(t?.data).toBe(81); });
    it('enqueue/dequeue 82', () => { const q = new PriorityQueue<number>(); q.enqueue('t82',82,2); const t=q.dequeue(); expect(t?.data).toBe(82); });
    it('enqueue/dequeue 83', () => { const q = new PriorityQueue<number>(); q.enqueue('t83',83,3); const t=q.dequeue(); expect(t?.data).toBe(83); });
    it('enqueue/dequeue 84', () => { const q = new PriorityQueue<number>(); q.enqueue('t84',84,4); const t=q.dequeue(); expect(t?.data).toBe(84); });
    it('enqueue/dequeue 85', () => { const q = new PriorityQueue<number>(); q.enqueue('t85',85,5); const t=q.dequeue(); expect(t?.data).toBe(85); });
    it('enqueue/dequeue 86', () => { const q = new PriorityQueue<number>(); q.enqueue('t86',86,6); const t=q.dequeue(); expect(t?.data).toBe(86); });
    it('enqueue/dequeue 87', () => { const q = new PriorityQueue<number>(); q.enqueue('t87',87,7); const t=q.dequeue(); expect(t?.data).toBe(87); });
    it('enqueue/dequeue 88', () => { const q = new PriorityQueue<number>(); q.enqueue('t88',88,8); const t=q.dequeue(); expect(t?.data).toBe(88); });
    it('enqueue/dequeue 89', () => { const q = new PriorityQueue<number>(); q.enqueue('t89',89,9); const t=q.dequeue(); expect(t?.data).toBe(89); });
    it('enqueue/dequeue 90', () => { const q = new PriorityQueue<number>(); q.enqueue('t90',90,0); const t=q.dequeue(); expect(t?.data).toBe(90); });
    it('enqueue/dequeue 91', () => { const q = new PriorityQueue<number>(); q.enqueue('t91',91,1); const t=q.dequeue(); expect(t?.data).toBe(91); });
    it('enqueue/dequeue 92', () => { const q = new PriorityQueue<number>(); q.enqueue('t92',92,2); const t=q.dequeue(); expect(t?.data).toBe(92); });
    it('enqueue/dequeue 93', () => { const q = new PriorityQueue<number>(); q.enqueue('t93',93,3); const t=q.dequeue(); expect(t?.data).toBe(93); });
    it('enqueue/dequeue 94', () => { const q = new PriorityQueue<number>(); q.enqueue('t94',94,4); const t=q.dequeue(); expect(t?.data).toBe(94); });
    it('enqueue/dequeue 95', () => { const q = new PriorityQueue<number>(); q.enqueue('t95',95,5); const t=q.dequeue(); expect(t?.data).toBe(95); });
    it('enqueue/dequeue 96', () => { const q = new PriorityQueue<number>(); q.enqueue('t96',96,6); const t=q.dequeue(); expect(t?.data).toBe(96); });
    it('enqueue/dequeue 97', () => { const q = new PriorityQueue<number>(); q.enqueue('t97',97,7); const t=q.dequeue(); expect(t?.data).toBe(97); });
    it('enqueue/dequeue 98', () => { const q = new PriorityQueue<number>(); q.enqueue('t98',98,8); const t=q.dequeue(); expect(t?.data).toBe(98); });
    it('enqueue/dequeue 99', () => { const q = new PriorityQueue<number>(); q.enqueue('t99',99,9); const t=q.dequeue(); expect(t?.data).toBe(99); });
    it('enqueue/dequeue 100', () => { const q = new PriorityQueue<number>(); q.enqueue('t100',100,0); const t=q.dequeue(); expect(t?.data).toBe(100); });
    it('enqueue/dequeue 101', () => { const q = new PriorityQueue<number>(); q.enqueue('t101',101,1); const t=q.dequeue(); expect(t?.data).toBe(101); });
    it('enqueue/dequeue 102', () => { const q = new PriorityQueue<number>(); q.enqueue('t102',102,2); const t=q.dequeue(); expect(t?.data).toBe(102); });
    it('enqueue/dequeue 103', () => { const q = new PriorityQueue<number>(); q.enqueue('t103',103,3); const t=q.dequeue(); expect(t?.data).toBe(103); });
    it('enqueue/dequeue 104', () => { const q = new PriorityQueue<number>(); q.enqueue('t104',104,4); const t=q.dequeue(); expect(t?.data).toBe(104); });
    it('enqueue/dequeue 105', () => { const q = new PriorityQueue<number>(); q.enqueue('t105',105,5); const t=q.dequeue(); expect(t?.data).toBe(105); });
    it('enqueue/dequeue 106', () => { const q = new PriorityQueue<number>(); q.enqueue('t106',106,6); const t=q.dequeue(); expect(t?.data).toBe(106); });
    it('enqueue/dequeue 107', () => { const q = new PriorityQueue<number>(); q.enqueue('t107',107,7); const t=q.dequeue(); expect(t?.data).toBe(107); });
    it('enqueue/dequeue 108', () => { const q = new PriorityQueue<number>(); q.enqueue('t108',108,8); const t=q.dequeue(); expect(t?.data).toBe(108); });
    it('enqueue/dequeue 109', () => { const q = new PriorityQueue<number>(); q.enqueue('t109',109,9); const t=q.dequeue(); expect(t?.data).toBe(109); });
    it('enqueue/dequeue 110', () => { const q = new PriorityQueue<number>(); q.enqueue('t110',110,0); const t=q.dequeue(); expect(t?.data).toBe(110); });
    it('enqueue/dequeue 111', () => { const q = new PriorityQueue<number>(); q.enqueue('t111',111,1); const t=q.dequeue(); expect(t?.data).toBe(111); });
    it('enqueue/dequeue 112', () => { const q = new PriorityQueue<number>(); q.enqueue('t112',112,2); const t=q.dequeue(); expect(t?.data).toBe(112); });
    it('enqueue/dequeue 113', () => { const q = new PriorityQueue<number>(); q.enqueue('t113',113,3); const t=q.dequeue(); expect(t?.data).toBe(113); });
    it('enqueue/dequeue 114', () => { const q = new PriorityQueue<number>(); q.enqueue('t114',114,4); const t=q.dequeue(); expect(t?.data).toBe(114); });
    it('enqueue/dequeue 115', () => { const q = new PriorityQueue<number>(); q.enqueue('t115',115,5); const t=q.dequeue(); expect(t?.data).toBe(115); });
    it('enqueue/dequeue 116', () => { const q = new PriorityQueue<number>(); q.enqueue('t116',116,6); const t=q.dequeue(); expect(t?.data).toBe(116); });
    it('enqueue/dequeue 117', () => { const q = new PriorityQueue<number>(); q.enqueue('t117',117,7); const t=q.dequeue(); expect(t?.data).toBe(117); });
    it('enqueue/dequeue 118', () => { const q = new PriorityQueue<number>(); q.enqueue('t118',118,8); const t=q.dequeue(); expect(t?.data).toBe(118); });
    it('enqueue/dequeue 119', () => { const q = new PriorityQueue<number>(); q.enqueue('t119',119,9); const t=q.dequeue(); expect(t?.data).toBe(119); });
  });
  describe('PriorityQueue priority ordering', () => {
    it('priority order 0', () => { const q = new PriorityQueue<number>(); q.enqueue('a',0,0); q.enqueue('b',100,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 1', () => { const q = new PriorityQueue<number>(); q.enqueue('a',1,1); q.enqueue('b',101,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 2', () => { const q = new PriorityQueue<number>(); q.enqueue('a',2,2); q.enqueue('b',102,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 3', () => { const q = new PriorityQueue<number>(); q.enqueue('a',3,3); q.enqueue('b',103,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 4', () => { const q = new PriorityQueue<number>(); q.enqueue('a',4,4); q.enqueue('b',104,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 5', () => { const q = new PriorityQueue<number>(); q.enqueue('a',5,5); q.enqueue('b',105,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 6', () => { const q = new PriorityQueue<number>(); q.enqueue('a',6,6); q.enqueue('b',106,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 7', () => { const q = new PriorityQueue<number>(); q.enqueue('a',7,7); q.enqueue('b',107,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 8', () => { const q = new PriorityQueue<number>(); q.enqueue('a',8,8); q.enqueue('b',108,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 9', () => { const q = new PriorityQueue<number>(); q.enqueue('a',9,9); q.enqueue('b',109,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 10', () => { const q = new PriorityQueue<number>(); q.enqueue('a',10,0); q.enqueue('b',110,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 11', () => { const q = new PriorityQueue<number>(); q.enqueue('a',11,1); q.enqueue('b',111,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 12', () => { const q = new PriorityQueue<number>(); q.enqueue('a',12,2); q.enqueue('b',112,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 13', () => { const q = new PriorityQueue<number>(); q.enqueue('a',13,3); q.enqueue('b',113,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 14', () => { const q = new PriorityQueue<number>(); q.enqueue('a',14,4); q.enqueue('b',114,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 15', () => { const q = new PriorityQueue<number>(); q.enqueue('a',15,5); q.enqueue('b',115,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 16', () => { const q = new PriorityQueue<number>(); q.enqueue('a',16,6); q.enqueue('b',116,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 17', () => { const q = new PriorityQueue<number>(); q.enqueue('a',17,7); q.enqueue('b',117,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 18', () => { const q = new PriorityQueue<number>(); q.enqueue('a',18,8); q.enqueue('b',118,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 19', () => { const q = new PriorityQueue<number>(); q.enqueue('a',19,9); q.enqueue('b',119,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 20', () => { const q = new PriorityQueue<number>(); q.enqueue('a',20,0); q.enqueue('b',120,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 21', () => { const q = new PriorityQueue<number>(); q.enqueue('a',21,1); q.enqueue('b',121,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 22', () => { const q = new PriorityQueue<number>(); q.enqueue('a',22,2); q.enqueue('b',122,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 23', () => { const q = new PriorityQueue<number>(); q.enqueue('a',23,3); q.enqueue('b',123,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 24', () => { const q = new PriorityQueue<number>(); q.enqueue('a',24,4); q.enqueue('b',124,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 25', () => { const q = new PriorityQueue<number>(); q.enqueue('a',25,5); q.enqueue('b',125,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 26', () => { const q = new PriorityQueue<number>(); q.enqueue('a',26,6); q.enqueue('b',126,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 27', () => { const q = new PriorityQueue<number>(); q.enqueue('a',27,7); q.enqueue('b',127,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 28', () => { const q = new PriorityQueue<number>(); q.enqueue('a',28,8); q.enqueue('b',128,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 29', () => { const q = new PriorityQueue<number>(); q.enqueue('a',29,9); q.enqueue('b',129,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 30', () => { const q = new PriorityQueue<number>(); q.enqueue('a',30,0); q.enqueue('b',130,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 31', () => { const q = new PriorityQueue<number>(); q.enqueue('a',31,1); q.enqueue('b',131,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 32', () => { const q = new PriorityQueue<number>(); q.enqueue('a',32,2); q.enqueue('b',132,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 33', () => { const q = new PriorityQueue<number>(); q.enqueue('a',33,3); q.enqueue('b',133,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 34', () => { const q = new PriorityQueue<number>(); q.enqueue('a',34,4); q.enqueue('b',134,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 35', () => { const q = new PriorityQueue<number>(); q.enqueue('a',35,5); q.enqueue('b',135,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 36', () => { const q = new PriorityQueue<number>(); q.enqueue('a',36,6); q.enqueue('b',136,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 37', () => { const q = new PriorityQueue<number>(); q.enqueue('a',37,7); q.enqueue('b',137,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 38', () => { const q = new PriorityQueue<number>(); q.enqueue('a',38,8); q.enqueue('b',138,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 39', () => { const q = new PriorityQueue<number>(); q.enqueue('a',39,9); q.enqueue('b',139,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 40', () => { const q = new PriorityQueue<number>(); q.enqueue('a',40,0); q.enqueue('b',140,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 41', () => { const q = new PriorityQueue<number>(); q.enqueue('a',41,1); q.enqueue('b',141,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 42', () => { const q = new PriorityQueue<number>(); q.enqueue('a',42,2); q.enqueue('b',142,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 43', () => { const q = new PriorityQueue<number>(); q.enqueue('a',43,3); q.enqueue('b',143,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 44', () => { const q = new PriorityQueue<number>(); q.enqueue('a',44,4); q.enqueue('b',144,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 45', () => { const q = new PriorityQueue<number>(); q.enqueue('a',45,5); q.enqueue('b',145,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 46', () => { const q = new PriorityQueue<number>(); q.enqueue('a',46,6); q.enqueue('b',146,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 47', () => { const q = new PriorityQueue<number>(); q.enqueue('a',47,7); q.enqueue('b',147,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 48', () => { const q = new PriorityQueue<number>(); q.enqueue('a',48,8); q.enqueue('b',148,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 49', () => { const q = new PriorityQueue<number>(); q.enqueue('a',49,9); q.enqueue('b',149,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 50', () => { const q = new PriorityQueue<number>(); q.enqueue('a',50,0); q.enqueue('b',150,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 51', () => { const q = new PriorityQueue<number>(); q.enqueue('a',51,1); q.enqueue('b',151,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 52', () => { const q = new PriorityQueue<number>(); q.enqueue('a',52,2); q.enqueue('b',152,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 53', () => { const q = new PriorityQueue<number>(); q.enqueue('a',53,3); q.enqueue('b',153,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 54', () => { const q = new PriorityQueue<number>(); q.enqueue('a',54,4); q.enqueue('b',154,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 55', () => { const q = new PriorityQueue<number>(); q.enqueue('a',55,5); q.enqueue('b',155,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 56', () => { const q = new PriorityQueue<number>(); q.enqueue('a',56,6); q.enqueue('b',156,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 57', () => { const q = new PriorityQueue<number>(); q.enqueue('a',57,7); q.enqueue('b',157,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 58', () => { const q = new PriorityQueue<number>(); q.enqueue('a',58,8); q.enqueue('b',158,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 59', () => { const q = new PriorityQueue<number>(); q.enqueue('a',59,9); q.enqueue('b',159,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 60', () => { const q = new PriorityQueue<number>(); q.enqueue('a',60,0); q.enqueue('b',160,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 61', () => { const q = new PriorityQueue<number>(); q.enqueue('a',61,1); q.enqueue('b',161,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 62', () => { const q = new PriorityQueue<number>(); q.enqueue('a',62,2); q.enqueue('b',162,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 63', () => { const q = new PriorityQueue<number>(); q.enqueue('a',63,3); q.enqueue('b',163,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 64', () => { const q = new PriorityQueue<number>(); q.enqueue('a',64,4); q.enqueue('b',164,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 65', () => { const q = new PriorityQueue<number>(); q.enqueue('a',65,5); q.enqueue('b',165,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 66', () => { const q = new PriorityQueue<number>(); q.enqueue('a',66,6); q.enqueue('b',166,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 67', () => { const q = new PriorityQueue<number>(); q.enqueue('a',67,7); q.enqueue('b',167,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 68', () => { const q = new PriorityQueue<number>(); q.enqueue('a',68,8); q.enqueue('b',168,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 69', () => { const q = new PriorityQueue<number>(); q.enqueue('a',69,9); q.enqueue('b',169,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 70', () => { const q = new PriorityQueue<number>(); q.enqueue('a',70,0); q.enqueue('b',170,5); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 71', () => { const q = new PriorityQueue<number>(); q.enqueue('a',71,1); q.enqueue('b',171,6); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 72', () => { const q = new PriorityQueue<number>(); q.enqueue('a',72,2); q.enqueue('b',172,7); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 73', () => { const q = new PriorityQueue<number>(); q.enqueue('a',73,3); q.enqueue('b',173,8); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 74', () => { const q = new PriorityQueue<number>(); q.enqueue('a',74,4); q.enqueue('b',174,9); const t=q.dequeue(); expect(t?.priority).toBe(4); });
    it('priority order 75', () => { const q = new PriorityQueue<number>(); q.enqueue('a',75,5); q.enqueue('b',175,0); const t=q.dequeue(); expect(t?.priority).toBe(0); });
    it('priority order 76', () => { const q = new PriorityQueue<number>(); q.enqueue('a',76,6); q.enqueue('b',176,1); const t=q.dequeue(); expect(t?.priority).toBe(1); });
    it('priority order 77', () => { const q = new PriorityQueue<number>(); q.enqueue('a',77,7); q.enqueue('b',177,2); const t=q.dequeue(); expect(t?.priority).toBe(2); });
    it('priority order 78', () => { const q = new PriorityQueue<number>(); q.enqueue('a',78,8); q.enqueue('b',178,3); const t=q.dequeue(); expect(t?.priority).toBe(3); });
    it('priority order 79', () => { const q = new PriorityQueue<number>(); q.enqueue('a',79,9); q.enqueue('b',179,4); const t=q.dequeue(); expect(t?.priority).toBe(4); });
  });
  describe('PriorityQueue peek', () => {
    it('peek 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t0',0,0); expect(q.peek()?.data).toBe(0); });
    it('peek 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t1',1,1); expect(q.peek()?.data).toBe(1); });
    it('peek 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t2',2,2); expect(q.peek()?.data).toBe(2); });
    it('peek 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t3',3,3); expect(q.peek()?.data).toBe(3); });
    it('peek 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t4',4,4); expect(q.peek()?.data).toBe(4); });
    it('peek 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t5',5,5); expect(q.peek()?.data).toBe(5); });
    it('peek 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t6',6,6); expect(q.peek()?.data).toBe(6); });
    it('peek 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t7',7,7); expect(q.peek()?.data).toBe(7); });
    it('peek 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t8',8,8); expect(q.peek()?.data).toBe(8); });
    it('peek 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t9',9,9); expect(q.peek()?.data).toBe(9); });
    it('peek 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t10',10,0); expect(q.peek()?.data).toBe(10); });
    it('peek 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t11',11,1); expect(q.peek()?.data).toBe(11); });
    it('peek 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t12',12,2); expect(q.peek()?.data).toBe(12); });
    it('peek 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t13',13,3); expect(q.peek()?.data).toBe(13); });
    it('peek 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t14',14,4); expect(q.peek()?.data).toBe(14); });
    it('peek 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t15',15,5); expect(q.peek()?.data).toBe(15); });
    it('peek 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t16',16,6); expect(q.peek()?.data).toBe(16); });
    it('peek 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t17',17,7); expect(q.peek()?.data).toBe(17); });
    it('peek 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t18',18,8); expect(q.peek()?.data).toBe(18); });
    it('peek 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t19',19,9); expect(q.peek()?.data).toBe(19); });
    it('peek 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t20',20,0); expect(q.peek()?.data).toBe(20); });
    it('peek 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t21',21,1); expect(q.peek()?.data).toBe(21); });
    it('peek 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t22',22,2); expect(q.peek()?.data).toBe(22); });
    it('peek 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t23',23,3); expect(q.peek()?.data).toBe(23); });
    it('peek 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t24',24,4); expect(q.peek()?.data).toBe(24); });
    it('peek 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t25',25,5); expect(q.peek()?.data).toBe(25); });
    it('peek 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t26',26,6); expect(q.peek()?.data).toBe(26); });
    it('peek 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t27',27,7); expect(q.peek()?.data).toBe(27); });
    it('peek 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t28',28,8); expect(q.peek()?.data).toBe(28); });
    it('peek 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t29',29,9); expect(q.peek()?.data).toBe(29); });
    it('peek 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t30',30,0); expect(q.peek()?.data).toBe(30); });
    it('peek 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t31',31,1); expect(q.peek()?.data).toBe(31); });
    it('peek 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t32',32,2); expect(q.peek()?.data).toBe(32); });
    it('peek 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t33',33,3); expect(q.peek()?.data).toBe(33); });
    it('peek 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t34',34,4); expect(q.peek()?.data).toBe(34); });
    it('peek 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t35',35,5); expect(q.peek()?.data).toBe(35); });
    it('peek 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t36',36,6); expect(q.peek()?.data).toBe(36); });
    it('peek 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t37',37,7); expect(q.peek()?.data).toBe(37); });
    it('peek 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t38',38,8); expect(q.peek()?.data).toBe(38); });
    it('peek 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t39',39,9); expect(q.peek()?.data).toBe(39); });
    it('peek 40', () => { const q = new PriorityQueue<number>(); q.enqueue('t40',40,0); expect(q.peek()?.data).toBe(40); });
    it('peek 41', () => { const q = new PriorityQueue<number>(); q.enqueue('t41',41,1); expect(q.peek()?.data).toBe(41); });
    it('peek 42', () => { const q = new PriorityQueue<number>(); q.enqueue('t42',42,2); expect(q.peek()?.data).toBe(42); });
    it('peek 43', () => { const q = new PriorityQueue<number>(); q.enqueue('t43',43,3); expect(q.peek()?.data).toBe(43); });
    it('peek 44', () => { const q = new PriorityQueue<number>(); q.enqueue('t44',44,4); expect(q.peek()?.data).toBe(44); });
    it('peek 45', () => { const q = new PriorityQueue<number>(); q.enqueue('t45',45,5); expect(q.peek()?.data).toBe(45); });
    it('peek 46', () => { const q = new PriorityQueue<number>(); q.enqueue('t46',46,6); expect(q.peek()?.data).toBe(46); });
    it('peek 47', () => { const q = new PriorityQueue<number>(); q.enqueue('t47',47,7); expect(q.peek()?.data).toBe(47); });
    it('peek 48', () => { const q = new PriorityQueue<number>(); q.enqueue('t48',48,8); expect(q.peek()?.data).toBe(48); });
    it('peek 49', () => { const q = new PriorityQueue<number>(); q.enqueue('t49',49,9); expect(q.peek()?.data).toBe(49); });
    it('peek 50', () => { const q = new PriorityQueue<number>(); q.enqueue('t50',50,0); expect(q.peek()?.data).toBe(50); });
    it('peek 51', () => { const q = new PriorityQueue<number>(); q.enqueue('t51',51,1); expect(q.peek()?.data).toBe(51); });
    it('peek 52', () => { const q = new PriorityQueue<number>(); q.enqueue('t52',52,2); expect(q.peek()?.data).toBe(52); });
    it('peek 53', () => { const q = new PriorityQueue<number>(); q.enqueue('t53',53,3); expect(q.peek()?.data).toBe(53); });
    it('peek 54', () => { const q = new PriorityQueue<number>(); q.enqueue('t54',54,4); expect(q.peek()?.data).toBe(54); });
    it('peek 55', () => { const q = new PriorityQueue<number>(); q.enqueue('t55',55,5); expect(q.peek()?.data).toBe(55); });
    it('peek 56', () => { const q = new PriorityQueue<number>(); q.enqueue('t56',56,6); expect(q.peek()?.data).toBe(56); });
    it('peek 57', () => { const q = new PriorityQueue<number>(); q.enqueue('t57',57,7); expect(q.peek()?.data).toBe(57); });
    it('peek 58', () => { const q = new PriorityQueue<number>(); q.enqueue('t58',58,8); expect(q.peek()?.data).toBe(58); });
    it('peek 59', () => { const q = new PriorityQueue<number>(); q.enqueue('t59',59,9); expect(q.peek()?.data).toBe(59); });
    it('peek 60', () => { const q = new PriorityQueue<number>(); q.enqueue('t60',60,0); expect(q.peek()?.data).toBe(60); });
    it('peek 61', () => { const q = new PriorityQueue<number>(); q.enqueue('t61',61,1); expect(q.peek()?.data).toBe(61); });
    it('peek 62', () => { const q = new PriorityQueue<number>(); q.enqueue('t62',62,2); expect(q.peek()?.data).toBe(62); });
    it('peek 63', () => { const q = new PriorityQueue<number>(); q.enqueue('t63',63,3); expect(q.peek()?.data).toBe(63); });
    it('peek 64', () => { const q = new PriorityQueue<number>(); q.enqueue('t64',64,4); expect(q.peek()?.data).toBe(64); });
    it('peek 65', () => { const q = new PriorityQueue<number>(); q.enqueue('t65',65,5); expect(q.peek()?.data).toBe(65); });
    it('peek 66', () => { const q = new PriorityQueue<number>(); q.enqueue('t66',66,6); expect(q.peek()?.data).toBe(66); });
    it('peek 67', () => { const q = new PriorityQueue<number>(); q.enqueue('t67',67,7); expect(q.peek()?.data).toBe(67); });
    it('peek 68', () => { const q = new PriorityQueue<number>(); q.enqueue('t68',68,8); expect(q.peek()?.data).toBe(68); });
    it('peek 69', () => { const q = new PriorityQueue<number>(); q.enqueue('t69',69,9); expect(q.peek()?.data).toBe(69); });
    it('peek 70', () => { const q = new PriorityQueue<number>(); q.enqueue('t70',70,0); expect(q.peek()?.data).toBe(70); });
    it('peek 71', () => { const q = new PriorityQueue<number>(); q.enqueue('t71',71,1); expect(q.peek()?.data).toBe(71); });
    it('peek 72', () => { const q = new PriorityQueue<number>(); q.enqueue('t72',72,2); expect(q.peek()?.data).toBe(72); });
    it('peek 73', () => { const q = new PriorityQueue<number>(); q.enqueue('t73',73,3); expect(q.peek()?.data).toBe(73); });
    it('peek 74', () => { const q = new PriorityQueue<number>(); q.enqueue('t74',74,4); expect(q.peek()?.data).toBe(74); });
    it('peek 75', () => { const q = new PriorityQueue<number>(); q.enqueue('t75',75,5); expect(q.peek()?.data).toBe(75); });
    it('peek 76', () => { const q = new PriorityQueue<number>(); q.enqueue('t76',76,6); expect(q.peek()?.data).toBe(76); });
    it('peek 77', () => { const q = new PriorityQueue<number>(); q.enqueue('t77',77,7); expect(q.peek()?.data).toBe(77); });
    it('peek 78', () => { const q = new PriorityQueue<number>(); q.enqueue('t78',78,8); expect(q.peek()?.data).toBe(78); });
    it('peek 79', () => { const q = new PriorityQueue<number>(); q.enqueue('t79',79,9); expect(q.peek()?.data).toBe(79); });
  });
  describe('PriorityQueue size/clear', () => {
    it('size 0', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 1', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 2', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 3', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 4', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 5', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 6', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 7', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 8', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 9', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 10', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 11', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 12', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 13', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 14', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 15', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 16', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 17', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 18', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 19', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 20', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 21', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 22', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 23', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 24', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 25', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 26', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 27', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 28', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 29', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 30', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 31', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 32', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 33', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 34', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 35', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 36', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 37', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 38', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 39', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 40', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 41', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 42', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 43', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 44', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 45', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 46', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 47', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 48', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 49', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 50', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 51', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 52', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 53', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 54', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 55', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 56', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 57', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 58', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 59', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 60', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 61', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 62', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 63', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 64', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 65', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 66', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 67', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 68', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 69', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('size 70', () => { const q = new PriorityQueue<number>(); for(let j=0;j<1;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(1); });
    it('size 71', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(2); });
    it('size 72', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(3); });
    it('size 73', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(4); });
    it('size 74', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(5); });
    it('size 75', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(6); });
    it('size 76', () => { const q = new PriorityQueue<number>(); for(let j=0;j<7;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(7); });
    it('size 77', () => { const q = new PriorityQueue<number>(); for(let j=0;j<8;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(8); });
    it('size 78', () => { const q = new PriorityQueue<number>(); for(let j=0;j<9;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(9); });
    it('size 79', () => { const q = new PriorityQueue<number>(); for(let j=0;j<10;j++) q.enqueue('t'+j,j,0); expect(q.size).toBe(10); });
    it('clear 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t',0,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t',1,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t',2,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t',3,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t',4,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t',5,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t',6,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t',7,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t',8,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t',9,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t',10,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t',11,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t',12,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t',13,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t',14,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t',15,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t',16,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t',17,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t',18,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t',19,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t',20,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t',21,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t',22,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t',23,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t',24,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t',25,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t',26,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t',27,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t',28,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t',29,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t',30,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t',31,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t',32,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t',33,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t',34,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t',35,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t',36,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t',37,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t',38,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t',39,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 40', () => { const q = new PriorityQueue<number>(); q.enqueue('t',40,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 41', () => { const q = new PriorityQueue<number>(); q.enqueue('t',41,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 42', () => { const q = new PriorityQueue<number>(); q.enqueue('t',42,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 43', () => { const q = new PriorityQueue<number>(); q.enqueue('t',43,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 44', () => { const q = new PriorityQueue<number>(); q.enqueue('t',44,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 45', () => { const q = new PriorityQueue<number>(); q.enqueue('t',45,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 46', () => { const q = new PriorityQueue<number>(); q.enqueue('t',46,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 47', () => { const q = new PriorityQueue<number>(); q.enqueue('t',47,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 48', () => { const q = new PriorityQueue<number>(); q.enqueue('t',48,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 49', () => { const q = new PriorityQueue<number>(); q.enqueue('t',49,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 50', () => { const q = new PriorityQueue<number>(); q.enqueue('t',50,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 51', () => { const q = new PriorityQueue<number>(); q.enqueue('t',51,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 52', () => { const q = new PriorityQueue<number>(); q.enqueue('t',52,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 53', () => { const q = new PriorityQueue<number>(); q.enqueue('t',53,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 54', () => { const q = new PriorityQueue<number>(); q.enqueue('t',54,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 55', () => { const q = new PriorityQueue<number>(); q.enqueue('t',55,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 56', () => { const q = new PriorityQueue<number>(); q.enqueue('t',56,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 57', () => { const q = new PriorityQueue<number>(); q.enqueue('t',57,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 58', () => { const q = new PriorityQueue<number>(); q.enqueue('t',58,0); q.clear(); expect(q.size).toBe(0); });
    it('clear 59', () => { const q = new PriorityQueue<number>(); q.enqueue('t',59,0); q.clear(); expect(q.size).toBe(0); });
  });
  describe('PriorityQueue has/remove', () => {
    it('has 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t0',0,0); expect(q.has('t0')).toBe(true); });
    it('has 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t1',1,0); expect(q.has('t1')).toBe(true); });
    it('has 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t2',2,0); expect(q.has('t2')).toBe(true); });
    it('has 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t3',3,0); expect(q.has('t3')).toBe(true); });
    it('has 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t4',4,0); expect(q.has('t4')).toBe(true); });
    it('has 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t5',5,0); expect(q.has('t5')).toBe(true); });
    it('has 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t6',6,0); expect(q.has('t6')).toBe(true); });
    it('has 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t7',7,0); expect(q.has('t7')).toBe(true); });
    it('has 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t8',8,0); expect(q.has('t8')).toBe(true); });
    it('has 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t9',9,0); expect(q.has('t9')).toBe(true); });
    it('has 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t10',10,0); expect(q.has('t10')).toBe(true); });
    it('has 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t11',11,0); expect(q.has('t11')).toBe(true); });
    it('has 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t12',12,0); expect(q.has('t12')).toBe(true); });
    it('has 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t13',13,0); expect(q.has('t13')).toBe(true); });
    it('has 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t14',14,0); expect(q.has('t14')).toBe(true); });
    it('has 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t15',15,0); expect(q.has('t15')).toBe(true); });
    it('has 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t16',16,0); expect(q.has('t16')).toBe(true); });
    it('has 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t17',17,0); expect(q.has('t17')).toBe(true); });
    it('has 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t18',18,0); expect(q.has('t18')).toBe(true); });
    it('has 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t19',19,0); expect(q.has('t19')).toBe(true); });
    it('has 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t20',20,0); expect(q.has('t20')).toBe(true); });
    it('has 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t21',21,0); expect(q.has('t21')).toBe(true); });
    it('has 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t22',22,0); expect(q.has('t22')).toBe(true); });
    it('has 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t23',23,0); expect(q.has('t23')).toBe(true); });
    it('has 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t24',24,0); expect(q.has('t24')).toBe(true); });
    it('has 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t25',25,0); expect(q.has('t25')).toBe(true); });
    it('has 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t26',26,0); expect(q.has('t26')).toBe(true); });
    it('has 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t27',27,0); expect(q.has('t27')).toBe(true); });
    it('has 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t28',28,0); expect(q.has('t28')).toBe(true); });
    it('has 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t29',29,0); expect(q.has('t29')).toBe(true); });
    it('has 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t30',30,0); expect(q.has('t30')).toBe(true); });
    it('has 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t31',31,0); expect(q.has('t31')).toBe(true); });
    it('has 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t32',32,0); expect(q.has('t32')).toBe(true); });
    it('has 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t33',33,0); expect(q.has('t33')).toBe(true); });
    it('has 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t34',34,0); expect(q.has('t34')).toBe(true); });
    it('has 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t35',35,0); expect(q.has('t35')).toBe(true); });
    it('has 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t36',36,0); expect(q.has('t36')).toBe(true); });
    it('has 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t37',37,0); expect(q.has('t37')).toBe(true); });
    it('has 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t38',38,0); expect(q.has('t38')).toBe(true); });
    it('has 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t39',39,0); expect(q.has('t39')).toBe(true); });
    it('has 40', () => { const q = new PriorityQueue<number>(); q.enqueue('t40',40,0); expect(q.has('t40')).toBe(true); });
    it('has 41', () => { const q = new PriorityQueue<number>(); q.enqueue('t41',41,0); expect(q.has('t41')).toBe(true); });
    it('has 42', () => { const q = new PriorityQueue<number>(); q.enqueue('t42',42,0); expect(q.has('t42')).toBe(true); });
    it('has 43', () => { const q = new PriorityQueue<number>(); q.enqueue('t43',43,0); expect(q.has('t43')).toBe(true); });
    it('has 44', () => { const q = new PriorityQueue<number>(); q.enqueue('t44',44,0); expect(q.has('t44')).toBe(true); });
    it('has 45', () => { const q = new PriorityQueue<number>(); q.enqueue('t45',45,0); expect(q.has('t45')).toBe(true); });
    it('has 46', () => { const q = new PriorityQueue<number>(); q.enqueue('t46',46,0); expect(q.has('t46')).toBe(true); });
    it('has 47', () => { const q = new PriorityQueue<number>(); q.enqueue('t47',47,0); expect(q.has('t47')).toBe(true); });
    it('has 48', () => { const q = new PriorityQueue<number>(); q.enqueue('t48',48,0); expect(q.has('t48')).toBe(true); });
    it('has 49', () => { const q = new PriorityQueue<number>(); q.enqueue('t49',49,0); expect(q.has('t49')).toBe(true); });
    it('has 50', () => { const q = new PriorityQueue<number>(); q.enqueue('t50',50,0); expect(q.has('t50')).toBe(true); });
    it('has 51', () => { const q = new PriorityQueue<number>(); q.enqueue('t51',51,0); expect(q.has('t51')).toBe(true); });
    it('has 52', () => { const q = new PriorityQueue<number>(); q.enqueue('t52',52,0); expect(q.has('t52')).toBe(true); });
    it('has 53', () => { const q = new PriorityQueue<number>(); q.enqueue('t53',53,0); expect(q.has('t53')).toBe(true); });
    it('has 54', () => { const q = new PriorityQueue<number>(); q.enqueue('t54',54,0); expect(q.has('t54')).toBe(true); });
    it('has 55', () => { const q = new PriorityQueue<number>(); q.enqueue('t55',55,0); expect(q.has('t55')).toBe(true); });
    it('has 56', () => { const q = new PriorityQueue<number>(); q.enqueue('t56',56,0); expect(q.has('t56')).toBe(true); });
    it('has 57', () => { const q = new PriorityQueue<number>(); q.enqueue('t57',57,0); expect(q.has('t57')).toBe(true); });
    it('has 58', () => { const q = new PriorityQueue<number>(); q.enqueue('t58',58,0); expect(q.has('t58')).toBe(true); });
    it('has 59', () => { const q = new PriorityQueue<number>(); q.enqueue('t59',59,0); expect(q.has('t59')).toBe(true); });
    it('has 60', () => { const q = new PriorityQueue<number>(); q.enqueue('t60',60,0); expect(q.has('t60')).toBe(true); });
    it('has 61', () => { const q = new PriorityQueue<number>(); q.enqueue('t61',61,0); expect(q.has('t61')).toBe(true); });
    it('has 62', () => { const q = new PriorityQueue<number>(); q.enqueue('t62',62,0); expect(q.has('t62')).toBe(true); });
    it('has 63', () => { const q = new PriorityQueue<number>(); q.enqueue('t63',63,0); expect(q.has('t63')).toBe(true); });
    it('has 64', () => { const q = new PriorityQueue<number>(); q.enqueue('t64',64,0); expect(q.has('t64')).toBe(true); });
    it('has 65', () => { const q = new PriorityQueue<number>(); q.enqueue('t65',65,0); expect(q.has('t65')).toBe(true); });
    it('has 66', () => { const q = new PriorityQueue<number>(); q.enqueue('t66',66,0); expect(q.has('t66')).toBe(true); });
    it('has 67', () => { const q = new PriorityQueue<number>(); q.enqueue('t67',67,0); expect(q.has('t67')).toBe(true); });
    it('has 68', () => { const q = new PriorityQueue<number>(); q.enqueue('t68',68,0); expect(q.has('t68')).toBe(true); });
    it('has 69', () => { const q = new PriorityQueue<number>(); q.enqueue('t69',69,0); expect(q.has('t69')).toBe(true); });
    it('has 70', () => { const q = new PriorityQueue<number>(); q.enqueue('t70',70,0); expect(q.has('t70')).toBe(true); });
    it('has 71', () => { const q = new PriorityQueue<number>(); q.enqueue('t71',71,0); expect(q.has('t71')).toBe(true); });
    it('has 72', () => { const q = new PriorityQueue<number>(); q.enqueue('t72',72,0); expect(q.has('t72')).toBe(true); });
    it('has 73', () => { const q = new PriorityQueue<number>(); q.enqueue('t73',73,0); expect(q.has('t73')).toBe(true); });
    it('has 74', () => { const q = new PriorityQueue<number>(); q.enqueue('t74',74,0); expect(q.has('t74')).toBe(true); });
    it('has 75', () => { const q = new PriorityQueue<number>(); q.enqueue('t75',75,0); expect(q.has('t75')).toBe(true); });
    it('has 76', () => { const q = new PriorityQueue<number>(); q.enqueue('t76',76,0); expect(q.has('t76')).toBe(true); });
    it('has 77', () => { const q = new PriorityQueue<number>(); q.enqueue('t77',77,0); expect(q.has('t77')).toBe(true); });
    it('has 78', () => { const q = new PriorityQueue<number>(); q.enqueue('t78',78,0); expect(q.has('t78')).toBe(true); });
    it('has 79', () => { const q = new PriorityQueue<number>(); q.enqueue('t79',79,0); expect(q.has('t79')).toBe(true); });
    it('remove 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t0',0,0); q.remove('t0'); expect(q.has('t0')).toBe(false); });
    it('remove 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t1',1,0); q.remove('t1'); expect(q.has('t1')).toBe(false); });
    it('remove 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t2',2,0); q.remove('t2'); expect(q.has('t2')).toBe(false); });
    it('remove 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t3',3,0); q.remove('t3'); expect(q.has('t3')).toBe(false); });
    it('remove 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t4',4,0); q.remove('t4'); expect(q.has('t4')).toBe(false); });
    it('remove 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t5',5,0); q.remove('t5'); expect(q.has('t5')).toBe(false); });
    it('remove 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t6',6,0); q.remove('t6'); expect(q.has('t6')).toBe(false); });
    it('remove 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t7',7,0); q.remove('t7'); expect(q.has('t7')).toBe(false); });
    it('remove 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t8',8,0); q.remove('t8'); expect(q.has('t8')).toBe(false); });
    it('remove 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t9',9,0); q.remove('t9'); expect(q.has('t9')).toBe(false); });
    it('remove 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t10',10,0); q.remove('t10'); expect(q.has('t10')).toBe(false); });
    it('remove 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t11',11,0); q.remove('t11'); expect(q.has('t11')).toBe(false); });
    it('remove 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t12',12,0); q.remove('t12'); expect(q.has('t12')).toBe(false); });
    it('remove 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t13',13,0); q.remove('t13'); expect(q.has('t13')).toBe(false); });
    it('remove 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t14',14,0); q.remove('t14'); expect(q.has('t14')).toBe(false); });
    it('remove 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t15',15,0); q.remove('t15'); expect(q.has('t15')).toBe(false); });
    it('remove 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t16',16,0); q.remove('t16'); expect(q.has('t16')).toBe(false); });
    it('remove 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t17',17,0); q.remove('t17'); expect(q.has('t17')).toBe(false); });
    it('remove 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t18',18,0); q.remove('t18'); expect(q.has('t18')).toBe(false); });
    it('remove 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t19',19,0); q.remove('t19'); expect(q.has('t19')).toBe(false); });
    it('remove 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t20',20,0); q.remove('t20'); expect(q.has('t20')).toBe(false); });
    it('remove 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t21',21,0); q.remove('t21'); expect(q.has('t21')).toBe(false); });
    it('remove 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t22',22,0); q.remove('t22'); expect(q.has('t22')).toBe(false); });
    it('remove 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t23',23,0); q.remove('t23'); expect(q.has('t23')).toBe(false); });
    it('remove 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t24',24,0); q.remove('t24'); expect(q.has('t24')).toBe(false); });
    it('remove 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t25',25,0); q.remove('t25'); expect(q.has('t25')).toBe(false); });
    it('remove 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t26',26,0); q.remove('t26'); expect(q.has('t26')).toBe(false); });
    it('remove 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t27',27,0); q.remove('t27'); expect(q.has('t27')).toBe(false); });
    it('remove 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t28',28,0); q.remove('t28'); expect(q.has('t28')).toBe(false); });
    it('remove 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t29',29,0); q.remove('t29'); expect(q.has('t29')).toBe(false); });
    it('remove 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t30',30,0); q.remove('t30'); expect(q.has('t30')).toBe(false); });
    it('remove 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t31',31,0); q.remove('t31'); expect(q.has('t31')).toBe(false); });
    it('remove 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t32',32,0); q.remove('t32'); expect(q.has('t32')).toBe(false); });
    it('remove 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t33',33,0); q.remove('t33'); expect(q.has('t33')).toBe(false); });
    it('remove 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t34',34,0); q.remove('t34'); expect(q.has('t34')).toBe(false); });
    it('remove 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t35',35,0); q.remove('t35'); expect(q.has('t35')).toBe(false); });
    it('remove 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t36',36,0); q.remove('t36'); expect(q.has('t36')).toBe(false); });
    it('remove 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t37',37,0); q.remove('t37'); expect(q.has('t37')).toBe(false); });
    it('remove 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t38',38,0); q.remove('t38'); expect(q.has('t38')).toBe(false); });
    it('remove 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t39',39,0); q.remove('t39'); expect(q.has('t39')).toBe(false); });
    it('remove 40', () => { const q = new PriorityQueue<number>(); q.enqueue('t40',40,0); q.remove('t40'); expect(q.has('t40')).toBe(false); });
    it('remove 41', () => { const q = new PriorityQueue<number>(); q.enqueue('t41',41,0); q.remove('t41'); expect(q.has('t41')).toBe(false); });
    it('remove 42', () => { const q = new PriorityQueue<number>(); q.enqueue('t42',42,0); q.remove('t42'); expect(q.has('t42')).toBe(false); });
    it('remove 43', () => { const q = new PriorityQueue<number>(); q.enqueue('t43',43,0); q.remove('t43'); expect(q.has('t43')).toBe(false); });
    it('remove 44', () => { const q = new PriorityQueue<number>(); q.enqueue('t44',44,0); q.remove('t44'); expect(q.has('t44')).toBe(false); });
    it('remove 45', () => { const q = new PriorityQueue<number>(); q.enqueue('t45',45,0); q.remove('t45'); expect(q.has('t45')).toBe(false); });
    it('remove 46', () => { const q = new PriorityQueue<number>(); q.enqueue('t46',46,0); q.remove('t46'); expect(q.has('t46')).toBe(false); });
    it('remove 47', () => { const q = new PriorityQueue<number>(); q.enqueue('t47',47,0); q.remove('t47'); expect(q.has('t47')).toBe(false); });
    it('remove 48', () => { const q = new PriorityQueue<number>(); q.enqueue('t48',48,0); q.remove('t48'); expect(q.has('t48')).toBe(false); });
    it('remove 49', () => { const q = new PriorityQueue<number>(); q.enqueue('t49',49,0); q.remove('t49'); expect(q.has('t49')).toBe(false); });
    it('remove 50', () => { const q = new PriorityQueue<number>(); q.enqueue('t50',50,0); q.remove('t50'); expect(q.has('t50')).toBe(false); });
    it('remove 51', () => { const q = new PriorityQueue<number>(); q.enqueue('t51',51,0); q.remove('t51'); expect(q.has('t51')).toBe(false); });
    it('remove 52', () => { const q = new PriorityQueue<number>(); q.enqueue('t52',52,0); q.remove('t52'); expect(q.has('t52')).toBe(false); });
    it('remove 53', () => { const q = new PriorityQueue<number>(); q.enqueue('t53',53,0); q.remove('t53'); expect(q.has('t53')).toBe(false); });
    it('remove 54', () => { const q = new PriorityQueue<number>(); q.enqueue('t54',54,0); q.remove('t54'); expect(q.has('t54')).toBe(false); });
    it('remove 55', () => { const q = new PriorityQueue<number>(); q.enqueue('t55',55,0); q.remove('t55'); expect(q.has('t55')).toBe(false); });
    it('remove 56', () => { const q = new PriorityQueue<number>(); q.enqueue('t56',56,0); q.remove('t56'); expect(q.has('t56')).toBe(false); });
    it('remove 57', () => { const q = new PriorityQueue<number>(); q.enqueue('t57',57,0); q.remove('t57'); expect(q.has('t57')).toBe(false); });
    it('remove 58', () => { const q = new PriorityQueue<number>(); q.enqueue('t58',58,0); q.remove('t58'); expect(q.has('t58')).toBe(false); });
    it('remove 59', () => { const q = new PriorityQueue<number>(); q.enqueue('t59',59,0); q.remove('t59'); expect(q.has('t59')).toBe(false); });
    it('remove 60', () => { const q = new PriorityQueue<number>(); q.enqueue('t60',60,0); q.remove('t60'); expect(q.has('t60')).toBe(false); });
    it('remove 61', () => { const q = new PriorityQueue<number>(); q.enqueue('t61',61,0); q.remove('t61'); expect(q.has('t61')).toBe(false); });
    it('remove 62', () => { const q = new PriorityQueue<number>(); q.enqueue('t62',62,0); q.remove('t62'); expect(q.has('t62')).toBe(false); });
    it('remove 63', () => { const q = new PriorityQueue<number>(); q.enqueue('t63',63,0); q.remove('t63'); expect(q.has('t63')).toBe(false); });
    it('remove 64', () => { const q = new PriorityQueue<number>(); q.enqueue('t64',64,0); q.remove('t64'); expect(q.has('t64')).toBe(false); });
    it('remove 65', () => { const q = new PriorityQueue<number>(); q.enqueue('t65',65,0); q.remove('t65'); expect(q.has('t65')).toBe(false); });
    it('remove 66', () => { const q = new PriorityQueue<number>(); q.enqueue('t66',66,0); q.remove('t66'); expect(q.has('t66')).toBe(false); });
    it('remove 67', () => { const q = new PriorityQueue<number>(); q.enqueue('t67',67,0); q.remove('t67'); expect(q.has('t67')).toBe(false); });
    it('remove 68', () => { const q = new PriorityQueue<number>(); q.enqueue('t68',68,0); q.remove('t68'); expect(q.has('t68')).toBe(false); });
    it('remove 69', () => { const q = new PriorityQueue<number>(); q.enqueue('t69',69,0); q.remove('t69'); expect(q.has('t69')).toBe(false); });
    it('remove 70', () => { const q = new PriorityQueue<number>(); q.enqueue('t70',70,0); q.remove('t70'); expect(q.has('t70')).toBe(false); });
    it('remove 71', () => { const q = new PriorityQueue<number>(); q.enqueue('t71',71,0); q.remove('t71'); expect(q.has('t71')).toBe(false); });
    it('remove 72', () => { const q = new PriorityQueue<number>(); q.enqueue('t72',72,0); q.remove('t72'); expect(q.has('t72')).toBe(false); });
    it('remove 73', () => { const q = new PriorityQueue<number>(); q.enqueue('t73',73,0); q.remove('t73'); expect(q.has('t73')).toBe(false); });
    it('remove 74', () => { const q = new PriorityQueue<number>(); q.enqueue('t74',74,0); q.remove('t74'); expect(q.has('t74')).toBe(false); });
    it('remove 75', () => { const q = new PriorityQueue<number>(); q.enqueue('t75',75,0); q.remove('t75'); expect(q.has('t75')).toBe(false); });
    it('remove 76', () => { const q = new PriorityQueue<number>(); q.enqueue('t76',76,0); q.remove('t76'); expect(q.has('t76')).toBe(false); });
    it('remove 77', () => { const q = new PriorityQueue<number>(); q.enqueue('t77',77,0); q.remove('t77'); expect(q.has('t77')).toBe(false); });
    it('remove 78', () => { const q = new PriorityQueue<number>(); q.enqueue('t78',78,0); q.remove('t78'); expect(q.has('t78')).toBe(false); });
    it('remove 79', () => { const q = new PriorityQueue<number>(); q.enqueue('t79',79,0); q.remove('t79'); expect(q.has('t79')).toBe(false); });
  });
  describe('PriorityQueue stats', () => {
    it('stats 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t',0,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t',1,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t',2,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t',3,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t',4,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t',5,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t',6,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t',7,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t',8,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t',9,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t',10,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t',11,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t',12,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t',13,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t',14,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t',15,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t',16,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t',17,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t',18,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t',19,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t',20,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t',21,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t',22,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t',23,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t',24,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t',25,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t',26,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t',27,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t',28,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t',29,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t',30,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t',31,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t',32,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t',33,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t',34,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t',35,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t',36,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t',37,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t',38,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t',39,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 40', () => { const q = new PriorityQueue<number>(); q.enqueue('t',40,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 41', () => { const q = new PriorityQueue<number>(); q.enqueue('t',41,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 42', () => { const q = new PriorityQueue<number>(); q.enqueue('t',42,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 43', () => { const q = new PriorityQueue<number>(); q.enqueue('t',43,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 44', () => { const q = new PriorityQueue<number>(); q.enqueue('t',44,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 45', () => { const q = new PriorityQueue<number>(); q.enqueue('t',45,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 46', () => { const q = new PriorityQueue<number>(); q.enqueue('t',46,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 47', () => { const q = new PriorityQueue<number>(); q.enqueue('t',47,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 48', () => { const q = new PriorityQueue<number>(); q.enqueue('t',48,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 49', () => { const q = new PriorityQueue<number>(); q.enqueue('t',49,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 50', () => { const q = new PriorityQueue<number>(); q.enqueue('t',50,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 51', () => { const q = new PriorityQueue<number>(); q.enqueue('t',51,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 52', () => { const q = new PriorityQueue<number>(); q.enqueue('t',52,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 53', () => { const q = new PriorityQueue<number>(); q.enqueue('t',53,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 54', () => { const q = new PriorityQueue<number>(); q.enqueue('t',54,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 55', () => { const q = new PriorityQueue<number>(); q.enqueue('t',55,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 56', () => { const q = new PriorityQueue<number>(); q.enqueue('t',56,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 57', () => { const q = new PriorityQueue<number>(); q.enqueue('t',57,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 58', () => { const q = new PriorityQueue<number>(); q.enqueue('t',58,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
    it('stats 59', () => { const q = new PriorityQueue<number>(); q.enqueue('t',59,0); q.dequeue(); const s=q.stats(); expect(s.processed).toBe(1); });
  });
  describe('FIFOQueue', () => {
    it('fifo 0', () => { const q = new FIFOQueue<number>(); q.enqueue(0); expect(q.dequeue()).toBe(0); });
    it('fifo 1', () => { const q = new FIFOQueue<number>(); q.enqueue(1); expect(q.dequeue()).toBe(1); });
    it('fifo 2', () => { const q = new FIFOQueue<number>(); q.enqueue(2); expect(q.dequeue()).toBe(2); });
    it('fifo 3', () => { const q = new FIFOQueue<number>(); q.enqueue(3); expect(q.dequeue()).toBe(3); });
    it('fifo 4', () => { const q = new FIFOQueue<number>(); q.enqueue(4); expect(q.dequeue()).toBe(4); });
    it('fifo 5', () => { const q = new FIFOQueue<number>(); q.enqueue(5); expect(q.dequeue()).toBe(5); });
    it('fifo 6', () => { const q = new FIFOQueue<number>(); q.enqueue(6); expect(q.dequeue()).toBe(6); });
    it('fifo 7', () => { const q = new FIFOQueue<number>(); q.enqueue(7); expect(q.dequeue()).toBe(7); });
    it('fifo 8', () => { const q = new FIFOQueue<number>(); q.enqueue(8); expect(q.dequeue()).toBe(8); });
    it('fifo 9', () => { const q = new FIFOQueue<number>(); q.enqueue(9); expect(q.dequeue()).toBe(9); });
    it('fifo 10', () => { const q = new FIFOQueue<number>(); q.enqueue(10); expect(q.dequeue()).toBe(10); });
    it('fifo 11', () => { const q = new FIFOQueue<number>(); q.enqueue(11); expect(q.dequeue()).toBe(11); });
    it('fifo 12', () => { const q = new FIFOQueue<number>(); q.enqueue(12); expect(q.dequeue()).toBe(12); });
    it('fifo 13', () => { const q = new FIFOQueue<number>(); q.enqueue(13); expect(q.dequeue()).toBe(13); });
    it('fifo 14', () => { const q = new FIFOQueue<number>(); q.enqueue(14); expect(q.dequeue()).toBe(14); });
    it('fifo 15', () => { const q = new FIFOQueue<number>(); q.enqueue(15); expect(q.dequeue()).toBe(15); });
    it('fifo 16', () => { const q = new FIFOQueue<number>(); q.enqueue(16); expect(q.dequeue()).toBe(16); });
    it('fifo 17', () => { const q = new FIFOQueue<number>(); q.enqueue(17); expect(q.dequeue()).toBe(17); });
    it('fifo 18', () => { const q = new FIFOQueue<number>(); q.enqueue(18); expect(q.dequeue()).toBe(18); });
    it('fifo 19', () => { const q = new FIFOQueue<number>(); q.enqueue(19); expect(q.dequeue()).toBe(19); });
    it('fifo 20', () => { const q = new FIFOQueue<number>(); q.enqueue(20); expect(q.dequeue()).toBe(20); });
    it('fifo 21', () => { const q = new FIFOQueue<number>(); q.enqueue(21); expect(q.dequeue()).toBe(21); });
    it('fifo 22', () => { const q = new FIFOQueue<number>(); q.enqueue(22); expect(q.dequeue()).toBe(22); });
    it('fifo 23', () => { const q = new FIFOQueue<number>(); q.enqueue(23); expect(q.dequeue()).toBe(23); });
    it('fifo 24', () => { const q = new FIFOQueue<number>(); q.enqueue(24); expect(q.dequeue()).toBe(24); });
    it('fifo 25', () => { const q = new FIFOQueue<number>(); q.enqueue(25); expect(q.dequeue()).toBe(25); });
    it('fifo 26', () => { const q = new FIFOQueue<number>(); q.enqueue(26); expect(q.dequeue()).toBe(26); });
    it('fifo 27', () => { const q = new FIFOQueue<number>(); q.enqueue(27); expect(q.dequeue()).toBe(27); });
    it('fifo 28', () => { const q = new FIFOQueue<number>(); q.enqueue(28); expect(q.dequeue()).toBe(28); });
    it('fifo 29', () => { const q = new FIFOQueue<number>(); q.enqueue(29); expect(q.dequeue()).toBe(29); });
    it('fifo 30', () => { const q = new FIFOQueue<number>(); q.enqueue(30); expect(q.dequeue()).toBe(30); });
    it('fifo 31', () => { const q = new FIFOQueue<number>(); q.enqueue(31); expect(q.dequeue()).toBe(31); });
    it('fifo 32', () => { const q = new FIFOQueue<number>(); q.enqueue(32); expect(q.dequeue()).toBe(32); });
    it('fifo 33', () => { const q = new FIFOQueue<number>(); q.enqueue(33); expect(q.dequeue()).toBe(33); });
    it('fifo 34', () => { const q = new FIFOQueue<number>(); q.enqueue(34); expect(q.dequeue()).toBe(34); });
    it('fifo 35', () => { const q = new FIFOQueue<number>(); q.enqueue(35); expect(q.dequeue()).toBe(35); });
    it('fifo 36', () => { const q = new FIFOQueue<number>(); q.enqueue(36); expect(q.dequeue()).toBe(36); });
    it('fifo 37', () => { const q = new FIFOQueue<number>(); q.enqueue(37); expect(q.dequeue()).toBe(37); });
    it('fifo 38', () => { const q = new FIFOQueue<number>(); q.enqueue(38); expect(q.dequeue()).toBe(38); });
    it('fifo 39', () => { const q = new FIFOQueue<number>(); q.enqueue(39); expect(q.dequeue()).toBe(39); });
    it('fifo 40', () => { const q = new FIFOQueue<number>(); q.enqueue(40); expect(q.dequeue()).toBe(40); });
    it('fifo 41', () => { const q = new FIFOQueue<number>(); q.enqueue(41); expect(q.dequeue()).toBe(41); });
    it('fifo 42', () => { const q = new FIFOQueue<number>(); q.enqueue(42); expect(q.dequeue()).toBe(42); });
    it('fifo 43', () => { const q = new FIFOQueue<number>(); q.enqueue(43); expect(q.dequeue()).toBe(43); });
    it('fifo 44', () => { const q = new FIFOQueue<number>(); q.enqueue(44); expect(q.dequeue()).toBe(44); });
    it('fifo 45', () => { const q = new FIFOQueue<number>(); q.enqueue(45); expect(q.dequeue()).toBe(45); });
    it('fifo 46', () => { const q = new FIFOQueue<number>(); q.enqueue(46); expect(q.dequeue()).toBe(46); });
    it('fifo 47', () => { const q = new FIFOQueue<number>(); q.enqueue(47); expect(q.dequeue()).toBe(47); });
    it('fifo 48', () => { const q = new FIFOQueue<number>(); q.enqueue(48); expect(q.dequeue()).toBe(48); });
    it('fifo 49', () => { const q = new FIFOQueue<number>(); q.enqueue(49); expect(q.dequeue()).toBe(49); });
    it('fifo 50', () => { const q = new FIFOQueue<number>(); q.enqueue(50); expect(q.dequeue()).toBe(50); });
    it('fifo 51', () => { const q = new FIFOQueue<number>(); q.enqueue(51); expect(q.dequeue()).toBe(51); });
    it('fifo 52', () => { const q = new FIFOQueue<number>(); q.enqueue(52); expect(q.dequeue()).toBe(52); });
    it('fifo 53', () => { const q = new FIFOQueue<number>(); q.enqueue(53); expect(q.dequeue()).toBe(53); });
    it('fifo 54', () => { const q = new FIFOQueue<number>(); q.enqueue(54); expect(q.dequeue()).toBe(54); });
    it('fifo 55', () => { const q = new FIFOQueue<number>(); q.enqueue(55); expect(q.dequeue()).toBe(55); });
    it('fifo 56', () => { const q = new FIFOQueue<number>(); q.enqueue(56); expect(q.dequeue()).toBe(56); });
    it('fifo 57', () => { const q = new FIFOQueue<number>(); q.enqueue(57); expect(q.dequeue()).toBe(57); });
    it('fifo 58', () => { const q = new FIFOQueue<number>(); q.enqueue(58); expect(q.dequeue()).toBe(58); });
    it('fifo 59', () => { const q = new FIFOQueue<number>(); q.enqueue(59); expect(q.dequeue()).toBe(59); });
    it('fifo 60', () => { const q = new FIFOQueue<number>(); q.enqueue(60); expect(q.dequeue()).toBe(60); });
    it('fifo 61', () => { const q = new FIFOQueue<number>(); q.enqueue(61); expect(q.dequeue()).toBe(61); });
    it('fifo 62', () => { const q = new FIFOQueue<number>(); q.enqueue(62); expect(q.dequeue()).toBe(62); });
    it('fifo 63', () => { const q = new FIFOQueue<number>(); q.enqueue(63); expect(q.dequeue()).toBe(63); });
    it('fifo 64', () => { const q = new FIFOQueue<number>(); q.enqueue(64); expect(q.dequeue()).toBe(64); });
    it('fifo 65', () => { const q = new FIFOQueue<number>(); q.enqueue(65); expect(q.dequeue()).toBe(65); });
    it('fifo 66', () => { const q = new FIFOQueue<number>(); q.enqueue(66); expect(q.dequeue()).toBe(66); });
    it('fifo 67', () => { const q = new FIFOQueue<number>(); q.enqueue(67); expect(q.dequeue()).toBe(67); });
    it('fifo 68', () => { const q = new FIFOQueue<number>(); q.enqueue(68); expect(q.dequeue()).toBe(68); });
    it('fifo 69', () => { const q = new FIFOQueue<number>(); q.enqueue(69); expect(q.dequeue()).toBe(69); });
    it('fifo 70', () => { const q = new FIFOQueue<number>(); q.enqueue(70); expect(q.dequeue()).toBe(70); });
    it('fifo 71', () => { const q = new FIFOQueue<number>(); q.enqueue(71); expect(q.dequeue()).toBe(71); });
    it('fifo 72', () => { const q = new FIFOQueue<number>(); q.enqueue(72); expect(q.dequeue()).toBe(72); });
    it('fifo 73', () => { const q = new FIFOQueue<number>(); q.enqueue(73); expect(q.dequeue()).toBe(73); });
    it('fifo 74', () => { const q = new FIFOQueue<number>(); q.enqueue(74); expect(q.dequeue()).toBe(74); });
    it('fifo 75', () => { const q = new FIFOQueue<number>(); q.enqueue(75); expect(q.dequeue()).toBe(75); });
    it('fifo 76', () => { const q = new FIFOQueue<number>(); q.enqueue(76); expect(q.dequeue()).toBe(76); });
    it('fifo 77', () => { const q = new FIFOQueue<number>(); q.enqueue(77); expect(q.dequeue()).toBe(77); });
    it('fifo 78', () => { const q = new FIFOQueue<number>(); q.enqueue(78); expect(q.dequeue()).toBe(78); });
    it('fifo 79', () => { const q = new FIFOQueue<number>(); q.enqueue(79); expect(q.dequeue()).toBe(79); });
    it('fifo 80', () => { const q = new FIFOQueue<number>(); q.enqueue(80); expect(q.dequeue()).toBe(80); });
    it('fifo 81', () => { const q = new FIFOQueue<number>(); q.enqueue(81); expect(q.dequeue()).toBe(81); });
    it('fifo 82', () => { const q = new FIFOQueue<number>(); q.enqueue(82); expect(q.dequeue()).toBe(82); });
    it('fifo 83', () => { const q = new FIFOQueue<number>(); q.enqueue(83); expect(q.dequeue()).toBe(83); });
    it('fifo 84', () => { const q = new FIFOQueue<number>(); q.enqueue(84); expect(q.dequeue()).toBe(84); });
    it('fifo 85', () => { const q = new FIFOQueue<number>(); q.enqueue(85); expect(q.dequeue()).toBe(85); });
    it('fifo 86', () => { const q = new FIFOQueue<number>(); q.enqueue(86); expect(q.dequeue()).toBe(86); });
    it('fifo 87', () => { const q = new FIFOQueue<number>(); q.enqueue(87); expect(q.dequeue()).toBe(87); });
    it('fifo 88', () => { const q = new FIFOQueue<number>(); q.enqueue(88); expect(q.dequeue()).toBe(88); });
    it('fifo 89', () => { const q = new FIFOQueue<number>(); q.enqueue(89); expect(q.dequeue()).toBe(89); });
    it('fifo 90', () => { const q = new FIFOQueue<number>(); q.enqueue(90); expect(q.dequeue()).toBe(90); });
    it('fifo 91', () => { const q = new FIFOQueue<number>(); q.enqueue(91); expect(q.dequeue()).toBe(91); });
    it('fifo 92', () => { const q = new FIFOQueue<number>(); q.enqueue(92); expect(q.dequeue()).toBe(92); });
    it('fifo 93', () => { const q = new FIFOQueue<number>(); q.enqueue(93); expect(q.dequeue()).toBe(93); });
    it('fifo 94', () => { const q = new FIFOQueue<number>(); q.enqueue(94); expect(q.dequeue()).toBe(94); });
    it('fifo 95', () => { const q = new FIFOQueue<number>(); q.enqueue(95); expect(q.dequeue()).toBe(95); });
    it('fifo 96', () => { const q = new FIFOQueue<number>(); q.enqueue(96); expect(q.dequeue()).toBe(96); });
    it('fifo 97', () => { const q = new FIFOQueue<number>(); q.enqueue(97); expect(q.dequeue()).toBe(97); });
    it('fifo 98', () => { const q = new FIFOQueue<number>(); q.enqueue(98); expect(q.dequeue()).toBe(98); });
    it('fifo 99', () => { const q = new FIFOQueue<number>(); q.enqueue(99); expect(q.dequeue()).toBe(99); });
    it('fifo order 0', () => { const q = new FIFOQueue<number>(); for(let j=0;j<2;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 1', () => { const q = new FIFOQueue<number>(); for(let j=0;j<3;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 2', () => { const q = new FIFOQueue<number>(); for(let j=0;j<4;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 3', () => { const q = new FIFOQueue<number>(); for(let j=0;j<5;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 4', () => { const q = new FIFOQueue<number>(); for(let j=0;j<6;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 5', () => { const q = new FIFOQueue<number>(); for(let j=0;j<7;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 6', () => { const q = new FIFOQueue<number>(); for(let j=0;j<8;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 7', () => { const q = new FIFOQueue<number>(); for(let j=0;j<9;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 8', () => { const q = new FIFOQueue<number>(); for(let j=0;j<10;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 9', () => { const q = new FIFOQueue<number>(); for(let j=0;j<11;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 10', () => { const q = new FIFOQueue<number>(); for(let j=0;j<2;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 11', () => { const q = new FIFOQueue<number>(); for(let j=0;j<3;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 12', () => { const q = new FIFOQueue<number>(); for(let j=0;j<4;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 13', () => { const q = new FIFOQueue<number>(); for(let j=0;j<5;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 14', () => { const q = new FIFOQueue<number>(); for(let j=0;j<6;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 15', () => { const q = new FIFOQueue<number>(); for(let j=0;j<7;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 16', () => { const q = new FIFOQueue<number>(); for(let j=0;j<8;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 17', () => { const q = new FIFOQueue<number>(); for(let j=0;j<9;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 18', () => { const q = new FIFOQueue<number>(); for(let j=0;j<10;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 19', () => { const q = new FIFOQueue<number>(); for(let j=0;j<11;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 20', () => { const q = new FIFOQueue<number>(); for(let j=0;j<2;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 21', () => { const q = new FIFOQueue<number>(); for(let j=0;j<3;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 22', () => { const q = new FIFOQueue<number>(); for(let j=0;j<4;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 23', () => { const q = new FIFOQueue<number>(); for(let j=0;j<5;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 24', () => { const q = new FIFOQueue<number>(); for(let j=0;j<6;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 25', () => { const q = new FIFOQueue<number>(); for(let j=0;j<7;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 26', () => { const q = new FIFOQueue<number>(); for(let j=0;j<8;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 27', () => { const q = new FIFOQueue<number>(); for(let j=0;j<9;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 28', () => { const q = new FIFOQueue<number>(); for(let j=0;j<10;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 29', () => { const q = new FIFOQueue<number>(); for(let j=0;j<11;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 30', () => { const q = new FIFOQueue<number>(); for(let j=0;j<2;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 31', () => { const q = new FIFOQueue<number>(); for(let j=0;j<3;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 32', () => { const q = new FIFOQueue<number>(); for(let j=0;j<4;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 33', () => { const q = new FIFOQueue<number>(); for(let j=0;j<5;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 34', () => { const q = new FIFOQueue<number>(); for(let j=0;j<6;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 35', () => { const q = new FIFOQueue<number>(); for(let j=0;j<7;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 36', () => { const q = new FIFOQueue<number>(); for(let j=0;j<8;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 37', () => { const q = new FIFOQueue<number>(); for(let j=0;j<9;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 38', () => { const q = new FIFOQueue<number>(); for(let j=0;j<10;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 39', () => { const q = new FIFOQueue<number>(); for(let j=0;j<11;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 40', () => { const q = new FIFOQueue<number>(); for(let j=0;j<2;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 41', () => { const q = new FIFOQueue<number>(); for(let j=0;j<3;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 42', () => { const q = new FIFOQueue<number>(); for(let j=0;j<4;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 43', () => { const q = new FIFOQueue<number>(); for(let j=0;j<5;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 44', () => { const q = new FIFOQueue<number>(); for(let j=0;j<6;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 45', () => { const q = new FIFOQueue<number>(); for(let j=0;j<7;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 46', () => { const q = new FIFOQueue<number>(); for(let j=0;j<8;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 47', () => { const q = new FIFOQueue<number>(); for(let j=0;j<9;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 48', () => { const q = new FIFOQueue<number>(); for(let j=0;j<10;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 49', () => { const q = new FIFOQueue<number>(); for(let j=0;j<11;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 50', () => { const q = new FIFOQueue<number>(); for(let j=0;j<2;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 51', () => { const q = new FIFOQueue<number>(); for(let j=0;j<3;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 52', () => { const q = new FIFOQueue<number>(); for(let j=0;j<4;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 53', () => { const q = new FIFOQueue<number>(); for(let j=0;j<5;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 54', () => { const q = new FIFOQueue<number>(); for(let j=0;j<6;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 55', () => { const q = new FIFOQueue<number>(); for(let j=0;j<7;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 56', () => { const q = new FIFOQueue<number>(); for(let j=0;j<8;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 57', () => { const q = new FIFOQueue<number>(); for(let j=0;j<9;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 58', () => { const q = new FIFOQueue<number>(); for(let j=0;j<10;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo order 59', () => { const q = new FIFOQueue<number>(); for(let j=0;j<11;j++) q.enqueue(j); expect(q.dequeue()).toBe(0); });
    it('fifo isEmpty', () => { const q = new FIFOQueue<number>(); expect(q.isEmpty()).toBe(true); });
    it('fifo size', () => { const q = new FIFOQueue<number>(); q.enqueue(1); q.enqueue(2); expect(q.size).toBe(2); });
  });
  describe('PriorityQueue drain', () => {
    it('drain 0', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 1', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 2', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 3', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 4', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 5', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 6', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 7', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 8', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 9', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 10', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 11', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 12', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 13', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 14', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 15', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 16', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 17', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 18', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 19', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 20', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 21', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 22', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 23', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 24', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 25', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 26', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 27', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 28', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 29', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 30', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 31', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 32', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 33', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 34', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 35', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 36', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 37', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 38', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 39', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 40', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 41', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 42', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 43', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 44', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 45', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 46', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 47', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 48', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 49', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 50', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 51', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 52', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 53', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 54', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
    it('drain 55', () => { const q = new PriorityQueue<number>(); for(let j=0;j<2;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(2); });
    it('drain 56', () => { const q = new PriorityQueue<number>(); for(let j=0;j<3;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(3); });
    it('drain 57', () => { const q = new PriorityQueue<number>(); for(let j=0;j<4;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(4); });
    it('drain 58', () => { const q = new PriorityQueue<number>(); for(let j=0;j<5;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(5); });
    it('drain 59', () => { const q = new PriorityQueue<number>(); for(let j=0;j<6;j++) q.enqueue('t'+j,j,j); const all=q.drain(); expect(all).toHaveLength(6); });
  });
  describe('updatePriority', () => {
    it('updatePriority 0', () => { const q = new PriorityQueue<number>(); q.enqueue('t0',0,1); const ok=q.updatePriority('t0',0); expect(ok).toBe(true); });
    it('updatePriority 1', () => { const q = new PriorityQueue<number>(); q.enqueue('t1',1,1); const ok=q.updatePriority('t1',1); expect(ok).toBe(true); });
    it('updatePriority 2', () => { const q = new PriorityQueue<number>(); q.enqueue('t2',2,1); const ok=q.updatePriority('t2',2); expect(ok).toBe(true); });
    it('updatePriority 3', () => { const q = new PriorityQueue<number>(); q.enqueue('t3',3,1); const ok=q.updatePriority('t3',3); expect(ok).toBe(true); });
    it('updatePriority 4', () => { const q = new PriorityQueue<number>(); q.enqueue('t4',4,1); const ok=q.updatePriority('t4',4); expect(ok).toBe(true); });
    it('updatePriority 5', () => { const q = new PriorityQueue<number>(); q.enqueue('t5',5,1); const ok=q.updatePriority('t5',5); expect(ok).toBe(true); });
    it('updatePriority 6', () => { const q = new PriorityQueue<number>(); q.enqueue('t6',6,1); const ok=q.updatePriority('t6',6); expect(ok).toBe(true); });
    it('updatePriority 7', () => { const q = new PriorityQueue<number>(); q.enqueue('t7',7,1); const ok=q.updatePriority('t7',7); expect(ok).toBe(true); });
    it('updatePriority 8', () => { const q = new PriorityQueue<number>(); q.enqueue('t8',8,1); const ok=q.updatePriority('t8',8); expect(ok).toBe(true); });
    it('updatePriority 9', () => { const q = new PriorityQueue<number>(); q.enqueue('t9',9,1); const ok=q.updatePriority('t9',9); expect(ok).toBe(true); });
    it('updatePriority 10', () => { const q = new PriorityQueue<number>(); q.enqueue('t10',10,1); const ok=q.updatePriority('t10',0); expect(ok).toBe(true); });
    it('updatePriority 11', () => { const q = new PriorityQueue<number>(); q.enqueue('t11',11,1); const ok=q.updatePriority('t11',1); expect(ok).toBe(true); });
    it('updatePriority 12', () => { const q = new PriorityQueue<number>(); q.enqueue('t12',12,1); const ok=q.updatePriority('t12',2); expect(ok).toBe(true); });
    it('updatePriority 13', () => { const q = new PriorityQueue<number>(); q.enqueue('t13',13,1); const ok=q.updatePriority('t13',3); expect(ok).toBe(true); });
    it('updatePriority 14', () => { const q = new PriorityQueue<number>(); q.enqueue('t14',14,1); const ok=q.updatePriority('t14',4); expect(ok).toBe(true); });
    it('updatePriority 15', () => { const q = new PriorityQueue<number>(); q.enqueue('t15',15,1); const ok=q.updatePriority('t15',5); expect(ok).toBe(true); });
    it('updatePriority 16', () => { const q = new PriorityQueue<number>(); q.enqueue('t16',16,1); const ok=q.updatePriority('t16',6); expect(ok).toBe(true); });
    it('updatePriority 17', () => { const q = new PriorityQueue<number>(); q.enqueue('t17',17,1); const ok=q.updatePriority('t17',7); expect(ok).toBe(true); });
    it('updatePriority 18', () => { const q = new PriorityQueue<number>(); q.enqueue('t18',18,1); const ok=q.updatePriority('t18',8); expect(ok).toBe(true); });
    it('updatePriority 19', () => { const q = new PriorityQueue<number>(); q.enqueue('t19',19,1); const ok=q.updatePriority('t19',9); expect(ok).toBe(true); });
    it('updatePriority 20', () => { const q = new PriorityQueue<number>(); q.enqueue('t20',20,1); const ok=q.updatePriority('t20',0); expect(ok).toBe(true); });
    it('updatePriority 21', () => { const q = new PriorityQueue<number>(); q.enqueue('t21',21,1); const ok=q.updatePriority('t21',1); expect(ok).toBe(true); });
    it('updatePriority 22', () => { const q = new PriorityQueue<number>(); q.enqueue('t22',22,1); const ok=q.updatePriority('t22',2); expect(ok).toBe(true); });
    it('updatePriority 23', () => { const q = new PriorityQueue<number>(); q.enqueue('t23',23,1); const ok=q.updatePriority('t23',3); expect(ok).toBe(true); });
    it('updatePriority 24', () => { const q = new PriorityQueue<number>(); q.enqueue('t24',24,1); const ok=q.updatePriority('t24',4); expect(ok).toBe(true); });
    it('updatePriority 25', () => { const q = new PriorityQueue<number>(); q.enqueue('t25',25,1); const ok=q.updatePriority('t25',5); expect(ok).toBe(true); });
    it('updatePriority 26', () => { const q = new PriorityQueue<number>(); q.enqueue('t26',26,1); const ok=q.updatePriority('t26',6); expect(ok).toBe(true); });
    it('updatePriority 27', () => { const q = new PriorityQueue<number>(); q.enqueue('t27',27,1); const ok=q.updatePriority('t27',7); expect(ok).toBe(true); });
    it('updatePriority 28', () => { const q = new PriorityQueue<number>(); q.enqueue('t28',28,1); const ok=q.updatePriority('t28',8); expect(ok).toBe(true); });
    it('updatePriority 29', () => { const q = new PriorityQueue<number>(); q.enqueue('t29',29,1); const ok=q.updatePriority('t29',9); expect(ok).toBe(true); });
    it('updatePriority 30', () => { const q = new PriorityQueue<number>(); q.enqueue('t30',30,1); const ok=q.updatePriority('t30',0); expect(ok).toBe(true); });
    it('updatePriority 31', () => { const q = new PriorityQueue<number>(); q.enqueue('t31',31,1); const ok=q.updatePriority('t31',1); expect(ok).toBe(true); });
    it('updatePriority 32', () => { const q = new PriorityQueue<number>(); q.enqueue('t32',32,1); const ok=q.updatePriority('t32',2); expect(ok).toBe(true); });
    it('updatePriority 33', () => { const q = new PriorityQueue<number>(); q.enqueue('t33',33,1); const ok=q.updatePriority('t33',3); expect(ok).toBe(true); });
    it('updatePriority 34', () => { const q = new PriorityQueue<number>(); q.enqueue('t34',34,1); const ok=q.updatePriority('t34',4); expect(ok).toBe(true); });
    it('updatePriority 35', () => { const q = new PriorityQueue<number>(); q.enqueue('t35',35,1); const ok=q.updatePriority('t35',5); expect(ok).toBe(true); });
    it('updatePriority 36', () => { const q = new PriorityQueue<number>(); q.enqueue('t36',36,1); const ok=q.updatePriority('t36',6); expect(ok).toBe(true); });
    it('updatePriority 37', () => { const q = new PriorityQueue<number>(); q.enqueue('t37',37,1); const ok=q.updatePriority('t37',7); expect(ok).toBe(true); });
    it('updatePriority 38', () => { const q = new PriorityQueue<number>(); q.enqueue('t38',38,1); const ok=q.updatePriority('t38',8); expect(ok).toBe(true); });
    it('updatePriority 39', () => { const q = new PriorityQueue<number>(); q.enqueue('t39',39,1); const ok=q.updatePriority('t39',9); expect(ok).toBe(true); });
    it('updatePriority missing', () => { const q = new PriorityQueue<number>(); expect(q.updatePriority('missing',5)).toBe(false); });
  });

  describe('task-queue extras', () => {
    it('pq extra 0: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t0',0,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 1: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t1',1,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 2: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t2',2,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 3: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t3',3,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 4: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t4',4,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 5: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t5',5,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 6: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t6',6,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 7: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t7',7,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 8: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t8',8,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 9: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t9',9,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 10: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t10',10,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 11: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t11',11,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 12: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t12',12,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 13: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t13',13,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 14: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t14',14,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 15: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t15',15,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 16: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t16',16,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 17: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t17',17,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 18: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t18',18,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 19: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t19',19,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 20: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t20',20,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 21: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t21',21,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 22: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t22',22,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 23: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t23',23,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 24: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t24',24,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 25: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t25',25,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 26: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t26',26,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 27: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t27',27,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 28: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t28',28,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 29: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t29',29,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 30: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t30',30,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 31: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t31',31,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 32: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t32',32,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 33: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t33',33,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 34: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t34',34,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 35: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t35',35,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 36: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t36',36,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 37: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t37',37,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 38: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t38',38,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 39: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t39',39,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 40: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t40',40,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 41: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t41',41,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 42: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t42',42,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 43: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t43',43,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 44: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t44',44,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 45: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t45',45,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 46: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t46',46,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 47: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t47',47,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 48: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t48',48,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 49: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t49',49,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 50: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t50',50,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 51: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t51',51,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 52: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t52',52,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 53: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t53',53,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 54: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t54',54,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 55: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t55',55,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 56: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t56',56,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 57: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t57',57,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 58: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t58',58,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 59: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t59',59,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 60: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t60',60,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 61: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t61',61,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 62: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t62',62,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 63: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t63',63,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 64: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t64',64,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 65: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t65',65,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 66: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t66',66,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 67: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t67',67,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 68: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t68',68,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 69: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t69',69,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 70: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t70',70,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 71: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t71',71,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 72: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t72',72,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 73: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t73',73,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 74: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t74',74,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 75: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t75',75,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 76: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t76',76,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 77: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t77',77,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 78: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t78',78,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 79: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t79',79,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 80: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t80',80,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 81: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t81',81,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 82: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t82',82,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 83: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t83',83,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 84: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t84',84,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 85: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t85',85,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 86: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t86',86,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 87: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t87',87,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 88: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t88',88,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 89: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t89',89,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 90: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t90',90,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 91: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t91',91,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 92: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t92',92,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 93: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t93',93,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 94: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t94',94,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 95: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t95',95,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 96: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t96',96,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 97: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t97',97,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 98: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t98',98,0); expect(q.toArray()).toHaveLength(1); });
    it('pq extra 99: toArray', () => { const q = new PriorityQueue<number>(); q.enqueue('t99',99,0); expect(q.toArray()).toHaveLength(1); });
  });
});
