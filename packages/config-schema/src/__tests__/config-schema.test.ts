// Copyright (c) 2026 Nexara DMCC. All rights reserved.

import { defineSchema, validate, coerce, applyDefaults, requiredFields,
  optionalFields, mergeSchemas, pickFields, stripUnknown } from '../config-schema';
import type { Schema } from '../config-schema';

const basicSchema: Schema = {
  name: { type: 'string', required: true, min: 1, max: 100 },
  age:  { type: 'number', required: true, min: 0, max: 150 },
  email: { type: 'string', required: false, pattern: /^[^@]+@[^@]+$/ },
  role:  { type: 'string', enum: ['admin','user','guest'], default: 'user' },
  active: { type: 'boolean', default: true },
};

describe('config-schema', () => {
  describe('defineSchema', () => {
    it('defineSchema 0', () => { const s = defineSchema({ field0: { type: 'string' } }); expect(s.field0).toBeDefined(); });
    it('defineSchema 1', () => { const s = defineSchema({ field1: { type: 'string' } }); expect(s.field1).toBeDefined(); });
    it('defineSchema 2', () => { const s = defineSchema({ field2: { type: 'string' } }); expect(s.field2).toBeDefined(); });
    it('defineSchema 3', () => { const s = defineSchema({ field3: { type: 'string' } }); expect(s.field3).toBeDefined(); });
    it('defineSchema 4', () => { const s = defineSchema({ field4: { type: 'string' } }); expect(s.field4).toBeDefined(); });
    it('defineSchema 5', () => { const s = defineSchema({ field5: { type: 'string' } }); expect(s.field5).toBeDefined(); });
    it('defineSchema 6', () => { const s = defineSchema({ field6: { type: 'string' } }); expect(s.field6).toBeDefined(); });
    it('defineSchema 7', () => { const s = defineSchema({ field7: { type: 'string' } }); expect(s.field7).toBeDefined(); });
    it('defineSchema 8', () => { const s = defineSchema({ field8: { type: 'string' } }); expect(s.field8).toBeDefined(); });
    it('defineSchema 9', () => { const s = defineSchema({ field9: { type: 'string' } }); expect(s.field9).toBeDefined(); });
    it('defineSchema 10', () => { const s = defineSchema({ field10: { type: 'string' } }); expect(s.field10).toBeDefined(); });
    it('defineSchema 11', () => { const s = defineSchema({ field11: { type: 'string' } }); expect(s.field11).toBeDefined(); });
    it('defineSchema 12', () => { const s = defineSchema({ field12: { type: 'string' } }); expect(s.field12).toBeDefined(); });
    it('defineSchema 13', () => { const s = defineSchema({ field13: { type: 'string' } }); expect(s.field13).toBeDefined(); });
    it('defineSchema 14', () => { const s = defineSchema({ field14: { type: 'string' } }); expect(s.field14).toBeDefined(); });
    it('defineSchema 15', () => { const s = defineSchema({ field15: { type: 'string' } }); expect(s.field15).toBeDefined(); });
    it('defineSchema 16', () => { const s = defineSchema({ field16: { type: 'string' } }); expect(s.field16).toBeDefined(); });
    it('defineSchema 17', () => { const s = defineSchema({ field17: { type: 'string' } }); expect(s.field17).toBeDefined(); });
    it('defineSchema 18', () => { const s = defineSchema({ field18: { type: 'string' } }); expect(s.field18).toBeDefined(); });
    it('defineSchema 19', () => { const s = defineSchema({ field19: { type: 'string' } }); expect(s.field19).toBeDefined(); });
    it('defineSchema 20', () => { const s = defineSchema({ field20: { type: 'string' } }); expect(s.field20).toBeDefined(); });
    it('defineSchema 21', () => { const s = defineSchema({ field21: { type: 'string' } }); expect(s.field21).toBeDefined(); });
    it('defineSchema 22', () => { const s = defineSchema({ field22: { type: 'string' } }); expect(s.field22).toBeDefined(); });
    it('defineSchema 23', () => { const s = defineSchema({ field23: { type: 'string' } }); expect(s.field23).toBeDefined(); });
    it('defineSchema 24', () => { const s = defineSchema({ field24: { type: 'string' } }); expect(s.field24).toBeDefined(); });
    it('defineSchema 25', () => { const s = defineSchema({ field25: { type: 'string' } }); expect(s.field25).toBeDefined(); });
    it('defineSchema 26', () => { const s = defineSchema({ field26: { type: 'string' } }); expect(s.field26).toBeDefined(); });
    it('defineSchema 27', () => { const s = defineSchema({ field27: { type: 'string' } }); expect(s.field27).toBeDefined(); });
    it('defineSchema 28', () => { const s = defineSchema({ field28: { type: 'string' } }); expect(s.field28).toBeDefined(); });
    it('defineSchema 29', () => { const s = defineSchema({ field29: { type: 'string' } }); expect(s.field29).toBeDefined(); });
    it('defineSchema 30', () => { const s = defineSchema({ field30: { type: 'string' } }); expect(s.field30).toBeDefined(); });
    it('defineSchema 31', () => { const s = defineSchema({ field31: { type: 'string' } }); expect(s.field31).toBeDefined(); });
    it('defineSchema 32', () => { const s = defineSchema({ field32: { type: 'string' } }); expect(s.field32).toBeDefined(); });
    it('defineSchema 33', () => { const s = defineSchema({ field33: { type: 'string' } }); expect(s.field33).toBeDefined(); });
    it('defineSchema 34', () => { const s = defineSchema({ field34: { type: 'string' } }); expect(s.field34).toBeDefined(); });
    it('defineSchema 35', () => { const s = defineSchema({ field35: { type: 'string' } }); expect(s.field35).toBeDefined(); });
    it('defineSchema 36', () => { const s = defineSchema({ field36: { type: 'string' } }); expect(s.field36).toBeDefined(); });
    it('defineSchema 37', () => { const s = defineSchema({ field37: { type: 'string' } }); expect(s.field37).toBeDefined(); });
    it('defineSchema 38', () => { const s = defineSchema({ field38: { type: 'string' } }); expect(s.field38).toBeDefined(); });
    it('defineSchema 39', () => { const s = defineSchema({ field39: { type: 'string' } }); expect(s.field39).toBeDefined(); });
    it('defineSchema 40', () => { const s = defineSchema({ field40: { type: 'string' } }); expect(s.field40).toBeDefined(); });
    it('defineSchema 41', () => { const s = defineSchema({ field41: { type: 'string' } }); expect(s.field41).toBeDefined(); });
    it('defineSchema 42', () => { const s = defineSchema({ field42: { type: 'string' } }); expect(s.field42).toBeDefined(); });
    it('defineSchema 43', () => { const s = defineSchema({ field43: { type: 'string' } }); expect(s.field43).toBeDefined(); });
    it('defineSchema 44', () => { const s = defineSchema({ field44: { type: 'string' } }); expect(s.field44).toBeDefined(); });
    it('defineSchema 45', () => { const s = defineSchema({ field45: { type: 'string' } }); expect(s.field45).toBeDefined(); });
    it('defineSchema 46', () => { const s = defineSchema({ field46: { type: 'string' } }); expect(s.field46).toBeDefined(); });
    it('defineSchema 47', () => { const s = defineSchema({ field47: { type: 'string' } }); expect(s.field47).toBeDefined(); });
    it('defineSchema 48', () => { const s = defineSchema({ field48: { type: 'string' } }); expect(s.field48).toBeDefined(); });
    it('defineSchema 49', () => { const s = defineSchema({ field49: { type: 'string' } }); expect(s.field49).toBeDefined(); });
    it('defineSchema 50', () => { const s = defineSchema({ field50: { type: 'string' } }); expect(s.field50).toBeDefined(); });
    it('defineSchema 51', () => { const s = defineSchema({ field51: { type: 'string' } }); expect(s.field51).toBeDefined(); });
    it('defineSchema 52', () => { const s = defineSchema({ field52: { type: 'string' } }); expect(s.field52).toBeDefined(); });
    it('defineSchema 53', () => { const s = defineSchema({ field53: { type: 'string' } }); expect(s.field53).toBeDefined(); });
    it('defineSchema 54', () => { const s = defineSchema({ field54: { type: 'string' } }); expect(s.field54).toBeDefined(); });
    it('defineSchema 55', () => { const s = defineSchema({ field55: { type: 'string' } }); expect(s.field55).toBeDefined(); });
    it('defineSchema 56', () => { const s = defineSchema({ field56: { type: 'string' } }); expect(s.field56).toBeDefined(); });
    it('defineSchema 57', () => { const s = defineSchema({ field57: { type: 'string' } }); expect(s.field57).toBeDefined(); });
    it('defineSchema 58', () => { const s = defineSchema({ field58: { type: 'string' } }); expect(s.field58).toBeDefined(); });
    it('defineSchema 59', () => { const s = defineSchema({ field59: { type: 'string' } }); expect(s.field59).toBeDefined(); });
    it('defineSchema 60', () => { const s = defineSchema({ field60: { type: 'string' } }); expect(s.field60).toBeDefined(); });
    it('defineSchema 61', () => { const s = defineSchema({ field61: { type: 'string' } }); expect(s.field61).toBeDefined(); });
    it('defineSchema 62', () => { const s = defineSchema({ field62: { type: 'string' } }); expect(s.field62).toBeDefined(); });
    it('defineSchema 63', () => { const s = defineSchema({ field63: { type: 'string' } }); expect(s.field63).toBeDefined(); });
    it('defineSchema 64', () => { const s = defineSchema({ field64: { type: 'string' } }); expect(s.field64).toBeDefined(); });
    it('defineSchema 65', () => { const s = defineSchema({ field65: { type: 'string' } }); expect(s.field65).toBeDefined(); });
    it('defineSchema 66', () => { const s = defineSchema({ field66: { type: 'string' } }); expect(s.field66).toBeDefined(); });
    it('defineSchema 67', () => { const s = defineSchema({ field67: { type: 'string' } }); expect(s.field67).toBeDefined(); });
    it('defineSchema 68', () => { const s = defineSchema({ field68: { type: 'string' } }); expect(s.field68).toBeDefined(); });
    it('defineSchema 69', () => { const s = defineSchema({ field69: { type: 'string' } }); expect(s.field69).toBeDefined(); });
    it('defineSchema 70', () => { const s = defineSchema({ field70: { type: 'string' } }); expect(s.field70).toBeDefined(); });
    it('defineSchema 71', () => { const s = defineSchema({ field71: { type: 'string' } }); expect(s.field71).toBeDefined(); });
    it('defineSchema 72', () => { const s = defineSchema({ field72: { type: 'string' } }); expect(s.field72).toBeDefined(); });
    it('defineSchema 73', () => { const s = defineSchema({ field73: { type: 'string' } }); expect(s.field73).toBeDefined(); });
    it('defineSchema 74', () => { const s = defineSchema({ field74: { type: 'string' } }); expect(s.field74).toBeDefined(); });
    it('defineSchema 75', () => { const s = defineSchema({ field75: { type: 'string' } }); expect(s.field75).toBeDefined(); });
    it('defineSchema 76', () => { const s = defineSchema({ field76: { type: 'string' } }); expect(s.field76).toBeDefined(); });
    it('defineSchema 77', () => { const s = defineSchema({ field77: { type: 'string' } }); expect(s.field77).toBeDefined(); });
    it('defineSchema 78', () => { const s = defineSchema({ field78: { type: 'string' } }); expect(s.field78).toBeDefined(); });
    it('defineSchema 79', () => { const s = defineSchema({ field79: { type: 'string' } }); expect(s.field79).toBeDefined(); });
  });
  describe('validate valid data', () => {
    it('valid 0', () => { const r = validate({name:'user0',age:0,email:'user0@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 1', () => { const r = validate({name:'user1',age:1,email:'user1@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 2', () => { const r = validate({name:'user2',age:2,email:'user2@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 3', () => { const r = validate({name:'user3',age:3,email:'user3@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 4', () => { const r = validate({name:'user4',age:4,email:'user4@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 5', () => { const r = validate({name:'user5',age:5,email:'user5@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 6', () => { const r = validate({name:'user6',age:6,email:'user6@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 7', () => { const r = validate({name:'user7',age:7,email:'user7@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 8', () => { const r = validate({name:'user8',age:8,email:'user8@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 9', () => { const r = validate({name:'user9',age:9,email:'user9@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 10', () => { const r = validate({name:'user10',age:10,email:'user10@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 11', () => { const r = validate({name:'user11',age:11,email:'user11@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 12', () => { const r = validate({name:'user12',age:12,email:'user12@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 13', () => { const r = validate({name:'user13',age:13,email:'user13@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 14', () => { const r = validate({name:'user14',age:14,email:'user14@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 15', () => { const r = validate({name:'user15',age:15,email:'user15@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 16', () => { const r = validate({name:'user16',age:16,email:'user16@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 17', () => { const r = validate({name:'user17',age:17,email:'user17@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 18', () => { const r = validate({name:'user18',age:18,email:'user18@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 19', () => { const r = validate({name:'user19',age:19,email:'user19@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 20', () => { const r = validate({name:'user20',age:20,email:'user20@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 21', () => { const r = validate({name:'user21',age:21,email:'user21@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 22', () => { const r = validate({name:'user22',age:22,email:'user22@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 23', () => { const r = validate({name:'user23',age:23,email:'user23@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 24', () => { const r = validate({name:'user24',age:24,email:'user24@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 25', () => { const r = validate({name:'user25',age:25,email:'user25@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 26', () => { const r = validate({name:'user26',age:26,email:'user26@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 27', () => { const r = validate({name:'user27',age:27,email:'user27@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 28', () => { const r = validate({name:'user28',age:28,email:'user28@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 29', () => { const r = validate({name:'user29',age:29,email:'user29@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 30', () => { const r = validate({name:'user30',age:30,email:'user30@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 31', () => { const r = validate({name:'user31',age:31,email:'user31@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 32', () => { const r = validate({name:'user32',age:32,email:'user32@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 33', () => { const r = validate({name:'user33',age:33,email:'user33@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 34', () => { const r = validate({name:'user34',age:34,email:'user34@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 35', () => { const r = validate({name:'user35',age:35,email:'user35@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 36', () => { const r = validate({name:'user36',age:36,email:'user36@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 37', () => { const r = validate({name:'user37',age:37,email:'user37@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 38', () => { const r = validate({name:'user38',age:38,email:'user38@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 39', () => { const r = validate({name:'user39',age:39,email:'user39@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 40', () => { const r = validate({name:'user40',age:40,email:'user40@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 41', () => { const r = validate({name:'user41',age:41,email:'user41@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 42', () => { const r = validate({name:'user42',age:42,email:'user42@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 43', () => { const r = validate({name:'user43',age:43,email:'user43@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 44', () => { const r = validate({name:'user44',age:44,email:'user44@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 45', () => { const r = validate({name:'user45',age:45,email:'user45@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 46', () => { const r = validate({name:'user46',age:46,email:'user46@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 47', () => { const r = validate({name:'user47',age:47,email:'user47@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 48', () => { const r = validate({name:'user48',age:48,email:'user48@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 49', () => { const r = validate({name:'user49',age:49,email:'user49@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 50', () => { const r = validate({name:'user50',age:50,email:'user50@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 51', () => { const r = validate({name:'user51',age:51,email:'user51@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 52', () => { const r = validate({name:'user52',age:52,email:'user52@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 53', () => { const r = validate({name:'user53',age:53,email:'user53@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 54', () => { const r = validate({name:'user54',age:54,email:'user54@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 55', () => { const r = validate({name:'user55',age:55,email:'user55@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 56', () => { const r = validate({name:'user56',age:56,email:'user56@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 57', () => { const r = validate({name:'user57',age:57,email:'user57@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 58', () => { const r = validate({name:'user58',age:58,email:'user58@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 59', () => { const r = validate({name:'user59',age:59,email:'user59@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 60', () => { const r = validate({name:'user60',age:60,email:'user60@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 61', () => { const r = validate({name:'user61',age:61,email:'user61@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 62', () => { const r = validate({name:'user62',age:62,email:'user62@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 63', () => { const r = validate({name:'user63',age:63,email:'user63@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 64', () => { const r = validate({name:'user64',age:64,email:'user64@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 65', () => { const r = validate({name:'user65',age:65,email:'user65@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 66', () => { const r = validate({name:'user66',age:66,email:'user66@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 67', () => { const r = validate({name:'user67',age:67,email:'user67@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 68', () => { const r = validate({name:'user68',age:68,email:'user68@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 69', () => { const r = validate({name:'user69',age:69,email:'user69@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 70', () => { const r = validate({name:'user70',age:70,email:'user70@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 71', () => { const r = validate({name:'user71',age:71,email:'user71@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 72', () => { const r = validate({name:'user72',age:72,email:'user72@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 73', () => { const r = validate({name:'user73',age:73,email:'user73@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 74', () => { const r = validate({name:'user74',age:74,email:'user74@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 75', () => { const r = validate({name:'user75',age:75,email:'user75@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 76', () => { const r = validate({name:'user76',age:76,email:'user76@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 77', () => { const r = validate({name:'user77',age:77,email:'user77@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 78', () => { const r = validate({name:'user78',age:78,email:'user78@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 79', () => { const r = validate({name:'user79',age:79,email:'user79@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 80', () => { const r = validate({name:'user80',age:80,email:'user80@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 81', () => { const r = validate({name:'user81',age:81,email:'user81@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 82', () => { const r = validate({name:'user82',age:82,email:'user82@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 83', () => { const r = validate({name:'user83',age:83,email:'user83@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 84', () => { const r = validate({name:'user84',age:84,email:'user84@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 85', () => { const r = validate({name:'user85',age:85,email:'user85@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 86', () => { const r = validate({name:'user86',age:86,email:'user86@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 87', () => { const r = validate({name:'user87',age:87,email:'user87@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 88', () => { const r = validate({name:'user88',age:88,email:'user88@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 89', () => { const r = validate({name:'user89',age:89,email:'user89@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 90', () => { const r = validate({name:'user90',age:90,email:'user90@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 91', () => { const r = validate({name:'user91',age:91,email:'user91@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 92', () => { const r = validate({name:'user92',age:92,email:'user92@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 93', () => { const r = validate({name:'user93',age:93,email:'user93@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 94', () => { const r = validate({name:'user94',age:94,email:'user94@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 95', () => { const r = validate({name:'user95',age:95,email:'user95@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 96', () => { const r = validate({name:'user96',age:96,email:'user96@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 97', () => { const r = validate({name:'user97',age:97,email:'user97@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 98', () => { const r = validate({name:'user98',age:98,email:'user98@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
    it('valid 99', () => { const r = validate({name:'user99',age:99,email:'user99@example.com',role:'user',active:true}, basicSchema); expect(r.valid).toBe(true); });
  });
  describe('validate missing required', () => {
    it('missing name 0', () => { const r = validate({age:0}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 1', () => { const r = validate({age:1}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 2', () => { const r = validate({age:2}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 3', () => { const r = validate({age:3}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 4', () => { const r = validate({age:4}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 5', () => { const r = validate({age:5}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 6', () => { const r = validate({age:6}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 7', () => { const r = validate({age:7}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 8', () => { const r = validate({age:8}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 9', () => { const r = validate({age:9}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 10', () => { const r = validate({age:10}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 11', () => { const r = validate({age:11}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 12', () => { const r = validate({age:12}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 13', () => { const r = validate({age:13}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 14', () => { const r = validate({age:14}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 15', () => { const r = validate({age:15}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 16', () => { const r = validate({age:16}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 17', () => { const r = validate({age:17}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 18', () => { const r = validate({age:18}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 19', () => { const r = validate({age:19}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 20', () => { const r = validate({age:20}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 21', () => { const r = validate({age:21}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 22', () => { const r = validate({age:22}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 23', () => { const r = validate({age:23}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 24', () => { const r = validate({age:24}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 25', () => { const r = validate({age:25}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 26', () => { const r = validate({age:26}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 27', () => { const r = validate({age:27}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 28', () => { const r = validate({age:28}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 29', () => { const r = validate({age:29}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 30', () => { const r = validate({age:30}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 31', () => { const r = validate({age:31}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 32', () => { const r = validate({age:32}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 33', () => { const r = validate({age:33}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 34', () => { const r = validate({age:34}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 35', () => { const r = validate({age:35}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 36', () => { const r = validate({age:36}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 37', () => { const r = validate({age:37}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 38', () => { const r = validate({age:38}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 39', () => { const r = validate({age:39}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 40', () => { const r = validate({age:40}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 41', () => { const r = validate({age:41}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 42', () => { const r = validate({age:42}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 43', () => { const r = validate({age:43}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 44', () => { const r = validate({age:44}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 45', () => { const r = validate({age:45}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 46', () => { const r = validate({age:46}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 47', () => { const r = validate({age:47}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 48', () => { const r = validate({age:48}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 49', () => { const r = validate({age:49}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 50', () => { const r = validate({age:50}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 51', () => { const r = validate({age:51}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 52', () => { const r = validate({age:52}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 53', () => { const r = validate({age:53}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 54', () => { const r = validate({age:54}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 55', () => { const r = validate({age:55}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 56', () => { const r = validate({age:56}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 57', () => { const r = validate({age:57}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 58', () => { const r = validate({age:58}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 59', () => { const r = validate({age:59}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 60', () => { const r = validate({age:60}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 61', () => { const r = validate({age:61}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 62', () => { const r = validate({age:62}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 63', () => { const r = validate({age:63}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 64', () => { const r = validate({age:64}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 65', () => { const r = validate({age:65}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 66', () => { const r = validate({age:66}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 67', () => { const r = validate({age:67}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 68', () => { const r = validate({age:68}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 69', () => { const r = validate({age:69}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 70', () => { const r = validate({age:70}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 71', () => { const r = validate({age:71}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 72', () => { const r = validate({age:72}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 73', () => { const r = validate({age:73}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 74', () => { const r = validate({age:74}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 75', () => { const r = validate({age:75}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 76', () => { const r = validate({age:76}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 77', () => { const r = validate({age:77}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 78', () => { const r = validate({age:78}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
    it('missing name 79', () => { const r = validate({age:79}, basicSchema); expect(r.valid).toBe(false); expect(r.errors.length).toBeGreaterThan(0); });
  });
  describe('validate type mismatch', () => {
    it('wrong type 0', () => { const r = validate({name:0,age:0}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 1', () => { const r = validate({name:1,age:1}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 2', () => { const r = validate({name:2,age:2}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 3', () => { const r = validate({name:3,age:3}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 4', () => { const r = validate({name:4,age:4}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 5', () => { const r = validate({name:5,age:5}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 6', () => { const r = validate({name:6,age:6}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 7', () => { const r = validate({name:7,age:7}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 8', () => { const r = validate({name:8,age:8}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 9', () => { const r = validate({name:9,age:9}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 10', () => { const r = validate({name:10,age:10}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 11', () => { const r = validate({name:11,age:11}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 12', () => { const r = validate({name:12,age:12}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 13', () => { const r = validate({name:13,age:13}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 14', () => { const r = validate({name:14,age:14}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 15', () => { const r = validate({name:15,age:15}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 16', () => { const r = validate({name:16,age:16}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 17', () => { const r = validate({name:17,age:17}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 18', () => { const r = validate({name:18,age:18}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 19', () => { const r = validate({name:19,age:19}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 20', () => { const r = validate({name:20,age:20}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 21', () => { const r = validate({name:21,age:21}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 22', () => { const r = validate({name:22,age:22}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 23', () => { const r = validate({name:23,age:23}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 24', () => { const r = validate({name:24,age:24}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 25', () => { const r = validate({name:25,age:25}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 26', () => { const r = validate({name:26,age:26}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 27', () => { const r = validate({name:27,age:27}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 28', () => { const r = validate({name:28,age:28}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 29', () => { const r = validate({name:29,age:29}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 30', () => { const r = validate({name:30,age:30}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 31', () => { const r = validate({name:31,age:31}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 32', () => { const r = validate({name:32,age:32}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 33', () => { const r = validate({name:33,age:33}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 34', () => { const r = validate({name:34,age:34}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 35', () => { const r = validate({name:35,age:35}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 36', () => { const r = validate({name:36,age:36}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 37', () => { const r = validate({name:37,age:37}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 38', () => { const r = validate({name:38,age:38}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 39', () => { const r = validate({name:39,age:39}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 40', () => { const r = validate({name:40,age:40}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 41', () => { const r = validate({name:41,age:41}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 42', () => { const r = validate({name:42,age:42}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 43', () => { const r = validate({name:43,age:43}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 44', () => { const r = validate({name:44,age:44}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 45', () => { const r = validate({name:45,age:45}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 46', () => { const r = validate({name:46,age:46}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 47', () => { const r = validate({name:47,age:47}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 48', () => { const r = validate({name:48,age:48}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 49', () => { const r = validate({name:49,age:49}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 50', () => { const r = validate({name:50,age:50}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 51', () => { const r = validate({name:51,age:51}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 52', () => { const r = validate({name:52,age:52}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 53', () => { const r = validate({name:53,age:53}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 54', () => { const r = validate({name:54,age:54}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 55', () => { const r = validate({name:55,age:55}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 56', () => { const r = validate({name:56,age:56}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 57', () => { const r = validate({name:57,age:57}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 58', () => { const r = validate({name:58,age:58}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 59', () => { const r = validate({name:59,age:59}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 60', () => { const r = validate({name:60,age:60}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 61', () => { const r = validate({name:61,age:61}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 62', () => { const r = validate({name:62,age:62}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 63', () => { const r = validate({name:63,age:63}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 64', () => { const r = validate({name:64,age:64}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 65', () => { const r = validate({name:65,age:65}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 66', () => { const r = validate({name:66,age:66}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 67', () => { const r = validate({name:67,age:67}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 68', () => { const r = validate({name:68,age:68}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 69', () => { const r = validate({name:69,age:69}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 70', () => { const r = validate({name:70,age:70}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 71', () => { const r = validate({name:71,age:71}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 72', () => { const r = validate({name:72,age:72}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 73', () => { const r = validate({name:73,age:73}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 74', () => { const r = validate({name:74,age:74}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 75', () => { const r = validate({name:75,age:75}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 76', () => { const r = validate({name:76,age:76}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 77', () => { const r = validate({name:77,age:77}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 78', () => { const r = validate({name:78,age:78}, basicSchema); expect(r.valid).toBe(false); });
    it('wrong type 79', () => { const r = validate({name:79,age:79}, basicSchema); expect(r.valid).toBe(false); });
  });
  describe('validate range', () => {
    it('age below min 0', () => { const r = validate({name:'n',age:-1}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 1', () => { const r = validate({name:'n',age:-2}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 2', () => { const r = validate({name:'n',age:-3}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 3', () => { const r = validate({name:'n',age:-4}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 4', () => { const r = validate({name:'n',age:-5}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 5', () => { const r = validate({name:'n',age:-6}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 6', () => { const r = validate({name:'n',age:-7}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 7', () => { const r = validate({name:'n',age:-8}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 8', () => { const r = validate({name:'n',age:-9}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 9', () => { const r = validate({name:'n',age:-10}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 10', () => { const r = validate({name:'n',age:-11}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 11', () => { const r = validate({name:'n',age:-12}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 12', () => { const r = validate({name:'n',age:-13}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 13', () => { const r = validate({name:'n',age:-14}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 14', () => { const r = validate({name:'n',age:-15}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 15', () => { const r = validate({name:'n',age:-16}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 16', () => { const r = validate({name:'n',age:-17}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 17', () => { const r = validate({name:'n',age:-18}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 18', () => { const r = validate({name:'n',age:-19}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 19', () => { const r = validate({name:'n',age:-20}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 20', () => { const r = validate({name:'n',age:-21}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 21', () => { const r = validate({name:'n',age:-22}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 22', () => { const r = validate({name:'n',age:-23}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 23', () => { const r = validate({name:'n',age:-24}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 24', () => { const r = validate({name:'n',age:-25}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 25', () => { const r = validate({name:'n',age:-26}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 26', () => { const r = validate({name:'n',age:-27}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 27', () => { const r = validate({name:'n',age:-28}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 28', () => { const r = validate({name:'n',age:-29}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 29', () => { const r = validate({name:'n',age:-30}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 30', () => { const r = validate({name:'n',age:-31}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 31', () => { const r = validate({name:'n',age:-32}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 32', () => { const r = validate({name:'n',age:-33}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 33', () => { const r = validate({name:'n',age:-34}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 34', () => { const r = validate({name:'n',age:-35}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 35', () => { const r = validate({name:'n',age:-36}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 36', () => { const r = validate({name:'n',age:-37}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 37', () => { const r = validate({name:'n',age:-38}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 38', () => { const r = validate({name:'n',age:-39}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 39', () => { const r = validate({name:'n',age:-40}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 40', () => { const r = validate({name:'n',age:-41}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 41', () => { const r = validate({name:'n',age:-42}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 42', () => { const r = validate({name:'n',age:-43}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 43', () => { const r = validate({name:'n',age:-44}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 44', () => { const r = validate({name:'n',age:-45}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 45', () => { const r = validate({name:'n',age:-46}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 46', () => { const r = validate({name:'n',age:-47}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 47', () => { const r = validate({name:'n',age:-48}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 48', () => { const r = validate({name:'n',age:-49}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 49', () => { const r = validate({name:'n',age:-50}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 50', () => { const r = validate({name:'n',age:-51}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 51', () => { const r = validate({name:'n',age:-52}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 52', () => { const r = validate({name:'n',age:-53}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 53', () => { const r = validate({name:'n',age:-54}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 54', () => { const r = validate({name:'n',age:-55}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 55', () => { const r = validate({name:'n',age:-56}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 56', () => { const r = validate({name:'n',age:-57}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 57', () => { const r = validate({name:'n',age:-58}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 58', () => { const r = validate({name:'n',age:-59}, basicSchema); expect(r.valid).toBe(false); });
    it('age below min 59', () => { const r = validate({name:'n',age:-60}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 0', () => { const r = validate({name:'n',age:151}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 1', () => { const r = validate({name:'n',age:152}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 2', () => { const r = validate({name:'n',age:153}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 3', () => { const r = validate({name:'n',age:154}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 4', () => { const r = validate({name:'n',age:155}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 5', () => { const r = validate({name:'n',age:156}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 6', () => { const r = validate({name:'n',age:157}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 7', () => { const r = validate({name:'n',age:158}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 8', () => { const r = validate({name:'n',age:159}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 9', () => { const r = validate({name:'n',age:160}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 10', () => { const r = validate({name:'n',age:161}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 11', () => { const r = validate({name:'n',age:162}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 12', () => { const r = validate({name:'n',age:163}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 13', () => { const r = validate({name:'n',age:164}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 14', () => { const r = validate({name:'n',age:165}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 15', () => { const r = validate({name:'n',age:166}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 16', () => { const r = validate({name:'n',age:167}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 17', () => { const r = validate({name:'n',age:168}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 18', () => { const r = validate({name:'n',age:169}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 19', () => { const r = validate({name:'n',age:170}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 20', () => { const r = validate({name:'n',age:171}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 21', () => { const r = validate({name:'n',age:172}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 22', () => { const r = validate({name:'n',age:173}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 23', () => { const r = validate({name:'n',age:174}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 24', () => { const r = validate({name:'n',age:175}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 25', () => { const r = validate({name:'n',age:176}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 26', () => { const r = validate({name:'n',age:177}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 27', () => { const r = validate({name:'n',age:178}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 28', () => { const r = validate({name:'n',age:179}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 29', () => { const r = validate({name:'n',age:180}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 30', () => { const r = validate({name:'n',age:181}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 31', () => { const r = validate({name:'n',age:182}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 32', () => { const r = validate({name:'n',age:183}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 33', () => { const r = validate({name:'n',age:184}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 34', () => { const r = validate({name:'n',age:185}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 35', () => { const r = validate({name:'n',age:186}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 36', () => { const r = validate({name:'n',age:187}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 37', () => { const r = validate({name:'n',age:188}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 38', () => { const r = validate({name:'n',age:189}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 39', () => { const r = validate({name:'n',age:190}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 40', () => { const r = validate({name:'n',age:191}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 41', () => { const r = validate({name:'n',age:192}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 42', () => { const r = validate({name:'n',age:193}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 43', () => { const r = validate({name:'n',age:194}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 44', () => { const r = validate({name:'n',age:195}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 45', () => { const r = validate({name:'n',age:196}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 46', () => { const r = validate({name:'n',age:197}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 47', () => { const r = validate({name:'n',age:198}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 48', () => { const r = validate({name:'n',age:199}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 49', () => { const r = validate({name:'n',age:200}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 50', () => { const r = validate({name:'n',age:201}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 51', () => { const r = validate({name:'n',age:202}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 52', () => { const r = validate({name:'n',age:203}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 53', () => { const r = validate({name:'n',age:204}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 54', () => { const r = validate({name:'n',age:205}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 55', () => { const r = validate({name:'n',age:206}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 56', () => { const r = validate({name:'n',age:207}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 57', () => { const r = validate({name:'n',age:208}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 58', () => { const r = validate({name:'n',age:209}, basicSchema); expect(r.valid).toBe(false); });
    it('age above max 59', () => { const r = validate({name:'n',age:210}, basicSchema); expect(r.valid).toBe(false); });
  });
  describe('validate enum', () => {
    it('bad enum 0', () => { const r = validate({name:'n',age:1,role:'superuser0'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 1', () => { const r = validate({name:'n',age:1,role:'superuser1'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 2', () => { const r = validate({name:'n',age:1,role:'superuser2'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 3', () => { const r = validate({name:'n',age:1,role:'superuser3'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 4', () => { const r = validate({name:'n',age:1,role:'superuser4'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 5', () => { const r = validate({name:'n',age:1,role:'superuser5'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 6', () => { const r = validate({name:'n',age:1,role:'superuser6'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 7', () => { const r = validate({name:'n',age:1,role:'superuser7'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 8', () => { const r = validate({name:'n',age:1,role:'superuser8'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 9', () => { const r = validate({name:'n',age:1,role:'superuser9'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 10', () => { const r = validate({name:'n',age:1,role:'superuser10'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 11', () => { const r = validate({name:'n',age:1,role:'superuser11'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 12', () => { const r = validate({name:'n',age:1,role:'superuser12'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 13', () => { const r = validate({name:'n',age:1,role:'superuser13'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 14', () => { const r = validate({name:'n',age:1,role:'superuser14'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 15', () => { const r = validate({name:'n',age:1,role:'superuser15'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 16', () => { const r = validate({name:'n',age:1,role:'superuser16'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 17', () => { const r = validate({name:'n',age:1,role:'superuser17'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 18', () => { const r = validate({name:'n',age:1,role:'superuser18'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 19', () => { const r = validate({name:'n',age:1,role:'superuser19'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 20', () => { const r = validate({name:'n',age:1,role:'superuser20'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 21', () => { const r = validate({name:'n',age:1,role:'superuser21'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 22', () => { const r = validate({name:'n',age:1,role:'superuser22'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 23', () => { const r = validate({name:'n',age:1,role:'superuser23'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 24', () => { const r = validate({name:'n',age:1,role:'superuser24'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 25', () => { const r = validate({name:'n',age:1,role:'superuser25'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 26', () => { const r = validate({name:'n',age:1,role:'superuser26'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 27', () => { const r = validate({name:'n',age:1,role:'superuser27'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 28', () => { const r = validate({name:'n',age:1,role:'superuser28'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 29', () => { const r = validate({name:'n',age:1,role:'superuser29'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 30', () => { const r = validate({name:'n',age:1,role:'superuser30'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 31', () => { const r = validate({name:'n',age:1,role:'superuser31'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 32', () => { const r = validate({name:'n',age:1,role:'superuser32'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 33', () => { const r = validate({name:'n',age:1,role:'superuser33'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 34', () => { const r = validate({name:'n',age:1,role:'superuser34'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 35', () => { const r = validate({name:'n',age:1,role:'superuser35'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 36', () => { const r = validate({name:'n',age:1,role:'superuser36'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 37', () => { const r = validate({name:'n',age:1,role:'superuser37'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 38', () => { const r = validate({name:'n',age:1,role:'superuser38'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 39', () => { const r = validate({name:'n',age:1,role:'superuser39'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 40', () => { const r = validate({name:'n',age:1,role:'superuser40'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 41', () => { const r = validate({name:'n',age:1,role:'superuser41'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 42', () => { const r = validate({name:'n',age:1,role:'superuser42'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 43', () => { const r = validate({name:'n',age:1,role:'superuser43'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 44', () => { const r = validate({name:'n',age:1,role:'superuser44'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 45', () => { const r = validate({name:'n',age:1,role:'superuser45'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 46', () => { const r = validate({name:'n',age:1,role:'superuser46'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 47', () => { const r = validate({name:'n',age:1,role:'superuser47'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 48', () => { const r = validate({name:'n',age:1,role:'superuser48'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 49', () => { const r = validate({name:'n',age:1,role:'superuser49'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 50', () => { const r = validate({name:'n',age:1,role:'superuser50'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 51', () => { const r = validate({name:'n',age:1,role:'superuser51'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 52', () => { const r = validate({name:'n',age:1,role:'superuser52'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 53', () => { const r = validate({name:'n',age:1,role:'superuser53'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 54', () => { const r = validate({name:'n',age:1,role:'superuser54'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 55', () => { const r = validate({name:'n',age:1,role:'superuser55'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 56', () => { const r = validate({name:'n',age:1,role:'superuser56'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 57', () => { const r = validate({name:'n',age:1,role:'superuser57'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 58', () => { const r = validate({name:'n',age:1,role:'superuser58'}, basicSchema); expect(r.valid).toBe(false); });
    it('bad enum 59', () => { const r = validate({name:'n',age:1,role:'superuser59'}, basicSchema); expect(r.valid).toBe(false); });
  });
  describe('applyDefaults', () => {
    it('default role 0', () => { const d = applyDefaults({name:'n0',age:0}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 1', () => { const d = applyDefaults({name:'n1',age:1}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 2', () => { const d = applyDefaults({name:'n2',age:2}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 3', () => { const d = applyDefaults({name:'n3',age:3}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 4', () => { const d = applyDefaults({name:'n4',age:4}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 5', () => { const d = applyDefaults({name:'n5',age:5}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 6', () => { const d = applyDefaults({name:'n6',age:6}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 7', () => { const d = applyDefaults({name:'n7',age:7}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 8', () => { const d = applyDefaults({name:'n8',age:8}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 9', () => { const d = applyDefaults({name:'n9',age:9}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 10', () => { const d = applyDefaults({name:'n10',age:10}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 11', () => { const d = applyDefaults({name:'n11',age:11}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 12', () => { const d = applyDefaults({name:'n12',age:12}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 13', () => { const d = applyDefaults({name:'n13',age:13}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 14', () => { const d = applyDefaults({name:'n14',age:14}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 15', () => { const d = applyDefaults({name:'n15',age:15}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 16', () => { const d = applyDefaults({name:'n16',age:16}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 17', () => { const d = applyDefaults({name:'n17',age:17}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 18', () => { const d = applyDefaults({name:'n18',age:18}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 19', () => { const d = applyDefaults({name:'n19',age:19}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 20', () => { const d = applyDefaults({name:'n20',age:20}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 21', () => { const d = applyDefaults({name:'n21',age:21}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 22', () => { const d = applyDefaults({name:'n22',age:22}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 23', () => { const d = applyDefaults({name:'n23',age:23}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 24', () => { const d = applyDefaults({name:'n24',age:24}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 25', () => { const d = applyDefaults({name:'n25',age:25}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 26', () => { const d = applyDefaults({name:'n26',age:26}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 27', () => { const d = applyDefaults({name:'n27',age:27}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 28', () => { const d = applyDefaults({name:'n28',age:28}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 29', () => { const d = applyDefaults({name:'n29',age:29}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 30', () => { const d = applyDefaults({name:'n30',age:30}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 31', () => { const d = applyDefaults({name:'n31',age:31}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 32', () => { const d = applyDefaults({name:'n32',age:32}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 33', () => { const d = applyDefaults({name:'n33',age:33}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 34', () => { const d = applyDefaults({name:'n34',age:34}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 35', () => { const d = applyDefaults({name:'n35',age:35}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 36', () => { const d = applyDefaults({name:'n36',age:36}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 37', () => { const d = applyDefaults({name:'n37',age:37}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 38', () => { const d = applyDefaults({name:'n38',age:38}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 39', () => { const d = applyDefaults({name:'n39',age:39}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 40', () => { const d = applyDefaults({name:'n40',age:40}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 41', () => { const d = applyDefaults({name:'n41',age:41}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 42', () => { const d = applyDefaults({name:'n42',age:42}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 43', () => { const d = applyDefaults({name:'n43',age:43}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 44', () => { const d = applyDefaults({name:'n44',age:44}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 45', () => { const d = applyDefaults({name:'n45',age:45}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 46', () => { const d = applyDefaults({name:'n46',age:46}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 47', () => { const d = applyDefaults({name:'n47',age:47}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 48', () => { const d = applyDefaults({name:'n48',age:48}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 49', () => { const d = applyDefaults({name:'n49',age:49}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 50', () => { const d = applyDefaults({name:'n50',age:50}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 51', () => { const d = applyDefaults({name:'n51',age:51}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 52', () => { const d = applyDefaults({name:'n52',age:52}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 53', () => { const d = applyDefaults({name:'n53',age:53}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 54', () => { const d = applyDefaults({name:'n54',age:54}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 55', () => { const d = applyDefaults({name:'n55',age:55}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 56', () => { const d = applyDefaults({name:'n56',age:56}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 57', () => { const d = applyDefaults({name:'n57',age:57}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 58', () => { const d = applyDefaults({name:'n58',age:58}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 59', () => { const d = applyDefaults({name:'n59',age:59}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 60', () => { const d = applyDefaults({name:'n60',age:60}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 61', () => { const d = applyDefaults({name:'n61',age:61}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 62', () => { const d = applyDefaults({name:'n62',age:62}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 63', () => { const d = applyDefaults({name:'n63',age:63}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 64', () => { const d = applyDefaults({name:'n64',age:64}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 65', () => { const d = applyDefaults({name:'n65',age:65}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 66', () => { const d = applyDefaults({name:'n66',age:66}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 67', () => { const d = applyDefaults({name:'n67',age:67}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 68', () => { const d = applyDefaults({name:'n68',age:68}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 69', () => { const d = applyDefaults({name:'n69',age:69}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 70', () => { const d = applyDefaults({name:'n70',age:70}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 71', () => { const d = applyDefaults({name:'n71',age:71}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 72', () => { const d = applyDefaults({name:'n72',age:72}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 73', () => { const d = applyDefaults({name:'n73',age:73}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 74', () => { const d = applyDefaults({name:'n74',age:74}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 75', () => { const d = applyDefaults({name:'n75',age:75}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 76', () => { const d = applyDefaults({name:'n76',age:76}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 77', () => { const d = applyDefaults({name:'n77',age:77}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 78', () => { const d = applyDefaults({name:'n78',age:78}, basicSchema); expect(d.role).toBe('user'); });
    it('default role 79', () => { const d = applyDefaults({name:'n79',age:79}, basicSchema); expect(d.role).toBe('user'); });
    it('default active 0', () => { const d = applyDefaults({name:'n0',age:0}, basicSchema); expect(d.active).toBe(true); });
    it('default active 1', () => { const d = applyDefaults({name:'n1',age:1}, basicSchema); expect(d.active).toBe(true); });
    it('default active 2', () => { const d = applyDefaults({name:'n2',age:2}, basicSchema); expect(d.active).toBe(true); });
    it('default active 3', () => { const d = applyDefaults({name:'n3',age:3}, basicSchema); expect(d.active).toBe(true); });
    it('default active 4', () => { const d = applyDefaults({name:'n4',age:4}, basicSchema); expect(d.active).toBe(true); });
    it('default active 5', () => { const d = applyDefaults({name:'n5',age:5}, basicSchema); expect(d.active).toBe(true); });
    it('default active 6', () => { const d = applyDefaults({name:'n6',age:6}, basicSchema); expect(d.active).toBe(true); });
    it('default active 7', () => { const d = applyDefaults({name:'n7',age:7}, basicSchema); expect(d.active).toBe(true); });
    it('default active 8', () => { const d = applyDefaults({name:'n8',age:8}, basicSchema); expect(d.active).toBe(true); });
    it('default active 9', () => { const d = applyDefaults({name:'n9',age:9}, basicSchema); expect(d.active).toBe(true); });
    it('default active 10', () => { const d = applyDefaults({name:'n10',age:10}, basicSchema); expect(d.active).toBe(true); });
    it('default active 11', () => { const d = applyDefaults({name:'n11',age:11}, basicSchema); expect(d.active).toBe(true); });
    it('default active 12', () => { const d = applyDefaults({name:'n12',age:12}, basicSchema); expect(d.active).toBe(true); });
    it('default active 13', () => { const d = applyDefaults({name:'n13',age:13}, basicSchema); expect(d.active).toBe(true); });
    it('default active 14', () => { const d = applyDefaults({name:'n14',age:14}, basicSchema); expect(d.active).toBe(true); });
    it('default active 15', () => { const d = applyDefaults({name:'n15',age:15}, basicSchema); expect(d.active).toBe(true); });
    it('default active 16', () => { const d = applyDefaults({name:'n16',age:16}, basicSchema); expect(d.active).toBe(true); });
    it('default active 17', () => { const d = applyDefaults({name:'n17',age:17}, basicSchema); expect(d.active).toBe(true); });
    it('default active 18', () => { const d = applyDefaults({name:'n18',age:18}, basicSchema); expect(d.active).toBe(true); });
    it('default active 19', () => { const d = applyDefaults({name:'n19',age:19}, basicSchema); expect(d.active).toBe(true); });
    it('default active 20', () => { const d = applyDefaults({name:'n20',age:20}, basicSchema); expect(d.active).toBe(true); });
    it('default active 21', () => { const d = applyDefaults({name:'n21',age:21}, basicSchema); expect(d.active).toBe(true); });
    it('default active 22', () => { const d = applyDefaults({name:'n22',age:22}, basicSchema); expect(d.active).toBe(true); });
    it('default active 23', () => { const d = applyDefaults({name:'n23',age:23}, basicSchema); expect(d.active).toBe(true); });
    it('default active 24', () => { const d = applyDefaults({name:'n24',age:24}, basicSchema); expect(d.active).toBe(true); });
    it('default active 25', () => { const d = applyDefaults({name:'n25',age:25}, basicSchema); expect(d.active).toBe(true); });
    it('default active 26', () => { const d = applyDefaults({name:'n26',age:26}, basicSchema); expect(d.active).toBe(true); });
    it('default active 27', () => { const d = applyDefaults({name:'n27',age:27}, basicSchema); expect(d.active).toBe(true); });
    it('default active 28', () => { const d = applyDefaults({name:'n28',age:28}, basicSchema); expect(d.active).toBe(true); });
    it('default active 29', () => { const d = applyDefaults({name:'n29',age:29}, basicSchema); expect(d.active).toBe(true); });
    it('default active 30', () => { const d = applyDefaults({name:'n30',age:30}, basicSchema); expect(d.active).toBe(true); });
    it('default active 31', () => { const d = applyDefaults({name:'n31',age:31}, basicSchema); expect(d.active).toBe(true); });
    it('default active 32', () => { const d = applyDefaults({name:'n32',age:32}, basicSchema); expect(d.active).toBe(true); });
    it('default active 33', () => { const d = applyDefaults({name:'n33',age:33}, basicSchema); expect(d.active).toBe(true); });
    it('default active 34', () => { const d = applyDefaults({name:'n34',age:34}, basicSchema); expect(d.active).toBe(true); });
    it('default active 35', () => { const d = applyDefaults({name:'n35',age:35}, basicSchema); expect(d.active).toBe(true); });
    it('default active 36', () => { const d = applyDefaults({name:'n36',age:36}, basicSchema); expect(d.active).toBe(true); });
    it('default active 37', () => { const d = applyDefaults({name:'n37',age:37}, basicSchema); expect(d.active).toBe(true); });
    it('default active 38', () => { const d = applyDefaults({name:'n38',age:38}, basicSchema); expect(d.active).toBe(true); });
    it('default active 39', () => { const d = applyDefaults({name:'n39',age:39}, basicSchema); expect(d.active).toBe(true); });
    it('default active 40', () => { const d = applyDefaults({name:'n40',age:40}, basicSchema); expect(d.active).toBe(true); });
    it('default active 41', () => { const d = applyDefaults({name:'n41',age:41}, basicSchema); expect(d.active).toBe(true); });
    it('default active 42', () => { const d = applyDefaults({name:'n42',age:42}, basicSchema); expect(d.active).toBe(true); });
    it('default active 43', () => { const d = applyDefaults({name:'n43',age:43}, basicSchema); expect(d.active).toBe(true); });
    it('default active 44', () => { const d = applyDefaults({name:'n44',age:44}, basicSchema); expect(d.active).toBe(true); });
    it('default active 45', () => { const d = applyDefaults({name:'n45',age:45}, basicSchema); expect(d.active).toBe(true); });
    it('default active 46', () => { const d = applyDefaults({name:'n46',age:46}, basicSchema); expect(d.active).toBe(true); });
    it('default active 47', () => { const d = applyDefaults({name:'n47',age:47}, basicSchema); expect(d.active).toBe(true); });
    it('default active 48', () => { const d = applyDefaults({name:'n48',age:48}, basicSchema); expect(d.active).toBe(true); });
    it('default active 49', () => { const d = applyDefaults({name:'n49',age:49}, basicSchema); expect(d.active).toBe(true); });
    it('default active 50', () => { const d = applyDefaults({name:'n50',age:50}, basicSchema); expect(d.active).toBe(true); });
    it('default active 51', () => { const d = applyDefaults({name:'n51',age:51}, basicSchema); expect(d.active).toBe(true); });
    it('default active 52', () => { const d = applyDefaults({name:'n52',age:52}, basicSchema); expect(d.active).toBe(true); });
    it('default active 53', () => { const d = applyDefaults({name:'n53',age:53}, basicSchema); expect(d.active).toBe(true); });
    it('default active 54', () => { const d = applyDefaults({name:'n54',age:54}, basicSchema); expect(d.active).toBe(true); });
    it('default active 55', () => { const d = applyDefaults({name:'n55',age:55}, basicSchema); expect(d.active).toBe(true); });
    it('default active 56', () => { const d = applyDefaults({name:'n56',age:56}, basicSchema); expect(d.active).toBe(true); });
    it('default active 57', () => { const d = applyDefaults({name:'n57',age:57}, basicSchema); expect(d.active).toBe(true); });
    it('default active 58', () => { const d = applyDefaults({name:'n58',age:58}, basicSchema); expect(d.active).toBe(true); });
    it('default active 59', () => { const d = applyDefaults({name:'n59',age:59}, basicSchema); expect(d.active).toBe(true); });
    it('default active 60', () => { const d = applyDefaults({name:'n60',age:60}, basicSchema); expect(d.active).toBe(true); });
    it('default active 61', () => { const d = applyDefaults({name:'n61',age:61}, basicSchema); expect(d.active).toBe(true); });
    it('default active 62', () => { const d = applyDefaults({name:'n62',age:62}, basicSchema); expect(d.active).toBe(true); });
    it('default active 63', () => { const d = applyDefaults({name:'n63',age:63}, basicSchema); expect(d.active).toBe(true); });
    it('default active 64', () => { const d = applyDefaults({name:'n64',age:64}, basicSchema); expect(d.active).toBe(true); });
    it('default active 65', () => { const d = applyDefaults({name:'n65',age:65}, basicSchema); expect(d.active).toBe(true); });
    it('default active 66', () => { const d = applyDefaults({name:'n66',age:66}, basicSchema); expect(d.active).toBe(true); });
    it('default active 67', () => { const d = applyDefaults({name:'n67',age:67}, basicSchema); expect(d.active).toBe(true); });
    it('default active 68', () => { const d = applyDefaults({name:'n68',age:68}, basicSchema); expect(d.active).toBe(true); });
    it('default active 69', () => { const d = applyDefaults({name:'n69',age:69}, basicSchema); expect(d.active).toBe(true); });
    it('default active 70', () => { const d = applyDefaults({name:'n70',age:70}, basicSchema); expect(d.active).toBe(true); });
    it('default active 71', () => { const d = applyDefaults({name:'n71',age:71}, basicSchema); expect(d.active).toBe(true); });
    it('default active 72', () => { const d = applyDefaults({name:'n72',age:72}, basicSchema); expect(d.active).toBe(true); });
    it('default active 73', () => { const d = applyDefaults({name:'n73',age:73}, basicSchema); expect(d.active).toBe(true); });
    it('default active 74', () => { const d = applyDefaults({name:'n74',age:74}, basicSchema); expect(d.active).toBe(true); });
    it('default active 75', () => { const d = applyDefaults({name:'n75',age:75}, basicSchema); expect(d.active).toBe(true); });
    it('default active 76', () => { const d = applyDefaults({name:'n76',age:76}, basicSchema); expect(d.active).toBe(true); });
    it('default active 77', () => { const d = applyDefaults({name:'n77',age:77}, basicSchema); expect(d.active).toBe(true); });
    it('default active 78', () => { const d = applyDefaults({name:'n78',age:78}, basicSchema); expect(d.active).toBe(true); });
    it('default active 79', () => { const d = applyDefaults({name:'n79',age:79}, basicSchema); expect(d.active).toBe(true); });
  });
  describe('coerce', () => {
    const numSchema: Schema = { count: { type: 'number' }, flag: { type: 'boolean' } };
    it('coerce number 0', () => { const d = coerce({count:'0'}, numSchema); expect(d.count).toBe(0); });
    it('coerce number 1', () => { const d = coerce({count:'1'}, numSchema); expect(d.count).toBe(1); });
    it('coerce number 2', () => { const d = coerce({count:'2'}, numSchema); expect(d.count).toBe(2); });
    it('coerce number 3', () => { const d = coerce({count:'3'}, numSchema); expect(d.count).toBe(3); });
    it('coerce number 4', () => { const d = coerce({count:'4'}, numSchema); expect(d.count).toBe(4); });
    it('coerce number 5', () => { const d = coerce({count:'5'}, numSchema); expect(d.count).toBe(5); });
    it('coerce number 6', () => { const d = coerce({count:'6'}, numSchema); expect(d.count).toBe(6); });
    it('coerce number 7', () => { const d = coerce({count:'7'}, numSchema); expect(d.count).toBe(7); });
    it('coerce number 8', () => { const d = coerce({count:'8'}, numSchema); expect(d.count).toBe(8); });
    it('coerce number 9', () => { const d = coerce({count:'9'}, numSchema); expect(d.count).toBe(9); });
    it('coerce number 10', () => { const d = coerce({count:'10'}, numSchema); expect(d.count).toBe(10); });
    it('coerce number 11', () => { const d = coerce({count:'11'}, numSchema); expect(d.count).toBe(11); });
    it('coerce number 12', () => { const d = coerce({count:'12'}, numSchema); expect(d.count).toBe(12); });
    it('coerce number 13', () => { const d = coerce({count:'13'}, numSchema); expect(d.count).toBe(13); });
    it('coerce number 14', () => { const d = coerce({count:'14'}, numSchema); expect(d.count).toBe(14); });
    it('coerce number 15', () => { const d = coerce({count:'15'}, numSchema); expect(d.count).toBe(15); });
    it('coerce number 16', () => { const d = coerce({count:'16'}, numSchema); expect(d.count).toBe(16); });
    it('coerce number 17', () => { const d = coerce({count:'17'}, numSchema); expect(d.count).toBe(17); });
    it('coerce number 18', () => { const d = coerce({count:'18'}, numSchema); expect(d.count).toBe(18); });
    it('coerce number 19', () => { const d = coerce({count:'19'}, numSchema); expect(d.count).toBe(19); });
    it('coerce number 20', () => { const d = coerce({count:'20'}, numSchema); expect(d.count).toBe(20); });
    it('coerce number 21', () => { const d = coerce({count:'21'}, numSchema); expect(d.count).toBe(21); });
    it('coerce number 22', () => { const d = coerce({count:'22'}, numSchema); expect(d.count).toBe(22); });
    it('coerce number 23', () => { const d = coerce({count:'23'}, numSchema); expect(d.count).toBe(23); });
    it('coerce number 24', () => { const d = coerce({count:'24'}, numSchema); expect(d.count).toBe(24); });
    it('coerce number 25', () => { const d = coerce({count:'25'}, numSchema); expect(d.count).toBe(25); });
    it('coerce number 26', () => { const d = coerce({count:'26'}, numSchema); expect(d.count).toBe(26); });
    it('coerce number 27', () => { const d = coerce({count:'27'}, numSchema); expect(d.count).toBe(27); });
    it('coerce number 28', () => { const d = coerce({count:'28'}, numSchema); expect(d.count).toBe(28); });
    it('coerce number 29', () => { const d = coerce({count:'29'}, numSchema); expect(d.count).toBe(29); });
    it('coerce number 30', () => { const d = coerce({count:'30'}, numSchema); expect(d.count).toBe(30); });
    it('coerce number 31', () => { const d = coerce({count:'31'}, numSchema); expect(d.count).toBe(31); });
    it('coerce number 32', () => { const d = coerce({count:'32'}, numSchema); expect(d.count).toBe(32); });
    it('coerce number 33', () => { const d = coerce({count:'33'}, numSchema); expect(d.count).toBe(33); });
    it('coerce number 34', () => { const d = coerce({count:'34'}, numSchema); expect(d.count).toBe(34); });
    it('coerce number 35', () => { const d = coerce({count:'35'}, numSchema); expect(d.count).toBe(35); });
    it('coerce number 36', () => { const d = coerce({count:'36'}, numSchema); expect(d.count).toBe(36); });
    it('coerce number 37', () => { const d = coerce({count:'37'}, numSchema); expect(d.count).toBe(37); });
    it('coerce number 38', () => { const d = coerce({count:'38'}, numSchema); expect(d.count).toBe(38); });
    it('coerce number 39', () => { const d = coerce({count:'39'}, numSchema); expect(d.count).toBe(39); });
    it('coerce number 40', () => { const d = coerce({count:'40'}, numSchema); expect(d.count).toBe(40); });
    it('coerce number 41', () => { const d = coerce({count:'41'}, numSchema); expect(d.count).toBe(41); });
    it('coerce number 42', () => { const d = coerce({count:'42'}, numSchema); expect(d.count).toBe(42); });
    it('coerce number 43', () => { const d = coerce({count:'43'}, numSchema); expect(d.count).toBe(43); });
    it('coerce number 44', () => { const d = coerce({count:'44'}, numSchema); expect(d.count).toBe(44); });
    it('coerce number 45', () => { const d = coerce({count:'45'}, numSchema); expect(d.count).toBe(45); });
    it('coerce number 46', () => { const d = coerce({count:'46'}, numSchema); expect(d.count).toBe(46); });
    it('coerce number 47', () => { const d = coerce({count:'47'}, numSchema); expect(d.count).toBe(47); });
    it('coerce number 48', () => { const d = coerce({count:'48'}, numSchema); expect(d.count).toBe(48); });
    it('coerce number 49', () => { const d = coerce({count:'49'}, numSchema); expect(d.count).toBe(49); });
    it('coerce number 50', () => { const d = coerce({count:'50'}, numSchema); expect(d.count).toBe(50); });
    it('coerce number 51', () => { const d = coerce({count:'51'}, numSchema); expect(d.count).toBe(51); });
    it('coerce number 52', () => { const d = coerce({count:'52'}, numSchema); expect(d.count).toBe(52); });
    it('coerce number 53', () => { const d = coerce({count:'53'}, numSchema); expect(d.count).toBe(53); });
    it('coerce number 54', () => { const d = coerce({count:'54'}, numSchema); expect(d.count).toBe(54); });
    it('coerce number 55', () => { const d = coerce({count:'55'}, numSchema); expect(d.count).toBe(55); });
    it('coerce number 56', () => { const d = coerce({count:'56'}, numSchema); expect(d.count).toBe(56); });
    it('coerce number 57', () => { const d = coerce({count:'57'}, numSchema); expect(d.count).toBe(57); });
    it('coerce number 58', () => { const d = coerce({count:'58'}, numSchema); expect(d.count).toBe(58); });
    it('coerce number 59', () => { const d = coerce({count:'59'}, numSchema); expect(d.count).toBe(59); });
    it('coerce boolean 0', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 1', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 2', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 3', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 4', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 5', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 6', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 7', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 8', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 9', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 10', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 11', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 12', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 13', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 14', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 15', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 16', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 17', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 18', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 19', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 20', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 21', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 22', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 23', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 24', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 25', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 26', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 27', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 28', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 29', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 30', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 31', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 32', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 33', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 34', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 35', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 36', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 37', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 38', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 39', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 40', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 41', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 42', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 43', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 44', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 45', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 46', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 47', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 48', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 49', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 50', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 51', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 52', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 53', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 54', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 55', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 56', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 57', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
    it('coerce boolean 58', () => { const d = coerce({flag:'true'}, numSchema); expect(d.flag).toBe(true); });
    it('coerce boolean 59', () => { const d = coerce({flag:'false'}, numSchema); expect(d.flag).toBe(false); });
  });
  describe('requiredFields', () => {
    it('required 0', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 1', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 2', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 3', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 4', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 5', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 6', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 7', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 8', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 9', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 10', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 11', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 12', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 13', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 14', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 15', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 16', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 17', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 18', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 19', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 20', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 21', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 22', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 23', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 24', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 25', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 26', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 27', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 28', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 29', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 30', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 31', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 32', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 33', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 34', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 35', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 36', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 37', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 38', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 39', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 40', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 41', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 42', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 43', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 44', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 45', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 46', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 47', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 48', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
    it('required 49', () => { const rf = requiredFields(basicSchema); expect(rf).toContain('name'); });
  });
  describe('optionalFields', () => {
    it('optional 0', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 1', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 2', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 3', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 4', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 5', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 6', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 7', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 8', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 9', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 10', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 11', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 12', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 13', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 14', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 15', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 16', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 17', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 18', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 19', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 20', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 21', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 22', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 23', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 24', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 25', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 26', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 27', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 28', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 29', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 30', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 31', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 32', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 33', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 34', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 35', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 36', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 37', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 38', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 39', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 40', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 41', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 42', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 43', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 44', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 45', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 46', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 47', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 48', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
    it('optional 49', () => { const of2 = optionalFields(basicSchema); expect(of2).toContain('email'); });
  });
  describe('mergeSchemas', () => {
    it('merge 0', () => { const s = mergeSchemas({a0:{type:'string'}},{b0:{type:'number'}}); expect(Object.keys(s)).toContain('a0'); expect(Object.keys(s)).toContain('b0'); });
    it('merge 1', () => { const s = mergeSchemas({a1:{type:'string'}},{b1:{type:'number'}}); expect(Object.keys(s)).toContain('a1'); expect(Object.keys(s)).toContain('b1'); });
    it('merge 2', () => { const s = mergeSchemas({a2:{type:'string'}},{b2:{type:'number'}}); expect(Object.keys(s)).toContain('a2'); expect(Object.keys(s)).toContain('b2'); });
    it('merge 3', () => { const s = mergeSchemas({a3:{type:'string'}},{b3:{type:'number'}}); expect(Object.keys(s)).toContain('a3'); expect(Object.keys(s)).toContain('b3'); });
    it('merge 4', () => { const s = mergeSchemas({a4:{type:'string'}},{b4:{type:'number'}}); expect(Object.keys(s)).toContain('a4'); expect(Object.keys(s)).toContain('b4'); });
    it('merge 5', () => { const s = mergeSchemas({a5:{type:'string'}},{b5:{type:'number'}}); expect(Object.keys(s)).toContain('a5'); expect(Object.keys(s)).toContain('b5'); });
    it('merge 6', () => { const s = mergeSchemas({a6:{type:'string'}},{b6:{type:'number'}}); expect(Object.keys(s)).toContain('a6'); expect(Object.keys(s)).toContain('b6'); });
    it('merge 7', () => { const s = mergeSchemas({a7:{type:'string'}},{b7:{type:'number'}}); expect(Object.keys(s)).toContain('a7'); expect(Object.keys(s)).toContain('b7'); });
    it('merge 8', () => { const s = mergeSchemas({a8:{type:'string'}},{b8:{type:'number'}}); expect(Object.keys(s)).toContain('a8'); expect(Object.keys(s)).toContain('b8'); });
    it('merge 9', () => { const s = mergeSchemas({a9:{type:'string'}},{b9:{type:'number'}}); expect(Object.keys(s)).toContain('a9'); expect(Object.keys(s)).toContain('b9'); });
    it('merge 10', () => { const s = mergeSchemas({a10:{type:'string'}},{b10:{type:'number'}}); expect(Object.keys(s)).toContain('a10'); expect(Object.keys(s)).toContain('b10'); });
    it('merge 11', () => { const s = mergeSchemas({a11:{type:'string'}},{b11:{type:'number'}}); expect(Object.keys(s)).toContain('a11'); expect(Object.keys(s)).toContain('b11'); });
    it('merge 12', () => { const s = mergeSchemas({a12:{type:'string'}},{b12:{type:'number'}}); expect(Object.keys(s)).toContain('a12'); expect(Object.keys(s)).toContain('b12'); });
    it('merge 13', () => { const s = mergeSchemas({a13:{type:'string'}},{b13:{type:'number'}}); expect(Object.keys(s)).toContain('a13'); expect(Object.keys(s)).toContain('b13'); });
    it('merge 14', () => { const s = mergeSchemas({a14:{type:'string'}},{b14:{type:'number'}}); expect(Object.keys(s)).toContain('a14'); expect(Object.keys(s)).toContain('b14'); });
    it('merge 15', () => { const s = mergeSchemas({a15:{type:'string'}},{b15:{type:'number'}}); expect(Object.keys(s)).toContain('a15'); expect(Object.keys(s)).toContain('b15'); });
    it('merge 16', () => { const s = mergeSchemas({a16:{type:'string'}},{b16:{type:'number'}}); expect(Object.keys(s)).toContain('a16'); expect(Object.keys(s)).toContain('b16'); });
    it('merge 17', () => { const s = mergeSchemas({a17:{type:'string'}},{b17:{type:'number'}}); expect(Object.keys(s)).toContain('a17'); expect(Object.keys(s)).toContain('b17'); });
    it('merge 18', () => { const s = mergeSchemas({a18:{type:'string'}},{b18:{type:'number'}}); expect(Object.keys(s)).toContain('a18'); expect(Object.keys(s)).toContain('b18'); });
    it('merge 19', () => { const s = mergeSchemas({a19:{type:'string'}},{b19:{type:'number'}}); expect(Object.keys(s)).toContain('a19'); expect(Object.keys(s)).toContain('b19'); });
    it('merge 20', () => { const s = mergeSchemas({a20:{type:'string'}},{b20:{type:'number'}}); expect(Object.keys(s)).toContain('a20'); expect(Object.keys(s)).toContain('b20'); });
    it('merge 21', () => { const s = mergeSchemas({a21:{type:'string'}},{b21:{type:'number'}}); expect(Object.keys(s)).toContain('a21'); expect(Object.keys(s)).toContain('b21'); });
    it('merge 22', () => { const s = mergeSchemas({a22:{type:'string'}},{b22:{type:'number'}}); expect(Object.keys(s)).toContain('a22'); expect(Object.keys(s)).toContain('b22'); });
    it('merge 23', () => { const s = mergeSchemas({a23:{type:'string'}},{b23:{type:'number'}}); expect(Object.keys(s)).toContain('a23'); expect(Object.keys(s)).toContain('b23'); });
    it('merge 24', () => { const s = mergeSchemas({a24:{type:'string'}},{b24:{type:'number'}}); expect(Object.keys(s)).toContain('a24'); expect(Object.keys(s)).toContain('b24'); });
    it('merge 25', () => { const s = mergeSchemas({a25:{type:'string'}},{b25:{type:'number'}}); expect(Object.keys(s)).toContain('a25'); expect(Object.keys(s)).toContain('b25'); });
    it('merge 26', () => { const s = mergeSchemas({a26:{type:'string'}},{b26:{type:'number'}}); expect(Object.keys(s)).toContain('a26'); expect(Object.keys(s)).toContain('b26'); });
    it('merge 27', () => { const s = mergeSchemas({a27:{type:'string'}},{b27:{type:'number'}}); expect(Object.keys(s)).toContain('a27'); expect(Object.keys(s)).toContain('b27'); });
    it('merge 28', () => { const s = mergeSchemas({a28:{type:'string'}},{b28:{type:'number'}}); expect(Object.keys(s)).toContain('a28'); expect(Object.keys(s)).toContain('b28'); });
    it('merge 29', () => { const s = mergeSchemas({a29:{type:'string'}},{b29:{type:'number'}}); expect(Object.keys(s)).toContain('a29'); expect(Object.keys(s)).toContain('b29'); });
    it('merge 30', () => { const s = mergeSchemas({a30:{type:'string'}},{b30:{type:'number'}}); expect(Object.keys(s)).toContain('a30'); expect(Object.keys(s)).toContain('b30'); });
    it('merge 31', () => { const s = mergeSchemas({a31:{type:'string'}},{b31:{type:'number'}}); expect(Object.keys(s)).toContain('a31'); expect(Object.keys(s)).toContain('b31'); });
    it('merge 32', () => { const s = mergeSchemas({a32:{type:'string'}},{b32:{type:'number'}}); expect(Object.keys(s)).toContain('a32'); expect(Object.keys(s)).toContain('b32'); });
    it('merge 33', () => { const s = mergeSchemas({a33:{type:'string'}},{b33:{type:'number'}}); expect(Object.keys(s)).toContain('a33'); expect(Object.keys(s)).toContain('b33'); });
    it('merge 34', () => { const s = mergeSchemas({a34:{type:'string'}},{b34:{type:'number'}}); expect(Object.keys(s)).toContain('a34'); expect(Object.keys(s)).toContain('b34'); });
    it('merge 35', () => { const s = mergeSchemas({a35:{type:'string'}},{b35:{type:'number'}}); expect(Object.keys(s)).toContain('a35'); expect(Object.keys(s)).toContain('b35'); });
    it('merge 36', () => { const s = mergeSchemas({a36:{type:'string'}},{b36:{type:'number'}}); expect(Object.keys(s)).toContain('a36'); expect(Object.keys(s)).toContain('b36'); });
    it('merge 37', () => { const s = mergeSchemas({a37:{type:'string'}},{b37:{type:'number'}}); expect(Object.keys(s)).toContain('a37'); expect(Object.keys(s)).toContain('b37'); });
    it('merge 38', () => { const s = mergeSchemas({a38:{type:'string'}},{b38:{type:'number'}}); expect(Object.keys(s)).toContain('a38'); expect(Object.keys(s)).toContain('b38'); });
    it('merge 39', () => { const s = mergeSchemas({a39:{type:'string'}},{b39:{type:'number'}}); expect(Object.keys(s)).toContain('a39'); expect(Object.keys(s)).toContain('b39'); });
    it('merge 40', () => { const s = mergeSchemas({a40:{type:'string'}},{b40:{type:'number'}}); expect(Object.keys(s)).toContain('a40'); expect(Object.keys(s)).toContain('b40'); });
    it('merge 41', () => { const s = mergeSchemas({a41:{type:'string'}},{b41:{type:'number'}}); expect(Object.keys(s)).toContain('a41'); expect(Object.keys(s)).toContain('b41'); });
    it('merge 42', () => { const s = mergeSchemas({a42:{type:'string'}},{b42:{type:'number'}}); expect(Object.keys(s)).toContain('a42'); expect(Object.keys(s)).toContain('b42'); });
    it('merge 43', () => { const s = mergeSchemas({a43:{type:'string'}},{b43:{type:'number'}}); expect(Object.keys(s)).toContain('a43'); expect(Object.keys(s)).toContain('b43'); });
    it('merge 44', () => { const s = mergeSchemas({a44:{type:'string'}},{b44:{type:'number'}}); expect(Object.keys(s)).toContain('a44'); expect(Object.keys(s)).toContain('b44'); });
    it('merge 45', () => { const s = mergeSchemas({a45:{type:'string'}},{b45:{type:'number'}}); expect(Object.keys(s)).toContain('a45'); expect(Object.keys(s)).toContain('b45'); });
    it('merge 46', () => { const s = mergeSchemas({a46:{type:'string'}},{b46:{type:'number'}}); expect(Object.keys(s)).toContain('a46'); expect(Object.keys(s)).toContain('b46'); });
    it('merge 47', () => { const s = mergeSchemas({a47:{type:'string'}},{b47:{type:'number'}}); expect(Object.keys(s)).toContain('a47'); expect(Object.keys(s)).toContain('b47'); });
    it('merge 48', () => { const s = mergeSchemas({a48:{type:'string'}},{b48:{type:'number'}}); expect(Object.keys(s)).toContain('a48'); expect(Object.keys(s)).toContain('b48'); });
    it('merge 49', () => { const s = mergeSchemas({a49:{type:'string'}},{b49:{type:'number'}}); expect(Object.keys(s)).toContain('a49'); expect(Object.keys(s)).toContain('b49'); });
  });
  describe('pickFields', () => {
    it('pick 0', () => { const d = pickFields({name:'n',age:0,extra:0}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 1', () => { const d = pickFields({name:'n',age:1,extra:1}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 2', () => { const d = pickFields({name:'n',age:2,extra:2}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 3', () => { const d = pickFields({name:'n',age:3,extra:3}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 4', () => { const d = pickFields({name:'n',age:4,extra:4}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 5', () => { const d = pickFields({name:'n',age:5,extra:5}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 6', () => { const d = pickFields({name:'n',age:6,extra:6}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 7', () => { const d = pickFields({name:'n',age:7,extra:7}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 8', () => { const d = pickFields({name:'n',age:8,extra:8}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 9', () => { const d = pickFields({name:'n',age:9,extra:9}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 10', () => { const d = pickFields({name:'n',age:10,extra:10}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 11', () => { const d = pickFields({name:'n',age:11,extra:11}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 12', () => { const d = pickFields({name:'n',age:12,extra:12}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 13', () => { const d = pickFields({name:'n',age:13,extra:13}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 14', () => { const d = pickFields({name:'n',age:14,extra:14}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 15', () => { const d = pickFields({name:'n',age:15,extra:15}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 16', () => { const d = pickFields({name:'n',age:16,extra:16}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 17', () => { const d = pickFields({name:'n',age:17,extra:17}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 18', () => { const d = pickFields({name:'n',age:18,extra:18}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 19', () => { const d = pickFields({name:'n',age:19,extra:19}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 20', () => { const d = pickFields({name:'n',age:20,extra:20}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 21', () => { const d = pickFields({name:'n',age:21,extra:21}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 22', () => { const d = pickFields({name:'n',age:22,extra:22}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 23', () => { const d = pickFields({name:'n',age:23,extra:23}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 24', () => { const d = pickFields({name:'n',age:24,extra:24}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 25', () => { const d = pickFields({name:'n',age:25,extra:25}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 26', () => { const d = pickFields({name:'n',age:26,extra:26}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 27', () => { const d = pickFields({name:'n',age:27,extra:27}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 28', () => { const d = pickFields({name:'n',age:28,extra:28}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 29', () => { const d = pickFields({name:'n',age:29,extra:29}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 30', () => { const d = pickFields({name:'n',age:30,extra:30}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 31', () => { const d = pickFields({name:'n',age:31,extra:31}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 32', () => { const d = pickFields({name:'n',age:32,extra:32}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 33', () => { const d = pickFields({name:'n',age:33,extra:33}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 34', () => { const d = pickFields({name:'n',age:34,extra:34}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 35', () => { const d = pickFields({name:'n',age:35,extra:35}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 36', () => { const d = pickFields({name:'n',age:36,extra:36}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 37', () => { const d = pickFields({name:'n',age:37,extra:37}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 38', () => { const d = pickFields({name:'n',age:38,extra:38}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 39', () => { const d = pickFields({name:'n',age:39,extra:39}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 40', () => { const d = pickFields({name:'n',age:40,extra:40}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 41', () => { const d = pickFields({name:'n',age:41,extra:41}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 42', () => { const d = pickFields({name:'n',age:42,extra:42}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 43', () => { const d = pickFields({name:'n',age:43,extra:43}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 44', () => { const d = pickFields({name:'n',age:44,extra:44}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 45', () => { const d = pickFields({name:'n',age:45,extra:45}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 46', () => { const d = pickFields({name:'n',age:46,extra:46}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 47', () => { const d = pickFields({name:'n',age:47,extra:47}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 48', () => { const d = pickFields({name:'n',age:48,extra:48}, basicSchema); expect('extra' in d).toBe(false); });
    it('pick 49', () => { const d = pickFields({name:'n',age:49,extra:49}, basicSchema); expect('extra' in d).toBe(false); });
  });
});
