{
  "name": "@ims/security-scanner",
  "version": "1.0.0",
  "description": "Security scanning: CVSS v3.1 scoring, patch management, vulnerability tracking",
  "main": "dist/index.js",
  "scripts": { "build": "tsc", "test": "jest" },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "transform": { "^.+\\.tsx?$": ["ts-jest", { "isolatedModules": true, "diagnostics": false }] }
  },
  "devDependencies": { "ts-jest": "*", "jest": "*", "@types/jest": "*", "typescript": "*" }
}
