import TemplatesClient from './client';

export const metadata = {
  title: 'Template Library — Nexara IMS',
  description: 'Browse and use ISO-compliant document templates across all IMS modules',
};

export default function TemplatesPage() {
  return <TemplatesClient />;
}
